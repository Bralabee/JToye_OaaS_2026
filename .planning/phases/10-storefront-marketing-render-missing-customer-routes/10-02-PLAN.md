---
phase: 10-storefront-marketing-render-missing-customer-routes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/types/storefront.ts
  - frontend/app/shop/[slug]/page.tsx
  - frontend/__tests__/shop/cart.test.tsx
autonomous: true
requirements: [STFR-03, STFR-04]
must_haves:
  truths:
    - "Shop detail page fetches /public/shops/{slug}/promotions and /public/shops/{slug}/announcements in the existing Promise.all block"
    - "Shop detail page renders the announcement banner above the menu using data from /announcements (not /config)"
    - "Product cards render a destructive-variant Badge showing '{n}% off' or '£X off' when an active promotion matches the product's category"
    - "The per-product promotion lookup is memoised as a Map<string, PublicPromotion> via useMemo — no .find() inside ProductCard"
    - "frontend/__tests__/shop/cart.test.tsx covers empty-cart and populated-cart states"
    - "npm --prefix frontend test -- --testPathPattern cart passes"
    - "Shop detail page still builds and renders (npm --prefix frontend run build succeeds OR dev-server renders the seed shop without console errors)"
  artifacts:
    - path: "frontend/types/storefront.ts"
      provides: "PublicPromotion + PublicAnnouncement TypeScript interfaces"
      contains: "export interface PublicPromotion"
    - path: "frontend/app/shop/[slug]/page.tsx"
      provides: "Announcement banner + discount badge render driven by dedicated public endpoints"
      contains: "promotionsByCategory"
    - path: "frontend/__tests__/shop/cart.test.tsx"
      provides: "Jest coverage for the standalone /shop/[slug]/cart page (empty + populated)"
      contains: "renders empty state when no items"
  key_links:
    - from: "frontend/app/shop/[slug]/page.tsx"
      to: "/public/shops/{slug}/promotions and /announcements"
      via: "publicApiClient.get in the existing Promise.all useEffect"
      pattern: "publicApiClient\\.get.*promotions|publicApiClient\\.get.*announcements"
    - from: "frontend/app/shop/[slug]/page.tsx ProductCard"
      to: "shadcn Badge component"
      via: "promotionsByCategory Map lookup by product.category"
      pattern: "promotionsByCategory\\.get"
---

<objective>
Complete the STFR-03 wiring: swap the shop detail page from the legacy
`/public/shops/{slug}/config` bundled response to the dedicated `/promotions`
and `/announcements` endpoints (shipped in phase 3), add per-product discount
badges that don't currently exist, and memoise the category→promotion lookup
so product cards stay O(P+N). Then close the STFR-04 test gap by adding Jest
coverage for the already-existing standalone cart page.

Purpose: The vendor marketing backend is wired end-to-end but the storefront
hasn't been rendering dedicated promo data and the cart page has no test
safety net. Both are Wave 0 gaps in 10-RESEARCH.md §Validation Architecture.

Output:
- PublicPromotion + PublicAnnouncement types in `types/storefront.ts`
- Shop detail page using dedicated endpoints + memoised lookup + badge overlay
- New Jest file covering empty and populated cart states
- No net-new routes, no new components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/10-storefront-marketing-render-missing-customer-routes/10-RESEARCH.md

@frontend/app/shop/[slug]/page.tsx
@frontend/app/shop/[slug]/cart/page.tsx
@frontend/app/shop/[slug]/layout.tsx
@frontend/components/storefront/cart-provider.tsx
@frontend/components/ui/badge.tsx
@frontend/lib/public-api-client.ts
@frontend/types/storefront.ts

<interfaces>
<!-- Derived from 10-RESEARCH.md Code Example 1 (lines 306-375) -->

TypeScript types to add to frontend/types/storefront.ts:

  export interface PublicPromotion {
    label: string
    discountType: "PERCENTAGE" | "FLAT_AMOUNT"
    discountPercent: number | null
    discountAmountPennies: number | null
    category: string | null
    validUntil: string
  }

  export interface PublicAnnouncement {
    title: string
    body: string | null
    validUntil: string | null
  }

Existing Promise.all shape (frontend/app/shop/[slug]/page.tsx:213-241):
  Loads shop, products, reviews, (currently) config — extend to add
  promotions + announcements via:
    publicApiClient.get<PublicPromotion[]>(`/public/shops/${slug}/promotions`)
      .catch(() => ({ data: [] }))
    publicApiClient.get<PublicAnnouncement[]>(`/public/shops/${slug}/announcements`)
      .catch(() => ({ data: [] }))

CartProvider contract (frontend/components/storefront/cart-provider.tsx:32-67):
  localStorage key: `jtoye-cart-${shopSlug}`
  Shape: { shopSlug: string, items: Array<{ productId, title,
           pricePennies, quantity, imageUrl: string|null, category: string }> }
  Usage in tests: seed localStorage BEFORE rendering the provider.

Cart page signature (frontend/app/shop/[slug]/cart/page.tsx):
  Uses Next.js 16 params Promise: params: Promise<{ slug: string }>
  In tests: wrap in Promise.resolve({ slug }) (research line 414).

Badge component (frontend/components/ui/badge.tsx):
  Variants: default, secondary, destructive, outline — use "destructive" for
  discount overlay per research Pattern 4.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PublicPromotion + PublicAnnouncement types</name>
  <files>frontend/types/storefront.ts</files>
  <behavior>
    - `PublicPromotion` exported with fields: label, discountType (union
      "PERCENTAGE" | "FLAT_AMOUNT"), discountPercent (number|null),
      discountAmountPennies (number|null), category (string|null),
      validUntil (string).
    - `PublicAnnouncement` exported with fields: title, body (string|null),
      validUntil (string|null).
    - `npm --prefix frontend run lint` (or `tsc --noEmit`) passes.
  </behavior>
  <action>
    Open frontend/types/storefront.ts. Add the two interfaces shown in the
    `<interfaces>` block above at the bottom of the file, following the
    export style already used there (check existing interfaces like
    PublicShop, PublicProduct for the house style — prefer `export interface`
    over `export type`).

    Do not remove any existing types. The legacy `ShopConfig` type (if
    present) stays — the shop page still consumes `featuredProducts` from
    /config per research Open Question 1 / Anti-Patterns line 222.
  </action>
  <verify>
    <automated>cd /home/sanmi/IdeaProjects/JToye_OaaS_2026/frontend &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>
    Both interfaces exist and type-check; no other type file touched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire dedicated endpoints + memoised lookup + discount badge into shop detail page</name>
  <files>frontend/app/shop/[slug]/page.tsx</files>
  <behavior>
    - Parallel fetch adds two new calls (`/promotions`, `/announcements`)
      to the existing Promise.all with `.catch(() => ({ data: [] }))` so a
      single failing endpoint does not break the page (research Pattern 2,
      line 173).
    - New state: `promotions: PublicPromotion[]`,
      `announcements: PublicAnnouncement[]`.
    - `promotionsByCategory` is a `useMemo`-wrapped `Map<string, PublicPromotion>`
      keyed on `promotion.category` (skip nulls). Recomputed only when
      `promotions` changes.
    - The announcement banner above the menu reads from the new
      `announcements` state (first active announcement) instead of
      `shopConfig.announcements`. If the array is empty, render nothing.
    - ProductCard receives `promo` as a prop (or consumes the map via a
      light-weight lookup) and overlays a `<Badge variant="destructive">`
      when `promo` is defined. Text is `{n}% off` for PERCENTAGE or
      `£{(amountPennies/100).toFixed(2)} off` for FLAT_AMOUNT.
    - No `.find()` inside ProductCard (O(P*N) forbidden per research
      Pattern 3, line 197).
  </behavior>
  <action>
    Read frontend/app/shop/[slug]/page.tsx in full before editing. Identify:
      - The Promise.all block (~line 217 per research).
      - The ProductCard component definition within the file.
      - The current announcement banner render (~line 421-443 per research).
      - The product image container with className="relative w-24 sm:w-28 flex-shrink-0"
        (~line 168-184 per research) — this is where the badge overlays.

    Edits in order:

    1. Imports:
       - Add `useMemo` to the existing `react` import if not present.
       - Add `import { Badge } from "@/components/ui/badge"`.
       - Add `import type { PublicPromotion, PublicAnnouncement } from "@/types/storefront"`.

    2. State additions — alongside the existing useState calls:
         const [promotions, setPromotions] = useState<PublicPromotion[]>([])
         const [announcements, setAnnouncements] = useState<PublicAnnouncement[]>([])

    3. Extend the existing Promise.all — do NOT replace it, ADD two entries:
         publicApiClient.get<PublicPromotion[]>(`/public/shops/${slug}/promotions`).catch(() => ({ data: [] })),
         publicApiClient.get<PublicAnnouncement[]>(`/public/shops/${slug}/announcements`).catch(() => ({ data: [] })),
       Then destructure and call setPromotions / setAnnouncements.
       Do NOT change the path prefix — paths are bare `/public/**`, NOT
       `/api/v1/public/**` (Pitfall 1, WebConfig.java:23-33 excludes storefront
       from the /api/v1 prefix). Copy the bare shape from the existing
       `/public/shops/${slug}` call right above.
       Keep the existing `/config` fetch intact — it still powers
       `shopConfig.featuredProducts` per research Anti-Patterns line 222.

    4. Memoise the lookup — place near the top of the render body:
         const promotionsByCategory = useMemo(() => {
           const map = new Map<string, PublicPromotion>()
           for (const p of promotions) {
             if (p.category) map.set(p.category, p)
           }
           return map
         }, [promotions])

    5. Swap the announcement banner render: find the existing block around
       lines 421-443 that currently reads `shopConfig?.announcements` and
       change it to read from the new `announcements` state. Take the first
       element; if absent, render nothing. Preserve the existing banner
       markup/classes (`bg-amber-50` / `bg-blue-50` etc. — research line 453
       references them for the Playwright selector in plan 10-03).

       SECURITY: React auto-escapes. Do NOT introduce
       `dangerouslySetInnerHTML` (research Security Domain XSS row).

    6. Pass `promo` into ProductCard where it is used in the menu loop:
         <ProductCard
           key={product.id}
           product={product}
           promo={product.category ? promotionsByCategory.get(product.category) : undefined}
         />
       Add `promo?: PublicPromotion` to ProductCard's props type.

    7. Inside ProductCard, add the badge overlay inside the existing
       `relative w-24 sm:w-28 flex-shrink-0` image container, AFTER the
       SafeImage element:
         {promo && (
           <Badge
             variant="destructive"
             className="absolute top-1.5 left-1.5 text-[10px] px-1.5 py-0 shadow-md"
           >
             {promo.discountType === "PERCENTAGE"
               ? `${promo.discountPercent}% off`
               : `£${((promo.discountAmountPennies ?? 0) / 100).toFixed(2)} off`}
           </Badge>
         )}

    DO NOT convert this file to a Server Component — entire file is
    `"use client"` (research Anti-Patterns line 220); the children need
    `CartProvider` from the layout.
    DO NOT touch the `params` unwrapping pattern (`const { slug } = use(params)`
    at line 201-202 per research Pitfall 3 — already correct).
  </action>
  <verify>
    <automated>cd /home/sanmi/IdeaProjects/JToye_OaaS_2026/frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <done>
    File type-checks and lints. Manual sanity: grep shows `promotionsByCategory`
    exactly once (the useMemo), `publicApiClient.get` now references both
    `/promotions` and `/announcements`, and the ProductCard body contains
    `variant="destructive"` Badge. No `promotions.find(` calls anywhere.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Jest coverage for the standalone cart page (STFR-04)</name>
  <files>frontend/__tests__/shop/cart.test.tsx</files>
  <behavior>
    - Test 1 "renders empty state when no items": localStorage is clean;
      render <CartProvider shopSlug="jollof-express"><CartPage
      params={Promise.resolve({slug:"jollof-express"})} /></CartProvider>.
      Assert an empty-state label exists (whatever string the actual
      page uses; use `screen.findByText` with a permissive regex or read
      the real file first to get the exact copy). Assert a "back to menu"
      style link points to `/shop/jollof-express`.
    - Test 2 "renders items when cart has contents": pre-seed
      localStorage.setItem(`jtoye-cart-jollof-express`, JSON.stringify({
        shopSlug: "jollof-express",
        items: [{ productId: "p1", title: "Jollof Rice",
                  pricePennies: 899, quantity: 2, imageUrl: null,
                  category: "Mains" }],
      })) BEFORE rendering. Assert "Jollof Rice" visible and a "checkout"
      style link exists.
    - afterEach clears localStorage.
    - `npm --prefix frontend test -- --testPathPattern cart` passes.
  </behavior>
  <action>
    Step 1 — Read frontend/app/shop/[slug]/cart/page.tsx in full BEFORE
    writing the test. The research snippet (Example 2, lines 377-414) uses
    placeholder strings ("Your basket is empty", "Back to menu", "Proceed
    to checkout") but the real page may use different copy. Use the actual
    strings from the page so the tests don't false-fail on text mismatch.

    Step 2 — Create frontend/__tests__/shop/cart.test.tsx. Base structure
    from research Example 2, with these required adjustments:
      - Import CartPage as default from `@/app/shop/[slug]/cart/page`.
      - Import CartProvider from `@/components/storefront/cart-provider`.
      - Wrap CartPage in CartProvider with the matching shopSlug.
      - Pass params as `Promise.resolve({ slug })` (Next.js 16 contract).
      - Seed localStorage BEFORE render for the populated case — mirrors
        CartProvider's useEffect-based hydration (Pitfall 4, research
        lines 277-281 — hydration happens in useEffect, so set the key
        before the component mounts).
      - Use `await screen.findByText(...)` so the hydration useEffect has
        a tick to run.
      - afterEach: `localStorage.clear()`.

    Step 3 — If the cart page pulls shop data (e.g. shop name) via
    publicApiClient, mock `@/lib/public-api-client` using Jest's
    `jest.mock` at the top of the test file, returning a minimal
    `{ get: jest.fn().mockResolvedValue({ data: {...} }) }`. Read the
    actual cart page imports first to decide whether this mock is needed
    — skip it if the page only reads from context + localStorage.

    Step 4 — No edits to the cart page itself. The existing implementation
    stays; this is a pure test addition.
  </action>
  <verify>
    <automated>cd /home/sanmi/IdeaProjects/JToye_OaaS_2026/frontend &amp;&amp; npm test -- --testPathPattern=cart</automated>
  </verify>
  <done>
    New test file exists at frontend/__tests__/shop/cart.test.tsx with both
    tests passing. No production source under frontend/app/ or frontend/lib/
    is modified.
  </done>
</task>

<task type="auto">
  <name>Task 4: Full frontend unit suite + atomic commits</name>
  <files>frontend/types/storefront.ts, frontend/app/shop/[slug]/page.tsx, frontend/__tests__/shop/cart.test.tsx</files>
  <action>
    Run full frontend unit tests to confirm no regression (baseline is
    69 frontend tests from M2, should be 71+ after this plan).

    Create two atomic commits on the same feature branch used by plan 10-01
    (or the current feature branch if 10-01 ran first in a different agent):

      1. feat(stfr): render promotions + announcements via dedicated
         public endpoints with memoised lookup and discount badges (STFR-03)
         — stages frontend/types/storefront.ts and frontend/app/shop/[slug]/page.tsx

      2. test(stfr): add Jest coverage for standalone cart page (STFR-04)
         — stages frontend/__tests__/shop/cart.test.tsx

    Do NOT update REQUIREMENTS.md Traceability here — plan 10-03 task 6
    owns the phase-closing narrative commit.
  </action>
  <verify>
    <automated>cd /home/sanmi/IdeaProjects/JToye_OaaS_2026/frontend &amp;&amp; npm test -- --watchAll=false</automated>
  </verify>
  <done>
    Full frontend unit suite green, two commits exist on the feature branch,
    only the three files listed above were touched.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean in frontend/
- `npm run lint` clean in frontend/
- `npm test -- --testPathPattern=cart` passes both new cases
- `npm test -- --watchAll=false` full unit suite green, count up by at least 2
- Grep confirms: `promotionsByCategory` exactly once in page.tsx (the useMemo);
  both `/promotions` and `/announcements` paths present; no `promotions.find(`
  inside ProductCard; no `dangerouslySetInnerHTML` anywhere in the diff
</verification>

<success_criteria>
STFR-03 render-path is wired through the dedicated endpoints with a
memoised lookup and real discount badges on product cards. STFR-04 cart
page has Jest coverage for both empty and populated states. No new
components, no new routes, no new backend.
</success_criteria>

<output>
After completion, create
`.planning/phases/10-storefront-marketing-render-missing-customer-routes/10-02-SUMMARY.md`
</output>
