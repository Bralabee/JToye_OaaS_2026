---
phase: 20-ai-1-mcp-server-read-only-slice
plan: 04
type: execute
wave: 3
depends_on: [20-01, 20-02, 20-03]
files_modified:
  - scripts/docs-freshness.sh
  - docs/metrics.json
  - CLAUDE.md
  - mcp-server/scripts/e2e.sh
  - mcp-server/scripts/e2e-rls.sh
autonomous: true
requirements: [AI-1]
must_haves:
  truths:
    - "docs-freshness counts a NEW mcp test family (path-anchored to mcp-server/) so vitest blocks register and the gate stays green above the 1208 baseline"
    - "docs/metrics.json is regenerated via --write and its total_logical_invocations includes the MCP tests"
    - "e2e.sh mints the integration-catalog-ro client-credentials token and proves list_products returns tenant-A rows through the MCP tool"
    - "e2e-rls.sh mints two password-grant tokens (tenant-a-user, tenant-b-user) and proves disjoint results through the MCP tool"
  artifacts:
    - path: "scripts/docs-freshness.sh"
      provides: "MCP_TEST_BLOCKS/MCP_TEST_FILES family folded into TOTAL + emitted JSON keys"
      contains: "mcp-server"
    - path: "docs/metrics.json"
      provides: "regenerated manifest with mcp_test_blocks/mcp_test_files keys + bumped total"
      contains: "mcp_test_blocks"
    - path: "mcp-server/scripts/e2e.sh"
      provides: "live read happy-path: token mint → MCP list_products → assert 200 + rows"
      min_lines: 25
    - path: "mcp-server/scripts/e2e-rls.sh"
      provides: "live cross-tenant RLS proof through the MCP tool (disjoint non-empty sets)"
      min_lines: 30
  key_links:
    - from: "scripts/docs-freshness.sh"
      to: "docs/metrics.json"
      via: "--write regenerates the manifest with the mcp family"
      pattern: "mcp_test_blocks"
    - from: "mcp-server/scripts/e2e-rls.sh"
      to: "MCP server POST /mcp (list_products)"
      via: "two tenant-scoped tokens asserted through the tool"
      pattern: "list_products"
---

<objective>
Make MCP test coverage visible to the CI gate and script the two live proofs. First, extend `scripts/docs-freshness.sh` with a NEW `mcp` test family (path-anchored to `mcp-server/`) and regenerate `docs/metrics.json` via `--write` so vitest blocks count toward `total_logical_invocations` (otherwise MCP tests are invisible and the "all new code requires tests" standard is unenforced — RESEARCH Pitfall 5). Second, write the two live-E2E scripts (`e2e.sh` read happy-path, `e2e-rls.sh` cross-tenant RLS proof) that the final wave runs against the rebuilt, realm-re-imported dev stack.

Purpose: The gate must enforce MCP coverage going forward, and the load-bearing RLS proof must be a repeatable script that exercises the MCP tool (not a raw core curl). Writing the scripts is autonomous code; running them live is the 20-05 operational gate.
Output: `docs-freshness.sh` + regenerated `metrics.json` + `CLAUDE.md` prose; `mcp-server/scripts/e2e.sh` + `e2e-rls.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/20-ai-1-mcp-server-read-only-slice/20-RESEARCH.md
@.planning/phases/20-ai-1-mcp-server-read-only-slice/20-PATTERNS.md
@.planning/phases/20-ai-1-mcp-server-read-only-slice/20-VALIDATION.md
@scripts/docs-freshness.sh
@scripts/verify-env.sh

<interfaces>
<!-- Facts this plan depends on (from 20-01/02/03 + existing repo). -->
- MCP tests live under mcp-server/src/**/*.test.ts, vitest, use it()/test() blocks (matched by the existing \b(it|test)\( content-regex).
- docs-freshness.sh helpers: count_occurrences '<path-regex>' '<content-regex>' and count_files_with; TOTAL = JAVA_TEST_METHODS + JEST_BLOCKS + GO_TEST_FUNCS + PLAYWRIGHT_BLOCKS (baseline 1208); --write emits COMPUTED JSON to docs/metrics.json. Clone the PLAYWRIGHT_* block.
- docs/metrics.json current keys incl. total_logical_invocations: 1208, schema_version: 50.
- MCP HTTP surface: POST http://localhost:9100/mcp with Authorization: Bearer <token>, body = a JSON-RPC tools/call for list_products (stateless Streamable HTTP). GET http://localhost:9100/health → 200.
- Token mint (host, split-horizon iss=localhost:8085):
    happy-path (client-credentials, tenant A): POST http://localhost:8085/realms/jtoye-dev/protocol/openid-connect/token, grant_type=client_credentials, client_id=integration-catalog-ro, client_secret=$INTEGRATION_CATALOG_RO_SECRET.
    RLS proof (password grant, core-api directAccessGrantsEnabled): grant_type=password, client_id=core-api, username=tenant-a-user | tenant-b-user, password per realm (dev creds from .env).
- Tenant B seeded probe (from 20-03): product SKU "TENANTB-PROBE-1" (tenant …0002); tenant A has the DemoDataSeeder catalogue (…0001).
- verify-env.sh idioms: curl -s -o /dev/null -w "%{http_code}" + pass/fail helpers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the mcp docs-freshness family + regenerate metrics + CLAUDE.md prose</name>
  <files>scripts/docs-freshness.sh, docs/metrics.json, CLAUDE.md</files>
  <read_first>
    - scripts/docs-freshness.sh (the PLAYWRIGHT_* family block + TOTAL line + COMPUTED JSON heredoc — clone the pattern)
    - docs/metrics.json (current keys + baseline 1208)
    - 20-RESEARCH.md § "Test Surface + docs-freshness reconciliation (detail)" (the exact MCP_TEST_BLOCKS/MCP_TEST_FILES lines + path regex ^mcp-server/(src|test)/.*\.(test|spec)\.ts$ + new-family decision) and § "Common Pitfalls" Pitfall 5
    - 20-PATTERNS.md § "docs-freshness CI gate (new test family)"
    - CLAUDE.md § Testing constraint (the "1208 logical invocations" prose that names the four families)
  </read_first>
  <action>
    In `scripts/docs-freshness.sh`, after the PLAYWRIGHT_* block add `MCP_TEST_BLOCKS=$(count_occurrences '^mcp-server/(src|test)/.*\.(test|spec)\.ts$' '\b(it|test)\(')` and `MCP_TEST_FILES=$(count_files_with '^mcp-server/(src|test)/.*\.(test|spec)\.ts$' '\b(it|test)\(')`. Fold MCP_TEST_BLOCKS into `TOTAL` (add the term). Add `"mcp_test_blocks": ${MCP_TEST_BLOCKS},` and `"mcp_test_files": ${MCP_TEST_FILES},` to the COMPUTED JSON heredoc. The content-regex `\b(it|test)\(` already matches vitest blocks, so only the path regex + keys are new. Then run `bash scripts/docs-freshness.sh --write` to regenerate `docs/metrics.json` (bumps total_logical_invocations above 1208 by the count of MCP it/test blocks; schema_version stays 50). Update the CLAUDE.md testing-standard prose to name the new MCP family and the new total (mirror how it currently enumerates Java @Test / Jest / Go / Playwright counts) so the docs-freshness gate stays green.
  </action>
  <verify>
    <automated>bash scripts/docs-freshness.sh --write && bash scripts/docs-freshness.sh && echo DOCS_FRESH_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/docs-freshness.sh` contains the `^mcp-server/(src|test)/.*\.(test|spec)\.ts$` path regex and folds MCP_TEST_BLOCKS into TOTAL.
    - `docs/metrics.json` contains `mcp_test_blocks` + `mcp_test_files` keys and `total_logical_invocations` > 1208; `schema_version` stays 50.
    - `bash scripts/docs-freshness.sh` (no --write) exits 0 (gate green — no drift).
    - CLAUDE.md testing prose names the MCP family and the new total.
  </acceptance_criteria>
  <done>MCP vitest tests are counted by the docs-freshness gate; metrics.json regenerated; gate green.</done>
</task>

<task type="auto">
  <name>Task 2: e2e.sh — live read happy-path (integration-catalog-ro → list_products → rows)</name>
  <files>mcp-server/scripts/e2e.sh</files>
  <read_first>
    - scripts/verify-env.sh (curl status-assert idiom curl -s -o /dev/null -w "%{http_code}" + pass/fail colored helpers + REQUIRED_VARS incl. INTEGRATION_CATALOG_RO_SECRET)
    - 20-PATTERNS.md § "mcp-server/scripts/e2e.sh + e2e-rls.sh" (token mint from host :8085, split-horizon) 
    - 20-RESEARCH.md § "Common Pitfalls" Pitfall 1 (mint from host :8085 → iss=localhost:8085 which core accepts) + Pitfall 2 (realm must be re-imported first) + § "Validation Architecture / Phase Requirements → Test Map" (read happy-path row)
    - 20-VALIDATION.md § "Per-Requirement Verification Map" (read happy-path)
  </read_first>
  <action>
    Write `mcp-server/scripts/e2e.sh` (bash, `set -euo pipefail`, mirror verify-env.sh pass/fail helpers). Steps: (1) preflight — assert `INTEGRATION_CATALOG_RO_SECRET` is set and GET `http://localhost:9100/health` returns 200 (fail fast with a clear message pointing at "rebuild all + re-import realm" if not). (2) Mint a client-credentials token from the HOST endpoint `http://localhost:8085/realms/jtoye-dev/protocol/openid-connect/token` (grant_type=client_credentials, client_id=integration-catalog-ro, client_secret=$INTEGRATION_CATALOG_RO_SECRET); assert a token came back (fail with the "invalid_client → realm not re-imported" hint if not). (3) Call the MCP server: POST `http://localhost:9100/mcp` with `Authorization: Bearer <token>`, `Accept: application/json, text/event-stream`, and a JSON-RPC `tools/call` body for `list_products`; assert HTTP 200 and that the tool result content is non-empty (tenant-A rows) and is NOT an isError. Print PASS/FAIL summary and exit non-zero on any failure. Never echo the token or full response body (log status + a row count only — PII).
  </action>
  <verify>
    <automated>bash -n mcp-server/scripts/e2e.sh && echo E2E_SYNTAX_OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n mcp-server/scripts/e2e.sh` passes (syntax valid); script is executable-ready with `set -euo pipefail`.
    - Script mints from `http://localhost:8085/.../token` with client_id=integration-catalog-ro (grep both).
    - Script asserts the MCP `list_products` call returns 200 + non-empty non-error content.
    - Script never echoes the token or the raw response body (PII guard) — greps for the token variable in an echo/log return nothing.
    - (Live run is executed in 20-05 against the rebuilt stack — this task only authors the script.)
  </acceptance_criteria>
  <done>e2e.sh scripts the read happy-path with the locked reference credential, gated on health + realm re-import preconditions.</done>
</task>

<task type="auto">
  <name>Task 3: e2e-rls.sh — live cross-tenant RLS proof through the MCP tool</name>
  <files>mcp-server/scripts/e2e-rls.sh</files>
  <read_first>
    - mcp-server/scripts/e2e.sh (Task 2 — reuse its token-mint + MCP-call helpers)
    - 20-VALIDATION.md § "Cross-tenant RLS proof design" (two password-grant tokens; disjoint seeded data; assert THROUGH the MCP tool; superuser Testcontainers cannot prove RLS)
    - 20-RESEARCH.md § "Open Questions" #1 (password grant against core-api with tenant-a-user/tenant-b-user, directAccessGrantsEnabled) + #2 (tenant-B probe product) + § "Common Pitfalls" Pitfall 3 (audience) + Pitfall 4 (superuser)
  </read_first>
  <action>
    Write `mcp-server/scripts/e2e-rls.sh` (bash, `set -euo pipefail`, verify-env helpers). Steps: (1) Mint TWO tokens via the direct-access PASSWORD grant against `client_id=core-api` at the host token endpoint: token A (username=tenant-a-user), token B (username=tenant-b-user) — passwords sourced from env/.env, never literals. Assert both minted (audience note: core-api carries the audience mapper so tokens pass the #88 gate). (2) Through the MCP tool (POST http://localhost:9100/mcp, Bearer = each token, `list_products` JSON-RPC call): capture A's product ids and B's product ids. (3) Assert the disjoint, bidirectional, non-empty proof: A's result CONTAINS the DemoDataSeeder tenant-A products and does NOT contain the tenant-B probe SKU "TENANTB-PROBE-1"; B's result CONTAINS "TENANTB-PROBE-1" and does NOT contain A's products. (4) Also assert an order-scope negative: with token A, a `read_orders` orderId that belongs to tenant B resolves to 404/empty (never B's row) — if a concrete B order id is not available, assert token A's order list does not contain a tenant-B-only marker. Print PASS/FAIL; exit non-zero on any leak. Compare row IDENTIFIERS only; never echo tokens or full PII bodies.
  </action>
  <verify>
    <automated>bash -n mcp-server/scripts/e2e-rls.sh && echo RLS_SYNTAX_OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n mcp-server/scripts/e2e-rls.sh` passes; `set -euo pipefail`.
    - Script mints two password-grant tokens (tenant-a-user + tenant-b-user) against client_id=core-api (grep both usernames + grant_type=password).
    - Script asserts disjoint non-empty sets THROUGH the MCP list_products tool: A contains A-products & NOT "TENANTB-PROBE-1"; B contains "TENANTB-PROBE-1" & NOT A-products (traces VALIDATION row "cross-tenant RLS proof").
    - Script asserts a cross-tenant order read resolves to 404/empty for the other tenant.
    - No token or PII body is echoed; comparisons are on identifiers only.
    - (Live run happens in 20-05 against the rebuilt, realm-re-imported stack.)
  </acceptance_criteria>
  <done>e2e-rls.sh proves RLS isolation at the HTTP boundary through the MCP tool with two genuinely tenant-scoped tokens against disjoint seeded data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E harness → Keycloak (host :8085) | Token mint; iss=localhost:8085 (split-horizon), core accepts |
| E2E harness → MCP server (:9100) | Bearer presented; proof exercises the tool, not raw core |
| two tenant tokens → core RLS | The isolation assertion resolves here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | e2e scripts logging | mitigate | Scripts log status + row counts/ids only; never echo tokens or PII response bodies. |
| T-20-02 | Spoofing | new/existing machine client audience | mitigate | Password grant uses core-api (carries the audience mapper); #88 gate rejects any token lacking aud=core-api — asserted implicitly by a 200. |
| T-20-03 | Elevation of Privilege / Info Disclosure | cross-tenant read | mitigate | e2e-rls.sh proves disjoint non-empty sets through the MCP tool; superuser Testcontainers explicitly not used (Pitfall 4). |
| T-20-08 | Tampering (silent under-count) | docs-freshness invisibility | mitigate | New mcp family folds MCP vitest blocks into TOTAL; --write reconciles metrics.json so the gate enforces coverage. |
</threat_model>

<verification>
- `bash scripts/docs-freshness.sh` exits 0 after --write (gate green, mcp family counted).
- `bash -n mcp-server/scripts/e2e.sh` and `bash -n mcp-server/scripts/e2e-rls.sh` — both syntactically valid.
- `grep -q mcp_test_blocks docs/metrics.json`.
</verification>

<success_criteria>
- MCP vitest tests count toward total_logical_invocations; docs-freshness gate green above 1208; schema stays V50.
- e2e.sh scripts the read happy-path with integration-catalog-ro; e2e-rls.sh scripts the disjoint cross-tenant RLS proof through the MCP tool — both authored and syntax-valid, ready for the 20-05 live run.
</success_criteria>

<output>
Create `.planning/phases/20-ai-1-mcp-server-read-only-slice/20-04-SUMMARY.md` when done.
</output>
