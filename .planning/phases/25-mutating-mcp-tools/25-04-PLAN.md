---
phase: 25-mutating-mcp-tools
plan: 04
type: execute
wave: 3
depends_on: [25-01, 25-02, 25-03]
files_modified:
  - docs/metrics.json
  - docs/security-scopes.md
  - docs/idempotency.md
  - mcp-server/README.md
  - CLAUDE.md
  - AGENTS.md
  - docs/api/openapi-snapshot.json
autonomous: false
requirements: [AI-02]

must_haves:
  truths:
    - "docs/metrics.json reconciles the new counts (mcp_test_blocks/files for the 2 vitest files + the Java scope/RLS tests) and docs-freshness check-mode exits 0"
    - "docs/security-scopes.md, mcp-server/README.md, docs/idempotency.md read consistently with the shipped write surface (orders:write/customers:write enforced, integration-orders-rw client, write-tools mandate the key)"
    - "OpenApiSnapshotTest is green (snapshot regenerated if the OpenApiConfig scope-doc change diffed it)"
    - "LIVE on the rebuilt dev stack (rebuild ALL + realm re-import): create_customer + replay returns the identical DTO with no duplicate; create_order 200; a foreign-tenant shopId → 403/empty; a no-write-scope token → 403; and NO opaque-UUID GROUP_ADMIN row appears in shop_staff after the create_order (VSA-02 allowlist works)"
  artifacts:
    - path: "docs/metrics.json"
      provides: "reconciled logical-invocation counts (the --write arbiter)"
      contains: "mcp_test_blocks"
    - path: "mcp-server/README.md"
      provides: "auth-model note flipped to 'write allowed under the RW credential' + live write-E2E recipe"
      contains: "integration-orders-rw"
  key_links:
    - from: "scripts/docs-freshness.sh --write"
      to: "docs/metrics.json + CLAUDE.md/AGENTS.md prose"
      via: "the --write run is the arbiter of the counts"
      pattern: "docs-freshness"
    - from: "live rebuilt stack + kc.sh import --override true"
      to: "integration-orders-rw token → POST /mcp create tools"
      via: "manual/scripted E2E (Playwright not in CI, D-12)"
      pattern: "kc.sh import --override true"
---

<objective>
Close the phase: reconcile the docs/counts gate now that all new tests exist, then prove the whole flow LIVE on the rebuilt dev stack. Run `scripts/docs-freshness.sh --write` (the `--write` run is the arbiter) to fold the two new vitest files + the new Java scope/RLS tests into `docs/metrics.json`, update the `CLAUDE.md`/`AGENTS.md` "logical invocations" prose, and refresh the write-surface docs (`security-scopes.md`, `mcp-server/README.md` auth-model note, `idempotency.md`). If the `OpenApiConfig` scope-doc change (25-01) diffed the committed snapshot, regenerate it so `OpenApiSnapshotTest` stays green. Finally, the manual/scripted live E2E (D-12): rebuild ALL containers, `kc.sh import --override true`, mint an `integration-orders-rw` token, and prove create → idempotent-replay → cross-tenant 403 → no-scope 403 — and confirm no rogue `shop_staff` GROUP_ADMIN row landed (VSA-02).

Purpose: AC-3's "proven live against the dev stack" + the docs-freshness CI gate (the phase PR fails on count drift). This is the phase-gate closer — it depends on every prior plan's tests existing.
Output: Reconciled `docs/metrics.json` + prose, refreshed write-surface docs, a green snapshot, and a human-verified live E2E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-mutating-mcp-tools/25-CONTEXT.md
@.planning/phases/25-mutating-mcp-tools/25-RESEARCH.md
@.planning/phases/25-mutating-mcp-tools/25-PATTERNS.md

<interfaces>
<!-- Reconcile mechanics + the live recipe. -->

docs-freshness: scripts/docs-freshness.sh counts the mcp family via regex `^mcp-server/(src|test)/.*\.(test|spec)\.ts$` matching `\b(it|test)\(` → writes mcp_test_blocks/mcp_test_files (:62-82). Java @Test counts + schema also folded. `--write` is the arbiter; check-mode must then exit 0. Current baseline docs/metrics.json: mcp_test_blocks 27 / mcp_test_files 6 / java_test_methods 1128 / java_test_files 199 / total_logical_invocations 1648. Adding 2 vitest files + the Java scope + RLS tests bumps mcp + java counts.

Snapshot: docs/api/openapi-snapshot.json is oasdiff-gated (OpenApiSnapshotTest, check-mode inside integrationTest). Both create PATHS already exist; only the scope-doc/security metadata may shift. If OpenApiSnapshotTest is RED after 25-01's OpenApiConfig change, run `./gradlew :core-java:updateOpenApiSnapshot`.

Docs to refresh (prose only): docs/security-scopes.md (enforced-scope list + integration-orders-rw reference client + INTEGRATION_ORDERS_RW_SECRET wiring + write-E2E recipe; §6 MCP-feeds is the mandate this phase discharges), mcp-server/README.md (flip "write is denied 403" → "write allowed under the RW credential", add the write recipe), docs/idempotency.md (note the MCP write tools now MANDATE the key, D-05).

Live E2E recipe (D-12, README §5 inverted): rebuild ALL containers → `kc.sh import --override true` (start-dev --import-realm will NOT overwrite an existing jtoye-dev) → set INTEGRATION_ORDERS_RW_SECRET + ACCESS_MACHINE_CLIENT_IDS in .env → mint integration-orders-rw client-credentials token → POST /mcp create_customer with an idempotencyKey (200 + DTO) → replay same key (identical DTO, no dup) → create_order referencing a discovered shop/product (200) → a no-write-scope token (integration-catalog-ro) → 403 sanitized isError → a tenant-A token targeting a tenant-B shop → empty/403 (RLS). Ports: Keycloak :8085, core :9090, MCP :9100. Login admin-user / JtoyeDev!2026.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile docs/metrics.json + write-surface docs + OpenAPI snapshot</name>
  <files>docs/metrics.json, docs/security-scopes.md, docs/idempotency.md, mcp-server/README.md, CLAUDE.md, AGENTS.md, docs/api/openapi-snapshot.json</files>
  <read_first>
    - scripts/docs-freshness.sh (:62-82 the mcp + java count logic; --write arbiter)
    - docs/metrics.json (current baseline counts)
    - docs/security-scopes.md (the scope taxonomy + integration-catalog-ro reference-client section to extend)
    - mcp-server/README.md (auth-model section — the "write is denied 403" note to flip)
    - docs/idempotency.md (the AC-1 coverage table — note the MCP mandate)
    - 25-PATTERNS.md "Docs reconcile" ; 25-RESEARCH.md Runtime State Inventory (snapshot regen)
  </read_first>
  <action>Run `scripts/docs-freshness.sh --write` to fold the new counts into `docs/metrics.json` (the 2 new vitest files bump `mcp_test_blocks`/`mcp_test_files`; the new Java scope + RLS integration tests bump `java_test_methods`/`java_test_files`; `total_logical_invocations` updates). Then update the "1648 logical invocations" prose in `CLAUDE.md` and `AGENTS.md` to the new arbiter total, and re-run `scripts/docs-freshness.sh` in check-mode until it exits 0. Refresh prose: in `docs/security-scopes.md` add `orders:write` + `customers:write` to the enforced list, document the `integration-orders-rw` reference client + `INTEGRATION_ORDERS_RW_SECRET` wiring, and add the write-E2E recipe; in `mcp-server/README.md` flip the auth-model "write is denied 403" note to "write allowed under the RW credential" and append the write recipe; in `docs/idempotency.md` note the MCP write tools now MANDATE the `Idempotency-Key` (D-05). If `OpenApiSnapshotTest` is RED after 25-01's `OpenApiConfig` doc change, run `./gradlew :core-java:updateOpenApiSnapshot` and commit the regenerated `docs/api/openapi-snapshot.json`; if it is already green, leave the snapshot untouched.</action>
  <verify>
    <automated>scripts/docs-freshness.sh && ./gradlew :core-java:integrationTest --tests '*OpenApiSnapshotTest*'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/docs-freshness.sh` (check-mode) exits 0 (`docs-freshness OK: metrics match source`).
    - `docs/metrics.json` `mcp_test_blocks` >= 29 and `mcp_test_files` >= 8 (baseline 27/6 + the 2 new tool tests; core-client.test.ts is an existing file extended, not new) and `total_logical_invocations` matches the arbiter.
    - `CLAUDE.md` and `AGENTS.md` "logical invocations" prose equals the new `total_logical_invocations`.
    - `mcp-server/README.md` no longer states writes are denied 403; it documents the RW credential + write recipe.
    - `OpenApiSnapshotTest` GREEN (snapshot regenerated only if it diffed).
  </acceptance_criteria>
  <done>The docs-freshness gate is green with the new counts, the write-surface docs read consistently, and the OpenAPI snapshot is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live E2E on the rebuilt stack — create / idempotent-replay / cross-tenant 403 / no rogue shop_staff row</name>
  <files>(no source files — live runtime verification; produces the E2E evidence recorded in the SUMMARY)</files>
  <read_first>
    - mcp-server/README.md §5 live-E2E recipe (inverted to allow writes)
    - 25-CONTEXT.md:119-124 (ports/creds + the @Valid-before-@PreAuthorize + outbox notes)
    - 25-RESEARCH.md §3 Pitfall 1 (the no-rogue-shop_staff-row check) + Pitfall 5 (rebuild + re-import)
  </read_first>
  <what-built>The full write path is wired: the `orders:write`/`customers:write` `@PreAuthorize` gates (25-01), the `integration-orders-rw` realm client + secret + `ACCESS_MACHINE_CLIENT_IDS` allowlist (25-02), and the `create_order`/`create_customer` MCP tools (25-03). This checkpoint proves it live — Playwright/live-Keycloak is NOT in CI (D-12), so the cross-stack flow is a manual/scripted claim on the dev stack.</what-built>
  <action>Automate everything BEFORE pausing (automation-first — Claude drives, the human confirms the outcome): set `INTEGRATION_ORDERS_RW_SECRET` + `ACCESS_MACHINE_CLIENT_IDS=integration-orders-rw` in `.env` and run `scripts/verify-env.sh`; rebuild ALL containers (RULE 0 — compose XOR local k8s, compose is canonical) and re-import the realm with `kc.sh import --override true` inside the Keycloak container; mint an `integration-orders-rw` client-credentials token AND an `integration-catalog-ro` (no-write-scope) token; run the numbered probes in `<how-to-verify>` via curl against `POST /mcp` (:9100) plus a `psql` query against `shop_staff`; capture each status/body/row-count. Then PAUSE for the human to confirm the observed outcomes match the acceptance criteria. Never write full tokens or customer PII into the SUMMARY — record statuses + assertions only.</action>
  <how-to-verify>
    1. Set `INTEGRATION_ORDERS_RW_SECRET` and `ACCESS_MACHINE_CLIENT_IDS=integration-orders-rw` in `.env`; run `scripts/verify-env.sh` (must pass).
    2. Rebuild ALL containers (`scripts/start-dev.sh` or the documented rebuild), then re-import the realm: `kc.sh import --override true` inside the Keycloak container (start-dev --import-realm will NOT overwrite the existing jtoye-dev). Tokens minted before this are fail-closed.
    3. Mint an `integration-orders-rw` client-credentials token (Keycloak :8085); confirm the decoded token carries `scope` with `orders:write customers:write catalog:read`, `aud=core-api`, and a `tenant_id` claim.
    4. POST /mcp `create_customer` with an `idempotencyKey` → expect 200 + a `CustomerDto`. Replay the SAME key → expect the IDENTICAL DTO and NO duplicate row (check the customers table count).
    5. POST /mcp `create_order` referencing a shop+product discovered via the read tools (catalog:read) → expect 200 + an `OrderDto` (DRAFT).
    6. With a no-write-scope token (`integration-catalog-ro`), POST /mcp `create_order` → expect a sanitized `isError` mapping core's 403 (no stack, no PII).
    7. With a tenant-A token targeting a tenant-B shop's `shopId`, POST /mcp `create_order` → expect empty/403 (RLS).
    8. VSA-02 check: query `shop_staff` (`SELECT id, user_id, role, grant_source FROM shop_staff WHERE role='GROUP_ADMIN'`) — confirm NO new opaque-UUID JIT `GROUP_ADMIN` row was created for the RW SA after the successful `create_order` (the `ACCESS_MACHINE_CLIENT_IDS` allowlist worked).
  </how-to-verify>
  <verify>
    <human-check>Human confirms the six probe outcomes + the psql no-rogue-row result match the acceptance criteria (Playwright/live-Keycloak is not in CI — D-12).</human-check>
  </verify>
  <acceptance_criteria>
    - create_customer returns 200 + DTO; the same-key replay returns the byte-identical DTO with the customers row count unchanged (no duplicate).
    - create_order returns 200 + an OrderDto under the RW token.
    - A no-write-scope token yields a sanitized RFC 7807 tool error (403), never a raw stack/PII.
    - A tenant-A token targeting a tenant-B shopId yields empty/403 (RLS boundary holds).
    - `shop_staff` gains NO rogue opaque-UUID GROUP_ADMIN row for the RW service-account after create_order (VSA-02 allowlist proven live).
  </acceptance_criteria>
  <done>The live create → idempotent-replay → cross-tenant-403 → no-scope-403 flow passes on the rebuilt stack, with no rogue shop_staff row — human-approved.</done>
  <resume-signal>Type "approved" with the observed statuses (200 / replay-identical / 403 / 403 / no-rogue-row), or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-re-import token → core | A token minted before the realm re-import lacks the new scopes and must fail-closed (403 on writes) |
| live RW credential → runtime state | The RW SA's writes must not mutate shop_staff (VSA-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-13 | Elevation of Privilege | A stale/pre-re-import token retains or fabricates write access | mitigate | Fail-closed contract — pre-re-import tokens 403 on writes; the live E2E re-mints after `kc.sh import --override true` (D-12) and asserts the no-scope token is 403 |
| T-25-14 | Tampering / privilege accretion | RW SA accumulates a permanent JIT GROUP_ADMIN row in shop_staff | mitigate | `ACCESS_MACHINE_CLIENT_IDS=integration-orders-rw` (25-02); the live E2E step-8 asserts no rogue row (the no-rogue-shop_staff-row acceptance) |
| T-25-15 | Repudiation | Docs (metrics/OpenAPI/README) drift from the shipped surface → CI gate red / stale contract | mitigate | `docs-freshness.sh --write` arbiter + OpenApiSnapshot regen (Task 1) |
</threat_model>

<verification>
- `scripts/docs-freshness.sh` check-mode EXIT 0; `OpenApiSnapshotTest` GREEN.
- Live E2E human-verified: 200 / replay-identical-no-dup / 403 (no-scope) / 403-empty (cross-tenant) / no rogue shop_staff row.
- Full suite green before the phase PR: `cd mcp-server && npm test` + `./gradlew :core-java:test :core-java:integrationTest`.
</verification>

<success_criteria>
The docs-freshness + OpenAPI gates are green with the shipped write surface, and the full create → idempotent-replay → cross-tenant-403 → no-scope-403 flow is proven live on the rebuilt dev stack with no runtime-state pollution — discharging AC-3.
</success_criteria>

<output>
Create `.planning/phases/25-mutating-mcp-tools/25-04-SUMMARY.md` when done.
</output>
