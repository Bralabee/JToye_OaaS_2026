---
phase: 25-mutating-mcp-tools
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/keycloak/realm-export.template.json
  - docker-compose.full-stack.yml
  - infra/docker-compose.yml
  - infra/docker-compose.hostnet.yml
  - .env.example
  - infra/.env.example
  - scripts/verify-env.sh
autonomous: true
requirements: [AI-02]
user_setup:
  - service: keycloak
    why: "Realm re-import is the operational gate for the new integration-orders-rw client + scopes to exist in the running IdP (verified live at E2E, plan 25-04)"
    env_vars:
      - name: INTEGRATION_ORDERS_RW_SECRET
        source: "Local .env — a strong random secret for the new RW client-credentials client (verify-env.sh fails loud if unset)"

must_haves:
  truths:
    - "The realm template defines client integration-orders-rw carrying orders:write + customers:write + catalog:read, and pointedly NOT catalog:write (D-10)"
    - "integration-orders-rw carries BOTH cloned protocol mappers (tenant-id-mapper + core-api-audience-mapper) — the audience mapper is mandatory or #88 AudienceValidator 401s before any scope check"
    - "core-api default-grants orders:write + customers:write so operator/dashboard tokens transparently carry them (D-03)"
    - "customers:read + customers:write client scopes are defined in the realm (only customers:write enforced, D-02)"
    - "INTEGRATION_ORDERS_RW_SECRET is wired as an envsubst placeholder across 3 composes + 2 .env.example + verify-env.sh (never a committed literal, D-11)"
    - "ACCESS_MACHINE_CLIENT_IDS=integration-orders-rw is set on the core-java compose env so the RW SA does not JIT-provision a rogue shop_staff GROUP_ADMIN row (VSA-02 mitigation)"
  artifacts:
    - path: "infra/keycloak/realm-export.template.json"
      provides: "integration-orders-rw client + SA user + customers scopes + core-api default-grant"
      contains: "integration-orders-rw"
    - path: "scripts/verify-env.sh"
      provides: "fail-loud presence check for INTEGRATION_ORDERS_RW_SECRET"
      contains: "INTEGRATION_ORDERS_RW_SECRET"
    - path: "docker-compose.full-stack.yml"
      provides: "RW secret env + envsubst allowlist + ACCESS_MACHINE_CLIENT_IDS"
      contains: "INTEGRATION_ORDERS_RW_SECRET"
  key_links:
    - from: "integration-orders-rw client"
      to: "core-api-audience-mapper + tenant-id-mapper"
      via: "cloned protocolMappers with fresh ids"
      pattern: "core-api-audience-mapper"
    - from: "core-java compose service env"
      to: "ShopAccessService allowlist (application.yml:128 ${ACCESS_MACHINE_CLIENT_IDS:})"
      via: "ACCESS_MACHINE_CLIENT_IDS=integration-orders-rw"
      pattern: "ACCESS_MACHINE_CLIENT_IDS"
---

<objective>
Ship the runtime enablement for the write credential: a new template-seeded Keycloak client `integration-orders-rw` (an exact mirror of `integration-catalog-ro`) carrying `orders:write` + `customers:write` + `catalog:read` (D-09/D-10), the new `customers:read`/`customers:write` client scopes (D-02), a `core-api` default-grant of the two write scopes (D-03), the `INTEGRATION_ORDERS_RW_SECRET` envsubst placeholder across all wiring sites (D-11), and — the load-bearing cross-phase fix the CONTEXT decisions omit — `ACCESS_MACHINE_CLIENT_IDS=integration-orders-rw` on the core-java compose env so the RW service-account does not silently accrue a permanent opaque-UUID `GROUP_ADMIN` row in `shop_staff` on its first `create_order` (VSA-02 / WR-09 interaction, RESEARCH §3).

Purpose: Everything the live E2E (25-04) needs to mint a self-sufficient RW token and hit the newly-gated create endpoints without polluting runtime state. Config/realm only — no code, no file overlap with 25-01, so it runs in parallel.
Output: One realm-template surgery (4 clone-edits), 6 secret-wiring sites, and the machine-client allowlist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-mutating-mcp-tools/25-CONTEXT.md
@.planning/phases/25-mutating-mcp-tools/25-RESEARCH.md
@.planning/phases/25-mutating-mcp-tools/25-PATTERNS.md

<interfaces>
<!-- The exact analog blocks the executor clones. Edit the TEMPLATE, never the rendered realm-export.json (envsubst regenerates it). -->

realm-export.template.json anchors (all clones of integration-catalog-ro siblings):
  - integration-catalog-ro client block: L958-1013 (serviceAccountsEnabled:true, standardFlowEnabled:false, directAccessGrantsEnabled:false, publicClient:false, clientAuthenticatorType:"client-secret")
  - its two protocol mappers: tenant-id-mapper L985-998 + core-api-audience-mapper L999-1010
  - RO secret placeholder: L966 ("${INTEGRATION_CATALOG_RO_SECRET}")
  - RO defaultClientScopes: L1012 ([ "catalog:read", "web-origins","acr","roles","profile","email" ])
  - orders:read / orders:write client-scope blocks: L1037 / L1047 (ALREADY EXIST — clone their SHAPE for customers:read/write; no new orders scope)
  - service-account SA user block: L448-463 (attributes.tenant_id ["00000000-0000-0000-0000-000000000001"], serviceAccountClientId, realmRoles ["default-roles-jtoye-dev"])
  - core-api client defaultClientScopes: L755 ([ "web-origins","acr","roles","profile","email","catalog:read","catalog:write" ])

Secret wiring sites (mirror every INTEGRATION_CATALOG_RO_SECRET site — RESEARCH §4 table):
  - docker-compose.full-stack.yml:54 (env) + :67 (envsubst allowlist)
  - infra/docker-compose.yml:44 + :53
  - infra/docker-compose.hostnet.yml:11 + :20
  - .env.example:113 (+ comment) ; infra/.env.example:55
  - scripts/verify-env.sh REQUIRED_VARS array (add after INTEGRATION_CATALOG_RO_SECRET, currently :47)

VSA-02 config: application.yml:128 binds `${ACCESS_MACHINE_CLIENT_IDS:}` → ShopAccessService.isAllowlistedMachineClient (:498). Set ACCESS_MACHINE_CLIENT_IDS=integration-orders-rw on the core-java service env in the composes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Realm template surgery — integration-orders-rw client + customers scopes + core-api default-grant</name>
  <files>infra/keycloak/realm-export.template.json</files>
  <read_first>
    - infra/keycloak/realm-export.template.json (the anchors in interfaces: L448-463 SA user, L755 core-api scopes, L958-1013 RO client + L985-1010 its two mappers, L1037/L1047 orders scope blocks)
    - 25-RESEARCH.md §4 "Realm template surgery (exact JSON additions)" (a/b/c/d)
    - 25-PATTERNS.md "infra/keycloak/realm-export.template.json (config — 4 edits, all clones)"
    - docs/security-scopes.md §4 Re-import + §5 KC24 managed-attribute trap (why template-seeded avoids the tenant_id strip)
  </read_first>
  <action>Make FOUR clone-edits to `realm-export.template.json` (the template only — never the rendered file). (a) Append two client scopes `customers:read` + `customers:write` to `clientScopes[]`, cloning the `orders:read`/`orders:write` block shape (`include.in.token.scope:"true"`, `display.on.consent.screen:"false"`, empty `protocolMappers`, each a fresh UUID `id`). (b) Add a new client `integration-orders-rw` to `clients[]` cloned verbatim from the `integration-catalog-ro` block, changing only: `id`→fresh UUID; `clientId`→"integration-orders-rw"; `name`/`description`→RW variant; `secret`→"${INTEGRATION_ORDERS_RW_SECRET}"; `defaultClientScopes`→`[ "orders:write", "customers:write", "catalog:read", "web-origins", "acr", "roles", "profile", "email" ]` (D-10 — write + read-for-discovery, NO catalog:write). KEEP `serviceAccountsEnabled:true`, `standardFlowEnabled:false`, `directAccessGrantsEnabled:false`, `publicClient:false`, `clientAuthenticatorType:"client-secret"`, and BOTH cloned protocol mappers `tenant-id-mapper` + `core-api-audience-mapper` each with a fresh `id` (the audience mapper is mandatory — Pitfall 3). (c) Add a new SA user `service-account-integration-orders-rw` to `users[]` cloned from `service-account-integration-catalog-ro`, changing `id`→fresh UUID, `username`, `serviceAccountClientId`→"integration-orders-rw", KEEPING `attributes.tenant_id: ["00000000-0000-0000-0000-000000000001"]` and `realmRoles: ["default-roles-jtoye-dev"]`. (d) Extend the `core-api` client's `defaultClientScopes` (L755) by appending `"orders:write","customers:write"` (D-03). Use fresh, distinct UUIDs for every new `id` (client, both mappers, both scopes, SA user) — a duplicate id breaks import.</action>
  <verify>
    <automated>jq -e '.clients[] | select(.clientId=="integration-orders-rw") | (.defaultClientScopes | index("orders:write")) and (.defaultClientScopes | index("customers:write")) and (.defaultClientScopes | index("catalog:read")) and ((.defaultClientScopes | index("catalog:write")) | not) and (.protocolMappers | map(.name) | index("core-api-audience-mapper") | type=="number")' infra/keycloak/realm-export.template.json</automated>
  </verify>
  <acceptance_criteria>
    - `jq` confirms client `integration-orders-rw` exists with `defaultClientScopes` containing `orders:write`, `customers:write`, `catalog:read` and NOT `catalog:write`, and a `core-api-audience-mapper` protocol mapper present.
    - `jq -e '.clients[] | select(.clientId=="integration-orders-rw") | .secret == "${INTEGRATION_ORDERS_RW_SECRET}"'` is true (no committed literal secret).
    - `jq -e '.clients[] | select(.clientId=="integration-orders-rw") | .protocolMappers | map(.name) | index("tenant-id-mapper") | type=="number"'` is true.
    - `jq -e '.clientScopes | map(.name) | (index("customers:read")|type=="number") and (index("customers:write")|type=="number")'` is true.
    - `jq -e '.clients[] | select(.clientId=="core-api") | (.defaultClientScopes | index("orders:write")|type=="number") and (.defaultClientScopes | index("customers:write")|type=="number")'` is true.
    - `jq -e '.users[] | select(.username=="service-account-integration-orders-rw") | .attributes.tenant_id[0]'` returns the tenant UUID.
    - Whole file parses: `jq empty infra/keycloak/realm-export.template.json` exits 0. No duplicate `id`: `jq '[.. | .id? // empty] | (length) == (unique | length)' infra/keycloak/realm-export.template.json` is `true`.
  </acceptance_criteria>
  <done>The realm template defines the RW client (correct scopes, both mappers, placeholder secret), the customers scopes, the SA user with tenant_id, and the core-api default-grant — all valid JSON with unique ids.</done>
</task>

<task type="auto">
  <name>Task 2: Secret + config wiring — INTEGRATION_ORDERS_RW_SECRET (6 sites) + ACCESS_MACHINE_CLIENT_IDS (VSA-02)</name>
  <files>docker-compose.full-stack.yml, infra/docker-compose.yml, infra/docker-compose.hostnet.yml, .env.example, infra/.env.example, scripts/verify-env.sh</files>
  <read_first>
    - scripts/verify-env.sh (:32 REQUIRED_VARS array, :47 INTEGRATION_CATALOG_RO_SECRET — add the new var beside it)
    - docker-compose.full-stack.yml (:54 keycloak env for INTEGRATION_CATALOG_RO_SECRET, :67 envsubst allowlist var list, and the core-java service env block)
    - infra/docker-compose.yml (:44 + :53) ; infra/docker-compose.hostnet.yml (:11 + :20)
    - .env.example (:108-114 INTEGRATION_CATALOG_RO_SECRET) ; infra/.env.example (:55)
    - 25-RESEARCH.md §4 "Secret wiring (D-11) — 6 files" table + "Also (VSA-02, §3): ACCESS_MACHINE_CLIENT_IDS"
    - 25-PATTERNS.md "Compose + env secret wiring" table + Shared Patterns "VSA-02 shop-access gate"
  </read_first>
  <action>Mirror every `INTEGRATION_CATALOG_RO_SECRET` site for the new `INTEGRATION_ORDERS_RW_SECRET`. In each of the three composes, add `INTEGRATION_ORDERS_RW_SECRET: ${INTEGRATION_ORDERS_RW_SECRET:?...must be set (renders realm integration-orders-rw client)}` to the keycloak service env AND append `$$INTEGRATION_ORDERS_RW_SECRET` to that compose's `envsubst '...'` variable allowlist (or the render will strip the placeholder). In both `.env.example` files add `INTEGRATION_ORDERS_RW_SECRET=CHANGE_ME` with a mirror of the RO comment line. Add `INTEGRATION_ORDERS_RW_SECRET` to the `REQUIRED_VARS` array in `scripts/verify-env.sh` (beside `INTEGRATION_CATALOG_RO_SECRET`). SEPARATELY, add `ACCESS_MACHINE_CLIENT_IDS: integration-orders-rw` to the CORE-JAVA service environment in each compose (this is NOT a secret — it binds `application.yml:128 ${ACCESS_MACHINE_CLIENT_IDS:}`; per RESEARCH §3 it makes the RW SA's `create_order` skip JIT-provision so no permanent opaque-UUID GROUP_ADMIN row lands in `shop_staff`, while `isGroupAdmin()` still passes via the strict-scoping-OFF implicit path). Do NOT commit any real secret value.</action>
  <verify>
    <automated>for f in docker-compose.full-stack.yml infra/docker-compose.yml infra/docker-compose.hostnet.yml .env.example infra/.env.example scripts/verify-env.sh; do grep -q INTEGRATION_ORDERS_RW_SECRET "$f" || { echo "MISSING in $f"; exit 1; }; done; grep -q 'ACCESS_MACHINE_CLIENT_IDS.*integration-orders-rw' docker-compose.full-stack.yml && grep -A40 'REQUIRED_VARS=(' scripts/verify-env.sh | grep -q INTEGRATION_ORDERS_RW_SECRET && echo WIRED</automated>
  </verify>
  <acceptance_criteria>
    - `INTEGRATION_ORDERS_RW_SECRET` appears in all six files (3 composes, 2 .env.example, verify-env.sh) — the loop above exits 0.
    - `scripts/verify-env.sh` lists `INTEGRATION_ORDERS_RW_SECRET` inside `REQUIRED_VARS` (so it fails loud when unset). A functional check: running `verify-env.sh` with the var unset/empty reports it as a missing required credential (non-zero exit).
    - Each compose's `envsubst` allowlist includes `$$INTEGRATION_ORDERS_RW_SECRET` (so the placeholder is not stripped at render).
    - `ACCESS_MACHINE_CLIENT_IDS: integration-orders-rw` is present on the core-java service env in each compose (`grep` confirms; the no-rogue-shop_staff-row runtime proof is asserted at the live E2E, plan 25-04).
    - No real secret literal committed: `.env.example` values read `CHANGE_ME`, the realm secret stays `${INTEGRATION_ORDERS_RW_SECRET}`.
  </acceptance_criteria>
  <done>The RW secret is wired fail-loud across all six sites and the machine-client allowlist is set on core-java, so the live stack renders the RW client and the RW SA's writes leave no shop_staff cruft.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git repo → CI/dev host | The realm template + composes are committed; secrets must never be literals in the repo |
| Keycloak IdP → core | The RW client's token (scopes + aud + tenant_id) crosses into core's auth chain |
| machine credential → shop_staff | The RW SA's first write can JIT-mutate `shop_staff` unless allowlisted (VSA-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Information Disclosure | `INTEGRATION_ORDERS_RW_SECRET` committed as a literal | mitigate | Realm uses the `${...}` envsubst placeholder; `.env.example` reads `CHANGE_ME`; `verify-env.sh` fails loud when unset (Task 2, D-11) |
| T-25-05 | Spoofing | New client missing `core-api-audience-mapper` → token accepted for wrong service / 401 before scope check | mitigate | Both cloned mappers (`tenant-id-mapper` + `core-api-audience-mapper`) kept on the RW client (Task 1, Pitfall 3) |
| T-25-06 | Elevation of Privilege | Over-privileged agent credential (carries `catalog:write`) | mitigate | RW `defaultClientScopes` is write + `catalog:read` ONLY, pointedly NOT `catalog:write` (Task 1, D-10); jq acceptance asserts absence |
| T-25-07 | Tampering / privilege accretion | RW SA silently JIT-provisions a permanent opaque-UUID `GROUP_ADMIN` row in `shop_staff` on first `create_order` (VSA-02/WR-09) | mitigate | `ACCESS_MACHINE_CLIENT_IDS=integration-orders-rw` on core-java env → `onRequest()` skips JIT-provision + directory upsert while implicit-GA still passes; no-rogue-row proven at 25-04 live E2E |
| T-25-SC | Tampering | Package installs (npm/pip/cargo) | accept | This phase installs NO packages (realm/config only); Package Legitimacy Gate N/A |
</threat_model>

<verification>
- `jq empty infra/keycloak/realm-export.template.json` exits 0 (valid JSON after surgery) and no duplicate `id`.
- The RW client jq assertions in Task 1 all pass; the secret-wiring grep loop in Task 2 exits 0.
- `git grep -n 'INTEGRATION_ORDERS_RW_SECRET' -- ':!*.md'` shows the placeholder form only — no literal secret.
</verification>

<success_criteria>
The realm template seeds a least-privilege RW client (both mappers, placeholder secret, no catalog:write), defines the customers scopes, extends the core-api default-grant, and the secret + machine-client allowlist are wired fail-loud across all compose/env sites — so 25-04 can re-import + mint a clean RW token.
</success_criteria>

<output>
Create `.planning/phases/25-mutating-mcp-tools/25-02-SUMMARY.md` when done.
</output>
