---
phase: 25-mutating-mcp-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core-java/src/main/java/uk/jtoye/core/order/OrderController.java
  - core-java/src/main/java/uk/jtoye/core/customer/CustomerController.java
  - core-java/src/main/java/uk/jtoye/core/config/OpenApiConfig.java
  - core-java/src/test/java/uk/jtoye/core/security/ScopedWriteAccessIntegrationTest.java
autonomous: true
requirements: [AI-02]

must_haves:
  truths:
    - "A fully-valid POST /api/v1/orders from a token lacking orders:write returns 403 (not 400, not 404) — converter-through-MockMvc proof sending a fully-valid body so @Valid does not mask the 403 (D-04)"
    - "A fully-valid POST /api/v1/customers from a token lacking customers:write returns 403"
    - "A token carrying orders:write is not 403 on POST /api/v1/orders; customers:write is not 403 on POST /api/v1/customers — the reserved orders:write scope is now enforced via @PreAuthorize (D-01)"
    - "OpenApiConfig documents orders:write + customers:write as enforced (no longer 'reserved')"
  artifacts:
    - path: "core-java/src/main/java/uk/jtoye/core/order/OrderController.java"
      provides: "orders:write scope gate on createOrder"
      contains: "hasAuthority('SCOPE_orders:write')"
    - path: "core-java/src/main/java/uk/jtoye/core/customer/CustomerController.java"
      provides: "customers:write scope gate on create"
      contains: "hasAuthority('SCOPE_customers:write')"
    - path: "core-java/src/test/java/uk/jtoye/core/security/ScopedWriteAccessIntegrationTest.java"
      provides: "converter-through-MockMvc 403/not-403 proof for both write scopes"
      contains: "not403"
    - path: "core-java/src/main/java/uk/jtoye/core/config/OpenApiConfig.java"
      provides: "scope taxonomy doc update (customers:read/write added, orders:write/customers:write enforced)"
      contains: "customers:write"
  key_links:
    - from: "ScopedWriteAccessIntegrationTest"
      to: "OrderController.createOrder / CustomerController.create"
      via: "MockMvc POST with valid body + JwtRolesAndScopesConverter token"
      pattern: "hasAuthority\\('SCOPE_(orders|customers):write'\\)"
---

<objective>
Activate the write-scope authorization gates in core so the create surfaces are least-privilege, and prove them in CI. `POST /api/v1/orders` gets `@PreAuthorize("hasAuthority('SCOPE_orders:write')")` (D-01, activating the reserved `orders:write` scope); `POST /api/v1/customers` gets a NEW `@PreAuthorize("hasAuthority('SCOPE_customers:write')")` (D-02). The gates are proven with the converter-through-MockMvc pattern (#83/#206 precedent): a no-write-scope token → 403, a write-scoped token → not-403, both sent with fully-valid bodies so `@Valid` (which runs before `@PreAuthorize`) cannot mask the authorization outcome.

Purpose: Makes AC-1's "mapped to the appropriate write scopes" real and discharges the `security-scopes.md §6` "feeds the MCP auth model" mandate. This is a Java-only, CI-provable slice with no runtime/realm dependency — it can land in parallel with the realm/config wiring (25-02).
Output: Two `@PreAuthorize` annotations, an updated `OpenApiConfig` scope taxonomy, and a new `ScopedWriteAccessIntegrationTest`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-mutating-mcp-tools/25-CONTEXT.md
@.planning/phases/25-mutating-mcp-tools/25-RESEARCH.md
@.planning/phases/25-mutating-mcp-tools/25-PATTERNS.md

<interfaces>
<!-- The exact analog the executor mirrors. Do not explore the codebase; copy these. -->

ProductController create-site gate (the annotation to copy, product/ProductController.java:16 import + :134):
  import org.springframework.security.access.prepost.PreAuthorize;
  @PreAuthorize("hasAuthority('SCOPE_catalog:write')")   // issue #206 [AI-4]
  @PostMapping
  public ResponseEntity<ProductDto> create(...)

OrderController.createOrder current shape (order/OrderController.java:61-72): `@PostMapping` + `@Idempotent(endpoint="orders.create")` + `public ResponseEntity<OrderDto> createOrder(...)` routing through `idempotencyService.execute(...)`. Add the @PreAuthorize ABOVE @PostMapping. Do NOT touch the idempotency wiring (D-06).

CustomerController.create current shape (customer/CustomerController.java:70-78): `@PostMapping` + `@Idempotent(endpoint="customers.create")` + `public ResponseEntity<CustomerDto> create(...)`. `CreateCustomerRequest` is an inner record at :156. Add the @PreAuthorize ABOVE @PostMapping.

Test template — core-java/src/test/java/uk/jtoye/core/security/ScopedCatalogAccessIntegrationTest.java (mirror VERBATIM): `@SpringBootTest @Testcontainers @Tag("testcontainers")`; token builder `jwt().jwt(j -> j.subject(UUID.random).claim("tenant_id", TENANT_A).claim("scope", "...")).authorities(new JwtRolesAndScopesConverter())`; the `@Valid`-before-`@PreAuthorize` Javadoc; `not403()` custom ResultMatcher; tenant seeding in `@BeforeEach`.

Fully-valid bodies (RESEARCH §3 — so @Valid passes and @PreAuthorize decides):
  VALID_ORDER_JSON = {"shopId":"<uuid>","items":[{"productId":"<uuid>","quantity":1}]}
  VALID_CUSTOMER_JSON = {"name":"Ada","email":"ada@example.com"}

OpenApiConfig scope block (config/OpenApiConfig.java:58-116): `orders:read`/`orders:write` currently documented "reserved, not yet enforced". Update those doc strings to "enforced" and ADD `customers:read` (defined-only) + `customers:write` (enforced) to the `Scopes()` list + the markdown taxonomy.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 RED — ScopedWriteAccessIntegrationTest (converter-through-MockMvc)</name>
  <files>core-java/src/test/java/uk/jtoye/core/security/ScopedWriteAccessIntegrationTest.java</files>
  <read_first>
    - core-java/src/test/java/uk/jtoye/core/security/ScopedCatalogAccessIntegrationTest.java (mirror verbatim: annotations, token builder, not403() matcher, @BeforeEach seeding, @Valid-before-@PreAuthorize Javadoc)
    - core-java/src/main/java/uk/jtoye/core/security/JwtRolesAndScopesConverter.java (the real production scope→SCOPE_* mapping the token builder wires via .authorities())
    - 25-RESEARCH.md §3 "The orders:write / customers:write CI 403 test recipe" (the exact valid-body JSON + token scopes)
    - 25-PATTERNS.md "ScopedOrdersCustomersAccessIntegrationTest" section
  </read_first>
  <behavior>
    - noScopeTokenForbiddenOnOrderCreate: a UUID-subject token with scope "catalog:read" (no orders:write) POSTing the fully-valid VALID_ORDER_JSON to /api/v1/orders → 403.
    - noScopeTokenForbiddenOnCustomerCreate: same token POSTing VALID_CUSTOMER_JSON to /api/v1/customers → 403.
    - writeScopedTokenNot403OnOrderCreate: a token with scope "orders:write" POSTing VALID_ORDER_JSON → not403() (a non-existent shopId 404s downstream via OrderService, which still satisfies not403()).
    - writeScopedTokenNot403OnCustomerCreate: a token with scope "customers:write" POSTing VALID_CUSTOMER_JSON → not403().
  </behavior>
  <action>Create `ScopedWriteAccessIntegrationTest` in package `uk.jtoye.core.security`, an EXACT structural mirror of `ScopedCatalogAccessIntegrationTest` (`@SpringBootTest`, `@AutoConfigureMockMvc`, `@Testcontainers`, `@Tag("testcontainers")`). Build tokens with the `jwt().jwt(...).authorities(new JwtRolesAndScopesConverter())` post-processor carrying a random-UUID `subject`, a `tenant_id` claim (TENANT_A), and a `scope` claim per case. Send FULLY-VALID bodies (the VALID_ORDER_JSON / VALID_CUSTOMER_JSON from read_first) so `@Valid` binding passes and `@PreAuthorize` decides the status — this is the D-04 ordering trap. Copy the `not403()` custom ResultMatcher verbatim. This test is authored BEFORE the gate exists, so the two `isForbidden()` cases are RED now (a no-scope authenticated token currently reaches OrderService and 404s / CustomerService and 201s — neither is 403). RED here proves the gate is genuinely absent. Do NOT add the `@PreAuthorize` annotations in this task.</action>
  <verify>
    <automated>./gradlew :core-java:integrationTest --tests '*ScopedWriteAccess*'</automated>
  </verify>
  <acceptance_criteria>
    - `ScopedWriteAccessIntegrationTest.java` compiles and runs under the `integrationTest` task (it carries `@Tag("testcontainers")`).
    - The run is RED on the two `noScopeToken...Forbidden...` cases: they assert `status().isForbidden()` but observe a non-403 (current behavior — no gate), proving the gate is absent. Record the observed statuses in the SUMMARY.
    - The test uses `new JwtRolesAndScopesConverter()` (the real production mapping), not a hand-stubbed authority set.
  </acceptance_criteria>
  <done>The failing scope-gate test exists and demonstrably RED-proves the gate is missing, mirroring ScopedCatalogAccessIntegrationTest structure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — add the two @PreAuthorize gates + OpenApiConfig taxonomy update</name>
  <files>core-java/src/main/java/uk/jtoye/core/order/OrderController.java, core-java/src/main/java/uk/jtoye/core/customer/CustomerController.java, core-java/src/main/java/uk/jtoye/core/config/OpenApiConfig.java</files>
  <read_first>
    - core-java/src/main/java/uk/jtoye/core/product/ProductController.java (the @PreAuthorize import + create-site annotation to copy, :16 + :134)
    - core-java/src/main/java/uk/jtoye/core/order/OrderController.java (:61-78 — add annotation above @PostMapping; leave @Idempotent + idempotencyService.execute untouched, D-06)
    - core-java/src/main/java/uk/jtoye/core/customer/CustomerController.java (:70-96 create + :156 CreateCustomerRequest record)
    - core-java/src/main/java/uk/jtoye/core/config/OpenApiConfig.java (:58-116 scope taxonomy block)
    - core-java/src/main/java/uk/jtoye/core/security/SecurityConfig.java (:30 confirms @EnableMethodSecurity already active — annotation-only, no config)
  </read_first>
  <action>Add `import org.springframework.security.access.prepost.PreAuthorize;` (if not present) and `@PreAuthorize("hasAuthority('SCOPE_orders:write')")` directly above `@PostMapping` on `OrderController.createOrder` (comment: Phase 25 [AI-02] activate reserved orders:write, D-01). Add the same import and `@PreAuthorize("hasAuthority('SCOPE_customers:write')")` above `@PostMapping` on `CustomerController.create` (comment: Phase 25 [AI-02] new customers:write scope, D-02). In `OpenApiConfig`, change the `orders:write` doc string from "reserved / not yet enforced" to "enforced; gates POST /orders (Phase 25 [AI-02])", change `orders:read` to note it stays reserved, and ADD two entries to the `Scopes()` list + markdown taxonomy: `customers:read` ("defined-only, not enforced this phase") and `customers:write` ("enforced; gates POST /customers"). Doc-string only — no bean/security config change. Do NOT alter the idempotency wiring on either controller (D-06). Do NOT add any negative/deny SpEL — positive gate only (#206 model).</action>
  <verify>
    <automated>./gradlew :core-java:integrationTest --tests '*ScopedWriteAccess*'</automated>
  </verify>
  <acceptance_criteria>
    - `OrderController.java` contains `@PreAuthorize("hasAuthority('SCOPE_orders:write')")` on `createOrder`; `CustomerController.java` contains `@PreAuthorize("hasAuthority('SCOPE_customers:write')")` on `create`.
    - `ScopedWriteAccessIntegrationTest` now passes GREEN (both `noScopeToken...` cases observe 403; both `writeScoped...` cases observe not-403).
    - `OpenApiConfig.java` contains `customers:write` and no longer describes `orders:write` as "reserved / not yet enforced".
    - No change to `@Idempotent`/`idempotencyService.execute` lines on either controller (`git diff` shows only the annotation + import + doc strings).
    - `./gradlew :core-java:compileJava` clean.
  </acceptance_criteria>
  <done>Both write-scope gates enforced and green under the CI proof; scope taxonomy docs updated; idempotency wiring untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → core REST | Untrusted Bearer token crosses here; the scope claim drives `@PreAuthorize` authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Elevation of Privilege | POST /orders, POST /customers left authenticated-only (no scope gate) | mitigate | Add `@PreAuthorize("hasAuthority('SCOPE_orders:write')")` / `'SCOPE_customers:write'` (Task 2); positive least-privilege gate, no deny SpEL |
| T-25-02 | Spoofing / masked authz | `@Valid` runs before `@PreAuthorize` — an invalid body 400s and masks a would-be 403, hiding a gate regression | mitigate | The CI proof (Task 1/2) sends FULLY-VALID bodies so `@PreAuthorize`, not `@Valid`, decides the outcome (D-04 ordering trap) |
| T-25-03 | Repudiation | OpenAPI advertises `orders:write` as "reserved/unenforced" while it is now enforced → stale contract | mitigate | Task 2 updates `OpenApiConfig` scope taxonomy to read "enforced" and adds `customers:read/write` |
</threat_model>

<verification>
- `./gradlew :core-java:integrationTest --tests '*ScopedWriteAccess*'` GREEN after Task 2.
- `./gradlew :core-java:compileJava` clean.
- `git diff` on the two controllers shows only the `@PreAuthorize` annotation + import (idempotency untouched).
</verification>

<success_criteria>
Both create endpoints deny a no-write-scope token with 403 (fully-valid body) and allow a write-scoped token, proven in CI via converter-through-MockMvc; OpenAPI scope docs reflect the newly-enforced write scopes.
</success_criteria>

<output>
Create `.planning/phases/25-mutating-mcp-tools/25-01-SUMMARY.md` when done.
</output>
