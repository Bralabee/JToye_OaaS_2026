---
phase: 29-deployable-staging-with-its-own-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/29-deployable-staging-with-its-own-monitoring/29-OPERATOR-DECISIONS.md
  - docs/architecture/decisions/ADR-0002-managed-vs-manifest-datastores.md
autonomous: false
requirements: [DPLY-01, DPLY-03, DPLY-04]
user_setup:
  - service: azure
    why: "Estate disposition + budget ceiling decision on a LIVE, billing resource group (D-03)"
    dashboard_config:
      - task: "Decide the fate of the snackpass-* Container Apps estate (six apps + snackpass-pg + Log Analytics) in jtoye-rg"
        location: "Azure Portal -> Subscription c483d353-5f61-4587-a790-addb9ab5fb94 -> jtoye-rg"
      - task: "Confirm whether the £0.00 month-to-date PostgreSQL line is a free-tier offer or deferred billing (assumption A3)"
        location: "Azure Portal -> Cost Management -> Credits + offers"
  - service: aws
    why: "#294 bucket verification (D-11) and the logical-dump backup bucket (D-12). Measured 2026-08-10: `aws sts get-caller-identity` returns 'Unable to locate credentials' on this host."
    env_vars:
      - name: AWS_ACCESS_KEY_ID
        source: "AWS IAM -> the identity that owns jtoye-images in eu-west-2"
      - name: AWS_SECRET_ACCESS_KEY
        source: "same"
  - service: gmail
    why: "D-17 alert delivery to the owner's real inbox — the phase's entire point"
    env_vars:
      - name: ALERTMANAGER_SMTP_PASSWORD
        source: "Google Account -> Security -> 2-Step Verification -> App passwords"
      - name: ALERTMANAGER_SMTP_FROM
        source: "the Gmail address the app password belongs to"
      - name: ALERTMANAGER_SMTP_TO
        source: "the inbox that must receive the alert"
  - service: ghcr
    why: "Assumption A5 — if ghcr.io/bralabee/jtoye-* packages are private, every pod hits ImagePullBackOff"
    dashboard_config:
      - task: "Record the visibility (public/private) of jtoye-core-java, jtoye-edge-go, jtoye-frontend"
        location: "github.com/users/Bralabee/packages"

must_haves:
  truths:
    - "The owner has decided, in writing and dated, what happens to the snackpass-* estate before any Azure resource is created"
    - "Every credential this phase cannot self-supply is either present on the host or recorded as an explicit blocker with the measurement that proves it absent"
    - "ADR-0002 reads Accepted with a date, so DPLY-04 is unblocked by a record rather than by an assumption"
    - "The PostgreSQL 16 requirement is written down as a deliberate deviation from CLAUDE.md's PostgreSQL 15, with its evidence"
  artifacts:
    - path: ".planning/phases/29-deployable-staging-with-its-own-monitoring/29-OPERATOR-DECISIONS.md"
      provides: "Dated record of every owner decision, credential state and re-measured Azure/DNS fact this phase depends on"
      contains: "snackpass"
    - path: "docs/architecture/decisions/ADR-0002-managed-vs-manifest-datastores.md"
      provides: "Signed hybrid datastore decision + PG16 skew addendum"
      contains: "Accepted"
  key_links:
    - from: ".planning/phases/29-deployable-staging-with-its-own-monitoring/29-OPERATOR-DECISIONS.md"
      to: "scripts/azure-staging-provision.sh (plan 29-05/29-10)"
      via: "the recorded SKU/count/ceiling decision is the script's input, not a literal in it"
      pattern: "Standard_B2s|B2s"
    - from: "docs/architecture/decisions/ADR-0002-managed-vs-manifest-datastores.md"
      to: "infra/dependency-horizons.yaml rabbitmq-k8s row (plan 29-09)"
      via: "the 2026-07-29 addendum's open question is closed here and the pin lands there"
      pattern: "rabbitmq"
---

<objective>
Close every decision and credential this phase cannot make for itself, BEFORE a single billable
Azure resource exists — and flip ADR-0002 to Accepted so DPLY-04 stops being blocked by paperwork.

The research measured the thing that changes the plan: `jtoye-rg` is **not empty**. It runs a live
`snackpass-*` Azure Container Apps estate burning ≈£3.17/day (≈£95/month) against D-03's £150/month
ceiling, and the costed staging estate is ≈£147/month. £147 + £95 = £242. Something has to give,
and which thing is an owner decision about a running estate, not a planner's cleanup.

Purpose: no downstream plan may assume a value it did not create or was not given.
Output: a dated decisions record, and an ADR that says Accepted.

Branch: `phase-29-research`. Never commit to main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-deployable-staging-with-its-own-monitoring/29-CONTEXT.md
@.planning/phases/29-deployable-staging-with-its-own-monitoring/29-RESEARCH.md
@docs/architecture/decisions/ADR-0002-managed-vs-manifest-datastores.md
@docs/runbooks/credential-rotation.md

<interfaces>
<!-- Values downstream plans will read out of this plan's record. Names are fixed here. -->

Decision keys the record MUST define (downstream plans grep for these exact keys):
  AZURE_SUBSCRIPTION_ID   c483d353-5f61-4587-a790-addb9ab5fb94   (the OWNER's sub; `Prod - HS2 Ltd` is the EMPLOYER's — never touch)
  AZURE_RESOURCE_GROUP    jtoye-rg
  AZURE_LOCATION          uksouth
  AKS_CLUSTER_NAME        jtoye-staging-aks
  NODE_VM_SIZE            <decided>       (research recommendation: Standard_B2s)
  NODE_COUNT              <decided>       (research recommendation: 3)
  PG_SERVER_VERSION       16              (NOT optional — see Blocker C)
  PG_SERVER_SKU           <decided>       (research recommendation: Standard_B2s Burstable)
  PG_ACCESS_MODE          public-with-firewall | private-vnet   (default public-with-firewall per research; 29-04 Task 1 reads this key — added per plan-checker warning 3)
  REDIS_SKU               <decided>       (research recommendation: Basic C0)
  SNACKPASS_DISPOSITION   delete | scale-to-zero | keep-and-raise-ceiling
  MONTHLY_CEILING_GBP     <decided>       (D-03 states ~150)

Credential presence keys (PRESENT / ABSENT, each with the command that measured it):
  AWS_CREDENTIALS · GMAIL_APP_PASSWORD · GRAFANA_ADMIN_PASSWORD · GHCR_VISIBILITY · NETLIFY_DNS_ACCESS
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Owner decision — the snackpass estate and the £150 ceiling</name>
  <files>.planning/phases/29-deployable-staging-with-its-own-monitoring/29-OPERATOR-DECISIONS.md</files>
  <read_first>
    - .planning/phases/29-deployable-staging-with-its-own-monitoring/29-RESEARCH.md (§ "Capacity and cost math" — the measured £30.09 MTD Container Apps figure, the ≈£147 estate table, alternative sizings)
    - .planning/phases/29-deployable-staging-with-its-own-monitoring/29-CONTEXT.md (D-01, D-03)
  </read_first>
  <action>Re-measure the live estate and its month-to-date spend with the commands named in the context above, present the three options with the arithmetic shown, and record the owner's verbatim answer as one of the three option ids into 29-OPERATOR-DECISIONS.md. Create, delete or scale NO Azure resource in this task.</action>
  <decision>What happens to the live `snackpass-*` estate in `jtoye-rg`, and does the ~£150/month ceiling hold?</decision>
  <context>
    RE-MEASURE FIRST, then present. The research figures are dated 2026-08-10 and the research
    itself says to re-measure cost and DNS before relying on either. Run, and paste the real
    output into the checkpoint:
      az resource list -g jtoye-rg --subscription $AZURE_SUBSCRIPTION_ID -o table
      az containerapp list -g jtoye-rg --subscription $AZURE_SUBSCRIPTION_ID -o table
      az rest --method post --url "https://management.azure.com/subscriptions/$AZURE_SUBSCRIPTION_ID/providers/Microsoft.CostManagement/query?api-version=2023-03-01" --body @<cost-query.json>
    Then state the arithmetic plainly: current run-rate + costed new estate vs the ceiling.
    The app names (java-core, go-edge, webapp, python-vision, redis, minio) suggest a prior
    Container Apps deployment of THIS platform. Someone chose it once. Deleting it may destroy
    something wanted — which is exactly why this is not a planner's call (assumption A4).
  </context>
  <options>
    <option id="delete-snackpass">
      <name>Delete the snackpass-* estate</name>
      <pros>Frees ≈£95/month immediately; the ≈£147 staging estate fits the ceiling with headroom</pros>
      <cons>Irreversible; if it is a working deployment of this platform, its data and config go with it</cons>
    </option>
    <option id="scale-to-zero">
      <name>Scale every snackpass Container App to minReplicas 0 and keep snackpass-pg</name>
      <pros>Reversible; kills most of the compute spend; the Postgres line already reads £0.00 MTD</pros>
      <cons>Container Apps still bills some idle; the £0.00 Postgres may be a free-tier offer that expires (assumption A3)</cons>
    </option>
    <option id="raise-ceiling">
      <name>Keep everything and raise the monthly ceiling above £242</name>
      <pros>Nothing is destroyed and nothing is deferred</pros>
      <cons>Changes a locked decision (D-03) — only the owner can do that, and it must be recorded as a supersession, not a drift</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The checkpoint message contains the RE-MEASURED `az resource list` and cost-query output, with the date it was taken — not the research's 2026-08-10 figures quoted back
    - The arithmetic is stated: `<measured run-rate> + <costed new estate> = <total>` against `<ceiling>`
    - The owner's answer is one of the three option ids, recorded verbatim
    - If `raise-ceiling` is chosen, the record names it as a deliberate supersession of D-03 with the new number
  </acceptance_criteria>
  <resume-signal>Select: delete-snackpass, scale-to-zero, or raise-ceiling (and the new ceiling if the last)</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator supplies the four credentials this host does not have</name>
  <files>.planning/phases/29-deployable-staging-with-its-own-monitoring/29-OPERATOR-DECISIONS.md</files>
  <read_first>
    - .planning/phases/29-deployable-staging-with-its-own-monitoring/29-RESEARCH.md (§ "Environment Availability" — the "Missing with no fallback (block execution)" list)
    - docs/runbooks/credential-rotation.md (the path every staging credential follows, per its own §6)
    - scripts/k8s-local-secrets.sh (the REQUIRED_VALUES fail-loud-by-NAME preflight this phase extends)
  </read_first>
  <action>Present the four missing inputs (AWS credentials, the Gmail app password with its From and To addresses, GHCR package visibility, Netlify DNS access), collect the operator's verifying command output for each, and record PRESENT or ABSENT per item into 29-OPERATOR-DECISIONS.md with the blocked downstream plan named. Record only the fact and its provenance, never a credential value.</action>
  <what-built>
    Nothing yet — this task exists because four inputs have NO automated fallback and were each
    measured absent on this host. Claude has already done everything it can: the research ran
    `aws sts get-caller-identity` (-> "Unable to locate credentials"), `dig` on all four
    `*-staging.olajay.co.uk` names (-> no A records, zone live at NS1/Netlify), and
    `az provider show` (-> ContainerService/Cache/Network all NotRegistered).
  </what-built>
  <how-to-verify>
    Supply or confirm each, and paste the verifying command's output:
    1. AWS credentials for eu-west-2 with access to the `jtoye-images` bucket. This is #294's
       bucket half (D-11), and the issue demands it happen BEFORE the first deploy, not after — so
       the check is the full one, not just "the credentials work":
         aws sts get-caller-identity
         aws s3api get-bucket-location --bucket jtoye-images          -> must be eu-west-2
         aws s3api get-bucket-policy   --bucket jtoye-images          -> read the policy
         aws s3api get-public-access-block --bucket jtoye-images
       Confirm the policy is the shape the platform relies on: public-read on the DERIVATIVE prefix
       only, and the quarantine prefix PRIVATE. A public quarantine prefix is a stored-XSS primitive
       on the storefront's own origin, which is why this is a security check and not an inventory.
       Also confirm the DEDICATED BACKUP BUCKET (D-12, eu-west-2, beside the media bucket) exists
       and is PRIVATE, or record that it must be created before plan 29-13's drill can upload.
    2. A Gmail app password for `smtp.gmail.com` (D-17), plus the From and To addresses.
       GLOBAL_RULE_6: the VALUE never enters a tracked file, a commit message or this record —
       only the fact that it exists and where it lives.
    3. Confirm GHCR package visibility for `jtoye-core-java`, `jtoye-edge-go`, `jtoye-frontend`.
       Verify: `docker pull ghcr.io/bralabee/jtoye-core-java:2.1.0` from a logged-OUT daemon,
       or read it off github.com/users/Bralabee/packages.
    4. Confirm you can add A records at Netlify DNS for the olajay.co.uk zone (records get added
       in plan 29-10, once the static IP exists — this is only the access confirmation).
    Re-run `dig +short NS olajay.co.uk` and `dig +short A app-staging.olajay.co.uk` and paste both.
  </how-to-verify>
  <acceptance_criteria>
    - Each of the four items is recorded PRESENT or ABSENT with the exact command output that established it
    - #294's bucket half is recorded with FOUR facts, each from its own command: the bucket exists, its region is eu-west-2, the derivative prefix is public-read, and the quarantine prefix is NOT public. A missing policy statement is recorded as a finding, never as "looked fine"
    - The public/private assertion is proven falsifiable: run the same public-access check against a bucket (or prefix) known to be public and confirm the check reports it as public — an is-private check that has never returned "public" is not evidence of privacy
    - The D-12 backup bucket is recorded as EXISTS-and-private or as MUST-BE-CREATED, with plan 29-13 named as the blocked consumer
    - No credential VALUE appears anywhere in the repo: `git diff` for this task shows zero secret material
    - `dig +short A app-staging.olajay.co.uk` output is recorded with its timestamp (expected: empty today)
    - Any item recorded ABSENT names which downstream plan it blocks (AWS -> 29-13/#294; Gmail -> 29-07/29-12; GHCR -> 29-05 imagePullSecret; DNS -> 29-10)
  </acceptance_criteria>
  <resume-signal>Type "supplied" with the four verification outputs, or name which are still absent</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the decisions and sign ADR-0002</name>
  <files>.planning/phases/29-deployable-staging-with-its-own-monitoring/29-OPERATOR-DECISIONS.md, docs/architecture/decisions/ADR-0002-managed-vs-manifest-datastores.md</files>
  <read_first>
    - docs/architecture/decisions/ADR-0002-managed-vs-manifest-datastores.md (whole file — especially line 3's Status line and the `## 2026-07-29 — Open question (Phase 27, plan 27-02)` section whose closing line says resolving the operator question "needs owner sign-off, which is a human decision and not an agent's to record")
    - .planning/phases/29-deployable-staging-with-its-own-monitoring/29-CONTEXT.md (D-09, D-10, D-11, D-12, D-13)
    - .planning/phases/29-deployable-staging-with-its-own-monitoring/29-RESEARCH.md (§ "Blocker C" for the PG16 evidence; § "State of the Art" for the Azure Cache Basic 2028-09-30 retirement)
    - CLAUDE.md (the "PostgreSQL 15" tech-stack line the PG16 choice deviates from)
  </read_first>
  <action>
    Create `.planning/phases/29-deployable-staging-with-its-own-monitoring/29-OPERATOR-DECISIONS.md`
    holding every key listed in this plan's `<interfaces>` block, one per line, each with the date
    and the command output that established it. Structure it after `docs/runbooks/credential-rotation.md`:
    an owner + provenance line, a GLOBAL_RULE_6 banner stating no literal credential value appears
    in the file, then the decision table, then the credential-presence table, then a
    "What is still ABSENT and what it blocks" section. Absent items are named, never omitted.

    Then edit ADR-0002 with exactly two changes, APPEND-style (the file's own convention — dated
    records are appended, never rewritten):
    1. Line 3's Status line changes from `Proposed (2026-07-12) — needs owner sign-off before #101
       implementation starts` to Accepted, dated 2026-08-10, naming `29-CONTEXT.md` D-09 as the
       sign-off venue and the discussion as where it happened.
    2. A NEW dated section in the shape of the existing `## 2026-07-29 — Open question (Phase 27,
       plan 27-02)` section, headed with today's date, that: closes that open question by naming
       the RabbitMQ cluster-operator v2.22.3 route and the `rabbitmq:4.3.4-management-alpine`
       pin that resolves the `rabbitmq-k8s` horizon row; records the **PostgreSQL 16 requirement**
       with its evidence (Azure Flexible Server exposes BYPASSRLS to `azure_pg_admin`-created roles
       only from PG16; `infra/backups/create-backup-role.sql` needs it or the dump captures zero
       rows from every FORCE-RLS table) and states plainly that this is a deliberate, staging-only
       deviation from CLAUDE.md's PostgreSQL 15 and from the `postgres:15-alpine` compose pin;
       and records the Azure Cache for Redis Basic retirement date 2028-09-30 as a known,
       non-blocking horizon.
    Do NOT rewrite the 2026-07-29 section's text. Do NOT touch PRODUCTION_READINESS_REPORT.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/29-deployable-staging-with-its-own-monitoring/29-OPERATOR-DECISIONS.md && grep -c 'SNACKPASS_DISPOSITION' .planning/phases/29-deployable-staging-with-its-own-monitoring/29-OPERATOR-DECISIONS.md && grep -n '^\*\*Status:\*\*' docs/architecture/decisions/ADR-0002-managed-vs-manifest-datastores.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Proposed' docs/architecture/decisions/ADR-0002-managed-vs-manifest-datastores.md` on the Status LINE returns 0 — read the line, do not count the word file-wide (the 2026-07-29 section legitimately quotes "Status deliberately unchanged")
    - The Status line matches `Accepted` and carries `2026-08-10`
    - The 2026-07-29 section is byte-identical to its pre-change state: prove it with `git diff -- docs/architecture/decisions/ADR-0002-managed-vs-manifest-datastores.md` showing no `-` lines inside that section
    - `29-OPERATOR-DECISIONS.md` defines all 10 decision keys and all 5 credential-presence keys from `<interfaces>`; a missing key is a failure, not a TODO
    - The PG16 paragraph names BOTH the CLAUDE.md PostgreSQL 15 line and the `postgres:15-alpine` compose pin it diverges from
    - FAIL-DIRECTION ARM: `grep -riE 'password|secret|access.?key' 29-OPERATOR-DECISIONS.md | grep -vi 'app password exists|present|absent'` returns no line carrying an actual value — and prove the grep can fire by running it against a scratch file containing a dummy `AKIA...` string
  </acceptance_criteria>
  <done>The decisions record exists with every key defined, ADR-0002 reads Accepted with today's date, the PG16 skew is written down rather than slipped in, and no credential value is in the repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator terminal -> Azure control plane | A destructive `az` command against the wrong subscription reaches EMPLOYER infrastructure |
| operator -> repo | Credential values supplied by a human can be pasted into a tracked file or a commit message |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-01 | Tampering | Azure subscription selection | mitigate | Every `az` invocation in this plan passes `--subscription c483d353-5f61-4587-a790-addb9ab5fb94` explicitly; `Prod - HS2 Ltd` is the employer's and is named in the record as DO-NOT-TOUCH |
| T-29-01-02 | Information Disclosure | Gmail app password / AWS keys in a tracked artifact | mitigate | GLOBAL_RULE_6 banner in the record; Task 3's fail-direction grep arm, itself proven able to fire against a dummy `AKIA` string |
| T-29-01-03 | Repudiation | An undated decision drifting into an assumption | mitigate | Every key carries the date and the command output that established it; absent items are recorded ABSENT with what they block |
| T-29-01-04 | Denial of Service | Deleting a wanted estate | accept | The decision is the owner's (assumption A4), presented with re-measured evidence and a reversible middle option |
</threat_model>

<verification>
- `git diff --staged` reviewed before every commit (a second session drives this checkout)
- No file under `k8s/`, `scripts/`, `core-java/` or `.github/` is touched by this plan
- `scripts/check-branch-behind-base.sh` exits 0 before the PR
</verification>

<success_criteria>
- The snackpass disposition is decided by the owner, with re-measured evidence, and recorded
- Every credential is PRESENT-with-proof or ABSENT-with-blocked-plan
- ADR-0002 Status reads Accepted, dated, with the sign-off venue named
- The PostgreSQL 16 deviation is written down with its evidence, in the ADR, not implied by a SKU
</success_criteria>

<output>
Create `.planning/phases/29-deployable-staging-with-its-own-monitoring/29-01-SUMMARY.md` when done
</output>
