---
phase: 09-repository-secrets-alerting
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - infra/monitoring/scripts/smoke-test-alertmanager.sh
  - docs/runbooks/alerts.md
  - infra/monitoring/README.md
  - .planning/REQUIREMENTS.md
  - .planning/state-of-codebase/STATE-OF-CODEBASE-2026-04-14.md
autonomous: false
requirements:
  - SECR-06
user_setup: []

must_haves:
  truths:
    - "A synthetic alert posted via `amtool alert add` arrives in the configured Slack channel within 60 seconds"
    - "A real `ServiceDown` alert (triggered by `docker compose stop core-java`) arrives in Slack within ~3 minutes"
    - "A resolved notification arrives after core-java restarts within the group_interval window"
    - "Operators have a runbook entry explaining what `ServiceDown` means, its expected impact, first-response steps, and escalation"
    - "REQUIREMENTS.md reflects the re-scope of SECR-01..03 and the addition of SECR-07, with an accurate coverage count"
    - "STATE-OF-CODEBASE-2026-04-14.md carries a footnote flagging §9 Blocker 5 and §11 Work Order A as verified-incorrect, without rewriting the historical sections"
  artifacts:
    - path: "infra/monitoring/scripts/smoke-test-alertmanager.sh"
      provides: "Idempotent two-test smoke script (synthetic + real ServiceDown)"
      contains: "amtool"
    - path: "docs/runbooks/alerts.md"
      provides: "Runbook skeleton with ServiceDown filled in + 9 TODO stubs"
      contains: "ServiceDown"
    - path: "infra/monitoring/README.md"
      provides: "Documentation of Alertmanager addition to the stack"
      contains: "Alertmanager"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Re-scoped SECR-01..03, new SECR-07, updated coverage count 17 → 18"
      contains: "SECR-07"
    - path: ".planning/state-of-codebase/STATE-OF-CODEBASE-2026-04-14.md"
      provides: "Footnote on §9 Blocker 5 and §11 Work Order A"
      contains: "verified incorrect during phase 9"
  key_links:
    - from: "infra/monitoring/scripts/smoke-test-alertmanager.sh"
      to: "jtoye-alertmanager :9093"
      via: "amtool --alertmanager.url=http://localhost:9093 alert add"
      pattern: "amtool.*alert add"
    - from: "infra/monitoring/scripts/smoke-test-alertmanager.sh"
      to: "core-java container"
      via: "docker compose stop core-java → ServiceDown rule"
      pattern: "docker compose.*stop core-java"
    - from: "docs/runbooks/alerts.md"
      to: "infra/monitoring/prometheus/alerts.yml"
      via: "one section per alert rule name"
      pattern: "## ServiceDown"
---

<objective>
Prove SECR-06 end-to-end: both a synthetic `amtool alert add` AND a real `docker compose stop core-java → ServiceDown` alert route through the Plan 01 Alertmanager to Slack within the expected time windows (RESEARCH Pattern 8, D-14). Then document the system — a new runbook skeleton at `docs/runbooks/alerts.md` with `ServiceDown` filled in as the worked example (D-15), an update to `infra/monitoring/README.md`, and the mandatory paperwork from CONTEXT.md `<critical_rescope>`: rewrite SECR-01..03 in `REQUIREMENTS.md`, add SECR-07, bump coverage count, and append a "verified incorrect" footnote to the state-of-codebase audit doc without rewriting §9 or §11.

Purpose: Close SECR-06 and make the re-scope visible in every downstream artifact. After this plan lands, the audit doc stays honest as a historical record, the requirements doc reflects current reality, and operators have a runbook to reach for when their phone buzzes at 2am.

Output: A working smoke test + runbook + updated planning artifacts. This is the last plan of phase 9.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/09-repository-secrets-alerting/09-CONTEXT.md
@.planning/phases/09-repository-secrets-alerting/09-RESEARCH.md
@.planning/phases/09-repository-secrets-alerting/09-01-PLAN.md
@.planning/phases/09-repository-secrets-alerting/09-02-PLAN.md
@infra/monitoring/README.md
@infra/monitoring/prometheus/alerts.yml
@.planning/state-of-codebase/STATE-OF-CODEBASE-2026-04-14.md

<interfaces>
<!-- Key values this plan needs — DO NOT re-derive from first principles. -->

Smoke test timing (RESEARCH Pattern 8 timing table):
- Synthetic via `amtool alert add`: ~30–45s to Slack (group_wait=30s)
- Real `ServiceDown`: ~2m30s–3m to Slack (rule for: 2m + group_wait: 30s)
- Resolved notification: ~5m after fix (group_interval=5m)

Alert rule names to enumerate in the runbook skeleton (D-15 — fill ServiceDown, TODO the other 9):
1. HighErrorRate
2. ServiceDown                ← fill this section as the worked example
3. HighResponseTime
4. DatabaseConnectionPoolExhausted
5. DatabaseDown
6. TooManyDatabaseConnections
7. HighMemoryUsage
8. FrequentGarbageCollection
9. NoOrdersCreated
10. TenantIsolationFailure

Runbook section schema (D-15):
- Trigger condition (promQL + for: clause)
- Expected impact (what users experience)
- First response (≤5 numbered steps, each runnable in ≤2m)
- Escalation (who to page / which channel)

REQUIREMENTS.md edit surface:
- Section "Work Order A — Repository secrets + alerting (SECR)" → rewrite SECR-01, SECR-02, SECR-03 bullets; add SECR-07
- Section "Coverage" at the tail → bump total from 17 to 18, SECR from ×6 to ×7
- Section "Traceability" table → update SECR-01..03 descriptions, add SECR-07 row
- DO NOT edit STFR / STMP requirements — they belong to phases 10 and 11

STATE-OF-CODEBASE edit surface (CRITICAL — do NOT rewrite the sections):
- §9 Blocker 5: append a `> **Footnote (2026-04-15, phase 9):** Verified incorrect during phase 9 planning. ...` blockquote immediately after the existing section body; preserve the original text verbatim.
- §11 Work Order A: same treatment — append a footnote blockquote, do not rewrite the work order table.
- The footnote must reference (a) the three verification commands run, (b) the re-scope captured in `.planning/phases/09-repository-secrets-alerting/09-CONTEXT.md`, (c) the new SECR-07 gitleaks requirement.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the smoke-test script + execute it end-to-end</name>
  <files>infra/monitoring/scripts/smoke-test-alertmanager.sh</files>
  <action>
Create `infra/monitoring/scripts/smoke-test-alertmanager.sh` verbatim from RESEARCH Pattern 8, with one adjustment: make the synthetic test fully non-interactive so it can run in CI later. The real-ServiceDown test stays interactive (requires a human to watch Slack).

Structure:
1. `#!/bin/sh` + `set -eu`
2. Env defaults: `ALERTMANAGER_URL="${ALERTMANAGER_URL:-http://localhost:9093}"`, `COMPOSE_FILE="${COMPOSE_FILE:-infra/monitoring/docker-compose.monitoring.yml}"`, `CORE_COMPOSE_FILE="${CORE_COMPOSE_FILE:-docker-compose.full-stack.yml}"`, `MODE="${1:-synthetic}"` (`synthetic` | `real` | `both`)
3. `cleanup()` trap: remove synthetic alerts via `amtool silence add` (idempotent), `docker compose start core-java` in case test (b) left it stopped
4. Test (a) — synthetic:
   - `docker exec jtoye-alertmanager amtool --alertmanager.url="$ALERTMANAGER_URL" alert add alertname=SmokeTestAlert severity=info service=smoke-test summary="Alertmanager smoke test" description="If you see this in Slack, Alertmanager → Slack roundtrip works."`
   - Wait 45s
   - Assert the alert is visible in Alertmanager: `docker exec jtoye-alertmanager amtool --alertmanager.url="$ALERTMANAGER_URL" alert query alertname=SmokeTestAlert | grep -q SmokeTestAlert`
   - Print: "Check Slack channel for SmokeTestAlert. Slack delivery is out-of-band; this script cannot verify it automatically."
5. Test (b) — real (only if `MODE` is `real` or `both`):
   - Print the wait budget (3 min 30 s worst case) and ask for Enter to proceed
   - `docker compose -f "$CORE_COMPOSE_FILE" stop core-java`
   - Poll `curl -s http://localhost:9091/api/v1/alerts | jq '.data.alerts[] | select(.labels.alertname=="ServiceDown")'` every 10s up to 4 minutes
   - On detection, print "ServiceDown fired — check Slack"
   - `docker compose -f "$CORE_COMPOSE_FILE" start core-java`
   - Remind operator that a `resolved` notification arrives ~5m later (group_interval)

Make the script executable (`chmod +x`) and commit with mode 0755.

**Now execute it:**
1. Ensure Plan 01 left the stack running, and that the real Slack webhook is in `.env`. If Plan 01 Wave 0 reported BLOCKED, stop here and request the webhook from the user before continuing.
2. `bash infra/monitoring/scripts/smoke-test-alertmanager.sh synthetic`
3. Confirm in Slack that `SmokeTestAlert` arrived within ~60 seconds. If it did not, `docker compose logs alertmanager | tail -50` and debug.
4. `bash infra/monitoring/scripts/smoke-test-alertmanager.sh real` — confirm the real `ServiceDown` alert fires and arrives in Slack within ~3 minutes.
5. Confirm the resolved message arrives ~5 minutes after core-java restarts.
6. Capture all three Slack screenshots (synthetic, firing, resolved) for the plan SUMMARY.

If the webhook is blocked, still create the script and run only Test (a)'s Alertmanager-visibility assertion (not Slack delivery). Mark SECR-06 as PARTIAL in the summary and defer the Slack verification as a follow-up.
  </action>
  <verify>
    <automated>bash -c 'test -x infra/monitoring/scripts/smoke-test-alertmanager.sh && grep -q "amtool" infra/monitoring/scripts/smoke-test-alertmanager.sh && grep -q "docker compose.*stop core-java" infra/monitoring/scripts/smoke-test-alertmanager.sh && bash infra/monitoring/scripts/smoke-test-alertmanager.sh synthetic'</automated>
  </verify>
  <done>
Script exists, is executable, and the synthetic test path runs without error. Real-mode test has been executed manually (or marked PARTIAL with reason). Slack delivery captured in the plan summary with screenshots or explicit BLOCKED note.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 — Checkpoint: Confirm Slack received the live alerts</name>
  <files>(human verification — no files modified)</files>
  <what-built>Plan 01 deployed Alertmanager; Task 1 executed the smoke test script in synthetic and real modes. The roundtrip cannot be verified from Claude's side — only the human watching Slack can confirm the messages arrived.</what-built>
  <action>
Human verification step. The operator must visually confirm that the Slack messages generated by Task 1 actually landed in the target channel. Claude cannot read Slack, so this is the only way to close SECR-06 end-to-end.
  </action>
  <how-to-verify>
1. Open the Slack workspace and navigate to `#jtoye-alerts-staging` (or whichever channel the `ALERTMANAGER_SLACK_CHANNEL` env var points at).
2. Confirm you see a message for `SmokeTestAlert` with severity `info`, service `smoke-test`, and the description "If you see this in Slack, Alertmanager → Slack roundtrip works." Message should have arrived within ~60s of Task 1's synthetic test.
3. Confirm you see a second message for `ServiceDown` with severity `critical`, referencing the `core-java` job, that arrived within ~3 minutes of Task 1 running `docker compose stop core-java`.
4. Wait ~5 minutes after core-java restarted. Confirm a `RESOLVED` message arrived for `ServiceDown`.
5. If any message is missing: report which one(s), paste the last 50 lines of `docker compose logs alertmanager`, and we will debug before proceeding.
6. If Slack webhook was unavailable from the start, type `blocked` and we will mark SECR-06 as PARTIAL pending webhook acquisition.
  </how-to-verify>
  <verify>
    <automated>MISSING — human-gated checkpoint; Slack delivery cannot be asserted from Claude's side. Resume-signal is the verification artifact.</automated>
  </verify>
  <done>Operator types `approved`, `partial`, or `blocked`. Result recorded in plan SUMMARY.</done>
  <resume-signal>Type `approved` (all three messages arrived), `partial` (synthetic ok, real missing), or `blocked` (no Slack webhook available)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write docs/runbooks/alerts.md skeleton + fill ServiceDown section</name>
  <files>docs/runbooks/alerts.md</files>
  <action>
Create `docs/runbooks/alerts.md` (and the `docs/runbooks/` directory if it doesn't exist — it currently doesn't). Structure per D-15:

1. Top-level intro (~10 lines): what this runbook is, where alerts come from (Prometheus `alerts.yml`), where they land (Slack `#jtoye-alerts-staging` / `#jtoye-alerts-prod`), the SLA expectation ("acknowledge within 15 minutes during business hours, 1 hour overnight"), and a pointer to `infra/monitoring/scripts/smoke-test-alertmanager.sh` for testing.

2. One H2 section per alert rule, in the same order as `alerts.yml`. Each section follows this schema:
   ```
   ## <AlertName>
   
   **Severity:** <severity from alerts.yml>
   **Service:** <service label from alerts.yml>
   **Trigger:** <the PromQL expression + for: clause, verbatim or summarised>
   **Expected impact:** <1–3 sentences on what users experience when this fires>
   **First response:**
   1. <step>
   2. <step>
   3. <step>
   **Escalation:** <who to page or which channel to escalate to>
   ```

3. Fill `ServiceDown` as the worked example:
   - **Severity:** critical
   - **Service:** infrastructure
   - **Trigger:** `up == 0` for 2 minutes on any scraped job
   - **Expected impact:** One of the monitored services (core-java, edge-go, postgres-exporter, keycloak, rabbitmq) is unreachable. Customer impact depends on which: `core-java` down = full outage; `edge-go` down = rate limiter + WhatsApp paths fail but SSR works; `postgres-exporter` down = metrics gap, no customer impact; `keycloak` down = no new logins.
   - **First response:**
     1. Check the `job` and `instance` labels on the Slack alert to identify which target is down.
     2. `docker compose ps <service>` — is the container running? If not, `docker compose up -d <service>` and monitor.
     3. `docker compose logs --tail=200 <service>` — look for the last error before it went silent.
     4. If `core-java`: confirm the database is healthy (`docker compose exec postgres pg_isready`) before restarting core-java — a DB failure masquerades as a core-java failure.
     5. If restart doesn't resolve in 5 minutes, escalate.
   - **Escalation:** Post in `#jtoye-incident-response` with the alert link and the last 50 log lines. Page the on-call engineer if after hours.

4. The other 9 rules each get the schema skeleton with `TODO` placeholders, so future oncall PRs can fill them in without touching the structure. Example stub:
   ```
   ## HighErrorRate
   
   **Severity:** critical
   **Service:** core-api
   **Trigger:** 5xx rate > 5% for 5m
   **Expected impact:** TODO
   **First response:** TODO
   **Escalation:** TODO
   ```

Include a tail section "Testing this runbook" pointing at `infra/monitoring/scripts/smoke-test-alertmanager.sh`.
  </action>
  <verify>
    <automated>bash -c 'test -f docs/runbooks/alerts.md && grep -c "^## " docs/runbooks/alerts.md | awk "{ exit (\$1 >= 10 ? 0 : 1) }" && grep -q "## ServiceDown" docs/runbooks/alerts.md && grep -A20 "## ServiceDown" docs/runbooks/alerts.md | grep -q "First response" && grep -A25 "## ServiceDown" docs/runbooks/alerts.md | grep -v "TODO" | grep -q "core-java"'</automated>
  </verify>
  <done>
Runbook file exists with at least 10 H2 sections (one per alert rule), `ServiceDown` section is fully fleshed out with concrete first-response steps referencing real commands (not TODO), and the other 9 sections are structured stubs ready for future fill-in.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update infra/monitoring/README.md to document the Alertmanager addition</name>
  <files>infra/monitoring/README.md</files>
  <action>
Append an `## Alertmanager` section to `infra/monitoring/README.md` (read the current file first to preserve existing content and match tone). The new section should cover:

1. What Alertmanager does in this stack (routes fired alerts to Slack).
2. How to start it: `docker compose -f infra/monitoring/docker-compose.monitoring.yml up -d alertmanager` (comes up with `prometheus` via `docker compose up -d` if you bring up the whole file).
3. Required env vars: `ALERTMANAGER_SLACK_WEBHOOK_URL` (required) and `ALERTMANAGER_SLACK_CHANNEL` (optional, defaults to `#jtoye-alerts-staging`). Point at `.env.example` for the placeholder lines.
4. Config: template at `infra/monitoring/alertmanager/alertmanager.yml.tmpl`; rendered at container start by `entrypoint.sh`; rendered file is gitignored and lives only inside the container.
5. Ports: Alertmanager UI at http://localhost:9093; health at http://localhost:9093/-/healthy.
6. Verifying the linkage: `curl -s http://localhost:9091/api/v1/alertmanagers | jq '.data.activeAlertmanagers'` should show one entry.
7. Smoke test: `bash infra/monitoring/scripts/smoke-test-alertmanager.sh [synthetic|real|both]`.
8. Runbook: `docs/runbooks/alerts.md` — one section per alert rule.
9. Upgrading: pinned to `prom/alertmanager:v0.27.0`; consider bumping to v0.28+ after milestone 4 (see RESEARCH section "Alternatives Considered").

Keep it concise — ~40 lines of markdown, same tone as the existing Prometheus/Grafana sections.
  </action>
  <verify>
    <automated>bash -c 'test -f infra/monitoring/README.md && grep -q "Alertmanager" infra/monitoring/README.md && grep -q "ALERTMANAGER_SLACK_WEBHOOK_URL" infra/monitoring/README.md && grep -q "9093" infra/monitoring/README.md && grep -q "smoke-test-alertmanager" infra/monitoring/README.md'</automated>
  </verify>
  <done>
README has an Alertmanager section with env vars, ports, health URL, smoke-test command, and runbook reference. Existing Prometheus/Grafana sections are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 5: Rewrite SECR-01..03 + add SECR-07 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Apply the re-scope from CONTEXT.md `<critical_rescope>` to `.planning/REQUIREMENTS.md`. Read the file first to preserve formatting and neighbouring content exactly.

**1. Under "Work Order A — Repository secrets + alerting (SECR)"** — rewrite the SECR-01..03 bullets and add SECR-07. Preserve SECR-04, SECR-05, SECR-06 verbatim (only their count wording in the work-order preamble needs a touch — the audit said "13 rules", research verified 10).

Replace:
```
- [ ] **SECR-01**: `.env` removed from git tracking (`git rm --cached .env`) with a matching `.gitignore` entry, verified by `git check-ignore`
- [ ] **SECR-02**: All 5 committed credentials rotated in running services — ...
- [ ] **SECR-03**: Rotated values distributed to GitHub Actions Secrets (for CI/CD) and `k8s/` Secret manifests (for staging/prod); no plaintext committed
```

With (copy the tone + checkbox style):
```
- [x] **SECR-01** (re-scoped 2026-04-15): Verify `.env` is not tracked by git and `.gitignore` excludes it — `git log --all --full-history -- .env` empty, `git ls-files --error-unmatch .env` errors, `git check-ignore -v .env` matches `.gitignore:64:.env`. Original premise (`.env` committed) verified false during phase 9 planning; see `.planning/phases/09-repository-secrets-alerting/09-CONTEXT.md` `<critical_rescope>`
- [x] **SECR-02** (dropped 2026-04-15): No-op. No committed credentials exist to rotate — `.env.example` uses `CHANGE_ME` placeholders (lines 7, 13, 18, 22, 25, 33, 35, 41, 51, 84, 86), `k8s/base/secrets-template.yaml` uses `REPLACE_WITH_*` placeholders. Re-scoped per phase 9 context; no rotation performed
- [x] **SECR-03** (dropped 2026-04-15): No-op. Follows from SECR-02 — no rotated values to distribute. Enforcement for future drift is handled by SECR-07
```

Also update the SECR work-order preamble: the audit-doc "13 Prometheus alert rules" phrase should be amended to "13 existing Prometheus alert rules (verified as 10 during phase 9 — see 09-CONTEXT.md D-03)". Similarly for SECR-04/05 where "13" appears.

Append to the SECR list (after SECR-06):
```
- [ ] **SECR-07** (added 2026-04-15): Gitleaks GitHub Action on PRs to `main` blocks any commit containing a secret; `.gitleaks.toml` allowlists the `.env.example` + `k8s/base/secrets-template.yaml` placeholder files so they don't false-positive; local opt-in `scripts/pre-commit-gitleaks.sh` for developer convenience
```

**2. Under "Traceability"** — update the SECR rows:

| Requirement | Phase | Status |
|-------------|-------|--------|
| SECR-01 | Phase 9 | Re-scoped (verified) |
| SECR-02 | Phase 9 | Dropped (no-op) |
| SECR-03 | Phase 9 | Dropped (no-op) |
| SECR-04 | Phase 9 | Pending |
| SECR-05 | Phase 9 | Pending |
| SECR-06 | Phase 9 | Pending |
| SECR-07 | Phase 9 | Pending |

Leave STFR/STMP rows untouched.

**3. Under "Coverage"** — bump the numbers:
- Before: `v1 requirements: 17 total (SECR ×6 + STFR ×6 + STMP ×5)` and `Mapped to phases: 17 (Phase 9 ×6, Phase 10 ×6, Phase 11 ×5)`
- After: `v1 requirements: 18 total (SECR ×7 + STFR ×6 + STMP ×5)` and `Mapped to phases: 18 (Phase 9 ×7, Phase 10 ×6, Phase 11 ×5)`

Do NOT modify the v2 requirements or Out of Scope sections.
  </action>
  </action>
  <verify>
    <automated>bash -c 'grep -q "SECR-07" .planning/REQUIREMENTS.md && grep -q "re-scoped 2026-04-15" .planning/REQUIREMENTS.md && grep -q "18 total" .planning/REQUIREMENTS.md'</automated>
  </verify>
  <done>
SECR-01..03 bullets rewritten with the re-scope annotation, SECR-07 added after SECR-06, traceability table updated with new statuses, coverage count bumped from 17 to 18 (SECR ×7), STFR and STMP sections untouched.
  </done>
</task>

<task type="auto">
  <name>Task 6: Append "verified incorrect" footnote to STATE-OF-CODEBASE-2026-04-14.md §9 + §11</name>
  <files>.planning/state-of-codebase/STATE-OF-CODEBASE-2026-04-14.md</files>
  <action>
Read `.planning/state-of-codebase/STATE-OF-CODEBASE-2026-04-14.md` first. Do NOT rewrite §9 Blocker 5 or §11 Work Order A — preserve them as historical artifacts. Append a blockquote footnote immediately after each of the two sections.

Locate §9 Blocker 5 (search for "Blocker 5" or the nearest heading; section title mentions `.env` committed). Immediately after the last paragraph/table row of that blocker but before the next blocker's heading, insert:

```
> **Footnote (2026-04-15, phase 9 planning):** Verified incorrect during phase 9 planning. Three independent checks confirm `.env` is not tracked and `.gitignore` excludes it: `git log --all --full-history -- .env` returns empty, `git ls-files --error-unmatch .env` errors with "did not match any file(s) known to git", and `git check-ignore -v .env` matches `.gitignore:64:.env`. Similarly, `.env.example` uses `CHANGE_ME` placeholders (lines 7, 13, 18, 22, 25, 33, 35, 41, 51, 84, 86) and `k8s/base/secrets-template.yaml` uses `REPLACE_WITH_*` placeholders — no real secrets leak through committed files. Phase 9 was re-scoped accordingly: SECR-01..03 collapsed to verification steps (no-ops), and a new **SECR-07** (gitleaks CI on PRs to `main` with an allowlist for the placeholder files) was added to prevent future drift from making this finding real. See `.planning/phases/09-repository-secrets-alerting/09-CONTEXT.md` `<critical_rescope>` and `.planning/REQUIREMENTS.md` SECR-01..07 for the updated requirements.
```

Locate §11 Work Order A. Append an identical-shape footnote immediately after the Work Order A table/paragraphs, before Work Order B:

```
> **Footnote (2026-04-15, phase 9 planning):** Work Order A was re-scoped during phase 9 planning. The premise (committed `.env`, 5 credentials to rotate) is verified false — see §9 Blocker 5 footnote above for the three-way git verification. The technical scope now is: deploy Alertmanager, wire Slack routing, smoke-test the full roundtrip, write the runbook, and add gitleaks CI enforcement (new SECR-07) so future drift cannot make the original finding real. Alertmanager + smoke test + runbook + gitleaks all ship in phase 9; the credential rotation line items are marked "dropped (no-op)" in `.planning/REQUIREMENTS.md`.
```

Do NOT:
- Rewrite the existing §9 Blocker 5 or §11 Work Order A prose
- Delete any original finding rows or bullets
- Touch §10, §12, or any other section
- Modify other Work Orders (B, C)

The footnote is purely additive. Use blockquote syntax (`>` prefix) so it's visually distinct from the audit narrative and readers immediately see it's a later annotation.
  </action>
  <verify>
    <automated>bash -c '[ "$(grep -c "verified incorrect during phase 9\\|Verified incorrect during phase 9" .planning/state-of-codebase/STATE-OF-CODEBASE-2026-04-14.md)" -ge 2 ] && grep -q "Footnote (2026-04-15, phase 9 planning)" .planning/state-of-codebase/STATE-OF-CODEBASE-2026-04-14.md && grep -q "SECR-07" .planning/state-of-codebase/STATE-OF-CODEBASE-2026-04-14.md'</automated>
  </verify>
  <done>
Two footnote blockquotes added — one after §9 Blocker 5, one after §11 Work Order A. Original prose preserved verbatim. Each footnote references the three verification commands, the re-scope document, and the new SECR-07. No other sections modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → smoke-test script | script executes destructive-ish action (`docker compose stop core-java`) |
| Alertmanager → Slack | outbound HTTPS with bearer webhook token |
| Audit doc → future readers | historical artifact; footnotes must not erase evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-12 | Denial of Service | Smoke test leaves core-java stopped after failure, causing a real outage | mitigate | `cleanup()` trap in the script runs `docker compose start core-java` on any exit path; Task 1 `done` criteria require operator confirmation that core-java is back up |
| T-09-13 | Information Disclosure | Runbook includes concrete commands; if published externally, leaks internal topology | accept | runbook lives in `docs/runbooks/alerts.md` inside the repo — same trust boundary as the rest of the codebase; no additional exposure |
| T-09-14 | Tampering | A future reader trusts the un-annotated §9 Blocker 5 and re-opens the rotation work | mitigate | Blockquote footnote is visually distinct and mandatory per CONTEXT.md; Task 6 verify asserts the footnote presence by grep |
| T-09-15 | Repudiation | Smoke-test result (synthetic + real Slack messages) cannot be proven later | mitigate | Checkpoint Task 2 captures human confirmation; plan SUMMARY logs Slack screenshots or explicit BLOCKED status |
</threat_model>

<verification>
Phase-level checks before phase 9 completes:
- `bash infra/monitoring/scripts/smoke-test-alertmanager.sh synthetic` exits 0 and operator confirmed Slack delivery (or BLOCKED recorded)
- Real `ServiceDown` test confirmed via checkpoint (or BLOCKED recorded)
- `docs/runbooks/alerts.md` exists with ≥10 H2 sections and ServiceDown fully filled
- `infra/monitoring/README.md` has an Alertmanager section
- `grep -c "SECR-07" .planning/REQUIREMENTS.md` ≥ 3 (bullet + traceability + coverage)
- `grep -c "verified incorrect\|Verified incorrect" .planning/state-of-codebase/STATE-OF-CODEBASE-2026-04-14.md` ≥ 2 (one footnote per section)
- `git diff .planning/state-of-codebase/STATE-OF-CODEBASE-2026-04-14.md` shows only additions (blockquotes), no deletions or rewrites
</verification>

<success_criteria>
- SECR-06 satisfied (or PARTIAL + BLOCKED): smoke test script exists, synthetic test executed, real-mode test executed and operator confirmed Slack delivery; if webhook was unavailable, partial status documented with a clear follow-up task.
- Runbook at `docs/runbooks/alerts.md` documents all 10 alert rules with ServiceDown filled as the worked example.
- `infra/monitoring/README.md` documents the Alertmanager addition.
- `.planning/REQUIREMENTS.md` accurately reflects the re-scope: SECR-01..03 marked as re-scoped/dropped, SECR-07 added, coverage count 18 (SECR ×7).
- `.planning/state-of-codebase/STATE-OF-CODEBASE-2026-04-14.md` §9 Blocker 5 and §11 Work Order A each carry a footnote blockquote flagging them as verified-incorrect; original prose preserved.
- Phase 11 can reuse the Alertmanager route for STMP-05 without additional infrastructure work.
</success_criteria>

<output>
After completion, create `.planning/phases/09-repository-secrets-alerting/09-03-SUMMARY.md` capturing:
- Smoke test mode run (synthetic / real / both) and wall-clock timings observed vs expected
- Slack screenshots or BLOCKED note
- Runbook file listing (10 sections, ServiceDown filled, 9 TODOs)
- REQUIREMENTS.md diff summary (SECR-01..03 rewritten, SECR-07 added, coverage 17→18)
- STATE-OF-CODEBASE diff summary (2 blockquote additions only)
- Recommendation for follow-up phase 9.1 (if any — e.g., k8s Alertmanager manifests, Slack severity routing, webhook acquisition if BLOCKED)
</output>
