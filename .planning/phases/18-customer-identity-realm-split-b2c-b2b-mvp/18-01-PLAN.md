---
phase: 18-customer-identity-realm-split-b2c-b2b-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/keycloak/realm-export-customers.template.json
  - docker-compose.full-stack.yml
  - frontend/Dockerfile
  - frontend/lib/customer-auth.ts
  - frontend/app/api/customer-auth/logout-url/route.ts
  - .env.example
  - .gitignore
  - frontend/e2e/customer-realm-split.verify.mjs
autonomous: true
requirements: [CID-01]

must_haves:
  truths:
    - "A storefront customer can self-register on the jtoye-customers realm (email/password)."
    - "That customer can log in and lands logged-in on /shop (/api/customer-auth/session returns authenticated:true)."
    - "The jtoye-customers realm is created reproducibly from a committed template that survives docker compose --import-realm (no down -v, no admin-API post-step)."
  artifacts:
    - path: "infra/keycloak/realm-export-customers.template.json"
      provides: "Committed jtoye-customers realm: storefront-client (public+PKCE S256), customer default role, self-registration ON, env-driven redirect URIs/web-origins"
      contains: "\"realm\" : \"jtoye-customers\""
    - path: "docker-compose.full-stack.yml"
      provides: "Second realm render (customers template) + second import mount + frontend customer-keycloak build arg and runtime envs"
      contains: "realm-export-customers"
    - path: "frontend/lib/customer-auth.ts"
      provides: "KC_BASE repointed at the customer realm via NEXT_PUBLIC_CUSTOMER_KEYCLOAK_URL"
      contains: "NEXT_PUBLIC_CUSTOMER_KEYCLOAK_URL"
    - path: "frontend/e2e/customer-realm-split.verify.mjs"
      provides: "Node Playwright verification: customer self-register + login lands on /shop"
      contains: "jtoye-customers"
  key_links:
    - from: "frontend/lib/customer-auth.ts"
      to: "jtoye-customers realm token/auth endpoints"
      via: "NEXT_PUBLIC_CUSTOMER_KEYCLOAK_URL (browser build arg)"
      pattern: "NEXT_PUBLIC_CUSTOMER_KEYCLOAK_URL"
    - from: "keycloak-realm-render sidecar"
      to: "keycloak --import-realm"
      via: "rendered realm-export-customers.json mounted into /opt/keycloak/data/import/"
      pattern: "realm-export-customers.json"
---

<objective>
Deliver the first vertical slice: a storefront customer can self-register and log in against a NEW, dedicated `jtoye-customers` Keycloak realm and land logged-in on `/shop`. The realm is created reproducibly from a committed, envsubst-rendered template (mirroring the existing `jtoye-dev` sidecar pattern) so it survives a `--import-realm` rebuild. The frontend `customer-auth.ts` is repointed at the customer realm via a new browser build arg.

Purpose: Isolate B2C customer identity from the B2B staff/vendor realm without touching the backend (customer tokens are frontend-only — verified in CONTEXT). This is the user-facing new capability of Phase 18.
Output: `realm-export-customers.template.json`, compose render+import wiring, repointed frontend, and a node Playwright verification script proving the customer login slice end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-customer-identity-realm-split-b2c-b2b-mvp/18-CONTEXT.md
@.planning/ROADMAP.md
@CLAUDE.md
@infra/keycloak/realm-export.template.json
@docker-compose.full-stack.yml
@docker-compose.frontend-3100.yml
@frontend/lib/customer-auth.ts
@frontend/app/api/customer-auth/logout-url/route.ts
@frontend/app/api/customer-auth/session/route.ts
@frontend/Dockerfile
@frontend/playwright.config.ts

<interfaces>
<!-- Contracts the executor needs. No codebase exploration required. -->

Existing jtoye-dev render sidecar (docker-compose.full-stack.yml, service `keycloak-realm-render`):
- alpine:3.20 + `apk add gettext`, runs `envsubst '$KEYCLOAK_CLIENT_SECRET $EDGE_API_CLIENT_SECRET $KC_SEED_USER_PASSWORD' < realm-export.template.json > realm-export.json`
- Passes an EXPLICIT var list to envsubst so Keycloak i18n placeholders like `${role_default-roles}` are NOT clobbered. Mirror this: pass only the customer var list when rendering the customers template.
- `keycloak` service depends_on `keycloak-realm-render: service_completed_successfully`, mounts `./infra/keycloak/realm-export.json:/opt/keycloak/data/import/realm-export.json:ro`, runs `start-dev --import-realm`.

customer-auth.ts (browser/client-side) current wiring:
- `const KC_BASE = process.env.NEXT_PUBLIC_KEYCLOAK_URL || "http://localhost:8085/realms/jtoye-dev"` (line 18) — this is the repoint target. NEXT_PUBLIC vars are inlined at BUILD time; today NEXT_PUBLIC_KEYCLOAK_URL is NOT a Dockerfile build arg, so the browser silently uses the jtoye-dev fallback literal.
- `const CLIENT_ID = "storefront-client"` (line 19) — client id is constant across realms; keep as-is.
- session route `/api/customer-auth/session` decodes the ID token only (no Keycloak call) — proves backend isolation. Do NOT change its contract; it returns `{ authenticated, expiresAt, profile }`.

logout-url route (server-side): `const KC_BASE = process.env.NEXT_PUBLIC_KEYCLOAK_URL || "http://localhost:8085/realms/jtoye-dev"` — must also honor the customer realm base.

frontend/Dockerfile: only `NEXT_PUBLIC_API_URL` is a build ARG/ENV today (lines 30-31). NEXT_PUBLIC_* must be present at BUILD time to reach the browser bundle.
</interfaces>
</context>

<phase_goal>
**As a** storefront customer, **I want to** register and log in against a dedicated customer identity realm separate from staff/vendor accounts, **so that** customer and staff logins are isolated and the admin dashboard no longer offers customer self-registration.
</phase_goal>

<tasks>

<task type="auto">
  <name>Task 1: Write the failing node Playwright verification for customer self-register + login on jtoye-customers</name>
  <files>frontend/e2e/customer-realm-split.verify.mjs</files>
  <read_first>
    - frontend/playwright.config.ts (chromium project, baseURL override via PLAYWRIGHT_BASE_URL)
    - frontend/lib/customer-auth.ts (customerLogin/customerRegister redirect to `${KC_BASE}/protocol/openid-connect/{auth,registrations}`; callback at /shop/auth/callback)
    - frontend/components/storefront/require-customer-auth.tsx (the "Sign in" entry point → customerLogin)
    - frontend/app/api/customer-auth/session/route.ts (the {authenticated:true} contract to assert)
    - .env.example (variable names: KC_SEED_USER_PASSWORD is a policy-compliant password `length>=12+upper+lower+digit+special`)
  </read_first>
  <action>
    Create a STANDALONE Node ESM script (not a `.spec.ts`, so the docs-freshness gate that counts `test()` blocks in `frontend/e2e/*.spec.ts` is untouched). Import `chromium` from `@playwright/test` resolved via `NODE_PATH=frontend/node_modules`. Read all secrets from `.env` using Node 20 `--env-file=.env`; NEVER print secret values (log only booleans/pass-fail).
    Implement one assertion block (Scenario A) now; leave clearly-labelled TODO stubs for Scenarios B and C which Plan 18-02 fills:
      - Scenario A (customer login on jtoye-customers): launch chromium, go to `${BASE}/shop` where BASE is `PLAYWRIGHT_BASE_URL` or `http://localhost:3100`. Trigger the storefront "Sign in" flow (require-customer-auth Sign-in button), which redirects to Keycloak. On the Keycloak login page follow the "Register" link, self-register a UNIQUE customer (email `cust+<epoch-ms>@example.com`, a policy-compliant password read from `KC_SEED_USER_PASSWORD`, first/last name), submit. Assert the browser lands back on the storefront under `/shop`. Then assert `GET ${BASE}/api/customer-auth/session` (with the browser's cookies) returns HTTP 200 with JSON `authenticated === true` and a non-empty `profile.email`. Also assert the Keycloak page origin during the flow was the `jtoye-customers` realm (URL path contains `/realms/jtoye-customers/`), NOT `jtoye-dev`.
    Export the discovered test-customer email to a file `frontend/e2e/.last-customer-email` (git-ignored path is fine; add to .gitignore in Task 2) so Plan 18-02's separate-pools check can look it up. Exit non-zero on any failed assertion; exit 0 on all-pass.
    Run it now and CONFIRM IT FAILS (no jtoye-customers realm exists yet, and the frontend still points at jtoye-dev) — this is the RED step of the slice.
  </action>
  <acceptance_criteria>
    - File `frontend/e2e/customer-realm-split.verify.mjs` exists and is a `.mjs` ESM script (NOT a `.spec.ts`).
    - `grep -q "jtoye-customers" frontend/e2e/customer-realm-split.verify.mjs` succeeds (asserts the target realm path).
    - No secret literal is printed: `grep -vE '^\s*//' frontend/e2e/customer-realm-split.verify.mjs | grep -Eiq 'console\.(log|error)\([^)]*(password|secret|KC_SEED)' && echo LEAK || echo NO-LEAK` prints `NO-LEAK`.
    - <automated>NODE_PATH=frontend/node_modules node --env-file=.env frontend/e2e/customer-realm-split.verify.mjs ; test $? -ne 0</automated> (RED: script runs and FAILS because the realm/repoint do not exist yet).
    - `jq -r '.playwright_specs' docs/metrics.json` still equals `5` (no new counted spec added).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create the jtoye-customers realm template, wire compose render+import + frontend build arg, repoint customer-auth, and make Scenario A pass</name>
  <files>infra/keycloak/realm-export-customers.template.json, docker-compose.full-stack.yml, frontend/Dockerfile, frontend/lib/customer-auth.ts, frontend/app/api/customer-auth/logout-url/route.ts, .env.example, .gitignore</files>
  <read_first>
    - infra/keycloak/realm-export.template.json (mirror realm structure: roles/default-roles composite, clientScopes, registration flags at realm root; storefront-client is NOT in this file — it currently lives in configure-keycloak.sh)
    - infra/keycloak/configure-keycloak.sh lines 179-245 (the storefront-client + customer-role definition being migrated INTO the committed customers template)
    - docker-compose.full-stack.yml lines 33-95 (render sidecar + keycloak import mount) and 246-283 (frontend build args + env)
    - frontend/Dockerfile lines 29-33 (build-arg pattern for NEXT_PUBLIC_API_URL)
    - frontend/lib/customer-auth.ts line 18-19 and frontend/app/api/customer-auth/logout-url/route.ts line 13-14 (KC_BASE fallback to repoint)
    - .gitignore lines 111,145-146 (keycloak_data + realm-export.json ignore pattern to mirror)
  </read_first>
  <action>
    Build the whole slice so the Task 1 script goes GREEN, honoring GLOBAL_RULE_6 (no env-varying literals committed):

    1. `infra/keycloak/realm-export-customers.template.json` (COMMITTED): a self-contained realm `jtoye-customers` mirroring the jtoye-dev template shape but customer-only. Set realm root: `registrationAllowed:true`, `registrationEmailAsUsername:true`, `loginWithEmailAllowed:true`, `resetPasswordAllowed:true`, `bruteForceProtected:true`, and `verifyEmail:${CUSTOMER_VERIFY_EMAIL}` (env placeholder; default-friendly for local dev). Define a realm role `customer` and add it to the `default-roles-jtoye-customers` composite so self-registrations receive it. Define exactly one client `storefront-client`: `publicClient:true`, `standardFlowEnabled:true`, `implicitFlowEnabled:false`, `directAccessGrantsEnabled:false`, attribute `pkce.code.challenge.method` = `S256`, `redirectUris` = `[ ${CUSTOMER_STOREFRONT_REDIRECT_URIS} ]`, `webOrigins` = `[ ${CUSTOMER_STOREFRONT_WEB_ORIGINS} ]`, `post.logout.redirect.uris` attribute = `+`, defaultClientScopes covering `web-origins, profile, email, roles`. Include NO seed users (customers self-register) and NO tenant_id mapper and NO backend-audience mapper (customer tokens are frontend-only per CONTEXT — do not add backend-affecting claims). Keep the standard built-in clientScopes needed for `openid email profile` so the ID token carries `email`, `name`, `sub`, `email_verified`.

    2. `.env.example` (COMMITTED) + local `.env` (git-ignored, executor updates so the stack renders): add a new "Customer identity realm (jtoye-customers)" block with `CUSTOMER_STOREFRONT_REDIRECT_URIS` (JSON-array element list as a single quoted string, e.g. the value expands inside the template's `[ ]`, covering `http://localhost:3000/*`, `http://localhost:3001/*`, and the port-3100 workaround `http://localhost:3100/*`), `CUSTOMER_STOREFRONT_WEB_ORIGINS` (matching origins WITHOUT the `/*`, incl `http://localhost:3100`), `CUSTOMER_VERIFY_EMAIL` (`false` for local dev), `NEXT_PUBLIC_CUSTOMER_KEYCLOAK_URL` (`http://localhost:8085/realms/jtoye-customers`), `CUSTOMER_KEYCLOAK_ISSUER` (`http://localhost:8085/realms/jtoye-customers`), `CUSTOMER_KEYCLOAK_ISSUER_INTERNAL` (`http://keycloak:8080/realms/jtoye-customers`), and `CUSTOMER_KEYCLOAK_CLIENT_ID` (`storefront-client`). Comment each like the existing render block.

    3. `docker-compose.full-stack.yml`: EXTEND the existing `keycloak-realm-render` sidecar (preferred over a second sidecar — CONTEXT discretion): add `CUSTOMER_STOREFRONT_REDIRECT_URIS`, `CUSTOMER_STOREFRONT_WEB_ORIGINS`, `CUSTOMER_VERIFY_EMAIL` to its `environment`, and a second `envsubst` line in its entrypoint that renders `realm-export-customers.template.json` → `realm-export-customers.json` passing ONLY the explicit customer var list (so `${role_...}` i18n placeholders survive). On the `keycloak` service add a second read-only mount `./infra/keycloak/realm-export-customers.json:/opt/keycloak/data/import/realm-export-customers.json:ro`. On the `frontend` service add build arg `NEXT_PUBLIC_CUSTOMER_KEYCLOAK_URL: ${NEXT_PUBLIC_CUSTOMER_KEYCLOAK_URL}` and runtime env `CUSTOMER_KEYCLOAK_ISSUER` / `CUSTOMER_KEYCLOAK_ISSUER_INTERNAL` / `NEXT_PUBLIC_CUSTOMER_KEYCLOAK_URL`. Do NOT alter the existing jtoye-dev render args, the core-api/edge-api envs, or the admin KEYCLOAK_ISSUER envs.

    4. `frontend/Dockerfile`: add `ARG NEXT_PUBLIC_CUSTOMER_KEYCLOAK_URL` + `ENV NEXT_PUBLIC_CUSTOMER_KEYCLOAK_URL=${NEXT_PUBLIC_CUSTOMER_KEYCLOAK_URL}` next to the existing NEXT_PUBLIC_API_URL build arg so the value is inlined into the browser bundle.

    5. `frontend/lib/customer-auth.ts` (line 18): change `KC_BASE` to read `process.env.NEXT_PUBLIC_CUSTOMER_KEYCLOAK_URL` first, falling back to `process.env.NEXT_PUBLIC_KEYCLOAK_URL` then a jtoye-customers dev default `http://localhost:8085/realms/jtoye-customers`. Do the same in `frontend/app/api/customer-auth/logout-url/route.ts` (line 13-14) so customer logout targets the customer realm end-session endpoint. Leave `CLIENT_ID = "storefront-client"` unchanged. Do NOT touch `frontend/auth.ts` (admin NextAuth stays on jtoye-dev).

    6. `.gitignore`: add `infra/keycloak/realm-export-customers.json` (rendered secret-free but env-specific; mirror the existing realm-export.json ignore) and `frontend/e2e/.last-customer-email`.

    7. Rebuild the local stack WITHOUT destroying volumes (NEVER `docker compose down -v` — shared app+keycloak Postgres): `docker compose -f docker-compose.full-stack.yml -f docker-compose.frontend-3100.yml up -d --build`. The new realm file is imported automatically on keycloak startup because `--import-realm` creates realms that do not yet exist; jtoye-dev is left intact. Then re-run the Task 1 verify script — Scenario A must PASS.
  </action>
  <acceptance_criteria>
    - `jq -e 'select(.realm=="jtoye-customers")' infra/keycloak/realm-export-customers.template.json` — but note the file has envsubst placeholders inside arrays, so instead: `grep -q '"realm" : "jtoye-customers"' infra/keycloak/realm-export-customers.template.json` AND `grep -q '"clientId" : "storefront-client"' infra/keycloak/realm-export-customers.template.json` AND `grep -q 'S256' infra/keycloak/realm-export-customers.template.json` AND `grep -q '"registrationAllowed" : true' infra/keycloak/realm-export-customers.template.json`.
    - No hardcoded redirect/web-origin literals in the committed template: `grep -Eq '\$\{CUSTOMER_STOREFRONT_REDIRECT_URIS\}' infra/keycloak/realm-export-customers.template.json` AND `grep -Eq '\$\{CUSTOMER_STOREFRONT_WEB_ORIGINS\}' infra/keycloak/realm-export-customers.template.json`.
    - Compose wires the second realm: `grep -c 'realm-export-customers' docker-compose.full-stack.yml` is `>= 2` (render output + import mount).
    - Frontend repoint present: `grep -q 'NEXT_PUBLIC_CUSTOMER_KEYCLOAK_URL' frontend/lib/customer-auth.ts` AND `grep -q 'NEXT_PUBLIC_CUSTOMER_KEYCLOAK_URL' frontend/Dockerfile`.
    - Admin path untouched: `git diff --name-only -- frontend/auth.ts | wc -l` equals `0`.
    - Rendered file is git-ignored: `git check-ignore infra/keycloak/realm-export-customers.json` prints the path.
    - Frontend typechecks: <automated>cd frontend && npm run build</automated> exits 0 (tsc, not just jest).
    - The customer realm is live: <automated>curl -sf http://localhost:8085/realms/jtoye-customers/.well-known/openid-configuration >/dev/null</automated>.
    - <automated>NODE_PATH=frontend/node_modules PLAYWRIGHT_BASE_URL=http://localhost:3100 node --env-file=.env frontend/e2e/customer-realm-split.verify.mjs</automated> exits 0 (GREEN: Scenario A — customer self-register + login lands on /shop with session authenticated:true).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (localhost:3100) → Keycloak jtoye-customers | Untrusted user drives OIDC auth-code + PKCE against the public storefront-client |
| Keycloak import dir → running Keycloak | Rendered realm JSON becomes live identity config on startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | storefront-client redirect_uri | mitigate | Explicit env-driven redirect-URI allow-list (localhost:3000/3001/3100 only); no wildcard host; `${CUSTOMER_STOREFRONT_REDIRECT_URIS}` sourced from .env, never committed literals |
| T-18-02 | Spoofing | Public PKCE client | mitigate | `pkce.code.challenge.method:S256` enforced; standard flow only; directAccessGrants disabled |
| T-18-03 | Information disclosure | Committed realm template | mitigate | Customer template carries NO secrets (public client), NO seed users, NO tenant_id/backend-audience mapper; rendered `realm-export-customers.json` is git-ignored |
| T-18-04 | Elevation of privilege | Self-registration default role | mitigate | New self-registrations receive only the `customer` realm role; realm has no staff/admin roles |
| T-18-05 | Denial of service | Open self-registration | accept | Local-dev only; realm `bruteForceProtected:true`. Production registration throttling deferred (Phase 2 scope) |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan (Keycloak JSON + compose + existing @playwright/test); nothing to legitimacy-gate |
</threat_model>

<verification>
- `docker compose -f docker-compose.full-stack.yml -f docker-compose.frontend-3100.yml up -d --build` (NEVER `down -v`).
- `curl -sf http://localhost:8085/realms/jtoye-customers/.well-known/openid-configuration` returns 200.
- `curl -sf http://localhost:8085/realms/jtoye-dev/.well-known/openid-configuration` returns 200 (jtoye-dev still intact — rollback safety).
- `NODE_PATH=frontend/node_modules PLAYWRIGHT_BASE_URL=http://localhost:3100 node --env-file=.env frontend/e2e/customer-realm-split.verify.mjs` exits 0 (Scenario A green).
- `cd frontend && npm run build` exits 0.
</verification>

<success_criteria>
- A storefront customer self-registers and logs in on `jtoye-customers` and lands logged-in on `/shop` (`/api/customer-auth/session` → `authenticated:true`).
- The realm is reproducible from the committed template via the render sidecar + `--import-realm` (no admin-API post-step, no volume reset).
- `jtoye-dev` remains fully operational (admin login unaffected by this slice).
- `frontend/auth.ts` (admin NextAuth) is untouched; only customer-auth paths repointed.
</success_criteria>

<output>
Create `.planning/phases/18-customer-identity-realm-split-b2c-b2b-mvp/18-01-SUMMARY.md` when done.
</output>
