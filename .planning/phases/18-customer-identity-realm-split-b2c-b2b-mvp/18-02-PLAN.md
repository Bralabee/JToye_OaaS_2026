---
phase: 18-customer-identity-realm-split-b2c-b2b-mvp
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - infra/keycloak/configure-keycloak.sh
  - frontend/e2e/customer-realm-split.verify.mjs
autonomous: true
requirements: [CID-01]

must_haves:
  truths:
    - "The admin/staff realm jtoye-dev no longer allows self-registration; its login page shows NO Register/New-user link."
    - "storefront-client no longer exists on jtoye-dev (it lives only in jtoye-customers)."
    - "Admin dashboard login still works on jtoye-dev with the unchanged core-api client and seed staff users."
    - "The two user pools are separate: the test customer is absent from jtoye-dev and admin-user is absent from jtoye-customers."
    - "Backend (core-java, edge-go) is unchanged — no JWT/issuer/security edits."
  artifacts:
    - path: "infra/keycloak/configure-keycloak.sh"
      provides: "jtoye-dev config with the registration-enable, storefront-client, and customer-role branches REMOVED; test-client + seed staff users retained"
      contains: "jtoye-dev"
    - path: "frontend/e2e/customer-realm-split.verify.mjs"
      provides: "Extended verification: Scenario B (admin login, no Register link) + Scenario C (separate pools) alongside Scenario A"
      contains: "admin-user"
  key_links:
    - from: "admin core-api login page (jtoye-dev)"
      to: "realm registrationAllowed:false"
      via: "no Register anchor rendered on the Keycloak login form"
      pattern: "registrationAllowed"
    - from: "jtoye-dev user pool"
      to: "jtoye-customers user pool"
      via: "admin-API per-realm user lookup returns disjoint sets"
      pattern: "admin/realms/(jtoye-dev|jtoye-customers)/users"
---

<objective>
Deliver the second vertical slice: harden the staff/vendor realm `jtoye-dev` so it is a clean staff-only surface — self-registration disabled (no Register link on the admin `core-api` login page) and `storefront-client` removed — while the admin dashboard login keeps working exactly as today. Prove the two identity pools are isolated and that the backend was never touched.

Purpose: Complete the B2C/B2B split. After Plan 18-01 gave customers their own realm, this slice removes the customer-signup affordances that were leaking onto the staff login and proves separation. Rollback safety: `jtoye-dev` admin login must remain working throughout.
Output: hardened `configure-keycloak.sh`, an extended browser verification (Scenarios B + C), and a backend-untouched assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-customer-identity-realm-split-b2c-b2b-mvp/18-CONTEXT.md
@.planning/ROADMAP.md
@CLAUDE.md
@infra/keycloak/configure-keycloak.sh
@infra/keycloak/realm-export.template.json
@frontend/auth.ts
@frontend/e2e/customer-realm-split.verify.mjs

<interfaces>
<!-- Contracts the executor needs. -->

infra/keycloak/configure-keycloak.sh (the file being hardened), current post-import blocks against jtoye-dev:
- Lines 164-177: PUT realm with `registrationAllowed:true, registrationEmailAsUsername:true, resetPasswordAllowed:true` — THIS is what flips the live realm to show a Register link. REMOVE this block.
- Lines 179-216: creates `storefront-client` on jtoye-dev with hardcoded localhost:3000/3001 redirect URIs. REMOVE this block (storefront-client now lives only in jtoye-customers per Plan 18-01).
- Lines 218-245: creates the `customer` realm role and adds it to jtoye-dev default-roles. REMOVE these blocks.
- KEEP: test-client create/update + tenant_id mapper (lines 42-99) and tenant-a-user/tenant-b-user seeding (lines 102-162). These are staff/test fixtures, not customer signup.

infra/keycloak/realm-export.template.json (jtoye-dev committed template): realm root already has `registrationAllowed : false` (line 30). This is the reproducible source of truth — assert it stays false; no edit expected.

Live-realm reality (CONTEXT load-bearing fact #2): `--import-realm` does NOT overwrite the EXISTING jtoye-dev realm, and we must NEVER `docker compose down -v`. So the persisted keycloak_data volume still carries `registrationAllowed:true` + a live `storefront-client` from a prior configure-keycloak.sh run. A one-time, non-destructive admin-API reconcile on the running realm is required IN ADDITION to editing the committed script.

frontend/auth.ts: admin NextAuth on client `core-api`, realm jtoye-dev, via KEYCLOAK_ISSUER/KEYCLOAK_ISSUER_INTERNAL. DO NOT TOUCH. Admin login behavior must be identical after this plan.

Keycloak master admin token (for admin-API): POST http://localhost:8085/realms/master/protocol/openid-connect/token with client_id=admin-cli, username from `KEYCLOAK_ADMIN`, password from `KEYCLOAK_ADMIN_PASSWORD` (both in .env). Per-realm user lookup: GET /admin/realms/{realm}/users?email=... or ?username=...
</interfaces>
</context>

<phase_goal>
**As a** storefront customer, **I want to** register and log in against a dedicated customer identity realm separate from staff/vendor accounts, **so that** customer and staff logins are isolated and the admin dashboard no longer offers customer self-registration.
</phase_goal>

<tasks>

<task type="auto">
  <name>Task 1: Extend the verification with Scenario B (admin login, no Register link) + Scenario C (separate pools) — RED</name>
  <files>frontend/e2e/customer-realm-split.verify.mjs</files>
  <read_first>
    - frontend/e2e/customer-realm-split.verify.mjs (the Scenario A script + Scenario B/C stubs from Plan 18-01; also reads `frontend/e2e/.last-customer-email`)
    - frontend/auth.ts (admin sign-in flow entry `/auth/signin` → Keycloak jtoye-dev core-api login page → `/dashboard`)
    - .env.example (KEYCLOAK_ADMIN, KEYCLOAK_ADMIN_PASSWORD, KC_SEED_USER_PASSWORD variable names)
  </read_first>
  <action>
    Fill the Scenario B and C stubs in the existing node Playwright script. Continue reading all secrets from `.env` via `--env-file`; never print them.
      - Scenario B (admin login, no Register link): navigate the browser to `${BASE}/auth/signin`, proceed to the Keycloak `jtoye-dev` `core-api` login page. Assert the login form contains NO registration affordance — no anchor whose href contains `/registrations` and no visible "Register"/"New user" link. Then log in as `admin-user` with password `KC_SEED_USER_PASSWORD` and assert the browser lands on `/dashboard` (admin login still works). Assert the login page origin path contained `/realms/jtoye-dev/`.
      - Scenario C (separate pools, admin-API): obtain a Keycloak master admin token (client_id=admin-cli, `KEYCLOAK_ADMIN`/`KEYCLOAK_ADMIN_PASSWORD`). Assert `GET /admin/realms/jtoye-dev/users?email=<the test customer email from .last-customer-email>` returns an EMPTY array (customer absent from staff realm). Assert `GET /admin/realms/jtoye-customers/users?username=admin-user` returns an EMPTY array (staff admin absent from customer realm). Assert `GET /admin/realms/jtoye-dev/clients?clientId=storefront-client` returns an EMPTY array (storefront-client gone from jtoye-dev).
    Keep Scenario A intact. The script should run all three scenarios and exit non-zero if ANY fails.
    Run the full script now and CONFIRM Scenarios B and C FAIL against the current live state (jtoye-dev still has registrationAllowed:true and a live storefront-client from the persisted volume) — this is the RED step.
  </action>
  <acceptance_criteria>
    - Script references all three surfaces: `grep -q 'admin-user' frontend/e2e/customer-realm-split.verify.mjs` AND `grep -q 'registrations' frontend/e2e/customer-realm-split.verify.mjs` AND `grep -q 'storefront-client' frontend/e2e/customer-realm-split.verify.mjs`.
    - No secret leak: `grep -vE '^\s*//' frontend/e2e/customer-realm-split.verify.mjs | grep -Eiq 'console\.(log|error)\([^)]*(password|secret|ADMIN_PASSWORD)' && echo LEAK || echo NO-LEAK` prints `NO-LEAK`.
    - Still not a counted spec: `jq -r '.playwright_specs' docs/metrics.json` equals `5`.
    - <automated>NODE_PATH=frontend/node_modules PLAYWRIGHT_BASE_URL=http://localhost:3100 node --env-file=.env frontend/e2e/customer-realm-split.verify.mjs ; test $? -ne 0</automated> (RED: Scenarios B/C fail because jtoye-dev is not yet hardened).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Harden jtoye-dev — strip customer-signup branches from configure-keycloak.sh + reconcile the live realm</name>
  <files>infra/keycloak/configure-keycloak.sh</files>
  <read_first>
    - infra/keycloak/configure-keycloak.sh (full file — identify blocks 164-177, 179-216, 218-245 to remove; retain test-client + tenant user seeding)
    - infra/keycloak/realm-export.template.json line 30 (assert registrationAllowed:false is the committed default — no edit)
  </read_first>
  <action>
    Make jtoye-dev a staff-only surface, reproducibly and on the running stack:

    1. Edit `infra/keycloak/configure-keycloak.sh`: DELETE the "Enable self-service registration" PUT block (currently ~lines 164-177), the "Configure storefront-client" create block (~179-216), and the "Create customer realm role" + "Set customer as default role" blocks (~218-245). Keep everything else: the admin-token bootstrap, test-client create/update + tenant_id mapper, and tenant-a-user/tenant-b-user seeding. The script must remain valid bash (`bash -n` passes) and its remaining curls must still target `${REALM}` = jtoye-dev only. Do NOT hardcode any password literal (the file already sources KC_ADMIN_PASSWORD/KC_SEED_USER_PASSWORD from the environment — preserve that).

    2. Confirm the committed jtoye-dev template keeps `registrationAllowed : false` (already true on line 30) — the reproducible source of truth for a fresh `--import-realm`. No edit if already false.

    3. Reconcile the CURRENTLY-RUNNING jtoye-dev realm (its persisted keycloak_data still has registrationAllowed:true + a live storefront-client, and `--import-realm` will NOT overwrite an existing realm; NEVER `down -v`). Using a master admin-API token: PUT the jtoye-dev realm with `registrationAllowed:false` and `registrationEmailAsUsername:false`, and DELETE the live `storefront-client` (look up its id via GET /admin/realms/jtoye-dev/clients?clientId=storefront-client). Leave the `customer` role deletion optional (harmless if it lingers; do NOT remove it from default-roles composites only if that risks staff login — safest is to leave the role but ensure no registration path). Do NOT touch core-api, edge-api, test-client, or the seed staff users. This admin-API step is a one-time operational reconcile; the committed script/template are the reproducible source of truth.

    4. Rebuild/refresh the stack without destroying volumes if needed (`docker compose -f docker-compose.full-stack.yml -f docker-compose.frontend-3100.yml up -d`). Confirm the admin login page no longer renders a Register link and admin-user can still sign in.
  </action>
  <acceptance_criteria>
    - Customer-signup branches removed from the committed script: `grep -c 'storefront-client' infra/keycloak/configure-keycloak.sh` equals `0` AND `grep -vE '^\s*#' infra/keycloak/configure-keycloak.sh | grep -Eiq 'registrationAllowed.*true' && echo PRESENT || echo ABSENT` prints `ABSENT`.
    - Staff fixtures retained: `grep -q 'tenant-a-user' infra/keycloak/configure-keycloak.sh` AND `grep -q 'test-client' infra/keycloak/configure-keycloak.sh`.
    - Script is valid bash: <automated>bash -n infra/keycloak/configure-keycloak.sh</automated> exits 0.
    - Committed jtoye-dev template stays hardened: `grep -q '"registrationAllowed" : false' infra/keycloak/realm-export.template.json`.
    - Live realm reconciled: admin-API GET /admin/realms/jtoye-dev returns `registrationAllowed:false` and GET /admin/realms/jtoye-dev/clients?clientId=storefront-client returns `[]` (verified by the Scenario B/C run in Task 3).
    - jtoye-dev still serves auth: <automated>curl -sf http://localhost:8085/realms/jtoye-dev/.well-known/openid-configuration >/dev/null</automated>.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Verify backend untouched + run the full 3-scenario E2E green + confirm docs-freshness unaffected</name>
  <files>frontend/e2e/customer-realm-split.verify.mjs</files>
  <read_first>
    - .planning/phases/18-customer-identity-realm-split-b2c-b2b-mvp/18-01-SUMMARY.md (what Plan 18-01 changed — to scope the backend-untouched diff)
    - core-java/src/main/java/uk/jtoye/core/security/SecurityConfig.java and AudienceValidator.java (the files that MUST NOT change)
    - CLAUDE.md (docs-freshness gate: docs/metrics.json is the single source of truth; frontend TS changes need `npm run build`)
  </read_first>
  <action>
    Final goal-backward verification of the whole phase:

    1. Backend-untouched assertion (verify-only, no edits): assert that NO core-java or edge-go source changed across Phase 18. Use `git diff --name-only <phase-base-ref>..HEAD -- core-java edge-go` and require an EMPTY result, and specifically confirm `core-java/.../security/SecurityConfig.java`, `core-java/.../security/AudienceValidator.java`, and any edge-go JWT/issuer files are absent from the diff. If any backend file appears, STOP and flag — customer tokens are frontend-only and no backend multi-issuer validation belongs in Phase 1 (CONTEXT).

    2. Run the full node Playwright verification (all three scenarios) on the rebuilt :3100 stack and require exit 0: Scenario A (customer self-register + login on jtoye-customers → /shop authenticated:true), Scenario B (admin login on jtoye-dev works, NO Register link), Scenario C (separate pools + storefront-client gone from jtoye-dev).

    3. Confirm the docs-freshness CI gate is unaffected: the verification lives in a `.verify.mjs` (not a `.spec.ts`), so no counted test blocks were added. Assert `docs/metrics.json` `total_logical_invocations` is unchanged (`771`) and `playwright_specs` is `5`. If the executor chose to author a `.spec.ts` with `test()` blocks instead, they MUST update docs/metrics.json + the CLAUDE.md counts consistently so `scripts/docs-freshness.sh` passes — otherwise keep it a non-spec script.
  </action>
  <acceptance_criteria>
    - Backend untouched: <automated>test -z "$(git diff --name-only $(git merge-base HEAD main)..HEAD -- core-java edge-go)"</automated> (empty diff → exit 0). If a different phase-base ref is more accurate, use it; the assertion must show zero core-java/edge-go changes.
    - Backend security files specifically unchanged: `git diff --name-only $(git merge-base HEAD main)..HEAD | grep -E 'SecurityConfig\.java|AudienceValidator\.java|edge-go/.*jwt' ; test $? -ne 0` (grep finds nothing → non-zero → pass).
    - Full E2E green: <automated>NODE_PATH=frontend/node_modules PLAYWRIGHT_BASE_URL=http://localhost:3100 node --env-file=.env frontend/e2e/customer-realm-split.verify.mjs</automated> exits 0 (Scenarios A + B + C all pass).
    - docs-freshness unaffected: `jq -r '.total_logical_invocations' docs/metrics.json` equals `771` AND `jq -r '.playwright_specs' docs/metrics.json` equals `5`.
    - docs-freshness gate green: <automated>bash scripts/docs-freshness.sh</automated> exits 0 (or its check subcommand — run whatever the CI job runs).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Keycloak jtoye-dev (core-api) | Staff authenticate; must NOT be offered self-registration |
| admin-API (master token) → per-realm user/client store | Privileged reconcile of the running jtoye-dev realm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Elevation of privilege | jtoye-dev self-registration | mitigate | `registrationAllowed:false` in committed template AND reconciled on the live realm; no self-signup path into the staff/vendor pool |
| T-18-07 | Spoofing | Residual storefront-client on jtoye-dev | mitigate | storefront-client removed from configure-keycloak.sh and deleted from the live jtoye-dev realm; it exists only in jtoye-customers |
| T-18-08 | Information disclosure | Cross-realm user pools | mitigate | Separate realms proven disjoint via admin-API per-realm lookup (Scenario C); staff and customer credentials never co-mingle |
| T-18-09 | Tampering | Backend JWT validation drift | mitigate | Explicit git-diff assertion that core-java/edge-go security code is unchanged; no backend multi-issuer validation added |
| T-18-10 | Repudiation | Admin-API reconcile without record | accept | One-time operational step; committed script/template are the durable source of truth and are auditable in git |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan (bash edits + existing @playwright/test); nothing to legitimacy-gate |
</threat_model>

<verification>
- `bash -n infra/keycloak/configure-keycloak.sh` passes.
- Live jtoye-dev: `registrationAllowed:false`, no `storefront-client`; `admin-user`/`KC_SEED_USER_PASSWORD` still logs into `/dashboard`.
- Full node Playwright script (Scenarios A+B+C) exits 0 on the :3100 stack.
- `git diff --name-only ..HEAD -- core-java edge-go` is empty (backend untouched).
- `bash scripts/docs-freshness.sh` exits 0; `docs/metrics.json` counts unchanged.
</verification>

<success_criteria>
- Admin `core-api` login page on `jtoye-dev` shows NO Register/New-user link; `admin-user` still signs into the dashboard.
- `storefront-client` no longer exists on `jtoye-dev`; realm self-registration disabled (committed + live).
- Test customer absent from `jtoye-dev`; `admin-user` absent from `jtoye-customers` (pools isolated).
- core-java and edge-go unchanged — no JWT/issuer/security edits; customer tokens remain frontend-only.
- docs-freshness gate stays green (no metrics drift).
</success_criteria>

<output>
Create `.planning/phases/18-customer-identity-realm-split-b2c-b2b-mvp/18-02-SUMMARY.md` when done.
</output>
