---
phase: 18-vendor-onboarding-first-slice
plan: 07
type: execute
wave: 5
depends_on: ["18-02", "18-05", "18-06"]
files_modified:
  - frontend/types/api.ts
  - frontend/app/dashboard/onboarding/page.tsx
  - frontend/app/dashboard/onboarding/__tests__/page.test.tsx
  - frontend/components/dashboard/sidebar.tsx
  - frontend/components/marketing/operator-pitch.tsx
  - frontend/components/marketing/__tests__/operator-pitch.test.tsx
  - frontend/app/dashboard/page.tsx
  - frontend/app/dashboard/__tests__/page.test.tsx
  - frontend/components/dashboard/__tests__/dashboard-shell.test.tsx
  - docs/metrics.json
  - CLAUDE.md
  - README.md
autonomous: true
requirements: [VOB-01, VOB-02, VOB-03]
user_setup: []

must_haves:
  truths:
    - "A prospective vendor sees a 'Start your application' CTA on /for-operators that links to /dashboard/onboarding (via sign-in if needed)"
    - "A signed-in vendor with no onboarding sees a create form (model + shop select + optional company number) and can create an application"
    - "After create, the vendor sees the live status view: overall-state badge + the 3-gate breakdown (Business / Food hygiene / Allergen)"
    - "While VERIFYING, the status refreshes automatically every 4s until a terminal/actionable state, then polling stops"
    - "When APPROVED, 'Go live' is enabled; clicking opens a confirm dialog then publishes; a guard-veto (400) shows a destructive toast and keeps the gate breakdown visible (no crash)"
    - "A 'Go live' item appears in the dashboard sidebar linking to /dashboard/onboarding"
    - "The dashboard home shows an incomplete-onboarding banner until the onboarding is LIVE"
    - "docs-freshness is green: docs/metrics.json regenerated and CLAUDE.md + README counts synced to it"
  artifacts:
    - path: "frontend/app/dashboard/onboarding/page.tsx"
      provides: "Single stateful onboarding page: create form + live status view + go-live"
      min_lines: 180
    - path: "frontend/app/dashboard/onboarding/__tests__/page.test.tsx"
      provides: "Jest coverage: empty/create, status, verifying-poll, go-live guard rejection"
      min_lines: 80
    - path: "frontend/types/api.ts"
      provides: "OnboardingDto / GateDto / CreateOnboardingRequest + onboarding enums"
      contains: "OnboardingDto"
    - path: "frontend/components/dashboard/sidebar.tsx"
      provides: "Go live nav item"
      contains: "/dashboard/onboarding"
    - path: "frontend/components/marketing/operator-pitch.tsx"
      provides: "Start your application CTA"
      contains: "/dashboard/onboarding"
    - path: "frontend/app/dashboard/page.tsx"
      provides: "Incomplete-onboarding banner (hidden when LIVE)"
      contains: "onboarding"
  key_links:
    - from: "frontend/app/dashboard/onboarding/page.tsx"
      to: "/api/v1/onboarding/me"
      via: "apiClient.get on mount + 4s poll while VERIFYING"
      pattern: "onboarding/me"
    - from: "frontend/app/dashboard/onboarding/page.tsx"
      to: "/api/v1/onboarding, /submit, /go-live"
      via: "apiClient.post"
      pattern: "onboarding(/submit|/go-live)?"
    - from: "frontend/app/dashboard/onboarding/page.tsx"
      to: "/api/v1/shops"
      via: "apiClient.get for the shop select"
      pattern: "api/v1/shops"
    - from: "frontend/components/dashboard/sidebar.tsx"
      to: "/dashboard/onboarding"
      via: "navigation array item"
      pattern: "/dashboard/onboarding"
---

<objective>
Ship the vendor-onboarding UI slice: make the already-merged onboarding backend VISIBLE and let a signed-in vendor take a shop live, plus an entry point from the prospective-vendor marketing page. Frontend-only — it consumes the live `/api/v1/onboarding` surface delivered in 18-02..18-06. No backend changes.

Purpose: 18-01..18-06 built the whole onboarding state machine, gate chain, and go-live endpoint but shipped zero UI. Without this slice a vendor has no way to create an onboarding, see gate results, or go live. This plan closes that gap against the APPROVED, LOCKED design contract in 18-UI-SPEC.md.

Output:
- New `/dashboard/onboarding` page — single stateful surface (create form → live status → go-live) driven by `GET /api/v1/onboarding/me`.
- "Start your application" CTA on `/for-operators`.
- "Go live" sidebar nav item + incomplete-onboarding banner on the dashboard home.
- Jest component coverage for every onboarding state incl. the go-live guard rejection, and a reconciled `docs/metrics.json` keeping the `docs-freshness` CI gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# LOCKED design contract — read in full and treat every table/copy string as non-negotiable
@.planning/phases/18-vendor-onboarding-first-slice/18-UI-SPEC.md

# Backend behaviour this UI binds to (auto-approve, guard-veto -> 400, 404-on-no-onboarding)
@.planning/phases/18-vendor-onboarding-first-slice/18-02-SUMMARY.md
@.planning/phases/18-vendor-onboarding-first-slice/18-06-SUMMARY.md

# Idioms to mirror VERBATIM (api-client usage, toast channel, raw <select> shop dropdown,
# loading spinner, empty state, Card/Badge/Dialog usage, status-badge class idiom)
@frontend/app/dashboard/marketing/page.tsx
# Dashboard home to extend with the banner (large chart component — touch surgically)
@frontend/app/dashboard/page.tsx
# Sidebar navigation array to append one item to
@frontend/components/dashboard/sidebar.tsx
# Marketing page to add the CTA to (extend, do NOT restyle — A5)
@frontend/components/marketing/operator-pitch.tsx
# Hardened axios instance: Bearer + X-Tenant-Id auto-injected; 5xx retried, 4xx (incl. 404/400) rejected
@frontend/lib/api-client.ts
# Jest idiom to mirror: jest.mock('@/lib/api-client') + jest.mock('@/hooks/use-toast')
@frontend/app/dashboard/products/__tests__/page.test.tsx

<interfaces>
<!-- Contracts extracted from the merged backend + spec. Use these directly — no codebase exploration needed. -->

Endpoints (all via `apiClient`, base already has NEXT_PUBLIC_API_URL; use the `/api/v1` prefix):
- `GET  /api/v1/onboarding/me`     -> 200 OnboardingDto | 404 (no onboarding yet -> show create form)
- `POST /api/v1/onboarding`        -> 201 OnboardingDto (body CreateOnboardingRequest) | 400 validation | 409 already exists
- `POST /api/v1/onboarding/submit` -> 200 OnboardingDto (DRAFT -> VERIFYING) | 400 illegal transition | 404
- `POST /api/v1/onboarding/go-live`-> 200 OnboardingDto (APPROVED -> LIVE) | 400 gates unsatisfied / illegal transition | 404
- `GET  /api/v1/shops?page=0&size=100&sort=name,asc` -> PageResponse<Shop> (`.data.content`)

TypeScript types to ADD to frontend/types/api.ts (mirror the Java DTOs — OffsetDateTime -> ISO string, nullable -> `| null`):
```typescript
export type OnboardingModel = "MARKETPLACE" | "WHITE_LABEL"
export type OnboardingState =
  | "DRAFT" | "VERIFYING" | "ACTION_REQUIRED" | "PENDING_APPROVAL"
  | "APPROVED" | "LIVE" | "SUSPENDED" | "REJECTED" | "WITHDRAWN"
export type GateType = "BUSINESS_VERIFIED" | "FOOD_HYGIENE_RATING" | "ALLERGEN_DATA_COMPLETE"
export type GateStatus = "PENDING" | "PASSED" | "FAILED" | "MANUAL_REVIEW" | "WAIVED"

export interface GateDto {
  gateType: GateType
  status: GateStatus
  mandatory: boolean
  reason: string | null
  checkedAt: string | null
}
export interface OnboardingDto {
  id: string
  status: OnboardingState
  model: OnboardingModel
  shopId: string
  companyNumber: string | null
  submittedAt: string | null
  approvedAt: string | null
  wentLiveAt: string | null
  gates: GateDto[]
}
export interface CreateOnboardingRequest {
  model: OnboardingModel
  shopId: string
  companyNumber?: string
}
```

Overall-state -> label + badge class (from 18-UI-SPEC "State & Status Mapping"):
DRAFT "Draft" `bg-slate-100 text-slate-600` · VERIFYING "Running checks" `bg-amber-100 text-amber-700` ·
ACTION_REQUIRED "Action required" `bg-orange-100 text-orange-700` · PENDING_APPROVAL "Awaiting approval" `bg-blue-100 text-blue-700` ·
APPROVED "Ready to go live" `bg-blue-100 text-blue-700` · LIVE "Live" `bg-emerald-100 text-emerald-700` ·
SUSPENDED "Suspended" `bg-red-100 text-red-700` · REJECTED "Rejected" `bg-red-100 text-red-700` · WITHDRAWN "Withdrawn" `bg-slate-100 text-slate-500`

Gate type -> label + lucide icon: BUSINESS_VERIFIED "Business verification" Building2 · FOOD_HYGIENE_RATING "Food hygiene rating" UtensilsCrossed · ALLERGEN_DATA_COMPLETE "Allergen data" Wheat.
Gate status -> label + badge + icon: PENDING "Checking…" `bg-amber-100 text-amber-700` Loader2(spin) · PASSED "Passed" `bg-emerald-100 text-emerald-700` CheckCircle2 · FAILED "Failed" `bg-red-100 text-red-700` XCircle · MANUAL_REVIEW "Manual review" `bg-amber-100 text-amber-700` AlertTriangle · WAIVED "Not required" `bg-slate-100 text-slate-600` MinusCircle.
(Map defensively — render an unknown gateType/status with a neutral slate fallback rather than crashing.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Onboarding types + /dashboard/onboarding stateful page (create -> status -> go-live) + Jest coverage</name>
  <files>frontend/types/api.ts, frontend/app/dashboard/onboarding/page.tsx, frontend/app/dashboard/onboarding/__tests__/page.test.tsx</files>
  <behavior>
    Component states (all covered by the Jest file — mock apiClient + useToast per the products-page test idiom):
    - GET /me rejects with 404 -> renders the "Take your shop live" empty state AND the create form (model select, shop select populated from GET /api/v1/shops, optional companyNumber input, "Create application" button).
    - GET /me resolves 200 DRAFT -> renders the overall-state badge "Draft" + a "Submit for verification" primary button; clicking POSTs /onboarding/submit.
    - GET /me resolves 200 VERIFYING -> gates render "Checking…"; a 4s poll is scheduled (assert via fake timers that a second GET /me fires after 4000ms; assert the interval is cleared on unmount and once state leaves VERIFYING).
    - GET /me resolves 200 APPROVED -> "Go live" button enabled; clicking opens the confirm dialog; confirming POSTs /onboarding/go-live.
    - go-live returns 400 (guard veto) -> a destructive toast fires with the spec copy ("Not ready to go live yet" / "Every check below must pass first…") AND the gate breakdown stays rendered (no crash/unmount).
    - GET /me resolves 200 LIVE -> emerald "Live" state, no go-live button.
  </behavior>
  <action>
    Add the onboarding types from the `<interfaces>` block to `frontend/types/api.ts` (append near the other DTO interfaces; do not reorder existing exports).

    Create `frontend/app/dashboard/onboarding/page.tsx` as a `"use client"` default-export component — the single stateful surface described in 18-UI-SPEC "Interaction Contract". It inherits `DashboardShell` (auth + sidebar + `p-8`) from the existing dashboard layout, so add NO layout/auth code. Do NOT add `export const dynamic`; sibling dashboard pages are client components with no server-side nonce read and comply with the enforced CSP as-is. Introduce NO inline `<script>` / `dangerouslySetInnerHTML`.

    Data + control flow: on mount `apiClient.get("/api/v1/onboarding/me")`; a 404 means "no onboarding" -> show the create form (this is the page's primary job, a Card not a Dialog). Any 5xx/network is auto-retried by the api-client then surfaced via `useToast` destructive with the spec's "Couldn't load your onboarding" copy. On a 200, render the status view keyed off `OnboardingDto.status`. Reuse the existing loading spinner (`h-32 w-32 animate-spin … border-blue-600`) and the `space-y-6` container / `text-4xl font-semibold text-slate-900` header idioms.

    Create form (per D-driven contract A2 — track the REAL DTO, NOT any older prose): `model` (required, default MARKETPLACE) as a select/segmented toggle with the spec copy ("How will you sell?" / "On the J'Toye marketplace" / "On my own storefront"); `shopId` (required) as the raw `<select>` idiom copied verbatim from `marketing/page.tsx` (`<option value="">Select a shop</option>`) populated from `GET /api/v1/shops?page=0&size=100&sort=name,asc`; `companyNumber` optional `Input` with helper "Companies House number — leave blank if you're a sole trader." Submit "Create application" -> `POST /api/v1/onboarding` (201) -> transition into the DRAFT status view. Handle 409 with the spec's "Onboarding already started" toast, then reload `/me`. If the shop list is empty, point the empty state at `/dashboard/shops` to create a shop first (A2).

    Status view: overall-state badge (label + badge class from the mapping), the 3-gate breakdown (icon + label + status badge + `reason` as `text-sm text-slate-600`, `mandatory` -> "Required"/"Optional" tag, `checkedAt` via `formatDistanceToNow` from date-fns like the marketing page, null -> "not yet checked"), a light milestone timeline from `submittedAt`/`approvedAt`/`wentLiveAt`, and the SINGLE state-driven primary CTA: DRAFT -> "Submit for verification" (`POST /onboarding/submit`); ACTION_REQUIRED -> surface each FAILED gate `reason` under an "Action required" heading + a "Re-run checks" affordance re-calling `POST /onboarding/submit` (A3 — if the backend 400s the resubmit, fall back to in-page guidance, do not crash); PENDING_APPROVAL -> keep polling briefly, "Your checks passed — approval is being finalised", no action; APPROVED -> enabled "Go live" button; LIVE -> "Your storefront is live" confirmation, no button; SUSPENDED/REJECTED/WITHDRAWN -> terminal badge + `reason`, no action. Go-live enablement is state-driven: the button is present+enabled ONLY when `status === "APPROVED"`.

    Polling (A4, not SSE): while `status === "VERIFYING"` (and briefly for PENDING_APPROVAL to catch background auto-approve), `setInterval` re-fetching `/me` every 4000ms; clear the interval on unmount and as soon as status leaves the polling set. Never leave a dangling interval.

    Go live: clicking "Go live" opens the existing `Dialog` (title "Go live?", body "This publishes your storefront and makes it visible to customers.", confirm "Go live" default/primary variant — NOT destructive, confirm copy per spec, cancel "Not yet"). Confirm -> `POST /api/v1/onboarding/go-live`. On 200 render LIVE. On 400 (guard veto — allergen/other gate not passed) do NOT crash: fire the destructive toast "Not ready to go live yet" / "Every check below must pass first. Resolve the flagged items and try again." and keep the gate breakdown visible so the vendor sees the blocking check (VOB-03 made visible). Never render `GateDto.evidence`/`externalRef` — the DTO omits them by design; only render the exposed fields.

    Write `frontend/app/dashboard/onboarding/__tests__/page.test.tsx` covering every case in `<behavior>`, mirroring the products-page test's mocking (`jest.mock('@/lib/api-client')`, `jest.mock('@/hooks/use-toast')`). Use `jest.useFakeTimers()` for the 4s poll assertion; drive URL-routed mock responses (return 404 vs OnboardingDto by requested path). Keep tests deterministic — no real network, no real timers left running.
  </action>
  <verify>
    <automated>cd frontend && npm test -- app/dashboard/onboarding/__tests__/page.test.tsx</automated>
  </verify>
  <done>The onboarding page test file passes; it exercises empty/create, DRAFT-submit, VERIFYING 4s-poll, APPROVED go-live confirm, and the go-live 400 guard-veto (destructive toast + breakdown still visible). Types compile against the real DTO shapes.</done>
</task>

<task type="auto">
  <name>Task 2: Entry surfaces — /for-operators CTA, sidebar nav item, dashboard incomplete-onboarding banner + tests</name>
  <files>frontend/components/marketing/operator-pitch.tsx, frontend/components/marketing/__tests__/operator-pitch.test.tsx, frontend/components/dashboard/sidebar.tsx, frontend/components/dashboard/__tests__/dashboard-shell.test.tsx, frontend/app/dashboard/page.tsx, frontend/app/dashboard/__tests__/page.test.tsx</files>
  <action>
    Sidebar (Surface 3): append ONE item to the existing `navigation` array in `frontend/components/dashboard/sidebar.tsx` — `{ name: "Go live", href: "/dashboard/onboarding", icon: Rocket }` — and add `Rocket` to the existing `lucide-react` import. Active-state styling is inherited from the existing `.map`; add no new styles. In `frontend/components/dashboard/__tests__/dashboard-shell.test.tsx` (which renders the shell/sidebar), add an assertion that a "Go live" link to `/dashboard/onboarding` renders; reuse whatever next-auth/next-navigation mocks that test already sets up.

    Marketing CTA (Surface 1, A5 — extend, do NOT restyle): in `frontend/components/marketing/operator-pitch.tsx` add a "Start your application" CTA in the hero action row next to the existing buttons, styled to MATCH the page's existing primary treatment (the orange `bg-[#f26522] … rounded-full … font-black` button idiom already on that page — the marketing brand accent is orange; do NOT import dashboard slate/blue tokens here). Implement it as a Next `Link` (or plain `<a>`) to `/dashboard/onboarding` — unauthenticated clicks are redirected to sign-in by the dashboard layout, so no auth logic is needed here. Copy is exactly "Start your application". Extend `frontend/components/marketing/__tests__/operator-pitch.test.tsx` to assert the CTA text renders with `href="/dashboard/onboarding"`.

    Dashboard banner (Surface 4): in `frontend/app/dashboard/page.tsx`, above the stats grid, render a dismissible info banner while onboarding is not `LIVE`. Fetch `GET /api/v1/onboarding/me` alongside the existing dashboard fetches; treat 404 (no onboarding) as "not started". Banner variants per spec: no onboarding / DRAFT -> amber `bg-amber-50 border border-amber-200 text-amber-800` "Finish setting up your shop to go live." + link "Start onboarding"; VERIFYING/PENDING_APPROVAL/APPROVED -> blue `bg-blue-50 border border-blue-200 text-blue-800` "Your onboarding is in progress." + link "View status"; LIVE -> render no banner. Use the `p-4 rounded-lg text-sm` info-strip idiom; both links target `/dashboard/onboarding`. Do NOT let a failed `/me` fetch break the existing dashboard render — swallow its error (banner simply hides), matching the page's non-critical-fetch pattern. Update `frontend/app/dashboard/__tests__/page.test.tsx` so its apiClient.get mock routes by URL (returns the OnboardingDto/404 for `/onboarding/me` and the existing fixtures for the other endpoints) and add cases: non-LIVE onboarding -> banner shown; LIVE -> banner absent.
  </action>
  <verify>
    <automated>cd frontend && npm test -- components/marketing/__tests__/operator-pitch.test.tsx components/dashboard/__tests__/dashboard-shell.test.tsx app/dashboard/__tests__/page.test.tsx</automated>
  </verify>
  <done>Sidebar renders a "Go live" link to /dashboard/onboarding; /for-operators shows a "Start your application" CTA linking to /dashboard/onboarding in the existing marketing button style; the dashboard home shows the correct banner variant while onboarding is not LIVE and none when LIVE; all three touched test files pass.</done>
</task>

<task type="auto">
  <name>Task 3: Full gates — typecheck build + full Jest suite + docs-freshness metrics reconcile</name>
  <files>docs/metrics.json, CLAUDE.md, README.md</files>
  <action>
    Run the full frontend typecheck build: `cd frontend && npm run build`. Jest does NOT type-check, so this is the gate that catches DTO/prop type errors in the new page and edits — fix any type or lint errors it surfaces before proceeding.

    Run the full Jest suite: `cd frontend && npm test`. All suites (including the new onboarding tests and the three edited test files) must be green.

    Reconcile the docs-freshness counts, which the new Jest tests changed: from the repo root run `bash scripts/docs-freshness.sh --write` to regenerate `docs/metrics.json` (recomputes Jest `it/test` block + file counts; Java/Go/Playwright/schema unchanged this frontend-only plan). Then sync the human-readable counts that mirror it — the `**Testing**:` constraint line in `CLAUDE.md` and the corresponding badge/status figures in `README.md` — to EXACTLY the regenerated `docs/metrics.json` values (never hand-compute; copy from the manifest, same as 18-06). Finally run `bash scripts/docs-freshness.sh` (check mode) and confirm it exits 0.

    Do NOT touch the running Docker stack and do NOT run Playwright — live-browser validation is the orchestrator's post-deploy step. Do NOT add any Playwright spec in this plan.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm test && cd .. && bash scripts/docs-freshness.sh</automated>
  </verify>
  <done>`npm run build` (tsc) passes with zero type errors; full Jest suite green; `docs/metrics.json` regenerated and `scripts/docs-freshness.sh` check mode exits 0; CLAUDE.md + README counts match the manifest exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (vendor dashboard) → core API | Authenticated vendor input crosses here; session Bearer + `X-Tenant-Id` injected by `api-client.ts`. This slice adds new client callers only — no new server surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07-01 | Information disclosure | Gate breakdown render in `onboarding/page.tsx` | mitigate | Render ONLY the exposed `GateDto` fields (gateType/status/mandatory/reason/checkedAt). The DTO already omits `evidence`/`externalRef` (18-02); the UI must never attempt to read or display them. |
| T-18-07-02 | Tampering (forced go-live) | "Go live" button enablement | mitigate | Client enablement is UX only. The real enforcement is server-side: the onboarding state machine is the sole writer of `Shop.published` and the GO_LIVE guard re-checks all mandatory gates (18-02/18-05). The client MUST surface the 400 guard-veto and never assume success — proven by the go-live-400 test in Task 1. |
| T-18-07-03 | Spoofing (cross-tenant onboarding) | `POST /onboarding` body | accept | `CreateOnboardingRequest` carries no `tenantId`; the server resolves tenant from the caller context and `api-client` injects `X-Tenant-Id` as defence in depth. No client-controllable tenant field is added. |
| T-18-07-SC | Tampering (supply chain) | npm dependencies | mitigate | No new npm dependencies. The whole slice is built from existing shadcn primitives, `lucide-react`, `framer-motion`, `date-fns` (all present). If any new dependency appears necessary, STOP and flag it as a blocker — do not install. |
</threat_model>

<verification>
- `cd frontend && npm run build` — tsc passes (jest does not type-check; this is the type gate).
- `cd frontend && npm test` — full Jest suite green, including onboarding page states (empty/create, DRAFT-submit, VERIFYING 4s-poll, APPROVED go-live, go-live 400 guard-veto) and the CTA/sidebar/banner tests.
- `bash scripts/docs-freshness.sh` — check mode exits 0 (metrics.json reconciled; CLAUDE.md + README synced).
- Manual/orchestrator (OUT of executor scope, post-deploy): live-browser Playwright walkthrough on the running stack.
</verification>

<success_criteria>
- A signed-in vendor with no onboarding can create an application (model + shop + optional company number) at `/dashboard/onboarding`.
- After create, the vendor sees the overall-state badge + 3-gate breakdown; while VERIFYING the view auto-refreshes every 4s and stops polling on a terminal/actionable state.
- When APPROVED the vendor can confirm and go live; a 400 guard-veto surfaces the spec toast and keeps the gate breakdown visible without crashing.
- `/for-operators` shows a "Start your application" CTA to `/dashboard/onboarding`; the sidebar shows a "Go live" item; the dashboard home shows the incomplete-onboarding banner until LIVE.
- Build (tsc) + full Jest suite green; `docs/metrics.json` reconciled and CLAUDE.md/README counts synced (`docs-freshness` exits 0).
- No backend changes, no new npm dependencies, no running-stack/Playwright interaction.
</success_criteria>

<output>
Create `.planning/phases/18-vendor-onboarding-first-slice/18-07-SUMMARY.md` when done.
</output>
