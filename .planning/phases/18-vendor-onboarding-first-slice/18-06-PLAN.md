---
phase: 18-vendor-onboarding-first-slice
plan: 06
type: execute
wave: 4
depends_on: [18-03, 18-04, 18-05]
autonomous: true
requirements: [VOB-01, VOB-02, VOB-03, VOB-04, VOB-05]
files_modified:
  - core-java/src/test/java/uk/jtoye/core/onboarding/VendorOnboardingEndToEndIntegrationTest.java
  - docs/metrics.json
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - CHANGELOG.md

must_haves:
  truths:
    - "The fully-automatic path — submit → all three gates green → auto-approve (onboarding.auto-approve=true, NO manual/admin APPROVE call) → go-live → Shop.published=true — passes on real Postgres"
    - "With onboarding.auto-approve=false the same green onboarding halts at PENDING_APPROVAL and go-live is rejected (documents the toggle; no admin approve endpoint exists in this slice)"
    - "docs/metrics.json reflects the real post-phase counts (schema_version 43, +1 controller, new test methods/files) and scripts/docs-freshness.sh exits 0"
    - "VOB-01..05 are registered in REQUIREMENTS.md and mapped to their phase-18 plans"
    - "ROADMAP Phase 18 shows the finalised plan list and requirement IDs"
  artifacts:
    - path: "core-java/src/test/java/uk/jtoye/core/onboarding/VendorOnboardingEndToEndIntegrationTest.java"
      provides: "cross-gate fully-automatic path proof (all 3 gates green → auto-APPROVED → LIVE → published)"
      contains: "published"
    - path: "docs/metrics.json"
      provides: "reconciled counts keeping the docs-freshness gate green"
      contains: "\"schema_version\": 43"
  key_links:
    - from: "docs/metrics.json"
      to: "scripts/docs-freshness.sh check"
      via: "regenerated by --write, verified by check mode"
      pattern: "schema_version"
---

<objective>
Wire and verify the whole slice: one cross-gate end-to-end integration test proving a vendor can submit → have all three gates (Companies House, FHRS, allergen) evaluate green → **auto-approve without any manual review** (`onboarding.auto-approve=true`) → go live with `Shop.published=true`; then reconcile the project's documentation-freshness metrics (a single writer for `docs/metrics.json`, avoiding parallel-plan conflicts) and register the new VOB requirement IDs. This is the closure/"wire" slice for the phase, and the proof that the headline "go live without manual review" goal is reachable end-to-end.

Purpose: prove the slices compose into the headline capability (with NO admin/service APPROVE crutch masking the gap) and leave the repo's CI gates (docs-freshness) green with full requirement traceability.
Output: the end-to-end test, a reconciled `docs/metrics.json`, and REQUIREMENTS/ROADMAP/CHANGELOG updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-vendor-onboarding-first-slice/18-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-vendor-onboarding-first-slice/18-02-SUMMARY.md
@.planning/phases/18-vendor-onboarding-first-slice/18-03-SUMMARY.md
@.planning/phases/18-vendor-onboarding-first-slice/18-04-SUMMARY.md
@.planning/phases/18-vendor-onboarding-first-slice/18-05-SUMMARY.md

@core-java/src/test/java/uk/jtoye/core/security/RlsContractTest.java
@scripts/docs-freshness.sh

<interfaces>
<!-- docs/metrics.json current baseline (before phase 18): -->
{ "java_test_methods": 568, "java_test_files": 91, "java_controllers": 14, "schema_version": 42, "total_logical_invocations": 782, ... }
<!-- docs-freshness recomputes these from git-tracked source; run --write then commit, do NOT hand-edit counts. -->
<!-- Gate types available after waves 1-3: BUSINESS_VERIFIED (CompaniesHouseGate), FOOD_HYGIENE_RATING (FhrsGate), ALLERGEN_DATA_COMPLETE (AllergenCompletenessGate). -->
<!-- Auto-approve (18-02): GateChainRunner.runAndRecompute, after GATES_PASSED, fires APPROVE when OnboardingProperties.isAutoApprove()==true (default false). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-gate end-to-end fully-automatic path integration test</name>
  <files>core-java/src/test/java/uk/jtoye/core/onboarding/VendorOnboardingEndToEndIntegrationTest.java</files>
  <read_first>
    - core-java/src/test/java/uk/jtoye/core/security/RlsContractTest.java (Testcontainers Postgres wiring)
    - .planning/phases/18-vendor-onboarding-first-slice/18-03-SUMMARY.md and 18-04-SUMMARY.md (how FhrsClient / CompaniesHouseClient are stubbed — reuse the ExchangeFunction stub approach or mock the client beans)
    - core-java/src/main/java/uk/jtoye/core/onboarding/GateChainRunner.java (async run + recompute; runAndRecompute auto-fires APPROVE when onboarding.auto-approve=true — the test must await the async completion and assert the auto-advance)
  </read_first>
  <action>
    Create `VendorOnboardingEndToEndIntegrationTest` (`@SpringBootTest @Testcontainers @ActiveProfiles("test") @Tag("testcontainers")`, Postgres wiring per RlsContractTest). Replace the two external HTTP clients with test doubles that return green results: use `@MockBean` on `FhrsClient` (return a rating-5 establishment) and `CompaniesHouseClient` (return an `active` profile), OR provide a `@TestConfiguration` that supplies stubbed client beans. Seed a fully allergen-complete shop + products (V41 fields set) so AllergenCompletenessGate passes for real. Use `@SpyBean OnboardingProperties` so `isAutoApprove()` can be toggled per test method (single container, deterministic).

    Primary test — the FULLY-AUTOMATIC path (proves the headline "go live without manual review", HIGH-1): stub `onboardingProperties.isAutoApprove()` → true. Drive with the tenant context set: `POST /onboarding` (DRAFT) → `POST /onboarding/submit` → await the `@Async` gate chain (bounded poll on `GET /onboarding/me` until status leaves VERIFYING, with a bounded timeout — e.g. an Awaitility-style loop using existing test utilities or a simple polling loop; do not add a new dependency; fail fast on timeout). Assert the three gate rows (BUSINESS_VERIFIED, FOOD_HYGIENE_RATING, ALLERGEN_DATA_COMPLETE) are all PASSED and that the onboarding AUTO-REACHED `APPROVED` (approvedAt non-null) with **NO admin/service-only APPROVE call anywhere in the test** — the auto-approve recompute must be the only thing that advanced it past PENDING_APPROVAL. Then `POST /onboarding/go-live` → assert onboarding `LIVE` and `shops.published = true` (`Boolean.TRUE`) for the seeded shop. Also assert the three PENDING gate rows were materialised on submit (proves the registry wired all three gate beans).

    Toggle test — documents the guard: stub `isAutoApprove()` → false, re-run `POST /onboarding` → `POST /onboarding/submit` → await the gate chain. Assert all three gate rows PASSED but the onboarding HALTS at `PENDING_APPROVAL` (no auto-advance), and that `POST /onboarding/go-live` from PENDING_APPROVAL is rejected (400) — i.e. without auto-approve, and with no admin-approve endpoint in this slice, the shop stays unpublished. Keep both async awaits bounded and deterministic. The test must NOT invoke any direct service/admin/controller APPROVE path to advance state — the only advancement to APPROVED is via the auto-approve recompute.
  </action>
  <verify>
    <automated>cd core-java && ./gradlew test --tests 'uk.jtoye.core.onboarding.VendorOnboardingEndToEndIntegrationTest' -x checkstyleMain 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Submit materialises exactly 3 gate rows (BUSINESS_VERIFIED, FOOD_HYGIENE_RATING, ALLERGEN_DATA_COMPLETE), all reach PASSED with the stubbed-green clients + complete catalogue.
    - With auto-approve=true the onboarding AUTO-reaches APPROVED (approvedAt non-null) with NO APPROVE call in the test, then go-live → LIVE and the seeded shop's published becomes Boolean.TRUE.
    - With auto-approve=false the same green onboarding halts at PENDING_APPROVAL and go-live is rejected (400) — the toggle is proven both ways.
    - The test advances to APPROVED only via the auto-approve recompute (no direct service/controller APPROVE invocation used to move state).
    - The async await is bounded (test does not hang; fails fast on timeout).
  </acceptance_criteria>
  <done>The three slices compose into the headline capability: a vendor submits, all gates pass automatically, auto-approve advances the onboarding with no manual review, and the vendor's go-live publishes the shop — proven on real Postgres, with the false-toggle documenting the guard.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile docs/metrics.json + register VOB requirements + ROADMAP/CHANGELOG closure</name>
  <files>docs/metrics.json, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, CHANGELOG.md</files>
  <read_first>
    - scripts/docs-freshness.sh (counts are derived from git-tracked files; run --write to regenerate the manifest)
    - .planning/REQUIREMENTS.md (the (SEC)/(CQ)/(AUDIT-W0) section format + the coverage table to extend)
    - .planning/ROADMAP.md (Phase 18 entry to finalise)
  </read_first>
  <action>
    Run `scripts/docs-freshness.sh --write` to regenerate `docs/metrics.json` from source (this reconciles `schema_version` → 43, `java_controllers` → 15 including OnboardingController, and the java_test_methods/java_test_files/total_logical_invocations to the real post-phase counts). Then run `scripts/docs-freshness.sh` (check mode) and confirm it exits 0. Do NOT hand-edit the numbers — let the script compute them. NOTE: all onboarding test files (18-01..18-05) must be git-tracked/staged for the counter to include them, since docs-freshness counts `git ls-files --cached --others --exclude-standard`.

    In `.planning/REQUIREMENTS.md`, add a new section `### Vendor onboarding (VOB) — added 2026-07-10` mirroring the existing category sections, registering five requirements derived from the ROADMAP Phase 18 success criteria:
      - VOB-01: tenant-scoped vendor_onboarding aggregate + state machine under RLS (Flyway V43), sole writer of Shop.published, with `onboarding.auto-approve` auto-advancing PENDING_APPROVAL → APPROVED so a fully-passing onboarding can reach live without manual review — Phase 18 (Plans 18-01, 18-02, 18-05).
      - VOB-02: automatic BUSINESS_VERIFIED (Companies House) + FOOD_HYGIENE_RATING (FSA FHRS, min-rating=2, x-api-version:2) gates on submit, recording pass/fail + evidence, MANUAL_REVIEW on no/ambiguous match — Phase 18 (Plans 18-03, 18-04).
      - VOB-03: ALLERGEN_DATA_COMPLETE gate blocks GO_LIVE until every product carries required V41 allergen data — Phase 18 (Plan 18-05).
      - VOB-04: FHRS threshold + API base URLs injected via onboarding.* config (${ENV:default}), never literals — Phase 18 (Plan 18-01).
      - VOB-05: tests (state-machine, RLS Testcontainers, gate evaluators, auto-approve toggle, cross-gate e2e) + docs/metrics.json bump keeping docs-freshness green — Phase 18 (Plans 18-01..18-06).
    Mark each `[x]` (delivered this phase). Add the five rows to the coverage table and update the Coverage totals line accordingly.

    In `.planning/ROADMAP.md`, finalise the Phase 18 entry: set the Requirements line to `VOB-01, VOB-02, VOB-03, VOB-04, VOB-05`, set Plans to 6, add the plan list (18-01..18-06 with one-line objectives), and mark the phase row/status appropriately in the Progress table. Add a `[Unreleased]` CHANGELOG.md entry summarising the vendor-onboarding first slice (V43 migration, onboarding state machine as sole Shop.published writer, three free-API/computed gates, config-driven auto-approve, MANUAL_REVIEW human fallback).
  </action>
  <verify>
    <automated>cd /home/sanmi/IdeaProjects/JToye_OaaS_2026 && git add -A && scripts/docs-freshness.sh 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/docs-freshness.sh` exits 0 (check mode); `grep '"schema_version": 43' docs/metrics.json` matches; `grep '"java_controllers": 15' docs/metrics.json` matches.
    - REQUIREMENTS.md contains VOB-01..VOB-05, each `[x]`, plus five coverage-table rows and an updated totals line.
    - ROADMAP Phase 18 lists 6 plans and the VOB requirement IDs; CHANGELOG has an [Unreleased] vendor-onboarding entry.
  </acceptance_criteria>
  <done>docs-freshness is green with reconciled counts; VOB-01..05 are registered and mapped; ROADMAP + CHANGELOG reflect the shipped slice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate chain → go-live publish | The composed flow must not publish unless all gates pass; verified end-to-end |
| auto-approve policy → APPROVED | Auto-approve may skip human review but must not skip the APPROVE guard; the e2e proves gates are all PASSED before auto-advance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06-T | Tampering | regression re-opens a bypass (publish without gates) | mitigate | End-to-end test asserts the full gated path with auto-approve — the onboarding only reaches APPROVED after all mandatory gates PASSED (APPROVE guard still enforced); RlsContractTest continues to guard RLS drift on the new tables |
| T-18-06-SC | Tampering | new package installs | accept | No new gradle/npm dependency added (async await uses a bounded polling loop, not a new library); package-legitimacy gate not triggered |
</threat_model>

<verification>
- `./gradlew test --tests 'uk.jtoye.core.onboarding.*' --tests 'uk.jtoye.core.security.RlsContractTest'` green.
- `scripts/docs-freshness.sh` exits 0.
</verification>

<success_criteria>
- Full fully-automatic happy-path composition proven on real Postgres (all gates green → auto-APPROVED with no manual review → go-live → published=true), plus the auto-approve=false toggle halting at PENDING_APPROVAL.
- docs-freshness gate green with reconciled counts; VOB-01..05 registered and mapped; ROADMAP/CHANGELOG finalised.
</success_criteria>

<output>
Create `.planning/phases/18-vendor-onboarding-first-slice/18-06-SUMMARY.md` when done.
</output>
</content>
