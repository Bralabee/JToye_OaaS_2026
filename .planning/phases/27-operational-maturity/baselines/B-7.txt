BASELINE: B-7
CAPTURED: 2026-07-27T12:02:15+00:00
TREE: 1499494b5535e35ea02a16cab9f7ca4a665298a6  clean=yes
CLOSED_BY: AC-1.3
SUBJECT: Two alerts labelled service: core-java are evaluating KEYCLOAK's JVM (F-3c). HighMemoryUsage
  (alerts.yml:96-109) and FrequentGarbageCollection (:111-124) use unqualified jvm_* selectors; with
  core-java's target down they bind to the only JVM Prometheus can see. They are NOT dataless — they
  are worse: a naive "selector matches >= 1 series" liveness gate reports them GREEN while they are
  completely blind to the service they name. This is the measurement that falsifies the drafted gate
  and forces the subject-correctness assertion L-2b / AC-4.3.
COMMAND: curl -sG http://localhost:9091/api/v1/query --data-urlencode 'query=count(jvm_memory_used_bytes) by (job)' | jq -c '.data.result[] | {job:.metric.job, count:.value[1]}'  ;  curl -sG http://localhost:9091/api/v1/query --data-urlencode 'query=count(jvm_gc_pause_seconds_count) by (job)' | jq -c '.data.result[] | {job:.metric.job, count:.value[1]}'
EXIT: 0
--- OUTPUT ---
count(jvm_memory_used_bytes) by (job):
{"job":"keycloak","count":"8"}
                                        # ONE job. No job="core-java" row exists.

count(jvm_gc_pause_seconds_count) by (job):
{"job":"keycloak","count":"1"}
                                        # ONE job. No job="core-java" row exists.

The two rules, read live from the evaluator, claiming a subject they cannot see:
{"name":"HighMemoryUsage","service":"core-java","query":"(jvm_memory_used_bytes{area=\"heap\"} / jvm_memory_max_bytes{area=\"heap\"}) * 100 > 85","health":"ok","state":"inactive"}
{"name":"FrequentGarbageCollection","service":"core-java","query":"rate(jvm_gc_pause_seconds_count[5m]) > 50","health":"ok","state":"inactive"}
--- CONTROL COMMAND ---
docker exec jtoye_oaas_2026-core-java-1 sh -c 'echo "jvm_memory_used_bytes on :9090 = $(wget -qO- http://localhost:9090/actuator/prometheus | grep -c "^jvm_memory_used_bytes{")"; echo "jvm_gc_pause_seconds_count on :9090 = $(wget -qO- http://localhost:9090/actuator/prometheus | grep -c "^jvm_gc_pause_seconds_count{")"'
CONTROL_EXIT: 0
--- CONTROL OUTPUT ---
jvm_memory_used_bytes on :9090 = 8
jvm_gc_pause_seconds_count on :9090 = 2
--- WHY THIS CONTROL ---
"No job=core-java row" is consistent with two very different worlds: core-java does not publish JVM
metrics at all, or it publishes them on a port nobody scrapes. The control reads the application's
own exposition endpoint and finds 8 jvm_memory_used_bytes series and 2 jvm_gc_pause_seconds_count
series sitting there unscraped. The metrics exist; Prometheus is looking at the wrong port (B-1).
That is what makes AC-1.3's post-fix re-measurement non-trivial — before the fix there was no
core-java JVM series for a subject-correctness check to find, so the check could not have been
proven to work on this tree without the fix landing first.
--- RE-MEASUREMENT OWED AT AC-1.3 ---
After Task 1, `count(jvm_memory_used_bytes) by (job)` must return BOTH job="core-java" and
job="keycloak". Record that output next to this one.
--- FOR THE RECORD, SAME INSTANT ---
ServiceDown{job="core-java"} state=firing activeAt=2026-07-25T13:01:41.425388701Z  (>46h)
alertmanager_notifications_total{integration="email"} = 5   failed_total = 0
Mailhog most recent delivery: 2026-07-27T11:44:11Z -> ops@jtoye.local   (a dev sink, no human)
docker inspect jtoye-alertmanager | grep -c ALERTMANAGER_SLACK = 0
  — only ALERTMANAGER_SMTP_{FROM,REQUIRE_TLS,SMARTHOST,TO} reach the container, confirming the
  plan's objective note #2: the Slack keys reach nothing, and the compose file is the reason.
