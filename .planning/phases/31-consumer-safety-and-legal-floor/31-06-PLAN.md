---
phase: 31-consumer-safety-and-legal-floor
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/retention-manifest.json
  - scripts/check-retention-enforcement.sh
  - .github/workflows/ci-cd.yaml
  - core-java/src/main/resources/application.yml
  - .planning/codebase/INTEGRATIONS.md
autonomous: true
requirements: [LGL-01]
must_haves:
  truths:
    - "Every retention period the platform is about to publish as Automated has a real enforcement site in code or config"
    - "A published period whose enforcement site is deleted or emptied reds CI"
    - "The dead cleanup key that nothing enforces is gone from config and from the docs that quoted it"
    - "The gate exits non-zero on missing tooling, an unreadable input or an empty discovery set"
  artifacts:
    - path: "docs/retention-manifest.json"
      provides: "The machine-readable source of truth for every published retention period"
      contains: "enforced_by"
    - path: "scripts/check-retention-enforcement.sh"
      provides: "Tree-to-manifest gate: every Automated row names a real enforcement site holding the declared value"
    - path: ".github/workflows/ci-cd.yaml"
      provides: "The workflow reference that must land in the SAME commit as the script"
      contains: "check-retention-enforcement.sh"
  key_links:
    - from: "scripts/check-retention-enforcement.sh"
      to: "core-java/src/main/resources/application.yml"
      via: "reads each Automated row's enforced_by path and asserts the declared value is present there, converted to the manifest's unit"
      pattern: "retention|cleanup"
---

<objective>
Land the machine-readable retention schedule and the gate that ties it to what the system actually
enforces — script and workflow reference in **one commit**, because no two-commit ordering is green.

Purpose: D-07 requires the published schedule's periods to come from what the system **measurably
enforces today**, not from aspiration, and D-08 requires a gate that fails when a published period
has no corresponding enforcement. That gate already has a live example of what it is for:
`cleanup.orphaned-image-days: 7` is declared at `application.yml:719` and has **zero Java consumers**,
verified two-arm against an identically shaped control that returned four hits. Publishing it as a
7-day automated deletion would put a false statement in a legally operative document.

This plan deliberately ships **no page**. 31-12 renders the table and adds the `claims.manifest` rows
that assert the page quotes these numbers. Splitting it this way is what keeps this plan's
`ci-cd.yaml` edit in wave 1 and 31-18's `ci-cd.yaml` edit in wave 5, with no same-wave collision on
that file.

Output: `docs/retention-manifest.json`, one new gate wired into CI in the same commit, and one dead
config key removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-consumer-safety-and-legal-floor/31-CONTEXT.md
@.planning/phases/31-consumer-safety-and-legal-floor/31-RESEARCH.md
@.planning/phases/31-consumer-safety-and-legal-floor/31-PATTERNS.md
@scripts/check-no-create-extension.sh
@scripts/gates/gate-enforcement.conf

<interfaces>
⚠ THE GATE-ENFORCEMENT DOUBLE BIND — measured in BOTH directions during research, with the restore
verified by content hash. `scripts/check-gate-enforcement.sh` is DEFAULT-DENY and fails in OPPOSITE
directions:

    a new scripts/check-*.sh with NO workflow reference        -> rc=1
    a gate-enforcement.conf entry naming a nonexistent script  -> rc=2 (VOID)

Because the two failures point opposite ways, **no ordering of two separate commits is green at both
points.** `scripts/check-retention-enforcement.sh` and its `ci-cd.yaml` step MUST land in the SAME
task and the SAME commit. Phase 33's 33-05/33-06 split is the worked precedent; do not collapse it
and do not attempt a two-commit sequence.

⚠ THIS GATE IS STATIC AND MUST BE WIRED, NEVER EXEMPTED. It reads YAML, Java and TypeScript source;
it invokes no `docker`, `psql` or `curl`. `check-gate-enforcement.sh` rejects a conf entry for a
script that invokes no runtime binary as a **stale exemption (rc=1)**, and the conf's own stated bar
is "this gate inspects something a GitHub-hosted runner does not have" — which is false here. So it
gets a `ci-cd.yaml` step, in the `check-no-create-extension.sh` shape at `ci-cd.yaml:684-689`.

THE MEASURED PERIODS (RESEARCH Q3 — re-verify each before publishing; do NOT quote them forward):

  R-1  Abandoned DRAFT orders, incl. guest PII on the order row.
       config/ScheduledCleanupService.java:57 @Scheduled(cron="0 0 3 * * *") -> cleanupStaleDraftOrders()
       24 hours · key cleanup.stale-draft-hours · application.yml:718 · AUTOMATED
  R-2  Webhook delivery records.
       webhook/WebhookRetentionCleanup.java:50 @Scheduled(fixedDelayString=...) -> pruneExpired()
       30 days · key webhook.delivery.retention-days · application.yml:634 · AUTOMATED
  R-3  Quarantined raw uploads (unvalidated user bytes in MinIO).
       media/MediaQuarantineRetentionSweep.java:104
       72 hours, stored as 259200000 ms · key jtoye.media.quarantine-retention-ms · application.yml:291 · AUTOMATED
  R-4  Customer session cookies (jtoye-customer-refresh, jtoye-customer-id).
       frontend/lib/customer-auth-cookies.ts:29 REFRESH_MAX_AGE = 60*60*24*30
       30 days · a TS constant, not a config key · AUTOMATED (browser-enforced)
  R-5  Customer access cookie (jtoye-customer-access).
       maxAge derived from the Keycloak realm's accessTokenLifespan (300s).
       DESCRIBE, DO NOT GATE — the number lives in the IdP realm, not in this repo. A gate against a
       value this repo does not own becomes a lie the first time the realm changes.
  R-6  Marketing opt-in / suppression rows — INDEFINITE BY DESIGN.
       V54's header: a GDPR/PECR opt-out must NOT expire, so there is deliberately no retention
       window or prune job on suppression (threat T-22-02-04); restated at
       WebhookRetentionCleanup.java:24-27. Gateable as a NEGATIVE — assert no prune path exists.
       QUOTE that header; do not paraphrase it.
  R-7  Customer PII across customers/orders/reviews and their _aud mirrors.
       gdpr/GdprService.eraseCustomerData — anonymise, not delete · on request, no time limit · OPERATIONAL
  R-8  Envers audit history (*_aud) — no deletion path anywhere · indefinite · OPERATIONAL
  R-9  Financial transactions / completed orders — no deletion path found · OPERATIONAL.
       ⚠ The ~6-year HMRC figure is [ASSUMED] — a legal position, not a measurement. Publish
       "retained for as long as required by law", never an unconfirmed number.
  R-10 Orphaned product images — cleanup.orphaned-image-days at application.yml:719 has ZERO Java
       consumers. Two-arm verified: rc=1/empty against a control (cleanup.stale-draft-hours)
       returning rc=0 with 4 hits, same globs, same flags, same directory. UNENFORCED — DELETE THE KEY.

⚠ UNIT DRIFT IS THE TRAP (Pitfall 7). The claim-gate engine has NO unit transform: `kind` is only
`json` or `regex`, `shape` only `int` or `semver`. So 259200000 ms and "72 hours" would silently
never compare. Keep HUMAN units in `docs/retention-manifest.json` and let THIS script own the
ms-to-hours conversion.

⚠ THE SELF-MATCH TRAP. A gate that names a required or forbidden token can fire on its own
definition. `check-no-create-extension.sh:34-44` documents it and solves it by scoping the scan to a
directory the script does not live in. Do the same, or assemble the token from fragments.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the retention manifest and remove the period nothing enforces</name>
  <files>docs/retention-manifest.json, core-java/src/main/resources/application.yml, .planning/codebase/INTEGRATIONS.md</files>
  <read_first>
    - docs/metrics.json (the other claim-gate `source` file — match its shape so one engine reads both)
    - core-java/src/main/resources/application.yml around lines 285-295, 630-640 and 712-722 (the four config keys, read in place)
    - core-java/src/main/java/uk/jtoye/core/config/ScheduledCleanupService.java:30-60
    - core-java/src/main/java/uk/jtoye/core/webhook/WebhookRetentionCleanup.java:17-60 (its header states R-6's negative verbatim)
    - core-java/src/main/java/uk/jtoye/core/media/MediaQuarantineRetentionSweep.java:95-110
    - frontend/lib/customer-auth-cookies.ts:20-40
    - .planning/codebase/INTEGRATIONS.md:260-275 (where the dead key is also quoted)
  </read_first>
  <action>
**Re-measure every row before writing it.** RESEARCH's Q3 table was measured 2026-08-15; a period
about to be published in a legally operative document is exactly the claim that must not be quoted
forward. For each of R-1 through R-10, open the enforcement site and read the number out of it.

Write `docs/retention-manifest.json` as a JSON array of rows. Each row carries a stable id; the data
category in the words the published page will use; the period as a **number plus a human unit**
(hours, days, or the literal indefinite); the lawful basis; an enforcement class that is exactly
`Automated` or `Operational`; and, for every `Automated` row, an `enforced_by` object naming the file
path and the config key or constant that holds the value, plus the unit that site stores it in.

Rules that make the file gateable rather than decorative:

  - **`Automated` means a gate can read the value out of a named file.** R-5 is enforced, but the
    value lives in the Keycloak realm, so it is published descriptively and classed `Operational`
    with a note. Do not gate a number this repo does not own.
  - **R-6 is an `Automated` NEGATIVE.** Record it as an explicit assertion that no prune path exists,
    with `WebhookRetentionCleanup.java:24-27` as its enforcement site. A GDPR opt-out that expires is
    the defect; the gate asserts the absence.
  - **No placeholder ships.** A `TBD` in a retention schedule is a defect, not a draft.
  - **R-9 carries no number.** The HMRC figure is `[ASSUMED]`; the row says "as long as required by
    law" and is `Operational`.
  - For R-3 record 72 with unit hours as the published period, and 259200000 with unit milliseconds
    inside `enforced_by`. The conversion is the script's job; putting the raw ms in the published
    period is how the page and the manifest silently stop comparing.

**Then delete the dead key.** Remove `cleanup.orphaned-image-days` from `application.yml` and remove
the sentence quoting it from `.planning/codebase/INTEGRATIONS.md`. Before deleting, re-run the
two-arm consumer search with a live control of the same shape — `cleanup.stale-draft-hours` must
return hits under the identical globs and flags, or the empty result is a fact about the search
rather than about the code. Record both arms' real output.

Deleting is preferable to publishing nothing about orphaned images: a config key promising a deletion
nobody performs is exactly the drift D-08 exists to catch, and leaving it invites someone to publish
it later.
  </action>
  <acceptance_criteria>
    - `docs/retention-manifest.json` parses under `jq` and contains at least 10 rows.
    - Every row's `enforcement` value is exactly `Automated` or `Operational` — asserted with `jq` over the distinct set, not by spot-check.
    - Every `Automated` row has a non-empty `enforced_by.path`, and every one of those paths exists on disk — asserted by iterating all of them.
    - No row's period value is `TBD`, empty or null.
    - `grep -c 'orphaned-image-days'` returns 0 in both `core-java/src/main/resources/application.yml` and `.planning/codebase/INTEGRATIONS.md`.
    - The two-arm consumer search is recorded in the SUMMARY: the dead key rc=1/empty AND the control key rc=0 with hits, under identical globs and flags.
    - `./gradlew :core-java:test --rerun-tasks` is green after the key removal — proving nothing read it, which is the claim that justified deleting it.
    - FAIL DIRECTION, run and recorded: point one `Automated` row's `enforced_by.path` at a file that does not exist and confirm the path-existence assertion fires. Commit first; restore by `git hash-object`; re-run clean LAST.
  </acceptance_criteria>
  <verify>
    <automated>jq -e 'length >= 10' docs/retention-manifest.json > /dev/null || exit 1; jq -e '[.[].enforcement] | unique | inside(["Automated","Operational"])' docs/retention-manifest.json > /dev/null || exit 1; for p in $(jq -r '.[] | select(.enforcement=="Automated") | .enforced_by.path' docs/retention-manifest.json); do test -e "$p" || { echo "missing enforcement site: $p"; exit 1; }; done; jq -e '[.[] | select(.period_value == null or .period_value == "TBD")] | length == 0' docs/retention-manifest.json > /dev/null || exit 1; out=$(grep -cF 'orphaned-image-days' core-java/src/main/resources/application.yml || true); grep -qx 0 <<< "$out" || exit 1; out=$(grep -cF 'orphaned-image-days' .planning/codebase/INTEGRATIONS.md || true); grep -qx 0 <<< "$out" || exit 1; ./gradlew :core-java:test --rerun-tasks</automated>
  </verify>
  <done>
The published schedule's source of truth exists, every `Automated` row names a real enforcement site,
and the one period nothing enforced is gone from config and from the doc that quoted it — with the
two-arm search recorded so the empty result is a fact about the code and not about the search. The
Java suite is green without the key, which is the evidence that removing it was safe.
  </done>
</task>

<task type="auto">
  <name>Task 2: The enforcement gate and its workflow reference — one commit</name>
  <files>scripts/check-retention-enforcement.sh, .github/workflows/ci-cd.yaml</files>
  <read_first>
    - scripts/check-no-create-extension.sh (all 155 lines — the exit-code contract at :26-32, the two shell hazards at :34-44, the overridable input at :46-61, the enumerate-then-refuse-on-empty at :63-67, and the exemption-by-addition with a VOID when the table stops describing the tree at :105-108 and :144-148)
    - .github/workflows/ci-cd.yaml:675-695 (the gate step shape to copy, including the comment explaining why a static gate lives in ci-cd.yaml)
    - .github/workflows/ci-cd.yaml around the check-gate-enforcement step (so the new step is placed with its siblings)
    - scripts/gates/gate-enforcement.conf (its header states the bar for an exemption — read it to confirm this gate does NOT meet it)
    - docs/retention-manifest.json (from Task 1)
  </read_first>
  <action>
**Both files change in ONE commit.** The script alone reds `check-gate-enforcement.sh` at rc=1; a conf
entry alone VOIDs it at rc=2. There is no green ordering of two commits. Do not add a
`gate-enforcement.conf` entry — this gate is static and must be wired.

Write `scripts/check-retention-enforcement.sh` in the `check-no-create-extension.sh` mould:
`set -uo pipefail`, `SCRIPT_DIR`/`REPO_ROOT` resolution, `fail()` and `void()` helpers, and an
**overridable input** (`MANIFEST` defaulting to `docs/retention-manifest.json`) so the VOID direction
is testable against an empty or absent file without inventing a second code path. That overridable
input is the single most useful idea to copy from the analog.

The script asserts three things the claim-gate engine cannot:

  1. **Existence.** Every `Automated` row's `enforced_by.path` exists AND contains the declared config
     key or constant. This is the R-10 case: a published period whose enforcement site is empty must
     exit 1, naming the row.
  2. **Value, converted.** The value at that site, converted into the manifest's published unit,
     equals the manifest's number. 259200000 ms must compare equal to 72 hours. Own the conversion
     here — the engine has none, so without this the two silently never compare.
  3. **The negative.** R-6: assert that no prune or delete path exists against
     `notification_suppression` or `marketing_opt_in`. Scope the scan to `core-java/src/main/java` by
     absolute path so the script cannot match its own definition, and give this assertion a **positive
     control** — the same scan shape must find the prune path that DOES exist for `webhook_delivery`,
     or a zero result is a statement about the search rather than about the code.

Exit **0 / 1 / 2**, and VOID at 2 on: a missing or unparseable manifest, missing `jq`, a manifest with
zero rows, zero `Automated` rows, or an unreadable enforcement site. "Found nothing" is never
"clean" — a zero-row scan reporting clean is the vacuous shape this repo has been bitten by
repeatedly.

Shell hazards, both already documented in the analog's own header and both binding here:
`cmd | grep -q X` under `pipefail` INVERTS on match via SIGPIPE, so use here-strings only; and
`grep -c` exits 1 on a zero count, i.e. on the DESIRED state of an absence check, so capture counts
with `|| true` and compare with `grep -qx 0 <<< "$out"`.

Add the `ci-cd.yaml` step in the `check-no-create-extension.sh` shape: `chmod +x` then run, with a
comment stating that the gate is static by construction — it reads files, touches no database and
makes no network call, so it says the same thing on a hosted runner as it does locally, which is why
it belongs in `ci-cd.yaml` and why it is wired rather than exempted.
  </action>
  <acceptance_criteria>
    - `bash scripts/check-retention-enforcement.sh` exits 0 on the clean tree and prints the number of rows scanned and the number of `Automated` rows checked.
    - `bash scripts/check-gate-enforcement.sh` exits 0 **after** this commit — proving the workflow reference satisfies the default-deny check. Its `gates:` count must be one higher than before, and both counts are recorded.
    - `.github/workflows/ci-cd.yaml` contains `check-retention-enforcement.sh`, and `scripts/gates/gate-enforcement.conf` does NOT — this gate is wired, not exempted.
    - The script and the workflow reference are in the same commit — assert with `git show --stat HEAD` listing both paths.
    - FAIL DIRECTION, four arms, all run and recorded:
      (a) **rc=1 existence** — point an `Automated` row at a real file that does not contain its key; the gate must exit 1 naming the row.
      (b) **rc=1 value** — change the manifest's 72 hours to 71 while `application.yml` still holds 259200000 ms; the gate must exit 1. This is the arm that proves the unit conversion actually runs; without it the two values never compare and the gate is decorative.
      (c) **rc=2 VOID** — run with `MANIFEST` pointing at an empty file; the gate must exit 2, not 0.
      (d) **the double bind, both directions** — with the workflow step temporarily removed, `check-gate-enforcement.sh` must exit **1** naming this script; with the step restored and a conf entry added instead, it must exit **1** as a stale exemption (the script invokes no runtime binary). Record both. This is the measurement that proves the same-commit rule was necessary rather than cautious.
    - Commit before the arms; restore each by `git hash-object` against `git rev-parse HEAD:<path>`; re-run the clean direction LAST for both scripts.
  </acceptance_criteria>
  <verify>
    <automated>chmod +x scripts/check-retention-enforcement.sh; bash scripts/check-retention-enforcement.sh; r=$?; test "$r" -eq 2 && { echo "VOID: retention gate could not scan"; exit 2; }; test "$r" -eq 0 || exit 1; MANIFEST=/dev/null bash scripts/check-retention-enforcement.sh; v=$?; test "$v" -eq 2 || { echo "gate failed to VOID on an empty manifest — it can report clean over nothing"; exit 1; }; bash scripts/check-gate-enforcement.sh; g=$?; test "$g" -eq 2 && { echo "VOID: gate enforcement could not enumerate"; exit 2; }; test "$g" -eq 0 || exit 1; out=$(grep -cF 'check-retention-enforcement.sh' .github/workflows/ci-cd.yaml || true); test "$out" -ge 1 || exit 1; out=$(grep -cF 'check-retention-enforcement.sh' scripts/gates/gate-enforcement.conf || true); grep -qx 0 <<< "$out" || exit 1</automated>
  </verify>
  <done>
The published retention periods are tied to their enforcement by a gate that runs on every PR, exits
0/1/2 with a real VOID arm, converts units so the ms-versus-hours comparison actually happens, and
carries a positive control on its negative assertion. The script and its workflow reference are in
one commit, and the double bind was measured in both directions rather than assumed —
`check-gate-enforcement.sh` is green, with the before/after `gates:` counts recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree -> `docs/retention-manifest.json` | Measured behaviour crosses into a legally operative published claim |
| manifest -> CI verdict | A JSON file decides whether a PR is blocked |
| new gate -> `check-gate-enforcement.sh` | A new script crosses a default-deny meta-gate that fails in two opposite directions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06-01 | Repudiation | Publishing a retention period nothing enforces | mitigate | The `Automated`/`Operational` split; existence and value assertions over every `Automated` row; `cleanup.orphaned-image-days` deleted rather than published |
| T-31-06-02 | Repudiation | The gate failing OPEN on missing tooling or an empty input | mitigate | Exit 2 (VOID) on a missing/unparseable manifest, missing `jq`, zero rows or zero `Automated` rows; the VOID arm is run via the overridable `MANIFEST` input |
| T-31-06-03 | Repudiation | Unit drift making the comparison vacuous | mitigate | The script owns the ms-to-hours conversion; the 72-to-71 break arm proves the comparison runs |
| T-31-06-04 | Tampering | A GDPR/PECR opt-out acquiring an expiry | mitigate | R-6's negative assertion, scoped by absolute path so the script cannot match itself, with a positive control proving the scan finds the prune path that does exist |
| T-31-06-05 | Repudiation | A guard that fails open under `pipefail` | mitigate | Here-strings only; counts captured with `|| true`; `rc` captured on the same statement |
| T-31-06-06 | Denial of service | An unsatisfiable required gate teaching people to add `|| true` | mitigate | The gate is static and wired rather than exempted, so it is satisfiable on a hosted runner by construction |
| T-31-06-SC | Tampering | package installs | accept | No package added; the gate is bash plus `jq`, both already required by the existing claim-gate engine |
</threat_model>

<verification>
## Control arms — each criterion, and the break that proves it can fail

| Criterion | Deliberate break | Expected failure |
|---|---|---|
| "Every Automated period has an enforcement site" | today's tree — `cleanup.orphaned-image-days` has zero consumers | that row would fail the gate; it is deleted instead, and the two-arm search proves the absence is real |
| "…that actually holds the key" | point a row at a real file lacking its key | rc=1 naming the row |
| "…at the declared value, converted" | manifest says 71 hours while the site says 259200000 ms | rc=1 — the arm that proves the conversion runs at all |
| "The gate cannot report clean over nothing" | `MANIFEST=/dev/null` | rc=2 VOID, not 0 |
| "An opt-out never expires" | add a prune path against `notification_suppression` | the negative assertion fires |
| "…and that scan is live" | narrow the scan so it finds nothing anywhere | the positive control on `webhook_delivery` fires |
| "The gate is wired, not exempted" | remove the `ci-cd.yaml` step | `check-gate-enforcement.sh` exits 1 naming this script |
| "…and a conf entry is not a substitute" | add a `gate-enforcement.conf` entry instead | `check-gate-enforcement.sh` exits 1 as a stale exemption — the script invokes no runtime binary |

## Instrument notes

- **The double bind is the reason for the same-commit rule**, and it is measured here rather than
  cited. Both directions are run: script-without-reference (rc=1) and conf-entry-instead (rc=1 stale).
- **A VOID is not a pass.** Every gate call in this plan's verify distinguishes 0, 1 and 2 explicitly.
- `cmd | grep -q X` under `pipefail` INVERTS on match via SIGPIPE — here-strings only.
- `grep -c` exits 1 on a zero count; always `out=$(grep -cF X f || true); grep -qx 0 <<< "$out"`.
- Capture `rc` on the same statement as its command; `$?` after an intervening command reports the
  wrong command's status.
- **Commit before any break arm.** `git checkout` restores from the INDEX and discards post-staging
  edits. Verify each restore by `git hash-object`, never by `git diff --stat`.
- **A truncating filter used to prove absence manufactures that absence.** The R-10 and R-6 scans
  must not pipe through `head`.
- `docs/metrics.json` is generated and is absent from `files_modified`.
</verification>

<success_criteria>
- `docs/retention-manifest.json` exists, parses, has >= 10 rows, uses only `Automated`/`Operational`,
  carries no placeholder, and every `Automated` row names an enforcement site that exists.
- `cleanup.orphaned-image-days` is gone from `application.yml` and from
  `.planning/codebase/INTEGRATIONS.md`, with the two-arm search recorded and the Java suite green
  without it.
- `scripts/check-retention-enforcement.sh` exits 0 on the clean tree, 1 on a missing or mismatched
  enforcement site, and 2 on an empty or unreadable manifest — with all four fail arms run and both
  directions recorded.
- The script and its `ci-cd.yaml` step are in the SAME commit, and `scripts/check-gate-enforcement.sh`
  exits 0 afterwards with its `gates:` count one higher than before.
- The gate is wired into a workflow and has NO `gate-enforcement.conf` entry.
- The double bind was measured in both directions, not assumed.
</success_criteria>

<output>
Create `.planning/phases/31-consumer-safety-and-legal-floor/31-06-SUMMARY.md` when done.
Record: the final row list with each period, unit, enforcement class and enforcement site — 31-12
renders exactly these and must not re-derive them; the two-arm output for the `orphaned-image-days`
removal; the `check-gate-enforcement.sh` `gates:` count before and after; and BOTH directions' output
for all four gate break arms including the double bind.
</output>
