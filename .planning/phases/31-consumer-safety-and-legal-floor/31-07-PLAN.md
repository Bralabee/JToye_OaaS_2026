---
phase: 31-consumer-safety-and-legal-floor
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/legal/article-26-arrangement.md
  - docs/legal/article-9-allergen-basis.md
autonomous: true
requirements: [LGL-01, LGL-03]
must_haves:
  truths:
    - "The joint-controller arrangement between the platform and its vendors exists as a written document with an essence that can be published"
    - "The recorded Article 9 determination states, dated, that the platform does not consult the stored customer allergen profile at checkout"
    - "Neither document contradicts the other, nor the trading line already drawn on /legal"
    - "Every legal position in both documents is marked as needing adviser confirmation, on its own face"
  artifacts:
    - path: "docs/legal/article-26-arrangement.md"
      provides: "The D-18 Article 26 arrangement, with a publishable essence section"
      contains: "essence"
    - path: "docs/legal/article-9-allergen-basis.md"
      provides: "The D-01 decision not to process the stored profile, dated and appended"
      contains: "2026-08"
  key_links:
    - from: "docs/legal/article-26-arrangement.md"
      to: "docs/legal/article-9-allergen-basis.md"
      via: "the special-category clause reuses the DPA wording the Article 9 doc already drafted, rather than restating it differently"
      pattern: "M.5|special category"
---

<objective>
Author the two legal documents this phase owes, in the place this repo already keeps legal documents,
without re-authoring or contradicting what is already there.

Purpose: D-18 requires the DPA / Article 26 arrangement to be **authored as a document in this
phase** — acceptance tracking, presentation at onboarding and e-signature are explicitly out and
belong with Phase 32. D-14 sets its substance: joint controllers for consumer order data, the
platform as sole controller for platform accounts, and the platform running the single point of
contact for data-subject requests. Article 26 requires that the **essence** of that arrangement be
made available to data subjects, which is what 31-11's privacy notice will publish.

D-01 requires a **dated, recorded decision not to process** the stored customer allergen profile at
checkout. It is important that this is scoped correctly:
`docs/legal/article-9-allergen-basis.md` **already exists** — 122 lines, dated 2026-07-30 — and
already records the controller/processor split, why Article 9(2)(a) explicit consent is the only
realistic condition, why (c)/(h)/(e) are each excluded, that the guest `customerAllergenMask` field
was removed on data-minimisation grounds, that Articles 17 and 20 are implemented and tested over the
field, and ready DPA wording. It also lists *"Write the privacy notice — there is currently none"* as
its own recommended step 4. **This phase dates and extends that document; it must not re-author it
and must not contradict it.**

Output: one new document, one appended section on an existing one. No code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-consumer-safety-and-legal-floor/31-CONTEXT.md
@.planning/phases/31-consumer-safety-and-legal-floor/31-RESEARCH.md
@docs/legal/article-9-allergen-basis.md
@docs/legal/derivation-clause.md

<interfaces>
WHAT THE EXISTING ARTICLE 9 DOCUMENT ALREADY SAYS — scope against these, do not restate them:
  ## The question
  ## Finding 1 — the stored field: the vendor is the controller, not us
  ### Why explicit consent, and not another condition
  ## Finding 2 — the guest checkout field: removed
  ## What is already satisfied
  ## What remains open   (the vendor has no mechanism to record consent, and no prompt saying one is needed)
  ### Design for the consent record (not built)
  ### DPA wording to add   (a ready block of processor-side special-category wording, ending "excluded
                            from Anonymous Statistical Data under clause M.5")
  ## Recommended next steps  (1 confirm with an adviser · 2 vendor-facing notice · 3 build the consent
                              record · 4 write the privacy notice)

Steps 2 and 4 are being satisfied elsewhere: the vendor-facing notice already exists and is asserted
by `frontend/app/dashboard/customers/__tests__/allergen-consent-notice.test.tsx`, and 31-11 writes
the privacy notice. Step 3 (build the consent record) stays OPEN — D-01 is the decision NOT to
process the field, so the consent record is not needed by this phase and must not be described as
delivered.

THE DERIVATION CLAUSE SITS BESIDE THESE — `docs/legal/derivation-clause.md` (DRAFT, 2026-07-30),
whose clause **M.5 excludes Article 9 data from all derivation**. The Article 26 document belongs
beside both and must reference M.5 rather than restating it in different words; two differently
worded versions of one exclusion is how a contradiction is created.

THE TWO LINES THAT MUST NOT COLLIDE (D-15). `frontend/app/legal/page.tsx` already draws the
**trading** line: vendors own their own trading disclosures. The **GDPR** line drawn here is
different — joint controllers for consumer order data. Both are true. The prose must say so
explicitly rather than leaving a reader to reconcile them, because a regulator reading one page and
then the other is exactly the audience.

COMPANY IDENTITY: the ACTIVE company is **16471464**. A dissolved namesake **13434105** exists and is
documented at `frontend/lib/company.ts:5-6`. Any company number in legal prose must be 16471464, and
the dissolved one must not appear. The live marketing site has already cited the wrong one once.

TONE PRECEDENT, AND IT IS THE RIGHT ONE. `derivation-clause.md` opens with:
*"This is a draft for review by a qualified solicitor, not legal advice. It was written by an
engineer to be precise about what the platform technically does, so that a lawyer has something
concrete to correct rather than a blank page."* Both documents in this plan carry the same framing.
Every legal position in RESEARCH is explicitly LOW confidence by design and says so on its own face;
do not upgrade any of it to a settled statement.

⚠ THIS PLAN AUTHORS DOCUMENTS ONLY. No acceptance flow, no e-signature, no onboarding gate, no
schema — D-18 puts all of that in Phase 32.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Article 26 joint-controller arrangement</name>
  <files>docs/legal/article-26-arrangement.md</files>
  <read_first>
    - docs/legal/derivation-clause.md (all of it — the DRAFT framing, the effectiveness gate, and clause M.5)
    - docs/legal/article-9-allergen-basis.md § "DPA wording to add" (the special-category block to reuse verbatim)
    - frontend/app/legal/page.tsx (the trading line already drawn, which this must not contradict)
    - frontend/lib/company.ts:1-50 (the identity fields and the dissolved-namesake warning)
    - .planning/phases/31-consumer-safety-and-legal-floor/31-CONTEXT.md D-14, D-15, D-16, D-18
  </read_first>
  <action>
Write `docs/legal/article-26-arrangement.md` beside the two existing legal documents, opening with
the same DRAFT framing `derivation-clause.md` uses — written by an engineer to be precise about what
the platform technically does, so a solicitor has something concrete to correct.

Cover, in this order:

  - **Status and date**, and an effectiveness gate in the `derivation-clause.md` shape: this is not
    legally effective until reviewed and until the acceptance mechanism exists (which is Phase 32's).
  - **The parties.** The platform operator by legal name and company number 16471464, and the vendor
    as the other controller. Do not name the dissolved 13434105 anywhere.
  - **The allocation (D-14).** Joint controllers for consumer **order** data — the data generated by
    a consumer buying from a vendor through the platform. The platform is **sole** controller for
    platform accounts (vendor staff accounts, the platform's own customer identity records). Say
    which is which by data category, not in the abstract, because "joint controllers" without a
    category list is unactionable.
  - **The single point of contact (D-14, D-16).** The platform runs the data-subject contact point.
    State what happens on receipt: intake is a request-thread endpoint; execution is a background
    process that acts per tenant. State that no human at the platform holds cross-tenant read of
    vendor data, because that is a real and unusual property of this design and it is the reason the
    single desk is compatible with the tenant wall. 31-05 built the intake; describe what it does,
    and do not describe capabilities that do not exist.
  - **Respective responsibilities** — transparency, subject rights, security, breach notification,
    and who answers a subject first. Article 26 requires the allocation of transparency duties in
    particular, since that is what D-15's layered notice implements.
  - **Special category data.** Reuse the block already drafted in
    `article-9-allergen-basis.md` § "DPA wording to add" **verbatim**, and cross-reference
    `derivation-clause.md` clause M.5 rather than restating the exclusion in new words.
  - **The essence, for publication.** A clearly delimited section — short, plain English, no defined
    terms — that 31-11's privacy notice reproduces or links to. Article 26(2) requires the essence to
    be made available to data subjects, so this section is the deliverable, not an appendix.
  - **What is deliberately NOT in scope here**: acceptance tracking, presentation at onboarding and
    e-signature, all of which are Phase 32 (D-18). Say so, so a reader does not assume a signature
    exists.

Do not contradict the trading line on `/legal`. Add a short paragraph naming both lines and stating
that they are different questions with different answers: who is responsible for trading disclosures
(the vendor) and who is responsible for personal data (both, jointly, for order data).
  </action>
  <acceptance_criteria>
    - `docs/legal/article-26-arrangement.md` exists and contains a section whose heading includes the word `essence`.
    - It contains `16471464` and does NOT contain `13434105` — assert both, because citing the dissolved company is a mistake this project has already made in public.
    - It contains the DRAFT / not-legal-advice framing and an effectiveness gate.
    - It references `M.5` and `derivation-clause.md` rather than restating the derivation exclusion.
    - It contains the words `Phase 32` or `acceptance` in a scope-exclusion sentence, so no reader infers a signature flow exists.
    - It states that no human holds cross-tenant read, and describes intake and execution as separate paths.
    - `bash scripts/check-doc-citations.sh` exits 0 (not 2) if this document falls within its scanned set; if it does not, record that measurement rather than assuming coverage.
    - FAIL DIRECTION, run and recorded: insert `13434105` into the document and confirm the dissolved-number assertion goes RED. Restore by `git hash-object`.
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/legal/article-26-arrangement.md || exit 1; out=$(grep -ciF 'essence' docs/legal/article-26-arrangement.md || true); test "$out" -ge 1 || exit 1; out=$(grep -cF '16471464' docs/legal/article-26-arrangement.md || true); test "$out" -ge 1 || exit 1; out=$(grep -cF '13434105' docs/legal/article-26-arrangement.md || true); grep -qx 0 <<< "$out" || exit 1; out=$(grep -cF 'M.5' docs/legal/article-26-arrangement.md || true); test "$out" -ge 1 || exit 1; out=$(grep -ciE 'Phase 32|acceptance' docs/legal/article-26-arrangement.md || true); test "$out" -ge 1 || exit 1; bash scripts/check-doc-citations.sh; c=$?; test "$c" -eq 2 && { echo "VOID: citation gate could not scan"; exit 2; }; test "$c" -eq 0 || exit 1</automated>
  </verify>
  <done>
The Article 26 arrangement exists as a document with a publishable essence section, allocates
controller roles by data category, describes the single point of contact in terms of what 31-05
actually built, reuses rather than restates the special-category and derivation wording, and names
its own exclusions so nobody infers an acceptance flow. It cites only the ACTIVE company number.
  </done>
</task>

<task type="auto">
  <name>Task 2: Date and extend the Article 9 determination with D-01</name>
  <files>docs/legal/article-9-allergen-basis.md</files>
  <read_first>
    - docs/legal/article-9-allergen-basis.md (all 122 lines — every finding, what it records as already satisfied, and its four recommended next steps)
    - frontend/app/dashboard/customers/__tests__/allergen-consent-notice.test.tsx (the vendor-facing notice already shipped, which step 2 asked for; the new text must agree with it from the other side)
    - .planning/phases/31-consumer-safety-and-legal-floor/31-CONTEXT.md D-01, D-02, D-03
    - .planning/phases/31-consumer-safety-and-legal-floor/31-UI-SPEC.md § "S3 — checkout allergen block" (the exact consumer-facing sentence this determination has to be consistent with)
  </read_first>
  <action>
**Append, do not rewrite.** Every existing finding, exclusion and next step stays exactly as it is.
Add a new dated section recording the decision D-01 makes, and update the next-steps list only to
reflect what has now happened.

The new section records, as of this phase's date:

  - **The decision.** The platform does NOT consult the stored customer allergen profile
    (`Customer.allergenRestrictions`) at checkout. The roadmap's criterion explicitly permits "or an
    explicit decision not to", and this is that decision, taken deliberately rather than by omission.
  - **The reasoning.** Consulting it would be processing special category data in a context where
    the platform has no Article 9(2) condition of its own — which is precisely what Finding 1 of this
    same document already establishes, so cite Finding 1 rather than re-arguing it. The bitmask stays
    vendor-entered exactly as today; no new consent capture, withdrawal or audit obligation is
    created by this phase.
  - **What the consumer sees instead (D-02).** The ORDER's own aggregated allergen set, which they
    explicitly acknowledge. No health data is read, stored or inferred; the platform never learns the
    customer's allergies; there is no customer-versus-product comparison anywhere. Quote the exact
    consumer-facing sentence the UI-SPEC fixes — *"We do not store your allergies and we cannot check
    this order against them."* — so the document and the product say the same words.
  - **The reconciliation added (D-03).** Product-level only: where a product's ingredients text names
    an allergen its declared mask omits, the order surfaces a "Check" flag. This is product data, not
    special category data, and it must be described as advisory — it never rewrites the vendor's
    declaration.
  - **What is unchanged.** `GdprController.CustomerExport` still includes `allergenRestrictions` in
    the Article 20 export, which this document already records as correct and tested. D-01 does not
    change that, and 31-11's privacy notice must describe it accurately rather than implying the
    field is never disclosed to its own subject.
  - **What remains open.** Step 3 — build the consent record — stays OPEN. D-01 is a decision not to
    process, so the consent record is not required by this phase and must not be marked done.

Update "Recommended next steps": mark step 2 (the vendor-facing notice) as delivered, naming the test
that asserts it; mark step 4 (write the privacy notice) as delivered by this phase, naming the route
31-11 creates; leave steps 1 and 3 open. Do not delete a completed step — striking through or
annotating preserves the record of what was asked for, which is the useful part.

Update the document's date line to show both the original 2026-07-30 determination and this phase's
extension date, so a reader can see it was revisited rather than replaced.
  </action>
  <acceptance_criteria>
    - `docs/legal/article-9-allergen-basis.md` still contains all of its original section headings — assert each of `## The question`, `## Finding 1`, `## Finding 2`, `## What is already satisfied`, `## What remains open`, `### DPA wording to add` and `## Recommended next steps` is still present. Regression by omission is a defect even when the new text is good.
    - It contains the original date `2026-07-30` AND a `2026-08` extension date.
    - It contains the exact consumer sentence `We do not store your allergies and we cannot check this order against them.`
    - It contains a sentence recording that the decision is NOT to consult the stored profile.
    - The consent record (step 3) is still described as open, not delivered.
    - `git diff --stat` on the file shows lines ADDED and (apart from the next-steps annotations and the date line) no original finding removed — inspect the diff and record it, because "extend and date, never contradict" is a property a grep cannot fully express.
    - FAIL DIRECTION, run and recorded: delete one original heading and confirm the heading-preservation assertion goes RED. Restore by `git hash-object` and re-run clean LAST.
  </acceptance_criteria>
  <verify>
    <automated>for h in '## The question' '## Finding 1' '## Finding 2' '## What is already satisfied' '## What remains open' '### DPA wording to add' '## Recommended next steps'; do out=$(grep -cF "$h" docs/legal/article-9-allergen-basis.md || true); test "$out" -ge 1 || { echo "lost original heading: $h"; exit 1; }; done; out=$(grep -cF '2026-07-30' docs/legal/article-9-allergen-basis.md || true); test "$out" -ge 1 || exit 1; out=$(grep -cF '2026-08' docs/legal/article-9-allergen-basis.md || true); test "$out" -ge 1 || exit 1; out=$(grep -cF 'We do not store your allergies and we cannot check this order against them.' docs/legal/article-9-allergen-basis.md || true); test "$out" -ge 1 || exit 1</automated>
  </verify>
  <done>
The Article 9 determination now carries a dated record of the decision not to process the stored
profile, alongside everything it already said — every original heading intact, the consent record
still open, and the exact consumer-facing sentence quoted so the document and the checkout say the
same words. The vendor-facing notice and the privacy notice are marked delivered with the artefacts
that deliver them named.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal determination -> published essence | Text written for engineers crosses into something a regulator or a data subject reads |
| two legal documents -> each other | An inconsistency between them is itself the exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07-01 | Repudiation | Citing the dissolved company number 13434105 | mitigate | Asserted absent by grep and 16471464 asserted present; the live marketing site has already cited the wrong one once |
| T-31-07-02 | Repudiation | Overclaiming a capability that does not exist | mitigate | The single point of contact is described in terms of what 31-05 actually built; acceptance/e-signature are explicitly named as out of scope |
| T-31-07-03 | Repudiation | Two differently worded versions of one exclusion | mitigate | The special-category block is reused verbatim from the Article 9 doc and the derivation exclusion is cross-referenced as M.5, never restated |
| T-31-07-04 | Repudiation | Silently overwriting a prior determination | mitigate | Append-and-date; every original heading asserted still present; completed next-steps annotated rather than deleted |
| T-31-07-05 | Information disclosure | Legal prose describing internal tenancy mechanics | accept | Stating that no human holds cross-tenant read is a design property worth publishing; no hostnames, realm ids, table names or endpoint internals appear |
| T-31-07-SC | Tampering | package installs | accept | No package added; documents only |
</threat_model>

<verification>
## Control arms — each criterion, and the break that proves it can fail

| Criterion | Deliberate break | Expected failure |
|---|---|---|
| "The Article 26 arrangement exists" | today's tree — `docs/legal/` holds exactly two files, neither of them this | the existence assertion is red before Task 1 |
| "It cites only the ACTIVE company" | insert 13434105 | the dissolved-number assertion fires |
| "It has a publishable essence" | remove the essence heading | the heading assertion fires |
| "It does not imply a signature flow" | remove the scope-exclusion sentence | the Phase-32/acceptance assertion fires |
| "The Article 9 doc was extended, not rewritten" | delete an original heading | the heading-preservation loop names the lost heading |
| "The document and the product say the same words" | reword the consumer sentence in either place | the exact-string assertion fires — and 31-14 asserts the same string from the component side, so a change in one place reds the other |

## Instrument notes

- **A VOID is not a pass.** `check-doc-citations.sh` exits 2 when it cannot scan; the verify
  distinguishes 0, 1 and 2. Note the recorded trap that new documents may sit OUTSIDE that gate's
  default scanned set — if this document is not covered, record the measurement rather than assuming
  it passed.
- `grep -c` exits 1 on a zero count; always `out=$(grep -cF X f || true); grep -qx 0 <<< "$out"`.
- **Commit before any break arm**; verify restores by `git hash-object`, never `git diff --stat`.
- **Backticks inside double quotes execute.** These documents quote clause names and file paths;
  pass any commit message or PR body containing them via a quoted heredoc or `-F <file>`, never an
  interpolating `-m` string, and read back what was actually stored with `git log -1 --format=%B`.
- Every legal position here is LOW confidence by design and says so on its own face. Do not upgrade
  any of it while editing.
</verification>

<success_criteria>
- `docs/legal/article-26-arrangement.md` exists, allocates controller roles by data category per
  D-14, describes the single point of contact in terms of the intake 31-05 built, carries a clearly
  delimited **essence** section for 31-11 to publish, reuses the existing special-category wording
  and cross-references clause M.5, names Phase 32 as the owner of acceptance and e-signature, and
  cites 16471464 and never 13434105.
- `docs/legal/article-9-allergen-basis.md` carries a dated D-01 section recording the decision not to
  consult the stored profile, quotes the exact consumer-facing sentence, keeps the consent record
  open, and retains every original heading.
- Neither document contradicts the other, nor the trading line already drawn on `/legal` — the
  distinction between the trading line and the GDPR line is stated explicitly rather than left to the
  reader.
- Both fail-direction arms have been run with output recorded and the clean arm re-run last.
</success_criteria>

<output>
Create `.planning/phases/31-consumer-safety-and-legal-floor/31-07-SUMMARY.md` when done.
Record: the full text of the **essence** section, because 31-11's privacy notice must reproduce or
link to exactly that and must not paraphrase it; whether `check-doc-citations.sh` actually covers
these paths or whether they sit outside its scanned set; and BOTH directions' output for the two
break arms.
</output>
