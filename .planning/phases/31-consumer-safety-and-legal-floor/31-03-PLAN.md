---
phase: 31-consumer-safety-and-legal-floor
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/public/public-shell.tsx
  - frontend/components/public/public-footer.tsx
  - frontend/app/auth/signin/page.tsx
  - frontend/app/auth/signin/layout.tsx
  - frontend/components/public/__tests__/public-shell-landmarks.test.tsx
autonomous: true
requirements: [LGL-02]
must_haves:
  truths:
    - "A keyboard user can skip past the header chrome to the page content on every public route"
    - "The vendor sign-in page exposes a main landmark, a single h1 and its own page title"
    - "Footer column headings no longer skip a heading level on pages whose content supplies no h2"
  artifacts:
    - path: "frontend/components/public/public-shell.tsx"
      provides: "Skip link plus a targetable main landmark, still a plain server component"
      contains: "id=\"main\""
    - path: "frontend/app/auth/signin/layout.tsx"
      provides: "Page-specific metadata replacing the stale root title on a declared surface"
      contains: "Metadata"
    - path: "frontend/components/public/__tests__/public-shell-landmarks.test.tsx"
      provides: "Component-level assertion that the skip link and main landmark exist and are wired"
  key_links:
    - from: "frontend/components/public/public-shell.tsx"
      to: "frontend/components/marketing/operator-pitch.tsx:70"
      via: "the skip-link class string is copied verbatim rather than reinvented"
      pattern: "focus:not-sr-only"
---

<objective>
Close the two largest remaining accessibility root causes on the declared surfaces: the vendor
sign-in page that has no page shell at all, and the missing skip link — plus the footer heading-level
skip that fires on two routes.

Purpose: `/auth/signin` is a D-09 declared surface and it accounts for **7 of the 15** measured axe
nodes on its own — `landmark-one-main:1`, `page-has-heading-one:1`, `region:5` — because
`app/auth/signin/page.tsx` renders a bare `<div className="min-h-screen flex …">` with no `<main>`,
no `<h1>`, no `PublicShell`. It also still serves the stale root title
*"J'Toye OaaS - Multi-Tenant Order Management"*, which the axe count does not show and which is an
orientation defect for a page the file's own header correctly calls "a LANDING DESTINATION, not just
transit". Every expired dashboard session lands here.

`A11Y-06` — no skip link on any declared surface — is not detectable by axe at all, so it cannot be
closed by the gate 31-18 builds. It has to be closed at source, and the pattern already exists three
times in the tree; the job is to reuse one, not to invent a fourth.

`F-C` is one component edit: `PublicFooter`'s column headings are `<h3>` with no `<h2>` above them,
which fires `heading-order` only on pages whose own content supplies no `<h2>` — measured on
`/shop/signin` and `/legal`, 2 of the 15 nodes.

Output: 9 of the 15 measured declared-surface axe nodes closed, plus one WCAG failure axe is blind to.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-consumer-safety-and-legal-floor/31-RESEARCH.md
@.planning/phases/31-consumer-safety-and-legal-floor/31-UI-SPEC.md
@.planning/phases/31-consumer-safety-and-legal-floor/31-PATTERNS.md
@frontend/components/public/public-shell.tsx
@frontend/components/public/public-footer.tsx
@frontend/app/auth/signin/page.tsx

<interfaces>
THE SKIP-LINK PATTERN — copy `components/marketing/operator-pitch.tsx:70` VERBATIM. Three copies
already exist and two of them already differ (`business-model-guide.tsx:110-115` uses `rounded` +
`font-semibold`); UI-SPEC names the `operator-pitch` one, so that is the one. Its class string is
`sr-only z-50 rounded-full bg-oxblood px-4 py-3 text-sm font-bold text-white focus:not-sr-only
focus:absolute focus:left-4 focus:top-4`. It must target a `<main>` carrying an `id`.

PUBLICSHELL IS A PLAIN SERVER COMPONENT AND MUST STAY ONE. Its header (`:4-13`) records why:
*"no client directive, no route-segment config — so the root layout's `dynamic = "force-dynamic"`
and CSP nonce cascade through untouched (the #89 failure mode)"*. A skip link is a plain `<a href>`
and needs no client boundary. Do not add `"use client"`.

`/auth/signin` IS A CLIENT COMPONENT (`"use client"` at line 1) and therefore **cannot export
`metadata`**. The title fix goes in a NEW `app/auth/signin/layout.tsx` — a server component that
exports `metadata` and renders `{children}` — which is the only way to give a client page route
metadata in the App Router.

WHAT `/auth/signin` MUST NOT LOSE (Incremental Betterment). Its header states it is a landing
destination that "must carry the shipped brand (oxblood + cream + amber — public-header wordmark
idiom) and must never be a dead end (escape links below the CTA)", and that the operator/customer
realm split is "not optional decoration". The `CompanyLegalLine`, the cross-link to `/shop/signin`,
the wordmark link to `/`, the gradient surface and every escape link stay. This task adds landmarks
and a heading; it does not restyle the page.

DO NOT WRAP `/auth/signin` IN `PublicShell`. `PublicShell` renders `PublicHeader`, whose "Sign in"
CTA points at this very page — a header offering a link to the page you are on is a navigation
defect, and the file's own header records that the unqualified header CTA is exactly how shoppers
used to arrive here by mistake. Give the page its own `<main id="main">` + `<h1>` + skip link
instead, reusing the same markup PublicShell uses.

FOOTER HEADING LEVELS (F-C): `components/public/public-footer.tsx` renders its column headings as
`<h3 className="text-xs font-bold uppercase tracking-[0.14em] text-gold">` — currently "For
customers" (~:112) and "For operators" (~:143). Change the TAG to `<h2>`; keep every class exactly
as-is. The `font-bold` weight is grandfathered on this existing surface (UI-SPEC Typography), so it
is not touched. 31-17 adds a third "Legal" column and it must also be `<h2>`.

⚠ `scripts/check-geo-attribution.sh` READS THIS FOOTER. It asserts all three OGL lines render
(Ordnance Survey, Royal Mail, National Statistics) and that the rendered year matches the committed
`core-java/src/main/resources/geo/SOURCE.md`. It exits **2 (VOID)** if it cannot find the footer —
and a VOID reads exactly like a missing footer. It is PASS (year 2026) today. Re-run it after the
edit; a VOID is a failure, not a skip.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skip link and targetable main landmark in PublicShell, and the footer heading level</name>
  <files>frontend/components/public/public-shell.tsx, frontend/components/public/public-footer.tsx, frontend/components/public/__tests__/public-shell-landmarks.test.tsx</files>
  <read_first>
    - frontend/components/public/public-shell.tsx (all 22 lines, including the "#89 failure mode" header)
    - frontend/components/marketing/operator-pitch.tsx:60-80 (the skip-link markup to copy verbatim)
    - frontend/components/public/public-footer.tsx:95-175 (the two column headings and the OGL attribution block below them)
    - frontend/components/public/__tests__/public-footer-persona.test.tsx (the existing test shape for this component)
  </read_first>
  <action>
In `public-shell.tsx`, add `id="main"` to the existing `<main className="flex-1">` and insert the
skip link as the **first child** of the outer `<div>`, before `<PublicHeader />`. Use the
`operator-pitch.tsx:70` class string verbatim, `href="#main"`, and the text "Skip to main content".
It must be the first focusable element in the DOM or it is unreachable by the keystroke it exists
for. Keep the component a plain server component — no `"use client"`, no route-segment config.

In `public-footer.tsx`, change the two column heading tags from `<h3>` to `<h2>` and change nothing
else. Do not touch the class strings, the persona gating, the OGL attribution block or the
`CompanyLegalLine`. This closes `heading-order` on `/shop/signin` and `/legal`, which are the two
routes whose own content supplies no `<h2>`.

Write `frontend/components/public/__tests__/public-shell-landmarks.test.tsx` in the plain
`@testing-library/react` shape this repo uses (see `components/ui/__tests__/ingredient-text.test.tsx`
— no custom harness). Assert four things:

  - exactly one `main` landmark, and it carries `id="main"`;
  - a link whose `href` is `#main` exists, and it is the FIRST link in document order — assert its
    position, not merely its presence, because a skip link rendered after the header is decoration;
  - the link carries the `sr-only` and `focus:not-sr-only` classes, so it is hidden until focused;
  - every footer column heading resolves at level 2 — query by role `heading` with `level: 2` and
    assert a non-zero count, and assert the level-3 count among those headings is zero.

Add a **non-vacuity control**: assert the rendered shell contains the footer's own known text before
asserting anything about headings. A heading query over an unrendered tree returns zero and looks
identical to a pass.
  </action>
  <acceptance_criteria>
    - `npx jest components/public/__tests__/public-shell-landmarks.test.tsx` passes with >= 4 assertions.
    - `frontend/components/public/public-shell.tsx` contains `id="main"`, contains `href="#main"`, contains `focus:not-sr-only`, and does NOT contain `"use client"`.
    - `frontend/components/public/public-footer.tsx` contains zero occurrences of `<h3` and at least 2 of `<h2`.
    - `bash scripts/check-geo-attribution.sh` exits 0 — **not 2**. A VOID here means the gate can no longer find the footer and must be treated as a failure of this task, not as an unrelated flake.
    - FAIL DIRECTION, run and recorded: (a) move the skip link to after `<PublicHeader />` and confirm the "first link in document order" assertion goes RED — a presence-only assertion would stay green, which is why the position is asserted; (b) revert one footer heading to `<h3>` and confirm the level-2 assertion goes RED. Commit before the arms, restore by `git hash-object`, re-run clean LAST.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx jest components/public/__tests__/public-shell-landmarks.test.tsx --ci; rc=$?; test "$rc" -eq 0 || exit 1; out=$(grep -cF 'id="main"' components/public/public-shell.tsx || true); test "$out" -ge 1 || exit 1; out=$(grep -cF 'href="#main"' components/public/public-shell.tsx || true); test "$out" -ge 1 || exit 1; out=$(grep -cF 'use client' components/public/public-shell.tsx || true); grep -qx 0 <<< "$out" || exit 1; out=$(grep -cF '<h3' components/public/public-footer.tsx || true); grep -qx 0 <<< "$out" || exit 1; out=$(grep -cF '<h2' components/public/public-footer.tsx || true); test "$out" -ge 2 || exit 1; cd .. && bash scripts/check-geo-attribution.sh; g=$?; test "$g" -eq 2 && { echo "VOID: geo attribution gate could not find the footer"; exit 2; }; test "$g" -eq 0 || exit 1</automated>
  </verify>
  <done>
Every public route served through `PublicShell` opens with a keyboard-reachable skip link targeting a
`<main id="main">`, `PublicShell` is still a plain server component, and the footer's column headings
are level 2. `check-geo-attribution.sh` is green rather than VOID. Both fail-direction arms were run
with output recorded and the clean arm re-run last.
Run `scripts/docs-freshness.sh --write` for the new test file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Give the vendor sign-in page a shell, a heading and its own title</name>
  <files>frontend/app/auth/signin/page.tsx, frontend/app/auth/signin/layout.tsx</files>
  <read_first>
    - frontend/app/auth/signin/page.tsx (all 97 lines — especially the header comment recording that this is a landing destination, the operator/customer realm split, and the escape links that must survive)
    - frontend/components/public/public-shell.tsx (the `<main>` + skip-link markup produced by Task 1 — reuse the same shape, do not import the shell)
    - frontend/app/legal/page.tsx:1-11 (the `export const metadata` + `alternates: { canonical }` idiom this repo uses)
    - .planning/phases/31-consumer-safety-and-legal-floor/31-PATTERNS.md § "Anti-Analogs" (this page's bare `<div>` shell is listed there as the defect, not the pattern)
  </read_first>
  <action>
Replace the outer `<div className="min-h-screen flex flex-col items-center justify-center …">` with
a structure that provides the three landmarks axe reports missing, while keeping the gradient
surface, the centring and every existing child exactly as they are:

  - a skip link as the first focusable element, same verbatim class string as Task 1,
    `href="#main"`, text "Skip to main content";
  - a `<main id="main">` wrapping the `Card`;
  - a single `<h1>` naming the page. `CardTitle` currently carries the visible heading text — promote
    that to the `<h1>` by rendering `CardTitle` as an `h1` (shadcn's `CardTitle` accepts a className
    and renders a heading element; if it does not render `h1`, add a visually-styled `<h1>` in its
    place rather than adding a second competing heading). There must be exactly ONE `h1` on the page.
  - the `region:5` nodes are content sitting outside any landmark. Once the `Card` and the escape
    links are inside `<main>`, they are inside a landmark; verify by re-reading the rendered tree,
    not by assuming — if any escape link or the `CompanyLegalLine` sits outside `<main>`, move it
    inside or wrap it in a labelled `<footer>`.

Create `frontend/app/auth/signin/layout.tsx` as a server component (no `"use client"`) that exports
`metadata` with a title naming operator sign-in, a description distinguishing this from the customer
door at `/shop/signin`, and `alternates: { canonical: "/auth/signin" }` in the
`app/legal/page.tsx:6-11` idiom. It renders `{children}` and nothing else — it must not add chrome,
because the page owns its own surface. This is the only way to replace the stale root title
*"J'Toye OaaS - Multi-Tenant Order Management"* on a `"use client"` page.

**Preserve, explicitly**: the wordmark link to `/`, the `ArrowLeft` escape link, the cross-link to
`/shop/signin`, the `CompanyLegalLine`, the realm-split copy, and the oxblood/cream/amber brand
treatment. Nothing on this page is deleted. If any of them ends up outside the new `<main>`, it moves
— it does not disappear.
  </action>
  <acceptance_criteria>
    - `frontend/app/auth/signin/page.tsx` contains exactly one `<h1`, contains `<main`, contains `id="main"` and contains `href="#main"`.
    - `frontend/app/auth/signin/layout.tsx` exists, exports `metadata`, contains `canonical` and does NOT contain `"use client"`.
    - The served page title no longer equals the root default — assert the rendered `<title>` contains a string naming operator or vendor sign-in, and assert it does NOT contain "Multi-Tenant Order Management".
    - Every preserved element still renders: assert the presence of a link to `/shop/signin`, a link to `/`, and the `CompanyLegalLine` text. Regression by omission is a defect even when the a11y count improves.
    - FAIL DIRECTION, run and recorded: delete the `<main>` wrapper and confirm the landmark assertion goes RED; delete the `layout.tsx` and confirm the title assertion goes RED naming the root default it falls back to. Restore by content hash and re-run clean LAST.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && out=$(grep -cF '<h1' app/auth/signin/page.tsx || true); test "$out" -eq 1 || exit 1; out=$(grep -cF '<main' app/auth/signin/page.tsx || true); test "$out" -ge 1 || exit 1; out=$(grep -cF 'href="#main"' app/auth/signin/page.tsx || true); test "$out" -ge 1 || exit 1; test -f app/auth/signin/layout.tsx || exit 1; out=$(grep -cF 'canonical' app/auth/signin/layout.tsx || true); test "$out" -ge 1 || exit 1; out=$(grep -cF 'use client' app/auth/signin/layout.tsx || true); grep -qx 0 <<< "$out" || exit 1; out=$(grep -cF '/shop/signin' app/auth/signin/page.tsx || true); test "$out" -ge 1 || exit 1; out=$(grep -cF 'CompanyLegalLine' app/auth/signin/page.tsx || true); test "$out" -ge 1 || exit 1; npm run build 2>&1 | tail -20; npm run lint</automated>
  </verify>
  <done>
`/auth/signin` exposes one `main`, one `h1` and a skip link, its content sits inside a landmark, and
it serves its own title instead of the root default. Every element the file's header calls
load-bearing — the escape links, the realm-split copy, the brand treatment, the
`CompanyLegalLine` — is still rendered. Both fail-direction arms were run with output recorded.
`npm run build` is the type-check gate for this change (jest does NOT type-check).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated visitor -> `/auth/signin` | A public landing destination for every expired dashboard session |
| footer markup -> `check-geo-attribution.sh` | An edit to shared chrome crosses into a gate that VOIDs when it cannot find it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03-01 | Information disclosure | `/auth/signin` metadata | mitigate | The new `layout.tsx` metadata describes the operator door and the realm split; it must not name internal realm ids, hostnames or the staff realm's URL |
| T-31-03-02 | Spoofing | Operator/customer realm confusion | mitigate | The cross-link to `/shop/signin` and the realm-split copy are explicitly preserved; a customer landing here must still be told plainly that their account does not exist in this realm |
| T-31-03-03 | Repudiation | Shared-chrome edit silently VOIDing an OGL attribution gate | mitigate | `check-geo-attribution.sh` is run in this plan's own verify and a VOID (exit 2) is treated as a failure, not a skip |
| T-31-03-04 | Tampering | `PublicShell` losing its server-component property | mitigate | Verify asserts the absence of `"use client"`; the #89 failure mode is a CSP-nonce regression, so this is a security property and not a style one |
| T-31-03-SC | Tampering | package installs | accept | No package added |
</threat_model>

<verification>
## Control arms — each criterion, and the break that proves it can fail

| Criterion | Deliberate break | Expected failure |
|---|---|---|
| "A skip link exists" | today's tree — `PublicShell` has none; the pattern exists only on 3 marketing components | the new test is RED before Task 1 |
| "…and is reachable first" | move it after `<PublicHeader />` | the document-order assertion fires where a presence-only assertion would not |
| "`/auth/signin` has a main landmark" | today's tree — `main = 0`, `banner = 0`, `contentinfo = 0`, `h1 = 0` | the landmark assertions are RED before Task 2 |
| "…and its own title" | delete `layout.tsx` | the title falls back to the root default and the negative assertion fires |
| "Footer headings are level 2" | revert one to `<h3>` | the level-2 count assertion fires |
| "The footer is still findable by the OGL gate" | remove the attribution block | `check-geo-attribution.sh` exits 2 (VOID), which this plan treats as failure |
| "Nothing was dropped from the sign-in page" | delete the `/shop/signin` cross-link | the preservation assertion fires |

## Instrument notes

- **A VOID is not a pass.** `check-geo-attribution.sh` exits 2 when it cannot find the footer, and
  that reads exactly like a missing footer. The verify distinguishes 0, 1 and 2 explicitly.
- **`npm run build` is the type-check gate.** Jest does NOT type-check; a TSX change that compiles
  in jest can still break `tsc`.
- `grep -c` exits 1 on a zero count; always `out=$(grep -cF X f || true); grep -qx 0 <<< "$out"`.
- **Commit before any break arm.** Verify restores by `git hash-object`, never `git diff --stat`.
- The exact axe node counts quoted here were measured 2026-08-15 and RESEARCH.md declares the a11y
  baseline valid only until 2026-08-22. Treat them as the expected direction of travel; 31-18
  re-measures and is the authority on the final count.
</verification>

<success_criteria>
- `PublicShell` renders a keyboard-reachable skip link as its first focusable element, targeting
  `<main id="main">`, and remains a plain server component.
- `PublicFooter`'s column headings are level 2, and `scripts/check-geo-attribution.sh` exits 0.
- `/auth/signin` exposes exactly one `main`, exactly one `h1`, and content that sits inside a
  landmark — closing the `landmark-one-main`, `page-has-heading-one` and `region` nodes measured
  there.
- `/auth/signin` serves its own `<title>` and canonical, and no longer serves the root default.
- Every element the sign-in page's own header calls load-bearing still renders.
- `npm run build` and `npm run lint` are green.
- Every fail-direction arm above has been run, both directions recorded, and the clean arm re-run last.
</success_criteria>

<output>
Create `.planning/phases/31-consumer-safety-and-legal-floor/31-03-SUMMARY.md` when done.
Record: the `check-geo-attribution.sh` exit code before and after the footer edit; the served
`<title>` string before and after on `/auth/signin`; and BOTH directions' output for all four break
arms.
</output>
