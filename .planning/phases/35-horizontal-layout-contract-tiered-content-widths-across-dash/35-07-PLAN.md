---
phase: 35-horizontal-layout-contract-tiered-content-widths-across-dash
plan: 07
type: tdd
wave: 3
depends_on: ["35-02"]
files_modified:
  - frontend/components/marketing/operator-pitch.tsx
  - frontend/components/marketing/__tests__/operator-pitch.test.tsx
  - frontend/components/marketing/business-model-guide.tsx
  - frontend/components/marketing/__tests__/business-model-guide.test.tsx
  - frontend/components/marketing/competitive-teardown.tsx
  - frontend/components/marketing/__tests__/competitive-teardown.test.tsx
  - frontend/app/shop/layout.tsx
  - frontend/app/shop/shop-discovery-client.tsx
  - frontend/app/shop/__tests__/shop-discovery-client.test.tsx
  - frontend/app/shop/loading.tsx
  - frontend/app/shop/[slug]/loading.tsx
autonomous: true
requirements: [UIX-07]

must_haves:
  truths:
    - "Every marketing surface already at the Marketing width declares that tier, so 1280 is a contract rather than a coincidence"
    - "The two surfaces that deliberately get no tier — the public shell and the shop not-found panel — are recorded as N/A with their reasons, and reach the manifest rather than being dropped"
    - "The public shop directory keeps its current width and its chrome keeps matching it"
    - "The shop detail skeleton and the content that replaces it declare the same width, so the page no longer narrows on hydration"
    - "No surface in this plan changes its rendered width"
  artifacts:
    - path: "frontend/components/marketing/operator-pitch.tsx"
      provides: "Marketing tier declared on the operator route's bands"
      contains: "data-width-tier"
    - path: "frontend/app/shop/layout.tsx"
      provides: "Marketing tier declared on the storefront header rail"
      contains: "data-width-tier"
    - path: "frontend/app/shop/[slug]/loading.tsx"
      provides: "skeleton band aligned to the content band it is replaced by — the 384px hydration jump removed"
  key_links:
    - from: "frontend/app/shop/[slug]/loading.tsx"
      to: "frontend/app/shop/[slug]/shop-detail-client.tsx"
      via: "the skeleton declares the same band width as the content, asserted by the gate in plan 35-10"
      pattern: "max-w-4xl"
---

<objective>
Turn the three marketing surfaces that already render at 1280px from coincidentally correct into
declared, and fix the storefront skeleton that narrows its page by 384px on hydration.

Two distinct jobs, one plan, because both are zero-width-delta edits on public surfaces.

**Job one — declaration.** PATTERNS.md F-2: the Marketing tier is already about 75% shipped. The
operator route, the business-model guide, the competitive teardown and both storefront rails already
render at exactly 1280px, but they do so because a stock scale token happens to equal that number,
not because anything declares it. UIX-07 asks for a DECLARED contract; a coincidence that nothing
asserts is the same class of problem as the inherited 1400 this phase exists to remove.

**Job two — the hydration jump.** PATTERNS.md A-12, an existing defect the inventory surfaced:
`app/shop/[slug]/loading.tsx:14` renders its skeleton band at 1280px while the content that replaces
it (`shop-detail-client.tsx`) renders at 896px. **The page narrows by 384px the moment real content
arrives.** This is ORCH-05 — an orchestrator decision (2026-08-29), see CONTEXT.md section 4b. It is a
one-line change and it is exactly the class of defect a width contract exists to prevent.

Purpose: no surface in this plan changes width. That is what makes it safe to run in parallel with
plan 35-06, which owns the one surface that does.

Output: five surfaces declaring the Marketing tier, one skeleton aligned to its content, and A-11
recorded as deferred with its reason.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-horizontal-layout-contract-tiered-content-widths-across-dash/CONTEXT.md
@.planning/phases/35-horizontal-layout-contract-tiered-content-widths-across-dash/PATTERNS.md
@.planning/phases/35-horizontal-layout-contract-tiered-content-widths-across-dash/35-02-SUMMARY.md

@frontend/components/layout/content-tier.tsx
@frontend/app/shop/layout.tsx
@frontend/app/shop/loading.tsx
@frontend/app/shop/[slug]/loading.tsx

<interfaces>
From plan 35-02:
  export const WIDTH_TIER_CLASS: Record<WidthTier, string>   // marketing -> the marketing utility

THE BAND SITES, measured. Each already carries an auto margin, the stock 1280 token and padding:
  components/marketing/operator-pitch.tsx        73, 80, 102     (sticky bar, hero grid, body)
  components/marketing/business-model-guide.tsx  118, 143, 161, 255  (header, tab rail, body, footer)
  components/marketing/competitive-teardown.tsx  220, 250, 266, 560  (header, filter rail, body, footer)
  app/shop/layout.tsx                             64              (storefront sticky header rail)
  app/shop/shop-discovery-client.tsx              384, 635        (directory band, skeleton band)
  app/shop/loading.tsx                            12              (route skeleton band)

DO NOT TOUCH the inner measure clamps inside those files — the headline and paragraph measures in the
marketing components, and the sub-heading and search-input widths in the discovery client. PATTERNS
1c classifies every one of them as a typographic measure or a control, orthogonal to the page tier.

THE SKELETON FIX is different in kind. `app/shop/[slug]/loading.tsx:14` moves DOWN, from 1280 to the
896 its content renders at. That value is not a tier token and must not be given one — see the
deferral block below.
</interfaces>

</context>

<na_surfaces>
TWO SURFACES THAT GET NO TIER, RECORDED SO THE OMISSION IS A DECISION. The N/A rule is that a
non-applicable surface is written down, never silently dropped — an unstated boundary reads as covered.
Both go into plan 35-10's `docs/architecture/layout-tiers.tsv` manifest with their N/A status, so
G-4's parity check and G-6's both-directions completeness check cover them rather than skipping them.

**`components/public/public-shell.tsx` — N/A, and it carries an ACTIVE TRAP.** Its `<main id="main">`
is deliberately width-free so its children own their bands. PATTERNS 1c records the standing
instruction verbatim: **do NOT add a cap here.** Two independent reasons — `/shop` has a separate
layout tree, and `PolicyPage` nests inside this one, so a cap here would silently narrow surfaces this
phase never examined. PATTERNS B-12 records the concrete consequence: `e2e/unsubscribe-flow.spec.ts`
asserts no horizontal overflow at 375px on `/unsubscribe`, which is `PublicShell` plus `max-w-lg`, and
is "only at risk if `PublicShell` gains a cap". Record it as N/A-container in the manifest WITH the
do-not-cap reason, so the next person adding a tier does not read a bare N/A as an oversight to fix.

**`app/shop/[slug]/not-found.tsx` — N/A, and it is a PARITY partner.** Line 30 renders the
shop-not-found panel at `max-w-4xl` (896px), which is exactly what `shop-detail-client.tsx` renders.
PATTERNS: "consistent today; keep it consistent." It gets no tier attribute for the same reason the
skeleton does not — the surface is deliberately unassigned pending A-11 — but it DOES belong in the
manifest as a third member of the `/shop/[slug]` width family, so G-4 checks all three against each
other. A parity check covering two of three files is worse than none, because it reads as covering the
route.

So the `/shop/[slug]` family is: the loading skeleton, the detail client, and the not-found panel —
all three at 896, all three moving together, none tiered.
</na_surfaces>

<a11_deferral>
**A-11, `/shop/[slug]` menu width, is DEFERRED and this plan does not widen it.** Recorded here so it
is a decision rather than an omission.

The shop detail client renders its menu at 896px across nine sites in one file. PATTERNS argues it
could defensibly take the Detail tier (1100), citing Linear at 1136 and Lightspeed at 1100, and also
argues the opposite: 896 is within prose-measure territory, a menu row is a scannable line rather than
a grid, the surface is absent from CONTEXT's measured baseline table, and it is the highest-churn edit
available in the phase. ORCH-01 scopes the public storefront to "unchanged", and
plan 35-05's contract rule settles the rest: **a tier is a ceiling, not a target**, so a surface
already narrower than its tier for a stated reason keeps its measure.

So `/shop/[slug]` keeps 896, the skeleton is aligned DOWN to match it, and neither gets a tier
attribute — an attribute would claim a tier the surface has not been assigned. Both files get a
comment saying the two must move together and that A-11 is deferred, and plan 35-10's gate asserts
the parity mechanically, because a comment is not an assertion.
</a11_deferral>

<context_cost_note>
These page files are large — several run to a thousand lines or more — and this plan's edit to each is
a SINGLE attribute on ONE element. Do not read them in full. Locate the component's main return with a
targeted search for the return statement or the root element's class string, then read only that range
with an offset and a limit. Reading eight large files end to end would consume most of an executor's
context to make eight one-line edits, and quality degrades with it.
</context_cost_note>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declare the Marketing tier on the three marketing components</name>
  <files>frontend/components/marketing/operator-pitch.tsx, frontend/components/marketing/__tests__/operator-pitch.test.tsx, frontend/components/marketing/business-model-guide.tsx, frontend/components/marketing/__tests__/business-model-guide.test.tsx, frontend/components/marketing/competitive-teardown.tsx, frontend/components/marketing/__tests__/competitive-teardown.test.tsx</files>
  <behavior>
    - Each of the eleven band sites declares `data-width-tier` with the value marketing and carries the
      marketing utility in place of the stock token
    - Per component the COUNT of marketing-tier bands is asserted — three for the operator pitch, four
      for the business-model guide, four for the competitive teardown — so a band left behind reds
    - No band element carries both the stock token and the tier utility; the swap is complete per site
    - The inner measure clamps are untouched, asserted by leaving every existing case in the three test
      files green without modification to those cases
    - The competitive teardown's radar-chart scroll wrapper keeps its overflow class, which its own
      test asserts
  </behavior>
  <action>
    Edit the three marketing components at the band sites listed in the interfaces block. In place per
    site: swap the stock token for the marketing utility from WIDTH_TIER_CLASS, add the tier attribute,
    keep the auto margin and all padding classes. No wrapper nodes — these routes are scroll-reveal
    driven and a new DOM node between a section and its band moves boundingBox assertions for nothing.

    Comment once per file, at the first band, in prose: this width is the declared Marketing tier and
    is deliberately equal to the public header and footer rails; it was previously the stock scale
    token that happens to equal the same number.

    Extend the three existing test files with the count assertions. Do not modify their existing cases
    — the point of this plan is that nothing changes width, so any existing case that reds is a real
    finding, not a test that needs updating.
  </action>
  <verify>
    <automated>cd frontend && npx jest components/marketing</automated>
  </verify>
  <done>
    Eleven band sites declare the Marketing tier, each component's band count is asserted, the stock
    token is gone from all of them, and every pre-existing marketing case passes unmodified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Declare the Marketing tier across the public shop directory and its chrome</name>
  <files>frontend/app/shop/layout.tsx, frontend/app/shop/shop-discovery-client.tsx, frontend/app/shop/__tests__/shop-discovery-client.test.tsx, frontend/app/shop/loading.tsx</files>
  <behavior>
    - The storefront header rail, the directory band, the directory's own skeleton band and the route
      skeleton band all declare the marketing tier and carry the marketing utility
    - The rendered width of every one of them is unchanged at 1280
    - The route skeleton and the directory content declare the SAME width — they are consistent today
      and must stay so, or the directory gains the hydration jump the detail route currently has
    - The storefront layout's skip link, sticky header behaviour and uncapped main element are unchanged
    - Every existing discovery-client and shop-layout-a11y case still passes
  </behavior>
  <action>
    Apply the in-place declaration at `app/shop/layout.tsx:64`, both sites in
    `app/shop/shop-discovery-client.tsx` and `app/shop/loading.tsx:12`.

    Record ORCH-01 in a comment on the directory band, in prose and with its reasoning, because this
    is the phase's most consequential deliberate non-change and PATTERNS flags it as such: the public
    directory stays at the Marketing width rather than going fluid like a dashboard resource index.
    Three reasons, all of which belong at the site — the Index tier is scoped to the dashboard
    resources, a fluid card grid would render wider than the header rail directly above it and invert
    the very misalignment this phase is fixing on the landing page, and this is an SEO-and-CLS-measured
    route where the least-cost correct change is none.

    ATTRIBUTE IT CORRECTLY IN THE SOURCE COMMENT — this is the ONE citation in the phase that gets
    written into the repository, so it is the one where a wrong attribution outlives the planning
    archive. Write it as an **orchestrator decision (2026-08-29), recorded in CONTEXT.md section 4b as
    ORCH-01**, and NEVER as a user decision. The owner delegated the phase autonomously and was told
    the open questions had been resolved; that is authority to decide, and it is not the owner having
    chosen this option. A comment claiming the user chose it would read to the next reader as settled
    product direction rather than as a reversible planning call. ORCH-01 is flagged
    owner-visible-if-wrong and is put in front of the owner at the 35-13 gate — a source comment that
    overstates its provenance quietly undermines that gate.

    Do not add a cap to the storefront layout's main element. It is deliberately width-free so its
    children own their bands, and the policy pages nest inside that tree.

    Extend the discovery-client test with the declaration and count assertions, reusing its setup.
  </action>
  <verify>
    <automated>cd frontend && npx jest app/shop/__tests__</automated>
  </verify>
  <done>
    Four storefront band sites declare the Marketing tier at an unchanged 1280, ORCH-01's reasoning
    AND its orchestrator provenance are recorded at the directory band, and every pre-existing
    storefront case passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Align the shop detail skeleton to its content, and record A-11 as deferred</name>
  <files>frontend/app/shop/[slug]/loading.tsx</files>
  <behavior>
    - The shop detail skeleton's band renders at the same width as the content that replaces it, so
      the page no longer narrows by 384px on hydration
    - The skeleton carries NO tier attribute, because the surface is deliberately not assigned a tier
    - The banner skeleton, the identity row and the menu-row skeletons are otherwise unchanged, so the
      skeleton still describes the shape of the page it stands in for
  </behavior>
  <action>
    Edit `frontend/app/shop/[slug]/loading.tsx` at line 14: change the band token from the 1280 value
    to the 896 value the content uses. Change nothing else in the file.

    Add a comment at that line recording the whole finding: the skeleton was 384px wider than the
    content it is replaced by, so the page visibly narrowed on hydration — a latent layout-shift
    contributor on a route with no recorded CLS budget. Record that the two must move together, that
    A-11 (widening the menu itself to the Detail tier) is DEFERRED with the reasoning in this plan's
    deferral block, and that the parity is asserted mechanically by the gate in plan 35-10 rather
    than trusted to this comment.

    Also record in the SUMMARY that the directory pair (`app/shop/loading.tsx` and the discovery
    client) is consistent today at 1280 and was kept so by Task 2 — a parity claim covering one of two
    route pairs would be worse than none, because it reads as covering both.

    And record the `/shop/[slug]` family as THREE files, not two: the skeleton, the detail client, and
    `app/shop/[slug]/not-found.tsx:30`, which renders the not-found panel at the same 896 and must stay
    equal to it. Confirm the not-found value on the tree and record it. Hand all three to plan 35-10 as
    the G-4 parity set, together with the `components/public/public-shell.tsx` N/A-container entry and
    its do-not-cap reason, so both appear in `layout-tiers.tsv` rather than being absent from a
    manifest whose completeness check runs in both directions.
  </action>
  <verify>
    <automated>cd frontend && npx jest app/shop</automated>
  </verify>
  <done>
    The skeleton and content bands agree, the file is otherwise untouched, A-11's deferral and its
    reasoning are recorded at the site, and both storefront route pairs' parity status is stated.
  </done>
</task>

<task type="auto">
  <name>Task 4: Fail arms and clean close</name>
  <files>frontend/components/marketing/business-model-guide.tsx, frontend/app/shop/[slug]/loading.tsx</files>
  <action>
    COMMIT Tasks 1 through 3 first.

    ARM A — revert one of the business-model guide's four bands to the stock token. Its count
    assertion must RED at three. Restore, verify by content, re-run green.

    ARM B — set the shop detail skeleton band back to the 1280 value. The parity claim must be shown
    to be checkable: since plan 35-10's gate does not exist yet, record here that the arm produces NO
    red today, and hand the arm to plan 35-10 as the acceptance criterion for its parity check. Do not
    report an unrun arm as passing. Restore immediately and verify by content.

    This is the honest handling of an assertion whose instrument is not built yet, and it is the
    reason plan 35-10 exists at all: a fix with no gate behind it is a fix that regresses silently.

    Then run this plan's closing arm, recording both directions of ARM A and the clean close.

    WAVE-3 CLOSING ARM — TARGETED, NOT A FULL PRODUCTION BUILD. Five plans run in parallel in this
    wave, and an earlier draft had each of them close with `npx jest && npm run build`: five full
    production builds for one wave. Two reasons that is the wrong shape, and the second is the one that
    matters. First, cost — a full Next build is the most expensive thing in this repo's frontend loop.
    Second, and decisively: each wave-3 plan runs on its own branch, so its build only ever proved that
    plan's tree in isolation. It never proved the wave. The build that actually answers "does the
    merged wave compile" is plan 35-08's, which depends on all five.

    So close here with the targeted Jest projects for THIS plan's files plus `npx tsc --noEmit -p
    tsconfig.json`. The type-check is not optional and is not covered by Jest: this repo has recorded
    that frontend TypeScript errors are invisible to Jest and surface only under a real compile, so
    dropping the build without adding a type-check would silently remove the type gate.

    Plan 35-06 keeps a real `npm run build` — it is the one plan whose width VALUE changes and it must
    read the emitted value out of the generated stylesheet, so a build is load-bearing there rather
    than ceremonial. Plan 35-08 runs the consolidated full `npx jest && npm run build` once the whole
    wave has merged. Record in the SUMMARY that this plan's close was targeted and name where the full
    build happens, so a reader does not mistake a narrowed arm for a skipped one.
  </action>
  <verify>
    <automated>cd frontend && npx jest components/marketing app/shop && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>
    ARM A recorded in both directions. ARM B recorded as UNVERIFIABLE-TODAY with its instrument named
    and handed to plan 35-10 — never reported as a pass. Full suite and build green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet -> marketing and storefront routes | Unauthenticated, indexable surfaces |
| server-rendered skeleton -> hydrated client content | The boundary where the 384px jump occurs today |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-24 | Tampering | half-completed token swap | mitigate | A band left on the stock token renders identically today and diverges the moment the tier value changes — the drift is invisible until it matters. Mitigated by per-component count assertions and ARM A |
| T-35-25 | Denial of Service (usability) | shop detail hydration | mitigate | The skeleton-to-content narrowing is a real layout shift on a route with no recorded CLS budget. Mitigated by aligning the skeleton and by handing the mechanical parity assertion to plan 35-10's gate rather than to a comment |
| T-35-26 | Tampering | storefront rail vs directory content | mitigate | If the directory ever goes fluid without its rail, the misalignment this phase fixes on the landing page is reintroduced on the storefront. Mitigated by declaring the same tier on both and recording ORCH-01's reasoning at the site |
| T-35-27 | Information Disclosure | public marketing markup | accept | No metadata, structured data, credential or tenant value is touched; no width changes. SEO recorded N/A with reason |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install in this plan |

ASVS L2: V14 advisory — utilities only, no inline styles. V2 through V6 do not apply.
</threat_model>

<verification>
1. `cd frontend && npx jest components/marketing app/shop` — green, with no pre-existing case modified
2. `cd frontend && npx tsc --noEmit -p tsconfig.json` — rc=0 (Jest does NOT type-check frontend TS)
3. The consolidated full `npx jest && npm run build` is plan 35-08's, once the wave has merged
4. ARM A recorded in both directions; ARM B recorded as unverifiable-today with its instrument named
5. Both storefront route pairs' skeleton/content parity status stated in the SUMMARY
</verification>

<success_criteria>
- Eleven marketing band sites and four storefront band sites declare the Marketing tier, at an unchanged 1280
- No surface in this plan changes rendered width, which is what makes it parallel-safe with plan 35-06
- The shop detail skeleton and its content agree; the 384px hydration narrowing is gone
- A-11 is recorded as a deliberate deferral with its reasoning, not omitted
- ARM B is handed to plan 35-10 rather than reported as passing

## Cross-Cutting Quality Contracts

- **Web performance**: the skeleton fix removes a layout-shift contributor. The route has no recorded
  CLS budget; do not invent one here — record the absence.
- **SEO**: N/A WITH REASON. Public indexable surfaces, but no metadata, structured data, canonical or
  crawlable link is touched and no width changes.
- **AI agent-readiness**: N/A. No API surface changes.
- **Security**: threat model above.
- **Falsifiable evidence**: one arm run in both directions; the second is explicitly owed.
</success_criteria>

<output>
Create `.planning/phases/35-horizontal-layout-contract-tiered-content-widths-across-dash/35-07-SUMMARY.md` when done
</output>
