---
phase: 24-image-architecture-cow-assets-safe-upload-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core-java/src/main/resources/db/migration/V53__media_asset.sql
  - core-java/src/main/java/uk/jtoye/core/media/MediaAsset.java
  - core-java/src/main/java/uk/jtoye/core/media/ProductMedia.java
  - core-java/src/main/java/uk/jtoye/core/media/MediaAssetRepository.java
  - core-java/src/main/java/uk/jtoye/core/media/ProductMediaRepository.java
  - core-java/src/main/java/uk/jtoye/core/media/MediaAssetService.java
  - core-java/src/main/java/uk/jtoye/core/storage/StorageService.java
  - core-java/src/main/java/uk/jtoye/core/product/ProductService.java
  - core-java/src/main/java/uk/jtoye/core/product/ProductMapper.java
  - core-java/src/test/java/uk/jtoye/core/media/MediaAssetRlsPolicyIntegrationTest.java
  - core-java/src/test/java/uk/jtoye/core/media/MediaCopyOnWriteIntegrationTest.java
  - core-java/src/test/java/uk/jtoye/core/media/MediaBackfillMigrationIntegrationTest.java
  - core-java/src/test/java/uk/jtoye/core/security/RlsContractTest.java
autonomous: true
requirements: [IMG-01]
must_haves:
  truths:
    - "media_asset + product_media RLS hides cross-tenant rows under the NOSUPERUSER role-downgrade"
    - "Editing a shared asset mints a new asset and repoints only the one product_media row (CoW); the other product is unchanged — the unified product_media join-table link shape (D-01)"
    - "Releasing the last product_media reference triggers a physical MinIO delete; a still-referenced asset is NOT deleted"
    - "An identical raw upload (same sha256) per tenant reuses the existing asset (unique index)"
    - "Existing products.image_url / additional_image_urls[] backfill into product_media under a per-tenant GUC loop (NOT zero rows)"
    - "A read resolves asset-first and falls back to image_url during the dual-read window; existing image_url values are wrapped as ACTIVE as-is without re-processing (D-03)"
  artifacts:
    - path: "core-java/src/main/resources/db/migration/V53__media_asset.sql"
      provides: "media_asset + product_media + media_asset_aud, ENABLE+FORCE RLS via current_tenant_id(), per-tenant backfill loop"
      contains: "current_tenant_id()"
    - path: "core-java/src/main/java/uk/jtoye/core/media/MediaAssetService.java"
      provides: "copy-on-write repoint + reference-counted delete + sha256 dedup"
      min_lines: 40
    - path: "core-java/src/main/java/uk/jtoye/core/media/ProductMediaRepository.java"
      provides: "countByAssetId (ref-count) + one-row repoint UPDATE"
      contains: "countByAssetId"
  key_links:
    - from: "MediaAssetService.releaseAsset"
      to: "StorageService.deleteByKey"
      via: "physical MinIO delete only at ref-count 0"
      pattern: "countByAssetId"
    - from: "ProductService dual-read resolver"
      to: "product_media ACTIVE derivative"
      via: "asset-first, image_url fallback"
      pattern: "findPrimaryActiveDerivative|getImageUrl"
    - from: "V53 backfill"
      to: "per-tenant set_config loop"
      via: "trap_rls_migration_backfill (V44 pattern)"
      pattern: "set_config"
---

<objective>
Deliver the copy-on-write, reference-counted `media_asset` data model (V53) with sha256 per-tenant dedup, the `product_media` join, the RLS wall proven under NOSUPERUSER, the per-tenant backfill of existing `image_url` values, and the asset-first dual-read resolver — IMG-01 in full.

Purpose: Products must reference assets and never own bytes; editing a shared asset must mint a new asset and repoint only that product (CoW); a physical MinIO delete must happen only at reference-count 0. This is the durable structural fix behind safe vendor image sharing/dedup.

Output: V53 migration (mirrors V52 `shop_staff` RLS exactly), `MediaAsset`/`ProductMedia` entities + repos, `MediaAssetService` (repoint/releaseAsset/dedup), key-addressed `StorageService` helpers the pipeline consumes, the dual-read resolver, and the four Wave-0 IMG-01 integration tests. Self-contained schema/service work — no normalizer or pipeline dependency; parallel-safe with 24-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-image-architecture-cow-assets-safe-upload-pipeline/24-CONTEXT.md
@.planning/phases/24-image-architecture-cow-assets-safe-upload-pipeline/24-RESEARCH.md
@.planning/phases/24-image-architecture-cow-assets-safe-upload-pipeline/24-PATTERNS.md
@.planning/phases/24-image-architecture-cow-assets-safe-upload-pipeline/24-VALIDATION.md

# Analogs — read the exact lines named in PATTERNS.md
@core-java/src/main/resources/db/migration/V52__shop_staff.sql
@core-java/src/main/resources/db/migration/V44__fts_leakproof_and_vector_backfill.sql
@core-java/src/main/java/uk/jtoye/core/security/access/ShopStaff.java
@core-java/src/main/java/uk/jtoye/core/security/access/ShopStaffRepository.java
@core-java/src/main/java/uk/jtoye/core/storage/StorageService.java
@core-java/src/test/java/uk/jtoye/core/security/access/ShopStaffRlsPolicyIntegrationTest.java
@core-java/src/test/java/uk/jtoye/core/security/RlsContractTest.java

<interfaces>
<!-- The V52 RLS block is the VERBATIM pattern to mirror. Use current_tenant_id(), NEVER ::uuid cast. -->
From V52__shop_staff.sql (46-55 table policy, 62-84 _aud with `tenant_id IS NULL OR tenant_id = current_tenant_id()`).

<!-- StorageService today: URL-addressed delete only; the pipeline needs key-addressed helpers ADDED here. -->
From storage/StorageService.java:
  public String upload(UUID tenantId, String pathPrefix, UUID entityId, MultipartFile file)   // existing
  public void delete(String imageUrl)                                                          // existing (line 222) — URL, not key
  // ADD in this plan: putBytes(String objectKey, byte[] bytes, String contentType),
  //                   byte[] getBytes(String objectKey), void deleteByKey(String objectKey)

<!-- Product flat columns kept this phase for dual-read (D-03a). -->
From product/Product.java: image_url (line 57), additional_image_urls TEXT[] (line 84).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: V53 migration (media_asset + product_media + media_asset_aud + RLS + per-tenant backfill) + entities + repos</name>
  <files>core-java/src/main/resources/db/migration/V53__media_asset.sql, core-java/src/main/java/uk/jtoye/core/media/MediaAsset.java, core-java/src/main/java/uk/jtoye/core/media/ProductMedia.java, core-java/src/main/java/uk/jtoye/core/media/MediaAssetRepository.java, core-java/src/main/java/uk/jtoye/core/media/ProductMediaRepository.java</files>
  <read_first>
    - core-java/src/main/resources/db/migration/V52__shop_staff.sql (lines 46-55 table RLS block, 62-84 `_aud` mirror + nullable-tenant policy)
    - core-java/src/main/resources/db/migration/V44__fts_leakproof_and_vector_backfill.sql (lines 91-133 per-tenant `set_config` loop)
    - core-java/src/main/java/uk/jtoye/core/security/access/ShopStaff.java (entity conventions: @Audited, @GeneratedValue UUID, hand-written accessors, @CreationTimestamp)
    - core-java/src/main/java/uk/jtoye/core/security/access/ShopStaffRepository.java (derived-query + native ON CONFLICT idiom)
    - 24-RESEARCH.md §Pattern 1 (media_asset schema), §Pitfall 4 (backfill zero-rows), §Open-Q2 (media_asset audited / product_media not)
    - 24-PATTERNS.md §"V53__media_asset.sql" backfill block
  </read_first>
  <action>
    Create `db/migration/V53__media_asset.sql`. `media_asset` columns per RESEARCH Pattern 1: `id UUID PK`, `tenant_id UUID NOT NULL`, `object_key TEXT NOT NULL`, `sha256 CHAR(64) NOT NULL`, `content_type VARCHAR(32) NOT NULL`, `width INT`, `height INT`, `bytes BIGINT`, `status VARCHAR(16) NOT NULL CHECK (status IN ('PENDING','ACTIVE','FAILED'))`, `flagged BOOLEAN NOT NULL DEFAULT false`, `failure_reason TEXT`, `uploaded_by UUID`, `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`, plus the pending-placement intent columns consumed by the worker on success (D-04a): `product_id UUID` (nullable target), `is_primary BOOLEAN`, `sort_order INT` — nullable, set on accept, read by the worker to repoint the `product_media` row only on ACTIVE. Add `CREATE UNIQUE INDEX uq_media_asset_tenant_sha ON media_asset (tenant_id, sha256)`. ENABLE + FORCE RLS with `media_asset_tenant_policy` USING/WITH CHECK `tenant_id = current_tenant_id()` — the safe V51 helper, NEVER the raw `::uuid` cast. `product_media` (`id UUID PK`, `tenant_id UUID NOT NULL` carried deliberately for RLS scoping, `product_id UUID NOT NULL REFERENCES products(id)`, `asset_id UUID NOT NULL REFERENCES media_asset(id)`, `is_primary BOOLEAN NOT NULL DEFAULT false`, `sort_order INT NOT NULL DEFAULT 0`) + `idx_product_media_asset (asset_id)` (ref-count query) + partial unique `uq_product_media_primary ON product_media (product_id) WHERE is_primary`, same ENABLE+FORCE RLS block. `media_asset_aud` mirrors `shop_staff_aud` (all cols nullable, PK `(id, rev)`, FK `rev→revinfo`, RLS predicate `tenant_id IS NULL OR tenant_id = current_tenant_id()`); `product_media` stays un-audited (Open-Q2). BACKFILL: mirror the V44 per-tenant loop EXACTLY (`FOR t IN SELECT id FROM tenants LOOP PERFORM set_config('app.current_tenant_id', t.id::text, true); ... END LOOP; PERFORM set_config('app.current_tenant_id','',true);`) — for each product with a non-null `image_url`, INSERT a `status='ACTIVE'` `media_asset` (object_key = the existing key parsed from the URL, sha256 computed, content_type from the extension) as-is WITHOUT re-running the pipeline (D-03) and INSERT a `product_media(is_primary=true)` row; map `additional_image_urls[]` → `product_media(sort_order=n)` preserving array order. Do NOT wrap seed images (`/products/seed/`) — they stay on the flat path (SPEC D1). Create `media/MediaAsset.java` (`@Entity @Table("media_asset") @Audited`, inline `enum Status {PENDING,ACTIVE,FAILED}`, hand-written accessors) and `media/ProductMedia.java` (`@Entity @Table("product_media")`, un-audited, tenant_id carried). Create `MediaAssetRepository` (`Optional<MediaAsset> findByTenantIdAndSha256(UUID,String)` dedup + `findById`) and `ProductMediaRepository` (`long countByAssetId(UUID)` + `@Modifying @Query("UPDATE ProductMedia pm SET pm.assetId=:newAssetId WHERE pm.id=:rowId") void repoint(...)`).
  </action>
  <acceptance_criteria>
    - `./gradlew :core-java:integrationTest --tests '*RlsContractTest*'` green: `everyPublicTableHasRlsAndForce` and `noPolicyUsesRawTenantGucCast` now cover `media_asset` + `product_media` (no raw `::uuid` cast in either policy).
    - Flyway applies V53 cleanly on a fresh Testcontainers DB (migration validates; V53 slot was reserved, `out-of-order=true` already set — no config change).
    - `grep -c 'current_tenant_id()' V53__media_asset.sql` >= 2 and `grep -c '::uuid' <policy lines>` == 0 (policies only).
    - `grep -c 'set_config' V53__media_asset.sql` >= 1 (per-tenant backfill loop present).
  </acceptance_criteria>
  <verify>
    <automated>cd core-java && ./gradlew :core-java:integrationTest --tests '*RlsContractTest*' 2>&1 | tail -15</automated>
  </verify>
  <done>V53 creates the RLS-forced media_asset/product_media/_aud tables via the safe helper, dedup index in place, and the per-tenant backfill loop wraps existing image_url values into ACTIVE assets + product_media rows. Entities + repos exist.</done>
</task>

<task type="auto">
  <name>Task 2: MediaAssetService (CoW repoint + reference-counted delete + dedup) + key-addressed StorageService helpers + dual-read resolver</name>
  <files>core-java/src/main/java/uk/jtoye/core/media/MediaAssetService.java, core-java/src/main/java/uk/jtoye/core/storage/StorageService.java, core-java/src/main/java/uk/jtoye/core/product/ProductService.java, core-java/src/main/java/uk/jtoye/core/product/ProductMapper.java</files>
  <read_first>
    - core-java/src/main/java/uk/jtoye/core/media/MediaAssetRepository.java + ProductMediaRepository.java (Task 1)
    - core-java/src/main/java/uk/jtoye/core/storage/StorageService.java (lines 74-116 upload/put idiom, 170-215 putSeedImage S3 put shape, 222 delete)
    - core-java/src/main/java/uk/jtoye/core/product/ProductService.java (line 269 uploadImage, toDto sites) + ProductMapper.java
    - 24-RESEARCH.md §Pattern 2 (CoW repoint + releaseAsset), §"Asset-first dual-read resolver" code example (D-03a)
  </read_first>
  <action>
    Add key-addressed helpers to `StorageService` the pipeline needs (single owner of MinIO I/O): `String putBytes(String objectKey, byte[] bytes, String contentType)` (S3 PutObject keyed directly, Content-Type set from the DETECTED/produced type — never `file.getContentType()`, closing the RESEARCH anti-pattern), `byte[] getBytes(String objectKey)` (GetObject for the worker to read quarantine), `void deleteByKey(String objectKey)` (DeleteObject by key — used for quarantine cleanup and ref-count-0 delete). Create `media/MediaAssetService.java`: `repoint(UUID productMediaRowId, UUID newAssetId)` = `productMediaRepository.repoint(...)` (the one-row CoW UPDATE, D-01); `releaseAsset(UUID oldAssetId)` = if `productMediaRepository.countByAssetId(oldAssetId)==0` then `storageService.deleteByKey(asset.getObjectKey())` + `mediaAssetRepository.delete(asset)` (physical MinIO delete ONLY at ref-count 0, IMG-01); `findDedup(UUID tenantId, String sha256)` = `mediaAssetRepository.findByTenantIdAndSha256(...)` (dedup short-circuit). Add the asset-first dual-read resolver to `ProductService`/`ProductMapper`: `resolveImageUrl(Product p)` returns the primary ACTIVE derivative URL from `product_media` if present, else falls back to `p.getImageUrl()` — wire it at the existing `toDto` sites so both storefront and dashboard read asset-first (D-03a). Keep the flat columns (dropped in a later phase).
  </action>
  <acceptance_criteria>
    - `MediaCopyOnWriteIntegrationTest#repointOnlyAffectsOneRow` green: an asset shared by two product_media rows, repointing one leaves the other's asset_id unchanged.
    - `#deletesOnlyAtRefCountZero` green: releasing the last ref calls `deleteByKey` + deletes the row; a still-referenced asset is neither deleted from MinIO nor the DB.
    - `#identicalUploadDedupsPerTenant` green: `findByTenantIdAndSha256` returns the existing ACTIVE asset for an identical raw sha256 within a tenant (unique index enforces it).
    - Dual-read: a product with an ACTIVE derivative resolves to the derivative URL; a product with only `image_url` resolves to the flat column (both proven by the integration test or a ProductService unit test).
  </acceptance_criteria>
  <verify>
    <automated>cd core-java && ./gradlew :core-java:integrationTest --tests '*MediaCopyOnWriteIntegrationTest*' 2>&1 | tail -15</automated>
  </verify>
  <done>CoW repoint, ref-count-0 delete, and sha256 dedup work over real Postgres; StorageService owns key-addressed put/get/delete; the dual-read resolver returns asset-first with image_url fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 IMG-01 integration tests — RLS-under-NOSUPERUSER + CoW/ref-count/dedup + non-fresh-DB backfill</name>
  <files>core-java/src/test/java/uk/jtoye/core/media/MediaAssetRlsPolicyIntegrationTest.java, core-java/src/test/java/uk/jtoye/core/media/MediaCopyOnWriteIntegrationTest.java, core-java/src/test/java/uk/jtoye/core/media/MediaBackfillMigrationIntegrationTest.java, core-java/src/test/java/uk/jtoye/core/security/RlsContractTest.java</files>
  <read_first>
    - core-java/src/test/java/uk/jtoye/core/security/access/ShopStaffRlsPolicyIntegrationTest.java (lines 74-81 NOSUPERUSER role, 94-118 seed+downgrade, 124-168 cross-tenant assertions — the exact template)
    - core-java/src/main/resources/db/migration/V44__fts_leakproof_and_vector_backfill.sql (per-tenant backfill semantics to assert against)
    - core-java/src/main/java/uk/jtoye/core/media/MediaAssetService.java (Task 2)
    - 24-VALIDATION.md §Per-Requirement Verification Map (IMG-01 rows) + §Wave 0 Requirements
    - trap_rls_migration_backfill (the non-fresh-DB backfill trap the migration test must catch)
  </read_first>
  <action>
    Create `MediaAssetRlsPolicyIntegrationTest.java` by cloning `ShopStaffRlsPolicyIntegrationTest`: `@SpringBootTest @Testcontainers @ActiveProfiles("test") @Tag("testcontainers")`, provision `rls_test_role NOSUPERUSER NOBYPASSRLS`, `SET LOCAL ROLE` downgrade per RLS-sensitive tx, seed media_asset/product_media under `TenantContext.set(tenantA)`, assert cross-tenant reads return 0 rows and cross-tenant writes throw with `row-level security` in the stack trace. Create `MediaCopyOnWriteIntegrationTest.java` with `repointOnlyAffectsOneRow` / `deletesOnlyAtRefCountZero` / `identicalUploadDedupsPerTenant` (the Task-2 acceptance behaviors). Create `MediaBackfillMigrationIntegrationTest.java` that proves the V53 backfill under a NOSUPERUSER migration-role fixture on a NON-fresh DB (seed products with `image_url`/`additional_image_urls[]` across TWO tenants BEFORE V53 via a superuser bypass, then apply V53 under the RLS-bound role, and assert every product got its `product_media` rows — falsifiable RED against a bare no-GUC UPDATE which would migrate ZERO rows, mirroring the 23-17 `V57GrantSourceBackfillIntegrationTest` proof). Extend `RlsContractTest` fixtures so the sweep includes `media_asset`/`product_media` (already asserted green in Task 1 — ensure the fixture lists them).
  </action>
  <acceptance_criteria>
    - `MediaAssetRlsPolicyIntegrationTest` green under the NOSUPERUSER downgrade: cross-tenant read == 0 rows, cross-tenant write throws `row-level security` (VALIDATION IMG-01 T-cross-tenant).
    - `MediaBackfillMigrationIntegrationTest` green on a non-fresh two-tenant DB AND demonstrably RED against a bare no-GUC UPDATE variant (the per-tenant set_config loop is load-bearing).
    - All three media tests + `RlsContractTest` pass in `:core-java:integrationTest`.
    - New `@Test` methods counted toward the docs/metrics.json reconcile (done at the phase gate, 24-06).
  </acceptance_criteria>
  <verify>
    <automated>cd core-java && ./gradlew :core-java:integrationTest --tests '*MediaAssetRlsPolicyIntegrationTest*' --tests '*MediaCopyOnWriteIntegrationTest*' --tests '*MediaBackfillMigrationIntegrationTest*' 2>&1 | tail -20</automated>
  </verify>
  <done>IMG-01 is proven end-to-end over real Postgres: RLS wall under NOSUPERUSER, CoW repoint, ref-count-0 delete, per-tenant dedup, and non-fresh-DB backfill (RED against the zero-rows trap).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant A ↔ tenant B | Cross-tenant access to `media_asset`/`product_media` rows and MinIO objects must be impossible |
| migration role → FORCE-RLS tables | The V53 backfill runs as the RLS-bound migration role; a bare UPDATE sees zero rows |
| client filename → object_key | The stored key must be server-generated, never client-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Information Disclosure / Tampering | cross-tenant asset read/delete | mitigate | ENABLE+FORCE RLS via `current_tenant_id()` on media_asset + product_media, proven under NOSUPERUSER (`MediaAssetRlsPolicyIntegrationTest`); ref-count delete runs inside the tenant GUC |
| T-24-06 | Tampering | path traversal via object_key | mitigate | object_key is server-generated (`<tenant>/media/<assetId>.webp`); the backfill parses only the existing trusted key, never interpolates a client filename |
| T-24-07 | Denial of Service (data-integrity) | V53 backfill zero-rows | mitigate | Per-tenant `set_config` loop (V44 pattern); `MediaBackfillMigrationIntegrationTest` proven RED against a bare UPDATE (`trap_rls_migration_backfill`) |
| T-24-08 | Elevation of Privilege | RLS policy raw `::uuid` cast reintroduction | mitigate | `RlsContractTest.noPolicyUsesRawTenantGucCast` sweep covers the new tables; safe helper only |
</threat_model>

<verification>
- `RlsContractTest` (FORCE + no-raw-cast) green over media_asset + product_media.
- `MediaAssetRlsPolicyIntegrationTest` cross-tenant isolation green under NOSUPERUSER.
- `MediaCopyOnWriteIntegrationTest` (repoint / ref-count-0 / dedup) green.
- `MediaBackfillMigrationIntegrationTest` green on a non-fresh DB and RED against the bare-UPDATE trap.
- Dual-read resolver returns asset-first with image_url fallback.
</verification>

<success_criteria>
IMG-01 complete: products reference `media_asset` rows via `product_media`, copy-on-write mints + repoints a single row, physical MinIO delete happens only at reference-count 0, sha256 dedups per tenant, existing images are backfilled under a per-tenant GUC loop (not zero rows), and reads resolve asset-first — all RLS-isolated under the NOSUPERUSER role-downgrade.
</success_criteria>

<output>
Create `.planning/phases/24-image-architecture-cow-assets-safe-upload-pipeline/24-02-SUMMARY.md` when done. Record the media_asset pending-placement column shape (product_id/is_primary/sort_order) — 24-03 (accept) sets them, 24-04 (worker) consumes them. Note that the vendor-triggered delete paths (`ProductService.removeImage` / `removeAdditionalImage`) are wired to `releaseAsset` in 24-05 Task 3 (they consume this plan's `releaseAsset` + `countByAssetId`).
</output>
</content>
