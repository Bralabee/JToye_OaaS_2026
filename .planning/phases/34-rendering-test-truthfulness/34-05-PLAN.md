---
phase: 34-rendering-test-truthfulness
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/e2e/dashboard-mobile.spec.ts
autonomous: true
requirements: [TRUTH-02]

must_haves:
  truths:
    - "All eleven dashboard routes are asserted free of horizontal overflow at 375px, not just /dashboard"
    - "The mobile Playwright project still runs at 390x844 — no shared baseline is moved"
    - "The 375px sweep fails when a route overflows, proven by injecting an overflow"
    - "What #286 still asks for, and what it no longer asks for, is recorded in the file that answers it"
  artifacts:
    - path: "frontend/e2e/dashboard-mobile.spec.ts"
      provides: "an additive 375px ROUTES sweep beside the existing 390px sweep"
      contains: "375"
  key_links:
    - from: "frontend/e2e/dashboard-mobile.spec.ts (375px sweep)"
      to: "the ROUTES array at :247-259"
      via: "the same eleven routes the 390px sweep uses"
      pattern: "for \\(const route of ROUTES\\)"
---

<objective>
Narrow #286 to what is actually outstanding and close that: assert the eleven-route dashboard sweep
has no horizontal overflow at 375px, additively, without touching the Playwright mobile project.

ROADMAP criterion 3, verbatim: *"#286 is narrowed to what is actually outstanding, not closed whole
... the `/dashboard/staff` click-through already runs live ... What remains is that `dashboard-mobile`
runs at 390 × 844 (`frontend/playwright.config.ts:84`), not the 375px the issue asks for, and carries
9 route stubs — which is #542's complaint, not a separate one."*

Purpose: the 375px coverage that exists today is ONE route (`/dashboard`, the describe at
`dashboard-mobile.spec.ts:424`); the eleven-route sweep runs only at 390px. Changing the project
viewport to 375 would silently move every mobile spec, the `mobile-instrument-contract` assertions
and every mobile perf baseline — the Incremental Betterment Doctrine forbids trading a working good
for a new one, so the coverage is extended additively instead.

Output: one new describe block in `dashboard-mobile.spec.ts` and a docblock stating the narrowed
#286 position with its measurements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/34-rendering-test-truthfulness/34-RESEARCH.md
@.planning/phases/34-rendering-test-truthfulness/34-PATTERNS.md
@.claude/skills/proof-standards/SKILL.md

@frontend/e2e/dashboard-mobile.spec.ts
</context>

<interfaces>
<!-- Already in the file; the new block reuses them and defines nothing new. -->
const ROUTES: Array<{ path: string; name: string; titleHasH1: boolean }>   // :247-259, eleven entries
async function setupStubs(context: BrowserContext): Promise<void>          // :261-302, nine context.route calls
async function vendorLogin(page: Page): Promise<void>                      // :100-140
function tabBarOf(page: Page)   // getByRole("navigation", { name: "Primary" }) — immune to the streaming staging buffer
function live(page: Page)       // page.locator("body > div:not([hidden])") — excludes React's hidden staged shell copy

<!-- The overflow assertion to lift, from the existing 375px block at :460-467 -->
const geom = await page.evaluate(() => ({ docScrollWidth: document.documentElement.scrollWidth, viewportWidth: window.innerWidth, mainWidth: ... }))
expect(geom.docScrollWidth).toBeLessThanOrEqual(geom.viewportWidth + 1)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the eleven-route 375px no-overflow sweep</name>
  <files>frontend/e2e/dashboard-mobile.spec.ts</files>
  <read_first>
    - frontend/e2e/dashboard-mobile.spec.ts (whole file — ROUTES at :247-259, setupStubs at :261-302, the 390px sweep at :304-422, the existing 375px block at :424-500)
    - frontend/playwright.config.ts:73-112 (why the mobile project's 390x844 must not move)
    - frontend/e2e/dashboard-interface-corrections.spec.ts:15-20 (the vacuous-pass this surface already documents: with the API refusing, the switcher falls back to a zero-grant state and a count reads 0 — a "pass" over a page that is not working)
  </read_first>
  <action>
    Add ONE new describe, placed after the existing 375px block, titled to distinguish it from its
    390px sibling — for example "Dashboard mobile shell (375px) — the eleven-route sweep has no
    horizontal overflow". Pin `test.use({ viewport: { width: 375, height: 812 }, isMobile: true })`
    inside the describe, exactly as the two existing blocks do at :309 and :425, and state in a
    comment that the pin is what makes the block correct under BOTH Playwright projects and that
    `playwright.config.ts` is deliberately not touched.

    `beforeEach` mirrors :311-315: `await setupStubs(context)` then `await vendorLogin(page)`. Loop
    `for (const route of ROUTES)` and, per route: `page.goto(`${BASE}${route.path}`, { waitUntil:
    "domcontentloaded" })`; assert the PRESENCE CONTROL first — `tabBarOf(page)` visible and, when
    `route.titleHasH1`, `live(page).locator("main h1").first()` visible with a 10s timeout — because
    an empty or errored page satisfies an overflow assertion trivially and that is precisely the
    vacuous shape this file already documents; then read the geometry with the same `page.evaluate`
    shape as :460-467 and assert `docScrollWidth <= viewportWidth + 1` and `mainWidth >= 300`.

    Do not add a `@desktop-only` / `@mobile-only` tag: the existing 390px and 375px blocks are
    enumerated by both projects by design, and consistency with the sibling blocks is worth more
    than halving the run. Do not change `setupStubs`, the ROUTES array, the 390px block or any
    existing assertion.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test e2e/dashboard-mobile.spec.ts --project=mobile</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx playwright test e2e/dashboard-mobile.spec.ts --project=mobile` and `--project=desktop` are both green against the live Compose stack; record the pass counts before and after the edit (the delta must be exactly the eleven new tests per project).
    - BREAK ARM, executed on a real route: inside the loop, temporarily inject an overflowing element (`await page.evaluate(() => { const d = document.createElement("div"); d.style.width = "1200px"; d.style.height = "1px"; document.body.appendChild(d) })`) before the geometry read, run the sweep, and confirm it goes RED naming `docScrollWidth`. Record the measured widths from the failure. Remove the injection and verify the restore by `git hash-object` against the committed blob (commit before running the arm).
    - BREAK ARM for the presence control: point one iteration at a route that 404s, confirm the block goes RED on the presence assertion rather than passing on the overflow one; restore.
    - `bash scripts/check-playwright-mobile-contract.sh` exits 0, and `git diff --name-only -- frontend/playwright.config.ts` prints nothing — the shared mobile baseline was not moved.
    - `cd frontend && npx playwright test e2e/mobile-instrument-contract.spec.ts --project=mobile` is green (the 390x844 contract this plan must not disturb).
    - `bash scripts/check-e2e-typecheck.sh` exits 0.
  </acceptance_criteria>
  <done>Eleven dashboard routes are asserted overflow-free at 375px, the assertion has been shown to fail, and no shared viewport baseline moved.</done>
</task>

<task type="auto">
  <name>Task 2: Record the narrowed #286 position in the file that answers it</name>
  <files>frontend/e2e/dashboard-mobile.spec.ts</files>
  <read_first>
    - frontend/e2e/dashboard-mobile.spec.ts:1-24 (the existing docblock this extends)
    - frontend/e2e/dashboard-interface-corrections.spec.ts:40-70 (the live-auth sibling: 3 vendorLogin references, 0 `.route(` calls — the measurement being cited)
    - .planning/phases/34-rendering-test-truthfulness/34-RESEARCH.md — the re-measurement ledger rows for #286
  </read_first>
  <action>
    Extend the spec's docblock with a dated "#286, NARROWED — measured 2026-08-28" stanza recording
    four things, each with the measurement that supports it rather than a claim:

    1. The `/dashboard/staff` click-through half is already satisfied live by
       `dashboard-interface-corrections.spec.ts` — quote the measured figures (3 `vendorLogin`
       references, 0 `context.route` calls) and re-run the count yourself rather than copying it.
    2. The 375px half is closed by the new sweep, naming the block and the eleven routes.
    3. The nine `context.route` stubs in `setupStubs` (`:267, :275, :280, :284, :288, :294, :300,
       :301` plus `:433`) REMAIN, deliberately: they exist so this dashboard surface can be driven
       without depending on seeded tenant data, and they are #542's complaint about SSR coverage
       rather than a #286 defect. Every route this file drives is a client-rendered dashboard page,
       so no raw-HTML assertion could cover them — record that, and record that the
       client-component classification of all eleven routes is declared in
       `scripts/gates/ssr-routes.conf` (plan 34-07).
    4. The mobile project stays at 390x844 and why.

    Re-count the stubs with `rg -uu -c 'context\.route\(' frontend/e2e/dashboard-mobile.spec.ts`
    rather than trusting the nine from the issue, and write the number you measured.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test e2e/dashboard-mobile.spec.ts --list --project=desktop</automated>
  </verify>
  <acceptance_criteria>
    - `rg -uu -c 'context\.route\(' frontend/e2e/dashboard-mobile.spec.ts` prints the number written into the docblock (record it; RESEARCH measured 9 across the file).
    - `rg -uu -c 'vendorLogin' frontend/e2e/dashboard-interface-corrections.spec.ts` and `rg -uu -c 'context\.route\(' frontend/e2e/dashboard-interface-corrections.spec.ts` print the two numbers quoted in the docblock (RESEARCH measured 3 and 0). If either differs from RESEARCH, write what you measured and flag the difference — do not copy the research figure.
    - The docblock stanza names all four points above, and the `--list` enumeration is unchanged by this task (a comment must not alter the test count): record the totals before and after.
    - `bash scripts/check-e2e-typecheck.sh` exits 0.
  </acceptance_criteria>
  <done>The narrowed #286 is written where the next reader of the spec will find it, with re-measured numbers rather than inherited ones.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright vendor login → Keycloak dev realm | The sweep authenticates as the seeded `admin-user`; the credential comes from `e2e/vendor-credentials.ts` |
| stubbed API responses → dashboard UI | `setupStubs` fulfils `/api/v1/**` in the browser; the responses are fixtures, not tenant data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05-01 | Information Disclosure | vendor credentials in a new block | mitigate | The new describe calls the existing `vendorLogin`/`skipWithoutVendorPassword` helpers; no credential literal is added. `rg -uu -n 'password' frontend/e2e/dashboard-mobile.spec.ts` must show no new literal |
| T-34-05-02 | Repudiation | an overflow assertion that cannot fail | mitigate | The break arm injects a 1200px element and records the red; the presence control prevents an empty page from satisfying the block |
| T-34-05-03 | Tampering | moving the shared mobile viewport | mitigate | `playwright.config.ts` is not in `files_modified`; `git diff --name-only` on it must be empty and `check-playwright-mobile-contract.sh` must exit 0 |
| T-34-05-04 | Spoofing | stubbed API masking a real authorisation failure | accept | Pre-existing and documented in this spec's siblings; the presence controls make a zero-grant fallback visible rather than silent |
| T-34-05-SC | Tampering | npm installs | mitigate | Nothing is installed; `git diff --name-only -- frontend/package.json frontend/package-lock.json` must be empty |
</threat_model>

<verification>
- `cd frontend && npx playwright test e2e/dashboard-mobile.spec.ts e2e/mobile-instrument-contract.spec.ts --project=mobile --project=desktop` is green.
- `bash scripts/check-playwright-mobile-contract.sh` exits 0.
- `bash scripts/check-e2e-typecheck.sh` exits 0.
- `git diff --name-only -- frontend/playwright.config.ts frontend/package.json frontend/package-lock.json` prints nothing.
- docs/metrics.json is NOT regenerated here — plan 34-10 is its single writer (this plan adds test declarations, so 34-10 must run `scripts/docs-freshness.sh --write`).
</verification>

<success_criteria>
- Eleven dashboard routes carry a 375px no-horizontal-overflow assertion with a presence control, and the assertion was shown to fail against an injected overflow.
- The Playwright mobile project is untouched at 390x844 and its instrument contract is still green.
- The narrowed #286 — what is done, what remains, and what belongs to #542 — is recorded with re-measured numbers in the spec's own docblock.
</success_criteria>

<output>
Create `.planning/phases/34-rendering-test-truthfulness/34-05-SUMMARY.md` when done, with the
before/after enumeration counts, both break-arm outputs, the restore hash, and every number you
re-measured (flagging any that differed from RESEARCH).
</output>
