---
phase: 34-rendering-test-truthfulness
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/e2e/onboarding-blocked-flow.spec.ts
  - scripts/gates/e2e-skip-budget.conf
autonomous: true
requirements: [TRUTH-02]

must_haves:
  truths:
    - "The mobile Playwright project no longer enumerates the single-tenant onboarding journey, so it is no longer counted as unverified surface"
    - "The desktop project still runs that journey"
    - "The skip budget's ceiling is 6, so the remaining skips cannot hide behind headroom"
    - "The two false statements in the skip-budget config are corrected, not carried forward"
  artifacts:
    - path: "frontend/e2e/onboarding-blocked-flow.spec.ts"
      provides: "the journey tagged @desktop-only with its reason, and the runtime project pin deleted"
      contains: "@desktop-only"
    - path: "scripts/gates/e2e-skip-budget.conf"
      provides: "MAX_SKIPS 6 with two ALLOW entries and a measured, not computed, total"
      contains: "MAX_SKIPS 6"
  key_links:
    - from: "frontend/e2e/onboarding-blocked-flow.spec.ts"
      to: "frontend/playwright.config.ts grepInvert"
      via: "the @desktop-only tag in the test title"
      pattern: "@desktop-only"
---

<objective>
Home the one E2E skip that has no owner, by correcting its diagnosis rather than inheriting it.

ROADMAP criterion 5, verbatim: *"#547 closes by closing its children, not by duplicating them — its
own body says so. 6 of the 7 skips are owned by #304 (Phase 29) and #61 (Phase 30); the
`onboarding-blocked-flow` mobile skip is the one with no owner and is this phase's to home. The skip
budget currently sits at 7 against a ceiling of 8."*

Purpose: both #547 and `scripts/gates/e2e-skip-budget.conf:46-48` say this skip needs a
`DemoDataSeeder` shop. The nightly report's own annotation says otherwise —
`skip=single-tenant onboarding journey pinned to the desktop project (UNIQUE(tenant_id) — no
cross-worker race)` — and the code proves it: the project pin fires at
`onboarding-blocked-flow.spec.ts:116`, BEFORE the no-shop guard at :169, and the desktop project
PASSES, which is only possible if the fixture is present. So this is a "not applicable here" skip,
and `playwright.config.ts` already has the mechanism for that: a `@desktop-only` tag plus the
mobile project's `grepInvert`. A skip must mean "nobody checked this"; it cannot also mean "not
applicable here" and stay useful.

Output: the tagged spec and a corrected, tightened skip-budget config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/34-rendering-test-truthfulness/34-RESEARCH.md
@.planning/phases/34-rendering-test-truthfulness/34-PATTERNS.md
@.claude/skills/proof-standards/SKILL.md

@scripts/gates/e2e-skip-budget.conf
@frontend/playwright.config.ts
</context>

<interfaces>
<!-- playwright.config.ts:73-112 — the mechanism -->
mobile  project: grepInvert: /@desktop-only/   // excludes from ENUMERATION, not a runtime skip
desktop project: grepInvert: /@mobile-only/

<!-- The three test.skip calls in onboarding-blocked-flow.spec.ts, and their fate -->
:116-119  test.skip(testInfo.project.name !== "desktop", ...)   -> DELETE (replaced by the tag)
:120-124  test.skip(!VENDOR_PASSWORD, ...)                       -> KEEP (a genuine "nobody checked this")
:152-157  test.skip(true, "already LIVE/terminal ...")           -> KEEP
:169-171  test.skip(true, "No shop available ...")               -> KEEP

<!-- scripts/gates/e2e-skip-budget.conf, current state -->
ALLOW stomp-relay.spec.ts                                            // 2 tests x 2 projects = 4, owned by #304
ALLOW vendor-refund-flow.spec.ts › vendor logs in, ... partial refund // 1 test x 2 projects = 2, owned by #61
ALLOW onboarding-blocked-flow.spec.ts                                 // 1 test, MOBILE ONLY = 1  -> DELETE
MAX_SKIPS 8                                                           // -> 6
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Tag the journey @desktop-only and delete the runtime project pin</name>
  <files>frontend/e2e/onboarding-blocked-flow.spec.ts</files>
  <read_first>
    - frontend/e2e/onboarding-blocked-flow.spec.ts:100-180 (the test title, all four skip calls, and the UNIQUE(tenant_id) rationale that must survive)
    - frontend/e2e/storefront-ssr-seo.spec.ts:72-77 (the in-repo `@desktop-only` tag with its reason stated inline)
    - frontend/e2e/dashboard-interface-corrections.spec.ts:97-105 (the in-repo model for "this tag is deliberate rather than a coverage gap", with a measured justification)
    - frontend/playwright.config.ts:73-112 (grepInvert on both projects, and the "a skip must mean nobody checked this" rationale)
  </read_first>
  <action>
    Append ` @desktop-only` to the test title at `:109` so it reads
    `bad company number -> fix inline -> re-run checks -> honest in-review @desktop-only`, and delete
    the `test.skip(testInfo.project.name !== "desktop", ...)` call at `:116-119`. Remove the now
    unused `testInfo` parameter from the test callback if nothing else uses it — a stale unused
    parameter is a lint warning and a false clue.

    Move the existing four-line UNIQUE(tenant_id) rationale into a docblock immediately above the
    test, in the style of `dashboard-interface-corrections.spec.ts:97-105`, and extend it with the
    measured correction: the nightly report's own annotation for this skip, the fact that the
    desktop project PASSES (so the `DemoDataSeeder` shop IS present and the conf's stated cause was
    wrong), and the fact that the no-shop guard at the end of the file is a DIFFERENT, genuine skip
    that stays.

    Do NOT touch the other three `test.skip` calls (`:120-124` password guard, `:152-157`
    LIVE/terminal guard, `:169-171` no-shop guard) — each is a real "nobody checked this".
  </action>
  <verify>
    <automated>cd frontend && npx playwright test e2e/onboarding-blocked-flow.spec.ts --list --project=mobile && npx playwright test e2e/onboarding-blocked-flow.spec.ts --list --project=desktop</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx playwright test e2e/onboarding-blocked-flow.spec.ts --list --project=mobile` reports `Total: 0 tests` (the mobile project no longer ENUMERATES the block), and `--list --project=desktop` reports 1. Record both, and record what each reported BEFORE the edit (expected: 1 and 1) — the before/after pair is the whole proof.
    - BREAK ARM: remove the `@desktop-only` tag, re-run the mobile `--list`, confirm it reports 1 again; restore and verify by `git hash-object` against the committed blob (commit before running the arm).
    - `rg -uu -c 'test\.skip\(' frontend/e2e/onboarding-blocked-flow.spec.ts` prints 3 after the edit (was 4 — record both).
    - `rg -uu -n 'testInfo' frontend/e2e/onboarding-blocked-flow.spec.ts` prints nothing, or every remaining hit is a real use.
    - `cd frontend && npx playwright test e2e/onboarding-blocked-flow.spec.ts --project=desktop` still runs the journey against the live stack; record the outcome verbatim, including which of the three remaining guards (if any) fired and why — a run that skipped for a legitimate reason must be reported as a skip, not as a pass.
    - `bash scripts/check-e2e-typecheck.sh` exits 0 and `cd frontend && npx eslint .` exits 0.
  </acceptance_criteria>
  <done>The mobile project stops counting a journey it was never meant to run, and the desktop journey is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the stale ALLOW, lower the ceiling to 6, and correct the config's two false claims</name>
  <files>scripts/gates/e2e-skip-budget.conf</files>
  <read_first>
    - scripts/gates/e2e-skip-budget.conf (whole file — the RETIRING protocol at :25-29 is the instruction being executed; the wrong-cause comment at :46-48; the stale arithmetic at :51-56)
    - scripts/check-e2e-skip-budget.sh:1-120 (the S-1..S-4 contract, the exit codes, and the digest freshness check)
    - .planning/phases/34-rendering-test-truthfulness/34-RESEARCH.md — "Pitfall 4" (the gate VOIDs today on a digest mismatch) and "Criterion 5"
  </read_first>
  <action>
    Execute the file's own retirement protocol. Delete `ALLOW onboarding-blocked-flow.spec.ts`
    (`:49`) together with its three-line justification at `:46-48` — do NOT carry that text forward
    anywhere: it names a cause ("needs a shop for the demo tenant") that was measured false.

    Lower `MAX_SKIPS` from 8 to 6 and replace the stale arithmetic comment at `:51-56`. The
    replacement states the measured composition — 4 from `stomp-relay.spec.ts` (2 tests x 2
    projects, #304) plus 2 from `vendor-refund-flow.spec.ts` (1 test x 2 projects, #61) — plus HOW
    it was measured (the nightly report's per-test skip annotations, read with `jq`, naming the run
    id and the date) and the correction it supersedes: the old comment's "Total = 8 (4 distinct
    tests x 2 projects)" was wrong because the onboarding skip was mobile-only, so the real total was
    7. Add one line stating that the figure is re-earned by plan 34-10's full-suite run and must not
    be hand-computed.

    State plainly, in the same stanza, WHY the ceiling has to move: at `MAX_SKIPS 8` the gate passes
    on 7 and would pass on 6 — lowering the ceiling is what makes this criterion falsifiable at all.
  </action>
  <verify>
    <automated>bash scripts/check-e2e-skip-budget.sh; echo "rc=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -uu -c '^ALLOW' scripts/gates/e2e-skip-budget.conf` prints 2 (was 3 — record both), and `rg -uu -n 'onboarding-blocked-flow' scripts/gates/e2e-skip-budget.conf` prints nothing.
    - `rg -uu -n '^MAX_SKIPS' scripts/gates/e2e-skip-budget.conf` prints `MAX_SKIPS 6`.
    - `rg -uu -n 'demo tenant|DemoDataSeeder' scripts/gates/e2e-skip-budget.conf` prints nothing — the false cause is gone, not relocated.
    - `bash scripts/check-e2e-skip-budget.sh` exits **2 (VOID)** today and its message names the missing or stale report. Record the rc captured on the same line as the command, and record WHY it is VOID (RESEARCH Pitfall 4: the tree's spec digest no longer matches any nightly, and this plan's own spec edit changes it again). A VOID here is the honest state — it must NOT be worked around, and it must NOT be reported as a pass. Plan 34-10 re-earns it after every spec edit in the phase has landed.
    - PARSER ARM (available without a report): append a line `NONSENSE foo` to the conf, run the gate, confirm it exits 2 naming the unknown directive — proving the config is actually being read and that this plan's edits are parsed at all; then delete the line and verify by `git hash-object`.
    - CEILING ARM (recorded, not left in place): with `MAX_SKIPS 8` restored the gate cannot distinguish 6 skips from 8, which is the reason for the change; state this in the SUMMARY with the conf's own arithmetic rather than claiming a gate run that no report can support.
  </acceptance_criteria>
  <done>The stale exemption and its false justification are gone, the ceiling is 6, and the config's arithmetic is a measurement with a stated method.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E report artifact → skip-budget gate | The gate's only input is a Playwright JSON report whose freshness is decided by a content digest |
| declared exemptions → merge decision | An ALLOW entry is a standing claim that unverified surface is acceptable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-06-01 | Repudiation | a stale ALLOW that matches nothing | mitigate | The entry is deleted rather than left "harmless"; the gate's S-3 assertion fails on a stale ALLOW, and 34-10's fresh report is what exercises it |
| T-34-06-02 | Repudiation | budget headroom hiding a new skip | mitigate | `MAX_SKIPS` drops to the measured 6 so any new skip is over budget immediately; the plan records that 8 could not have caught this |
| T-34-06-03 | Tampering | a runtime skip that means "not applicable" | mitigate | Replaced by an enumeration-level tag, proven by the `--list` before/after pair and its break arm |
| T-34-06-04 | Denial of Service | reading a VOID gate as a pass | mitigate | The acceptance criteria REQUIRE recording rc=2 and its cause; no workaround is permitted, and the re-earn is assigned to plan 34-10 |
| T-34-06-SC | Tampering | npm installs | mitigate | Nothing is installed; `git diff --name-only -- frontend/package.json frontend/package-lock.json` must be empty |
</threat_model>

<verification>
- `cd frontend && npx playwright test e2e/onboarding-blocked-flow.spec.ts --list --project=mobile` reports 0 tests; `--project=desktop` reports 1.
- `bash scripts/check-e2e-skip-budget.sh` exits 2 with a stale/absent-report message (recorded, not worked around).
- `bash scripts/check-e2e-typecheck.sh` exits 0 and `cd frontend && npx eslint .` exits 0.
- `git diff --name-only -- frontend/playwright.config.ts` prints nothing.
- docs/metrics.json is NOT regenerated here — a tag does not change the static declaration count, and 34-10 is the single writer regardless.
</verification>

<success_criteria>
- The single-tenant onboarding journey is `@desktop-only` and the mobile project no longer enumerates it, proven by a before/after `--list` pair and a break arm.
- `e2e-skip-budget.conf` declares two ALLOW entries and `MAX_SKIPS 6`, with a measured composition and a stated measurement method.
- Both false statements the config carried — the wrong skip cause and the wrong total — are corrected rather than copied forward.
- The gate's current VOID state is recorded as VOID, with the re-earn assigned to plan 34-10.
</success_criteria>

<output>
Create `.planning/phases/34-rendering-test-truthfulness/34-06-SUMMARY.md` when done, with the
before/after `--list` counts, the parser arm's output, the recorded rc=2 and its cause, and the
restore hashes.
</output>
