---
phase: 23-vendor-scoped-access-responsive-dashboard-nav
plan: 15
type: execute
wave: 5
depends_on: [23-08, 23-09, 23-10, 23-11, 23-12, 23-13, 23-14]
gap_closure: true
files_modified:
  - docs/api/openapi-snapshot.json
  - docs/metrics.json
  - CLAUDE.md
  - AGENTS.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-VALIDATION.md
  - .planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-CONTEXT.md
  - .planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/deferred-items.md
autonomous: true
requirements: [VSA-02, VSA-04]

must_haves:
  truths:
    - "CI is green: the OpenAPI snapshot check no longer fails, and docs-freshness check mode exits 0"
    - "The OpenAPI snapshot contains all four /api/v1/staff endpoints, including the new /me"
    - "23-VALIDATION.md's per-requirement map lists every new gap-closure proof with its automated command"
    - "REQUIREMENTS.md reflects the real state of VSA-02 and VSA-04 rather than a stale marking"
    - "The revised D-04/D-12/D-05 semantics are recorded in 23-CONTEXT.md so the locked-decision record matches what shipped"
  artifacts:
    - path: "docs/api/openapi-snapshot.json"
      provides: "Regenerated contract including the staff surface"
      contains: "api/v1/staff"
    - path: "docs/metrics.json"
      provides: "Reconciled test counts after the gap-closure suites"
  key_links:
    - from: "docs/metrics.json"
      to: "CLAUDE.md"
      via: "scripts/docs-freshness.sh --write reconciling both"
      pattern: "total_logical_invocations"
---

<objective>
Close the two known-red CI gates carried by this phase and reconcile the planning record with
what actually shipped.

Purpose: this is the phase-gate plan. Two gates are known red and both are regeneration steps,
not code defects:
- `docs/api/openapi-snapshot.json` lacks the `/api/v1/staff` endpoints (`grep -c "api/v1/staff"`
  returns 0). `OpenApiSnapshotTest` runs in **check** mode inside `integrationTest`, so the phase
  PR fails until the snapshot is regenerated and committed. 23-04 deferred this to "the phase-gate
  reconcile"; 23-06 owned that gate but could not execute it, because the task boots the full
  Spring context against a throwaway Testcontainers Postgres and that session was under a
  no-Docker constraint. This plan has Docker.
- `docs/metrics.json` and the CLAUDE.md prose counts are currently reconciled at **1511**. Plans
  23-08 through 23-14 add substantial new suites, so the count has drifted and the
  `docs-freshness` CI gate will fail on it.

Beyond CI, the planning record itself is now inaccurate: REQUIREMENTS.md, 23-VALIDATION.md and
23-CONTEXT.md all describe a state that these gap plans changed.

Output: a phase whose gates are green and whose documentation tells the truth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-VALIDATION.md
@.planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/deferred-items.md
@.planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-VERIFICATION.md

<environment>
Facts learned during 23-01..23-07 — do not rediscover these:
- The Gradle wrapper is at the repo ROOT (`./gradlew` from `/home/sanmi/IdeaProjects/JToye_OaaS_2026`).
  There is NO `core-java/gradlew`.
- `buildDir` is `build-local`, not `build`.
- Testcontainers tests are `@Tag("testcontainers")` and are EXCLUDED from the `test` task — run them
  via `./gradlew :core-java:integrationTest`.
- Frontend: `cd frontend && npx jest <paths>` and `cd frontend && npm run build` (tsc gate — jest
  does NOT type-check).
- Docker is available; the full Compose stack runs on :3000.
- `scripts/docs-freshness.sh` is the count gate, enforced by `.github/workflows/docs-freshness.yml`.
</environment>

<precedent>
The 23-06 phase-gate reconcile is the pattern to follow (see `deferred-items.md`): run
`scripts/docs-freshness.sh --write`, then update the CLAUDE.md prose counts to match, then confirm
check mode exits 0. That run moved the manifest 1456 → 1511 across
`java_test_methods` / `java_test_files` / `java_controllers` / `jest_blocks` / `jest_files` /
`playwright_blocks`, and left `schema_version` at 56.

Two things differ this time:
- **`schema_version` moves to 57** if plan 23-14 shipped V57. Confirm whether it did (the plan
  carries a blocking decision checkpoint the user could have rejected) before touching that field.
- A **known pre-existing methodology quirk**: the script counts textual `\b(it|test)\(` occurrences,
  which also matches `RegExp.prototype.test(` calls — so the manifest reads ~5 blocks higher than
  `npx jest` actually executes. This is pre-existing, not introduced by Phase 23, and the gate stays
  self-consistent (manifest == computed). Do NOT "fix" it here — that would shift the committed
  baseline and is its own change.
</precedent>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the OpenAPI snapshot</name>
  <files>docs/api/openapi-snapshot.json</files>

  <read_first>
    - .planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/deferred-items.md (the 23-06 entry describing this exact gate)
    - core-java/src/main/java/uk/jtoye/core/security/access/StaffController.java (the four endpoints that must appear — three original plus `/me` from plan 23-12)
    - .planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-12-SUMMARY.md (confirms the `/me` endpoint registration)
  </read_first>

  <action>
    Run the snapshot regeneration task from the repo root:

    `./gradlew :core-java:updateOpenApiSnapshot`

    This boots the full Spring context against a throwaway Testcontainers Postgres, so Docker must be
    running. Then commit the resulting diff to `docs/api/openapi-snapshot.json`.

    **Review the diff before committing — do not rubber-stamp it.** The snapshot has been stale since
    Phase 21, so the diff will contain endpoints from Phases 21, 22 and 23, not just this phase's.
    That is expected (22-07 recorded the same situation: "+14 endpoints, 0 removed"). What matters is
    that the diff is **additive**. If any endpoint is *removed* by this regeneration, stop — a removal
    means either a route regressed or the generation ran against an incomplete context, and
    committing it would silently ratify a broken contract.

    Confirm specifically that all four `/api/v1/staff` routes are present: `GET /api/v1/staff`,
    `POST /api/v1/staff/grant`, `DELETE /api/v1/staff/{id}`, and `GET /api/v1/staff/me`. The `/me`
    endpoint is new in plan 23-12 and is the one most likely to be missing if its OpenAPI annotations
    were not applied.

    Then run the check-mode gate to confirm it now passes:
    `./gradlew :core-java:integrationTest --tests "*OpenApiSnapshotTest"`
  </action>

  <verify>
    <automated>./gradlew :core-java:integrationTest --tests "*OpenApiSnapshotTest"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "api/v1/staff" docs/api/openapi-snapshot.json` returns ≥ 4 (was 0).
    - `grep -c "api/v1/staff/me" docs/api/openapi-snapshot.json` returns ≥ 1.
    - `OpenApiSnapshotTest` passes in check mode.
    - The diff is additive — the count of endpoints removed is zero, confirmed by inspecting the diff, not assumed.
    - The SUMMARY records how many endpoints were added and from which phases.
  </acceptance_criteria>

  <done>The CI-blocking snapshot gate is green and the committed contract matches the live API.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full suite, then reconcile the docs-freshness counts</name>
  <files>docs/metrics.json, CLAUDE.md, AGENTS.md</files>

  <read_first>
    - scripts/docs-freshness.sh (the counting methodology and the fields it writes)
    - docs/metrics.json (the current committed manifest at 1511)
    - .planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/deferred-items.md (the 23-06 reconcile precedent and the counting quirk)
    - CLAUDE.md (the "Testing" constraint prose carrying the counts)
  </read_first>

  <action>
    **Run the full suite first — the reconcile is meaningless if the suite is red.** In order:

    1. `./gradlew :core-java:test`
    2. `./gradlew :core-java:integrationTest`
    3. `cd frontend && npm run build && npx jest`
    4. `cd frontend && npx playwright test`

    Record any failure and STOP rather than reconciling counts over a red suite. A green
    docs-freshness gate over failing tests is precisely the green-by-construction pattern this phase's
    verification caught.

    Then reconcile: `scripts/docs-freshness.sh --write`, and update the CLAUDE.md prose counts to
    match the new manifest — including the `AGENTS.md` mirror, which the 260715-fcq quick task
    established must stay in sync.

    Update `schema_version` to **57** only if plan 23-14 actually shipped migration V57. That plan
    carries a blocking decision checkpoint the user may have rejected or modified, so read
    `23-14-SUMMARY.md` to confirm what landed rather than assuming. If V57 did not ship,
    `schema_version` stays 56.

    Finally confirm check mode exits 0: `scripts/docs-freshness.sh` (no `--write`).
  </action>

  <verify>
    <automated>scripts/docs-freshness.sh</automated>
  </verify>

  <acceptance_criteria>
    - The full suite was run and is green BEFORE the reconcile — paste the four commands' summary lines into the SUMMARY.
    - `scripts/docs-freshness.sh` (check mode) exits 0.
    - `docs/metrics.json`, CLAUDE.md and AGENTS.md all carry the same reconciled total.
    - `schema_version` matches what 23-14 actually shipped, verified against `23-14-SUMMARY.md` and the migration directory.
    - The pre-existing `RegExp.test(` counting quirk is NOT "fixed" — the baseline is not shifted.
  </acceptance_criteria>

  <done>The count gate is green over a genuinely green suite.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the planning record with what shipped</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-VALIDATION.md, .planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-CONTEXT.md, .planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/deferred-items.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md (lines ~44-50 the VSA entries; lines ~122-126 the status table)
    - .planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-VALIDATION.md (the per-requirement verification map to extend)
    - .planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-CONTEXT.md (D-04 lines ~75-84, D-05 ~86-88, D-12 ~126-131)
    - All eight SUMMARYs: 23-08 through 23-14 (what actually shipped, including any deferrals and the 23-14 decision outcome)
  </read_first>

  <action>
    **23-VALIDATION.md — extend the per-requirement map.** It currently lists the original Phase 23
    proofs with every status `⬜ pending`. Add a row for each gap-closure proof delivered by plans
    23-08..23-14, each with its requirement, behaviour, test type, **automated command** and file:

    | plan | proof | command |
    |---|---|---|
    | 23-08 | non-UUID-subject JWT denied on all staff endpoints; `require(null,…)` typed 403 | `./gradlew :core-java:integrationTest --tests "*ShopAccessFailClosedIntegrationTest"` |
    | 23-09 | role change applies + audited; concurrent revokes cannot empty the tenant | `./gradlew :core-java:integrationTest --tests "*StaffManagementIntegrationTest"` |
    | 23-10 | warm-cache cross-shop read denied, with caching genuinely enabled | `./gradlew :core-java:integrationTest --tests "*ShopAccessCacheBypassIntegrationTest"` |
    | 23-11 | STOMP subscribe shop-gated; day-one user still permitted | `./gradlew :core-java:test --tests "*TenantChannelInterceptorTest"` |
    | 23-12 | grant input validation; `/me`; directory PII masked + erasable | `./gradlew :core-java:integrationTest --tests "*StaffManagementIntegrationTest" --tests "*Gdpr*"` |
    | 23-13 | server-sourced group-admin; no silent pinning; sub-based identity | `cd frontend && npx jest components/dashboard app/dashboard && npm run build` |
    | 23-14 | strict-scoping genuinely tightens; membership cache real + eviction proven | `./gradlew :core-java:integrationTest --tests "*StrictScopingTighteningIntegrationTest"` |

    Update the statuses of the rows that are now genuinely green, and set `nyquist_compliant: true`
    in the frontmatter only if every row has an automated command. Update the "Manual-Only
    Verifications" table if any gap plan introduced a new manual check.

    **REQUIREMENTS.md — correct the status.** VSA-02 and VSA-04 currently read "Gaps found
    (23-VERIFICATION: …)". Update the status table (lines ~122-126) to reference the gap-closure
    plans that closed them and mark the `- [ ]` checkboxes for VSA-02 and VSA-04 complete —
    **but only for gaps that were genuinely closed.** If plan 23-14's decision checkpoint was
    rejected, CR-07 remains open and VSA-02 must NOT be marked complete; say so plainly instead.
    Anti-false-green is the standing rule on this phase — 23-01 and 23-05 both deliberately left
    requirements pending rather than overclaim.

    **23-CONTEXT.md — annotate the revised decisions.** If 23-14 shipped, append a dated revision
    note to D-04, D-05 and D-12 recording the new semantics (JIT provenance marking; strict-scoping
    de-honouring JIT rows with a bootstrap admin; the membership-cache mechanism corrected). Do NOT
    rewrite the original decision text — append, so the record shows what was decided and what was
    later revised and why. Reference 23-14-SUMMARY.md for the detail.

    **deferred-items.md — record every conscious deferral** from this gap-closure run, each with its
    one-line reason, so nothing is silently dropped:
    - WR-04 — products/marketing narrow client-side over a single server-paginated page (wrong counts,
      false empty state, unreachable rows). Highest-priority deferral: a genuine user-visible
      correctness defect, but it needs new `?shopId=` API surface on the products and marketing list
      endpoints plus gating and two screen reworks — its own plan, and not a security bypass since
      the underlying set is already grant-scoped server-side.
    - WR-03 — post-revocation SSE window, bounded at 5 minutes by `SSE_TIMEOUT` (see 23-11).
    - IN-01 — `fetchMyShops` hard-codes `size=200` (see 23-13).
    - The `asSystem()` ThreadLocal marker for the retained `auth == null` bypass (see 23-08).
    - `@PreAuthorize` scope backstop on `StaffController`, belonging to issue #206 (see 23-08).
    - Bulk-revoke of JIT rows in the staff screen (see 23-14).
    - Whole-batch CSV abort inconsistency, if 23-10 fell back rather than delivering per-row errors.
  </action>

  <verify>
    <automated>grep -c "23-08\|23-09\|23-10\|23-11\|23-12\|23-13\|23-14" .planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-VALIDATION.md</automated>
  </verify>

  <acceptance_criteria>
    - 23-VALIDATION.md contains a row with an automated command for every gap-closure proof; the grep above returns ≥ 7.
    - REQUIREMENTS.md VSA-02 and VSA-04 statuses match the SUMMARYs — and remain open if 23-14's checkpoint was rejected.
    - 23-CONTEXT.md carries dated revision notes on D-04, D-05 and D-12 (appended, not rewritten) if 23-14 shipped.
    - deferred-items.md lists all seven deferrals above, each with a reason.
    - No requirement is marked complete without a green automated proof named in 23-VALIDATION.md.
  </acceptance_criteria>

  <done>The planning record matches the shipped state, with every deferral named rather than dropped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed API contract → consumers | The OpenAPI snapshot is what clients and agents build against |
| documentation → future decisions | A stale or overclaiming record misleads the next phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-15-01 | Tampering | OpenAPI snapshot regeneration | mitigate | Diff reviewed for removals before commit; a removal aborts the task rather than silently ratifying a broken contract (Task 1) |
| T-23-15-02 | Repudiation | count reconcile over a red suite | mitigate | Full suite run and confirmed green BEFORE `--write`; failures stop the task (Task 2) |
| T-23-15-03 | Repudiation | requirements marked complete without proof | mitigate | Statuses driven by the SUMMARYs and by named automated commands in 23-VALIDATION.md; anti-false-green rule restated (Task 3) |
| T-23-15-04 | Information Disclosure | secret-shaped strings in regenerated artifacts | mitigate | The snapshot is generated from annotations and carries no credentials; if gitleaks or GitGuardian flags anything in the diff, investigate BEFORE merging (GitGuardian is not a required check and merges succeed while red) |
| T-23-15-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies — legitimacy gate not engaged |
</threat_model>

<verification>
- `./gradlew :core-java:test` green.
- `./gradlew :core-java:integrationTest` green — including `OpenApiSnapshotTest` in check mode.
- `cd frontend && npm run build && npx jest` green.
- `cd frontend && npx playwright test` green.
- `scripts/docs-freshness.sh` (check mode) exits 0.
- 23-VALIDATION.md, REQUIREMENTS.md, 23-CONTEXT.md and deferred-items.md all reconciled.
</verification>

<success_criteria>
- Both known-red CI gates are green.
- The OpenAPI snapshot is additive and contains all four staff endpoints.
- Counts are reconciled across metrics.json, CLAUDE.md and AGENTS.md over a genuinely green suite.
- The planning record states what shipped, what was revised, and what was deferred — with no requirement overclaimed.
</success_criteria>

<output>
Create `.planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-15-SUMMARY.md` when done.

**Post-execution routing (not a task in this plan):** `SECURITY.md` for this phase is produced by
`/gsd:secure-phase 23`, a separate command. Given the eight criticals this gap-closure run
addressed, that step is load-bearing rather than optional — flag it as the recommended next action
after this plan completes, alongside re-running `/gsd:verify-work` to re-verify VSA-02 and VSA-04.
</output>
</content>
