---
phase: 23-vendor-scoped-access-responsive-dashboard-nav
plan: 06
type: execute
wave: 5
depends_on: ["23-04", "23-05"]
files_modified:
  - frontend/app/dashboard/staff/page.tsx
  - frontend/lib/staff-api.ts
  - frontend/components/dashboard/sidebar.tsx
  - frontend/app/dashboard/__tests__/staff-page.test.tsx
  - docs/metrics.json
  - CLAUDE.md
autonomous: true
requirements: [VSA-04, MOBL-01]
user_setup: []

must_haves:
  truths:
    - "A GROUP_ADMIN can open /dashboard/staff, see the login-populated directory + current grants, grant a role per shop, and revoke a grant"
    - "A non-GROUP_ADMIN visiting /dashboard/staff sees the access-required state (typed shop 403), not a crash or a blank"
    - "A 'Staff' nav item appears in the shared navigation array and auto-flows into the mobile 'More' sheet"
    - "Revoking the last GROUP_ADMIN surfaces the 409 as a clear in-UI message, not a raw error"
    - "docs/metrics.json + CLAUDE.md test counts are reconciled so the docs-freshness CI gate passes"
  artifacts:
    - path: "frontend/app/dashboard/staff/page.tsx"
      provides: "GROUP_ADMIN staff-management screen (list/grant/revoke, 403 → access-required)"
      min_lines: 60
    - path: "frontend/components/dashboard/sidebar.tsx"
      provides: "Staff nav item added to the shared navigation array (D-10)"
      contains: "Staff"
    - path: "docs/metrics.json"
      provides: "reconciled test counts (all lanes) for the docs-freshness gate"
  key_links:
    - from: "app/dashboard/staff/page.tsx"
      to: "GET/POST/DELETE /api/v1/staff"
      via: "lib/staff-api.ts apiClient"
      pattern: "/api/v1/staff"
    - from: "staff/page.tsx"
      to: "403 → access-required card"
      via: "httpStatus + forbidden state (finance/page.tsx idiom)"
      pattern: "forbidden"
    - from: "sidebar.tsx navigation array"
      to: "mobile More sheet"
      via: "MobileTabBar imports the shared array"
      pattern: "Staff"
---

<objective>
Ship the GROUP_ADMIN-only staff-management screen (VSA-04 frontend) and add the "Staff" nav item to
the shared navigation array (D-10), then reconcile the phase's test counts so the `docs-freshness` CI
gate passes. The screen lists the login-populated user directory + current grants, grants a
`(user, shop|null, role)`, and revokes a grant — calling the 23-04 endpoints via `apiClient`; on a 403
it renders the existing access-required state (finance/page.tsx idiom, D-10/D-13) and surfaces the
last-GROUP_ADMIN 409 as a clear message. The Staff nav item is added to the single-source-of-truth
`navigation` array in `sidebar.tsx`, so it auto-appears in the mobile "More" sheet (mirroring Approvals).
This is the last plan, so it runs `scripts/docs-freshness.sh --write` to reconcile `docs/metrics.json`
+ CLAUDE.md prose counts for all the Java/Jest/Playwright tests added across 23-01..23-06.

Purpose: This closes VSA-04 end-to-end (backend from 23-04 + UI here) and clears the CI count gate.

Output: `staff/page.tsx` + `lib/staff-api.ts` + the Staff nav item + a Jest page spec + reconciled
metrics/docs. Depends on 23-04 (staff API) and 23-05 (owns the prior sidebar.tsx edits — sequential).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-RESEARCH.md
@.planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-PATTERNS.md
@.planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-04-SUMMARY.md
@.planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-05-SUMMARY.md

<interfaces>
<!-- CONSUMED from 23-04. -->
GET    /api/v1/staff        → { directory: DirectoryEntryDto[], grants: StaffMemberDto[] }  (GROUP_ADMIN only; 403 otherwise)
POST   /api/v1/staff/grant  → body { userId, shopId|null, role }  → 200/201
DELETE /api/v1/staff/{id}   → 204; last-GROUP_ADMIN → 409 (type "/last-group-admin")
DirectoryEntryDto: { userId, email, displayName, lastSeen }
StaffMemberDto:    { id, userId, shopId, role }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Staff-management page (list/grant/revoke, 403 → access-required, 409 message) + staff-api</name>
  <files>frontend/app/dashboard/staff/page.tsx; frontend/lib/staff-api.ts; frontend/app/dashboard/__tests__/staff-page.test.tsx</files>
  <read_first>
    - frontend/app/dashboard/finance/page.tsx lines 55-138 — the canonical httpStatus() helper + forbidden state + ShieldCheck access-required card to copy for GROUP_ADMIN-only gating (D-10/D-13)
    - .planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-PATTERNS.md "app/dashboard/staff/page.tsx" + "Access-required-on-403 page state" + "Authenticated API client"
    - frontend/lib/api-client.ts lines 29-42, 72-111 — apiClient attaches Bearer + X-Tenant-Id + 5xx retry (no new plumbing)
    - .planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-RESEARCH.md §8 (staff screen + api-client) + D-11 (409 last-GA)
  </read_first>
  <behavior>
    - GROUP_ADMIN: page lists directory entries (email/displayName/lastSeen) + current grants (shop, role); can grant a (user, shop|null, role) and revoke a grant; the list refreshes after each write
    - non-GA: a 403 from GET /api/v1/staff renders the access-required card (not a blank/crash)
    - grant a shop-scoped role from the directory picker → the new grant appears in the grants list
    - revoke → the grant disappears; a last-GROUP_ADMIN revoke (409) shows a clear "cannot remove the last group admin" message
  </behavior>
  <action>
    Create `lib/staff-api.ts` wrapping `apiClient`: `fetchStaff()` (GET /api/v1/staff),
    `grantStaff({userId, shopId, role})` (POST /api/v1/staff/grant), `revokeStaff(id)` (DELETE
    /api/v1/staff/{id}). Create `app/dashboard/staff/page.tsx` ("use client") mirroring finance/page.tsx:
    the `httpStatus()` helper + a `forbidden` state that renders the ShieldCheck access-required card when
    GET returns 403 (D-10/D-13). When authorized, render the directory as the grant-target picker + a grant
    form (shop dropdown incl. an "All shops / tenant-wide" option → shopId null for GROUP_ADMIN, role
    select) + a grants table with revoke buttons. On a 409 from revoke, surface a clear inline message
    ("cannot remove the last group admin"), not a raw error toast. Create
    `app/dashboard/__tests__/staff-page.test.tsx` covering: list render, grant flow, revoke flow, 403 →
    access-required, 409 → last-GA message.
  </action>
  <acceptance_criteria>
    - `grep -c "/api/v1/staff" frontend/lib/staff-api.ts` ≥ 3 (list + grant + revoke)
    - `staff/page.tsx` contains a `forbidden` state and the httpStatus/403 handling (grep for both)
    - `cd frontend && npx jest app/dashboard/__tests__/staff-page.test.tsx` exits 0 with list/grant/revoke/403/409 cases green
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx jest app/dashboard/__tests__/staff-page.test.tsx</automated>
  </verify>
  <done>Staff screen lists/grants/revokes; 403 → access-required card; 409 → clear last-GA message; all via apiClient.</done>
</task>

<task type="auto">
  <name>Task 2: Add the GROUP_ADMIN-only Staff nav item to the shared navigation array + typecheck</name>
  <files>frontend/components/dashboard/sidebar.tsx</files>
  <read_first>
    - frontend/components/dashboard/sidebar.tsx lines 26-42 — the shared `navigation` array (single source of truth) + the Approvals item (line 38) convention (admin-only page renders access-required on 403)
    - frontend/components/dashboard/mobile-tab-bar.tsx — confirms it imports the shared navigation array; PRIMARY_ORDER is fixed (Dashboard/Orders/Products/Kitchen), so Staff auto-falls into the "More" sheet
    - .planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-PATTERNS.md "Shared nav array (single source of truth)" + D-10
    - .planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-05-SUMMARY.md — the prior sidebar.tsx edits (switcher mount) this plan builds on
  </read_first>
  <action>
    Add `{ name: "Staff", href: "/dashboard/staff", icon: <a Users-like lucide icon> }` to the shared
    `navigation` array in `sidebar.tsx` (do NOT redeclare the array; MobileTabBar imports it, so Staff
    auto-appears in the "More" sheet). Follow the Approvals convention: the nav item is always present; the
    /dashboard/staff PAGE gates (access-required on 403 for non-GA, from Task 1). Do not add it to
    PRIMARY_ORDER. Preserve the 23-05 switcher mount already in the logo header.
  </action>
  <acceptance_criteria>
    - `grep -c "/dashboard/staff" frontend/components/dashboard/sidebar.tsx` ≥ 1 and the Staff entry is inside the `navigation` array (not PRIMARY_ORDER)
    - `cd frontend && npm run build` exits 0 (tsc typecheck gate)
    - The switcher mount from 23-05 is still present: `grep -c "ShopSwitcher" sidebar.tsx` ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Staff nav item in the shared array (auto-flows to mobile "More"); switcher mount preserved; tsc build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-gate reconcile — docs/metrics.json + CLAUDE.md prose counts via docs-freshness</name>
  <files>docs/metrics.json; CLAUDE.md</files>
  <read_first>
    - scripts/docs-freshness.sh — the --write behaviour that recomputes counts and rewrites docs/metrics.json + the CLAUDE.md prose counts
    - docs/metrics.json — the current baseline (java_test_methods 989, jest_blocks 324, playwright_blocks 39, mcp 27, total 1456, schema_version 56)
    - .planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-RESEARCH.md Pitfall 5 (line 431 — the count gate; schema_version stays 56 since HEAD V is unchanged by V52<V56) 
    - CLAUDE.md — the prose "1456 logical invocations" breakdown line the gate enforces
  </read_first>
  <action>
    After all six plans' tests exist, run `scripts/docs-freshness.sh --write` from the repo root to
    recompute every lane and rewrite `docs/metrics.json` (java @Test methods, jest it/test blocks,
    playwright test() blocks — the new integration/Jest/375px tests bump these) and the CLAUDE.md prose
    counts to match. Confirm the script then passes in check mode (no `--write`) with exit 0. If the
    project mirrors counts into AGENTS.md, update it too (memory 260715-fcq notes the AGENTS.md mirror).
    schema_version stays 56 (V52/V53 are below the V56 HEAD).
  </action>
  <acceptance_criteria>
    - `scripts/docs-freshness.sh` (check mode, no --write) exits 0 — no drift between docs/metrics.json, CLAUDE.md prose, and the actual test counts
    - `docs/metrics.json` total_logical_invocations reflects the new tests (> 1456) and `git diff --stat docs/metrics.json CLAUDE.md` shows both updated
  </acceptance_criteria>
  <verify>
    <automated>cd /home/sanmi/IdeaProjects/JToye_OaaS_2026 && scripts/docs-freshness.sh</automated>
  </verify>
  <done>docs/metrics.json + CLAUDE.md prose counts reconciled to the phase's actual test totals; docs-freshness check-mode green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-GA visiting /dashboard/staff | the page must gate (access-required), the nav item is always visible |
| grant/revoke UI actions → backend | authorization is enforced server-side (23-04); the UI mirrors, never replaces, the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06-01 | Elevation | non-GA reaching staff management via direct URL | mitigate | server GET returns typed 403 → page renders access-required (D-10/D-13); the write endpoints are GROUP_ADMIN-gated in 23-04 |
| T-23-06-02 | Information disclosure | directory PII (email) rendered to a non-GA | mitigate | the directory only loads on a 200 from the GROUP_ADMIN-gated GET; a non-GA gets 403 and no data |
| T-23-06-03 | Denial of service (self) | UI lets a GA remove the last group admin without warning | mitigate | 409 surfaced as a clear inline message; server guard (D-11) is the hard stop |
| T-23-06-04 | Tampering | stale docs counts hide a broken CI gate | mitigate | docs-freshness --write reconcile + check-mode green (Pitfall 5) |
| T-23-06-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `cd frontend && npm run build` (tsc) green
- `npx jest app/dashboard` green (staff page: list/grant/revoke/403/409)
- Staff nav item present in the shared array; switcher mount preserved
- `scripts/docs-freshness.sh` (check mode) exits 0
- Full phase gate: `cd core-java && ./gradlew test integrationTest` + `cd frontend && npm run build && npx jest` + `npx playwright test` green before /gsd:verify-work
</verification>

<success_criteria>
- GROUP_ADMIN staff screen lists directory + grants, grants/revokes per shop; non-GA → access-required
- Last-GROUP_ADMIN 409 surfaced clearly in-UI
- Staff nav item in the shared navigation array, auto-flowing to the mobile "More" sheet (D-10)
- docs/metrics.json + CLAUDE.md counts reconciled; docs-freshness CI gate green
</success_criteria>

<output>
Create `.planning/phases/23-vendor-scoped-access-responsive-dashboard-nav/23-06-SUMMARY.md` when done
</output>
