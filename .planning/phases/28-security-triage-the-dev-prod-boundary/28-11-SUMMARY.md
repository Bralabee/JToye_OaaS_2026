---
phase: 28-security-triage-the-dev-prod-boundary
plan: 11
subsystem: docs / phase close-out (manifest, triage record, handoff, changelog)
tags: [SEC-01, SEC-02, SEC-03, SEC-04, docs-freshness, pentest, triage, handoff, changelog, "#552", "#629", falsifiability]

requires:
  - plan: 28-01
    provides: "A1 FALSIFIED + the +1 Java @Test (RlsContractTest 5->6) folded into the manifest here"
  - plan: 28-04
    provides: "#281 FIXED disposition + the 5-minute SSE / cross-replica residuals"
  - plan: 28-05
    provides: "the triage record whose B1 row this plan upgrades, and the jtoye_backup placeholder row"
  - plan: 28-06
    provides: "#283/#284 resolved + the +15 Java @Test (SystemPrincipal) in the manifest"
  - plan: 28-07
    provides: "the jtoye_backup #629 defect (repaired live) that fills the §6 placeholder row"
  - plan: 28-10
    provides: "the owner's 2026-08-10 rotation decision (six keys by name) carried into the B1 row; the realm import that dated E1's §5.5"
provides:
  - "docs/metrics.json regenerated by script (2769 -> 2807) with CLAUDE.md/AGENTS.md/README.md prose reconciled; both doc gates rc=0"
  - "docs/security/PENTEST-TRIAGE.md B1 upgraded OPEN-TRACKED -> FIXED; jtoye_backup row filled with #629; E1 §5.5 caveat corrected"
  - "#552 CLOSED at phase close (comment references the triage doc, no credential values)"
  - "HANDOFF.md Phase 28 resume block with the MEASURED gate count (36); check-handoff-contract.sh rc=0"
  - "docs/CHANGELOG.md Phase 28 entry (in-flight; merge-step PR-number note)"
  - "deferred-items.md — DI-28-01 (check-doc-citations line-drift from 28-07/28-09), a pre-merge fix"
affects: [29]

tech-stack:
  added: []
  patterns:
    - "One plan owns the DESIGNED doc-metrics red across a whole phase and closes it by script, never by arithmetic"
    - "A phase finisher re-measures every count it quotes forward (manifest, gate count, suite) rather than carrying a remembered number"
    - "An in-flight CHANGELOG entry carries no invented PR number; the number is appended to the heading at PR-open"

key-files:
  created:
    - .planning/phases/28-security-triage-the-dev-prod-boundary/28-11-SUMMARY.md
    - .planning/phases/28-security-triage-the-dev-prod-boundary/deferred-items.md
  modified:
    - docs/metrics.json
    - CLAUDE.md
    - AGENTS.md
    - README.md
    - docs/security/PENTEST-TRIAGE.md
    - HANDOFF.md
    - docs/CHANGELOG.md

key-decisions:
  - "The doc-metrics red was DESIGNED and carried across ten plans by instruction; this plan owns and closes it (33-07 precedent)"
  - "docs/metrics.json regenerated by scripts/docs-freshness.sh --write, never by arithmetic (the gate greps literal @Test/it(/test( openers)"
  - "Only #552 is closed via gh at close-out (the orchestrator's explicit B1 close-out); #281/#283/#284 are resolved-on-branch and close at merge via the PR's `closes` keywords, to keep issue state synced with `main`"
  - "check-doc-citations rc=1 is a phase-introduced (28-07/28-09) line-drift OUTSIDE this plan's files; logged to deferred-items.md rather than fixed here (SCOPE BOUNDARY)"

requirements-completed: [SEC-01, SEC-02, SEC-03, SEC-04]

metrics:
  duration: ~45 min
  completed: 2026-08-10
  tasks: 3
  commits: 3
---

# Phase 28 Plan 11: Phase close-out — manifest, dispositions, gate sweep, changelog Summary

**The phase's designed doc-metrics red is closed (`docs/metrics.json` regenerated by script 2769 → 2807
with the prose reconciled, both gates green and the fail direction observed); the pentest triage record
is finalised (B1 upgraded to FIXED, #552 closed, the jtoye_backup placeholder filled with #629, E1's
stale realm caveat corrected); every gate in the tree was swept with its rc recorded; the HANDOFF's
gate count is the checker's own live measurement (36); and the CHANGELOG carries the phase — with the
runtime-parity rebuild, the full suite and the branch fail-direction deferred to the orchestrator per
this plan's docs-only scope.**

## Performance

- **Duration:** ~45 min
- **Completed:** 2026-08-10
- **Tasks:** 3 of 3
- **Commits:** 3 per-task + this metadata commit

## Task Commits

| Task | Artifact | Commit |
|---|---|---|
| 1 | `docs/metrics.json` + prose in `CLAUDE.md`/`AGENTS.md`/`README.md` | `16099f1f` |
| 2 | `docs/security/PENTEST-TRIAGE.md` + `HANDOFF.md` + `deferred-items.md`; #552 closed | `55d7c06c` |
| 3 | `docs/CHANGELOG.md` | `643df99c` |

---

## Task 1 — the test-count manifest and the prose that quotes it

### The manifest, before and after, with the delta explained

| key | before (committed) | after (regenerated) | Δ |
|---|---|---|---|
| `java_test_methods` | 1595 | **1633** | +38 |
| `java_test_files` | 256 | **264** | +8 |
| `total_logical_invocations` | 2769 | **2807** | **+38** |

Everything else is unchanged (jest 944/99, go 81/11, playwright 101/20, mcp 48/8, schema 61,
controllers 23). Regenerated by `bash scripts/docs-freshness.sh --write` — **never by arithmetic**,
which is wrong because the counter greps literal `@Test`/`it(`/`test(` openers.

**The +38 is fully attributed to the phase's plans**, and it equals the sum of what each plan reports
adding (no phantom, no lost test):

| plan | @Test added | where |
|---|---|---|
| 28-01 | +1 | `RlsContractTest` 5 → 6 (a MODIFIED file) |
| 28-02 | +3 | `TenantHeaderAbsentDocumentTest` |
| 28-04 | +9 | `OrderSseGrantRecheckTest` (7) + `OrderSseGrantRecheckIntegrationTest` (2) |
| 28-06 | +15 | `SystemPrincipalTest` (8) + `SystemPrincipalGuardTest` (7) |
| 28-07 | +3 | `FlywayCredentialDecouplingTest` |
| 28-08 | +7 | `DatabaseConfigurationValidatorOwnershipTest` (2) + `RuntimeRoleGrantContractTest` (5) |
| **total** | **+38** | 8 new @Test-bearing files (+37) + one modified file (+1) |

`java_test_files` went **+8 not +10** because two further new files (`AsSystemHarness`,
`SystemHarnessExtension`) are test harnesses with 0 `@Test`.

**This red was DESIGNED and carried across ten plans by instruction** (28-01, 28-02, 28-03, 28-04,
28-06, 28-07, 28-08 each recorded leaving the manifest untouched): two same-wave plans both
regenerating `docs/metrics.json` would collide, and arithmetic on it is wrong. One plan owns it and
closes it — the 33-07 precedent.

### Both gates green, and the fail direction observed

| gate | direction | rc |
|---|---|---|
| `docs-freshness.sh` (tree → manifest) | baseline (stale manifest) | **1** (2769 vs computed 2807) |
| `docs-freshness.sh` | after `--write` + prose update | **0** |
| `check-doc-metrics.sh` (prose → manifest) | clean | **0** (37 claims across 3 docs) |
| `check-doc-metrics.sh` | **FAIL DIRECTION** — README Total 2807 → 2806 | **1** — named `README.md [total_logical_invocations]: doc says 2806, docs/metrics.json says 2807` |
| `check-doc-metrics.sh` | restore (verified by content: 2807 present, 2806 absent), clean again | **0** |

The manifest is script-shaped (same key order and 2-space indent `docs-freshness.sh` emits), not
hand-edited.

---

## Task 2 — dispositions, #552, the gate sweep, the HANDOFF count

### `docs/security/PENTEST-TRIAGE.md`

- **B1 upgraded OPEN-TRACKED → FIXED** in the gated §3 table and §4.B1, carrying the owner's
  2026-08-10 "include-runtime-password" decision. The six rotated keys are named **BY KEY NAME only,
  no value**: `DB_PASSWORD` (the `jtoye_runtime` role password), `KEYCLOAK_CLIENT_SECRET`,
  `EDGE_API_CLIENT_SECRET`, `INTEGRATION_CATALOG_RO_SECRET`, `INTEGRATION_ORDERS_RW_SECRET`,
  `GRAFANA_ADMIN_PASSWORD`. The role split (non-owner `jtoye_runtime` + boot-time ownership validator)
  is framed as a **durability fix, not a live-leak closure** (every tenant table was already
  ENABLE+FORCE).
- **The jtoye_backup placeholder row (§6) filled with #629** — repaired live and made durable via
  `FOR ROLE jtoye_app` by 28-07 (40 → 41 readable), the pg_dump `COPY` failure direction recorded.
- **E1's now-stale §5.5 caveat corrected** — the realm import DID run in 28-10 (11 → 10 clients,
  `test-client` absent), so §5.3's live arm is no longer reproducible; §7's bullets updated to match.
- `check-pentest-triage.sh` **rc=0**, still exactly **11 rows**. Sanitization re-checked: the doc's
  high-entropy scan returns only three dictionary identifiers, a synthetic-token positive control fires.

### #552 CLOSED (the only forge closure this plan performs)

`gh issue comment 552 --body-file … && gh issue close 552 --reason completed`. State read back
**CLOSED**; the stored comment names `docs/security/PENTEST-TRIAGE.md` once and leaks no value (its
own high-entropy scan matched only the path fragment `docs/security/PENTEST`). Body written via
`--body-file` (never an interpolating string), number-before-keyword.

**Deliberately NOT closed via gh here:** #281 (SSE, 28-04) and #283/#284 (asSystem, 28-06) are
resolved by shipped code but are not pentest-triage findings; they close at merge via the PR's
`closes` keywords, keeping issue state synced with `main` (the fix is branch-only until merge). #629
(#jtoye_backup, filed+fixed 28-07) and #627/#628 stay tracked. #626/#270 were closed by 28-09. #488's
urgent limb is measured-closed and gated; its Core-Web-Vitals residual is deferred to a dated media
backfill plan.

### The full gate sweep — every `check-*.sh` + `docs-freshness.sh`, rc recorded

Read-only sweep (no rebuild, no stack touch), `timeout 90` per gate. **32 of 36 rc=0.** The four
non-zero, each classified:

| gate | rc | classification |
|---|---|---|
| check-alert-liveness | 0 | pass |
| check-alert-metrics | 0 | pass (no core-java recreate this plan, so its standing post-recreate red did not fire) |
| check-alert-mute | 0 | pass |
| check-alert-rules | 0 | pass |
| check-branch-behind-base | 0 | pass (HEAD not behind origin/main) |
| check-changelog-cites-pr | 0 | pass (SKIP off a PR) |
| check-changelog-contract | 0 | pass |
| check-claims | 0 | pass |
| check-container-config-drift | 0 | pass |
| check-dependency-horizons | 0 | pass |
| **check-doc-citations** | **1** | **DI-28-01** — phase-introduced line drift (28-07/28-09), OUT of this plan's files, logged to `deferred-items.md`, must fix before merge |
| check-doc-metrics | 0 | pass (Task 1) |
| check-doc-versions | 0 | pass |
| check-e2e-baseurl-contract | 0 | pass |
| **check-e2e-skip-budget** | **2 (VOID)** | standing once-per-merge staleness detector; re-earned by an E2E suite re-run (deferred-to-orchestrator) |
| check-e2e-typecheck | 0 | pass |
| check-edge-core-contract | 0 | pass |
| check-gate-enforcement | 0 | pass |
| check-geo-attribution | 0 | pass |
| **check-handoff-contract** | 1 → **0** | red at sweep-time (`EXPECT 34` vs 36); **fixed in Task 2**, re-run rc=0 |
| check-image-supply-chain | 0 | pass |
| **check-infra-exposure** | **1** | the FOREIGN `asao-*` compose stack (OlaJay's) on `0.0.0.0`; **zero jtoye services fail**. Environmental, not this repo's defect (28-10 recorded the same) |
| check-live-shop-coordinates | 0 | pass |
| check-media-content-types | 0 | pass |
| check-no-create-extension | 0 | pass |
| check-no-measured-placeholders | 0 | pass |
| check-openapi-snapshot-fresh | 0 | pass |
| check-pentest-triage | 0 | pass (11 rows, B1 FIXED) |
| check-playwright-mobile-contract | 0 | pass |
| check-postgres-major-parity | 0 | pass |
| check-project-version | 0 | pass |
| check-runtime-freshness | 0 | pass read-only (28-10 rebuilt core-java; this plan's commits are docs-only, so the running images still match the tree). The DEFINITIVE by-content rebuild proof is deferred-to-orchestrator |
| check-terminal-states | 0 | pass |
| check-test-block-counter | 0 | pass |
| check-test-count-oracle | 0 | pass |
| docs-freshness | 0 | pass (Task 1) |

"Not run" appears in no cell. The only non-zero that is a real pre-merge action item is
**check-doc-citations (DI-28-01)**, and it is out of this plan's declared files (a 28-07/28-09
regression), logged rather than fixed per the SCOPE BOUNDARY rule.

### HANDOFF.md — gate count MEASURED, resume block refreshed

- Live measurement: `ls scripts/check-*.sh scripts/docs-freshness.sh | wc -l` = **36**.
- The sole H-1 anchor (§6 resume-instructions, line ~2625) updated **34 → 36** with the derivation
  (phase 28 added `check-media-content-types.sh` and `check-pentest-triage.sh`). Value written = 36 =
  the live measurement; **fail direction observed** — the gate was red at sweep-time
  (`EXPECT 34 x rc=0` vs "repo has 36 gate scripts"), and the phase-start value **34** is carried
  in the derivation, so a stale number would differ.
- A fresh Phase 28 resume section added on top (supersedes the Phase 33 block), listing each plan's
  delivery, the branch state, the four non-green gates with their classification, the deferred
  runtime work, and the issue close-out. `#552` is the only capitalised CLOSED state claim (forge:
  closed).
- `check-handoff-contract.sh` **rc=0** — H-1: 1 claim / 36 gate scripts; H-2: **45/45** state claims
  matched the forge; H-3: 1 commit behind (budget 3); H-4/H-5 green.

---

## Task 3 — the CHANGELOG, and the runtime deferred

### `docs/CHANGELOG.md`

An `[Unreleased]` Phase 28 entry (Security / Hardening / Notes), pointing at
`docs/security/PENTEST-TRIAGE.md`. **No PR number invented** (the PR does not exist); a merge-step
HTML comment records that the number is appended to the heading at PR-open and that "take theirs"
silently deletes this entry on a CHANGELOG conflict.

Gate evidence, both directions (proves the citation gate is not vacuous):

| check | invocation | rc |
|---|---|---|
| `check-changelog-contract.sh` | merged-history (FLOOR fbee244d..origin/main) | **0** (2 merged feat/fix PRs cited) |
| `check-changelog-cites-pr.sh` | bare (no PR context) | **0** (SKIP — in-flight) |
| `check-changelog-cites-pr.sh` | `--pr 999999 --title 'fix: …'` (**FAIL direction**) | **1** — names the uncited PR |
| `check-changelog-cites-pr.sh` | `--pr 620 --title 'fix: …'` | **0** (cited) |
| `check-changelog-cites-pr.sh` | `--pr 999999 --title 'docs: …'` | **0** (docs subject skipped) |

### Deferred to the orchestrator (runtime, per this plan's docs-only scope)

The orchestrator directed: do not rebuild core-java or touch the live stack; note anything needing
the runtime as deferred. Accordingly the following Task-3 items are **deferred-to-orchestrator**:

- **`docker compose … up -d --build --force-recreate`** and the by-content parity proof —
  `check-runtime-freshness.sh` rc=0 per service with a stopped-service VOID fail direction, and
  `application.yml` read from **inside** `/app/app.jar` (`unzip -p … BOOT-INF/classes/application.yml`).
  Read-only, the gate is currently rc=0 (28-10's rebuild is still current because this plan changed no
  build input), but the definitive rebuild + fat-jar read was not performed here.
- **`check-branch-behind-base.sh`** — rc=0 in the sweep (HEAD not behind origin/main); its
  deliberately-stale-ref fail direction belongs to the same runtime step.
- **The full `:core-java:test :core-java:integrationTest`** on the final tree. Not run here. The
  phase's static `@Test` census is the standing regression signal and grew **monotonically +38 with
  zero deletions** (1595 → 1633), and 28-06 ran the full 1650-test suite (test 1095 / integration 555)
  green with 28-04's SSE change in-tree, 28-08 added +7 integration (562) at 0 failures. No count
  regression is expected; the green run is the orchestrator's to take on the rebuilt tree.
- **The Playwright re-run** that re-earns `check-e2e-skip-budget` (VOID once per merge).

---

## ROADMAP success criteria — Phase 28 (verdict + measurement each)

| SC | Requirement | Verdict | Measurement |
|---|---|---|---|
| **SC-1** | SEC-01 | **SATISFIED** | 28-01 re-verified A1 against a stack rebuilt from HEAD and recorded its root cause **FALSIFIED** (both tables carry tenant_id + ENABLE/FORCE RLS, two policies each; the real block is the service-layer ownership gate, shown able to fail — one named test red, restore hash-verified). |
| **SC-2** | SEC-02 | **SATISFIED** | All 11 findings dispositioned in `docs/security/PENTEST-TRIAGE.md`, `check-pentest-triage.sh` rc=0 (11 rows, closed vocabulary, non-empty dispositions). B1 now FIXED and #552 closed. `SECURITY-FINDINGS.md` remains git-excluded. |
| **SC-3** | SEC-03 | **SATISFIED (RESTATED)** | **Restated against the BUILT/served document**, because the ROADMAP flagged a source/snapshot grep as a false red (`OpenApiConfig.java:51` is stripped at build). 28-02 asserted on the document springdoc **serves** (`/v3/api-docs`) that the dev-only header is absent on deployed profiles, with a filter-present control, a non-empty-paths denominator, and three observed red break arms. |
| **SC-4** | SEC-04 | **SATISFIED (RESTATED)** | **The loopback half was ALREADY SATISFIED before the phase** (all five named infra ports bind `${JTOYE_BIND_HOST:-127.0.0.1}`; `.planning/CRITERIA-DECAY-2026-08-08.md`); the **rotation half is what shipped** — 28-10 rotated the six named credentials on the running stack, each proven superseded-refused / current-accepted in one run. The three still-public ports are applications, which SC-4 does not govern. |

Both restatements are recorded explicitly rather than reported as plain passes (SC-3's artifact,
SC-4's already-satisfied loopback half), per the falsifiable-evidence contract.

---

## Deviations from Plan

### Auto-fixed / recorded

**1. [Rule 3 — Blocking, deferred not fixed] `check-doc-citations.sh` rc=1 is a phase regression outside this plan's files.**
- **Found during:** Task 2 gate sweep.
- **Issue:** 6 C-3 line-drift citations in `.planning/codebase/STACK.md`, `.planning/codebase/INTEGRATIONS.md`
  and `k8s/LOCAL.md` cite `docker-compose.full-stack.yml`/`application.yml` lines that plans 28-07 and
  28-09 shifted (verified: `docker-compose.full-stack.yml:644` was `mailhog` on origin/main, now
  `start_period: 30s`). It is wired into CI.
- **Action:** logged to `deferred-items.md` as **DI-28-01** (must fix before the phase PR merges),
  NOT fixed here — the citing docs and the source files are outside this plan's seven `files_modified`,
  so the SCOPE BOUNDARY rule requires logging rather than fixing inside an unrelated plan.

**2. [Recorded correction] E1's §5.5 "not yet reached the running realm" caveat was stale.**
- The realm import ran in 28-10 (11 → 10 clients). The caveat was corrected in place with a dated
  update, and §7's bullets updated, rather than left as a known-false statement.

**3. [Scope, recorded] Only #552 closed via gh; #281/#283/#284 close at merge.**
- The plan Task 2 named #552/#283/#284/#270/#281/#488 "as applicable". #270/#626 were already closed
  (28-09); #488 is deferred (CWV residual). #281/#283/#284 are genuinely fixed but are not
  pentest-triage findings and their code is branch-only, so they close at merge via the PR's `closes`
  keywords rather than prematurely on the forge — keeping issue state synced with `main`.

**Total deviations:** 1 blocking (deferred by scope), 2 recorded. **Scope:** no source touched beyond
the seven planned docs plus `deferred-items.md` (sanctioned by the deviation process).

## Threat Model Outcomes

| Threat | Disposition | Evidence |
|---|---|---|
| T-28-57 shipping a runtime missing this phase's own changes | **deferred-to-orchestrator** | this plan changed no build input; the definitive rebuild + fat-jar read is the orchestrator's runtime step (recorded above) |
| T-28-58 a branch behind its base | **partially mitigated** | `check-branch-behind-base.sh` rc=0 (HEAD not behind origin/main); the stale-ref fail direction is in the deferred runtime step |
| T-28-59 a security phase reporting criteria that could not fail as satisfied | **mitigated** | SC-3 (built artifact) and SC-4 (pre-satisfied loopback half) restatements recorded explicitly, not reported as plain passes |
| T-28-60 a stale gate/test count quoted forward | **mitigated** | manifest regenerated by script; gate count = the checker's own `ls … | wc -l` = 36, derivation carried; both re-measured, not remembered |
| T-28-61 a credential value reaching the triage doc / CHANGELOG / HANDOFF | **mitigated** | B1 names KEYS only; high-entropy scans of the triage doc and the #552 comment returned only dictionary identifiers, synthetic-token positive controls fire |
| T-28-62 a CHANGELOG entry silently deleted by "take theirs" | **mitigated** | the hazard is named in the entry's merge-step comment and in HANDOFF; both changelog gates run rc=0 |

Cross-cutting: web-perf **N/A**, SEO **N/A**, agent-readiness **N/A** (docs only). Falsifiable
evidence: fail direction on `check-doc-metrics` (2807→2806), on `check-handoff-contract` (EXPECT 34 vs
36), and on `check-changelog-cites-pr` (#999999 → rc=1); the clean state asserted after each.

## Known Stubs

None. No UI, no data path, no placeholder values. The one former placeholder (the triage §6
`jtoye_backup` row) is now filled with #629.

## Threat Flags

None. No new network endpoint, auth path, file-access pattern or trust-boundary schema change — this
plan edits documentation and closes an issue.

## Self-Check: PASSED

| Claim | Verification | Result |
|---|---|---|
| `28-11-SUMMARY.md`, `deferred-items.md` | `test -f` | FOUND |
| `docs/metrics.json`, `CLAUDE.md`, `AGENTS.md`, `README.md`, `docs/security/PENTEST-TRIAGE.md`, `HANDOFF.md`, `docs/CHANGELOG.md` | `test -f` | FOUND |
| Commits `16099f1f` / `55d7c06c` / `643df99c` | `git cat-file -t` | ALL commit |
| This plan's file set (`eff0a29e..HEAD`) | `git diff --name-only` | exactly the 7 docs + `deferred-items.md` |
| **STATE.md / ROADMAP.md NOT modified by this plan** | `git diff --name-only eff0a29e..HEAD -- …` | empty (untouched) |
| #552 | `gh issue view 552 --json state` | CLOSED |
| `check-pentest-triage.sh` / `check-handoff-contract.sh` / `check-doc-metrics.sh` / `docs-freshness.sh` | rc | all 0 |
