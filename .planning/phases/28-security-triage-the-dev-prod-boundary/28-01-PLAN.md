---
phase: 28-security-triage-the-dev-prod-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core-java/src/test/java/uk/jtoye/core/security/RlsContractTest.java
autonomous: true
requirements: [SEC-01, SEC-04]

must_haves:
  truths:
    - "Pentest finding A1's stated root cause is recorded CONFIRMED or FALSIFIED against a stack rebuilt from HEAD, with the measurement that settled it"
    - "The A1 guard is proven capable of failing: neutralising the promotion-create ownership check reds exactly one named test"
    - "A public table with RLS forced but zero policies fails the build"
    - "The zero-policy-less-tables result is proven non-vacuous by an explicit denominator"
  artifacts:
    - path: "core-java/src/test/java/uk/jtoye/core/security/RlsContractTest.java"
      provides: "everyRlsEnabledTableHasAtLeastOnePolicy() schema-walk with denominator control"
      contains: "pg_policy"
  key_links:
    - from: "RlsContractTest"
      to: "pg_class LEFT JOIN pg_policy"
      via: "jdbc.queryForList schema walk"
      pattern: "pg_policy"
    - from: "A1 re-verification"
      to: "PromotionService.java:90 shopAccessService.require"
      via: "break arm neutralising the ownership check"
      pattern: "require\\(request\\.getShopId\\(\\), ShopRole\\.SHOP_MANAGER\\)"
---

<objective>
Settle SEC-01 by re-verifying pentest finding A1 against a stack rebuilt from HEAD, and close
D-13's one real gap: nothing in the tree sweeps for "RLS enabled but ZERO policies".

Purpose: A1 is the phase's highest-CVSS finding (9.1) and the ROADMAP records that its stated root
cause does not hold on the tree. The criterion FAILS if the re-verification is skipped or reports
"as filed" without an arm. D-13 asked for a measurement first — RESEARCH DEC-1 did it: the
table-coverage sweep already exists (`everyPublicTableHasRlsAndForce`), so the honest reading of
"extend only if missing/partial" is the one-method policy-count extension, nothing more.

Output: a recorded CONFIRMED/FALSIFIED verdict for A1 with both directions of its arm, and a new
permanent `RlsContractTest` method that reds on a policy-less RLS table.

Runs FIRST and reads the LIVE stack. Plan 28-07 (the role split) declares a dependency on this
plan's wave precisely so the role catalogue does not change under this measurement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-CONTEXT.md
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-RESEARCH.md
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-PATTERNS.md

<interfaces>
Contracts this plan asserts against — take these as given, do not re-derive by exploration.

The A1 target test (already exists, 6 tests in the class):
  core-java/src/test/java/uk/jtoye/core/security/access/CrossTenantAuthzIntegrationTest.java:124
    void createPromotion_crossTenantShop_isBlocked()

The ownership check that arm neutralises (create path):
  core-java/src/main/java/uk/jtoye/core/shop/PromotionService.java:90
    shopAccessService.require(request.getShopId(), ShopRole.SHOP_MANAGER);
  (the sibling coordinate, NOT the target: AnnouncementService.java:90 — same shape)

Companion suites: ShopPromotionsRlsPolicyIntegrationTest (3 tests, core-java/src/test/java/uk/jtoye/core/marketing/),
ShopScopedListGateTest.

The schema-walk to mirror (self-analog):
  RlsContractTest.java:140-167  everyPublicTableHasRlsAndForce()
  RlsContractTest.java:158-168  EXEMPT_TABLES (by-addition, written justification per entry)

Measured live 2026-08-10 (re-measure before quoting — these decay on any migration):
  36 tables ENABLE+FORCE · 5 exempt (flyway_schema_history, postcode_centroid,
  processed_stripe_events, revinfo, tenants) · 36 tables have >=1 policy · 0 policy-less tables
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify A1 against a stack rebuilt from HEAD, both directions</name>
  <files>core-java/src/main/java/uk/jtoye/core/shop/PromotionService.java (break arm ONLY — restored, net zero change)</files>
  <read_first>
    - core-java/src/test/java/uk/jtoye/core/security/access/CrossTenantAuthzIntegrationTest.java (the whole class — you must know all 6 test names to claim "exactly 1 failed")
    - core-java/src/main/java/uk/jtoye/core/shop/PromotionService.java (lines 60-140 — see which of the 5 require() calls is the create path)
    - core-java/src/test/java/uk/jtoye/core/marketing/ShopPromotionsRlsPolicyIntegrationTest.java
    - .planning/phases/28-security-triage-the-dev-prod-boundary/28-RESEARCH.md § "The existing cross-tenant guard A1's re-verification breaks" and § "The A1 / D-04 live isolation arm, with its control"
    - scripts/check-runtime-freshness.sh (header — the exit contract, and why rc=2 is VOID)
  </read_first>
  <action>
    Establish runtime parity BEFORE measuring anything. Rebuild the stack with
    `docker compose -f docker-compose.full-stack.yml up -d --build` — never `start`, which does not
    rebuild — and add `--force-recreate` if `check-runtime-freshness.sh` reports a container still
    on an older image ID than its tag (Phase 33 hit exactly that: `up -d --build` leaves containers
    on old IDs). Then run `bash scripts/check-runtime-freshness.sh` and require rc=0 with all built
    services FRESH. rc=2 is VOID, not a pass — a stopped or missing service voids the whole run, so
    do not proceed to the measurement on a VOID.

    Commit the tree BEFORE running the break arm. `git checkout` restores from the INDEX, so an
    uncommitted edit made after staging is silently discarded by the restore.

    Run the four-part protocol in this order and record real output for every part:
    CLEAN -> BREAK -> RESTORE -> CLEAN AGAIN. The closing clean arm is not ceremony; it is the only
    thing that proves the restore happened.
      * CLEAN: `./gradlew :core-java:integrationTest --tests '*CrossTenantAuthzIntegrationTest' --tests '*ShopPromotionsRlsPolicyIntegrationTest'` green, with the test counts stated (expect 6 + 3).
      * BREAK: neutralise the create-path ownership check at PromotionService.java:90 — the
        `shopAccessService.require(request.getShopId(), ShopRole.SHOP_MANAGER)` call. Use an
        unambiguous edit (comment out that exact line by its line number, or wrap it in an
        `if (false)`); do NOT use a `String.replace` on the call text, because PromotionService
        carries five `shopAccessService.require(` calls and a first-match replace edits the wrong
        one — the 33-07 ambiguity-guard lesson. Re-run and require EXACTLY ONE failure, named
        `createPromotion_crossTenantShop_isBlocked`. Any other count is the finding, not a nuisance:
        zero failures means the criterion cannot fail; more than one means the arm is broader than
        the claim.
      * RESTORE: `git checkout -- core-java/src/main/java/uk/jtoye/core/shop/PromotionService.java`,
        then verify BY CONTENT — `git hash-object` on the file must equal `git rev-parse HEAD:<path>`.
        Never verify a restore with `git diff --stat`, which is empty both when a file is restored
        and when it was never written.
      * CLEAN AGAIN: re-run both suites green.

    Then take the live RLS arm on the running database, as the app role, with its non-vacuity
    control, using `docker exec -i jtoye-postgres psql ...` (without `-i` a heredoc never reaches
    psql and the command exits 0 having done nothing). Count `products` with no tenant GUC, then
    under each of two tenant GUCs, then `RESET ROLE` and count again as the superuser. The
    superuser total must equal the sum of the two tenant counts — that is what makes the leading 0
    evidence about RLS rather than about an empty table. Do NOT run this arm on `shops`: it carries
    a permissive `shops_public_read` SELECT policy `(published = true) OR (tenant_id =
    current_tenant_id())`, so it legitimately returns rows with no GUC pinned, and "fixing" that
    would break the public storefront. Record the `shops` exception in the SUMMARY so a later reader
    does not treat it as a defect.

    Write the verdict as CONFIRMED or FALSIFIED against A1's stated root cause — that
    `shop_promotions`/`shop_announcements` lack a `tenant_id` column or an RLS policy — and state
    which measurement settled it. Do not write "as filed". Sanitization holds: refer to the finding
    as A1, never quote the pentest report's payload or chain.
  </action>
  <verify>
    <automated>bash scripts/check-runtime-freshness.sh; echo "rc=$?"</automated>
    <automated>cd core-java && ./gradlew :core-java:integrationTest --tests '*CrossTenantAuthzIntegrationTest' --tests '*ShopPromotionsRlsPolicyIntegrationTest'</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-runtime-freshness.sh` exits 0 with every built service reported FRESH, run BEFORE the measurement. FAIL DIRECTION: stop one built service (`docker compose stop core-java`) and confirm the script exits 2 (VOID) naming that service — an unverifiable service must never be reported inside a pass.
    - CLEAN arm: both suites green, with the exact test counts printed (9 total across the two classes).
    - BREAK arm: with PromotionService.java:90's `require(...)` neutralised, the run reports EXACTLY 1 failure and its name is `createPromotion_crossTenantShop_isBlocked`. The recorded output shows the failure count and the method name. A run reporting 0 failures means the criterion is incapable of failing and MUST be reported as such rather than as a pass.
    - RESTORE verified by content: `git hash-object core-java/src/main/java/uk/jtoye/core/shop/PromotionService.java` equals `git rev-parse HEAD:core-java/src/main/java/uk/jtoye/core/shop/PromotionService.java`. Both values recorded.
    - CLEAN-AGAIN arm re-run after the restore and green — recorded separately from the first clean arm.
    - Live RLS arm: `products` count as the app role with no GUC is 0; under each of two tenant GUCs it is non-zero; the superuser count equals the sum of the two tenant counts. All four numbers recorded. FAIL DIRECTION for the instrument: the superuser control returning 0 would mean the table is empty and the leading 0 proves nothing — record the superuser number explicitly so this cannot be assumed.
    - The SUMMARY records the word CONFIRMED or FALSIFIED for A1, names the measurement that settled it, and names the `shops` permissive-policy exception.
    - No literal secret value, DB->OAuth->header chain, or repro payload appears in any committed file.
  </acceptance_criteria>
  <done>A1 has a recorded CONFIRMED/FALSIFIED verdict backed by a four-part arm whose break direction was observed, plus a live RLS measurement with a non-vacuous superuser control. PromotionService.java is byte-identical to HEAD.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep for RLS-enabled tables with zero policies (D-13)</name>
  <files>core-java/src/test/java/uk/jtoye/core/security/RlsContractTest.java</files>
  <read_first>
    - core-java/src/test/java/uk/jtoye/core/security/RlsContractTest.java (whole file — the Testcontainers bootstrap at :51-83, EXEMPT_TABLES at :88-134, and the schema walk at :140-167 you are mirroring)
    - core-java/src/main/java/uk/jtoye/core/config/DatabaseConfigurationValidator.java:122-152 (validateRlsPolicies — the FIVE-table hardcoded check this sweep generalises; do NOT extend that method, RESEARCH puts the sweep on the test)
    - .planning/phases/28-security-triage-the-dev-prod-boundary/28-RESEARCH.md § DEC-1
  </read_first>
  <action>
    Add ONE test method to `RlsContractTest`, named `everyRlsEnabledTableHasAtLeastOnePolicy`. It
    walks `pg_class` for `relkind = 'r'` in `'public'::regnamespace` where `relrowsecurity = true`,
    LEFT JOINs `pg_policy` on `polrelid = oid`, groups by relname, and asserts the policy count is
    >= 1 for every non-`EXEMPT_TABLES` table. Mirror `everyPublicTableHasRlsAndForce()`'s shape at
    :140-167 exactly: same query style, same `EXEMPT_TABLES.contains(name)` skip, and the same
    failure-message convention — the message must tell the reader what to DO, including that a
    table legitimately without a policy is added to `EXEMPT_TABLES` BY ADDITION with a written
    justification, and that the assertion itself is never weakened.

    Include the denominator in the SAME method, not as a separate test: assert that the number of
    tables observed WITH at least one policy is >= 30. RESEARCH measured 36. Without that
    assertion, a query that returns no rows at all — a typo in the catalog name, a schema filter
    that matches nothing — satisfies "zero policy-less tables" vacuously. This is the
    "found nothing is never clean" rule; it is the same reason `check-no-create-extension.sh:67`
    refuses to report clean over an empty scan.

    Explain in the method's javadoc why this is a LIVENESS defect and not a leak: a table with RLS
    forced and no policy denies everything, so it fails closed. It is worth catching because a
    silently-dead table is indistinguishable from an empty one.

    Do NOT run `scripts/docs-freshness.sh --write` and do NOT touch `docs/metrics.json` or the
    prose counts in CLAUDE.md / AGENTS.md / README.md. Plan 28-11 owns the manifest for the whole
    phase; a red `check-doc-metrics` between here and 28-11 is DESIGNED, matching the 33-07
    precedent. State the expected red in the SUMMARY so a later reader does not treat it as a
    regression.
  </action>
  <verify>
    <automated>cd core-java && ./gradlew :core-java:integrationTest --tests '*RlsContractTest'</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :core-java:integrationTest --tests '*RlsContractTest'` is green, and the run reports one MORE test than before the change (record both counts).
    - The new method's SQL references `pg_policy`. Assert by source: `grep -c pg_policy core-java/src/test/java/uk/jtoye/core/security/RlsContractTest.java` is >= 1 where it was 0 before.
    - FAIL DIRECTION (mandatory, run and record): inside the test's own Testcontainers database, create a throwaway table, `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` with no policy, and confirm the method fails naming that table. Restore by dropping the table. A method observed only passing is not evidence.
    - SECOND FAIL DIRECTION for the denominator: temporarily change the walk's schema filter to a name that matches nothing and confirm the method fails on the >= 30 denominator rather than passing on "zero policy-less tables". Restore, verify by `git hash-object` against `git rev-parse HEAD:<path>` after the final restore, and re-run green.
    - `docs/metrics.json` is unchanged by this plan: `git diff --name-only` does not list it.
    - The javadoc states the fail-closed/liveness rationale and the by-addition exemption rule in words.
  </acceptance_criteria>
  <done>A policy-less RLS table reds `RlsContractTest`, proven by creating one; the denominator proves the sweep can see rows; `docs/metrics.json` untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendor JWT -> PromotionService/AnnouncementService | a shop id supplied by an authenticated user of tenant A naming a shop of tenant B |
| application role -> PostgreSQL row-security barrier | every tenant-scoped read; the wall this phase is testing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Elevation of Privilege | PromotionService/AnnouncementService create+update paths (A1) | mitigate | `shopAccessService.require(shopId, SHOP_MANAGER)` already present; this plan proves it is capable of failing by neutralising it and observing exactly one named test red |
| T-28-02 | Information Disclosure | any future public table added without an RLS policy | mitigate | `everyRlsEnabledTableHasAtLeastOnePolicy` schema walk with a >= 30 denominator, plus the existing ENABLE+FORCE walk |
| T-28-03 | Information Disclosure | the verification query itself, under RLS | mitigate | superuser control arm — the sum of two tenant counts must equal the unrestricted count, so a 0 cannot be read off an empty table (`trap_rls_blinds_the_verification_query`) |
| T-28-04 | Information Disclosure | this plan's own SUMMARY in a PUBLIC repo | mitigate | findings referenced by ID (A1) only; no literal values, no chain, no payload; `SECURITY-FINDINGS.md` stays git-excluded |

Cross-cutting dimensions: web-perf **N/A** (no user-facing page changes), SEO **N/A** (no public surface),
agent-readiness **N/A** (no API surface change). Falsifiable evidence: every criterion above carries a
stated fail direction. Runtime parity: enforced by `check-runtime-freshness.sh` before Task 1's measurement.
</threat_model>

<verification>
- `bash scripts/check-runtime-freshness.sh` rc=0 before the A1 measurement (rc=2 VOID is not a pass).
- `./gradlew :core-java:integrationTest --tests '*CrossTenantAuthzIntegrationTest' --tests '*ShopPromotionsRlsPolicyIntegrationTest' --tests '*RlsContractTest'` green.
- Break arms recorded for BOTH tasks, with the clean state asserted last as well as first.
- `git status` shows exactly one modified file: `RlsContractTest.java`.
</verification>

<success_criteria>
- A1 recorded CONFIRMED or FALSIFIED, with the measurement, in the SUMMARY.
- The A1 break arm produced exactly 1 named failure and the restore was verified by content hash.
- `RlsContractTest` has a policy-count sweep proven to red on a deliberately policy-less table.
- No prose or manifest count was edited (28-11 owns those).
</success_criteria>

<output>
Create `.planning/phases/28-security-triage-the-dev-prod-boundary/28-01-SUMMARY.md` when done.
Record: the A1 verdict and its four arms with real output; the live RLS numbers including the
superuser control and the `shops` exception; the RlsContractTest before/after test counts; and the
DESIGNED red on `check-doc-metrics` that 28-11 will close.
</output>
