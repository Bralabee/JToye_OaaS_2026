---
phase: 28-security-triage-the-dev-prod-boundary
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core-java/src/test/java/uk/jtoye/core/config/TenantHeaderAbsentDocumentTest.java
  - core-java/src/main/java/uk/jtoye/core/security/SecurityConfig.java
autonomous: true
requirements: [SEC-03]

must_haves:
  truths:
    - "The document springdoc actually SERVES omits the tenant-override header when TenantFilter is absent"
    - "The same document RETAINS the header when TenantFilter is present, so the absence is about the strip and not about an empty document"
    - "The served document is non-empty, so the absence assertion cannot pass vacuously"
    - "The already-shipped staging/prod doc-endpoint gating is proven capable of failing"
  artifacts:
    - path: "core-java/src/test/java/uk/jtoye/core/config/TenantHeaderAbsentDocumentTest.java"
      provides: "three-arm assertion on the SERVED /v3/api-docs document"
      contains: "TenantFilter.TENANT_HEADER"
  key_links:
    - from: "TenantHeaderAbsentDocumentTest"
      to: "/v3/api-docs"
      via: "MockMvc fetch of the served document"
      pattern: "get\\(\"/v3/api-docs\"\\)"
    - from: "test context initializer"
      to: "tenantFilter bean definition"
      via: "BeanDefinitionRegistryPostProcessor removeBeanDefinition"
      pattern: "removeBeanDefinition"
---

<objective>
Close SEC-03's last open link: prove the BUILT, SERVED OpenAPI document omits the dev-only
tenant-override header on a profile where `TenantFilter` is absent — and prove the assertion is
capable of failing.

Purpose: the ROADMAP's SC-3 as written greps source, and the decay doc already caught that. RESEARCH
DEC-3 caught the next layer: `docs/api/openapi-snapshot.json` legitimately CONTAINS `tenant-header`
(72 occurrences) because `OpenApiSnapshotTest` boots under `test`, where `TenantFilter` IS present.
A gate over either artifact is a permanently-red required check on a correct tree, which this repo
records as worse than no check. The only artifact that answers the question is the document
springdoc serves with the filter absent.

`TenantHeaderSchemeCustomizerTest` already asserts the strip on the OpenAPI MODEL object, both
directions. What is unproven is that springdoc applies that customizer to what it SERVES. That one
link is this plan's whole job.

Output: a three-arm integration test, and a recorded fail-direction proof for the already-shipped
`#549`/D-14 profile gating (which RESEARCH DEC-2 measured as complete — so this is a proof task,
not a fix task; a task that changes nothing would otherwise read as done while proving nothing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/CRITERIA-DECAY-2026-08-08.md
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-CONTEXT.md
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-RESEARCH.md
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-PATTERNS.md

<interfaces>
Contracts to build against — do not re-derive by exploration.

The strip mechanism (the `else` branch is what SC-3 asserts):
  core-java/src/main/java/uk/jtoye/core/config/TenantHeaderSchemeCustomizer.java:70-71
    public void customise(OpenAPI openApi) {
        if (openApi == null || tenantFilterProvider.getIfAvailable() != null) {   // -> LEAVE ALONE
  The class already references TenantFilter.TENANT_HEADER (:146, :155) rather than a literal —
  the test must do the same.

The served-document fetch recipe to copy:
  core-java/src/test/java/uk/jtoye/core/integration/OpenApiSnapshotTest.java:100-104
  and its one-line Testcontainers bootstrap at :86-95
    IntegrationTestSupport.registerPostgresTestProperties(registry, postgres);
  Its javadoc at :39-46 records WHY this is MockMvc and not springdoc-openapi-gradle-plugin
  (the plugin boots out-of-band, needs a live DB and port juggling in CI). Carry that reasoning forward.

The falsifiability discipline to copy verbatim:
  core-java/src/test/java/uk/jtoye/core/security/OpenApiProdProfileGatingTest.java:81-96
    "THE LINE THAT MAKES THIS TEST CAPABLE OF FAILING" — registry.add("springdoc.api-docs.enabled", "true")
  core-java/src/test/java/uk/jtoye/core/security/OpenApiProdProfileGatingTest.java:126-148
    aGenuinelyPublicRouteIsStillAnonymousInProd() — the non-vacuity control shape

The D-14 surface, ALREADY SHIPPED by #442 (RESEARCH DEC-2, verified by source read):
  core-java/src/main/java/uk/jtoye/core/security/SecurityConfig.java:190-196
    boolean looksLocal = ... "dev" || "test" || "local";
    boolean isDeployedProfile = active.contains("prod") || active.contains("staging");
    if (looksLocal && !isDeployedProfile) { auth.requestMatchers("/v3/api-docs/**", ...).permitAll(); }
  Existing both-direction tests:
    OpenApiDevProfileGatingTest.apiDocsStayAnonymousInDev          dev     -> 200
    StagingActuatorPortIsolationTest.apiDocsNotAnonymousInStaging  staging -> not 200
    OpenApiProdProfileGatingTest                                   prod    -> not 200
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assert the served document with TenantFilter absent — three arms</name>
  <files>core-java/src/test/java/uk/jtoye/core/config/TenantHeaderAbsentDocumentTest.java</files>
  <read_first>
    - core-java/src/main/java/uk/jtoye/core/config/TenantHeaderSchemeCustomizer.java (whole file — the guard at :70-71, and the TENANT_HEADER references at :146 and :155)
    - core-java/src/main/java/uk/jtoye/core/security/TenantFilter.java (the TENANT_HEADER constant and the @Profile annotation)
    - core-java/src/test/java/uk/jtoye/core/integration/OpenApiSnapshotTest.java (the fetch at :100-104, the bootstrap at :86-95, the javadoc rejection at :39-46)
    - core-java/src/test/java/uk/jtoye/core/security/OpenApiProdProfileGatingTest.java (the capable-of-failing comment at :81-96 and the non-vacuity control at :126-148)
    - core-java/src/test/java/uk/jtoye/core/config/TenantHeaderSchemeCustomizerTest.java (the existing MODEL-level proof — state in javadoc what this new test adds beyond it)
    - core-java/src/test/java/uk/jtoye/core/testsupport/IntegrationTestSupport.java:26-60
  </read_first>
  <action>
    Create `TenantHeaderAbsentDocumentTest` in `core-java/src/test/java/uk/jtoye/core/config/`,
    tagged so it runs in `integrationTest` (it needs Testcontainers Postgres via
    `IntegrationTestSupport.registerPostgresTestProperties`). Boot under `test` so the existing
    infrastructure stubbing applies, and remove the `TenantFilter` bean DEFINITION rather than
    switching profile — the customizer keys off `ObjectProvider<TenantFilter>.getIfAvailable()`, so
    bean removal reproduces the deployed-profile condition exactly while `OpenApiConfig` stays
    loaded (it is `@Profile("!prod")`). Implement with an `ApplicationContextInitializer` that
    registers a `BeanDefinitionRegistryPostProcessor` calling `removeBeanDefinition("tenantFilter")`,
    wired via `@ContextConfiguration(initializers = ...)`. Confirm the actual bean name from the
    `TenantFilter` declaration before hardcoding it, and make the removal fail loudly if the name
    is not registered — a silent no-op removal would leave arm 1 asserting nothing.

    Fetch the SERVED document through MockMvc at `/v3/api-docs` and assert on the response STRING,
    not on the `OpenAPI` model object. The model-level assertion already exists; the open link is
    that springdoc applies the customizer to what it serves.

    Write three arms, all three required:
      * arm 1 (the claim): with the filter bean removed, the served string contains neither
        `TenantFilter.TENANT_HEADER` (reference the constant, never a copied literal) nor the
        scheme key `tenant-header`.
      * arm 2 (control): a stock `test` context with the filter present serves a document that DOES
        contain both. Without this, arm 1's absence is indistinguishable from a document that could
        not be built.
      * arm 3 (denominator): the served document's `paths` object is non-empty. An empty document
        satisfies arm 1 vacuously — the same rule `check-openapi-snapshot-fresh.sh` encodes as its
        A-2 assertion.

    Carry a comment block in the style of `OpenApiProdProfileGatingTest:81-96` naming the property
    or condition that makes this class capable of failing, and stating that RESEARCH assumption A4
    (bean removal faithfully reproduces the staging document shape) is what arm 2 exists to catch —
    if springdoc caches the document before the postprocessor runs, arm 2 is what reveals it.

    Do NOT add a `scripts/check-*.sh` for this. It needs no runtime, it belongs in
    `integrationTest`, and a shell gate would immediately owe `scripts/gates/gate-enforcement.conf`
    an entry it cannot honestly make (the conf's bar is "a hosted runner does not have the thing
    this inspects", which is false here). SC-3's ROADMAP wording asks for "a CI gate shown to fail"
    — this test IS that gate, because `integrationTest` runs in CI; say so explicitly in the
    SUMMARY rather than leaving the substitution implicit.

    Do NOT touch `docs/metrics.json` or the prose counts — plan 28-11 owns the manifest.
  </action>
  <verify>
    <automated>cd core-java && ./gradlew :core-java:integrationTest --tests '*TenantHeaderAbsentDocumentTest'</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :core-java:integrationTest --tests '*TenantHeaderAbsentDocumentTest'` green, reporting 3 tests.
    - Arm 2 (filter present) asserts the served string CONTAINS both strings and passes — recorded output shows it ran, not just that the class was green.
    - Arm 3 asserts `paths` is non-empty with a stated minimum path count; the observed count is recorded.
    - FAIL DIRECTION 1 (the strip): neutralise `TenantHeaderSchemeCustomizer.customise` with an early `return` as its first statement, confirm arm 1 goes RED naming the header string, restore with `git checkout --`, verify by `git hash-object` == `git rev-parse HEAD:<path>`, and re-run all three arms green. All four states recorded (clean -> break -> restore -> clean).
    - FAIL DIRECTION 2 (the harness): make the bean-removal a no-op (skip the `removeBeanDefinition` call) and confirm arm 1 goes RED. This proves arm 1 is measuring the removal and not an unrelated context difference. Restore and re-run.
    - The test source references `TenantFilter.TENANT_HEADER` rather than a copied string literal of the header name. Assert by source: the constant reference is present.
    - `git diff --name-only` does not list `docs/metrics.json`, `CLAUDE.md`, `AGENTS.md` or `README.md`.
    - No new `scripts/check-*.sh` file is created by this task; `bash scripts/check-gate-enforcement.sh` exits 0 unchanged.
  </acceptance_criteria>
  <done>The served `/v3/api-docs` document is proven to omit the tenant-override header with `TenantFilter` absent, with a filter-present control, a non-empty-paths denominator, and two independent break arms observed red.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the shipped staging/prod doc gating is capable of failing (D-14 / #549)</name>
  <files>core-java/src/main/java/uk/jtoye/core/security/SecurityConfig.java (break arm ONLY — restored, net zero change)</files>
  <read_first>
    - core-java/src/main/java/uk/jtoye/core/security/SecurityConfig.java:170-210 (the `looksLocal && !isDeployedProfile` block at :190-196 and its surrounding chain)
    - core-java/src/test/java/uk/jtoye/core/security/StagingActuatorPortIsolationTest.java (the staging arm)
    - core-java/src/test/java/uk/jtoye/core/security/OpenApiProdProfileGatingTest.java (the prod arm and its capable-of-failing comment)
    - core-java/src/test/java/uk/jtoye/core/security/OpenApiDevProfileGatingTest.java (the dev control — 200)
    - .planning/phases/28-security-triage-the-dev-prod-boundary/28-RESEARCH.md § DEC-2
  </read_first>
  <action>
    D-14 asks for a config change. RESEARCH DEC-2 measured that the change already shipped in #442,
    and that the both-direction profile-parameterised test D-14 asks for already exists as three
    classes. So do NOT write a config change: there is nothing to change, and a task that changes
    nothing reads as done while proving nothing. What is genuinely missing is the fail direction.

    Commit the tree first (`git checkout` restores from the index, so a post-staging edit would be
    discarded by the restore). Then run the protocol CLEAN -> BREAK -> RESTORE -> CLEAN AGAIN:
      * CLEAN: all three gating classes green — `OpenApiDevProfileGatingTest` (dev serves 200),
        `StagingActuatorPortIsolationTest` (staging not 200), `OpenApiProdProfileGatingTest`
        (prod not 200). Record the per-class test counts.
      * BREAK: revert `SecurityConfig.java:190-196`'s condition to the pre-#442 shape — gate the
        `permitAll` on "not prod" alone instead of `looksLocal && !isDeployedProfile`, so that a
        `staging` profile once again matches. Edit by line number and read the result back; do not
        use a text `replace`, which can hit an explanatory comment instead of the live condition
        (33-07 lost an arm exactly that way and reported the criterion unfalsifiable while the real
        code was untouched). Confirm `StagingActuatorPortIsolationTest.apiDocsNotAnonymousInStaging`
        goes RED and record the assertion message. If it stays green, the criterion cannot fail and
        that must be reported as the finding — do not substitute a different arm silently.
      * RESTORE: `git checkout --` the file, verify by `git hash-object` against
        `git rev-parse HEAD:<path>`.
      * CLEAN AGAIN: re-run all three classes green.

    Record in the SUMMARY, for the triage doc that plan 28-05 writes: finding C3's disposition is
    FIXED-BY-#442 with this fail-direction proof as its evidence, and #549's description of staging
    as unauthenticated is stale. Do not close #549 here — 28-05 closes it pointing at the triage
    doc, so the closure and the record land together.
  </action>
  <verify>
    <automated>cd core-java && ./gradlew :core-java:integrationTest --tests '*OpenApiDevProfileGatingTest' --tests '*StagingActuatorPortIsolationTest' --tests '*OpenApiProdProfileGatingTest'</automated>
  </verify>
  <acceptance_criteria>
    - CLEAN arm: all three classes green with per-class test counts recorded.
    - BREAK arm: with `SecurityConfig.java:190-196` reverted to a not-prod-only condition, `StagingActuatorPortIsolationTest.apiDocsNotAnonymousInStaging` is RED and the failure message is recorded verbatim. A green break arm is a reportable finding, not a pass.
    - RESTORE verified by content: recorded `git hash-object` output equals recorded `git rev-parse HEAD:<path>` output.
    - CLEAN-AGAIN arm green, recorded separately from the first clean arm.
    - `git status` at task end shows NO modification to `SecurityConfig.java`.
    - The SUMMARY states C3's disposition (FIXED-BY-#442) and the exact evidence, in sanitized form (finding ID only), for 28-05 to transcribe.
  </acceptance_criteria>
  <done>The staging/prod doc-endpoint gating has an observed red break arm; C3's disposition and evidence are recorded for the triage doc; SecurityConfig.java is byte-identical to HEAD.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated network -> /v3/api-docs | anyone who can reach the service on a deployed profile |
| published API contract -> a client trusting it | the contract describes what mechanisms exist; describing a dev-only override teaches an attacker to try it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05 | Spoofing | OpenAPI document advertising the `X-Tenant-Id` override on a profile where `TenantFilter` is absent | mitigate | `TenantHeaderSchemeCustomizer` strips scheme, global requirement, per-operation requirements and the prose line; this plan asserts the SERVED document, with a filter-present control |
| T-28-06 | Information Disclosure | `/v3/api-docs` reachable anonymously on staging/prod | mitigate | already shipped by #442 (`looksLocal && !isDeployedProfile`); this plan supplies the missing fail direction so the control is trusted rather than assumed |
| T-28-07 | Tampering | a future edit reintroducing an unconditional `permitAll` or an unconditional header advertisement | mitigate | three existing profile-gating classes plus this plan's new three-arm document test, all in `integrationTest` which runs in CI |
| T-28-08 | Information Disclosure | a vacuous assertion passing over an empty or unbuildable document | mitigate | arm 3's non-empty `paths` denominator plus arm 2's filter-present control |

Cross-cutting dimensions: web-perf **N/A**, SEO **N/A** (the API doc endpoint is not a public marketing
surface and is non-200 on deployed profiles), agent-readiness **relevant and preserved** — the contract
must keep matching live responses on the profiles where it IS served, which arm 2 asserts. Falsifiable
evidence: two independent break arms in Task 1, one in Task 2, all with restores verified by content hash.
</threat_model>

<verification>
- `./gradlew :core-java:integrationTest --tests '*TenantHeaderAbsentDocumentTest' --tests '*OpenApiDevProfileGatingTest' --tests '*StagingActuatorPortIsolationTest' --tests '*OpenApiProdProfileGatingTest'` green.
- `bash scripts/check-gate-enforcement.sh` rc=0 (no new gate script was added).
- `git status` shows exactly one new file and zero modified source files.
</verification>

<success_criteria>
- The SERVED document omits the tenant-override header with `TenantFilter` absent, and retains it with the filter present.
- Three break arms observed red across the two tasks, each with a content-hash-verified restore and a re-run clean arm.
- C3's disposition and evidence recorded for plan 28-05's triage doc.
- No `docs/metrics.json` or prose edit (28-11 owns the manifest).
</success_criteria>

<output>
Create `.planning/phases/28-security-triage-the-dev-prod-boundary/28-02-SUMMARY.md` when done.
Record: the served-document arm results with the observed `paths` count; both Task 1 break arms with
their red output; Task 2's four-part protocol with the restore hashes; and the explicit statement
that `integrationTest` in CI is SC-3's "CI gate", with the reason a shell gate was rejected.
</output>
