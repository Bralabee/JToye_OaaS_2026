---
phase: 28-security-triage-the-dev-prod-boundary
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-media-content-types.sh
  - scripts/gates/gate-enforcement.conf
  - docs/security/MEDIA-BACKFILL-PLAN-2026-08-10.md
autonomous: true
requirements: [SEC-02]

must_haves:
  truths:
    - "The count of stored objects whose Content-Type is outside the image allowlist is measured, with a denominator, and the probe is proven able to report a non-allowlist type"
    - "A future object with a spoofable Content-Type reds a permanent gate"
    - "The deferred full-catalogue sweep exists as a dated written plan naming what stays exposed until it runs"
    - "The anonymous bucket-enumeration exposure is filed as a sanitized public issue"
  artifacts:
    - path: "scripts/check-media-content-types.sh"
      provides: "credentialed enumeration of jtoye-images stored Content-Types, relation-not-census"
      min_lines: 60
    - path: "docs/security/MEDIA-BACKFILL-PLAN-2026-08-10.md"
      provides: "the dated D-06/D-07/D-08 specification for the deferred sweep"
    - path: "scripts/gates/gate-enforcement.conf"
      provides: "runtime-dependency justification for the new gate"
      contains: "check-media-content-types.sh"
  key_links:
    - from: "scripts/check-media-content-types.sh"
      to: "scripts/gates/gate-enforcement.conf"
      via: "registration landed in the SAME task as the script"
      pattern: "check-media-content-types\\.sh"
---

<objective>
Discharge #488 honestly: measure the population, remediate the urgent subset, and write down the
deferred half with a date and an exposure statement.

Purpose: D-05 says "measure first, remediate the urgent subset now". RESEARCH DEC-4 measured it —
against the live `jtoye-images` bucket, 768 objects, the urgent subset (stored Content-Type outside
the image allowlist) is **ZERO**, and the EXIF/GPS half is **0 of 37** legacy objects, both with
controls. So the deliverable is the enumeration tool, the recorded measurement, and the dated plan
— NOT a re-pipeline run. A plan that schedules a re-pipeline schedules a task that processes zero
objects and reports success, and D-06/D-07/D-08 would be built over an empty input.

That is a scope observation, not a scope reduction: D-06/D-07/D-08 are preserved in full as the
written specification the deferred sweep executes, with their analogs already identified.

Output: a permanent gate so the count cannot silently become non-zero, the census with its controls,
the dated deferred plan, and a sanitized issue for the new exposure RESEARCH found (DEC-5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-CONTEXT.md
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-RESEARCH.md
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-PATTERNS.md

<interfaces>
Measured 2026-08-10 against the live bucket — RE-MEASURE, do not quote these. Any upload moves them.
  768 objects total; prefix census media 731 / products 33 / shops 4
  content-type census: image/webp 731, image/jpeg 35, image/png 2
  objects with Content-Type outside the allowlist: 0   (control: the same probe emitted
    application/xml against a known non-image response, so it CAN report a non-allowlist type)
  legacy (non-pipeline) objects carrying [EXIF] or [GPS]: 0 of 37   (control: the same exiftool
    census over a copy with GPS injected reported [EXIF] GPSLatitudeRef, [Composite] GPSPosition)
  objects under a */quarantine/* prefix: 0

DEC-5, the new exposure this phase files:
  `mc anonymous set download local/jtoye-images` grants s3:ListBucket AS WELL AS s3:GetObject.
  Verified live: curl "http://localhost:9000/jtoye-images?list-type=2&max-keys=1000" -> 200, KeyCount 768,
  with NO credential. MediaProperties' javadoc already records the READ half ("a quarantine object is
  anonymously readable by key"); the LIST half means an attacker does not need the key.
  Consequence for D-06: a "non-public quarantine prefix" is NOT achievable by a naming convention.
  The FIX lands in plan 28-09 (same compose bootstrap block as #270). This plan FILES it.

Gate shape to copy (runtime-dependent, conf-registered, relation-not-census):
  scripts/check-live-shop-coordinates.sh  and its entry at scripts/gates/gate-enforcement.conf:35
Gate header/exit-code contract to copy:
  scripts/check-geo-attribution.sh:1-30 (exit 0/1/2 table, the two shell-shape warnings)
Deferred-sweep implementation analogs, for the dated plan to name (do NOT build them here):
  MediaAssetService re-drive path :360-414 (resets to PENDING from retained quarantine bytes + enqueues outbox)
  MediaProcessingEvent(tenantId, assetId) on media_event_outbox — MediaEventOutboxFlusher.publishRow
    has NO closed-set dispatch (:24-39, :218-224), so outbox_flusher_dispatch_trap does NOT apply
  V60 quarantine_expires_at / quarantine_reclaimed_at column pair
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Credentialed Content-Type enumeration gate, landed with its registration</name>
  <files>scripts/check-media-content-types.sh, scripts/gates/gate-enforcement.conf</files>
  <read_first>
    - scripts/check-geo-attribution.sh (whole file — the header contract at :1-30, path resolution and fail/void helpers at :30-54, and the required-token loop with `grep -cF ... || true` and the empty-result VOID at :70-83)
    - scripts/check-live-shop-coordinates.sh (whole file — the relation-with-a-denominator shape and the stopped-stack rc=2 behaviour)
    - scripts/gates/gate-enforcement.conf (the FILE HEADER at :1-14 — the bar for an entry, and the VOID-on-nonexistent-script trap — plus the check-live-shop-coordinates.sh entry at :35 as the wording model)
    - scripts/check-gate-enforcement.sh:140-165 (how entries are validated)
    - docker-compose.full-stack.yml:516-560 (the minio and minio-init services, the network name, and the `$$VAR` escaping idiom)
    - core-java/src/main/java/uk/jtoye/core/media/MediaNormalizer.java (the magic-byte allowlist — the gate's allowlist must be the SAME set, named from this source, not invented)
  </read_first>
  <action>
    Create `scripts/check-media-content-types.sh` asserting one invariant: every object stored in
    the `jtoye-images` bucket carries a stored Content-Type inside the image allowlist that
    `MediaNormalizer` enforces on upload. Read the allowlist out of `MediaNormalizer` and name the
    same values in the script's own comment so a future divergence is visible.

    Enumerate with CREDENTIALS, not anonymously. Run `minio/mc` as a one-shot container on the
    compose network using `MINIO_ROOT_USER` / `MINIO_ROOT_PASSWORD` from the environment, exactly
    the way `minio-init` does, including the `$$VAR` escaping if you express it through compose.
    This is load-bearing sequencing: plan 28-09 removes anonymous `s3:ListBucket` from this bucket,
    so a gate that enumerates anonymously would start VOIDing the moment that fix lands — and a
    VOID that arrives with an unrelated change reads as a broken gate rather than as a fixed bucket.

    Assert a RELATION with an explicit denominator, never a census: report
    "N of M objects carry a Content-Type outside the allowlist" and fail when N > 0. A gate that
    asserts a fixed object count reds on legitimate new uploads, and a gate that reds on legitimate
    data gets silenced.

    Copy the exit-code contract verbatim from `check-geo-attribution.sh:1-30`: 0 the invariant
    holds, 1 violated and NAMED with the offending key, 2 VOID. VOID on: the container not running,
    `mc` unavailable, credentials unset, an unparseable listing, or an EMPTY listing. M == 0 is a
    VOID, not a pass — "I could not find any objects" must never read as "every object is fine".
    Use `set -uo pipefail`; never `cmd | grep -q X` (it INVERTS on match via SIGPIPE promoted to
    141 under pipefail — use a here-string); capture every `rc` on the same statement as its
    command, never after an intervening `echo`.

    In the SAME task, add the `gate-enforcement.conf` entry with its reasoning written to the
    file header's bar — a hosted runner has no MinIO container to exec into and no bucket, so the
    script's own preconditions would exit 2 there on every run; its purpose is to prove a property
    of the DELIVERED object store, which is by definition not a property of a source checkout.
    Never split the script and its registration across tasks or plans: the conf VOIDs on an entry
    naming a script that does not exist, and pre-declaring a sibling plan's script forced an extra
    wave in Phase 33.
  </action>
  <verify>
    <automated>bash scripts/check-media-content-types.sh; echo "rc=$?"</automated>
    <automated>bash scripts/check-gate-enforcement.sh; echo "rc=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-media-content-types.sh` exits 0 on the running stack and prints both N (offending) and M (total enumerated), with M > 0.
    - `bash scripts/check-gate-enforcement.sh` exits 0 with the new script registered — run it AFTER adding the conf entry and record the gate count it reports.
    - FAIL DIRECTION 1 (the invariant): upload one object to `jtoye-images` with a deliberately non-allowlist stored Content-Type (e.g. `text/html`), confirm the gate exits 1 and NAMES that key, then delete the object and confirm the gate returns to 0. Both directions recorded.
    - FAIL DIRECTION 2 (fail-closed): stop the MinIO container and confirm the gate exits 2 (VOID), not 0. Restart and confirm 0. A gate that fails OPEN on missing input is worse than no gate.
    - FAIL DIRECTION 3 (denominator): point the script at a non-existent bucket name and confirm it exits 2 on the empty listing rather than 0 on "no offending objects".
    - The script does NOT rely on anonymous access: assert by source that the enumeration path passes `MINIO_ROOT_USER`/`MINIO_ROOT_PASSWORD` (or an equivalent credentialed alias), and that no unauthenticated `list-type=2` URL appears in it.
    - `scripts/gates/gate-enforcement.conf` names `check-media-content-types.sh` with a reason that states what a hosted runner lacks. Committed in the same commit as the script.
    - No literal credential value appears in the script or the conf — values come from the environment.
  </acceptance_criteria>
  <done>A credentialed, relation-shaped gate reds on a spoofed-Content-Type object and VOIDs on a stopped stack, and it is registered in the same commit that creates it.</done>
</task>

<task type="auto">
  <name>Task 2: Record the census, write the dated deferred plan, file the anonymous-listing exposure</name>
  <files>docs/security/MEDIA-BACKFILL-PLAN-2026-08-10.md</files>
  <read_first>
    - docs/security/PII-EXPOSURE-ASSESSMENT-2026-07-08.md:1-60 (the front-matter block at :1-9 and the per-item measured-prose style at :36-49 — this is the shape for a dated sanitized security record)
    - .planning/phases/28-security-triage-the-dev-prod-boundary/28-RESEARCH.md § DEC-4 and § DEC-5
    - .planning/phases/28-security-triage-the-dev-prod-boundary/28-CONTEXT.md § "Media backfill (#488)" (D-05..D-08 verbatim) and § "Deferred Ideas"
    - core-java/src/main/java/uk/jtoye/core/media/MediaProperties.java (its javadoc records the anonymous-read half of DEC-5 already)
    - core-java/src/main/java/uk/jtoye/core/media/MediaAssetService.java:360-414 (the re-drive path the deferred sweep will use)
  </read_first>
  <action>
    Re-run the three censuses on the live bucket and record the numbers with their controls — do
    not quote RESEARCH's figures, which decay on any upload:
      * stored Content-Type census by value, with the total;
      * the EXIF/GPS census over the legacy (non-pipeline) objects using `exiftool -q -s -G`,
        with the control that made it non-vacuous — the same census over a copy with GPS injected
        must report `[EXIF]`/`[GPS]` tags. Without the control, "0 objects carry EXIF" is
        indistinguishable from "exiftool is not reporting tags";
      * the `*/quarantine/*` prefix census.
    Never bound a stream you are using to prove a negative — no `| head` on the listing, because a
    truncating filter manufactures the absence it is asked to disprove.

    Write `docs/security/MEDIA-BACKFILL-PLAN-2026-08-10.md` following the front-matter block and
    measured-prose style of the PII assessment. It must contain, in this order:
      * the three censuses with their controls and the date;
      * the urgent-subset disposition: the population is the measured count (expected 0), the
        permanent gate that now holds it there is `scripts/check-media-content-types.sh`, and #488's
        urgent limb is therefore CLOSED by measurement rather than by a re-pipeline run;
      * a correction to #488's framing, stated with its control: the GDPR/EXIF exposure it describes
        is measured at 0 of 37 legacy objects, so the remaining cost of the deferred half is
        CWV/WebP over 37 objects, not GDPR. #488's GDPR framing is what makes the deferral feel
        dangerous, so correcting it is part of the deferral being honest;
      * the deferred sweep's full specification, carrying D-06, D-07 and D-08 forward unchanged —
        originals to a quarantine prefix on a declared expiry mirroring V60's
        `quarantine_expires_at`/`quarantine_reclaimed_at`; non-decodable objects pulled from the
        public origin, marked FAILED with a reason and rendered by the existing IMG-04 vendor UI;
        successful normalisation to the standard WebP derivative + 400px thumbnail path with
        media_asset and the flat dual-read columns updated. State D-08's invariant explicitly: no
        storefront image 404s during the transition;
      * the implementation analogs so the future executor does not rebuild them: MediaAssetService's
        re-drive path, `MediaProcessingEvent(tenantId, assetId)` on `media_event_outbox`, and the
        explicit note that `outbox_flusher_dispatch_trap` does NOT apply here because
        `MediaEventOutboxFlusher.publishRow` has no closed-set dispatch;
      * a named blocker: D-06 requires a PREFIX-SCOPED bucket policy, not a naming convention,
        because the bucket-wide anonymous policy covers every key. Cross-reference the issue filed
        below and the fix landing in plan 28-09;
      * a target: Phase 29 or later, or a background job — and a statement of what stays exposed
        until it runs, in the measured terms above rather than #488's original framing.

    Then file ONE sanitized public issue for DEC-5 with `gh issue create`: the `jtoye-images` bucket
    grants anonymous `s3:ListBucket` alongside `s3:GetObject`, so the whole object inventory is
    enumerable with no credential. Impact + fix + acceptance only. Name the fix as a prefix-scoped
    or GetObject-only bucket policy and say the fix lands in this phase alongside #270. Do NOT
    include the enumeration URL, a key listing, or any credential. Pass the issue body through a
    quoted heredoc or `-F <file>`, never an interpolating double-quoted string — backticks inside
    `"..."` execute and silently drop the phrases they were meant to quote. Record the issue number
    in the SUMMARY; plan 28-05 puts it in the triage doc.

    Do NOT touch `docs/metrics.json` or the prose counts — plan 28-11 owns the manifest.
  </action>
  <verify>
    <automated>test -s docs/security/MEDIA-BACKFILL-PLAN-2026-08-10.md && grep -c "quarantine_expires_at" docs/security/MEDIA-BACKFILL-PLAN-2026-08-10.md</automated>
    <automated>bash scripts/check-media-content-types.sh; echo "rc=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/security/MEDIA-BACKFILL-PLAN-2026-08-10.md` exists, is non-empty, and contains all three censuses WITH their control results and the measurement date.
    - The EXIF census records its positive control (a GPS-injected copy reporting `[EXIF]`/`[GPS]` tags). A census recorded without its control is not acceptable — it cannot distinguish "no EXIF" from "exiftool reported nothing".
    - The doc names D-06, D-07 and D-08 and reproduces each one's requirement, including D-08's no-404 invariant. Assert by source: `grep -c "D-06\|D-07\|D-08"` >= 3.
    - The doc names the prefix-scoped-bucket-policy blocker and cross-references the filed issue number and plan 28-09.
    - The doc names the three implementation analogs (MediaAssetService re-drive, MediaProcessingEvent on media_event_outbox, V60 quarantine columns) and states that outbox_flusher_dispatch_trap does not apply, with the reason.
    - `gh issue view <n>` shows the DEC-5 issue open, containing impact, fix and acceptance, and containing NO enumeration URL, NO object key listing and NO credential. FAIL DIRECTION for the sanitization claim: grep the issue body for the bucket-listing query string and for `MINIO_ROOT` and confirm both counts are 0 — run the same grep against a string that IS present to prove the grep can match.
    - The issue body as STORED is verified after creation (`gh issue view --json body`), not merely as composed — an interpolating quote would have dropped or executed content invisibly at write time.
    - `git diff --name-only` does not list `docs/metrics.json`, `CLAUDE.md`, `AGENTS.md` or `README.md`.
  </acceptance_criteria>
  <done>The three censuses are recorded with controls, the deferred sweep has a dated specification carrying D-06/D-07/D-08 intact, and the anonymous-listing exposure is filed as a sanitized issue whose stored body was read back.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous internet -> MinIO `jtoye-images` | the bucket is anonymously readable by design (public storefront images) |
| vendor upload -> stored object metadata | a client-declared Content-Type persisted on a public origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-09 | Tampering (stored XSS) | objects on the public origin whose stored Content-Type is outside the image allowlist | mitigate | `MediaNormalizer`'s magic-byte allowlist on the upload path (already shipped, #445/#479) plus the new `check-media-content-types.sh` gate holding the count at its measured value |
| T-28-10 | Information Disclosure | anonymous `s3:ListBucket` exposing the whole object inventory (DEC-5) | mitigate | filed as a sanitized issue here; the bucket-policy fix lands in plan 28-09. Recorded as UNMITIGATED-TODAY in the dated plan so the gap is visible rather than implied |
| T-28-11 | Information Disclosure | EXIF/GPS on legacy objects | accept, time-boxed | measured 0 of 37 with a positive control; the residual is CWV/WebP not GDPR. Carried in the dated deferred plan with a target, not silently dropped |
| T-28-12 | Tampering | the gate itself failing OPEN on a stopped stack or an empty listing | mitigate | exit 2 VOID on missing container, missing credentials, unparseable or EMPTY listing; both proven by break arm |

Cross-cutting dimensions: web-perf **relevant, deferred with a number** — 37 non-WebP legacy objects are
the remaining CWV cost, recorded in the dated plan; SEO **N/A**; agent-readiness **N/A** (no API surface
change). Falsifiable evidence: three break arms on the gate, plus a positive control on the EXIF census
and a fail-direction grep on the sanitization claim.
</threat_model>

<verification>
- `bash scripts/check-media-content-types.sh` rc=0 on a running stack, rc=2 on a stopped one.
- `bash scripts/check-gate-enforcement.sh` rc=0 with the new entry present.
- `docs/security/MEDIA-BACKFILL-PLAN-2026-08-10.md` carries all three censuses with controls.
- The DEC-5 issue exists, and its STORED body was read back and grepped for leakage.
</verification>

<success_criteria>
- #488's urgent limb is closed by measurement, and a permanent gate stops the count drifting silently.
- The deferred half exists as a dated, sanitized plan that preserves D-06/D-07/D-08 in full and names what stays exposed.
- The anonymous-enumeration exposure is publicly tracked before its fix ships in 28-09.
</success_criteria>

<output>
Create `.planning/phases/28-security-triage-the-dev-prod-boundary/28-03-SUMMARY.md` when done.
Record: the three censuses with controls and totals; all three gate break arms with real rc values;
the gate count `check-gate-enforcement.sh` reports after registration; and the DEC-5 issue number,
for plan 28-05's triage doc.
</output>
