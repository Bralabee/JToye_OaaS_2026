---
phase: 28-security-triage-the-dev-prod-boundary
plan: 05
type: execute
wave: 2
depends_on: ["28-01", "28-02", "28-03", "28-04"]
files_modified:
  - docs/security/PENTEST-TRIAGE.md
  - scripts/check-pentest-triage.sh
  - .github/workflows/ci-cd.yaml
  - infra/keycloak/realm-export.template.json
autonomous: true
requirements: [SEC-02, SEC-03]

must_haves:
  truths:
    - "Every one of the 11 pentest finding IDs has a disposition in a tracked, sanitized, public-safe document"
    - "Removing one disposition row fails a CI gate that names the missing ID"
    - "The gate cannot pass over a document that lost its table but kept the IDs in prose"
    - "Which Keycloak clients can mint the core-api audience is recorded, and a client that should not is shown to be rejected by an actual token and an actual 401"
  artifacts:
    - path: "docs/security/PENTEST-TRIAGE.md"
      provides: "one machine-greppable disposition row per finding ID A1..E1"
      contains: "E1"
    - path: "scripts/check-pentest-triage.sh"
      provides: "completeness gate over the 11 literal finding IDs, with a denominator and a status vocabulary"
      min_lines: 70
  key_links:
    - from: ".github/workflows/ci-cd.yaml"
      to: "scripts/check-pentest-triage.sh"
      via: "ops-contracts job step, landed in the same task as the script"
      pattern: "check-pentest-triage\\.sh"
    - from: "scripts/check-pentest-triage.sh"
      to: "docs/security/PENTEST-TRIAGE.md"
      via: "absolute-path scan scoped to the triage doc only"
      pattern: "PENTEST-TRIAGE\\.md"
---

<objective>
Discharge SEC-02: all 11 findings triaged, each with a sanitized public issue or a dated written
acceptance, in a record that repo-level review can actually see.

Purpose: D-11 rejected issue-comments-only because that record is invisible to repo-level review —
the 2026-08-02 lesson, where findings existed only in a gitignored directory and two of them sat in
no group at all. A tracked doc plus a gate that fails on a removed line is the form that survives.

This plan runs in wave 2 because it TRANSCRIBES the wave-1 verdicts: A1's CONFIRMED/FALSIFIED
(28-01), C3's fail-direction proof (28-02), the #488 census and the new DEC-5 issue number (28-03),
and #281's disposition plus any STOMP issue (28-04). Read those SUMMARYs; do not re-derive.

Output: the triage doc, its completeness gate wired into CI in the same task, the E1 audience audit
with a live rejection arm, and the closure of #548/#549/#551.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-CONTEXT.md
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-RESEARCH.md
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-PATTERNS.md
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-01-SUMMARY.md
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-02-SUMMARY.md
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-03-SUMMARY.md
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-04-SUMMARY.md

<interfaces>
The 11 finding IDs, which the gate embeds as a literal because SECURITY-FINDINGS.md is git-excluded
and the gate cannot read it (RESEARCH Open Question 4):
  A1 A2 A3 B1 B2 C1 C2 C3 C4 D1 E1

Known dispositions going in — VERIFY each against its source before writing it down:
  A1  -> 28-01-SUMMARY.md's CONFIRMED/FALSIFIED verdict; tracking issue #548
  A2  -> DO NOT RE-FILE from OpenApiConfig.java:51. That coordinate is the source TEMPLATE, stripped
         at document-build time when TenantFilter is absent — plan 28-02 proved the strip on the
         SERVED document. Record it as such (memory: security-findings-untracked-pentest)
  C3  -> FIXED by #442 (SecurityConfig.java:190-196, `looksLocal && !isDeployedProfile`), with
         28-02 Task 2's observed break arm as its evidence; issue #549
  C4  -> already CLOSED (#550)
  B1  -> the remainder is rotation + the runtime-role split, tracked by #552 and IN FLIGHT in this
         phase (plans 28-07..28-10). An OPEN finding tracked by a sanitized issue IS a disposition;
         plan 28-11 upgrades this row at close-out
  E1  -> #551, audited below
  #281, #488, DEC-5, any STOMP finding -> from the wave-1 SUMMARYs
  The remaining IDs -> 8 of 11 were measured as already remediated by other shipped work on
         2026-08-05 with controls. RE-STATE the evidence per ID; do not carry "8 of 11" as a bulk claim.

The E1 audit, ALREADY ANSWERABLE STATICALLY — measured with jq over both committed realm exports:
  jtoye-dev / core-api               confidential, service acct, direct core-api audience mapper -> INTENDED (resource server's own client)
  jtoye-dev / integration-catalog-ro confidential, service acct, direct mapper -> INTENDED (#206 least-privilege, catalog:read)
  jtoye-dev / integration-orders-rw  confidential, service acct, direct mapper -> INTENDED (Phase 25, orders:write/customers:write/catalog:read)
  jtoye-dev / edge-api               no core-api audience mapper -> CORRECT (forwards user tokens)
  jtoye-dev / test-client            PUBLIC, unused, no mapper -> THE E1 SYMPTOM. #551 asks: remove it or write down why it stays
  jtoye-dev built-ins (account, account-console, admin-cli, broker, realm-management, security-admin-console) -> no mapper
  jtoye-customers / storefront-client -> no core-api audience, trust-scoped by issuer
    (CustomerJwtVerifier.java:142-144 records this deliberately)
  INDIRECT PATH worth naming: every client carries the `roles` default client scope containing an
  `oidc-audience-resolve-mapper`, which adds the audience of any client for which the user holds
  CLIENT ROLES. Measured: core-api declares ZERO client roles and no seeded user has a client-role
  mapping, so the path is currently INERT. Record it WITH the measurement — it is the mechanism that
  silently widens the audience the day someone adds a core-api client role.

  #551's second acceptance criterion needs a LIVE token, not a config read:
    core-java/src/main/java/uk/jtoye/core/security/AudienceValidator.java:37-43 rejects a token whose
    `aud` lacks the configured expected audience. test-client is PUBLIC, so a token can be requested
    without a secret. That is the arm to run and record.

Gate analogs:
  scripts/check-geo-attribution.sh:1-30 (header + exit contract), :30-54 (path resolution, fail/void
    helpers), :41 and :70-83 (the required-token loop with `grep -cF ... || true` and the empty-result VOID)
  scripts/check-no-create-extension.sh:34-39 (the SELF-MATCH guard — this gate names all 11 IDs, so
    its scan MUST be scoped by absolute path to the triage doc only)
CI wiring analog:
  .github/workflows/ci-cd.yaml:677-689 — the ops-contracts step for check-no-create-extension.sh:
  a preceding comment saying WHY the gate is static, then `chmod +x` then invoke. Job permissions
  are declared at :635-641.
Doc analog:
  docs/security/PII-EXPOSURE-ASSESSMENT-2026-07-08.md:1-9 (front matter), :36-49 (measured prose style)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the sanitized triage record — one dispositioned row per finding</name>
  <files>docs/security/PENTEST-TRIAGE.md</files>
  <read_first>
    - docs/security/PII-EXPOSURE-ASSESSMENT-2026-07-08.md (whole file — the front-matter block and the measured-prose discipline)
    - .planning/phases/28-security-triage-the-dev-prod-boundary/28-01-SUMMARY.md through 28-04-SUMMARY.md (the four wave-1 verdicts you are transcribing)
    - SECURITY-FINDINGS.md at the repo root (git-excluded — READ IT LOCALLY for the ID-to-title mapping and severities; NOTHING from it is copied verbatim into the tracked doc)
    - .planning/phases/28-security-triage-the-dev-prod-boundary/28-CONTEXT.md § "Sanitization rule" and § D-11/D-12
    - the GitHub issues by number: #548 #549 #550 #551 #552 #283 #284 #270 #281 #488 (`gh issue view`)
  </read_first>
  <action>
    Create `docs/security/PENTEST-TRIAGE.md`. Open with the front-matter block in the PII
    assessment's shape at :1-9 — title, reference (issue #548, Strix run `d8c0`, 2026-07-31),
    assessment date, classification, status — followed by an explicit sanitization statement: this
    repository is PUBLIC, findings are referenced by ID only, and no literal secret value, no
    DB-to-OAuth-to-header chain and no repro payload appears here. Name `SECURITY-FINDINGS.md` as
    git-excluded and say it stays that way.

    Then a table with exactly ELEVEN disposition rows, one per ID in the order A1 A2 A3 B1 B2 C1 C2
    C3 C4 D1 E1. Each row carries: the ID, a STATUS from a closed vocabulary you define in the doc
    itself, and a disposition cell holding either an issue link or a DATED written acceptance. Cite
    the row format in the doc's own prose so a reader can see what the gate reads — the gate and
    the doc must agree, and the only way to keep them agreeing is to write the contract down once
    where both can be checked against it.

    Populate every row from evidence, not from the bulk "8 of 11 remediated" claim: state per ID
    what shipped, when, and what measurement or control settled it. Three specific rows:
      * A1 takes 28-01's CONFIRMED/FALSIFIED verdict verbatim, including which measurement settled it.
      * A2 records the do-not-re-file rule with its reason — `OpenApiConfig.java:51` is a source
        template stripped at document-build time, and 28-02 proved the strip on the SERVED document.
        A future reader grepping that coordinate must find the answer here rather than re-file it.
      * B1 is OPEN, tracked by #552, with a note that the remainder is in flight in this phase and
        that plan 28-11 upgrades the row at close-out. An open finding tracked by a sanitized issue
        IS a disposition; do not pre-date it as fixed.

    Add a section for the D-12 / E1 audience audit: the full client table from <interfaces>, RE-RUN
    with `jq` over `infra/keycloak/realm-export.template.json` and `infra/keycloak/realm-export-customers.json`
    rather than copied, plus the indirect `oidc-audience-resolve-mapper` path recorded WITH its
    measurement (core-api's client-role count and the seeded users' client-role mappings). State the
    mechanism, so the day someone adds a core-api client role the consequence is already written down.

    Add a short section listing the findings this phase FILED rather than fixed, with their issue
    numbers: the DEC-5 anonymous-listing exposure from 28-03, any STOMP temporal gap from 28-04, and
    (once plan 28-07 files it) the `jtoye_backup` default-privileges defect. Leave a named
    placeholder row for the last one rather than omitting it — 28-11 fills it, and a placeholder is
    visible where an omission is not.

    Do NOT touch `docs/metrics.json` or the prose counts — plan 28-11 owns the manifest.
  </action>
  <verify>
    <automated>test -s docs/security/PENTEST-TRIAGE.md && grep -cE '^\| *(A1|A2|A3|B1|B2|C1|C2|C3|C4|D1|E1) *\|' docs/security/PENTEST-TRIAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - The doc exists, is non-empty, and the line-anchored row grep counts EXACTLY 11. FAIL DIRECTION: delete one row and confirm the count is 10 — record both numbers, because a count only ever observed at 11 does not prove the pattern is anchored to rows rather than matching prose.
    - Every row's disposition cell contains either a `#NNN` issue reference or an ISO date. Assert: no row's disposition cell is empty or whitespace-only.
    - A1's row reproduces 28-01's verdict word (CONFIRMED or FALSIFIED) and names the measurement.
    - A2's row states the do-not-re-file rule and the build-time-strip reason.
    - B1's row is OPEN and links #552.
    - The E1 section's client table was regenerated by `jq` in this task (record the command and its output), not copied from RESEARCH, and it records core-api's client-role count as the evidence that the indirect resolve path is inert.
    - SANITIZATION, verified rather than asserted: grep the committed doc for the env-var key names that hold secret VALUES and for any 20+ character high-entropy token, and confirm 0 matches; then run the same greps against a string known to be present to prove they can match. A sanitization claim backed by a grep that never matches anything is worthless.
    - `git diff --name-only` does not list `docs/metrics.json`, `CLAUDE.md`, `AGENTS.md` or `README.md`.
  </acceptance_criteria>
  <done>All 11 findings carry an evidenced, sanitized disposition in a tracked doc, the E1 audit is regenerated rather than transcribed, and the sanitization claim was proven with a grep shown able to match.</done>
</task>

<task type="auto">
  <name>Task 2: Completeness gate, landed with its CI wiring in the same commit</name>
  <files>scripts/check-pentest-triage.sh, .github/workflows/ci-cd.yaml</files>
  <read_first>
    - scripts/check-geo-attribution.sh (whole file — copy the header/exit contract at :1-30, the path resolution and helpers at :30-54, and the required-token loop at :70-83 including `grep -cF ... || true` and the empty-result VOID)
    - scripts/check-no-create-extension.sh:26-44 and :34-39 (the exit-code rationale and the SELF-MATCH guard — this gate names all 11 IDs and must not scan itself)
    - .github/workflows/ci-cd.yaml:630-700 (the ops-contracts job header, its declared permissions, and the check-no-create-extension.sh step at :677-689)
    - scripts/check-gate-enforcement.sh (how a static gate satisfies the default-deny rule via a workflow reference)
    - docs/security/PENTEST-TRIAGE.md as written in Task 1 (the row contract the gate reads)
  </read_first>
  <action>
    Create `scripts/check-pentest-triage.sh` asserting three things, not one:
      1. COMPLETENESS — each of the eleven literal IDs `A1 A2 A3 B1 B2 C1 C2 C3 C4 D1 E1` appears as
         a LINE-ANCHORED disposition row. Match on the row shape, never on a bare ID: a bare `A1`
         matches prose and would pass over a doc whose table was deleted.
      2. DENOMINATOR — the total count of disposition rows is exactly 11. Without this, a document
         that lost its table but kept the IDs in a paragraph could still satisfy assertion 1.
      3. STATUS VOCABULARY — every row carries one of the allowed statuses AND a non-empty
         disposition cell holding an issue link or a date. A row that is present but empty is not a
         disposition, and SEC-02's requirement is a disposition, not a mention.

    Copy the exit-code contract verbatim from `check-geo-attribution.sh:1-30`: 0 holds, 1 violated
    and NAMED with the offending ID and its line, 2 VOID. VOID when the doc is missing, empty, or
    when any count yields an empty result. Use `set -uo pipefail`. Never `cmd | grep -q X` — under
    pipefail it INVERTS on match via SIGPIPE promoted to 141, which has already made a guard in this
    repo fail OPEN; use here-strings. Capture counts with `|| true` and compare them as values,
    because `grep -c` exits 1 on a zero count, which is the desired state of an absence check.

    Guard the self-match hazard the way `check-no-create-extension.sh:34-39` documents: this script
    contains all eleven IDs in its own source, so its scan must be scoped by ABSOLUTE PATH to the
    triage doc only. Never let it glob the repo.

    In the SAME task and the SAME commit, add the `ops-contracts` step in `.github/workflows/ci-cd.yaml`
    following the `check-no-create-extension.sh` step at :677-689 exactly: a preceding comment
    stating why the gate is static (it reads one tracked markdown file, touches no database and
    makes no network call, so it says the same thing on a hosted runner as it does locally), a
    `name:` that states the invariant, then `chmod +x` and the invocation. This gate is static, so
    it wires into the workflow and must NEVER get a `scripts/gates/gate-enforcement.conf` entry —
    the conf's bar is "a hosted runner does not have the thing this inspects", which is false here.

    Two hazards this repo has been bitten by, both applicable: a new gate that lands without its
    wiring reds `check-gate-enforcement.sh` on a DIFFERENT gate; and a merge-time truth checked by a
    PR-time gate passes on the PR and reds main at merge — so after wiring, confirm the step will
    actually run on this branch's events rather than only asserting that the script works.
  </action>
  <verify>
    <automated>bash scripts/check-pentest-triage.sh; echo "rc=$?"</automated>
    <automated>bash scripts/check-gate-enforcement.sh; echo "rc=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-pentest-triage.sh` exits 0 and prints all 11 IDs as found, plus the observed row count.
    - `bash scripts/check-gate-enforcement.sh` exits 0 and reports a gate count one higher than before this task (record both numbers).
    - FAIL DIRECTION 1 (completeness): delete one disposition row, confirm exit 1 NAMING that ID, restore, verify by `git hash-object` == `git rev-parse HEAD:<path>` (or the pre-edit hash if the doc is not yet committed — record which baseline was used), re-run 0. Clean -> break -> restore -> clean, all four recorded.
    - FAIL DIRECTION 2 (denominator): add a twelfth row for a non-existent ID and confirm exit 1 rather than 0. Restore and re-run.
    - FAIL DIRECTION 3 (vocabulary): blank one row's disposition cell while leaving the ID in place, and confirm exit 1. This is the arm that distinguishes "mentioned" from "dispositioned". Restore and re-run.
    - FAIL DIRECTION 4 (fail-closed): rename the triage doc, confirm exit 2 (VOID) not 0, and restore. "I could not find the doc" must never read as "the doc is fine".
    - SELF-MATCH GUARD: assert by source that the scan target is an absolute path to `docs/security/PENTEST-TRIAGE.md` and that no repo-wide glob appears. Prove it: temporarily move the triage doc aside and confirm the gate VOIDs rather than finding its own eleven IDs in its own source.
    - The `ci-cd.yaml` step exists in the `ops-contracts` job and is committed in the SAME commit as the script.
    - The script is NOT added to `scripts/gates/gate-enforcement.conf`: `grep -c check-pentest-triage scripts/gates/gate-enforcement.conf` is 0.
  </acceptance_criteria>
  <done>Removing, blanking or losing a disposition row fails a CI gate that names the ID, the gate cannot pass over its own source, and script and wiring landed together.</done>
</task>

<task type="auto">
  <name>Task 3: E1's live rejection arm, the realm cleanup, and the issue closures</name>
  <files>infra/keycloak/realm-export.template.json</files>
  <read_first>
    - infra/keycloak/realm-export.template.json (the clients array — specifically `test-client` and the `${...}` secret placeholders)
    - infra/keycloak/README.md (the re-import procedure — `kc.sh import --override true` plus a restart; the realm is Postgres-backed so dropping the volume is a NO-OP)
    - core-java/src/main/java/uk/jtoye/core/security/AudienceValidator.java:30-50 (what rejects a token whose `aud` lacks the expected audience)
    - core-java/src/main/java/uk/jtoye/core/security/CustomerJwtVerifier.java:135-150 (why storefront tokens deliberately carry no core-api audience)
    - docs/security/PENTEST-TRIAGE.md as written in Task 1
    - the issues: `gh issue view 548`, `gh issue view 549`, `gh issue view 551`
  </read_first>
  <action>
    Run #551's second acceptance criterion as an actual measurement, not a config read: request a
    token for the PUBLIC `test-client` from the running Keycloak (no secret is needed — that is the
    E1 symptom), present it to a core-api endpoint that requires authentication, and record the
    actual status. Expect a 401 from `AudienceValidator`. Then run the positive control in the same
    session: present a token that DOES carry the expected audience to the SAME endpoint and record a
    non-401. Without the control, a 401 is indistinguishable from a service that rejects everything
    — the endpoint must be shown to discriminate, exactly as `check-infra-exposure.sh`'s C3 arm
    insists for Grafana. Record both statuses in the triage doc's E1 section. Record no token value.

    Then apply D-12's fix half to the realm TEMPLATE: `test-client` is an unused public client in a
    committed export, and #551's ask is binary — remove it, or write down why it stays. Take the
    decision, apply it to `infra/keycloak/realm-export.template.json`, and record the reasoning in
    the triage doc. Do NOT run the Keycloak re-import here: the realm is Postgres-backed, the change
    reaches the running realm only via `kc.sh import --override true` plus a restart, and D-02's
    rotation needs the SAME import. Plan 28-10 performs one import carrying both payloads. Say so in
    the SUMMARY, and make sure the template edit is the ONLY realm change this plan makes so 28-10
    inherits a clean single import. If the audit surfaces anything beyond `test-client` that is
    non-trivial to fix, file it as a sanitized follow-up issue rather than widening this plan.

    Close the issues that this phase has now discharged, each closing comment pointing at
    `docs/security/PENTEST-TRIAGE.md`:
      * #549 — C3 is fixed by #442 and 28-02 Task 2 supplied the observed break arm.
      * #551 — E1 audited, the client table recorded, the live 401 arm run with its control, the
        template decision applied.
      * #548 — the tracking issue: every one of the 11 findings now has a disposition. Say
        explicitly in the comment that B1 remains OPEN under #552 and that an open finding tracked by
        a sanitized issue is a completed triage, so a reader does not mistake the closure for a claim
        that everything is fixed.
    Do NOT close #552, #283, #284, #270, #281 or #488 here — those close in their own plans, or in
    28-11. Pass every issue body and comment through a quoted heredoc or `-F <file>`, never an
    interpolating double-quoted string: backticks inside `"..."` execute and silently drop the
    phrases they were meant to quote. Read each comment back after posting.
  </action>
  <verify>
    <automated>bash scripts/check-pentest-triage.sh; echo "rc=$?"</automated>
    <automated>jq -e '.clients | length' infra/keycloak/realm-export.template.json</automated>
  </verify>
  <acceptance_criteria>
    - The triage doc's E1 section records BOTH statuses from the live arm: the `test-client` token's status against a protected core-api endpoint, and the control token's status against the SAME endpoint. A 401 recorded without its control is not acceptable.
    - No token value, client secret or credential appears in the triage doc, in any issue comment, or in this plan's SUMMARY. Verified by grep with a positive control proving the grep can match.
    - `infra/keycloak/realm-export.template.json` remains valid JSON: `jq -e . infra/keycloak/realm-export.template.json` exits 0. FAIL DIRECTION: confirm `jq -e` exits non-zero on a deliberately corrupted copy, so the pass is evidence about validity rather than about jq being lenient.
    - The `test-client` decision is applied to the template AND its reasoning is written in the triage doc. If the decision is "remove", `jq '[.clients[].clientId]'` no longer lists it; if "keep", the doc states why in a sentence a reviewer can disagree with.
    - No Keycloak re-import is performed by this plan: the SUMMARY states that the running realm still carries the old template and that plan 28-10 performs the single import carrying both D-02 and D-12 payloads.
    - `gh issue view 549`, `gh issue view 551` and `gh issue view 548` all show state CLOSED, each with a comment referencing `docs/security/PENTEST-TRIAGE.md`. #548's comment states that B1 remains open under #552.
    - `gh issue view 552` shows state OPEN — assert it, so a stray closure keyword cannot pass unnoticed. Any issue number written in prose must be placed BEFORE any closing keyword, because GitHub's keyword parser is lexical and "does not close #N" still closes #N.
    - `bash scripts/check-pentest-triage.sh` still exits 0 after the doc edits.
  </acceptance_criteria>
  <done>E1 is proven by an actual token and an actual 401 with a discriminating control, the realm template carries the D-12 decision for 28-10's single import, and #548/#549/#551 are closed pointing at the tracked record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private pentest report -> PUBLIC repository | the sanitization boundary this entire plan writes across |
| any Keycloak realm client -> core-api's expected audience | who may mint a token the resource server will accept |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-19 | Information Disclosure | the triage doc, the issue bodies and the SUMMARY, all public | mitigate | findings by ID only; no literal values, chain or payload; the sanitization claim is proven by greps shown able to match, not asserted |
| T-28-20 | Spoofing | a realm client able to mint the `core-api` audience unintentionally (E1) | mitigate | full client table regenerated with `jq`; the unused PUBLIC `test-client` decided and applied to the template; live 401 arm with a discriminating control |
| T-28-21 | Spoofing | the indirect `oidc-audience-resolve-mapper` path widening the audience silently | accept, disclosed with its measurement | core-api declares zero client roles and no seeded user has a client-role mapping, so the path is inert today; the mechanism is written into the triage doc so the consequence is known before someone adds a client role |
| T-28-22 | Repudiation | a disposition record that can be silently emptied or truncated | mitigate | the completeness gate asserts IDs, a row denominator AND a status vocabulary, with four independent break arms including a fail-closed VOID |
| T-28-23 | Tampering | the gate passing by matching its own source | mitigate | absolute-path scan scoped to the triage doc, proven by moving the doc aside and observing a VOID |

Cross-cutting dimensions: web-perf **N/A**; SEO **N/A**; agent-readiness **N/A** (no API surface change —
the E1 arm only exercises existing endpoints). Falsifiable evidence: four break arms on the gate, a
positive control on the live 401, a fail direction on the JSON-validity check, and a controlled grep
behind every sanitization claim.
</threat_model>

<verification>
- `bash scripts/check-pentest-triage.sh` rc=0; four break arms recorded rc=1/rc=1/rc=1/rc=2.
- `bash scripts/check-gate-enforcement.sh` rc=0 with the gate count incremented and recorded.
- `jq -e . infra/keycloak/realm-export.template.json` rc=0, with its fail direction shown.
- #548, #549, #551 CLOSED; #552 asserted still OPEN.
</verification>

<success_criteria>
- SEC-02 satisfied: 11 of 11 findings dispositioned in a tracked, sanitized, gated document.
- The gate reds on a removed row, a spurious row, a blanked disposition, and a missing file.
- E1 is settled by measurement — a client table regenerated with jq, and a live token producing an actual 401 beside a control that does not.
- The realm template carries D-12's fix, staged for the single import in 28-10.
</success_criteria>

<output>
Create `.planning/phases/28-security-triage-the-dev-prod-boundary/28-05-SUMMARY.md` when done.
Record: the 11 dispositions in one table; all four gate break arms with real rc values; the gate
count before and after; the live E1 statuses with the control; the `test-client` decision and its
reasoning; and the explicit note that no Keycloak import ran, so 28-10 owns the single import.
</output>
