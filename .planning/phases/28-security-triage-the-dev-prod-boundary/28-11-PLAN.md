---
phase: 28-security-triage-the-dev-prod-boundary
plan: 11
type: execute
wave: 7
depends_on: ["28-01", "28-02", "28-03", "28-04", "28-05", "28-06", "28-07", "28-08", "28-09", "28-10"]
files_modified:
  - docs/metrics.json
  - CLAUDE.md
  - AGENTS.md
  - README.md
  - docs/security/PENTEST-TRIAGE.md
  - docs/CHANGELOG.md
  - HANDOFF.md
autonomous: true
requirements: [SEC-01, SEC-02, SEC-03, SEC-04]

must_haves:
  truths:
    - "The test-count manifest and every prose count that quotes it agree, and both gates pass"
    - "Every finding's disposition reflects what this phase actually shipped, including the ones it left open"
    - "Every gate script in the tree runs somewhere, and the count quoted to the next session equals the checker's own measurement"
    - "The delivered runtime matches the branch, and the branch is not behind its base"
  artifacts:
    - path: "docs/metrics.json"
      provides: "regenerated by script, never by arithmetic"
    - path: "docs/security/PENTEST-TRIAGE.md"
      provides: "final dispositions, with B1 upgraded to what actually shipped"
  key_links:
    - from: "docs/metrics.json"
      to: "the prose counts in CLAUDE.md / AGENTS.md / README.md"
      via: "scripts/check-doc-metrics.sh"
      pattern: "total_logical_invocations"
    - from: "HANDOFF.md resume block"
      to: "the real gate count"
      via: "ls scripts/check-*.sh scripts/docs-freshness.sh | wc -l"
      pattern: "check-\\*\\.sh"
---

<objective>
Close the phase: reconcile the test-count manifest that ten plans deliberately left red, finalise
the dispositions, prove every gate runs and every count quoted is the checker's own measurement,
and prove the delivered runtime matches the branch.

Purpose: this plan owns the phase's DESIGNED red. Every earlier plan added tests and was explicitly
forbidden from touching `docs/metrics.json` or the prose counts, because two same-wave plans both
regenerating the manifest would collide, and because arithmetic on that file is wrong
(`trap_docs_freshness_block_counter`). That is the 33-07 precedent applied deliberately: one plan
owns the manifest, states the red in advance, and closes it.

Three counts in this repository have gone stale in exactly this position before — the test-count
manifest, the gate count in the HANDOFF resume block, and the phase's own claims about the runtime.
Each is re-measured here rather than carried forward.

Output: green doc gates, a final triage record, a full gate sweep, a CHANGELOG entry, an accurate
HANDOFF, and a runtime proven to match the branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-CONTEXT.md
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-RESEARCH.md
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-01-SUMMARY.md
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-05-SUMMARY.md
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-08-SUMMARY.md
@.planning/phases/28-security-triage-the-dev-prod-boundary/28-10-SUMMARY.md

<interfaces>
Manifest bookkeeping — NEVER arithmetic:
  bash scripts/docs-freshness.sh --write     # regenerates docs/metrics.json from the SOURCE TREE
  # then hand-update the prose counts in CLAUDE.md, AGENTS.md, README.md — the script does not touch prose
  bash scripts/docs-freshness.sh             # gate 1: source tree -> docs/metrics.json
  bash scripts/check-doc-metrics.sh          # gate 2: prose -> docs/metrics.json
  Both fail the build on drift. Gate 2 exists because gate 1 never opened a doc: README sat at 921
  for months while the tree was at 1895, and gate 1 was green on every one of those commits.
  Baseline at phase start: docs/metrics.json total_logical_invocations = 2769 (re-measure, do not assume).

Gate count — quote the CHECKER's own measurement, never a remembered number:
  ls scripts/check-*.sh scripts/docs-freshness.sh | wc -l     # was 34 at phase start
  This phase adds two: check-media-content-types.sh (plan 28-03) and check-pentest-triage.sh (plan 28-05).
  The HANDOFF resume block's "EXPECT N x rc=0" promise went RED in Phase 33 for exactly this reason —
  the phase added two gates and the claim still said the old number. Carry the derivation, not the digit.

Standing gate quirks that are NOT this phase's defects — recognise them rather than investigating:
  check-alert-metrics    reds after ANY core-java recreate; the documented remedy is
                         `scripts/seed-order-metric.sh`, not an investigation
  check-e2e-skip-budget  VOIDs once per merge because a merge refreshes frontend/e2e's mtime; it is a
                         staleness detector, and the report is re-earned by re-running the suite

Runtime + branch parity, both fail CLOSED at exit 2:
  bash scripts/check-runtime-freshness.sh    # per service: .Metadata.LastTagTime vs the newest commit
                                             # touching that service's build paths, AND the running
                                             # container's image ID vs the tag's. ANY built service
                                             # missing or not running VOIDs the whole run.
  bash scripts/check-branch-behind-base.sh   # git log HEAD..origin/<default> must be empty
  Run BOTH from the MAIN checkout, not a worktree: compose derives its project name from the
  directory, so a worktree makes the runtime gate VOID.

Changelog:
  docs/CHANGELOG.md plus scripts/check-changelog-contract.sh / check-changelog-cites-pr.sh.
  Rebase-merge STRIPS "(#NNN)" from commit subjects and the contract gate can then only VOID — on the
  NEXT unrelated PR, because the PR-time run is green by construction. Prefer a SQUASH merge with an
  explicit `(#NNN)` subject. On a CHANGELOG conflict, "take theirs" is right for the regenerated docs
  and silently DELETES your own entry in docs/CHANGELOG.md — no gate sees it until after merge.

Dispositions to finalise (read the SUMMARYs, do not re-derive):
  B1  -> upgrade from OPEN/#552 to what actually shipped in 28-07/28-08/28-10, including the owner's
         confirmed credential list BY NAME from 28-10 Task 1
  A1  -> 28-01's CONFIRMED/FALSIFIED verdict (already written by 28-05; verify it still matches)
  the jtoye_backup default-privileges issue (28-07) -> fill 28-05's named placeholder row
  #281 -> 28-04; #488 -> 28-03; #270 + DEC-5 -> 28-09; #283/#284 -> 28-06
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the test-count manifest and the prose that quotes it</name>
  <files>docs/metrics.json, CLAUDE.md, AGENTS.md, README.md</files>
  <read_first>
    - docs/metrics.json (the current values, so the delta is observed rather than assumed)
    - scripts/docs-freshness.sh (its `--write` behaviour and what it counts — it greps LITERAL block openers, which is why arithmetic on it is wrong)
    - scripts/check-doc-metrics.sh (which prose claims in which files it validates)
    - CLAUDE.md § Constraints (the "2769 logical invocations" sentence and its breakdown by language)
    - AGENTS.md and README.md (the same counts, wherever they appear)
  </read_first>
  <action>
    Record `docs/metrics.json`'s current values FIRST, then run `bash scripts/docs-freshness.sh
    --write` to regenerate from the source tree, then record the new values. Never compute the delta
    by hand and never edit the JSON directly: the script greps literal block openers, so a
    hand-computed number is wrong in ways that look right.

    The script does not touch prose. Update the counts in `CLAUDE.md`, `AGENTS.md` and `README.md`
    to match the regenerated manifest, including the per-language breakdown wherever it appears —
    Java `@Test` methods and their file count, Jest blocks, Go `Test*` funcs, Playwright `test()`
    blocks, and the mcp-server vitest blocks. Gate 2 exists precisely because gate 1 never opens a
    doc, and a prose number can be stale for months with gate 1 green the whole time.

    Then run BOTH gates and require rc=0 from each — they check opposite halves of the loop
    (source tree -> manifest, and prose -> manifest) and one passing tells you nothing about the other.

    State in the SUMMARY that this red was DESIGNED and carried across ten plans by instruction, so a
    reader of the phase's git history does not read the intervening reds as a regression that went
    unnoticed.
  </action>
  <verify>
    <automated>bash scripts/docs-freshness.sh; echo "rc=$?"</automated>
    <automated>bash scripts/check-doc-metrics.sh; echo "rc=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/docs-freshness.sh` exits 0 and `bash scripts/check-doc-metrics.sh` exits 0. Both rc values recorded, captured on the same statement as their command (never after an intervening `echo`, which reports the echo's status).
    - `docs/metrics.json`'s `total_logical_invocations` before and after are both recorded, and the delta equals the number of test blocks this phase added. If it does not, that discrepancy is the finding — investigate before closing, because it means something was counted that no plan reports adding, or a test was lost.
    - The prose counts in CLAUDE.md, AGENTS.md and README.md match the regenerated manifest. FAIL DIRECTION: change one prose number by one and confirm `check-doc-metrics.sh` exits non-zero naming that claim; restore and re-run 0. A gate observed only passing is not evidence.
    - `docs/metrics.json` was regenerated by the script, not hand-edited: assert that the file's structure and any generated timestamp field are script-shaped.
  </acceptance_criteria>
  <done>Both halves of the doc-metrics loop are green, the delta is explained, and the fail direction was observed.</done>
</task>

<task type="auto">
  <name>Task 2: Finalise the dispositions, close the issues this phase actually closed, and sweep the gates</name>
  <files>docs/security/PENTEST-TRIAGE.md, HANDOFF.md</files>
  <read_first>
    - docs/security/PENTEST-TRIAGE.md (as written by plan 28-05 — the row contract, the vocabulary, and the named placeholder row)
    - scripts/check-pentest-triage.sh (the contract you must not break while editing the doc)
    - every 28-NN-SUMMARY.md in this phase directory (the dispositions you are transcribing)
    - HANDOFF.md (the resume block and its gate-count claim, plus its derivation comment)
    - scripts/check-gate-enforcement.sh and scripts/gates/gate-enforcement.conf
    - .planning/ROADMAP.md § Phase 28 (the four success criteria you are reporting against)
  </read_first>
  <action>
    Update `docs/security/PENTEST-TRIAGE.md` to reflect what shipped. Upgrade B1's row from OPEN to
    what 28-07, 28-08 and 28-10 actually delivered, including the owner-confirmed credential list BY
    NAME (never a value) from 28-10 Task 1. Fill the named placeholder row 28-05 left for the
    `jtoye_backup` default-privileges issue. Re-verify every other row against its SUMMARY rather
    than assuming 28-05 got it right — a transcription written before four plans finished is exactly
    the kind of claim that goes stale in place.

    Be precise about what did NOT close, and make it visible rather than implied: the deferred
    full-catalogue EXIF/WebP sweep (dated plan from 28-03), and anything 28-04's STOMP measurement
    filed. State the residuals with their numbers — the SSE idle-connection window and the
    cross-replica revocation latency are both 5 minutes and both belong in writing.

    Re-run `bash scripts/check-pentest-triage.sh` after every edit; the doc and the gate agree only
    as long as the row contract is honoured.

    Then close the issues this phase genuinely closed, each comment pointing at the tracked record
    and quoting the measurement that settled it: #552, #283, #284, #270, #281, #488 as applicable —
    check each SUMMARY for whether the plan already closed it, and do not double-post. Where a plan
    filed a follow-up instead of fixing, say so in the closing comment rather than implying
    completeness. Put any issue number BEFORE a closing keyword in prose; GitHub's parser is lexical
    and "does not close #N" still closes #N. Pass every body through a quoted heredoc or `-F <file>`,
    never an interpolating string, and read the stored text back.

    Then sweep every gate: run all `scripts/check-*.sh` plus `scripts/docs-freshness.sh` and record
    each rc. Two known-standing behaviours are NOT this phase's defects — `check-alert-metrics` reds
    after any core-java recreate and its documented remedy is `scripts/seed-order-metric.sh`, and
    `check-e2e-skip-budget` VOIDs once per merge because it is a staleness detector. Apply the
    documented remedy for the first; re-earn the report for the second by re-running the suite rather
    than touching the file.

    Finally, update `HANDOFF.md`'s resume block with the gate count MEASURED right now —
    `ls scripts/check-*.sh scripts/docs-freshness.sh | wc -l` — not a remembered number, and carry
    the derivation in the comment so the next bump is mechanical. This claim went red in Phase 33 for
    exactly this reason. Re-run every other measurement the resume block quotes before repeating it.
  </action>
  <verify>
    <automated>bash scripts/check-pentest-triage.sh; echo "rc=$?"</automated>
    <automated>bash scripts/check-gate-enforcement.sh; echo "rc=$?"</automated>
    <automated>ls scripts/check-*.sh scripts/docs-freshness.sh | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-pentest-triage.sh` exits 0 after all edits, and the row count is still exactly 11.
    - `bash scripts/check-gate-enforcement.sh` exits 0.
    - Every `scripts/check-*.sh` plus `docs-freshness.sh` was run and its rc recorded in a table. Any non-zero is either (a) one of the two documented standing behaviours with its remedy applied and the re-run recorded, or (b) named as an open defect with an issue. "Not run" is not an acceptable cell.
    - B1's row names the owner-confirmed credential list BY KEY NAME and contains no value.
    - The `jtoye_backup` placeholder row is filled with its issue number.
    - The residuals are stated with numbers: the SSE idle window (5 minutes) and the cross-replica revocation latency (5 minutes), and the deferred media sweep with its dated plan path.
    - `HANDOFF.md`'s gate count equals the live `ls ... | wc -l` output — record the command's output and the value written, and confirm they are identical. FAIL DIRECTION: confirm the two would differ if the old number were kept, by recording the phase-start value (34) alongside.
    - Every issue closed by this task shows state CLOSED with a comment referencing `docs/security/PENTEST-TRIAGE.md`. Assert per issue; do not assert in bulk.
  </acceptance_criteria>
  <done>The triage record matches what shipped including what did not, every gate in the tree was run with its rc recorded, and the HANDOFF's gate count is the checker's own live measurement.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the delivered runtime matches the branch, and record the phase</name>
  <files>docs/CHANGELOG.md</files>
  <read_first>
    - scripts/check-runtime-freshness.sh (its per-service contract and every condition that yields exit 2)
    - scripts/check-branch-behind-base.sh (how it resolves the base from the remote rather than hardcoding it)
    - docs/CHANGELOG.md (the entry format, and the most recent entries as models)
    - scripts/gates/changelog-contract.conf (the FLOOR note and the EXEMPT mechanism, including the 2026-08-09 note about unattributable subjects)
    - scripts/check-changelog-contract.sh and scripts/check-changelog-cites-pr.sh
  </read_first>
  <action>
    Rebuild the stack and prove parity by CONTENT and IDENTITY, not by status code. An HTTP 200, a
    rendered page and a green suite are identical whether the running code is current or months
    stale — this project has shipped a runtime missing its own `application.yml` change past four
    green gates, and the owner caught it by eye. Run `docker compose ... up -d --build` (add
    `--force-recreate` if any container is still on an older image ID than its tag), then
    `bash scripts/check-runtime-freshness.sh` and require rc=0 with every built service FRESH. Run it
    from the MAIN checkout, never a worktree: compose derives its project name from the directory, so
    a worktree VOIDs the gate for a reason that has nothing to do with the runtime.

    Read this phase's own change out of the running artifact rather than off the branch. The
    Flyway-credential indirection lives in `application.yml`, which is packaged INSIDE the fat jar —
    read it from inside the archive (`unzip -p /app/app.jar BOOT-INF/classes/application.yml`), not
    with a filesystem `find`, which returns a misleading 0.

    Run `bash scripts/check-branch-behind-base.sh` and require rc=0. A branch behind its base ships a
    runtime missing already-merged work, and no rebuild can fix that.

    Run the FULL suite one more time — `./gradlew :core-java:test :core-java:integrationTest` — on
    the final tree. This phase changed authorisation, the database role and the SSE delivery path;
    every one of those has a recorded history of breaking tests that the touching change never ran.

    Write the `docs/CHANGELOG.md` entry. Read `scripts/gates/changelog-contract.conf` first,
    including its FLOOR note, so the entry satisfies the contract rather than being retrofitted.
    Prefer a SQUASH merge with an explicit `(#NNN)` subject: rebase-merge strips the PR number from
    every subject, and `check-changelog-contract.sh` can then only VOID — on the next unrelated PR,
    because the PR-time run is green by construction and the symptom appears later on somebody else's
    branch. If a CHANGELOG conflict arises, note that "take theirs" is correct for the regenerated
    docs and silently DELETES your own entry in `docs/CHANGELOG.md`, which no gate catches until
    after the merge.

    Finally, report the phase against the ROADMAP's four success criteria. For each, state satisfied
    or not and name the measurement — including where the criterion as written could not fail and was
    replaced with a stronger form. SC-4's loopback half was already satisfied before this phase began
    and its rotation half is what shipped; SC-3 was re-stated against the BUILT document because a
    source or snapshot grep is a false red. Record both substitutions explicitly. Never report a
    vacuous pass as satisfied.
  </action>
  <verify>
    <automated>bash scripts/check-runtime-freshness.sh; echo "rc=$?"</automated>
    <automated>bash scripts/check-branch-behind-base.sh; echo "rc=$?"</automated>
    <automated>cd core-java && ./gradlew :core-java:test :core-java:integrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/check-runtime-freshness.sh` exits 0 with EVERY built service reported FRESH. FAIL DIRECTION: stop one built service and confirm exit 2 naming it — a per-service VOID, not a pass with an unverified service inside it. Restart and re-confirm 0.
    - `bash scripts/check-branch-behind-base.sh` exits 0. FAIL DIRECTION: confirm the gate is capable of failing by checking it against a deliberately stale ref, and record the output.
    - The Flyway-credential indirection is read OUT OF the running jar (`unzip -p ... BOOT-INF/classes/application.yml`) and the matching line is quoted. A filesystem `find` result is not acceptable evidence.
    - `./gradlew :core-java:test :core-java:integrationTest` GREEN on the final tree, with both test counts recorded and compared against the phase-start counts. A drop in either is a regression by omission and fails this criterion even on a green run.
    - `docs/CHANGELOG.md` has an entry for this phase, and `bash scripts/check-changelog-contract.sh` plus `bash scripts/check-changelog-cites-pr.sh` both exit 0 (or their VOID conditions are recorded with the reason).
    - The SUMMARY reports all four ROADMAP success criteria with a verdict and a named measurement each, and explicitly records the two criteria that were RESTATED (SC-3's artifact, SC-4's already-satisfied half) rather than reporting either as a plain pass.
    - Both parity gates were run from the main checkout, not a worktree — state which directory.
  </acceptance_criteria>
  <done>The runtime is proven current by content read out of the running artifact, the branch is not behind its base, the full suite is green with no count regression, and the phase is reported against its criteria including the two that were restated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the branch -> the delivered runtime | a stack that starts cleanly on stale images looks identical to a current one |
| the phase's claims -> the next session | a stale count in HANDOFF sends the next session to a false conclusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-57 | Tampering | shipping a runtime missing this phase's own changes | mitigate | rebuild with `--build`, `check-runtime-freshness.sh` rc=0 per service, and the changed value read from INSIDE the fat jar rather than off the filesystem |
| T-28-58 | Tampering | a branch behind its base shipping a runtime missing already-merged work | mitigate | `check-branch-behind-base.sh` rc=0, with its fail direction shown |
| T-28-59 | Repudiation | a security phase reporting criteria that could not fail as satisfied | mitigate | both restated criteria (SC-3's artifact, SC-4's already-satisfied half) recorded explicitly rather than reported as plain passes |
| T-28-60 | Repudiation | a stale gate count or test count quoted forward to the next session | mitigate | every count re-measured with the checker's own command, and the derivation carried in the comment |
| T-28-61 | Information Disclosure | a credential value reaching the triage doc, CHANGELOG or HANDOFF at close-out | mitigate | B1's row names KEYS only; the value-grep from plan 28-10 is re-run across the final tree |
| T-28-62 | Tampering | a CHANGELOG entry silently deleted by a "take theirs" conflict resolution | mitigate | the hazard is named in the action, and both changelog gates are run after the entry lands |

Cross-cutting dimensions: web-perf **N/A**; SEO **N/A**; agent-readiness **N/A**. Falsifiable evidence:
fail directions on the doc-metrics gate, the runtime gate, the branch gate, and the HANDOFF count.
Runtime parity: this plan is where the phase's parity claim is made and proven.
</threat_model>

<verification>
- `bash scripts/docs-freshness.sh` and `bash scripts/check-doc-metrics.sh` both rc=0, with a fail direction shown on the second.
- `bash scripts/check-pentest-triage.sh` rc=0 with 11 rows; `bash scripts/check-gate-enforcement.sh` rc=0.
- Every `check-*.sh` swept with its rc recorded in a table.
- `bash scripts/check-runtime-freshness.sh` rc=0 (rc=2 with a service stopped); `bash scripts/check-branch-behind-base.sh` rc=0.
- `./gradlew :core-java:test :core-java:integrationTest` green, counts compared to phase start.
- `HANDOFF.md`'s gate count equals the live measurement.
</verification>

<success_criteria>
- The designed doc-metrics red is closed and both halves of the loop pass.
- The triage record states what shipped and what did not, with numbers.
- Every gate in the tree ran, and the count quoted forward is the checker's own.
- The runtime is proven current by content, the branch is current with its base, and the four ROADMAP criteria are reported with their measurements — including the two that were restated.
</success_criteria>

<output>
Create `.planning/phases/28-security-triage-the-dev-prod-boundary/28-11-SUMMARY.md` when done.
Record: the manifest before/after with the explained delta and the doc-gate fail direction; the full
gate sweep table with every rc; the live gate count and the value written to HANDOFF; the runtime
and branch gate results with their fail directions; the value read out of the running jar; the full
suite counts against phase start; and the four ROADMAP criteria with their verdicts and the two
restatements named.
</output>
