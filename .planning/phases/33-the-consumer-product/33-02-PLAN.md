---
phase: 33-the-consumer-product
plan: 02
type: execute
wave: 3
depends_on: [33-01]
files_modified:
  - core-java/src/main/resources/db/migration/V61__postcode_centroid.sql
  - core-java/src/main/java/uk/jtoye/core/geo/PostcodeCentroid.java
  - core-java/src/main/java/uk/jtoye/core/geo/PostcodeCentroidRepository.java
  - core-java/src/main/java/uk/jtoye/core/geo/PostcodeCentroidImporter.java
  - core-java/src/main/java/uk/jtoye/core/geo/PostcodeGeocoder.java
  - core-java/src/main/java/uk/jtoye/core/geo/GeoBounds.java
  - core-java/src/main/resources/application.yml
  - core-java/src/test/resources/application-test.yml
  - core-java/src/test/resources/geo/postcode-centroids-fixture.csv
  - core-java/src/test/java/uk/jtoye/core/geo/PostcodeGeocoderTest.java
  - core-java/src/test/java/uk/jtoye/core/geo/GeoBoundsTest.java
  - core-java/src/test/java/uk/jtoye/core/geo/PostcodeCentroidImportIntegrationTest.java
  - core-java/src/test/java/uk/jtoye/core/security/RlsContractTest.java
  - scripts/check-no-create-extension.sh
  - .github/workflows/ci-cd.yaml
autonomous: true
requirements: [CUST-01]
must_haves:
  truths:
    - "A UK postcode resolves to a WGS84 coordinate offline, with no API key and no runtime network call"
    - "An unknown postcode resolves to empty, never to (0,0) and never by throwing"
    - "The importer is ENABLED BY DEFAULT in dev, so the substrate is populated rather than silently empty"
    - "No postcode in the loaded table sits at Null Island"
    - "No migration in this repo can introduce a CREATE EXTENSION that the Flyway role cannot execute"
  artifacts:
    - path: "core-java/src/main/resources/db/migration/V61__postcode_centroid.sql"
      provides: "postcode_centroid DDL + PK, and the shops(latitude, longitude) btree the distance prefilter needs"
      contains: "CREATE TABLE"
    - path: "core-java/src/main/java/uk/jtoye/core/geo/PostcodeGeocoder.java"
      provides: "The single postcode->coordinate implementation, shared by the API write path and the dev seeder"
      exports: ["locate"]
    - path: "core-java/src/main/java/uk/jtoye/core/geo/GeoBounds.java"
      provides: "radius -> leakproof lat/lon bounding box, pole-guarded"
      exports: ["boxAround"]
    - path: "scripts/check-no-create-extension.sh"
      provides: "Gate enforcing the no-CREATE-EXTENSION invariant that was previously an unchecked claim"
  key_links:
    - from: "core-java/src/main/java/uk/jtoye/core/geo/PostcodeGeocoder.java"
      to: "postcode_centroid"
      via: "PostcodeCentroidRepository primary-key lookup"
      pattern: "PostcodeCentroidRepository"
    - from: "core-java/src/main/resources/application.yml"
      to: "33-05 backfill flag and 33-06 radius keys"
      via: "this plan is the SOLE owner of the jtoye.geo.* block"
      pattern: "geo:"
---

<objective>
The schema and the Java surface: `postcode_centroid`, an importer that is actually on, the single
geocoder both write paths share, and the bounding-box helper the distance query needs.

**REVISED (plan-checker W5):** split out of the former single substrate plan, which carried 21
`files_modified`. `33-01` owns the data artefact and the licence; this plan owns the schema and the
code. Wave 4 depends only on this plan's Java surface.

**REVISED (plan-checker B3):** the importer's **dev default is now stated and asserted**. The previous
version gated it behind `jtoye.geo.postcode-import.enabled` without saying what the default was. If it
had defaulted off, `postcode_centroid` would be empty, every coordinate would stay NULL, and the
located row would silently degrade to "nothing within radius" while every verify in the phase stayed
green — a vacuous pass on the phase's headline claim.

**REVISED (plan-checker W10):** "V61 contains no `CREATE EXTENSION`" was a success criterion with no
check anywhere. It now has a wired gate.

**This plan is the SOLE owner of `application.yml` in this phase.** Declare the WHOLE `jtoye.geo.*`
block here — the import enable flag and its dev default, the coordinate-backfill flag `33-05` reads,
and the default and maximum search radius `33-06` reads. Those two plans share a wave and must not
both edit this file; they read these keys and do not add them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-the-consumer-product/CONTEXT.md
@.planning/phases/33-the-consumer-product/RESEARCH.md
@.planning/phases/33-the-consumer-product/33-CONTROL-ARMS.md
@.planning/phases/33-the-consumer-product/33-01-SUMMARY.md
@core-java/src/test/java/uk/jtoye/core/security/RlsContractTest.java
@core-java/src/main/resources/db/migration/V60__media_quarantine_durability.sql

<interfaces>
<!-- WHY NO EXTENSION. Measured on the live stack, in a rolled-back transaction:
       cube          1.5  trusted=t  superuser=t
       earthdistance 1.1  trusted=f  superuser=t
     Flyway runs as `${spring.datasource.username}` = DB_USER = `jtoye_app`, which is
     rolsuper=f, rolbypassrls=f, and has_database_privilege('jtoye_app','jtoye','CREATE') = false.
       SET ROLE jtoye_app; CREATE EXTENSION IF NOT EXISTS cube;
       ERROR:  permission denied to create extension "cube"
     So even the TRUSTED extension fails. Granting `jtoye_app` CREATE ON DATABASE would be a
     privilege escalation on the RLS-bound role — do NOT. PostGIS is absent entirely
     (pg_available_extensions -> 0 rows). Plain SQL is the answer, not a fallback. -->

Existing RLS exemption precedent to copy — `RlsContractTest.EXEMPT_TABLES` already carries
`flyway_schema_history`, `processed_stripe_events` and `tenants`, each with a written justification,
under the standing instruction "DO NOT weaken the assertion below".

Postcode key shape from 33-01: SPACE-STRIPPED, UPPERCASED. Field length varies 6/7/8 upstream, so a
padding-based parser mis-keys.

`SOURCE.md` (33-01) records the exact post-filter row count. The importer asserts against that number.

SHELL IDIOM — REQUIRED. `grep -c` exits **1** on a zero count, i.e. on the DESIRED state of an
absence check, and `cmd | grep -q X` under `pipefail` INVERTS on match. This repo's hook blocks both.
Always: `out=$(grep -c X f || true); grep -qx 0 <<< "$out"`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: V61 DDL, the geocoder, and the bounding box</name>
  <files>core-java/src/main/resources/db/migration/V61__postcode_centroid.sql, core-java/src/main/java/uk/jtoye/core/geo/PostcodeCentroid.java, core-java/src/main/java/uk/jtoye/core/geo/PostcodeCentroidRepository.java, core-java/src/main/java/uk/jtoye/core/geo/PostcodeGeocoder.java, core-java/src/main/java/uk/jtoye/core/geo/GeoBounds.java, core-java/src/test/resources/geo/postcode-centroids-fixture.csv, core-java/src/test/java/uk/jtoye/core/geo/PostcodeGeocoderTest.java, core-java/src/test/java/uk/jtoye/core/geo/GeoBoundsTest.java</files>
  <behavior>
    - `PostcodeGeocoder.locate("48 Rye Lane, Peckham, London SE15 5BS")` returns a coordinate within 10 m of the ONSPD reference for SE15 5BS.
    - `locate("12 Bellenden Road, Peckham, London SE15 4QA")` returns **empty** — the regex extracts `SE154QA`, the table rejects it. Empty, never (0,0), never an exception.
    - `locate("1 Probe Lane, London")` returns empty — no extractable postcode.
    - `locate("—")` and `locate(null)` return empty without throwing.
    - Extraction is case- and space-insensitive: `"…se15 5bs"`, `"…SE155BS"` and `"…SE15  5BS"` all normalise to one key and return the same coordinate.
    - `GeoBounds.boxAround(lat, lon, r)` fully contains the circle: a point at exactly `r` due north is inside the box; a point at `2r` due north is outside.
    - `GeoBounds.boxAround(89.9999, 0, 5)` does not divide by ~0 — the longitude half-extent clamps to 180.
  </behavior>
  <action>
Write the `<behavior>` tests first and watch them fail, then implement.

`V61__postcode_centroid.sql` is **DDL only** — no data. Create `postcode_centroid(postcode text
primary key, latitude double precision not null, longitude double precision not null)` plus a btree
index on `shops(latitude, longitude)` that `33-06`'s distance prefilter uses. A ~46 MB data migration
would execute on every Testcontainers integration test that spins a fresh Postgres, and this repo
runs a dedicated `integrationTest` task against real Postgres — minutes per container for data no
unit test needs. **Add no `CREATE EXTENSION`** — see `<interfaces>` for why it cannot work.

`postcode-centroids-fixture.csv` must contain the real centroids for the postcodes the demo shops use
— SE15 5BS and SW9 8PS — plus spread-out GB postcodes for `33-06`'s ordering tests, plus the
Bellenden Road replacement postcode `33-05` will seed. It must **deliberately NOT contain SE15 4QA**:
that is this phase's free permanent negative control and it costs nothing to keep.

`PostcodeGeocoder.locate(String address)` returns `Optional`: extract the trailing postcode with a
**permissive** regex, normalise (uppercase, strip spaces), look up by primary key. The table is the
authority — a hit is proof the postcode is real, a miss is proof it is not. Do NOT add a "correct" UK
postcode regex or a validation library: `SE15 4QA` passes every plausible regex and is not a real
postcode, and a regex that disagrees with the dataset rejects real vendors. Anchor the regex to the
string end with bounded quantifiers — no nested repetition, since the input is untrusted vendor text.
Log a WARN naming the address on a miss; return empty. Never default to (0,0), never throw.

`GeoBounds.boxAround` computes the half-extents in Java so the SQL can use leakproof float8
comparisons. Guard the longitude divisor when `cos(lat)` approaches zero.
  </action>
  <verify>
    <automated>./gradlew :core-java:test --tests '*PostcodeGeocoderTest*' --tests '*GeoBoundsTest*' --rerun-tasks</automated>
  </verify>
  <done>
All `<behavior>` cases pass. `SE15 4QA` returns empty, not (0,0). V61 has no `CREATE EXTENSION`.
`--rerun-tasks` is present because a cached `UP-TO-DATE` reports BUILD SUCCESSFUL while executing
nothing; read results from `build-local`, not the stale `build`.

**Fail direction:** add `SE154QA` to the fixture and confirm the unknown-postcode case starts
returning a coordinate and fails. Remove it, verify the restore by content.
  </done>
</task>

<task type="auto">
  <name>Task 2: The importer, ON by default in dev, with the whole geo config block</name>
  <files>core-java/src/main/java/uk/jtoye/core/geo/PostcodeCentroidImporter.java, core-java/src/main/resources/application.yml, core-java/src/test/resources/application-test.yml, core-java/src/test/java/uk/jtoye/core/geo/PostcodeCentroidImportIntegrationTest.java</files>
  <action>
`PostcodeCentroidImporter` loads the gzipped resource with `COPY` into a staging table then promotes,
and is a **no-op** when the table is already populated at the stored source version. Assert the
post-load row count matches the exact count recorded in `SOURCE.md` and **fail startup loudly** on
mismatch rather than serving a partial table.

**State the default explicitly, and make it ON for dev** (plan-checker B3). Declare the whole
`jtoye.geo.*` block in `application.yml`:
  - `postcode-import.enabled` — **default true**, so a dev stack populates the substrate rather than
    silently running with an empty table. Write a comment saying that a `false` default would make
    every downstream locality criterion in this phase vacuous: coordinates stay NULL, the located row
    degrades to "nothing within radius", and every automated verify stays green.
  - `coordinate-backfill.enabled` — the flag `33-05` reads. Default true for dev.
  - `default-radius-km` and `max-radius-km` — the keys `33-06` reads. Q-2 chose a platform default, so
    it must be readable and changeable without a code edit.

Set `postcode-import.enabled: false` in `application-test.yml` and load the small committed fixture
instead — that is what makes integration tests offline-deterministic. The test profile deliberately
diverges from dev here, and the divergence is the point: dev must be populated, tests must be fast.

Write `PostcodeCentroidImportIntegrationTest` against Testcontainers Postgres 15, asserting: the
fixture loads to its exact expected row count; **zero rows have latitude = 0 AND longitude = 0**; a
re-run is idempotent and does not duplicate; and a deliberately injected `pq = 90` row is rejected by
the loader rather than stored. That last assertion is the falsification arm for the Null Island
pitfall — 879 such rows exist in the real dataset, the sentinel is in a different column from the
coordinates, and loaded blind they become the nearest shop to every customer.

Add one further assertion that the dev default is what this plan claims: read the resolved property
value under the dev profile and assert it is `true`. A comment claiming a default is not a default.
  </action>
  <verify>
    <automated>./gradlew :core-java:integrationTest --tests '*PostcodeCentroidImport*' --rerun-tasks && out=$(grep -cE 'default-radius-km|max-radius-km' core-java/src/main/resources/application.yml || true); test "$out" -ge 2 && out2=$(grep -cE 'coordinate-backfill' core-java/src/main/resources/application.yml || true); test "$out2" -ge 1</automated>
  </verify>
  <done>
The import test is green including the injected-`pq=90` rejection arm and the dev-default assertion.
`application.yml` declares all four `jtoye.geo.*` keys; the radius-key grep measures **0 on today's
tree**, so this verify is live rather than pre-satisfied.

**Fail direction:** flip `postcode-import.enabled` to `false` in the dev profile and confirm the
default assertion fails. This is the arm that would have caught the silent-empty-substrate failure.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend the RLS contract, and gate the no-extension invariant</name>
  <files>core-java/src/test/java/uk/jtoye/core/security/RlsContractTest.java, scripts/check-no-create-extension.sh, .github/workflows/ci-cd.yaml</files>
  <action>
Extend `RlsContractTest.EXEMPT_TABLES` with `postcode_centroid` and a **written justification** in the
style of the three existing entries: public reference data, no `tenant_id` column, no tenant
dimension, no customer data — the postcode of a public address is not tenant information. **Do not
weaken the assertion**, which is what the file's own comment forbids; add the entry.

Then prove the sweep still bites: temporarily add a public table without RLS, watch the test FAIL,
restore, confirm it passes again. Verify the restore **by content** — grep a unique token or compare
`git hash-object` output — never by `git diff --stat`, which is empty both when a file is restored and
when it was never written. **Commit before running the arm**, because `git checkout` restores from the
INDEX and silently discards edits made after staging.

Write `scripts/check-no-create-extension.sh` (plan-checker W10 — this invariant was previously an
unchecked claim in a success-criteria list). It must scan **every** file under
`core-java/src/main/resources/db/migration/` for `CREATE EXTENSION` and exit non-zero on any hit,
because the constraint is not specific to V61: Flyway runs as `jtoye_app`, which cannot execute the
statement at all, so any migration adding one breaks every environment.

Two hazards to avoid, both recorded in this repo:
  - **The gate must not fire on its own definition.** It has to name the string it forbids. Scope the
    scan to the migration directory only, and if the script must contain the literal, ensure the scan
    path cannot reach the script.
  - **Fail closed at exit 2 (VOID)** when the migration directory is missing or the scan returns an
    empty file list. A zero-file scan reporting "clean" is the vacuous shape.
Use here-strings, never `cmd | grep -q X` under `pipefail`.

Wire it into `.github/workflows/ci-cd.yaml` and confirm `scripts/check-gate-enforcement.sh` sees the
wiring. Verifying the script is not verifying the CI job.
  </action>
  <verify>
    <automated>./gradlew :core-java:integrationTest --tests '*RlsContractTest*' --rerun-tasks || exit 1; bash scripts/check-no-create-extension.sh; rc=$?; test "$rc" -eq 2 && { echo "VOID: extension gate could not run"; exit 2; }; test "$rc" -eq 0 || exit 1; out=$(grep -cF 'check-no-create-extension' .github/workflows/ci-cd.yaml || true); test "$out" -ge 1 || exit 1; bash scripts/check-gate-enforcement.sh || exit 1; bash scripts/docs-freshness.sh || exit 1</automated>
  </verify>
  <done>
`RlsContractTest` is green with `postcode_centroid` exempted BY ADDITION, and the break arm was
observed failing with the restore verified by content. `check-no-create-extension.sh` passes, is wired
into CI, and `check-gate-enforcement.sh` is green.

**Two fail-direction arms:** (1) add `CREATE EXTENSION IF NOT EXISTS cube;` to V61 and confirm the
gate exits non-zero — then remove it; (2) point the gate at an empty directory and confirm it exits
**2**, not 0. Regenerate `docs/metrics.json` with `scripts/docs-freshness.sh --write` — never by
arithmetic.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed artefact -> `postcode_centroid` | Reference rows cross into a table driving customer-visible ranking |
| `postcode_centroid` -> RLS sweep | A new public table crosses the schema-walk guarding tenant isolation |
| free-text address -> geocoder regex | Untrusted vendor text crosses into a pattern matcher |
| migration -> database role | A DDL statement crosses into a role that cannot execute all DDL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-02-01 | Tampering | Null Island rows | mitigate | Loader filters both columns; the integration test asserts zero `(0,0)` rows and rejects an injected `pq=90` row |
| T-33-02-02 | Elevation of privilege | Flyway role | mitigate | No `CREATE EXTENSION` in V61, now enforced by a CI-wired gate across the whole migration directory. Granting `jtoye_app` `CREATE ON DATABASE` is explicitly rejected |
| T-33-02-03 | Information disclosure | `postcode_centroid` RLS exemption | accept | Public reference data, no `tenant_id`, no customer rows. Exempted BY ADDITION with written justification; the sweep is never weakened, and the break arm proves it still bites |
| T-33-02-04 | Denial of service | Address regex on untrusted vendor text | mitigate | Anchored to string end, bounded quantifiers, no nested repetition; lookup is a primary-key hit |
| T-33-02-05 | Denial of service | Silent empty substrate | mitigate | Import enabled by default in dev, the default asserted by a test, and a startup failure on a row-count mismatch rather than a partial table |
| T-33-02-SC | Tampering | package installs | accept | No package added. PostGIS and `earthdistance` both rejected on measured grounds |
</threat_model>

<verification>
## Control arms — each criterion, and the break that proves it can fail

| Criterion | Deliberate break | Expected failure |
|---|---|---|
| "An unknown postcode yields empty" | add `SE154QA` to the fixture | the unknown-postcode case returns a coordinate and fails |
| "The loader rejects Null Island" | inject a `pq=90` / `0,0` row | the `(0,0)` count assertion goes 0 -> 1 |
| "The import is ON by default in dev" | set the dev default to `false` | the resolved-property assertion fails. **This is the arm that catches the silent-empty-substrate vacuous pass** |
| "The radius keys exist" | today's tree | `default-radius-km|max-radius-km` measures **0** in `application.yml` — live, not pre-satisfied |
| "No migration creates an extension" | add `CREATE EXTENSION … cube;` to V61 | the gate exits non-zero. Measured 0 hits today, so the gate starts green and can go red |
| "The extension gate fails CLOSED" | point it at an empty directory | exit **2**, not 0 |
| "The RLS sweep still bites" | add a public table with no RLS | `RlsContractTest` fails naming it |

## Instrument notes

- **Never `grep -c X f | grep -qx 0`** — `grep -c` exits 1 on the DESIRED (zero) state and the hook
  blocks the shape. Use `out=$(grep -c X f || true); grep -qx 0 <<< "$out"`.
- `--rerun-tasks` on every gradle call: a cached `UP-TO-DATE` reports BUILD SUCCESSFUL while executing
  nothing. Read results from `build-local`; `core-java/build/` is a stale artefact directory that
  reports old failures as this run's.
- `grep` is **ugrep** — `{`/`}` are metacharacters; use `-F` for literals, `-E` for alternation.
- **Commit before any break arm**; `git checkout` restores from the INDEX. Verify restores by content.

- **`docs/metrics.json` is a GENERATED artefact and is deliberately absent from every plan's
  `files_modified`.** No plan owns it; several regenerate it. Resolve any conflict by re-running
  `scripts/docs-freshness.sh --write`, never by merging hunks and never by arithmetic.
</verification>

<success_criteria>
- A UK postcode resolves offline to within 10 m of an independent reference.
- `SE15 4QA` resolves to empty — not (0,0), not an exception — and stays absent from the fixture.
- Zero rows in the loaded table sit at (0,0), proven by an assertion observed failing.
- The importer is **enabled by default in dev**, asserted by a test, and fails startup on a row-count
  mismatch. The test profile uses the offline fixture.
- `application.yml` declares all four `jtoye.geo.*` keys, including the two `33-05`/`33-06` read.
- No migration contains `CREATE EXTENSION`, enforced by a CI-wired gate that fails closed at exit 2.
- `RlsContractTest` is green with `postcode_centroid` exempted by addition, never by weakening, and
  the sweep was proven to still bite.
</success_criteria>

<output>
Create `.planning/phases/33-the-consumer-product/33-02-SUMMARY.md` when done.
</output>
