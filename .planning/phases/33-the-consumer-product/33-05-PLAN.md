---
phase: 33-the-consumer-product
plan: 05
type: execute
wave: 4
depends_on: [33-02]
files_modified:
  - core-java/src/main/java/uk/jtoye/core/shop/ShopService.java
  - core-java/src/main/java/uk/jtoye/core/shop/dto/CreateShopRequest.java
  - core-java/src/main/java/uk/jtoye/core/dev/DemoDataSeeder.java
  - core-java/src/main/java/uk/jtoye/core/geo/ShopCoordinateBackfill.java
  - core-java/src/test/java/uk/jtoye/core/shop/ShopServiceGeocodeTest.java
  - core-java/src/test/java/uk/jtoye/core/geo/ShopCoordinateBackfillIntegrationTest.java
  - scripts/check-live-shop-coordinates.sh
  - .planning/phases/33-the-consumer-product/33-CONTROL-ARMS.md
autonomous: true
requirements: [CUST-01]
must_haves:
  truths:
    - "Every seeded shop with a real GB postcode has non-NULL coordinates in the LIVE dev database after a rebuild"
    - "A shop whose postcode does not geocode keeps its storefront, keeps NULL coordinates, and does not land at (0,0)"
    - "The backfill updates rows under FORCE RLS and is provably a no-op without a tenant GUC"
    - "A client cannot persist an out-of-range coordinate through the public write path"
  artifacts:
    - path: "core-java/src/main/java/uk/jtoye/core/geo/ShopCoordinateBackfill.java"
      provides: "Idempotent, tenant-looped backfill of existing NULL-coordinate shops"
    - path: "scripts/check-live-shop-coordinates.sh"
      provides: "Closes CA-1 against the DELIVERED RUNTIME, as both database roles"
    - path: "core-java/src/main/java/uk/jtoye/core/shop/dto/CreateShopRequest.java"
      provides: "Range-validated latitude/longitude on the write path"
      contains: "DecimalMin"
  key_links:
    - from: "core-java/src/main/java/uk/jtoye/core/dev/DemoDataSeeder.java"
      to: "PostcodeGeocoder"
      via: "the seeder uses the SAME geocoder as the API write path, never a second implementation"
      pattern: "PostcodeGeocoder|postcodeGeocoder"
    - from: "scripts/check-live-shop-coordinates.sh"
      to: "33-CONTROL-ARMS.md CA-1"
      via: "re-runs CA-1's exact query post-population and writes the result beside the capture"
      pattern: "CA-1"
---

<objective>
Populate `shops.latitude` / `shops.longitude` — link 3 of #460's five-link chain, and the one the
roadmap does not name — then **close the control arm against the live runtime**.

Purpose: `DemoDataSeeder.upsertShop` takes no coordinate parameters and the seeder never calls
`setLatitude`/`setLongitude`, so every shop on the tree has NULL coordinates (measured: 5 rows, 0 with
latitude, 3 published). A ranking feature over NULL coordinates returns nothing before and nothing
after — it cannot be shown to fail. This plan is what makes every downstream locality criterion
falsifiable.

**REVISED (plan-checker B3) — the control arm was captured and never closed.** My previous version's
only evidence that population worked was Testcontainers tests running against `33-02`'s small fixture
CSV — not the real dataset, not the seeded shops, not the delivered runtime. Nothing anywhere re-read
the live database after population. Combined with the importer-default gap (now fixed in `33-02`),
`postcode_centroid` could have been empty, every coordinate could have stayed NULL, and the located row
would have degraded to "nothing within radius" while every verify in the phase stayed green — a vacuous
pass on the phase's headline claim. Task 4 now closes CA-1 against the running stack, as both roles.

Two links CONTEXT.md does not name, both handled here:
  - **3a** — the write path exists and is UNVALIDATED. `CreateShopRequest` declares
    `latitude`/`longitude` at lines 24-25 with no `@DecimalMin`/`@DecimalMax`, and the generated
    `ShopMapperImpl` writes them on create and update. A tenant can POST `latitude: 999` today and it
    persists.
  - **3b** — `DemoDataSeeder` is `@Profile("dev")` and is NOT the production write path. Fixing the
    seeder fixes the demo only. Both paths must call the SAME `PostcodeGeocoder`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-the-consumer-product/CONTEXT.md
@.planning/phases/33-the-consumer-product/RESEARCH.md
@.planning/phases/33-the-consumer-product/33-CONTROL-ARMS.md
@.planning/phases/33-the-consumer-product/33-02-SUMMARY.md
@core-java/src/main/java/uk/jtoye/core/shop/ShopService.java
@core-java/src/main/java/uk/jtoye/core/dev/DemoDataSeeder.java
@core-java/src/main/java/uk/jtoye/core/shop/dto/CreateShopRequest.java

<interfaces>
From 33-02 — use these; do not re-derive:
  uk.jtoye.core.geo.PostcodeGeocoder
      Optional<Coordinate> locate(String address)   // empty on: null, no extractable postcode, or a
                                                    // postcode absent from postcode_centroid.
                                                    // NEVER (0,0), never throws.

CONFIG OWNERSHIP: `core-java/src/main/resources/application.yml` is owned by 33-02, which declares the
whole `jtoye.geo.*` block including `coordinate-backfill.enabled`. **READ that key; do NOT add it
here.** This plan and its wave sibling 33-06 would otherwise both edit the same file in the same wave,
and `application.yml` is deliberately absent from this plan's `files_modified` and from every task's
`<files>`.

The five shops on the tree, and what each proves. Measured live as the container superuser
(`docker exec jtoye-postgres printenv POSTGRES_USER` -> `jtoye`; the role `postgres` does not exist):

  slug                    published  address                                          expectation
  mama-ades-kitchen       t          48 Rye Lane, Peckham, London SE15 5BS            GEOCODES
  brixton-village-grill   t          Unit 74, Brixton Village Market, London SW9 8PS  GEOCODES
  peckham-jollof-co       t          12 Bellenden Road, Peckham, London SE15 4QA      DOES NOT — see below
  tenant-b-probe          f          1 Probe Lane, London                             no postcode at all
  unsorted-legacy-items   f          —                                                no address at all

**`SE15 4QA` IS NOT A REAL POSTCODE.** Measured against Code-Point Open 2026-08 (`RC=1`, no output,
with `SE15 5BS` as the control returning a row) and independently confirmed against ONSPD via
postcodes.io (`SE154QA` -> HTTP 404 "Postcode not found", while `SE155BS` and `SW98PS` return 200).
`12 Bellenden Road` is a real street; live postcodes on it are SE15 4QJ / 4QL / 4QN / 4QR / 4QS / 4QW /
4QY. Left alone, "Peckham Jollof Co." silently gets NULL coordinates and vanishes from every distance
result — a regression-by-omission introduced by the change meant to fix locality.

Existing seeder shape:
  DemoDataSeeder.upsertShop(SeedResult, name, slug, address, description, tags, logoUrl,
                            deliveryFeePennies, freeDeliveryThresholdPennies)
  — takes NO coordinate parameters; find-by-slug, set fields, save.
  @Component @Profile("dev") implements ApplicationRunner.

THE RECURRING RLS-BACKFILL TRAP (V25 -> V44 -> V57, three times):
`shops` is ENABLE + FORCE RLS, and FORCE applies to the table owner too. A bare
`UPDATE shops SET latitude = … WHERE latitude IS NULL` with no tenant GUC matches **ZERO rows and
reports success**. Read the row COUNT, never the exit code.

RLS BLINDS THE VERIFICATION QUERY: as `jtoye_app` with no tenant GUC, `shops` shows only PUBLISHED rows
— 3 of 5. Any criterion about the two UNPUBLISHED shops MUST name the superuser role or it passes
vacuously (plan-checker W1).

SHELL IDIOM — REQUIRED. `grep -c` exits **1** on a zero count, i.e. on the DESIRED state of an absence
check, and `cmd | grep -q X` under `pipefail` INVERTS on match. This repo's hook blocks both.
Always: `out=$(grep -c X f || true); grep -qx 0 <<< "$out"`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Geocode on the write path, and validate what a client sends</name>
  <files>core-java/src/main/java/uk/jtoye/core/shop/ShopService.java, core-java/src/main/java/uk/jtoye/core/shop/dto/CreateShopRequest.java, core-java/src/test/java/uk/jtoye/core/shop/ShopServiceGeocodeTest.java</files>
  <behavior>
    - `createShop` with an address ending in a real GB postcode and no client-supplied coordinates persists the geocoded latitude/longitude.
    - `createShop` with an address whose postcode is not in the table persists NULL coordinates, logs a WARN naming the shop, does NOT throw, and does NOT write (0,0).
    - `createShop` with `latitude: 999` is rejected with a 400-class validation error before persistence — this FAILS on today's tree, where it persists.
    - `createShop` with `latitude: 51.47, longitude: -0.07` is accepted.
    - `updateShop` that changes the address re-geocodes.
    - `updateShop` that does not change the address does NOT clobber an existing coordinate with NULL.
    - The chosen precedence rule (client-supplied vs geocoded) is asserted explicitly, so a future reader cannot mistake it for an accident.
  </behavior>
  <action>
Write the `<behavior>` tests first and watch them fail. Note the `latitude: 999` case fails today for
the opposite reason — it is accepted — and that is exactly the control arm.

Add `@DecimalMin`/`@DecimalMax` to `CreateShopRequest.latitude` (-90 to 90) and `.longitude` (-180 to
180). These are the ASVS V5 controls RESEARCH flags: the fields have no validation today. The endpoint
already runs `@Valid`, so a range violation becomes an RFC 7807 typed 400 via
`GlobalExceptionHandler` rather than a persisted absurdity.

Inject `PostcodeGeocoder` into `ShopService` and call it from both `createShop` and `updateShop` after
the mapper runs.

**Decide and document the precedence rule.** RESEARCH recommends geocoding authoritatively from the
postcode and ignoring client-supplied coordinates on the public write path — the current silent-accept
is the worse of both. Implement one and state which, and why, in a code comment: (a) geocode always and
overwrite anything the client sent, or (b) accept a client coordinate only when it passes the new range
validation AND the address does not geocode. Do not leave it implicit. Either way, `updateShop` must not
turn a populated coordinate back to NULL because a re-geocode missed.

Respect the existing sole-writer invariant in `updateShop` (threat T-18-05-T): this change must not give
`updateShop` a new route to alter `published`.
  </action>
  <verify>
    <automated>./gradlew :core-java:test --tests '*ShopServiceGeocodeTest*' --rerun-tasks && out=$(grep -cF 'DecimalMin' core-java/src/main/java/uk/jtoye/core/shop/dto/CreateShopRequest.java || true); test "$out" -ge 2</automated>
  </verify>
  <done>
All `<behavior>` cases pass. `latitude: 999` is rejected, and the test was observed FAILING against the
pre-change tree where it persisted. The precedence rule is implemented and named in a comment.
`updateShop` cannot NULL an existing coordinate on a geocode miss.
  </done>
</task>

<task type="auto">
  <name>Task 2: Seed real coordinates, and correct the postcode that does not exist</name>
  <files>core-java/src/main/java/uk/jtoye/core/dev/DemoDataSeeder.java</files>
  <action>
Inject the SAME `PostcodeGeocoder` into `DemoDataSeeder` and call it inside `upsertShop` from the
address already being set. Do NOT add coordinate parameters to `upsertShop`'s signature and do NOT
hardcode literal lat/lon values — a second coordinate source is a second thing to drift, and link 3b's
whole point is that the seeder and the API share one implementation.

Correct `Peckham Jollof Co.`'s address. `SE15 4QA` exists in neither Code-Point Open nor ONSPD. Replace
it with a real Bellenden Road postcode — SE15 4QJ, 4QL, 4QN, 4QR, 4QS, 4QW or 4QY — and add a comment
recording that the original was measured absent from both datasets, so a future reader does not
"restore" it. **Verify the replacement actually geocodes before committing**; do not swap one unverified
postcode for another. It must also be present in `33-02`'s test fixture.

**Keep the deliberate negative controls.** `tenant-b-probe` ("1 Probe Lane, London") and
`unsorted-legacy-items` ("—") have no extractable postcode and must stay that way — they are free
permanent controls for the "no postcode -> NULL coordinates, no crash, no (0,0)" path. Do not give them
addresses. Note in a comment that they are load-bearing as controls, so a later tidy-up does not
helpfully add postcodes and silently delete the only rows exercising that branch.

Preserve everything else `upsertShop` does — tags, logo, fees, and the deliberate dev-only
`setPublished(true)` bypass of the Phase-18 sole-writer rule, which carries its own justification
comment. This change is additive.
  </action>
  <verify>
    <automated>out=$(grep -cF 'SE15 4QA' core-java/src/main/java/uk/jtoye/core/dev/DemoDataSeeder.java || true); grep -qx 0 <<< "$out" && out2=$(grep -cE 'PostcodeGeocoder|postcodeGeocoder' core-java/src/main/java/uk/jtoye/core/dev/DemoDataSeeder.java || true); test "$out2" -ge 1 && out3=$(grep -cE 'setLatitude\s*\(\s*[0-9-]' core-java/src/main/java/uk/jtoye/core/dev/DemoDataSeeder.java || true); grep -qx 0 <<< "$out3"</automated>
  </verify>
  <done>
The seeder geocodes through the shared `PostcodeGeocoder` and hardcodes no literal coordinate.
`SE15 4QA` is gone, replaced by a postcode verified to resolve and present in the test fixture. The two
address-less shops are untouched and commented as deliberate negative controls.
  </done>
</task>

<task type="auto">
  <name>Task 3: Backfill under FORCE RLS, and prove the RLS half</name>
  <files>core-java/src/main/java/uk/jtoye/core/geo/ShopCoordinateBackfill.java, core-java/src/test/java/uk/jtoye/core/geo/ShopCoordinateBackfillIntegrationTest.java</files>
  <action>
Write an idempotent backfill for shops that already exist with NULL coordinates. New shops get
coordinates from Task 1's write path; existing rows do not, and this is a **data migration**, not only
a code edit. Read `jtoye.geo.coordinate-backfill.enabled` — declared by `33-02`, not by this plan.

Do it in Java with `TenantContext`, or loop tenants with `set_config('app.current_tenant_id', …, true)`.
**Do NOT write a bare `UPDATE` in a Flyway migration.** `shops` is ENABLE + FORCE RLS, FORCE applies to
the owner, and with no tenant GUC the statement matches zero rows and reports success. This exact defect
is recorded three times in this repo's migration history (V25, V44, V57).

Enumerate tenants from the `tenants` table, which has no RLS and is the source of identity. Make the
runner idempotent — a second execution updates zero rows and logs that it did. Log the per-tenant
updated-row COUNT and the count of shops visited that did not geocode **separately**: those are
different facts and collapsing them hides the miss rate.

Write `ShopCoordinateBackfillIntegrationTest` asserting BOTH directions; the negative one is what
matters:
  a. WITH a tenant GUC: the backfill updates a positive number of rows, and the specific seeded shop's
     coordinate is non-NULL afterwards.
  b. WITHOUT a tenant GUC: the same operation updates **ZERO** rows. Assert the returned COUNT, **never**
     the exit status. This proves the code is RLS-aware rather than accidentally working because a test
     ran with a GUC already set.
  c. A shop whose postcode does not geocode is left NULL, is NOT set to (0,0), and the run completes.
  d. Re-running is a no-op.

For (b): the recorded trap is that a tenant pin sits UNDER a global aspect, so breaking the pin at the
wrong layer proves nothing. Remove the `set_config`/`TenantContext.set` **at the layer the backfill
itself uses** and confirm the count goes to zero — do not break a lower-level helper and assume the same
thing was tested.

`shops_aud` mirrors exist (`V16:92-93`), so the backfill generates audit revisions. That is expected, not
a defect — note it in the class comment so a reviewer does not read a revision spike as damage.
  </action>
  <verify>
    <automated>./gradlew :core-java:integrationTest --tests '*ShopCoordinateBackfill*' --rerun-tasks</automated>
  </verify>
  <done>
The backfill is tenant-looped and idempotent. The integration test asserts >0 updated rows WITH a GUC
and exactly 0 WITHOUT one, both on the returned count rather than an exit status. The non-geocoding shop
is left NULL, not (0,0). The break arm was run at the correct layer.
  </done>
</task>

<task type="auto">
  <name>Task 4: Close CA-1 against the DELIVERED RUNTIME, as both roles</name>
  <files>scripts/check-live-shop-coordinates.sh, .planning/phases/33-the-consumer-product/33-CONTROL-ARMS.md</files>
  <action>
This task exists because plan-checker B3 found that nothing in the phase re-read the live database after
population. Testcontainers tests over a small fixture prove the code; they do not prove the delivered
runtime. Proof Standard #2: verify the running thing, not the source.

**Rebuild ALL containers first**, then confirm parity by content — `docker compose start` does not
rebuild, and `scripts/check-runtime-freshness.sh` compares `.Metadata.LastTagTime`, not `.Created`
(which Docker preserves across a fully cached rebuild). Run it from the MAIN checkout: the compose
project name comes from the directory, so a worktree VOIDs it.

Write `scripts/check-live-shop-coordinates.sh`, which re-runs **CA-1's exact query** against the live dev
database and asserts the post-population state:
  - As the container superuser (read from `POSTGRES_USER`; it is `jtoye`, not `postgres`): assert the
    **relation**, not the census. The invariant is *every published shop with an extractable GB
    postcode has a non-NULL coordinate* — i.e. the count of published shops whose address yields a
    postcode **and** whose `latitude IS NULL` must be **0**. **Do not assert `total = 5`.** That
    equality reds on any sixth shop, including one an E2E run or a manual test creates, and a gate
    that fails on legitimate data teaches people to ignore it — which costs more than the coverage it
    buys. The relational form is both stronger (it scales with the data instead of pinning it) and
    less brittle.
    Record today's census — 5 total / 3 published / 3 geocoded — as **context in the output**, not as
    an assertion. The two address-less shops stay NULL: that is the negative control holding, not a
    failure, and the relational predicate excludes them by construction rather than by a magic number.
  - As `jtoye_app` with no tenant GUC: 3 visible, 3 with latitude. Record BOTH and the sentence
    explaining the disagreement, exactly as CA-1 does.
  - Assert **zero** rows anywhere with `latitude = 0 AND longitude = 0`, as the superuser — Null Island
    reaching a shop row is the failure this whole phase's filtering exists to prevent.
  - Assert `postcode_centroid` is non-empty. An empty reference table with NULL coordinates everywhere is
    the exact silent-degradation B3 identified, and it is indistinguishable from "the geocoder is
    broken" without this line.

**Fail closed at exit 2 (VOID)** when the stack is not running, `psql` is unavailable, the role cannot be
read from the container, or any query returns empty. "Found nothing" is never "clean". Read the row
COUNTS, never the exit code: `docker exec` without `-i` will not deliver a heredoc to `psql` and still
exits 0 having done nothing. Capture `rc` on the same statement as the command — `$?` after an
intervening `echo` reports the echo's status.

Do not paste any command line carrying a literal password value. Use the env-var form.

Append the results to `33-CONTROL-ARMS.md` under `## CA-1 CLOSED — post-population, live runtime`,
beside the original capture so the before and after sit together and a reader can compare them without
hunting. Include the rebuild evidence (image tag times vs the newest commit touching each service's
build paths).
  </action>
  <verify>
    <automated>bash scripts/check-runtime-freshness.sh; rf=$?; test "$rf" -eq 0 && bash scripts/check-live-shop-coordinates.sh; rc=$?; test "$rc" -eq 0 && out=$(grep -cF 'CA-1 CLOSED' .planning/phases/33-the-consumer-product/33-CONTROL-ARMS.md || true); test "$out" -ge 1 && bash scripts/docs-freshness.sh && bash scripts/check-doc-metrics.sh</automated>
  </verify>
  <done>
The live dev database shows 3 of 5 shops with coordinates as the superuser and 3 of 3 as `jtoye_app`,
`postcode_centroid` is non-empty, and zero rows sit at (0,0). Both readings and the explanation are
written beside CA-1's original capture. Runtime freshness is green, proven by content.

**Three fail-direction arms, all run and recorded:** (1) stop the stack and confirm the script exits
**2**, not 0 — this is the arm that proves it cannot report a clean result over a dead runtime; (2) set
one published shop's latitude to NULL directly and confirm the count assertion fails; (3) set one shop
to `(0,0)` and confirm the Null Island assertion fires. Restore each by content-verified update and
re-run the clean direction LAST, so the closing arm proves the restores happened.
Regenerate `docs/metrics.json` with `scripts/docs-freshness.sh --write` — never by arithmetic.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> `POST/PUT /api/v1/shops` | Tenant-supplied coordinates cross into persistent storage |
| backfill -> `shops` | A bulk write crosses the FORCE RLS tenant wall |
| free-text address -> geocoder | Untrusted vendor text crosses into a regex and a table lookup |
| live database -> evidence file | Query output crosses into git history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-05-01 | Tampering | `CreateShopRequest.latitude/longitude` | mitigate | `@DecimalMin`/`@DecimalMax` bounds plus a documented precedence rule; today an unvalidated `latitude: 999` persists |
| T-33-05-02 | Elevation of privilege | Backfill crossing tenants | mitigate | Loop the `tenants` registry and pin the GUC per tenant; never `rolbypassrls`, never a privileged bare UPDATE. The zero-rows-without-GUC assertion is the proof |
| T-33-05-03 | Denial of service | Address regex on untrusted vendor text | mitigate | Anchored, bounded quantifiers, no nested repetition (33-02); lookup is a primary-key hit |
| T-33-05-04 | Information disclosure | WARN logs on geocode miss, and the evidence file | mitigate | Log the shop slug and extracted postcode only — a public business postcode is not personal data. No pasted command in the evidence file carries a password value |
| T-33-05-05 | Repudiation | `shops_aud` revision spike from the backfill | accept | Envers mirrors exist by design; the backfill legitimately generates revisions. Documented so it is not misread as damage |
| T-33-05-06 | Denial of service | Verification reporting clean over a dead runtime | mitigate | `check-live-shop-coordinates.sh` exits 2 (VOID) on a stopped stack, missing tooling, unreadable role or empty result, and the arm proving it is run |
| T-33-05-SC | Tampering | package installs | accept | No package added; `PostcodeGeocoder` came from 33-02 |
</threat_model>

<verification>
## Control arms — each criterion, and the break that proves it can fail

| Criterion | Deliberate break | Expected failure |
|---|---|---|
| "Shops have coordinates" | **today's tree** — CA-1 records `count(latitude) = 0` of 5 | the Task 4 assertion fails. This arm EXPIRES the moment Task 2 or 3 runs, which is why 33-00 captured it first |
| "…in the DELIVERED RUNTIME" | rebuild nothing and re-run Task 4 | `check-runtime-freshness.sh` goes red or VOID before the coordinate check even runs |
| "The check cannot pass over a dead stack" | stop the stack | exit **2**, not 0 |
| "postcode_centroid is populated" | disable the importer and re-seed | the non-empty assertion fires — this is the arm that distinguishes "geocoder broken" from "reference table empty" |
| "The backfill is RLS-aware" | remove `set_config`/`TenantContext.set` **at the backfill's own layer** | the update count drops to 0. Assert the COUNT, never the exit code |
| "Out-of-range coordinates are rejected" | POST `latitude: 999` | today it PERSISTS; after this plan it is a typed 400 |
| "An unknown postcode yields NULL, not (0,0)" | restore `SE15 4QA` as the seeded address | the shop must still be NULL and absent from distance results; a (0,0) would make it nearest to everyone |
| "The seeder shares one geocoder" | hardcode `setLatitude(51.47)` in the seeder | the literal-coordinate count goes 0 -> 1 |
| "No shop sits at Null Island" | set one shop to `(0,0)` | the superuser Null Island assertion fires |

## Instrument notes

- **Name the role in any criterion about unpublished shops** (plan-checker W1). As `jtoye_app` with no
  tenant GUC, `shops` shows only PUBLISHED rows — 3 of 5 — so a criterion about `tenant-b-probe` or
  `unsorted-legacy-items` passes vacuously as the app role. Read the superuser name from
  `POSTGRES_USER`; it is `jtoye`, and the role `postgres` does not exist.
- **Read the row counts, never the exit code.** `docker exec` without `-i` does not deliver a heredoc to
  `psql` and still exits 0, having done nothing.
- Capture `rc` on the same statement as its command; `$?` after an `echo` reports the echo.
- **Never `grep -c X f | grep -qx 0`** — `grep -c` exits 1 on the DESIRED (zero) state and the hook blocks
  the shape. Use `out=$(grep -c X f || true); grep -qx 0 <<< "$out"`.
- `--rerun-tasks` on every gradle call; a cached `UP-TO-DATE` reports success while executing nothing.
  Read results from `build-local`, not the stale `build`.
- **Commit before any break arm**; `git checkout` restores from the INDEX and discards post-staging
  edits. Verify restores by content and **assert the clean state LAST as well as first** — the restore is
  the part nothing watches.

- **`docs/metrics.json` is a GENERATED artefact and is deliberately absent from every plan's
  `files_modified`.** No plan owns it; several regenerate it. Resolve any conflict by re-running
  `scripts/docs-freshness.sh --write`, never by merging hunks and never by arithmetic.
</verification>

<success_criteria>
- **In the LIVE dev database after a verified rebuild**, asserted as a RELATION rather than a census:
  **zero** published shops whose address yields a GB postcode have a NULL coordinate — read as the
  superuser `jtoye` and again as `jtoye_app`; `postcode_centroid` is non-empty; and zero rows sit at
  (0,0). Both readings are recorded beside CA-1's original capture, together with the day's census
  (5 total / 3 published / 3 geocoded) as **context, not as an assertion** — pinning the total reds on
  any sixth shop, including one an E2E run creates, and a gate that fails on legitimate data gets
  ignored.
- The two address-less shops (named, and read **as the superuser** — `jtoye_app` cannot see them) retain
  NULL coordinates and are documented as deliberate controls.
- `SE15 4QA` no longer appears in the seeder; its replacement was verified to resolve before commit.
- `POST /api/v1/shops` with `latitude: 999` returns a typed 400, and the test was observed failing
  against the pre-change tree.
- The backfill updates >0 rows WITH a tenant GUC and exactly 0 WITHOUT one, both on the returned count.
- `check-live-shop-coordinates.sh` exits 2 over a stopped stack, demonstrated.
</success_criteria>

<output>
Create `.planning/phases/33-the-consumer-product/33-05-SUMMARY.md` when done.
</output>
