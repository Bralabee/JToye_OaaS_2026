---
phase: 26-local-k8s-overlay-verified-breakage-fixes
plan: 05
type: execute
wave: 5
depends_on: ["26-04"]
files_modified:
  - .env.example
  - scripts/lib/k8s-local-guards.sh
  - scripts/k8s-local-secrets.sh
  - scripts/k8s-local-up.sh
  - scripts/deploy.sh
  - frontend/e2e/stomp-relay.spec.ts
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "One command brings the local cluster up from a stopped minikube, and it is safe to re-run"
    - "Both bootstrap scripts refuse to act unless the resolved kubectl context is the local minikube profile — a careless run can never target the employer AKS cluster"
    - "The bootstrap refuses to proceed while any compose APP container is running, and refuses if the compose backing services are down"
    - "Every secret the base manifests consume is created idempotently from .env, and nothing becomes a kustomize resource"
    - "The BYPASSRLS jtoye_backup role and the jtoye-db-backups MinIO bucket are created by the bootstrap, because neither exists today"
    - "No authored script contains an environment-varying host or port literal — all come from .env or from the committed overlay"
    - "The frontend image is built with the local API URL baked in, so a dashboard page calls api.jtoye.local and not the compose core-java that the XOR rule just shut down"
    - "The rehearsal records image digests, so a stale-image pass cannot masquerade as a green one"
    - "scripts/deploy.sh no longer accepts a phantom `dev` target and no longer applies base files behind kustomize's back"
    - "The existing STOMP relay E2E spec can run against app.jtoye.local instead of only localhost"
  artifacts:
    - path: "scripts/k8s-local-secrets.sh"
      provides: "Idempotent secret + backup-role + bucket bootstrap from .env (D-01/D-02/D-03)"
      contains: "create-backup-role.sql"
    - path: "scripts/k8s-local-up.sh"
      provides: "The single idempotent bring-up entry point (D-14)"
      contains: "rollout status"
    - path: "scripts/lib/k8s-local-guards.sh"
      provides: "Shared context guard + compose-XOR guard + .env preflight"
    - path: ".env.example"
      provides: "Documented K8S_LOCAL_* + DB_BACKUP_PASSWORD keys"
      contains: "K8S_LOCAL_"
  key_links:
    - from: "scripts/k8s-local-secrets.sh"
      to: "infra/backups/create-backup-role.sql"
      via: "docker exec psql with an injected password variable"
      pattern: "create-backup-role\\.sql"
    - from: "scripts/k8s-local-up.sh"
      to: "k8s/local"
      via: "kubectl apply -k"
      pattern: "apply -k"
---

<objective>
Deliver the imperative half of INFRA-01: a shared guard library, an idempotent secret/role/bucket
bootstrap, and `scripts/k8s-local-up.sh` as the single bring-up entry point that replaces the
2026-07-14 rehearsal's multi-step imperative sequence — plus the two adjacent defects that make that
entry point trustworthy (`scripts/deploy.sh`'s phantom `dev` target and the Playwright cookie-domain
hardcode).

Purpose: an overlay that only a human can apply correctly is not a durable replacement for imperative
patches. Three environment facts research verified make the guards load-bearing rather than
decorative: `kubectl config current-context` is UNSET and the only context present is
`sipbihs2aks` (EMPLOYER infrastructure); the compose APP containers are running right now, sharing
the dev Postgres the cluster is about to write to; and neither the `jtoye_backup` role nor the
`jtoye-db-backups` bucket exists, so an un-bootstrapped backup rehearsal would either fail at upload
or "pass" on a zero-row dump.

Output: `.env`/`.env.example` keys, `scripts/lib/k8s-local-guards.sh`,
`scripts/k8s-local-secrets.sh`, `scripts/k8s-local-up.sh`, a corrected `scripts/deploy.sh`, and a
parameterised cookie domain in `frontend/e2e/stomp-relay.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-local-k8s-overlay-verified-breakage-fixes/26-CONTEXT.md
@.planning/phases/26-local-k8s-overlay-verified-breakage-fixes/26-RESEARCH.md
@.planning/phases/26-local-k8s-overlay-verified-breakage-fixes/26-VALIDATION.md
@.planning/phases/26-local-k8s-overlay-verified-breakage-fixes/26-04-SUMMARY.md

<interfaces>
<!-- Verified host facts (probed read-only during research; re-probe before the 26-07 rehearsal,
     because a second session can drive this checkout — see the env_concurrent_working_tree memory). -->

- `kubectl` v1.33.3 (Kustomize v5.6.0); `minikube` v1.36.0 (docker driver); Docker server 29.6.2.
- minikube profile `jtoye` EXISTS but is **Stopped**; IP `192.168.49.2`; k8s v1.33.1.
- `kubectl config current-context` is **UNSET**. The ONLY context is **`sipbihs2aks` = EMPLOYER AKS
  — never target it.** Every `kubectl` call must pass `--context` explicitly, and the scripts must
  refuse if the resolved context is not the configured local one. NEVER call
  `kubectl config use-context`.
- All ten compose services are healthy; every port publishes on `0.0.0.0` (a `host.minikube.internal`
  prerequisite). Compose **app** containers `jtoye-frontend`, `jtoye-edge-go`, `jtoye-mcp-server` and
  the un-named `core-java` service are UP — the XOR guard is immediately load-bearing.
- Host DB roles: `jtoye` (superuser + BYPASSRLS) and `jtoye_app` (NOSUPERUSER). **`jtoye_backup`
  does NOT exist.**
- Host MinIO `/data` contains only `jtoye-images`. **`jtoye-db-backups` does NOT exist** — `aws s3 cp`
  to a missing bucket fails `NoSuchBucket`, so the CronJob would fail at upload AFTER a successful dump.
- The `ghcr.io/bralabee/jtoye-{core-java,frontend,edge-go}:2.1.0` images on this host date from
  2026-07-13/14 — BEFORE Phases 23, 24 and 25. CONTEXT.md's assumption that compose already produces
  the ghcr-tagged images is inaccurate: `docker compose build` produces
  `jtoye_oaas_2026-<svc>:latest`. A stale-image rehearsal is a FALSE GREEN.
- `scripts/build-images.sh` is NOT usable here: it tags `ghcr.io/jtoye/<svc>:<tag>` (a third naming
  scheme matching neither the manifests nor compose) and passes NO `NEXT_PUBLIC_*` build args.

Container names and mechanisms to reuse (from `docker-compose.full-stack.yml`):
- Postgres: `jtoye-postgres`. Follow `scripts/verify-env.sh`'s idiom —
  `docker exec -i jtoye-postgres psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" ...` — so no host psql
  client and no host port literal are needed.
- MinIO: `jtoye-minio`; bucket creation precedent is the `minio-init` service (line ~416) which runs
  `minio/mc` with `mc alias set local http://minio:9000 …` then `mc mb --ignore-existing` and
  `mc anonymous set download`. Preferred literal-free approach: discover the compose network with
  `docker inspect jtoye-minio --format '{{range $k,$v := .NetworkSettings.Networks}}{{$k}}{{end}}'`,
  then `docker run --rm --network <that> minio/mc …` against `http://minio:9000`. The backup bucket
  must NOT get the public-read `mc anonymous set download` policy that `jtoye-images` gets.
- Mailhog SMTP `1025`; Keycloak `8085`; Redis `6379`; RabbitMQ `5672`/`61613`; MinIO `9000`;
  Postgres `5433`.

New `.env` keys (Claude's discretion per CONTEXT.md; use exactly these names and document each in
`.env.example`):
`DB_BACKUP_PASSWORD`, `NOTIFICATION_UNSUBSCRIBE_SECRET`, `K8S_LOCAL_POD_HOST`,
`K8S_LOCAL_DB_PORT`, `K8S_LOCAL_KC_PORT`, `K8S_LOCAL_REDIS_PORT`, `K8S_LOCAL_AMQP_PORT`,
`K8S_LOCAL_STOMP_PORT`, `K8S_LOCAL_MINIO_PORT`, `K8S_LOCAL_SMTP_PORT`, `K8S_LOCAL_KUBE_CONTEXT`,
`K8S_LOCAL_MINIKUBE_PROFILE`, `K8S_LOCAL_MINIKUBE_CPUS`, `K8S_LOCAL_MINIKUBE_MEMORY`,
`K8S_LOCAL_BACKUP_BUCKET`.
Naming reconciliation (research OQ-3): `k8s/QUICK_START.md` uses the shell name
`POSTGRES_BACKUP_PASSWORD` for the same value. `DB_BACKUP_PASSWORD` is chosen because it pairs with
the existing `DB_USER`/`DB_PASSWORD` app-role keys and is deliberately distinct from the
`POSTGRES_*` superuser pair — the very distinction DEF-2 is about. Plan 26-02 already noted both
names in QUICK_START; keep them consistent.

Secrets to create (only keys the base manifests actually consume):
- `postgres-credentials`: `host`=`$K8S_LOCAL_POD_HOST`, `port`=`$K8S_LOCAL_DB_PORT`,
  `database`=`$POSTGRES_DB`, `username`=`$DB_USER`, `password`=`$DB_PASSWORD`,
  `backup-username`=`jtoye_backup` (the role name defined in
  `infra/backups/create-backup-role.sql`), `backup-password`=`$DB_BACKUP_PASSWORD`.
  **`DB_USER` / `DB_PASSWORD`, never `POSTGRES_USER` / `POSTGRES_PASSWORD` — DEF-2.**
- `redis-credentials`: `password`
- `rabbitmq-credentials`: `username`, `password`, `stomp-login`, `stomp-passcode`
- `keycloak-credentials`: `admin-username`, `admin-password`, `frontend-client-secret`
- `nextauth-secret`: `secret`
- `s3-backup-credentials`: `access-key`, `secret-key` (MinIO root creds)
- `s3-media-credentials`: `access-key`, `secret-key` (MinIO root creds)
- `notification-credentials`: `unsubscribe-signing-secret` — created only when
  `NOTIFICATION_UNSUBSCRIBE_SECRET` is non-empty
- `stripe-credentials`: `api-key`, `webhook-secret` — created only when `STRIPE_API_KEY` is non-empty
- `smtp-credentials`: NOT created — Mailhog takes no auth and plan 26-02 made that ref `optional: true`

Idempotent creation pattern (D-01, mandatory):
`kubectl create secret generic <name> --from-literal=… --dry-run=client -o yaml | kubectl --context … apply -f -`
No delete window, no error on re-run, and nothing becomes a kustomize resource.

Do NOT add `DB_BACKUP_PASSWORD` or `NOTIFICATION_UNSUBSCRIBE_SECRET` to `scripts/verify-env.sh`'s
`REQUIRED_VARS`: that file gates EVERY `scripts/start-dev.sh` compose run, and making a k8s-local-only
key mandatory for compose dev would be a regression to a working path (Incremental Betterment).
Validate them inside `k8s-local-secrets.sh` instead, fail-loud and by NAME.

Security rule inherited from `scripts/verify-env.sh`: **never print a secret VALUE** — variable
NAMES only, in both new scripts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: .env keys, shared guard library, and the secret/role/bucket bootstrap</name>
  <files>.env.example, scripts/lib/k8s-local-guards.sh, scripts/k8s-local-secrets.sh</files>
  <read_first>
    - scripts/verify-env.sh (the fail-loud, names-only preflight to mirror: env-file sourcing with `set -a`, REQUIRED_VARS loop, weak deny-list, and its `docker exec jtoye-postgres psql -U …` idiom)
    - .env.example (the comment-then-key documentation shape and where the k8s/backup sections sit)
    - infra/backups/create-backup-role.sql (the `-v backup_password=` injection contract, the idempotent DO block, and the superuser-only BYPASSRLS rationale)
    - docker-compose.full-stack.yml lines 10-45 (postgres container name, POSTGRES_* vars, published 5433) and lines 390-432 (minio + minio-init: the mc mechanism, the network, and the public-read policy that must NOT be applied to the backup bucket)
    - k8s/base/core-java-deployment.yaml, k8s/base/frontend-deployment.yaml, k8s/base/pg-backup-cronjob.yaml (the authoritative list of secret NAMES and KEYS that must exist, incl. which are `optional: true` after plan 26-02)
    - k8s/local/kustomization.yaml (the `namespace:` line — the single source of truth the script must derive the namespace from, rather than duplicating it in .env)
  </read_first>
  <action>
**`.env.example` + the developer's `.env`.** Add a documented `K8S_LOCAL_*` section with every key
from `<interfaces>`, each with a comment naming its consumer and the compose line its default value
comes from. Values: `K8S_LOCAL_POD_HOST=host.minikube.internal`, `K8S_LOCAL_DB_PORT=5433`,
`K8S_LOCAL_KC_PORT=8085`, `K8S_LOCAL_REDIS_PORT=6379`, `K8S_LOCAL_AMQP_PORT=5672`,
`K8S_LOCAL_STOMP_PORT=61613`, `K8S_LOCAL_MINIO_PORT=9000`, `K8S_LOCAL_SMTP_PORT=1025`,
`K8S_LOCAL_KUBE_CONTEXT=jtoye`, `K8S_LOCAL_MINIKUBE_PROFILE=jtoye`, `K8S_LOCAL_MINIKUBE_CPUS=4`,
`K8S_LOCAL_MINIKUBE_MEMORY=12g`, `K8S_LOCAL_BACKUP_BUCKET=jtoye-db-backups`. Add
`DB_BACKUP_PASSWORD=CHANGE_ME` with a comment stating it is the BYPASSRLS `jtoye_backup` role
password, required only for the k8s-local path, and that `k8s/QUICK_START.md` calls the same value
`POSTGRES_BACKUP_PASSWORD` in its shell recipe. Add `NOTIFICATION_UNSUBSCRIBE_SECRET=` (empty
default) documenting that an empty value leaves the RFC 8058 one-click unsubscribe feature inert
(HMAC over an empty key), and generate a real value for the k8s-local run. Also set real values for
the new keys in the gitignored `.env` on this host so the bootstrap can run.

**`scripts/lib/k8s-local-guards.sh`.** A sourceable library (not executable on its own) providing:
- `k8s_local_load_env` — source `.env` with `set -a`, then assert every `K8S_LOCAL_*` key used by
  the caller is set and non-empty, failing by NAME. Never print values.
- `k8s_local_assert_context` — resolve the target context from `K8S_LOCAL_KUBE_CONTEXT`; refuse if
  that context does not exist in kubeconfig; refuse if it is not the configured name; and
  additionally assert that its cluster server host equals the `minikube ip` of
  `K8S_LOCAL_MINIKUBE_PROFILE`, so a same-named context pointing elsewhere is still rejected. NEVER
  call `kubectl config use-context`. The failure message must state plainly that the only other
  context on this host is employer infrastructure and must never be targeted.
- `k8s_local_assert_compose_xor` — refuse while ANY compose APP container is running (the three
  named containers plus the un-named `core-java` compose service, discovered via
  `docker compose -f docker-compose.full-stack.yml ps`), and refuse if any of the six backing
  services (postgres, redis, rabbitmq, keycloak, minio, mailhog) is NOT running. The message must
  explain both halves: apps down because the cluster and compose would be two writers on the same
  dev Postgres, backing services up because the cluster consumes them via `host.minikube.internal`.
- `k8s_local_namespace` — echo the namespace parsed from `k8s/local/kustomization.yaml`'s
  `namespace:` line, so the namespace has exactly one source of truth.
- `k8s_local_kubectl` — a wrapper that always passes `--context "$K8S_LOCAL_KUBE_CONTEXT"`.
Use the sibling gates' exit-code convention (0/1/2) and `set -euo pipefail` in the callers.

**`scripts/k8s-local-secrets.sh`.** Sources the guard library, then:
1. `k8s_local_load_env`, `k8s_local_assert_context`, `k8s_local_assert_compose_xor`. Validate
  `DB_BACKUP_PASSWORD` is set, non-empty, and not `CHANGE_ME` — fail by name.
2. Ensure the namespace exists (idempotent apply of `k8s/local/namespace.yaml`).
3. Bootstrap the BYPASSRLS role (D-02): pipe `infra/backups/create-backup-role.sql` into
  `docker exec -i jtoye-postgres psql` as the superuser with `-v backup_password=` bound from
  `DB_BACKUP_PASSWORD` and `ON_ERROR_STOP`. Then VERIFY by querying `pg_roles` that `jtoye_backup`
  exists with `rolbypassrls = true`, and fail if not. Do not reimplement the SQL.
4. Create the backup bucket (`K8S_LOCAL_BACKUP_BUCKET`) in host MinIO using the `minio/mc`
  mechanism from `<interfaces>`; idempotent via `mc mb --ignore-existing`. Explicitly do NOT apply
  the public-read `mc anonymous set download` policy — database dumps must not be world-readable.
  Verify the bucket exists afterwards with `mc ls` and fail if not.
5. Create every Secret in the `<interfaces>` list with the mandated
  `--dry-run=client -o yaml | kubectl apply -f -` pipeline. Create the two conditional Secrets only
  when their source value is non-empty, and log the SKIP by name.
6. Print a summary listing each Secret name, its key NAMES, and whether it was created or skipped —
  never a value.
Header comment must state: no port or host literal appears in this script (GLOBAL_RULE_6 /
ARCHITECTURE_RULE_8), so a compose port shift is a one-file `.env` fix; secrets are never kustomize
resources (#100), which is why this script is imperative by necessity; and the script is safe to
re-run.
  </action>
  <verify>
    <automated>bash -n scripts/lib/k8s-local-guards.sh && bash -n scripts/k8s-local-secrets.sh && bash scripts/k8s-local-secrets.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/lib/k8s-local-guards.sh` and `bash -n scripts/k8s-local-secrets.sh` both exit 0; `test -x scripts/k8s-local-secrets.sh` succeeds.
    - `grep -cE '(localhost|127\.0\.0\.1|:5433|:8085|:9000|:61613|:5672|:1025)' scripts/k8s-local-secrets.sh scripts/lib/k8s-local-guards.sh` returns 0 — no environment-varying host or port literal in either authored script.
    - `grep -c 'kubectl config use-context' scripts/lib/k8s-local-guards.sh scripts/k8s-local-secrets.sh` returns 0.
    - `grep -c 'sipbihs2aks' scripts/lib/k8s-local-guards.sh` is 0 as a TARGET but the refusal message references employer infrastructure — assert instead that `k8s_local_assert_context` exists and that every kubectl call goes through `k8s_local_kubectl` or passes `--context` explicitly: `grep -cE 'kubectl ' scripts/k8s-local-secrets.sh` equals `grep -cE 'k8s_local_kubectl|kubectl [^|]*--context' scripts/k8s-local-secrets.sh`.
    - Context-guard falsifiability: run with `K8S_LOCAL_KUBE_CONTEXT=sipbihs2aks` in the environment and confirm the script exits non-zero WITHOUT issuing any mutating kubectl call (verify by also passing a nonexistent namespace and confirming nothing was created). Record the refusal output.
    - XOR-guard falsifiability: with the compose app containers UP, the script exits non-zero naming the running app container(s). After `docker compose -f docker-compose.full-stack.yml stop core-java frontend edge-go mcp-server`, it proceeds. Record both outputs.
    - Backing-services falsifiability: with `jtoye-redis` stopped, the script exits non-zero naming redis. Restart and re-confirm.
    - `for k in DB_BACKUP_PASSWORD NOTIFICATION_UNSUBSCRIBE_SECRET K8S_LOCAL_POD_HOST K8S_LOCAL_DB_PORT K8S_LOCAL_KC_PORT K8S_LOCAL_REDIS_PORT K8S_LOCAL_AMQP_PORT K8S_LOCAL_STOMP_PORT K8S_LOCAL_MINIO_PORT K8S_LOCAL_SMTP_PORT K8S_LOCAL_KUBE_CONTEXT K8S_LOCAL_MINIKUBE_PROFILE K8S_LOCAL_MINIKUBE_CPUS K8S_LOCAL_MINIKUBE_MEMORY K8S_LOCAL_BACKUP_BUCKET; do grep -q "^$k=" .env.example || echo "MISSING $k"; done` prints nothing.
    - `git diff --quiet scripts/verify-env.sh` is true — the compose preflight was deliberately NOT made stricter.
    - After a successful run against a started minikube: `docker exec jtoye-postgres psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -tAc "SELECT rolbypassrls FROM pg_roles WHERE rolname='jtoye_backup'"` prints `t`.
    - The backup bucket exists and is NOT public: an unauthenticated `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:${K8S_LOCAL_MINIO_PORT}/${K8S_LOCAL_BACKUP_BUCKET}/` returns 403 (contrast: the same probe against `jtoye-images` returns 200).
    - `kubectl --context "$K8S_LOCAL_KUBE_CONTEXT" -n "$(bash -c 'source scripts/lib/k8s-local-guards.sh; k8s_local_namespace')" get secrets -o name | sort` lists at least `postgres-credentials`, `redis-credentials`, `rabbitmq-credentials`, `keycloak-credentials`, `nextauth-secret`, `s3-backup-credentials`, `s3-media-credentials`.
    - `kubectl --context "$K8S_LOCAL_KUBE_CONTEXT" -n <ns> get secret postgres-credentials -o jsonpath='{.data.username}' | base64 -d` prints the `jtoye_app` role name, NOT `jtoye` (DEF-2 proven at the live secret).
    - Running the script twice in a row exits 0 both times and the second run reports no resource-version churn errors (idempotence).
    - `bash k8s/scripts/check-no-plaintext-secrets.sh` still exits 0 — nothing the script creates became a kustomize resource.
    - No line of either script's output contains a secret value: `bash scripts/k8s-local-secrets.sh 2>&1 | grep -cF "$DB_PASSWORD"` returns 0.
  </acceptance_criteria>
  <done>`.env`/`.env.example` document every new key; the guard library refuses a wrong context, a running compose app container, and a missing backing service; the bootstrap creates the BYPASSRLS role and the non-public backup bucket and every consumed Secret idempotently from `.env`, with no host/port literal and no printed value.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/k8s-local-up.sh single entry point + scripts/deploy.sh phantom-dev fix</name>
  <files>scripts/k8s-local-up.sh, scripts/deploy.sh</files>
  <read_first>
    - scripts/lib/k8s-local-guards.sh and scripts/k8s-local-secrets.sh (from Task 1 — the functions to reuse)
    - scripts/deploy.sh (the whole file: the environment regex at line 27, the namespace create/label block, deploy_service's raw `kubectl apply -f k8s/base/<svc>-deployment.yaml`, the rollout-status + `rollout undo` rollback logic to preserve, and the trailing raw configmap/ingress applies)
    - scripts/build-images.sh (confirm the ghcr.io/jtoye tagging and absent NEXT_PUBLIC args — this is why it cannot be reused)
    - frontend/Dockerfile lines 19-42 (the five NEXT_PUBLIC_* build ARGs available)
    - docker-compose.full-stack.yml lines 295-320 (the frontend build args compose passes, incl. the `:?`-required NEXT_PUBLIC_CUSTOMER_KEYCLOAK_URL)
    - core-java/Dockerfile and edge-go/Dockerfile (build contexts; note .github/workflows/ci-cd.yaml:445 uses context '.' for core-java and the service dir for the others)
    - infra/backups/Dockerfile (the pg-backup image the CronJob pins at ghcr.io/bralabee/jtoye-pg-backup:15)
    - k8s/local/kustomization.yaml (the `images:` newTag `local` this script must produce, and the namespace)
    - .planning/phases/26-local-k8s-overlay-verified-breakage-fixes/26-RESEARCH.md PIT-3, PIT-4, PIT-8, PIT-11 and § "Making the live proofs reproducible"
  </read_first>
  <action>
**`scripts/k8s-local-up.sh`** — the single idempotent bring-up entry point (D-14). Source the guard
library and execute, in order, failing loudly at the first problem:
1. `k8s_local_load_env`; run `scripts/verify-env.sh` as the standard `.env` preflight;
   assert `docker`, `kubectl` and `minikube` are on PATH.
2. `k8s_local_assert_compose_xor`.
3. Start the minikube profile if it is not already Running, using `K8S_LOCAL_MINIKUBE_PROFILE`,
   `K8S_LOCAL_MINIKUBE_CPUS` and `K8S_LOCAL_MINIKUBE_MEMORY` (the 2026-07-14 rehearsal used 4 CPUs /
   12g). Then `k8s_local_assert_context` — after start, not before, since the context only exists
   once the profile has been created.
4. Enable the `ingress` addon idempotently for that profile. Do NOT enable metrics-server (D-09
   removed the need) and do NOT set `allow-snippet-annotations` on the controller ConfigMap (PIT-1 —
   the local overlay nulls the annotation instead; weakening the cluster's posture to make an apply
   succeed is forbidden).
5. Host-services reachability probe from INSIDE the cluster (PIT-11): for each of
   `K8S_LOCAL_DB_PORT`, `K8S_LOCAL_KC_PORT`, `K8S_LOCAL_REDIS_PORT`, `K8S_LOCAL_AMQP_PORT`,
   `K8S_LOCAL_STOMP_PORT`, `K8S_LOCAL_MINIO_PORT`, `K8S_LOCAL_SMTP_PORT`, run a `nc -vz` against
   `K8S_LOCAL_POD_HOST` via `minikube ssh` and fail naming the unreachable port. The message must
   suggest a host firewall on the minikube bridge as the likely cause, so the manifests are not
   blamed first.
6. `/etc/hosts` check (D-12): derive the two ingress hostnames from `kubectl kustomize k8s/local`
   (do not hardcode them), resolve each, and compare against `minikube ip`. If missing or wrong,
   PRINT the exact hosts-file line and the exact `sudo` command, then exit non-zero. This step must
   never attempt privilege escalation itself.
7. Images (PIT-3/PIT-4). Build all four with the manifest-matching names and the `local` tag the
   overlay pins: `ghcr.io/bralabee/jtoye-core-java:local`, `ghcr.io/bralabee/jtoye-edge-go:local`,
   `ghcr.io/bralabee/jtoye-frontend:local`, and `ghcr.io/bralabee/jtoye-pg-backup:15` (that tag is
   pinned in `k8s/base/pg-backup-cronjob.yaml`, so do NOT retag it). The frontend build MUST pass
   `--build-arg NEXT_PUBLIC_API_URL=<the api.url value read from the rendered local ConfigMap>`
   plus `NEXT_PUBLIC_CUSTOMER_KEYCLOAK_URL`, `NEXT_PUBLIC_SUPPORT_EMAIL`, `NEXT_PUBLIC_SUPPORT_URL`
   and `NEXT_PUBLIC_ONBOARDING_REVIEW_SLA_DAYS` from `.env` — `NEXT_PUBLIC_*` is frozen at build
   time, so a runtime env cannot fix a wrong bake and the omitted customer-realm arg would bake an
   empty string into the browser bundle. Do not use `scripts/build-images.sh`. Then
   `minikube -p <profile> image load` each image, and print each image's ID/digest so the rehearsal
   evidence can be pinned to code identity.
   Support a `--skip-build` flag that loads existing `:local` images without rebuilding, and refuse
   `--skip-build` if any required `:local` tag is absent.
8. Secrets, role and bucket: invoke `scripts/k8s-local-secrets.sh`.
9. Apply: `kubectl apply -f k8s/local/namespace.yaml` FIRST (PIT-8: a server dry-run cannot create
   the Namespace it is validating), then `kubectl apply -k k8s/local --dry-run=server` printing the
   output VERBATIM, then the real `kubectl apply -k k8s/local`. All three through
   `k8s_local_kubectl`. Support a `--dry-run-only` flag that stops after the dry-run.
10. `kubectl rollout status` for `deploy/core-java` (5m), `deploy/frontend` (3m) and
   `deploy/edge-go` (3m).
11. Smoke: core `/health` and `/public/shops` through the api ingress host, and the frontend
   `/api/health` through the app ingress host — both via the `.local` hostnames, never `localhost`.
   A `localhost:9090` anywhere in the output would mean the compose apps were up and the XOR guard
   was bypassed.
12. Print a copy-pasteable evidence block: the four image identities, the namespace, the resolved
   ingress hosts, and the pod list — the exact material plan 26-06's `k8s/LOCAL.md` template expects.

**`scripts/deploy.sh`.** Two fixes:
- Restrict the environment regex to `staging|production`. If `dev` or `local` is passed, print a
  message pointing at `scripts/k8s-local-up.sh` and exit non-zero. The `dev` target was a phantom:
  accepted by the regex with no `k8s/dev` overlay in the repo.
- Replace `deploy_service`'s raw `kubectl apply -f k8s/base/<svc>-deployment.yaml` (and the two
  trailing raw configmap/ingress applies) with a single `kubectl apply -k "k8s/$ENVIRONMENT"`,
  keeping the existing per-service `rollout status` + `rollout undo` rollback logic and the
  `SERVICE` argument as the selector for which rollout to wait on. Raw base applies bypassed
  kustomize entirely, so they silently skipped the overlay's namespace transformer, ConfigMap patch
  and image-tag pin. Add a comment recording that. Leave the namespace create/label block and all
  reporting output intact.
  </action>
  <verify>
    <automated>bash -n scripts/k8s-local-up.sh && bash -n scripts/deploy.sh && bash scripts/k8s-local-up.sh --dry-run-only; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/k8s-local-up.sh` and `bash -n scripts/deploy.sh` exit 0; `test -x scripts/k8s-local-up.sh` succeeds.
    - `grep -cE '(localhost|127\.0\.0\.1|192\.168\.49|:5433|:8085|:9000|:61613|:5672|:1025|jtoye\.local)' scripts/k8s-local-up.sh` returns 0 — hosts and ports come from `.env` or the rendered overlay, never from a literal.
    - `grep -c 'build-images.sh' scripts/k8s-local-up.sh` returns 0.
    - `grep -c 'allow-snippet-annotations' scripts/k8s-local-up.sh` returns 0 (PIT-1: the cluster's posture is not weakened).
    - `grep -c 'NEXT_PUBLIC_API_URL' scripts/k8s-local-up.sh` is >= 1 and the value is read from the rendered local ConfigMap, not hardcoded.
    - `grep -c 'sudo' scripts/k8s-local-up.sh` is >= 1 and every occurrence is inside a printed instruction string, never an executed command: `grep -nE '^\s*sudo ' scripts/k8s-local-up.sh` returns nothing.
    - `bash scripts/k8s-local-up.sh --dry-run-only` completes with the server dry-run output printed verbatim and exit 0 (requires the cluster started; if the run stops at the `/etc/hosts` step, that refusal counts as a PASS for this criterion provided the exact hosts line and sudo command were printed — record which).
    - `./scripts/deploy.sh dev all` exits non-zero and the message names `scripts/k8s-local-up.sh`.
    - `./scripts/deploy.sh local all` exits non-zero with the same guidance.
    - `grep -cE 'kubectl apply -f "\$PROJECT_ROOT/k8s/base/' scripts/deploy.sh` returns 0 — no raw base-file apply remains.
    - `grep -c 'apply -k' scripts/deploy.sh` is >= 1.
    - `grep -c 'rollout undo' scripts/deploy.sh` is >= 1 (the rollback good was preserved).
    - `grep -cE '\^\(dev\|staging\|production\)\$' scripts/deploy.sh` returns 0 and the regex now matches only staging and production.
    - The evidence block printed at the end contains four image identities and no secret value.
  </acceptance_criteria>
  <done>One command brings the cluster up from stopped, refuses on a wrong context / running compose app / unreachable host service / missing hosts entry, builds and loads four correctly-named and correctly-baked images, applies the overlay after a verbatim server dry-run, waits for three rollouts, smokes through the ingress hosts, and prints a digest-pinned evidence block; `deploy.sh` no longer offers a phantom `dev` target nor bypasses kustomize.</done>
</task>

<task type="auto">
  <name>Task 3: parameterise the STOMP relay E2E cookie domain (PIT-9)</name>
  <files>frontend/e2e/stomp-relay.spec.ts</files>
  <read_first>
    - frontend/e2e/stomp-relay.spec.ts (lines 18-45 and 110-130: the BASE/API env reads and the two `context.addCookies` calls with `domain: "localhost"`)
    - frontend/playwright.config.ts (baseURL already reads PLAYWRIGHT_BASE_URL — the precedent for config injection)
    - frontend/e2e/dashboard-mobile.spec.ts (how BASE and API are derived from env there; the pattern to align with)
  </read_first>
  <action>
Derive the cookie domain from the spec's own base URL instead of hardcoding it: compute the hostname
once (e.g. from `new URL(BASE).hostname`) and use that value at both `context.addCookies` call sites.
A browser at `app.jtoye.local` does not send a cookie scoped to `domain: "localhost"`, so the spec
currently cannot run against the ingress at all — `playwright.config.ts` already parameterises the
base URL, but the cookie domain was left behind.

Align the spec's base-URL env read with `dashboard-mobile.spec.ts` so a single
`PLAYWRIGHT_BASE_URL` drives both, while keeping the existing `FRONTEND_URL` / `EDGE_URL` /
`RELAY_E2E` variables working as they do today (additive, no removal — the compose-targeted
invocation must behave exactly as before).

Add a comment recording that these two cookies are STUB session cookies used for the relay
transport check, and that the DEF-5 split-horizon proof requires a REAL Keycloak login, which is
`dashboard-mobile.spec.ts`'s job in plan 26-07 — so nobody mistakes a stub-cookie pass for an auth proof.

Do not change any assertion, any timing, or the `RELAY_E2E` gate. This is a test-harness
parameterisation, not a behaviour change, and it must not alter the counted `test()` block total.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json && npx playwright test --list e2e/stomp-relay.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'domain: "localhost"' frontend/e2e/stomp-relay.spec.ts` returns 0.
    - `grep -c 'new URL(' frontend/e2e/stomp-relay.spec.ts` is >= 1 and the derived hostname is used at both `addCookies` sites (`grep -c 'domain:' ` returns 2, both referencing the derived variable).
    - `cd frontend && npx playwright test --list e2e/stomp-relay.spec.ts` lists the same number of tests as before the change (record the number in the SUMMARY).
    - `cd frontend && npx tsc --noEmit -p tsconfig.json` exits 0 — a tsc pass is required because jest does not type-check.
    - `cd frontend && npm run build` exits 0.
    - `grep -c 'PLAYWRIGHT_BASE_URL' frontend/e2e/stomp-relay.spec.ts` is >= 1 and `grep -c 'FRONTEND_URL' frontend/e2e/stomp-relay.spec.ts` is still >= 1 (additive, nothing removed).
    - `PLAYWRIGHT_BASE_URL=http://app.jtoye.local npx playwright test --list e2e/stomp-relay.spec.ts` succeeds, and a debug print of the derived domain yields `app.jtoye.local` (record the observed value).
    - `scripts/docs-freshness.sh` reports the SAME `playwright_blocks` count as before this task (no `test()` block added or removed).
  </acceptance_criteria>
  <done>The relay spec's cookie domain follows its base URL, so the existing specs can target `app.jtoye.local`; the compose-targeted invocation is unchanged and the Playwright block count is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer host → kubectl context | The only context in kubeconfig is employer AKS; a mis-targeted apply is a tampering event on someone else's infrastructure |
| `.env` → cluster Secrets | Plaintext credentials cross from a gitignored file into cluster objects |
| host superuser Postgres → new BYPASSRLS role | Creating a role that bypasses every RLS policy |
| compose runtime ↔ cluster runtime | Two writers on one dev Postgres |
| host MinIO → backup bucket | Database dumps at rest in object storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-24 | Tampering | applying manifests to the employer AKS cluster | mitigate | ASVS V14. `k8s_local_assert_context` refuses unless the resolved context name equals `K8S_LOCAL_KUBE_CONTEXT` AND its cluster server host equals the configured minikube profile's IP; `kubectl config use-context` is never called; every kubectl call passes `--context`. Proven RED by running with the context set to the AKS name. |
| T-26-25 | Elevation of Privilege | the BYPASSRLS `jtoye_backup` role | mitigate | ASVS V4. The role is the deliberate scoped exception: `create-backup-role.sql` grants SELECT only, no DDL, no write. The script must invoke that file rather than reimplement the SQL, and then VERIFY `rolbypassrls` — so a silently-failed bootstrap cannot masquerade as success and yield a zero-row dump. |
| T-26-26 | Information Disclosure | database dumps in the backup bucket | mitigate | The bucket is created WITHOUT the public-read policy `jtoye-images` carries. Asserted by an unauthenticated HTTP probe returning 403 while the images bucket returns 200. |
| T-26-27 | Information Disclosure | secret values in script output or shell history | mitigate | ASVS V7. Both scripts follow `scripts/verify-env.sh`'s rule: variable NAMES only, never values. Asserted by grepping the run output for a known secret value and expecting 0 hits. |
| T-26-28 | Information Disclosure | secret values in process argv | accept | `kubectl create secret --from-literal` places values in argv, briefly visible to `ps` on the same host. Accepted: this is D-01's mandated pattern, `k8s/QUICK_START.md` already documents it, and the host is a single-user development machine. Revisiting belongs with the deferred sealed-secrets/external-secrets adoption (PROJECT.md:141). |
| T-26-29 | Tampering / Denial of Service | two writers on the shared dev Postgres | mitigate | The XOR guard refuses while any compose app container runs and refuses if a backing service is down, converting the doc-line footgun into a guard rail (D-04). Proven RED in both directions. |
| T-26-30 | Elevation of Privilege | the `/etc/hosts` step | mitigate | The script only PRINTS the required line and the `sudo` command; it never attempts privilege escalation. Asserted by grepping for an executed `sudo` and expecting none. |
| T-26-31 | Spoofing | a stale image producing a false-green rehearsal | mitigate | PIT-4: the on-host `:2.1.0` images predate Phases 23/24/25. The script rebuilds all four with manifest-matching names and prints image identities into the evidence block, so "which code did we prove?" is answerable. `--skip-build` refuses when a `:local` tag is missing. |
| T-26-32 | Tampering | `scripts/deploy.sh` raw base applies | mitigate | Bypassing kustomize skipped the namespace transformer, the ConfigMap patch and the image-tag pin — any of which could deploy the wrong config to staging/production. Replaced with `apply -k` while preserving the rollout-status and `rollout undo` rollback behaviour. |
| T-26-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed. The `minio/mc` and `minio/minio` container images are already the mechanism compose uses, and the four application images are built from committed Dockerfiles. `26-RESEARCH.md` § Package Legitimacy Audit records the phase as N/A. |

## Other Quality Contracts

- Web performance (mobile-first): **N/A** — the frontend image build changes only baked env values; no bundle, route or asset change.
- SEO / discoverability: **N/A** — `.local` hosts are not internet-reachable and no public surface changed.
- AI agent-readiness: **N/A** — no endpoint, contract or OpenAPI change.
</threat_model>

<verification>
```bash
bash -n scripts/lib/k8s-local-guards.sh && bash -n scripts/k8s-local-secrets.sh && bash -n scripts/k8s-local-up.sh && bash -n scripts/deploy.sh
./scripts/deploy.sh dev all            # expect non-zero + pointer to k8s-local-up.sh
bash scripts/k8s-local-up.sh --dry-run-only
cd frontend && npx tsc --noEmit -p tsconfig.json && npm run build
bash k8s/scripts/check-no-plaintext-secrets.sh && bash k8s/scripts/check-render-invariants.sh
```
The full live rollout, boot-log assertions, STOMP proof, backup restore drill and ingress login are
plan 26-07's — this plan delivers and proves the tooling, up to and including a verbatim server
dry-run.

Known-deferred (unchanged): `scripts/docs-freshness.sh` check mode stays RED until plan 26-06.
</verification>

<success_criteria>
- `.env.example` documents all 15 new keys and the developer `.env` carries real values.
- `scripts/lib/k8s-local-guards.sh` provides the context, XOR, namespace and kubectl-wrapper helpers, and both scripts use them.
- The context guard is proven to refuse a non-local context without issuing a mutating call; the XOR guard is proven RED for a running app container and for a stopped backing service.
- `jtoye_backup` exists with `rolbypassrls = true`; the `jtoye-db-backups` bucket exists and returns 403 to an unauthenticated read while `jtoye-images` returns 200.
- Every consumed Secret exists in the local namespace, `postgres-credentials/username` decodes to the `jtoye_app` role, and a second run is a clean no-op.
- No authored script contains a host or port literal; no secret value is printed.
- `scripts/k8s-local-up.sh --dry-run-only` reaches the verbatim server dry-run (or halts at the `/etc/hosts` step having printed the exact remediation).
- `scripts/deploy.sh` rejects `dev` and `local`, applies via `-k`, and keeps its rollback logic.
- The relay spec's cookie domain follows its base URL; `tsc` and `npm run build` are clean and the Playwright block count is unchanged.
</success_criteria>

<output>
Create `.planning/phases/26-local-k8s-overlay-verified-breakage-fixes/26-05-SUMMARY.md` when done.
Record: every refusal message captured from the guard falsifiability probes, the `rolbypassrls`
query result, both MinIO bucket HTTP probe codes, the secret name/key inventory as created, the four
image identities printed by the evidence block, and the verbatim server dry-run output.
</output>
