---
phase: 26-local-k8s-overlay-verified-breakage-fixes
plan: 09
type: execute
wave: 9
depends_on: ["26-08"]
files_modified:
  - k8s/LOCAL.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/26-local-k8s-overlay-verified-breakage-fixes/26-VALIDATION.md
autonomous: true
requirements: [INFRA-01, INFRA-02]

must_haves:
  truths:
    - "The full test suite is green after every change this phase made, not just the tests each plan added"
    - "Every static gate and the docs-freshness gate pass on the final tree"
    - "The k8s/LOCAL.md evidence block has an actual recorded output for every live row, with no placeholder left"
    - "INFRA-01 and INFRA-02 are marked complete only with named, live proofs cited next to them"
    - "The validation strategy records the achieved status per row and is marked nyquist-compliant"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "INFRA-01 / INFRA-02 marked complete with cited proofs"
    - path: ".planning/phases/26-local-k8s-overlay-verified-breakage-fixes/26-VALIDATION.md"
      provides: "Per-row achieved status + nyquist_compliant true"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "k8s/LOCAL.md evidence block"
      via: "proof citation"
      pattern: "LOCAL\\.md"
---

<objective>
Close the phase: run the FULL regression suite over everything the phase changed, confirm no evidence
row is left unfilled, and mark INFRA-01 and INFRA-02 complete with named live proofs.

Purpose: this repo has a recorded pattern of green-by-construction failures — a verifier passing every
suite while missing a defect no test covered (Phase 24's CR-01), and per-plan executors running only
their own new tests and shipping a suite-wide regression (the scope-gate `integrationTest` trap). This
phase changed `application.yml`, so a full `:core-java:test` and `:core-java:integrationTest` run is
the only honest closer, even though no plan's own tests failed.

Output: a complete evidence block, a full-suite result, and requirement/roadmap/state/validation
records that a later reader can audit without re-running anything.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-local-k8s-overlay-verified-breakage-fixes/26-CONTEXT.md
@.planning/phases/26-local-k8s-overlay-verified-breakage-fixes/26-VALIDATION.md
@.planning/phases/26-local-k8s-overlay-verified-breakage-fixes/26-07-SUMMARY.md
@.planning/phases/26-local-k8s-overlay-verified-breakage-fixes/26-08-SUMMARY.md
@k8s/LOCAL.md

<interfaces>
<!-- Suite baselines and the closure contract. -->

Regression scope. This phase changed `core-java/src/main/resources/application.yml` (the STOMP
credential chain) and added one Java test file, so the whole JVM suite is in scope, not just the new
test. Run BOTH `./gradlew :core-java:test` and `./gradlew :core-java:integrationTest --no-daemon`
(the latter takes roughly 30-35 minutes and boots real Postgres via Testcontainers). Phase 23's
recorded baseline for `integrationTest` was 332 tests / 0 failures / 1 skipped across 81 classes;
Phase 25 shipped with `docs/metrics.json` total 1684 and this phase's plan 26-06 regenerated it — use
the regenerated value as the expected total, not a remembered number.

Frontend scope: `npm run build` (tsc — jest does NOT type-check, a recorded trap) plus the jest suite.
Plan 26-05 touched `frontend/e2e/stomp-relay.spec.ts` only, so no jest count should move.

Go scope: `edge-go` gained an env entry in a manifest, no Go source change — no Go suite run needed
unless `git diff` shows a `.go` file changed, in which case run it.

Anti-false-green requirements (why INFRA-01/02 were held pending across plans 26-01..26-08):
- INFRA-01 acceptance is `kubectl kustomize k8s/local` building AND a dry-run apply resolving all
  refs. Both are now proven — the static half by `check-render-invariants.sh` LOC-1..LOC-5 and the
  live half by plan 26-07's verbatim server dry-run.
- INFRA-02 acceptance spans four sub-items: (a) `DB_PORT` via `secretKeyRef` with no hardcoded port,
  (b) secrets using the NOSUPERUSER `DB_USER`/`DB_PASSWORD`, (c) the pg-backup CronJob targeting host
  MinIO for the PITR rehearsal, (d) STOMP relay credentials reaching Spring config. Each needs its
  own citation: (a) INV-1/INV-2 plus the live Deployment env, (b) the validator log AND the
  independent DB-side `rolsuper`/`pg_stat_activity` check, (c) the in-cluster job with
  `.status.succeeded == 1` AND the two-arm row-count falsification, (d) the env-contract gate
  direction (a) plus the zero `Access refused for user` count plus the broker-side STOMP identity
  showing the dedicated login and no guest connection.

Do NOT mark a requirement complete by citing a plan number alone. Cite the assertion and its recorded
result — that is the difference between a proof and a claim.

ROADMAP bookkeeping: Phase 26's "Plans: TBD (est. 2)" line and its two-item plan list are stale — this
phase shipped nine plans across nine waves. Update the count, replace the plan list with the actual
nine entries and their one-line objectives, mark them all complete, and update the Progress table row
for Phase 26 (`0/2 Not started` → `9/9 Complete` with the date). Also update the "Milestone v2.3 Phase
Map" estimate note in `.planning/STATE.md` so the 2-plan estimate does not read as the delivered
shape, and add a Roadmap-Evolution entry explaining WHY the phase expanded: DEF-6/D-15 was scoped at
"roughly one extra plan" in CONTEXT.md, research added D-17/D-18/D-19 plus a golden-render harness and
an env-contract gate, and planning discovered two further live blockers (the realm redirect URI and
the hardcoded frontend OIDC client id).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: full regression sweep + evidence-block completeness check</name>
  <files>k8s/LOCAL.md</files>
  <read_first>
    - k8s/LOCAL.md (the whole evidence block, to find any row still lacking an actual output)
    - .planning/phases/26-local-k8s-overlay-verified-breakage-fixes/26-VALIDATION.md § Per-Task Verification Map (the authoritative row list the evidence block must cover)
    - .planning/phases/26-local-k8s-overlay-verified-breakage-fixes/26-07-SUMMARY.md and 26-08-SUMMARY.md (the recorded live outputs to reconcile against the block)
    - core-java/build.gradle.kts lines 106-146 (the test vs integrationTest task split and the testcontainers tag exclusion, so the right commands are run)
    - docs/metrics.json (the post-26-06 counts the suite results must be consistent with)
  </read_first>
  <action>
Run the full regression set and record each result with its actual numbers, not a description:
- `./gradlew :core-java:test --no-daemon`
- `./gradlew :core-java:integrationTest --no-daemon` (expect ~30-35 minutes; record the class count,
  test count, failures, errors and skips)
- `cd frontend && npm run build` (the tsc gate — jest does not type-check)
- the frontend jest suite
- `bash scripts/docs-freshness.sh`
- all five k8s static gates: `check-no-plaintext-secrets.sh`, `check-connection-math.sh`,
  `check-env-contract.sh`, `check-render-invariants.sh`, `render-golden.sh`
- `git diff --name-only` against the phase's base commit, to confirm no `.go` source changed; run the
  Go suite only if one did.

If anything fails, fix it and re-run rather than recording a known-red gate. If a failure is genuinely
out of this phase's scope, record it as an explicit finding with a deferred item and say why it is out
of scope — do not leave it unexplained.

Then audit `k8s/LOCAL.md`'s evidence block against `26-VALIDATION.md`'s Per-Task Verification Map:
every row typed **live** must have a recorded command AND its actual output. Fill any gap from the
26-07 / 26-08 summaries, or re-run the command. Confirm the anti-anecdote rules hold on the finished
block: the four image identities are present, every negative is expressed as a count, both backup arms
are recorded, no URL is a `localhost` app URL, and the dry-run output is verbatim.

Finally, confirm no secret value appears in the tracked runbook: grep the block for the decoded values
of the local secrets and expect zero hits.
  </action>
  <verify>
    <automated>bash k8s/scripts/check-no-plaintext-secrets.sh && bash k8s/scripts/check-connection-math.sh && bash k8s/scripts/check-env-contract.sh && bash k8s/scripts/check-render-invariants.sh && bash k8s/scripts/render-golden.sh && bash scripts/docs-freshness.sh && ./gradlew :core-java:test --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :core-java:test --no-daemon` BUILD SUCCESSFUL with 0 failures; the executed test count is recorded.
    - `./gradlew :core-java:integrationTest --no-daemon` BUILD SUCCESSFUL with 0 failures and 0 errors; the class/test/skip counts are recorded and compared against Phase 23's 81 classes / 332 tests / 1 skipped baseline, with any delta explained.
    - `cd frontend && npm run build` exits 0.
    - The frontend jest suite passes with 0 failures; the block count matches `docs/metrics.json`'s `jest_blocks`.
    - `bash scripts/docs-freshness.sh` exits 0.
    - All five k8s static gates exit 0, each recorded individually.
    - `git diff --name-only <phase-base>..HEAD -- '*.go'` is empty, or the Go suite was run and passed.
    - Every **live** row in `26-VALIDATION.md`'s map has a corresponding filled row in `k8s/LOCAL.md` with a non-empty actual output; the count of live rows and filled rows is stated and equal.
    - `grep -c 'TODO\|TBD\|<fill\|_____' k8s/LOCAL.md` returns 0 within the evidence block.
    - `grep -c 'localhost:9090\|localhost:3000' k8s/LOCAL.md` returns 0 within the evidence block.
    - The evidence block contains four image identities and both backup arms' row counts.
    - For each local secret key, the decoded value does not appear in `k8s/LOCAL.md` — verify with a loop over the secret keys and expect zero grep hits.
  </acceptance_criteria>
  <done>The full JVM, frontend and static-gate sets are green with recorded numbers; the evidence block has an actual output for every live row, carries image identities and both backup arms, and leaks no secret value.</done>
</task>

<task type="auto">
  <name>Task 2: mark INFRA-01 / INFRA-02 complete with cited proofs; reconcile roadmap, state and validation</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/26-local-k8s-overlay-verified-breakage-fixes/26-VALIDATION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 74-84 (the INFRA-01 / INFRA-02 entries verbatim, incl. INFRA-02's (a)-(d) sub-items and their "Source: HANDOFF k8s breakage list #N" provenance, plus the completion-note style used for AI-02)
    - .planning/ROADMAP.md § "Phase 26" (the goal, the four success criteria, the stale "Plans: TBD (est. 2)" line and two-item plan list) and the Progress table row for Phase 26
    - .planning/STATE.md (the frontmatter progress block, the Current Position section, the Milestone v2.3 Phase Map estimate column, the Performance Metrics by-phase table, and the Roadmap Evolution list)
    - .planning/phases/26-local-k8s-overlay-verified-breakage-fixes/26-VALIDATION.md (the frontmatter `nyquist_compliant: false` / `wave_0_complete: false` flags, the Status column, and the Validation Sign-Off checklist)
    - all nine 26-0N-SUMMARY.md files (the per-plan objectives and recorded proofs the roadmap list and requirement citations must reflect)
    - k8s/LOCAL.md (the finished evidence block being cited)
  </read_first>
  <action>
**REQUIREMENTS.md.** Mark INFRA-01 and INFRA-02 complete in the style the AI-02 entry uses: keep the
original requirement text verbatim, then append a dated completion note. For INFRA-01, cite the
static build/gate proof and the live server dry-run. For INFRA-02, cite each sub-item (a), (b), (c),
(d) SEPARATELY with the specific assertion and its recorded result per `<interfaces>` — a plan number
alone is not a proof. Name `k8s/LOCAL.md`'s evidence block as the location of the live outputs.

**ROADMAP.md.** Replace `**Plans:** TBD (est. 2)` with the delivered count and wave structure, and
replace the two-item plan list with the actual nine entries, each ticked and carrying a one-line
objective. Update the Progress table row for Phase 26 from `0/2 Not started` to `9/9 Complete` with
the completion date. Confirm each of the four Phase 26 success criteria is satisfiable from the
recorded evidence and append a short proof pointer to each — if any criterion is NOT fully met, say so
explicitly rather than ticking it.

**STATE.md.** Update the frontmatter progress block (completed phases, total/completed plans,
percent), the `status` and `stopped_at` fields, and the Current Position section with a Phase 26
status entry in the established style. Update the Milestone v2.3 Phase Map's estimate column for
Phase 26 so the stale 2-plan estimate does not read as the delivered shape, and add the by-phase
Performance Metrics row. Add a Roadmap Evolution entry explaining WHY the phase expanded from the
2-plan estimate to nine, using the four reasons in `<interfaces>`.

**26-VALIDATION.md.** Fill the Status column for every row in the Per-Task Verification Map with the
achieved state, tick the Wave 0 Requirements checklist (each item now exists — name the file that
satisfies it), tick the Manual-Only Verifications rows with a pointer to the recorded evidence, tick
the Validation Sign-Off checklist, and set `nyquist_compliant: true` and `wave_0_complete: true` in
the frontmatter. Where a row was satisfied differently from the original plan — notably the STOMP
relay row, which `26-CONTEXT.md` expected `stomp-relay.spec.ts` to cover but which was instead proven
at the broker plus a real-login browser journey — record the substitution and its reason rather than
silently ticking it.

Do not touch `docs/metrics.json` (plan 26-06 was its single writer) and do not modify any dated
document (`k8s/PRODUCTION_READINESS_REPORT.md`, `docs/runbooks/backups.md`'s 2026-07-10 section).
  </action>
  <verify>
    <automated>grep -c '\[x\] \*\*INFRA-0[12]\*\*' .planning/REQUIREMENTS.md && grep -c 'nyquist_compliant: true' .planning/phases/26-local-k8s-overlay-verified-breakage-fixes/26-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[x\] \*\*INFRA-01\*\*' .planning/REQUIREMENTS.md` is 1 and the same for INFRA-02.
    - The INFRA-02 completion note contains four separately-cited sub-item proofs; verify by grepping the note for `(a)`, `(b)`, `(c)` and `(d)`.
    - Both completion notes reference `k8s/LOCAL.md` as the evidence location.
    - The original INFRA-01 / INFRA-02 requirement text is unchanged: `git diff .planning/REQUIREMENTS.md | grep '^-' | grep -c 'Source: HANDOFF'` returns 0.
    - `grep -c 'TBD (est. 2)' .planning/ROADMAP.md` returns 0.
    - The ROADMAP Phase 26 plan list has nine ticked entries: `awk '/### Phase 26/,/^### |^## Progress/' .planning/ROADMAP.md | grep -c '^- \[x\] 26-0'` returns 9.
    - The ROADMAP Progress table row for Phase 26 reads `9/9` and `Complete` with a date: `grep -c '26. Local-K8s Overlay.*9/9.*Complete' .planning/ROADMAP.md` is 1.
    - Each of the four Phase 26 success criteria has an appended proof pointer, or an explicit statement that it is not fully met.
    - `.planning/STATE.md` frontmatter shows `completed_phases: 6` and a `total_plans`/`completed_plans` pair increased by 9 versus its pre-phase values; the percent is recalculated.
    - `.planning/STATE.md` gained a Roadmap Evolution entry naming all four expansion reasons.
    - `grep -c 'nyquist_compliant: true' .../26-VALIDATION.md` is 1 and `grep -c 'wave_0_complete: true'` is 1.
    - `grep -c '⬜ pending' .../26-VALIDATION.md` returns 0 — every Status cell is resolved.
    - The Wave 0 Requirements checklist in `26-VALIDATION.md` has all five items ticked, each naming the file that satisfies it.
    - The STOMP relay row records the substituted proof and its reason: `grep -c 'stomp-relay.spec' .../26-VALIDATION.md` is >= 1 with the substitution stated.
    - `git diff --quiet docs/metrics.json k8s/PRODUCTION_READINESS_REPORT.md` is true.
    - All five static gates plus `scripts/docs-freshness.sh` still exit 0 after these edits.
  </acceptance_criteria>
  <done>INFRA-01 and INFRA-02 are marked complete with per-sub-item cited proofs pointing at recorded live outputs; the roadmap reflects nine delivered plans with a completion date; STATE.md explains the expansion; the validation strategy is fully resolved and nyquist-compliant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded evidence → project record | Requirement closure becomes the basis on which later phases assume this infrastructure works |
| per-plan test runs → whole-suite reality | A plan that only runs its own tests can ship a suite-wide regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-53 | Repudiation | requirement closure without a proof | mitigate | ASVS V7. Each INFRA-02 sub-item must cite its own assertion and recorded result; a plan number alone is rejected. The criteria assert all four sub-item citations exist. |
| T-26-54 | Tampering | a suite-wide regression shipped green | mitigate | The recorded repo pattern is that per-plan executors run only their own new tests. This phase changed `application.yml`, so the closer runs BOTH `:core-java:test` and `:core-java:integrationTest` in full, plus the frontend tsc gate, and compares against the recorded baseline. |
| T-26-55 | Information Disclosure | secret values pasted into the tracked runbook | mitigate | ASVS V7. Task 1 greps the finished evidence block for the decoded value of every local secret key and requires zero hits. `k8s/LOCAL.md` is tracked and GitGuardian is not a required check, so a leak would ship. |
| T-26-56 | Repudiation | a silently-substituted verification | mitigate | The STOMP relay row was expected to be covered by `stomp-relay.spec.ts` and was instead proven at the broker plus a real-login journey. The substitution and its reason must be recorded in `26-VALIDATION.md`, not silently ticked. |
| T-26-57 | Repudiation | rewriting a dated record | mitigate | `docs/metrics.json` has a single writer (26-06) and the dated audit plus the 2026-07-10 backup results are untouched — asserted by `git diff --quiet`. |
| T-26-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed. `26-RESEARCH.md` § Package Legitimacy Audit records the phase as N/A. |

## Other Quality Contracts

- Web performance (mobile-first): **N/A** — no user-facing route, bundle or asset changed anywhere in this phase.
- SEO / discoverability: **N/A** — no public/unauthenticated surface changed.
- AI agent-readiness: **N/A** — no endpoint, contract or OpenAPI change; `docs/api/openapi-snapshot.json` needs no regeneration because no controller changed.
</threat_model>

<verification>
```bash
./gradlew :core-java:test --no-daemon
./gradlew :core-java:integrationTest --no-daemon
cd frontend && npm run build && npx jest --ci && cd ..
bash scripts/docs-freshness.sh
for g in check-no-plaintext-secrets check-connection-math check-env-contract check-render-invariants render-golden; do
  bash "k8s/scripts/$g.sh" || echo "FAILED: $g"
done
grep -c '⬜ pending' .planning/phases/26-local-k8s-overlay-verified-breakage-fixes/26-VALIDATION.md   # expect 0
```
Expected: every suite and gate green; zero unresolved validation rows.
</verification>

<success_criteria>
- `:core-java:test` and `:core-java:integrationTest` both BUILD SUCCESSFUL with 0 failures and 0 errors, counts recorded and compared to baseline.
- `npm run build` and the jest suite are green; `docs-freshness.sh` exits 0.
- All five k8s static gates exit 0.
- Every live row in `26-VALIDATION.md` has a filled evidence row in `k8s/LOCAL.md`; no placeholder and no `localhost` app URL remains; no secret value appears.
- INFRA-01 and INFRA-02 are marked complete with cited proofs, INFRA-02 citing all four sub-items separately.
- ROADMAP shows nine ticked plans, a `9/9 Complete` progress row with a date, and a proof pointer on each of the four success criteria.
- STATE.md progress, position, phase-map estimate, metrics row and a Roadmap Evolution entry explaining the expansion are all updated.
- `26-VALIDATION.md` has no `⬜ pending` cell, all Wave 0 items ticked, the relay-proof substitution recorded, and `nyquist_compliant: true`.
</success_criteria>

<output>
Create `.planning/phases/26-local-k8s-overlay-verified-breakage-fixes/26-09-SUMMARY.md` when done.
Record: the exact suite numbers for both Gradle tasks plus jest, the count of live evidence rows
filled, the four INFRA-02 sub-item citations verbatim, and the Roadmap Evolution text added.
</output>
