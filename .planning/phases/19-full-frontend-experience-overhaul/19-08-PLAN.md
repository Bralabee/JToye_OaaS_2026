---
phase: 19-full-frontend-experience-overhaul
plan: 08
type: execute
wave: 3
depends_on: [19-04, 19-05, 19-06, 19-07]
files_modified:
  - frontend/app/dashboard/finance/page.tsx
  - frontend/app/dashboard/kitchen/page.tsx
  - frontend/app/dashboard/orders/page.tsx
  - frontend/app/dashboard/page.tsx
  - frontend/app/dashboard/products/page.tsx
  - frontend/components/dashboard/orders/OrderDetailPanel.tsx
  - frontend/components/dashboard/sidebar.tsx
  - frontend/app/shop/orders/page.tsx
  - frontend/app/shop/[slug]/cart/page.tsx
  - frontend/app/shop/[slug]/checkout/page.tsx
  - frontend/app/shop/[slug]/orders/[orderNumber]/page.tsx
  - frontend/app/shop/[slug]/page.tsx
  - frontend/app/track/page.tsx
  - frontend/components/marketing/business-model-guide.tsx
  - frontend/components/marketing/operator-pitch.tsx
  - frontend/app/api/customer-auth/session/route.ts
  - frontend/lib/customer-auth.ts
  - frontend/__tests__/palette-discipline.test.ts
autonomous: true
requirements: [UIX-06]

must_haves:
  truths:
    - "The undocumented purple hue is gone app-wide — PREPARING/VAT purple → amber, stat/gradient/avatar purple → blue (grep purple- == 0)"
    - "The off-scale text-[10px] is gone app-wide — every occurrence → text-xs (grep text-[10px] == 0)"
    - "Public page loads no longer emit the expected-401 customer-session console noise (real source proven live, minimal fix)"
    - "A committed discipline test asserts the palette/type/IA grep gates so they cannot regress"
  artifacts:
    - path: "frontend/__tests__/palette-discipline.test.ts"
      provides: "grep-gate assertions (purple==0, text-[10px]==0, marketing hex==0, href=/track >=3)"
      contains: "purple"
  key_links:
    - from: "__tests__/palette-discipline.test.ts"
      to: "app + components"
      via: "static grep gates fail on any purple/text-[10px]/marketing-hex regression"
      pattern: "purple-"
---

<objective>
Close UIX-06 discipline items #10, #11, #13 as the final cross-cutting sweep (this wave runs AFTER every structural plan so it does not conflict with them):

1. **Purple removal (#10):** purple is an undocumented 7th hue (17 hits across 7 files). Map every one per the UI-SPEC exhaustive list — `PREPARING`/VAT purple → amber; stat-accent/gradient/avatar purple → blue.
2. **text-[10px] (#11):** 36 off-scale occurrences across 9 files → `text-xs` (12px accessibility floor). No sub-12px token.
3. **Quiet the 401 probe (#13) — VERIFY-FIRST:** the audit reported repeated expected-401 console noise from customer-session probing. Grep on 2026-07-11 found ZERO `console.*` in `lib/customer-auth.ts` AND `lib/api-client.ts` — the noise is likely the browser auto-logging the `/api/customer-auth/session` 401 response, not a `console.error`. Reproduce it LIVE, prove the real source, and fix only what's proven (may already be resolved).
4. **Discipline test:** codify the palette/type/IA grep gates so they cannot silently regress.

Purpose: converge the app onto one documented palette + type scale and a clean public console.
Output: purple==0, text-[10px]==0, a quiet public console, and a committed discipline test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-full-frontend-experience-overhaul/19-UI-SPEC.md
@.planning/phases/19-full-frontend-experience-overhaul/19-PATTERNS.md
@.planning/phases/19-full-frontend-experience-overhaul/19-RESEARCH.md

<interfaces>
<!-- The EXHAUSTIVE purple map (UI-SPEC § Color #10). Every hit enumerated. -->
PREPARING → amber: kitchen/page.tsx:46,59 ; dashboard/orders/page.tsx:98,99,183 ; dashboard/page.tsx:90 (chartColor "#a855f7" → "#f59e0b") ; OrderDetailPanel.tsx:57.
Finance VAT accent → amber: finance/page.tsx:168,171.
Stat accents → blue: dashboard/page.tsx:214 (purple-100/purple-600 → blue-100/blue-600) ; products/page.tsx:411-412 (purple-100/purple-600 → blue).
Gradients → drop purple: dashboard/orders/page.tsx:485 (from-blue-50 to-purple-50 → to-blue-100) ; sidebar.tsx:70 avatar (from-blue-400 to-purple-500 → to-blue-600).
Net gate: `grep -rn "purple-" app components` == 0 AND `grep -rno "purple-[0-9]*" app components` == 0 (down from 17).

text-[10px] (36 across 9 files) → text-xs: app/dashboard/products/page.tsx, app/shop/orders/page.tsx, app/shop/[slug]/cart/page.tsx, app/shop/[slug]/checkout/page.tsx, app/shop/[slug]/orders/[orderNumber]/page.tsx, app/shop/[slug]/page.tsx, app/track/page.tsx, components/marketing/business-model-guide.tsx, components/marketing/operator-pitch.tsx. Gate: `grep -rn "text-\[10px\]" app components` == 0. (Also fold in any residual text-[9px] on track.)

401 probe: app/api/customer-auth/session/route.ts returns 401 for no-session; lib/customer-auth.ts consumes it (silent catches). VERIFY-FIRST — browsers log 4xx responses to console automatically; suppressing requires the probe to return a non-error status (e.g. 200 {authenticated:false}) — but confirm live before changing, and keep the customer-auth session tests green.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove purple app-wide (exhaustive map → amber/blue)</name>
  <files>frontend/app/dashboard/finance/page.tsx, frontend/app/dashboard/kitchen/page.tsx, frontend/app/dashboard/orders/page.tsx, frontend/app/dashboard/page.tsx, frontend/app/dashboard/products/page.tsx, frontend/components/dashboard/orders/OrderDetailPanel.tsx, frontend/components/dashboard/sidebar.tsx</files>
  <read_first>
    - 19-UI-SPEC.md § Color "Backlog #10 — remove the undocumented purple" (the exhaustive per-line map)
    - each target file's purple lines (confirm current line numbers — they may have shifted after Wave 2 edits to kitchen/OrderDetailPanel/sidebar)
  </read_first>
  <action>
    Apply the exhaustive UI-SPEC purple map: `PREPARING` status + Finance VAT purple → amber (`bg-amber-500`, `text-amber-600/700`, chart `#f59e0b`); the Products/Dashboard stat accents purple → blue (`bg-blue-100`/`text-blue-600`); `dashboard/orders/page.tsx` gradient `to-purple-50` → `to-blue-100`; `sidebar.tsx:70` avatar gradient `to-purple-500` → `to-blue-600`. `CONFIRMED` stays blue, `READY` stays emerald — amber keeps `PREPARING` distinct. Re-confirm each line number in the read_first files (Wave 2 may have shifted kitchen/OrderDetailPanel/sidebar). Do NOT touch text-[10px] here (Task 2). End state: `grep -rn "purple-" app components` == 0.
  </action>
  <acceptance_criteria>
    - `grep -rn "purple-" frontend/app frontend/components | wc -l` == 0.
    - `grep -rno "purple-[0-9]*" frontend/app frontend/components | wc -l` == 0.
    - `grep -c "a855f7" frontend/app/dashboard/page.tsx` == 0 (chartColor swapped to amber #f59e0b).
    - `npm run build` succeeds; PREPARING renders amber, CONFIRMED blue, READY emerald (assert via kitchen/orders tests still green).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && [ "$(grep -rn 'purple-' app components | wc -l)" -eq 0 ] && npm run build</automated>
  </verify>
  <done>Zero purple app-wide; PREPARING/VAT amber, stat/gradient/avatar blue; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Replace text-[10px] with text-xs app-wide</name>
  <files>frontend/app/dashboard/products/page.tsx, frontend/app/shop/orders/page.tsx, frontend/app/shop/[slug]/cart/page.tsx, frontend/app/shop/[slug]/checkout/page.tsx, frontend/app/shop/[slug]/orders/[orderNumber]/page.tsx, frontend/app/shop/[slug]/page.tsx, frontend/app/track/page.tsx, frontend/components/marketing/business-model-guide.tsx, frontend/components/marketing/operator-pitch.tsx</files>
  <read_first>
    - 19-UI-SPEC.md § Spacing "KILL text-[10px] (backlog #11)" (replace every occurrence with text-xs; no sub-12px token)
    - each target file (confirm remaining text-[10px]/text-[9px] after Wave 2 rewrites of checkout/track/marketing/shop)
  </read_first>
  <action>
    Replace every remaining `text-[10px]` (and any `text-[9px]` on track) with `text-xs` across the 9 files. Some occurrences were already removed by Wave 2 (checkout footnote, marketing re-skin, track rewrite) — handle only the residuals. Do NOT add a sub-12px token (12px is the accessibility floor). Do NOT touch purple (Task 1). End state: `grep -rn "text-\[10px\]" app components` == 0.
  </action>
  <acceptance_criteria>
    - `grep -rn "text-\[10px\]" frontend/app frontend/components | wc -l` == 0.
    - `grep -rn "text-\[9px\]" frontend/app frontend/components | wc -l` == 0.
    - No new sub-12px arbitrary size introduced (grep: `grep -rnoE "text-\[[0-9]px\]" app components | wc -l` == 0).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && [ "$(grep -rn 'text-\[10px\]' app components | wc -l)" -eq 0 ] && [ "$(grep -rnoE 'text-\[[0-9]px\]' app components | wc -l)" -eq 0 ] && npm run build</automated>
  </verify>
  <done>Zero text-[10px]/text-[9px] app-wide; all replaced with text-xs; no sub-12px token added; build green.</done>
</task>

<task type="auto">
  <name>Task 3: VERIFY-FIRST quiet the 401 probe + palette/type/IA discipline test</name>
  <files>frontend/app/api/customer-auth/session/route.ts, frontend/lib/customer-auth.ts, frontend/__tests__/palette-discipline.test.ts</files>
  <read_first>
    - frontend/app/api/customer-auth/session/route.ts (returns 401 for no-session — the likely browser-console noise source)
    - frontend/lib/customer-auth.ts (session consumer; silent catches at :76,86,99,115,158,306,341,364,377 — no console.*)
    - frontend/lib/api-client.ts:72-107 (dashboard 401 interceptor — NOT the public path; do not change)
    - frontend/app/api/customer-auth/__tests__/route.test.ts + any session-callback test (keep green)
    - 19-PATTERNS.md § "lib/customer-auth.ts (EVOLVE — #13 quiet 401) — VERIFY FIRST" (the discrepancy: no console.* found; confirm the real source live before fixing)
  </read_first>
  <action>
    VERIFY-FIRST: with the stack running, load a public page (e.g. `/` or `/shop`) as an unauthenticated visitor and open the browser console. If the repeated expected-401 noise reproduces, prove the source (most likely the browser auto-logging the `/api/customer-auth/session` 401 response, since no `console.*` exists in the JS). If proven, apply the MINIMAL fix: have the session probe return `200 {authenticated:false}` for the no-session/expired case (so the browser does not log a failed request), and update `lib/customer-auth.ts` (and any consumer) to read the body's `authenticated` flag instead of the status — keeping all existing customer-auth session tests green. If the noise does NOT reproduce (already resolved), do NOT change any code — document #13 as already-closed with the live evidence in the SUMMARY. Either way, add `frontend/__tests__/palette-discipline.test.ts` asserting the four grep gates: `purple-` == 0, `text-[10px]` == 0, `#hex` in components/marketing == 0, and `href="/track"` >= 3 (so palette/type/IA cannot regress).
  </action>
  <acceptance_criteria>
    - Live evidence recorded (screenshot/console log) of the 401-noise reproduction attempt; the SUMMARY states whether a code fix was applied or #13 was already-closed.
    - If fixed: `app/api/customer-auth/session/route.ts` returns 200 with `authenticated:false` for no-session, consumers updated, and `npx jest app/api/customer-auth/__tests__/route.test.ts` passes.
    - `palette-discipline.test.ts` asserts purple==0, text-[10px]==0, marketing-hex==0, href=/track>=3 and passes.
    - `npx jest __tests__/palette-discipline.test.ts` passes; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx jest __tests__/palette-discipline.test.ts app/api/customer-auth/__tests__/route.test.ts && npm run build</automated>
  </verify>
  <done>The expected-401 public console noise is quieted (or proven already-resolved with evidence) with no session-test regression; a discipline test locks the grep gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/customer-auth/session (public probe) | must not leak session data; changing 401→200 must not weaken any auth decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08-01 | Spoofing/Elevation | 401→200 probe weakens an auth gate | mitigate | The probe is a client-side convenience read only; the authoritative gate is server-side (dashboard/layout.tsx auth() + core-java RLS); body `authenticated:false` conveys no session; all customer-auth tests kept green |
| T-19-08-02 | Info Disclosure | session probe body leaks profile when unauthenticated | mitigate | No-session response carries only `{authenticated:false}` — no profile/claims (existing contract preserved) |
| T-19-08-03 | Tampering | palette regression re-introduces off-brand hues | mitigate | Committed palette-discipline test fails CI on any purple/text-[10px]/marketing-hex/orphan-track regression |
| T-19-08-SC | Tampering | supply chain | accept | No package installs (mechanical edits + one test) |
</threat_model>

<verification>
Scoped: the four grep gates (purple==0, text-[10px]==0, marketing-hex==0, href=/track>=3) via `__tests__/palette-discipline.test.ts` + `npm run build`. Confirm kitchen/orders/marketing tests still green after the sweep. Full suite at closure (19-09).
</verification>

<success_criteria>
- purple- == 0 and text-[10px] == 0 app-wide; no sub-12px token added.
- Expected-401 public console noise quieted (or evidenced as already-resolved) with no session-test regression.
- A committed discipline test locks the palette/type/IA grep gates.
</success_criteria>

<output>
Create `.planning/phases/19-full-frontend-experience-overhaul/19-08-SUMMARY.md` when done. Commit (do NOT push).
</output>
