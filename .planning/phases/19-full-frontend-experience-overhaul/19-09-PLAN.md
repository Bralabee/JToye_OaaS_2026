---
phase: 19-full-frontend-experience-overhaul
plan: 09
type: execute
wave: 4
depends_on: [19-08]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - docs/metrics.json
  - CHANGELOG.md
  - .planning/STATE.md
autonomous: false
requirements: [UIX-01, UIX-02, UIX-03, UIX-04, UIX-05, UIX-06]

must_haves:
  truths:
    - "UIX-01..UIX-06 are registered in REQUIREMENTS.md with phase/plan traceability"
    - "docs/metrics.json is reconciled (schema_version 43→45; new jest/playwright/java counts) and the docs-freshness gate passes"
    - "The full gate is green: gradle test + integrationTest, jest, npm run build, Playwright mobile+desktop against a freshly-rebuilt Docker stack, and every UIX grep gate"
    - "All 15 audit backlog items are closed or explicitly deferred (only #14 error boundary is a documented leave-as-is)"
    - "The 6 ROADMAP success criteria are visually confirmed on a real 390px + desktop viewport (images naturalWidth>0, palette locked orange/emerald/slate)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "UIX-01..06 registration + traceability"
      contains: "UIX-01"
    - path: "docs/metrics.json"
      provides: "reconciled counts + schema_version 45"
      contains: "schema_version"
  key_links:
    - from: "docs/metrics.json"
      to: "scripts/docs-freshness.sh"
      via: "--write reconcile (generator is the arbiter)"
      pattern: "schema_version"
---

<objective>
Close the phase: register UIX-01..UIX-06 in REQUIREMENTS.md, reconcile the docs-freshness counts (schema 43→45 + new test counts across all plans), run the full gate against a freshly-rebuilt Docker stack, and confirm the 6 ROADMAP success criteria in a whole-app browser UAT.

This is the ONLY plan that touches docs/metrics.json (the generator is the arbiter — a single authoritative reconcile after all plans merge) and the ONLY plan that runs the full suite + palette grep gates + live Playwright. Backlog #14 (generic error boundary) is an explicit LEAVE-AS-IS — no code task; documented here.

Purpose: prove the whole overhaul lands green with zero regression of the 921 logical invocations and comparator-grade quality.
Output: registered requirements, reconciled metrics, a green full gate, and a signed-off browser UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-full-frontend-experience-overhaul/19-RESEARCH.md
@.planning/phases/19-full-frontend-experience-overhaul/19-VALIDATION.md
@.planning/phases/18-vendor-onboarding-first-slice/18-UI-REVIEW.md

<interfaces>
<!-- The 6 ROADMAP success criteria → UIX-01..06 (ROADMAP.md:207-213). Register verbatim. -->
UIX-01 landing + shared shell + zero orphans (link-graph). UIX-02 all 11 dashboard routes at 390px. UIX-03 real product names (OrderItem snapshot). UIX-04 checkout address + fee-before-pay (V45). UIX-05 per-shop menus (shop_id + scoped query). UIX-06 all 15 backlog items closed/deferred + 921 invocations green + palette locked.
docs/metrics.json current: schema_version 43, jest_blocks 130, playwright_blocks 23, java_test_methods 693, total_logical_invocations 921. Regenerate via scripts/docs-freshness.sh (--write); the CLAUDE.md count narrative must stay consistent. V44 is absent (reserved for #96) — schema_version becomes 45 (max applied migration).
Backlog #14: LEAVE-AS-IS (app/error.tsx generic copy — acceptable last-resort per UI-SPEC § Interaction Contracts).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register UIX-01..06 + ROADMAP/CHANGELOG/STATE + docs-freshness reconcile</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, CHANGELOG.md, .planning/STATE.md, docs/metrics.json</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the register format + Traceability table to extend; note it is titled v2.2 — add a v2.3 UIX section)
    - .planning/ROADMAP.md:203-215 (Phase 19 entry + success criteria → UIX mapping) + the Progress table row
    - CHANGELOG.md (the [Unreleased] entry convention from Phase 16.1/18)
    - scripts/docs-freshness.sh + docs/metrics.json (the generator + current counts)
    - 18-UI-REVIEW.md § Remediation Backlog (the 15 items — map each to its closing plan / leave-as-is)
  </read_first>
  <action>
    In REQUIREMENTS.md, add a "Experience overhaul (UIX) — v2.3" section registering UIX-01..UIX-06 verbatim from the ROADMAP Phase 19 success criteria, each with its closing plan(s), and add the 6 rows to the Traceability table (UIX-01 → 19-03/19-05; UIX-02 → 19-04; UIX-03 → 19-01/19-07; UIX-04 → 19-01/19-06; UIX-05 → 19-02; UIX-06 → 19-07/19-08/19-09). Include a short "15-item backlog closure" note mapping each backlog item to its plan (and #14 as explicit LEAVE-AS-IS). In ROADMAP.md, mark the Phase 19 plans list (9 plans) + Progress table row (0/9 → 9/9 on completion) + goal status. Add a CHANGELOG.md [Unreleased] entry for Phase 19. Update STATE.md Current Position to Phase 19 complete. Run `scripts/docs-freshness.sh --write` to reconcile docs/metrics.json (schema_version 43→45; new jest/playwright/java counts from all plans) and confirm the CLAUDE.md count narrative stays consistent. The generator is the arbiter — if metrics.json shows a merge conflict from parallel plans, re-run to resolve.
  </action>
  <acceptance_criteria>
    - REQUIREMENTS.md contains UIX-01..UIX-06 with descriptions + Traceability rows mapping each to its plan(s).
    - REQUIREMENTS.md documents backlog #14 as LEAVE-AS-IS with reason; all other 14 items mapped to a closing plan.
    - docs/metrics.json `schema_version` == 45; counts regenerated (jest_blocks/playwright_blocks/java_test_methods increased to match added tests).
    - `scripts/docs-freshness.sh` exits 0 (no drift).
    - ROADMAP Phase 19 shows 9 plans; STATE + CHANGELOG updated.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/sanmi/IdeaProjects/JToye_OaaS_2026 && grep -c "UIX-0" .planning/REQUIREMENTS.md && bash scripts/docs-freshness.sh && grep '"schema_version": 45' docs/metrics.json</automated>
  </verify>
  <done>UIX-01..06 registered with traceability; #14 documented leave-as-is; metrics reconciled (schema 45); docs-freshness green.</done>
</task>

<task type="auto">
  <name>Task 2: Full gate — backend + frontend suites + palette grep gates + Playwright vs rebuilt Docker</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - 19-VALIDATION.md § "Before /gsd:verify-work" (full suite + docs-freshness + browser UAT; SSE pages domcontentloaded; images naturalWidth>0)
    - 19-RESEARCH.md § Regression Tripwires (the full list) + § Pitfall 8 (confirm the port the stack binds) + § Environment Availability (Stripe keys absent — verify fee-before-payment UI + COD path, not a live card)
    - CLAUDE.md (Docker: rebuild ALL containers after code changes before E2E)
  </read_first>
  <action>
    Rebuild ALL Docker containers, bring the stack up, and confirm the frontend port it binds (3000 or 3100 — do not assume). Run the FULL gate: `cd core-java && ./gradlew test integrationTest`; `cd frontend && npm test && npm run build`; the UIX grep gates (`grep -rn "purple-" app components` == 0, `text-[10px]` == 0, `grep -rlE "#[0-9a-fA-F]{3,8}" components/marketing/*.tsx` == 0, `grep -rn 'href="/track"' app components` >= 3); and Playwright `--project=mobile` + `--project=desktop` (landing, dashboard-mobile, storefront-flows, kitchen-flow, csp-no-violations, header-snapshot) against the rebuilt stack using `domcontentloaded` for SSE/live pages. With Stripe test keys absent, verify the fee-before-payment UI + COD confirmation path rather than a live card charge. Record the final counts in STATE.md. Fix any regression from the tripwire list before proceeding.
  </action>
  <acceptance_criteria>
    - `./gradlew test integrationTest` BUILD SUCCESSFUL (incl. OrderFulfilmentAudit, ProductRepositoryScoping, GdprErasure, RlsContractTest, FTS/#96).
    - `npm test` all green (incl. landing, link-graph, checkout, palette-discipline, dashboard-shell, marketing) and `npm run build` (tsc) succeeds.
    - All four UIX grep gates satisfied.
    - Playwright mobile+desktop green against the rebuilt stack; csp-no-violations green; no console CSP violations on the landing page.
    - total_logical_invocations in docs/metrics.json >= 921 (no regression) and matches the regenerated count.
  </acceptance_criteria>
  <verify>
    <automated>cd core-java && ./gradlew test integrationTest -q && cd ../frontend && npm test && npm run build && [ "$(grep -rn 'purple-' app components | wc -l)" -eq 0 ] && [ "$(grep -rn 'text-\[10px\]' app components | wc -l)" -eq 0 ]</automated>
  </verify>
  <done>Full backend + frontend suites, all UIX grep gates, and Playwright mobile+desktop are green against a freshly-rebuilt stack; zero regression of the 921 invocations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Whole-app browser UAT — the 6 ROADMAP success criteria</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md:207-213 (the 6 Phase 19 success criteria being confirmed)
    - .planning/phases/19-full-frontend-experience-overhaul/19-UI-SPEC.md (the visual contract executors were held to)
  </read_first>
  <action>
    After Task 2's automated gate is green, drive a live whole-app browser UAT against the freshly-rebuilt stack (confirm the bound port first), capturing screenshots at 390px and desktop and asserting populated images resolve `naturalWidth > 0` (not the SafeImage fallback). Walk every one of the 6 ROADMAP success criteria (see how-to-verify), then PAUSE for the human to confirm the app is functional AND comparator-grade before phase sign-off. Do not auto-advance — this is a blocking human gate.
  </action>
  <what-built>The full-frontend overhaul on a freshly-rebuilt Docker stack: persona-routed landing + connected shells, mobile dashboard, real product names, checkout fee-before-payment, per-shop menus, and the converged palette.</what-built>
  <how-to-verify>
    1. Rebuild ALL containers; open the app; confirm the bound frontend port.
    2. **UIX-01:** Visit `/` — a real landing page (no redirect), two doors → `/shop` and `/for-operators`. From the public header/footer reach `/for-operators`, `/business-model-guide`, `/track`, `/shop`. No orphan routes.
    3. **UIX-02:** On a 390px viewport, open all 4 bottom tabs + the More drawer; titles readable, sidebar gone; `/dashboard/onboarding` unchanged.
    4. **UIX-03:** Open `/dashboard/kitchen` + an order detail — real product names, NO "Unknown Product"; badges not clipped; elapsed capped.
    5. **UIX-04:** In checkout, toggle Delivery/Collection, enter a UK address (delivery), and confirm the Subtotal + Delivery/Free + VAT + Total breakdown shows BEFORE payment (footnote gone).
    6. **UIX-05:** Open two different shops — each shows its OWN menu, no duplicate rows, no cross-shop bleed; realistic shop/product/customer names (no "Test Shop"/lorem).
    7. **UIX-06 / quality:** palette is orange/emerald/slate + blue-600 (no purple, no serif/editorial); populated images render (naturalWidth>0, not fallback); public console has no expected-401 spam; backlog #14 error boundary intentionally unchanged.
  </how-to-verify>
  <acceptance_criteria>
    - All 6 ROADMAP success criteria visually confirmed on 390px + desktop.
    - Populated images assert naturalWidth>0 (not the SafeImage fallback) on the landing/storefront.
    - No purple, no serif, no editorial aesthetic; palette locked orange/emerald/slate + blue-600.
    - Human confirms the app is functional AND comparator-grade (Deliveroo/Just Eat storefront; Square/Toast dashboard).
  </acceptance_criteria>
  <resume-signal>Type "approved" or list the issues to fix before phase sign-off.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| full-stack UAT | the aggregate public surface must expose no auth-gated data; CSP nonce must survive on all new pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-09-01 | Tampering (XSS) | CSP regression across the phase's new pages | mitigate | csp-no-violations Playwright gate + header-snapshot run in the full gate; landing stays force-dynamic/nonce-safe |
| T-19-09-02 | Info Disclosure | address PII in logs surfaces during UAT | mitigate | UAT step checks the console/logs carry no delivery address; GDPR erasure covered by 19-01 tests |
| T-19-09-03 | Repudiation | count drift hides a dropped test | mitigate | docs-freshness --write reconcile + total_logical_invocations >= 921 assertion; generator is the arbiter |
| T-19-09-SC | Tampering | supply chain | mitigate | Confirm zero package.json/lockfile change across the phase (sheet vendored source only); gitleaks/GitGuardian clean on planning docs |
</threat_model>

<verification>
Full gate (this plan IS the gate): `./gradlew test integrationTest` + `npm test && npm run build` + all four UIX grep gates + Playwright mobile+desktop vs rebuilt Docker + `scripts/docs-freshness.sh` exit 0 + human UAT of the 6 success criteria.
</verification>

<success_criteria>
- UIX-01..06 registered with traceability; all 15 backlog items closed/deferred (#14 documented leave-as-is).
- docs/metrics.json reconciled (schema 45), docs-freshness green, total_logical_invocations >= 921 (no regression).
- Full backend+frontend+Playwright gate green on a rebuilt stack; the 6 ROADMAP success criteria confirmed in a browser UAT; palette locked.
</success_criteria>

<output>
Create `.planning/phases/19-full-frontend-experience-overhaul/19-09-SUMMARY.md` when done. Commit (do NOT push).
</output>
