---
phase: 19-full-frontend-experience-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core-java/src/main/java/uk/jtoye/core/product/ProductRepository.java
  - core-java/src/main/java/uk/jtoye/core/dev/DemoDataSeeder.java
  - core-java/src/test/java/uk/jtoye/core/product/ProductRepositoryScopingIntegrationTest.java
autonomous: true
requirements: [UIX-05]

must_haves:
  truths:
    - "Each shop renders only its own products — no product bleeds across shops in a tenant"
    - "The storefront menu query resolves products strictly by shop_id (the OR shopId IS NULL fallback is removed)"
    - "A committed, reproducible dev/demo seed exists that assigns every demo product a shop_id with realistic UK shop/product/customer names — and never runs in Testcontainers (test profile) or prod"
    - "Duplicate line items ('Jollof Rice' twice) never render on a shop menu"
  artifacts:
    - path: "core-java/src/main/java/uk/jtoye/core/dev/DemoDataSeeder.java"
      provides: "dev-profile-gated idempotent demo seed + shop_id alignment"
      contains: "@Profile(\"dev\")"
    - path: "core-java/src/test/java/uk/jtoye/core/product/ProductRepositoryScopingIntegrationTest.java"
      provides: "per-shop scoping proof (shop A menu != shop B, no NULL bleed)"
      contains: "@Tag(\"testcontainers\")"
  key_links:
    - from: "core-java/.../product/ProductRepository.java"
      to: "storefront menu (PublicStorefrontService:215)"
      via: "findAvailableByShopOrderedByCategory scoped to shopId"
      pattern: "p\\.shopId = :shopId ORDER BY"
---

<objective>
Close UIX-05 (backlog #6, Critical + #15 data): every shop under a tenant currently renders the same 24-item menu because `ProductRepository.java:21` matches `(p.shopId = :shopId OR p.shopId IS NULL)` and 24 of 25 seeded products have `shop_id = NULL`, so they leak into every shop — including visible duplicate rows.

Per the LOCKED decision (CONTEXT resolved-question 2): assign every live/demo product a `shop_id` AND remove the `OR p.shopId IS NULL` bleed. There is NO "tenant-wide items" feature. Deliver the reproducible demo seed as a committed, dev-profile-gated seeder (resolved-question 1) — it must NOT run in Testcontainers or prod; the live dev DB is aligned by the same seeder.

Purpose: a real multi-shop vendor's second shop must show its own menu, not a duplicated clone.
Output: dev-profile `DemoDataSeeder`, a scoped `ProductRepository` query, and a Testcontainers proof of per-shop isolation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-full-frontend-experience-overhaul/19-RESEARCH.md
@.planning/phases/19-full-frontend-experience-overhaul/19-PATTERNS.md
@.planning/phases/19-full-frontend-experience-overhaul/19-UI-SPEC.md

<interfaces>
<!-- The buggy query and the correctly-scoped sibling. Executor uses these directly. -->
From core-java/.../product/ProductRepository.java:
  :21 @Query("SELECT p FROM Product p WHERE p.available = true AND (p.shopId = :shopId OR p.shopId IS NULL) ORDER BY p.category NULLS LAST, p.displayOrder ASC, p.title ASC")
  :22 List<Product> findAvailableByShopOrderedByCategory(@Param("shopId") UUID shopId);   // ONLY caller: PublicStorefrontService:215
  :26 List<Product> findByShopId(UUID shopId);   // already correctly scoped (no NULL fallback) — the analog

From core-java/.../product/Product.java: shopId is nullable @Column(name="shop_id"); title, category, pricePennies, available fields.
Seed analog: V13__seed_default_tenants.sql uses INSERT ... ON CONFLICT (id) DO NOTHING (idempotent) — mirror the idempotency, but as a dev-profile runtime seeder (NOT a Flyway migration, so it never runs in test/prod).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dev-profile DemoDataSeeder (idempotent, realistic UK data, shop_id assigned)</name>
  <files>core-java/src/main/java/uk/jtoye/core/dev/DemoDataSeeder.java</files>
  <read_first>
    - core-java/src/main/resources/db/migration/V13__seed_default_tenants.sql (the only committed seed — INSERT ... ON CONFLICT idempotency idiom)
    - core-java/src/main/java/uk/jtoye/core/product/Product.java (fields: shopId, title, category, pricePennies, available, description)
    - core-java/src/main/java/uk/jtoye/core/shop/Shop.java (fields incl. deliveryFeePennies, freeDeliveryThresholdPennies, published)
    - core-java/src/main/java/uk/jtoye/core/security/TenantContext.java (tenant scoping — seeder must set the tenant before writes)
    - 19-RESEARCH.md § Open Question 1 + § Runtime State Inventory (no committed product/shop/order seed exists today; shared dev volume)
    - 19-UI-SPEC.md § Surface G (seed realism: real UK shop/customer names, plausible prices; "Test Shop 1"/lorem MUST NOT appear)
  </read_first>
  <action>
    Create `DemoDataSeeder` in package `uk.jtoye.core.dev`, annotated `@Component @Profile("dev")` implementing `ApplicationRunner` (runs at dev startup only — Testcontainers use `@ActiveProfiles("test")` so it is excluded; prod uses prod profile so it is excluded). Guard writes under the default demo tenant (`00000000-0000-0000-0000-000000000001`) via TenantContext so RLS is satisfied. Idempotently UPSERT (INSERT ... ON CONFLICT DO NOTHING, or existsBy-guarded saves) a realistic multi-shop dataset: 2-3 published shops with real UK names (e.g. "Mama Ade's Kitchen", "Peckham Jollof Co.") each with deliveryFeePennies + freeDeliveryThresholdPennies set; each product assigned to exactly one shop via `shop_id` (NO NULL shop_id); realistic customers (e.g. "Aisha Bello", "Tom Whitfield"). ALSO align the shared dev DB: assign a `shop_id` to any existing product rows where `shop_id IS NULL` (distribute deterministically across the demo shops) so the live dev volume matches the new scoped query. No "Test Shop", no lorem, no duplicate line items within a shop's category. Do NOT seed via Flyway. Log an INFO summary. This seeder is the reproducible replacement for the ad-hoc dev-volume data.
  </action>
  <acceptance_criteria>
    - `DemoDataSeeder.java` is annotated `@Profile("dev")` (grep: `grep -c '@Profile("dev")' DemoDataSeeder.java` == 1) and `implements ApplicationRunner`.
    - Seeder assigns a non-null `shop_id` to every demo product and to any existing NULL-shop_id rows.
    - Seed data contains realistic UK names; grep for placeholder pollution returns 0: `grep -ciE "test shop|lorem ipsum" DemoDataSeeder.java` == 0.
    - Seeder is idempotent (re-running does not duplicate rows — ON CONFLICT / existsBy guard present).
    - `./gradlew :core-java:compileJava` succeeds; the seeder bean is NOT instantiated under the test profile (no `@Profile("test")` / no default profile).
  </acceptance_criteria>
  <verify>
    <automated>cd core-java && ./gradlew compileJava -q && grep -c '@Profile("dev")' src/main/java/uk/jtoye/core/dev/DemoDataSeeder.java</automated>
  </verify>
  <done>A committed dev-only seeder produces realistic, per-shop-scoped demo data and aligns any NULL-shop_id dev rows; it never runs in test or prod.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scope the storefront menu query + per-shop isolation integration test + FTS/#96 tripwire fix</name>
  <files>core-java/src/main/java/uk/jtoye/core/product/ProductRepository.java, core-java/src/test/java/uk/jtoye/core/product/ProductRepositoryScopingIntegrationTest.java</files>
  <read_first>
    - core-java/src/main/java/uk/jtoye/core/product/ProductRepository.java:21-26 (the OR shopId IS NULL bleed to remove; findByShopId:26 the scoped analog)
    - core-java/src/main/java/uk/jtoye/core/storefront/PublicStorefrontService.java:215 (the ONLY caller of findAvailableByShopOrderedByCategory)
    - existing product/FTS test classes (any test that relies on NULL shop_id products — search: `grep -rln "findAvailableByShopOrderedByCategory\|shopId IS NULL\|search_vector" core-java/src/test`)
    - core-java/src/test/java/uk/jtoye/core/order/OrderControllerIntegrationTest.java:40-70 (Testcontainers scaffold)
    - 19-RESEARCH.md § Regression Tripwires (FTS/#96 NULL search_vector — tests must seed shop_id)
  </read_first>
  <behavior>
    - Test: two shops in one tenant, each with distinct products → shop A's menu contains only A's products, shop B's only B's (zero overlap).
    - Test: a product with NULL shop_id does NOT appear in any shop's menu (bleed removed).
    - Test: no duplicate title within a shop category.
  </behavior>
  <action>
    In `ProductRepository`, change the `findAvailableByShopOrderedByCategory` @Query (line 21) to drop `OR p.shopId IS NULL`, leaving `WHERE p.available = true AND p.shopId = :shopId ORDER BY p.category NULLS LAST, p.displayOrder ASC, p.title ASC`. The signature is unchanged (one caller, PublicStorefrontService:215 — no caller edit needed). Create `ProductRepositoryScopingIntegrationTest` (Testcontainers, `@Tag("testcontainers")`, `@ActiveProfiles("test")`, NOSUPERUSER RLS pattern) seeding two shops with distinct products (and one NULL-shop_id product) under one tenant, asserting per-shop menu disjointness and that the NULL product is absent. Update any existing FTS/#96 product test that previously relied on NULL-shop_id products to now seed an explicit `shop_id` (RESEARCH tripwire) — keep the FTS query-plan pins intact.
  </action>
  <acceptance_criteria>
    - `grep -c "shopId IS NULL" ProductRepository.java` == 0.
    - `grep -c "p.shopId = :shopId ORDER BY" ProductRepository.java` >= 1.
    - `ProductRepositoryScopingIntegrationTest` asserts shop A menu ∩ shop B menu == ∅ and NULL-shop_id product absent.
    - Existing product/FTS tests updated to seed shop_id and still pass.
    - `./gradlew :core-java:integrationTest --tests '*ProductRepositoryScoping*' --tests '*Product*'` passes (incl. #96 FTS tripwires).
  </acceptance_criteria>
  <verify>
    <automated>cd core-java && ./gradlew integrationTest --tests '*ProductRepositoryScoping*' --tests '*Product*' -q</automated>
  </verify>
  <done>The storefront query is strictly per-shop; a Testcontainers test proves no cross-shop bleed; FTS/#96 tripwires seed shop_id and stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storefront request (slug → tenant) → ProductRepository | product listing must stay tenant-scoped (RLS) and now shop-scoped |
| dev seeder → dev DB | must never execute in test (Testcontainers) or prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-01 | Info Disclosure | scoping change leaks cross-tenant products | mitigate | RLS already tenant-scopes `products`; the new query only NARROWS within-tenant by shop_id — no widening; integration test asserts isolation |
| T-19-02-02 | Tampering | dev seeder writing to prod/test data | mitigate | `@Profile("dev")` gate; Testcontainers run `@ActiveProfiles("test")` (bean excluded); prod runs prod profile (excluded) |
| T-19-02-03 | Info Disclosure | seed data with real-looking PII in git | accept | Names are fictional demo values, no real customer PII, no secrets; gitleaks/GitGuardian scan applies at closure |
| T-19-02-SC | Tampering | supply chain | accept | No package installs (backend Java only) |
</threat_model>

<verification>
Scoped: `./gradlew :core-java:integrationTest --tests '*ProductRepositoryScoping*' --tests '*Product*'`. Confirm the seeder bean is absent under test profile. Full suite deferred to plan 19-09.
</verification>

<success_criteria>
- `findAvailableByShopOrderedByCategory` is strictly `p.shopId = :shopId` (no NULL bleed).
- A committed dev-profile seeder produces realistic per-shop data and aligns NULL-shop_id dev rows; never runs in test/prod.
- Testcontainers proves shop A menu ≠ shop B menu; FTS/#96 tripwires updated and green.
</success_criteria>

<output>
Create `.planning/phases/19-full-frontend-experience-overhaul/19-02-SUMMARY.md` when done. Commit (do NOT push).
</output>
