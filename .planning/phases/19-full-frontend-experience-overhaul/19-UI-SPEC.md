---
phase: 19
slug: full-frontend-experience-overhaul
status: draft
shadcn_initialized: true
preset: none
created: 2026-07-11
---

# Phase 19 — UI Design Contract

> Visual and interaction contract for the **whole-app experience overhaul** that closes the 15-item
> remediation backlog in `18-UI-REVIEW.md` (whole-app 42/72; Experience Design 1/4 on every surface).
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **This is a convergence, not a redesign.** Every token below is lifted from the running app
> (`tailwind.config.ts`, `app/globals.css`, `components/ui/*`) and from the internal quality
> reference (`18-UI-SPEC.md`, the one screen built to a written contract). The job is to make three
> surfaces that "happen to share a git repo" behave as one product — connect the navigation, complete
> the broken flows, make it work on a phone — **without** introducing a fourth visual system.
>
> **Comparator bar:** Deliveroo / Just Eat for public + storefront; Square / Toast / Shopify for the
> dashboard. **Standard:** functional AND aesthetically impressive, production-grade, no "AI-looking"
> output, verified in Playwright with real data (images asserted `naturalWidth > 0`, not just markup).

---

## Locked Constraints (restated — non-negotiable)

1. **Palette LOCKED** to the existing orange / emerald / slate food-delivery family (customer-facing)
   + the documented **blue-600 (`--primary`)** dashboard accent. No new hues. A Warm-Editorial / serif
   redesign was rejected and reverted (PR #49 → #52) — **no serif display type, no newspaper aesthetic,
   anywhere.**
2. **No functional regression.** The 921 logical test invocations stay green. Any change touching
   `OrderItem` / `ProductRepository` / `Sidebar` / `DashboardShell` ships with tests, not just a visual patch.
3. **Mobile-first.** The single most-violated constraint today. Every surface is designed at 390px first,
   then 768px, then 1280px.
4. **Do NOT regress `/dashboard/onboarding`.** It matches `18-UI-SPEC.md` almost line-for-line and is the
   reference pattern — replicate its idioms, do not touch its output.
5. **Zero orphan routes.** Every route reaches ≥1 inbound navigational link, enforced by a link-graph test.
6. **Design system stays** shadcn/ui + Radix + Tailwind + CSS-variable tokens. The fix for marketing is to
   stop bypassing the token layer with hardcoded hex — not to add a new system.

---

## Scope Surfaces

| # | Surface | Route(s) / File(s) | Backlog items | Kind |
|---|---------|--------------------|---------------|------|
| A | **Public landing page** | `/` (`app/page.tsx` — replace blind `redirect("/dashboard")`) | #4 | New |
| B | **Shared public shell** (header + footer) | new `components/public/*`, wraps `/`, `/for-operators`, `/business-model-guide`, `/track`, `/shop` | #5, #7, #9 | New + evolve |
| C | **`/for-operators` + `/business-model-guide` re-skin** | `components/marketing/operator-pitch.tsx`, `components/marketing/business-model-guide.tsx` | #5, #7 | Token migration |
| D | **Dashboard responsive shell** | `components/dashboard/dashboard-shell.tsx`, `components/dashboard/sidebar.tsx`, new `components/dashboard/mobile-tab-bar.tsx` | #1 | Evolve |
| E | **Checkout fulfilment + fee transparency** | `app/shop/[slug]/checkout/page.tsx` (+ V45 schema) | #3 | Evolve |
| F | **Kitchen + order-detail product names & fixes** | `app/dashboard/kitchen/page.tsx`, `app/dashboard/orders/[id]/page.tsx`, `components/dashboard/orders/OrderDetailPanel.tsx` (+ `OrderItem` backend) | #2, #8, #12 | Fix |
| G | **Storefront + directory polish** | `app/shop/page.tsx`, `app/shop/[slug]/page.tsx`, `app/shop/[slug]/cart/page.tsx` (+ `ProductRepository` scoping) | #6, #11, #13, #15 | Fix |
| H | **Track page** (guest lookup + shell) | `app/track/page.tsx` | #9 | Evolve |
| I | **Cross-surface color/type discipline** | dashboard + storefront | #10, #11, #14 | Fix |

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui — already initialized (`components.json`). shadcn init gate: **satisfied**, no init required. |
| Preset | not applicable — style `new-york`, baseColor `slate`, `cssVariables: true`, RSC + TSX, prefix "" |
| Component library | Radix UI primitives (via shadcn) |
| Icon library | `lucide-react` (only icon source — no other icon packs) |
| Font | **Inter** (`next/font/google`, applied as `inter.className` in `app/layout.tsx`). Do NOT introduce a new font; the unused Geist woffs in `app/fonts` stay unwired. |

**Existing UI primitives to reuse (`components/ui/`):** `badge`, `button`, `card`, `dialog`, `dropdown-menu`,
`input`, `label`, `pagination`, `safe-image`, `select`, `table`, `toast` + `toaster`. Build everything from
these first.

**New official-registry primitives permitted (needs no vetting — official shadcn):**
- `sheet` — the mobile "More" drawer for the dashboard (`npx shadcn add sheet`) and the public mobile nav sheet.
- `tabs` — optional, for the checkout fulfilment toggle (or build a bespoke 2-button segmented control from
  `button` — see Surface E). Prefer the bespoke control to keep the new-dependency surface minimal.

No other new components. **No third-party registries** → registry vetting gate not triggered (see Registry Safety).

**Shared idioms every new/edited surface MUST mirror (verbatim from the running app / `18-UI-SPEC.md`):**
- Dashboard page container `<div className="space-y-6">`; page header `<h1 className="text-4xl font-semibold text-slate-900">` + `<p className="mt-2 text-sm text-slate-600">`.
- Loading spinner: `<div className="h-32 w-32 animate-spin rounded-full border-b-2 border-t-2 border-blue-600" />` inside `flex h-full items-center justify-center` (dashboard); storefront spinner is `Loader2 animate-spin text-orange-500`.
- Card: `Card / CardHeader / CardTitle / CardDescription / CardContent`.
- Empty state (dashboard): centered column, lucide icon `h-12 w-12 text-slate-300`, `h3 text-lg font-semibold text-slate-900`, `p text-sm text-slate-500`, then an `outline` button.
- Errors: `useToast()` `variant: "destructive"` (dashboard) / inline `rounded-xl bg-red-50 border border-red-100 p-3 text-sm text-red-700` (storefront).
- Skeletons: `animate-pulse` with `bg-slate-200`/`bg-slate-100` blocks (the shop-directory skeleton is the reference).
- Data fetching: dashboard via `apiClient` (`/api/v1` prefix, Bearer + `X-Tenant-Id` auto-injected); public via `publicApiClient` (`/public` prefix).

---

## Spacing Scale

Tailwind 4px base — all multiples of 4. Identical to `18-UI-SPEC.md`, extended with two public-hero rhythm tokens.

| Token | Value | Tailwind | Usage |
|-------|-------|----------|-------|
| xs | 4px | `gap-1` / `space-y-1` | Icon-to-label micro gaps |
| sm | 8px | `gap-2` / `space-y-2` | Compact rows, badge/icon groups, form label→input |
| md | 16px | `gap-4` / `p-4` | Default element spacing, card inner padding |
| lg | 24px | `gap-6` / `space-y-6` | Section gaps, card-grid gaps, dashboard page rhythm |
| xl | 32px | `p-8` / `space-y-8` | `DashboardShell` container padding, major stacks |
| 2xl | 48px | `py-12` | Empty-state vertical padding, minor public section breaks |
| 3xl | 64px | `py-16` | Full-page empty blocks, storefront hero padding |
| **4xl** | **80px** | `py-20` | **Public marketing section breaks (`/`, `/for-operators`)** |
| **5xl** | **96px** | `py-24` | **Public landing hero vertical padding (≥md)** |

**Exceptions (documented — do not flag):**
- Fixed mobile bottom tab bar height = **56px** (`h-14`) + `pb-[env(safe-area-inset-bottom)]`. Main scroll
  area gets `pb-20` on mobile so content clears the bar. iOS safe-area inset is additive, not a scale violation.
- Touch targets: every mobile tap target ≥ **44px** (`min-h-11 min-w-11` or `py-3` on full-width buttons).
- Form controls keep shadcn defaults (`h-10`, `px-3 py-2`); storefront inputs use `px-3 py-2.5`. Control sizing,
  not layout spacing.
- **KILL `text-[10px]` (backlog #11):** it appears 36× across 9 files and is below the smallest token. Replace
  every occurrence with `text-xs` (12px). Do **not** add a sub-12px token — accessibility floor is 12px.

---

## Typography

Two contexts share one weight discipline. Roles map to Tailwind's existing scale. `next/font` Inter only.

| Role | Size | Tailwind | Weight | Line height |
|------|------|----------|--------|-------------|
| **Display — page/section title** | 36px | `text-4xl` | semibold 600 | ~1.1 (`leading-tight`) |
| **Heading — section (public) / card (app)** | 24px public · 18px app | `text-2xl` / `text-lg` | semibold 600 | 1.2 / 1.4 |
| **Body** | 16px public/storefront · 14px dashboard | `text-base` / `text-sm` | regular 400 | 1.5 (`leading-normal`) |
| **Label / meta** | 12px | `text-xs` | regular 400 (medium 500 on form labels/badges) | 1.4 |

**Weight contract — exactly two systematic weights: regular 400 + semibold 600.**
- New **dashboard** code uses 400/600 only. Do NOT propagate `font-bold` (700) into dashboard code (legacy
  dashboard `font-bold` on titles is tolerated where it exists; new work matches the onboarding slice's 600).
- **One documented 700 exception, scoped to the two customer-facing brand surfaces only** (public landing +
  storefront): `font-bold` is permitted on (a) the public hero H1, (b) storefront/checkout price emphasis and
  primary CTA labels. This is the *established* storefront idiom (Color pillar scored 4/4 with it) — keep it,
  do not restyle it.

**Documented display exception — public marketing hero H1 (backlog #7 boundary):**

| Role | Size | Tailwind | Weight | Line height | Reserved for |
|------|------|----------|--------|-------------|--------------|
| Public Hero Display | 48px → 60px | `text-5xl md:text-6xl` | bold 700 | 1.05 (`leading-[1.05] tracking-tight`) | `/` hero H1 and the `/for-operators` / `/business-model-guide` hero H1 **only** |

The audit flagged `text-6xl`/`text-7xl`/`font-black` as a scale unique to 2 of 23 routes. This contract
**caps the public display at `text-6xl` (60px) and weight 700 — `text-7xl` and `font-black` are banned.** The
hero headline is the *only* place this tier appears; it never leaks into body, cards, or the dashboard.

**Numeric/id values:** `font-mono text-xs` (order-id idiom) wherever a raw identifier is shown.

---

## Color

**Two documented identities, one shared neutral + semantic system.** This is the existing reality, not a new
split: the customer-facing brand accent is **orange-500**; the dashboard `--primary` accent is **blue-600**.
Both sit on a **slate** neutral chrome with an **emerald** success axis. Do not merge them; do not add a
seventh hue.

### Public + Storefront identity (Surfaces A, B, C, E, G, H)

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | white `#ffffff` + slate-50 `#f8fafc` | Page background, card surfaces |
| Secondary (30%) | slate-900 `#0f172a` + slate-100 `#f1f5f9` | Dark hero/footer sections, muted chips, borders (`border-slate-200`) |
| Accent (10%) | **orange-500** `#f97316` | See reserved list |
| Success | emerald-500/600 `#10b981` | "Open", "Free delivery", confirmed/complete states |
| Destructive | red-500/600 (`--destructive`) | Errors, "Cancelled", validation |

**Orange accent reserved for — explicit list, never "all interactive elements":**
1. The wordmark badge (the `J` tile) in the shared public header + storefront header.
2. The single primary CTA per view (landing "Order food near you" door; checkout "Place order"; track "Track order").
3. Price emphasis + the active order-progress step/bar (storefront + track).
4. Focus rings on storefront inputs (`focus:ring-orange-100 focus:border-orange-300`) and card hover title color.
   Everything else stays slate.

### Dashboard identity (Surfaces D, F) — LOCKED, do not regress

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | slate-50 `#f8fafc` + white | Main scroll area + card surfaces |
| Secondary (30%) | slate-900 `#0f172a` + slate-100 | Sidebar/bottom-bar chrome, table zebra, neutral badges |
| Accent (10%) | **blue-600** `hsl(221 83% 53%)` = `--primary` | Primary CTA; active nav item (`bg-blue-600 text-white shadow-lg shadow-blue-500/50`); focus rings; spinner |
| Destructive | red (`--destructive`) | Destructive actions only |

**Semantic status palette (functional second axis — badges + inline icons only, idiom `bg-{hue}-100 text-{hue}-700`),
inherited verbatim from `18-UI-SPEC.md`:** emerald = success/live/PASSED · blue = ready/informational ·
amber = in-progress/waiting · orange = needs-attention · red = failure/terminal-bad · slate = neutral/N-A.

**Backlog #10 — remove the undocumented purple (13 occurrences).** Purple is not in either identity. Fix:
- Order status **`PREPARING` → amber** everywhere it renders (`kitchen/page.tsx`, `dashboard/orders/page.tsx`,
  `dashboard/page.tsx` chart `chartColor: "#f59e0b"`, `OrderDetailPanel.tsx`). `CONFIRMED` stays blue, `READY`
  stays green/emerald — amber keeps `PREPARING` visually distinct.
- Finance VAT accent (`finance/page.tsx`) purple → **amber**.
- Products stat accent (`dashboard/page.tsx`) purple → **blue** (`text-blue-600 bg-blue-100`).
- Sidebar avatar gradient `from-blue-400 to-purple-500` → `from-blue-400 to-blue-600`.
- Net result: `grep -rn "purple-" app components` returns **0**.

### `/for-operators` + `/business-model-guide` re-skin (backlog #7 — Surface C)

These two pages hardcode two bespoke palettes (17–20 hex values each) that bypass the token system. Migrate them
onto tokens **within the locked family — this is a re-skin, not a rebuild. The dark, confident, full-bleed pitch
layout and the (genuinely sharp) copy are PRESERVED; only the color values change** so the page stays recognisably
itself while joining the family:

| Old hardcoded hex | Replace with | Notes |
|-------------------|--------------|-------|
| `operator-pitch.tsx` navy `#211c36` / `#1a1a33` | `bg-slate-900` / `bg-slate-950` | Keeps the dark hero — it is NOT gutted, just tokenised |
| `operator-pitch.tsx` orange `#f26522` | `text-orange-500` / `bg-orange-500` | Brand accent, now on-token |
| `operator-pitch.tsx` yellow `#ffdf7e` | `text-amber-300` (on dark) | Within-family highlight; drop if it reads off |
| `business-model-guide.tsx` teal `#122c33` | `bg-slate-900` | Converge on the SAME dark surface as operator-pitch |
| `business-model-guide.tsx` rust `#b75d2c` | `text-orange-600` / `bg-orange-500` | |
| `business-model-guide.tsx` olive `#e4eecd` | `bg-slate-50` / `bg-emerald-50` panels | |

After migration, `grep -rlE "#[0-9a-fA-F]{3,8}" components/marketing/*.tsx` returns **0** (the only tolerated
app-wide hex remain `checkout/page.tsx`'s Stripe `colorPrimary` config and `dashboard/page.tsx` chart colors).
`text-primary|bg-primary|orange-*` usage in `components/marketing/*` goes from **0** to non-zero.

---

## Surface A — Public Landing Page (`/`) · backlog #4

Replace `app/page.tsx`'s `redirect("/dashboard")` with a real, server-rendered landing page. **No blind
redirect, ever — even signed-in vendors land here** (they get a "Go to dashboard" affordance in the header, not
an auto-redirect). Wrapped in the Shared Public Shell (Surface B).

**Section-by-section:**

1. **Header** — Shared Public Shell header (Surface B).
2. **Split-persona hero** (the locked decision). Above the fold:
   - **Brand statement** H1 (Public Hero Display, 48→60px, 700): e.g. *"Order from local kitchens. Or run yours."*
   - **Subhead** (Body 16px, slate-600, `max-w-xl`): one sentence naming both audiences.
   - **Two equal "doors"** — side-by-side cards, equal height/weight:
     | Door | Copy | Icon | Treatment | Route |
     |------|------|------|-----------|-------|
     | Customer | **"Order food near you"** + "Browse independent kitchens and order in minutes." | `UtensilsCrossed` | Orange primary — `bg-orange-500 text-white`, hover `-translate-y-0.5 shadow-lg` | `/shop` |
     | Operator | **"Run your food business"** + "Take orders, manage your kitchen, go live in a day." | `Store` | Dark — `bg-slate-900 text-white`, orange icon/CTA | `/for-operators` |
   - **Art direction (Claude's discretion, decided):** gradient-forward, no stock-photo dependency. Hero backdrop
     is the storefront brand gradient already in use on shop cards — `bg-gradient-to-br from-orange-400 via-orange-500 to-rose-500` — as a soft, partially-masked wash on an off-white base (do NOT put body text directly on it at low contrast). Doors may carry optional food imagery via `SafeImage` with the gradient as fallback; **if imagery is used it is Playwright-verified `naturalWidth > 0`.** No serif, no editorial.
3. **"How it works" band** (customer trust, 3 steps, `py-20`): Browse (`Search`) → Order & pay (`ShoppingBag`) →
   Track live (`MapPin`). Each step: icon in an orange-tinted circle (`bg-orange-100 text-orange-600`), Heading
   (18px/600), Body (14–16px/slate-600).
4. **Trust strip** (social proof within the family): 3 markers — "UK food-hygiene verified", "Allergen info on
   every item", "No app to download". Slate chips with emerald check icons. (Uses real product truths, not
   fabricated testimonials.)
5. **Footer** — Shared Public Shell footer (Surface B).

**Responsive:**
- **390px:** H1 `text-4xl` (36px) → guard the hero display at `text-4xl` on the smallest width to avoid overflow,
  stepping to `text-5xl` at `sm`; doors **stack vertically**, full-width, `gap-4`; "How it works" steps stack.
- **768px:** H1 `text-5xl`; doors **2-col** `grid-cols-2 gap-6`; steps 3-col.
- **1280px:** H1 `text-6xl`; hero content `max-w-5xl mx-auto`, generous `py-24`; doors 2-col centered; steps 3-col.

---

## Surface B — Shared Public Shell (header + footer) · backlog #5, #9

New `components/public/public-header.tsx` + `components/public/public-footer.tsx`. Wraps `/`, `/for-operators`,
`/business-model-guide`, `/track`, and the `/shop` **directory** (the per-shop storefront `/shop/[slug]` keeps
its cart-aware `StorefrontNav` header — see note). This is the connective tissue that de-orphans every route.

**Header** — sticky, `bg-white border-b border-slate-200 shadow-sm`, `h-14`, `max-w-7xl` inner:
- Left: wordmark = orange `J` badge (`inline-flex h-8 w-8 rounded-lg bg-orange-500 text-white font-bold`) +
  "J'Toye" (`text-lg font-semibold text-slate-900`) → links `/`.
- Right (≥sm): nav links — **Shops** (`/shop`), **For operators** (`/for-operators`), **Track order** (`/track`),
  and a **Sign in** button (`/auth/signin`, slate-900 pill — the existing storefront sign-in idiom).
- Right (<sm): wordmark + hamburger (`Menu` icon) → shadcn `sheet` with the same four links stacked, ≥44px targets.
- **Active state:** the link matching the current route renders `text-slate-900 font-semibold` (others
  `text-slate-600 hover:text-slate-900`); detect via `usePathname` prefix match.

**Footer** — `border-t border-slate-200 bg-white`, `max-w-7xl`, `py-12`, columns collapse to a stack <sm:
- **Brand** column: wordmark + one-line blurb.
- **For customers:** Browse shops (`/shop`) · Track order (`/track`).
- **For operators:** Become a vendor (`/for-operators`) · Business model guide (`/business-model-guide`) ·
  Vendor sign in (`/auth/signin`).
- **Bottom row:** `© {year} J'Toye OaaS` + "Allergen info available on all products" (preserve the existing note).

**This footer is the mechanism that closes backlog #5** — it gives `/business-model-guide` and `/track` their
first inbound links and gives `/for-operators` a second, non-orphan one.

**Storefront cross-link (closes the last IA gap):** the per-shop `StorefrontNav` (`components/storefront/storefront-nav.tsx`)
gains a **"Track order"** link (`/track`) alongside "Browse"/"My Orders", and the storefront footer
(`app/shop/layout.tsx`) gains a "For operators" link — so a customer browsing a shop can always discover the
vendor pitch, satisfying the audit's "customer never sees a path to become a vendor."

**Link-graph invariant (success criterion, testable):** after this surface ships, every route in `docs/SITEMAP.md`
has ≥1 inbound `href` from another page. A link-graph test asserts this and fails if any route becomes an orphan.

---

## Surface D — Dashboard Responsive Shell · backlog #1 (Blocker)

The locked decision: **desktop keeps the 256px sidebar; mobile gets a bottom tab bar (4 tabs) + a "More" drawer.**
Square/Toast operator pattern. Breakpoint = **`md` (768px)** — matches the audit's own "collapse under `md:`"
recommendation.

**Single source of truth:** keep the existing `navigation` array in `sidebar.tsx` and drive BOTH the sidebar and
the bottom bar from it (do not fork the list). Active-state detection stays `usePathname`; icons stay lucide.

**`< md` (mobile, ≤767px):**
- **Sidebar hidden** (`hidden md:flex`).
- **Fixed bottom tab bar** (`components/dashboard/mobile-tab-bar.tsx`): `fixed inset-x-0 bottom-0 z-50 h-14 bg-white
  border-t border-slate-200 flex`, `pb-[env(safe-area-inset-bottom)]`. **4 primary tabs** in the thumb zone —
  **Dashboard** (`LayoutDashboard` → `/dashboard`), **Orders** (`ShoppingCart` → `/dashboard/orders`),
  **Products** (`Package` → `/dashboard/products`), **Kitchen** (`UtensilsCrossed` → `/dashboard/kitchen`) — plus
  a **5th "More"** tab (`Menu` icon) opening the drawer.
  - Each tab: vertical icon (20px) + label (`text-xs`), ≥44px target, active = `text-blue-600` (icon + label),
    inactive = `text-slate-500`. No full blue pill on the light bar (that idiom is the dark sidebar's).
- **"More" drawer** = shadcn `sheet` (side `bottom` or `right`) listing the remaining routes — **Shops**,
  **Customers**, **Finance**, **Marketing**, **Go live** (`/dashboard/onboarding`) — plus the user block, theme
  toggle, and Sign out that live in the sidebar footer today.
- **Top app bar** (mobile only): slim `h-14 bg-white border-b` with the wordmark, so the page has brand chrome
  once the sidebar is gone.
- **Main scroll area** gets `pb-20` on mobile so content clears the fixed bar; container padding drops from `p-8`
  to `p-4` below `sm`.

**`≥ md` (tablet + desktop):** unchanged — the existing 256px `bg-slate-900` sidebar, bottom bar hidden
(`md:hidden`), container `p-8`. **Zero visual change to the current desktop dashboard.**

**Conventions that carry over from `sidebar.tsx`:** the `navigation` array, `cn()` active-class composition, lucide
icons, the dark-slate chrome for the sidebar, the theme-toggle + sign-out controls (relocated into the drawer on
mobile). The `Go live` item and its `Rocket` icon (added in Phase 18) are preserved.

**Verification:** all 11 dashboard routes render usably at 390px; page titles no longer wrap one-word-per-line;
Playwright mobile-viewport (390×844) spec passes on every route. `/dashboard/onboarding` is visually unchanged.

---

## Surface E — Checkout Fulfilment + Fee Transparency · backlog #3 (Blocker)

Locked decision: **delivery + collection; ask fulfilment type first; address only for delivery; full fee
breakdown BEFORE payment.** Schema via **V45** (`fulfilment_type` + nullable `address_line1/line2/city/postcode`).
**V44 stays reserved for issue #96.** This is added to `app/shop/[slug]/checkout/page.tsx` Step 1 (details form).

**Fulfilment toggle (new, top of Step 1):** a bespoke 2-button **segmented control** (no new dependency) —
`Delivery` (`Bike`/`Truck` icon) | `Collection` (`ShoppingBag`/`Store` icon). Full-width, `grid-cols-2`, the
selected segment `bg-orange-500 text-white`, the other `bg-slate-100 text-slate-600`. **Default = Delivery**
(collection is one tap away). State: `fulfilmentType: "DELIVERY" | "COLLECTION"`.

**Conditional UK address block (renders only when `DELIVERY`):** appears above "Your details", same card idiom,
same input styling (`rounded-lg border-slate-200 px-3 py-2.5 focus:ring-orange-100 focus:border-orange-300`):

| Field | id | Required | Validation |
|-------|-----|----------|-----------|
| Address line 1 | `address1` | yes (delivery) | non-empty |
| Address line 2 | `address2` | no | — |
| Town / city | `city` | yes (delivery) | non-empty |
| Postcode | `postcode` | yes (delivery) | UK postcode regex `^[A-Z]{1,2}\d[A-Z\d]?\s?\d[A-Z]{2}$`, **auto-uppercase on blur** |

- When `COLLECTION` is selected: address block is hidden, address fields are not required, and delivery fee = £0.

**Fee breakdown BEFORE payment (replaces the deferred footnote):** the Step 1 "Order summary" card's total block
becomes a **definite** breakdown — **remove** the line *"Final total confirmed after order is placed. Delivery fee
may apply."* Show, in order:
- Subtotal — `formatPrice(subtotal)`
- Delivery — `formatPrice(shop.deliveryFeePennies)` when `DELIVERY` & below free threshold; **"Free"** in
  `text-emerald-600 font-medium` when `COLLECTION` or above `freeDeliveryThresholdPennies`
- VAT (incl. 20%) — `formatPrice(Math.floor(total * 20 / 120))` (existing VAT-inclusive idiom, unchanged)
- **Total** — bold, `text-base font-bold text-slate-900`
This mirrors the fee breakdown that already exists on the *post-order* confirmation + payment screens — the fix is
to surface it *before* the customer commits, matching Deliveroo/Just Eat.

**Order-creation payload gains** `fulfilmentType` and (for delivery) `address: { line1, line2?, city, postcode }`.

**Validation / error states:**
- Field-level: invalid/empty required field → `border-red-300` + inline `text-xs text-red-600` message under the
  field; block submit.
- Postcode: on submit, if regex fails → inline "Enter a valid UK postcode (e.g. SW1A 1AA)".
- Form-level (API failure): existing `rounded-xl bg-red-50 border border-red-100 p-3 text-sm text-red-700` banner.
- Empty cart: existing empty state — but **center it vertically** (`min-h-[60vh] flex flex-col items-center
  justify-center`) to kill the ~700px dead-space the audit flagged (backlog #15 / vertical balance).

---

## Surface F — Kitchen + Order-Detail Product Names & Fixes · backlog #2 (Blocker), #8, #12

**Product-name rendering contract (#2 — data-integrity, not cosmetic):**
- **Contract:** `OrderItem.productName` MUST be snapshotted from `Product.name` at **order creation** for every
  line item. The literal string **`"Unknown Product"` MUST NEVER render for an order line that references a
  product that exists.** `OrderItem.java:34`'s `"Unknown Product"` default is a last-resort fallback for genuinely
  orphaned data only.
- **Backend (in scope):** populate `productName` on the order-creation path; **backfill** the affected seeded
  rows; add a regression test proving a freshly created order carries real names.
- **UI rendering:** kitchen card renders `{item.quantity}x {item.productName}`; order-detail renders
  `productName` + qty + unit price. Playwright asserts **no `"Unknown Product"` string** appears on seeded live
  orders in kitchen or order-detail.

**Kitchen card badge clipping (#8):** the card header (`kitchen/page.tsx` ~L422) currently
`flex items-start justify-between` with an order number at `text-2xl font-bold` that wraps and collides with the
status badge. Fix: order number `min-w-0 truncate text-xl font-semibold`, badge `flex-shrink-0`, header
`gap-2`. Long `ORD-…` numbers truncate cleanly instead of wrapping under the badge.

**KDS elapsed time (#12):** `elapsedText()` (`kitchen/page.tsx:75`) shows raw uncapped minutes ("2245m ago").
Cap/format: `<1m` → "just now"; `<60m` → "Xm ago"; `<24h` → "Xh ago" (or "Xh Ym"); `≥24h` → "Xd ago".

**No other change to the KDS** — the real-time STOMP feed, age-border colors (green/yellow/red), and mute toggle
are working and stay. `PREPARING` badge/button color moves purple → amber per Surface I / backlog #10.

---

## Surface G — Storefront + Directory Polish · backlog #6 (Critical), #11, #13, #15

**Per-shop menu / product bleed (#6 — Critical):** every shop under a tenant currently renders the identical
24-item menu because `ProductRepository.java:21` matches `shopId = :shopId OR shopId IS NULL` and 24/25 seeded
products have `shop_id = NULL`. **Resolve the `IS NULL` fallback deliberately** (CONTEXT grants this as
discretion, decided at planning from data reality — 24/25 products need a `shop_id` either way):
- **Decision direction:** require `shop_id` on product creation and **scope the query to the shop** (drop the
  `OR shopId IS NULL` bleed), OR — only if a product decision explicitly says tenant-wide items are a feature —
  render NULL-scoped items as a clearly-labelled "Available at all our shops" section, **not** silently
  duplicated into each shop's own categories. Re-seed demo data so no duplicate line items ("Jollof Rice" twice)
  appear. The UI contract: **a shop shows its own menu; identical duplicated rows never render.**
- **Menu empty state** (new): a shop with zero assigned products shows a centered empty state
  (`UtensilsCrossed h-12 w-12 text-slate-300`, "No items yet", "This kitchen hasn't added anything to its menu.").

**Card anatomy + image handling (#15):** the shop card (`app/shop/page.tsx` `ShopCard`) is the reference and
stays. **Contract for every card image** (shop banner, shop logo, product image): use `SafeImage` with a branded
fallback (the orange→rose gradient for banners; `Store`/`UtensilsCrossed` icon tile for logos/products) — **no
broken `<img>` ever renders**. Playwright asserts populated images resolve `naturalWidth > 0`. Product cards
follow: image-or-fallback → name (`font-semibold text-slate-900`) → price (`text-orange-600 font-bold`) →
description (`line-clamp-2 text-sm text-slate-500`) → add control.

**Empty-state vertical balance (#15):** cart + checkout empty states center within `min-h-[60vh]` instead of
pinning to the top (kills the ~700px dead grey space the audit flagged).

**Quiet the 401 probe (#13):** `lib/customer-auth.ts` logs `console.error` on every expected-401 unauthenticated
session check. Handle the expected 401 quietly (no `console.error`) — a clean console on public page loads.

**`text-[10px]` (#11):** replace all storefront occurrences with `text-xs` (see Spacing exceptions).

---

## Surface H — Track Page · backlog #9 (Major)

`/track` currently forces a **full sign-in wall** (`RequireCustomerAuth`), contradicting its own name and
duplicating the better email-gated guest pattern already on the order-confirmation page.

**Fix — consolidate on the guest-tracking pattern (order number + email), no forced sign-in:**
- Remove the `RequireCustomerAuth` wrapper. The page is a **guest lookup**: two inputs — order number
  (`font-mono`) + email — calling `GET /public/orders/{orderNumber}?email=` (the existing, IDOR-hardened
  endpoint from AUDIT-W0-02, which requires the email as proof-of-ownership).
- If a customer session exists, **pre-fill** the email (convenience) — but never require it.
- Keep the existing progress stepper (Received → Confirmed → Preparing → Ready → Completed) and 15s auto-refresh.
- **Shell:** replace the page's bespoke mini-header with the **Shared Public Shell** (Surface B) so `/track` is
  visually part of the family and reachable from the public header/footer.

**Inbound links (closes the orphan):** `/track` is linked from (1) the Shared Public Shell header + footer,
(2) `StorefrontNav` "Track order", and (3) the order-confirmation page (`/shop/[slug]/orders/[orderNumber]`) via a
"Track this order" affordance. Post-fix, `grep -rn 'href="/track'` returns ≥3 matches (currently 0).

---

## Interaction & State Contracts (consistent across surfaces) · backlog #13, #14

The audit dinged *inconsistency*, not absence. These are the app-wide contracts every surface conforms to:

| State | Dashboard | Public / Storefront |
|-------|-----------|---------------------|
| **Loading (full page)** | `h-32 w-32 animate-spin border-b-2 border-t-2 border-blue-600` centered | `Loader2 animate-spin text-orange-500` centered |
| **Loading (list/grid)** | `animate-pulse` skeleton cards (shop-directory skeleton is the reference — reuse its shape) | same skeleton idiom |
| **Empty** | centered column, `icon h-12 w-12 text-slate-300`, `h3 text-lg font-semibold`, `p text-sm text-slate-500`, `outline` button | same, with orange primary button where an action exists |
| **Error (transient/action)** | `useToast({ variant: "destructive" })` title + description | inline `bg-red-50 border-red-100 text-red-700` banner |
| **Error (route boundary)** | `app/error.tsx` generic copy — **leave as-is** (#14; acceptable last-resort) | same |
| **Success (transient)** | `useToast()` default variant / emerald confirmation block | emerald confirmation block (order-confirmed pattern) |

- **Skeletons, not spinners, for content grids** (directory, products, orders) so layout doesn't jump.
- **Toasts** are the dashboard error channel; **inline banners** are the storefront error channel — do not mix.
- Expected-401 session probes are **silent** (backlog #13) — no `console.error` on public loads.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Landing H1 | **Order from local kitchens. Or run yours.** (executor may refine within the split-persona intent; must name both audiences, no serif) |
| Landing — customer door CTA | **Order food near you** (→ `/shop`) |
| Landing — operator door CTA | **Run your food business** (→ `/for-operators`) |
| Public header nav | Shops · For operators · Track order · Sign in |
| Checkout — fulfilment toggle | **Delivery** / **Collection** |
| Checkout — primary CTA | **Place order · £{total}** (unchanged idiom; now after a definite fee breakdown) |
| Checkout — postcode error | Enter a valid UK postcode (e.g. SW1A 1AA) |
| Checkout — address required error | Add a delivery address, or switch to Collection. |
| Track — primary CTA | **Track order** |
| Track — not found error | Order not found. Check your order number and email address. |
| Directory — empty (no shops) | Heading **No shops found** · body "Try a different search term." / "No shops are currently available." (existing — keep) |
| Menu — empty (shop, no products) | Heading **No items yet** · body "This kitchen hasn't added anything to its menu." |
| Kitchen — empty | Heading **No active orders** · body "Orders will appear here when customers place them." (existing — keep) |
| Kitchen/order line item | `{qty}x {productName}` — **never** "Unknown Product" for an existing product |
| Cart — empty | **Your basket is empty** (existing storefront microcopy — keep, center vertically) |
| Primary CTA (dashboard, new) | verb + noun, blue-600 default `Button` (e.g. "Save changes", "Go live") — no `font-bold` |

**Destructive actions in Phase 19:** **none new.** This phase completes navigation and flows; it exposes no
delete/withdraw endpoints. Existing destructive actions (order cancel, refund — Phase 17) are unchanged and keep
their `destructive`-variant confirmation dialogs. No new destructive-confirmation copy is introduced.

---

## Registry Safety

| Registry | Blocks used | Safety gate |
|----------|-------------|-------------|
| shadcn official | Existing: `badge`, `button`, `card`, `dialog`, `dropdown-menu`, `input`, `label`, `pagination`, `select`, `table`, `toast`. New (official): `sheet` (mobile drawer), optional `tabs` (checkout toggle — bespoke preferred). | **not required** (official registry) |
| third-party | **none** | not applicable |

**No third-party registries declared → registry vetting gate not triggered.** `components.json` is present
(shadcn initialized), style `new-york`, baseColor `slate`, cssVariables. `npx shadcn view/diff` not required for
any block in this phase. Verified 2026-07-11: no `fetch`/`process.env`/`eval`/dynamic-import review needed —
zero external-registry code enters the contract.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
