---
phase: 19-full-frontend-experience-overhaul
plan: 05
type: execute
wave: 2
depends_on: [19-03]
files_modified:
  - frontend/app/for-operators/page.tsx
  - frontend/app/business-model-guide/page.tsx
  - frontend/components/marketing/operator-pitch.tsx
  - frontend/components/marketing/business-model-guide.tsx
  - frontend/components/marketing/__tests__/operator-pitch.test.tsx
  - frontend/components/marketing/__tests__/business-model-guide.test.tsx
  - frontend/app/track/page.tsx
  - frontend/app/shop/[slug]/orders/[orderNumber]/page.tsx
autonomous: true
requirements: [UIX-01]

must_haves:
  truths:
    - "/for-operators and /business-model-guide keep their dark full-bleed pitch layout + copy but use only design-token colours within the orange/emerald/slate family (zero hardcoded hex)"
    - "Both marketing pages are wrapped in the shared PublicShell (part of the connected surface)"
    - "/track is a guest lookup (order number + email) with NO forced sign-in, wrapped in PublicShell, and reachable from >=3 inbound links"
    - "The order-confirmation page offers a 'Track this order' affordance to /track"
  artifacts:
    - path: "frontend/components/marketing/operator-pitch.tsx"
      provides: "token-skinned operator pitch (no hex)"
    - path: "frontend/app/track/page.tsx"
      provides: "guest order lookup wrapped in PublicShell (no RequireCustomerAuth)"
  key_links:
    - from: "frontend/app/track/page.tsx"
      to: "GET /public/orders/{orderNumber}?email="
      via: "publicApiClient guest lookup (IDOR-hardened, AUDIT-W0-02)"
      pattern: "/public/orders/"
    - from: "frontend/app/for-operators/page.tsx"
      to: "PublicShell"
      via: "wraps the marketing content in the shared shell"
      pattern: "PublicShell"
---

<objective>
Close UIX-01 Surfaces C + H (backlog #7, #9):

1. **Marketing re-skin (#7):** `operator-pitch.tsx` and `business-model-guide.tsx` each hardcode a bespoke palette (17-20 hex values) that bypasses the design-token system entirely. Migrate BOTH onto Tailwind tokens within the LOCKED orange/emerald/slate family. This is a re-skin, not a rebuild — the dark, confident, full-bleed layout and the (genuinely sharp) copy are PRESERVED; only the colour values change. Wrap both pages in the shared `PublicShell` so they join the connected surface. NO serif, NO new palette, NO editorial aesthetic.
2. **Track page (#9):** `/track` forces a full sign-in wall (`RequireCustomerAuth`), contradicting its name and duplicating the better email-gated guest pattern. Consolidate on the existing IDOR-hardened guest endpoint (order number + email), remove the auth wall, wrap in `PublicShell`, keep the stepper + 15s refresh, and add a "Track this order" affordance on the order-confirmation page.

Purpose: converge the marketing surface into one product identity and make order tracking guest-accessible + reachable.
Output: token-skinned marketing pages, a guest track page, and cross-links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-full-frontend-experience-overhaul/19-UI-SPEC.md
@.planning/phases/19-full-frontend-experience-overhaul/19-PATTERNS.md
@.planning/phases/19-full-frontend-experience-overhaul/19-RESEARCH.md

<interfaces>
<!-- Exact hex → token mapping (UI-SPEC Surface C). Layout + copy PRESERVED. -->
operator-pitch.tsx: navy #211c36/#1a1a33 → bg-slate-900/bg-slate-950 ; orange #f26522 → text-orange-500/bg-orange-500 ; yellow #ffdf7e → text-amber-300 (on dark).
business-model-guide.tsx: teal #122c33 → bg-slate-900 ; rust #b75d2c → text-orange-600/bg-orange-500 ; olive #e4eecd → bg-slate-50/bg-emerald-50.
Success gate: `grep -rlE "#[0-9a-fA-F]{3,8}" components/marketing/*.tsx` == 0.

Track page (frontend/app/track/page.tsx): RequireCustomerAuth wrapper at :38,42 (REMOVE); email pre-fill from session at :49-55 (KEEP, never require); endpoint GET /public/orders/{orderNumber}?email= at :82-84 (IDOR-hardened, AUDIT-W0-02 — verify/email is mandatory proof-of-ownership); bespoke mini-header at :115-125 (replace with PublicShell).
PublicShell: frontend/components/public/public-shell.tsx (created in 19-03).
Note: text-[10px] on marketing/track is NOT touched here — plan 19-08 sweep owns the app-wide text-[10px]→text-xs pass.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Marketing token re-skin (operator-pitch + business-model-guide) + PublicShell wrap</name>
  <files>frontend/app/for-operators/page.tsx, frontend/app/business-model-guide/page.tsx, frontend/components/marketing/operator-pitch.tsx, frontend/components/marketing/business-model-guide.tsx, frontend/components/marketing/__tests__/operator-pitch.test.tsx, frontend/components/marketing/__tests__/business-model-guide.test.tsx</files>
  <read_first>
    - frontend/components/marketing/operator-pitch.tsx (whole file — hardcoded hex to migrate; layout + copy to PRESERVE)
    - frontend/components/marketing/business-model-guide.tsx (whole file — the third bespoke palette to migrate)
    - frontend/app/for-operators/page.tsx + frontend/app/business-model-guide/page.tsx (the page wrappers to add PublicShell around)
    - frontend/components/marketing/__tests__/operator-pitch.test.tsx + business-model-guide.test.tsx (tests that assert on hex — flip to assert tokens)
    - 19-UI-SPEC.md § Color "/for-operators + /business-model-guide re-skin" table (exact hex→token mapping) + § Typography (public display exception cap: text-6xl/700 max; no text-7xl/font-black)
  </read_first>
  <action>
    Migrate every hardcoded hex in `operator-pitch.tsx` and `business-model-guide.tsx` to Tailwind tokens per the UI-SPEC Surface C mapping (navy/teal → `bg-slate-900`/`bg-slate-950`; orange hexes → `orange-500`/`orange-600`; yellow → `amber-300`; olive → `slate-50`/`emerald-50`). PRESERVE the dark full-bleed layout and all copy — this is a re-skin. Converge both pages on the SAME dark surface (`bg-slate-900`). Cap the hero display at `text-6xl` / weight 700 (public display exception); remove any `text-7xl`/`font-black`/serif. Wrap the rendered content of `app/for-operators/page.tsx` and `app/business-model-guide/page.tsx` in `<PublicShell>` (import from `@/components/public/public-shell`) so they carry the shared header/footer. Update the two marketing test files to assert TOKEN classes (e.g. `bg-slate-900`, `text-orange-500`) instead of the old hex, and to assert the PublicShell renders.
  </action>
  <acceptance_criteria>
    - `grep -rlE "#[0-9a-fA-F]{3,8}" components/marketing/*.tsx | wc -l` == 0 (no hardcoded hex remains).
    - `grep -rc "text-primary\|bg-primary\|orange-" components/marketing/operator-pitch.tsx components/marketing/business-model-guide.tsx` > 0 (now uses tokens — was 0).
    - `grep -rcE "text-7xl|font-black|font-serif" components/marketing/*.tsx` == 0.
    - `app/for-operators/page.tsx` and `app/business-model-guide/page.tsx` import + render `PublicShell`.
    - Marketing tests assert tokens (not hex) and pass; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && [ "$(grep -rlE '#[0-9a-fA-F]{3,8}' components/marketing/*.tsx | wc -l)" -eq 0 ] && npx jest components/marketing/__tests__ && npm run build</automated>
  </verify>
  <done>Both marketing pages use only tokens within the locked family, preserve their layout/copy, and are wrapped in PublicShell; tests assert tokens; zero hex remains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Track page → guest lookup (no auth wall) + PublicShell + order-confirmation cross-link</name>
  <files>frontend/app/track/page.tsx, frontend/app/shop/[slug]/orders/[orderNumber]/page.tsx</files>
  <read_first>
    - frontend/app/track/page.tsx (RequireCustomerAuth :38,42; email pre-fill :49-55; endpoint :82-84; stepper :95-107; mini-header :115-125)
    - frontend/components/storefront/require-customer-auth.tsx (the wrapper being removed)
    - frontend/app/shop/[slug]/orders/[orderNumber]/page.tsx (order-confirmation — add a "Track this order" link to /track)
    - 19-UI-SPEC.md § Surface H + § Copywriting Contract (Track CTA, not-found error) + § Interaction Contracts
    - 19-RESEARCH.md § Pattern 7 (reuse the mandatory-verify endpoint; never a second auth wall) + § Pitfall 5 (domcontentloaded for the 15s auto-refresh page)
  </read_first>
  <behavior>
    - Test: /track renders two inputs (order number + email) and NO sign-in wall (RequireCustomerAuth absent).
    - Test: submitting order# + email calls GET /public/orders/{orderNumber}?email= and renders the stepper.
    - Test: if a customer session exists, email is pre-filled but not required.
    - Test: not-found renders "Order not found. Check your order number and email address."
  </behavior>
  <action>
    In `app/track/page.tsx`: remove the `RequireCustomerAuth` wrapper (:38,42) and its import; the page becomes a guest lookup — order number (`font-mono`) + email inputs calling the existing IDOR-hardened `publicApiClient.get('/public/orders/{orderNumber}', { params: { email }})` (email is mandatory proof-of-ownership per AUDIT-W0-02). Keep the session email PRE-FILL (:49-55) but never require it. Keep the progress stepper + 15s auto-refresh. Replace the bespoke mini-header (:115-125) with `<PublicShell>`. On `app/shop/[slug]/orders/[orderNumber]/page.tsx`, add a "Track this order" affordance linking to `/track?order={orderNumber}&email={email}` so the confirmation page de-orphans /track (its 3rd inbound link). Do NOT change the text-[10px] here (plan 19-08 sweep owns it). Add/adjust a Jest test for the guest-lookup render + no-auth-wall.
  </action>
  <acceptance_criteria>
    - `grep -c "RequireCustomerAuth" app/track/page.tsx` == 0.
    - `app/track/page.tsx` imports + renders `PublicShell` and calls `/public/orders/`.
    - `app/shop/[slug]/orders/[orderNumber]/page.tsx` contains a link to `/track`.
    - App-wide `grep -rn 'href="/track"' app components | wc -l` >= 3 (public footer/header + storefront-nav from 19-03, now + confirmation page).
    - Track Jest test passes; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c "RequireCustomerAuth" app/track/page.tsx | grep -qx 0 && grep -q "PublicShell" app/track/page.tsx && npx jest app/__tests__ track 2>/dev/null; npm run build</automated>
  </verify>
  <done>/track is a guest lookup with no forced sign-in, wrapped in PublicShell, reachable from >=3 links; confirmation page links to it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guest → GET /public/orders/{orderNumber}?email= | order lookup must prove ownership (email) — no enumeration |
| /track (public) | must not require or leak a session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05-01 | Info Disclosure | /track order enumeration by email only | mitigate | Reuse the AUDIT-W0-02 mandatory-verify endpoint (order# + email required) + existing public IP rate limiter (#88); do NOT build a second lookup path |
| T-19-05-02 | Info Disclosure | removing the auth wall exposes account data | mitigate | The guest lookup returns only the single matched order's status (existing endpoint contract); no account/order-list surface is added |
| T-19-05-03 | Tampering (XSS) | re-skin/PublicShell adds CSP-unsafe markup | mitigate | Token-only class changes; pages stay server-component-wrapped; csp-no-violations gate at 19-09 |
| T-19-05-SC | Tampering | supply chain | accept | No package installs (token re-skin + existing components) |
</threat_model>

<verification>
Scoped: `npx jest components/marketing/__tests__ track` + `npm run build`. Marketing hex gate: `grep -rlE "#[0-9a-fA-F]{3,8}" components/marketing/*.tsx` == 0. Playwright storefront/csp specs at closure (19-09).
</verification>

<success_criteria>
- Zero hardcoded hex in components/marketing; both pages use tokens + PublicShell; layout/copy preserved.
- /track is a guest lookup (no auth wall), PublicShell-wrapped, reachable from >=3 inbound links.
- No serif/editorial; hero display capped at text-6xl/700.
</success_criteria>

<output>
Create `.planning/phases/19-full-frontend-experience-overhaul/19-05-SUMMARY.md` when done. Commit (do NOT push).
</output>
