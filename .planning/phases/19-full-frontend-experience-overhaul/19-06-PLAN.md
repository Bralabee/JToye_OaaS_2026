---
phase: 19-full-frontend-experience-overhaul
plan: 06
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - frontend/app/shop/[slug]/checkout/page.tsx
  - frontend/app/shop/[slug]/cart/page.tsx
  - frontend/app/shop/[slug]/page.tsx
  - frontend/app/shop/[slug]/checkout/__tests__/checkout.test.tsx
  - frontend/e2e/storefront-flows.spec.ts
autonomous: true
requirements: [UIX-04]

must_haves:
  truths:
    - "Checkout asks fulfilment type first (Delivery | Collection); a UK address block renders only for Delivery and is validated"
    - "A definite fee breakdown (subtotal + delivery + VAT + total) is shown BEFORE payment — the deferred 'Delivery fee may apply' footnote is gone"
    - "The client fee preview mirrors the server waiver exactly; delivery fee = £0 for Collection or above the free threshold"
    - "The order-creation payload carries fulfilmentType and (for delivery) address"
    - "A shop with zero assigned products shows a 'No items yet' menu empty state; product-card images use SafeImage (no broken img)"
    - "Empty cart + checkout centre within min-h-[60vh] (no ~700px dead space)"
  artifacts:
    - path: "frontend/app/shop/[slug]/checkout/page.tsx"
      provides: "fulfilment toggle + conditional address + fee-before-payment"
      contains: "fulfilmentType"
    - path: "frontend/app/shop/[slug]/checkout/__tests__/checkout.test.tsx"
      provides: "fulfilment/postcode/fee-preview unit coverage"
  key_links:
    - from: "frontend/app/shop/[slug]/checkout/page.tsx"
      to: "GET /public/shops/{slug}"
      via: "publicApiClient shop fetch for deliveryFeePennies + freeDeliveryThresholdPennies"
      pattern: "/public/shops/"
    - from: "checkout payload"
      to: "POST /public/shops/{slug}/orders"
      via: "GuestOrderRequest gains fulfilmentType + address"
      pattern: "fulfilmentType"
---

<objective>
Close UIX-04 frontend (backlog #3, Blocker) + storefront polish (#15): checkout collects Full name / Email / Phone / Notes only — no delivery address exists in the file — and hides the fee behind "Final total confirmed after order is placed. Delivery fee may apply." This fails the Deliveroo/Just Eat comparator (ETA + fee before pay) and cannot fulfil a delivery order.

Per the LOCKED decision: delivery + collection; ask fulfilment type first; address form only for delivery; full fee breakdown BEFORE payment. The V45 backend (plan 19-01) persists `fulfilmentType` + address; this plan wires the frontend end-to-end. Also fix the empty-state dead space and the shop menu empty state that per-shop scoping (19-02) can now surface.

Purpose: a customer sees exactly what they'll pay and can actually place a delivery order.
Output: fulfilment toggle + conditional address + fee-before-payment on checkout, centred empty states, a shop menu empty state, and updated unit + e2e coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-full-frontend-experience-overhaul/19-UI-SPEC.md
@.planning/phases/19-full-frontend-experience-overhaul/19-PATTERNS.md
@.planning/phases/19-full-frontend-experience-overhaul/19-RESEARCH.md

<interfaces>
<!-- Backend contract from plan 19-01 (consume, do not re-derive). -->
GuestOrderRequest now accepts: fulfilmentType ("DELIVERY"|"COLLECTION") + address (line1, line2?, city, postcode) — server validates + recomputes fee.
Shop fee source: GET /public/shops/{slug} → PublicShopDto with deliveryFeePennies + freeDeliveryThresholdPennies (PublicShopDto.java:19-20). Checkout does NOT fetch the shop today — ADD this (copy the publicApiClient.get idiom from app/shop/page.tsx).
Server fee waiver to mirror EXACTLY (PublicStorefrontService.java:424-428): deliveryFee = shop.deliveryFeePennies; waived to 0 when subtotal >= freeDeliveryThresholdPennies OR fulfilment == COLLECTION.
VAT idiom (unchanged): Math.floor(total * 20 / 120) VAT-inclusive.

Current checkout (app/shop/[slug]/checkout/page.tsx): form fields id="name"/"email"/"phone"/"notes" (:477,490,503,516 — no address); deferred footnote :558-561 ("Estimated total" + "Final total confirmed..."); post-order breakdown rows already exist at :386-413 (copy their shape); order payload built ~:212-224; input styling idiom `rounded-lg border-slate-200 px-3 py-2 focus:ring-orange-100 focus:border-orange-300`.
UK postcode regex (UI-SPEC Surface E): ^[A-Z]{1,2}\d[A-Z\d]?\s?\d[A-Z]{2}$ + auto-uppercase on blur.
SafeImage: components/ui/safe-image.tsx (Playwright asserts naturalWidth>0). Note: text-[10px] here is NOT touched — plan 19-08 sweep owns it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fulfilment toggle + conditional UK address + shop fetch + fee-before-payment</name>
  <files>frontend/app/shop/[slug]/checkout/page.tsx</files>
  <read_first>
    - frontend/app/shop/[slug]/checkout/page.tsx (Step-1 form :456-591; deferred footnote :557-561; existing breakdown rows :386-413; payload :212-224; input idiom :477+)
    - frontend/app/shop/page.tsx (the publicApiClient.get shop-fetch idiom to copy)
    - 19-UI-SPEC.md § Surface E (segmented toggle, conditional address table, fee-breakdown order, validation/error states, Total weight exception) + § Copywriting Contract (postcode error, address-required error, Place order CTA)
  </read_first>
  <behavior>
    - Test: default fulfilment = DELIVERY; the UK address block renders; toggling to COLLECTION hides it and zeroes delivery fee.
    - Test: submitting DELIVERY with an empty required address field or invalid postcode blocks submit with the exact inline errors; postcode uppercases on blur.
    - Test: the fee breakdown (subtotal + delivery-or-Free + VAT + total) renders BEFORE payment; the "Final total confirmed..." footnote is gone.
    - Test: the order payload includes fulfilmentType and (for delivery) address.
  </behavior>
  <action>
    Add a bespoke 2-button segmented control at the top of Step 1 — `Delivery` (`Bike`/`Truck`) | `Collection` (`ShoppingBag`/`Store`), `grid-cols-2`, selected = `bg-orange-500 text-white`, other = `bg-slate-100 text-slate-600`; state `fulfilmentType: "DELIVERY" | "COLLECTION"`, default DELIVERY. Render a conditional UK address block (only when DELIVERY) above "Your details" using the existing input idiom: `address1` (required), `address2` (optional), `city` (required), `postcode` (required, regex `^[A-Z]{1,2}\d[A-Z\d]?\s?\d[A-Z]{2}$`, auto-uppercase on blur). Fetch the shop via `publicApiClient.get('/public/shops/{slug}')` to read `deliveryFeePennies` + `freeDeliveryThresholdPennies`. REMOVE the deferred footnote (:557-561) and replace with a DEFINITE breakdown copied from the existing post-order rows (:386-413): Subtotal, Delivery (`formatPrice(deliveryFee)` or "Free" in `text-emerald-600 font-semibold` for COLLECTION/above-threshold), VAT (incl. 20%, `Math.floor(total*20/120)`), Total (`text-lg font-bold text-slate-900` — inside the documented weight exception). Mirror the server waiver EXACTLY (deliveryFee=shop.deliveryFeePennies, 0 when subtotal>=freeDeliveryThreshold or COLLECTION). Field-level errors: invalid/empty required field → `border-red-300` + inline `text-xs text-red-600`, block submit; postcode error "Enter a valid UK postcode (e.g. SW1A 1AA)"; address-required "Add a delivery address, or switch to Collection." Add `fulfilmentType` + (delivery) `address: { line1, line2?, city, postcode }` to the order payload. The client fee is a PREVIEW; the server total stays authoritative.
  </action>
  <acceptance_criteria>
    - `grep -c "Final total confirmed" checkout/page.tsx` == 0 (footnote removed).
    - `grep -c "fulfilmentType" checkout/page.tsx` >= 1 and the payload includes it.
    - `grep -c "/public/shops/" checkout/page.tsx` >= 1 (shop fetch added).
    - Checkout renders Subtotal + Delivery/Free + VAT + Total before payment (grep: `grep -c "Subtotal" checkout/page.tsx` >= 1).
    - Postcode regex `[A-Z]{1,2}` present; uppercase-on-blur handler present.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c "Final total confirmed" app/shop/\[slug\]/checkout/page.tsx | grep -qx 0 && grep -q "fulfilmentType" app/shop/\[slug\]/checkout/page.tsx && grep -q "/public/shops/" app/shop/\[slug\]/checkout/page.tsx && npm run build</automated>
  </verify>
  <done>Checkout asks fulfilment first, collects + validates a UK address for delivery, and shows a definite fee breakdown before payment; payload carries fulfilment/address.</done>
</task>

<task type="auto">
  <name>Task 2: Empty-state centring (cart + checkout) + shop menu empty state + product-card SafeImage</name>
  <files>frontend/app/shop/[slug]/cart/page.tsx, frontend/app/shop/[slug]/page.tsx</files>
  <read_first>
    - frontend/app/shop/[slug]/cart/page.tsx (empty-cart state pinned to top)
    - frontend/app/shop/[slug]/page.tsx (menu render + product cards — now that per-shop scoping (19-02) can yield zero products)
    - frontend/components/ui/safe-image.tsx (the mandated image-with-fallback)
    - frontend/app/shop/page.tsx (ShopCard — the reference card anatomy)
    - 19-UI-SPEC.md § Surface G (menu empty state copy, card image contract, empty-state vertical balance) + § Copywriting Contract (Menu empty, Cart empty)
  </read_first>
  <action>
    Cart empty state (`app/shop/[slug]/cart/page.tsx`): centre within `min-h-[60vh] flex flex-col items-center justify-center` (kills the ~700px dead space) — keep the "Your basket is empty" microcopy. Same centring for the empty-cart branch on checkout if present. On `app/shop/[slug]/page.tsx`: add a menu empty state for a shop with zero assigned products — centered `UtensilsCrossed h-12 w-12 text-slate-300`, heading "No items yet", body "This kitchen hasn't added anything to its menu." Ensure every product-card image (and shop banner/logo) uses `SafeImage` with a branded fallback (orange→rose gradient for banners; `UtensilsCrossed`/`Store` icon tile for products/logos) so no broken `<img>` renders. Do NOT touch text-[10px] here (plan 19-08 owns it). Do NOT touch purple (none here).
  </action>
  <acceptance_criteria>
    - `grep -c "min-h-\[60vh\]" app/shop/[slug]/cart/page.tsx` >= 1.
    - `app/shop/[slug]/page.tsx` contains the "No items yet" empty-state heading + copy.
    - Product-card images use `SafeImage` (grep: `grep -c "SafeImage" app/shop/[slug]/page.tsx` >= 1).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -q "min-h-\[60vh\]" app/shop/\[slug\]/cart/page.tsx && grep -q "No items yet" app/shop/\[slug\]/page.tsx && npm run build</automated>
  </verify>
  <done>Empty cart/checkout centre in min-h-[60vh]; a zero-product shop shows a menu empty state; product cards use SafeImage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Checkout unit tests + storefront-flows e2e update</name>
  <files>frontend/app/shop/[slug]/checkout/__tests__/checkout.test.tsx, frontend/e2e/storefront-flows.spec.ts</files>
  <read_first>
    - frontend/e2e/storefront-flows.spec.ts (existing checkout e2e — a regression tripwire; update for fulfilment/address/fee-before-pay + per-shop menu)
    - frontend/__tests__/shop/cart.test.tsx (Jest conventions: render/screen, resolvedThenable for use() params, localStorage.clear())
    - 19-VALIDATION.md § Per-Task Verification Map (UIX-04 rows) + 19-RESEARCH.md § Regression Tripwires + § Pitfall 5 (domcontentloaded)
  </read_first>
  <behavior>
    - Test (jest): fulfilment toggle switches address visibility; invalid postcode blocks; fee preview equals the server waiver (delivery shown when below threshold, "Free" for collection/above threshold).
    - Test (e2e): add item → checkout → choose Delivery → enter address → see fee breakdown before pay; per-shop menu shows the shop's own items (no duplicate rows) — use domcontentloaded.
  </behavior>
  <action>
    Create `checkout.test.tsx` (Jest) covering: fulfilment toggle visibility, conditional address requiredness, postcode validation + uppercase, and the fee-preview computation (mock the shop fetch; assert client total == expected server total for delivery-below-threshold, delivery-above-threshold (Free), and collection (Free)). Update `e2e/storefront-flows.spec.ts` to walk the new checkout (Delivery → address → fee breakdown visible before pay) and assert a per-shop menu shows only that shop's products with no duplicate line items (from 19-02). Use `domcontentloaded`. Do NOT edit docs/metrics.json (plan 19-09 reconciles).
  </action>
  <acceptance_criteria>
    - `checkout.test.tsx` asserts fee-preview parity for all three cases (delivery below/above threshold, collection) and postcode validation.
    - `storefront-flows.spec.ts` asserts fee breakdown visible before pay + per-shop menu (no duplicate rows).
    - `npx jest app/shop/\[slug\]/checkout/__tests__/checkout.test.tsx` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx jest app/shop/\[slug\]/checkout/__tests__/checkout.test.tsx</automated>
  </verify>
  <done>Checkout unit tests cover fulfilment/address/postcode/fee-parity; storefront e2e updated for the new flow + per-shop menu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkout form → POST /public/shops/{slug}/orders | untrusted fulfilment/address/fee inputs cross here |
| client fee preview vs server total | client value must never become authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06-01 | Tampering | client sets a lower fee/fulfilment to underpay | mitigate | Fee is a display PREVIEW only; server (19-01) recomputes the delivery fee + total and validates fulfilment/address; e2e asserts client==server total |
| T-19-06-02 | Tampering/DoS | oversized/injected address | mitigate | Client field limits + regex; server @Size caps (19-01) are the real guard |
| T-19-06-03 | Info Disclosure | address (PII) rendered/logged on the client | mitigate | Address is shown only to the ordering customer; not written to console/analytics; no address in client logs |
| T-19-06-04 | Tampering (XSS) | new checkout markup adds CSP-unsafe inline | mitigate | Token/component-only changes; csp-no-violations gate at 19-09 |
| T-19-06-SC | Tampering | supply chain | accept | No package installs |
</threat_model>

<verification>
Scoped: `npx jest app/shop/[slug]/checkout/__tests__/checkout.test.tsx` + `npm run build` (tsc). storefront-flows Playwright runs against the rebuilt stack at closure (19-09). Confirm the client fee preview equals the server total in the e2e.
</verification>

<success_criteria>
- Checkout asks fulfilment first, collects + validates a UK delivery address, and shows a definite fee breakdown before payment (footnote removed).
- Client fee mirrors the server waiver exactly; payload carries fulfilment/address; server total stays authoritative.
- Empty cart/checkout centred; zero-product shop shows a menu empty state; product cards use SafeImage.
</success_criteria>

<output>
Create `.planning/phases/19-full-frontend-experience-overhaul/19-06-SUMMARY.md` when done. Commit (do NOT push).
</output>
