---
phase: 22-notifications-comms
plan: 06
type: execute
wave: 4
depends_on: [22-03, 22-05]
files_modified:
  - frontend/lib/api-client.ts
  - frontend/lib/webhooks-api.ts
  - frontend/app/dashboard/webhooks/page.tsx
  - frontend/app/dashboard/webhooks/[id]/page.tsx
  - frontend/components/dashboard/webhooks/status-badge.tsx
  - frontend/components/dashboard/webhooks/WebhookCreateDialog.tsx
  - frontend/components/dashboard/webhooks/SecretRevealDialog.tsx
  - frontend/components/dashboard/webhooks/ConfirmActionDialog.tsx
  - frontend/components/dashboard/sidebar.tsx
  - frontend/app/dashboard/webhooks/__tests__/webhooks-page.test.tsx
  - frontend/app/dashboard/webhooks/__tests__/delivery-log.test.tsx
autonomous: true
requirements: [COMMS-06]
must_haves:
  truths:
    - "A vendor can add an HTTPS webhook endpoint, see it in the list, pause/resume/revoke it, and rotate its secret from the dashboard"
    - "On create/rotate the signing secret is shown ONCE in a focus-trapped dialog with a copy button and cannot be dismissed by backdrop click"
    - "The endpoint detail page shows a delivery log filterable by event type + status, surfaces an AUTO-PAUSED amber alert, and offers a manual Replay carrying an Idempotency-Key header"
    - "Every subscription/delivery state renders a tinted badge with a lucide icon AND a text label (never colour alone)"
    - "The list and delivery log render as cards below sm and a Table at sm+ with NO horizontal overflow at 375px, and the new routes ship no unbounded/duplicate bundle growth (mobile-first web-perf contract)"
  artifacts:
    - path: "frontend/app/dashboard/webhooks/page.tsx"
      provides: "subscription list + create/pause/resume/revoke/rotate (Surface A)"
      min_lines: 120
    - path: "frontend/app/dashboard/webhooks/[id]/page.tsx"
      provides: "endpoint detail + delivery-log browser + filter + replay (Surface B)"
      min_lines: 120
    - path: "frontend/components/dashboard/webhooks/status-badge.tsx"
      provides: "statusConfig taxonomy (icon+label per subscription/delivery state)"
      contains: "lucide"
  key_links:
    - from: "webhooks/page.tsx"
      to: "/api/v1/webhooks"
      via: "apiClient GET/POST"
      pattern: "apiClient"
    - from: "WebhookCreateDialog"
      to: "SecretRevealDialog"
      via: "create response returns plaintext secret once"
      pattern: "signingSecret"
    - from: "[id]/page.tsx replay"
      to: "/webhooks/{id}/deliveries/{deliveryId}/replay"
      via: "makeIdempotencyKey() header"
      pattern: "Idempotency-Key"
---

<objective>
Build the vendor-facing webhook management + delivery-log UI (COMMS-06), mobile-first at 375px: a subscriptions list (`/dashboard/webhooks`) with create/pause/resume/revoke + once-only secret reveal + rotate, and an endpoint detail page (`/dashboard/webhooks/[id]`) with a filterable delivery-log browser, auto-pause alert, and a manual Idempotency-Key-safe Replay. Follow the LOCKED `22-UI-SPEC.md` contract exactly (orange/emerald/slate food-delivery palette, card-stacking below sm, icon+label badges).

Purpose: the self-serve surface for the machine channel. Reuses `orders/page.tsx` (statusConfig/filter/Table→cards), `RefundDialog.tsx` (Dialog+Zod+makeIdempotencyKey), and the dashboard shell.

**Web-perf ownership (CLAUDE.md cross-cutting, W5):** `/dashboard/webhooks` + `/dashboard/webhooks/[id]` are new user-facing routes, so they own their Core Web Vitals at a throttled mobile profile. This plan's build-time obligation: reuse existing primitives only (no new heavy deps), no unbounded/duplicate bundle growth. The throttled-mobile CWV smoke (LCP/CLS/INP) on these routes is MEASURED in 22-07's Playwright E2E pass; this plan asserts the bundle-size guardrail here.
Output: api-client webhook methods + 2 pages + 3 dialogs + status-badge helper + one sidebar nav entry + Jest render/375px tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-notifications-comms/22-SPEC.md
@.planning/phases/22-notifications-comms/22-UI-SPEC.md
@.planning/phases/22-notifications-comms/22-PATTERNS.md

# Backend contracts consumed (read the SUMMARYs for exact endpoint + DTO shapes):
@.planning/phases/22-notifications-comms/22-03-SUMMARY.md
@.planning/phases/22-notifications-comms/22-05-SUMMARY.md

<interfaces>
<!-- REST surface this UI consumes (from 22-03 + 22-05). -->
GET    /api/v1/webhooks                         -> WebhookSubscriptionDto[] (no secret)
POST   /api/v1/webhooks                         -> { ...dto, signingSecret } (secret ONCE)
GET    /api/v1/webhooks/{id}                    -> WebhookSubscriptionDto (no secret)
POST   /api/v1/webhooks/{id}/rotate-secret      -> { signingSecret } (ONCE)
POST   /api/v1/webhooks/{id}/pause | /resume | /revoke
GET    /api/v1/webhooks/{id}/deliveries?eventType=&status=&page=  -> paged WebhookDelivery rows
POST   /api/v1/webhooks/{id}/deliveries/{deliveryId}/replay  (header Idempotency-Key)
Subscription status: ACTIVE | PAUSED | AUTO_PAUSED | REVOKED
Delivery status: PENDING | DELIVERED | RETRYING | FAILED  (+ is_replay tag)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: webhooks-api client methods + status-badge taxonomy + subscriptions list page (Surface A) + sidebar nav entry</name>
  <files>frontend/lib/webhooks-api.ts, frontend/lib/api-client.ts, frontend/components/dashboard/webhooks/status-badge.tsx, frontend/app/dashboard/webhooks/page.tsx, frontend/components/dashboard/sidebar.tsx</files>
  <read_first>
    - frontend/app/dashboard/orders/page.tsx §73-112 (statusConfig Record<Status,{label,color,bgColor,icon}> map), §441 (do NOT copy text-4xl H1 — use text-2xl sm:text-3xl per UI-SPEC), §504-519 (filter Select), §569 (font-mono identifiers), §642 (Pagination); the useEffect+useState+apiClient+useToast data pattern
    - frontend/components/dashboard/sidebar.tsx §25-38 (the exported `navigation` array — add ONE entry; mobile-tab-bar imports it, do NOT re-declare)
    - frontend/lib/api-client.ts (default apiClient with Bearer + X-Tenant-Id interceptors)
    - .planning/phases/22-notifications-comms/22-UI-SPEC.md §"Color" (badge taxonomy table), §"Surfaces & Layout / Surface A", §"Copywriting Contract", §"Mobile-First / 375px Contract"
  </read_first>
  <action>
    Add typed webhook methods to `frontend/lib/webhooks-api.ts` wrapping the default `apiClient` (list/create/get/rotateSecret/pause/resume/revoke/listDeliveries/replay per <interfaces>). Create `status-badge.tsx` as a `statusConfig` map (orders/page.tsx §73-112 shape) covering BOTH the subscription states (Active=emerald/CheckCircle2, Paused=slate/Pause, Auto-paused=amber/AlertTriangle, Revoked=red/Ban) and delivery states (Pending=slate/Clock, Delivered=emerald/CheckCircle2, Retrying=amber/RefreshCcw, Failed=red/XCircle) + a Replay outline tag (RotateCcw) — every badge = tinted bg + lucide icon + text label (UI-SPEC Color table). Build `/dashboard/webhooks/page.tsx`: header `flex-col sm:flex-row` with `text-2xl sm:text-3xl` H1 "Webhooks" + subtitle + orange **Add endpoint** CTA (full-width on mobile); Card wrapping a shadcn `Table` at `sm+` (Endpoint URL `font-mono text-xs truncate`, Events, Status badge, Last delivery, Actions) and **card-stacking below sm** (one `rounded-lg border` card each, `break-all` URL, `min-h-11` action buttons) — the hard 375px contract; empty state per Copywriting ("No webhook endpoints yet" + CTA); data via `useEffect`+`useState`+`apiClient`, errors → destructive `useToast` (never blank). Add `{ name: "Webhooks", href: "/dashboard/webhooks", icon: Webhook }` (lucide `Webhook`) to the `navigation` array. Reuse ONLY existing vendored primitives + lucide icons — add no new npm dependency (bundle-growth guardrail).
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/components/dashboard/sidebar.tsx` `navigation` array contains a `/dashboard/webhooks` entry using the lucide `Webhook` icon
    - `npm run build` (tsc typecheck) exits 0 — no type errors on the new page/api-client
    - `status-badge.tsx` maps all four subscription states and all four delivery states, each with a lucide icon AND a text label (grep shows an `icon:` and `label:` per entry)
    - the list page H1 uses `text-2xl sm:text-3xl` (NOT `text-4xl`) and the header container uses `flex-col sm:flex-row`
    - no new dependency added to `frontend/package.json` for this UI (git diff shows package.json dependencies unchanged) — bundle-growth guardrail
  </acceptance_criteria>
  <done>Subscriptions list renders (Table at sm+, cards below sm), nav entry added, api-client + badge taxonomy in place; build type-clean; no new deps.</done>
</task>

<task type="auto">
  <name>Task 2: WebhookCreateDialog (HTTPS Zod + event-type groups) + SecretRevealDialog (once, focus-trapped) + ConfirmActionDialog (rotate/revoke)</name>
  <files>frontend/components/dashboard/webhooks/WebhookCreateDialog.tsx, frontend/components/dashboard/webhooks/SecretRevealDialog.tsx, frontend/components/dashboard/webhooks/ConfirmActionDialog.tsx</files>
  <read_first>
    - frontend/components/dashboard/orders/RefundDialog.tsx §104-136 (useForm+zodResolver), §140-145 (reset-on-open), §169-179 (RFC 7807 error extraction e?.response?.data?.detail), §186-195 (Dialog max-w-md), §71-88 + §160-165 (makeIdempotencyKey — reuse EXACTLY for replay in Task 3)
    - .planning/phases/22-notifications-comms/22-UI-SPEC.md §"Interaction Contracts" (secret shown once, no-backdrop-dismiss, event-type grouped checkboxes Orders/Onboarding/Payments/Refunds, ≥1 required), §"Copywriting Contract" (all dialog copy verbatim), §"Accessibility Contract" (role="alert" warning, aria-label, min-h-11)
    - .planning/phases/22-notifications-comms/22-PATTERNS.md §"WebhookCreateDialog / SecretRevealDialog" (lines 331-338)
  </read_first>
  <action>
    Create `WebhookCreateDialog` (Dialog `max-w-md max-h-[90vh] overflow-y-auto`, react-hook-form + zodResolver): URL `Input` validated `z.string().url().startsWith("https://")` (field error copy "Enter an HTTPS URL…"), event-type selection as grouped checkboxes by family (Orders/Onboarding/Payments/Refunds, ≥1 required, each row `min-h-11`), Cancel (outline) + **Add endpoint** (`bg-orange-500 hover:bg-orange-600`); on success surface the returned plaintext `signingSecret` by opening `SecretRevealDialog`. Create `SecretRevealDialog`: readOnly `font-mono` `Input` with the secret, a `role="alert"` warning (Copywriting "This is the only time…"), a **Copy** button (writes clipboard + fires an aria-live "Copied" toast), NOT dismissible by backdrop (only the **I've saved it** button), `aria-label="Signing secret"`. Create a reusable `ConfirmActionDialog` for Rotate (confirm → rotate-secret → re-open SecretRevealDialog with the new secret) and Revoke (destructive `Button variant="destructive"`, copy per Copywriting). Extract RFC 7807 `detail` for errors (RefundDialog §169-179).
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `WebhookCreateDialog` Zod schema rejects a non-`https://` URL and requires ≥1 event type (assertable in Task 3 jest)
    - `SecretRevealDialog` sets the secret `Input` `readOnly` with `aria-label="Signing secret"`, includes a `role="alert"` element, and does NOT close on backdrop click (only the explicit confirm button)
    - `npm run build` exits 0
    - all dialog copy strings match the UI-SPEC Copywriting Contract table verbatim (e.g. "Copy your signing secret", "I've saved it", "Rotate signing secret?")
  </acceptance_criteria>
  <done>Create/secret-reveal/rotate/revoke dialogs match the UI-SPEC contract (HTTPS Zod, once-only focus-trapped secret, destructive revoke); build type-clean.</done>
</task>

<task type="auto">
  <name>Task 3: Endpoint detail + delivery-log browser (Surface B) — filter, auto-pause alert, Replay — + Jest render/375px tests + web-perf guardrail</name>
  <files>frontend/app/dashboard/webhooks/[id]/page.tsx, frontend/app/dashboard/webhooks/__tests__/webhooks-page.test.tsx, frontend/app/dashboard/webhooks/__tests__/delivery-log.test.tsx</files>
  <read_first>
    - frontend/app/dashboard/orders/page.tsx (back-link ArrowLeft, §504-519 filter Select, Table→cards, §642 Pagination) — the detail-page analog
    - frontend/components/dashboard/orders/RefundDialog.tsx §71-88 (makeIdempotencyKey — reuse for the Replay POST header)
    - .planning/phases/22-notifications-comms/22-UI-SPEC.md §"Surface B" (summary card + auto-pause amber alert role="status" + delivery-log filters + Table sm+/cards <sm + Replay), §"States" (loading/empty/filtered-empty/error), §"Mobile-First / 375px Contract"
    - frontend/CLAUDE.md web-perf contract note (mobile-first Core Web Vitals) + existing frontend jest specs (e.g. any *page.test.tsx) for the render-harness + a 375px viewport assertion pattern
  </read_first>
  <action>
    Build `/dashboard/webhooks/[id]/page.tsx`: back link to `/dashboard/webhooks`; a summary Card (URL, status badge, event-type chips, created date, Rotate/Pause·Resume/Revoke actions); if status is AUTO_PAUSED render an inline amber alert (`bg-amber-50 border border-amber-200 text-amber-800 rounded-md p-4`, `role="status"`) with the auto-pause explanation + last-error line + **Resume delivery**; a delivery-log Card with two `Select` filters (Event type + Status, stacked `flex-col` on mobile / `sm:w-[180px]` inline) driving `GET /webhooks/{id}/deliveries?eventType=&status=&page=`, a `Table` at `sm+` (Event type, Status badge + `font-mono` HTTP code, Attempts, Replay tag, When, **Replay** action) and card-stacking below sm, `<Pagination>` when paged; empty + filtered-empty states per Copywriting. Replay: a confirm dialog → `POST .../replay` with a header `Idempotency-Key` from the reused `makeIdempotencyKey()` → success toast "Replay queued…". Jest tests: (a) list renders subscriptions + a delivery row; (b) filtering by status narrows the rows; (c) the page renders without horizontal overflow at a 375px viewport (assert no element scrollWidth exceeds 375, or the container has no `overflow-x`); (d) replay fires a POST carrying an `Idempotency-Key` header. Keep the page lazy/paginated (no unbounded row rendering) so the delivery log does not balloon the DOM at throttled mobile.
  </action>
  <verify>
    <automated>cd frontend && npm test -- webhooks 2>&1 | tail -12 && npm run build 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Jest: the delivery-log page renders a delivery row and filtering by `status=FAILED` reduces the visible rows
    - Jest: a 375px-viewport render asserts no horizontal overflow (container/table stacks to cards; no element wider than the viewport)
    - Jest: the Replay action issues a POST whose config headers include `Idempotency-Key` (spy on apiClient)
    - Web-perf (W5, CLAUDE.md): `npm run build` output shows the two new `/dashboard/webhooks` route chunks do not introduce unbounded/duplicate bundle growth (no new heavy dep, delivery log is paginated not fully rendered); the throttled-mobile LCP/CLS/INP smoke on these routes runs in 22-07's Playwright E2E and must not regress
    - `npm test -- webhooks` and `npm run build` both exit 0
  </acceptance_criteria>
  <done>Endpoint detail + delivery-log with filter/replay/auto-pause alert render mobile-first at 375px with automated Jest coverage; new routes carry a bundle-growth guardrail (CWV measured in 22-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → webhook API | authenticated dashboard calls (apiClient Bearer + X-Tenant-Id) |
| rendered data → DOM | delivery URLs / HTTP codes / secrets rendered into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06-01 | Information Disclosure | Signing secret persisted/re-fetchable in the UI | mitigate | secret shown once in SecretRevealDialog from the create/rotate response; never stored in state beyond the dialog; GET DTO carries no secret (22-03) |
| T-22-06-02 | Tampering | XSS via rendered delivery URL/error text | mitigate | React auto-escaping; render identifiers as text in `font-mono` (no dangerouslySetInnerHTML) |
| T-22-06-03 | Tampering | Duplicate delivery on replay | mitigate | Replay carries a fresh `Idempotency-Key` (reused secure `makeIdempotencyKey()`); backend replay is a tagged new attempt |
| T-22-06-04 | Denial of Service (UX) | Horizontal overflow / broken mobile UI / bundle bloat at 375px | mitigate | card-stacking below sm + Jest 375px no-overflow assertion; paginated delivery log + no new heavy dep (bundle-growth guardrail); CWV smoke in 22-07 |
| T-22-06-SC | Tampering | npm installs / new UI primitive | accept | UI-SPEC Registry Safety: reuse vendored shadcn primitives; if a new primitive is needed it MUST be shadcn-official (first-party) — no third-party registry |
</threat_model>

<verification>
- `cd frontend && npm test -- webhooks` green (render, filter, 375px no-overflow, replay Idempotency-Key)
- `cd frontend && npm run build` type-clean (frontend typecheck gate — jest does NOT type-check) + no new dependency in package.json
</verification>

<success_criteria>
- Create → list → pause/resume/revoke + once-only secret reveal + rotate all work from the dashboard
- Delivery log filters by event type + status; auto-pause amber alert surfaces; replay adds a tagged Idempotency-Key-safe attempt
- No horizontal overflow at 375px (cards below sm, Table at sm+); every status badge = icon + label
- New user-facing routes own their web-perf: no unbounded/duplicate bundle growth (this plan) + throttled-mobile CWV smoke non-regression (22-07)
</success_criteria>

<output>
Create `.planning/phases/22-notifications-comms/22-06-SUMMARY.md` when done.
</output>
