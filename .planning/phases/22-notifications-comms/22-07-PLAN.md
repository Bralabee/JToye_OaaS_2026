---
phase: 22-notifications-comms
plan: 07
type: execute
wave: 5
depends_on: [22-02, 22-06]
files_modified:
  - frontend/app/unsubscribe/page.tsx
  - frontend/lib/public-api-client.ts
  - frontend/app/sitemap.ts
  - docs/SITEMAP.md
  - frontend/e2e/webhooks-flow.spec.ts
  - frontend/e2e/unsubscribe-flow.spec.ts
  - frontend/app/unsubscribe/__tests__/unsubscribe-page.test.tsx
  - docs/metrics.json
autonomous: true
requirements: [COMMS-03, COMMS-06]
must_haves:
  truths:
    - "A recipient visiting the one-click unsubscribe link sees a mobile-first confirmation (unsubscribed / already-unsubscribed / invalid) with no dashboard chrome and no sign-in prompt"
    - "The /unsubscribe page is noindex,nofollow, excluded from sitemap.xml, and never renders the email/token query params into meta or visible body"
    - "A Playwright journey proves: create a webhook endpoint → see it listed → filter the delivery log → replay a delivery → no horizontal overflow at 375px"
    - "A Playwright journey proves the unsubscribe link resolves and confirms opt-out end-to-end"
    - "docs/metrics.json is reconciled to source reality (schema 56, new test counts) and scripts/docs-freshness.sh exits 0"
  artifacts:
    - path: "frontend/app/unsubscribe/page.tsx"
      provides: "public no-auth one-click unsubscribe confirmation (Surface C)"
      contains: "robots"
    - path: "frontend/e2e/webhooks-flow.spec.ts"
      provides: "COMMS-06 E2E (create/list/filter/replay/375px)"
      contains: "375"
    - path: "docs/metrics.json"
      provides: "reconciled test/schema manifest (docs-freshness gate green)"
      contains: "schema_version"
  key_links:
    - from: "unsubscribe/page.tsx"
      to: "/api/v1/public/unsubscribe"
      via: "publicApiClient POST token"
      pattern: "publicApiClient"
    - from: "sitemap.ts"
      to: "/unsubscribe"
      via: "excluded (allowlist omits it)"
      pattern: "unsubscribe"
---

<objective>
Finish the public unsubscribe surface (COMMS-03 UI half) and prove the whole phase end-to-end (COMMS-06 E2E), then reconcile the docs-freshness gate. Build `frontend/app/unsubscribe/page.tsx` (token-based, no-auth, mobile-first, `noindex`, sitemap-excluded, PII-safe) mirroring `track/page.tsx`; write Playwright journeys for the webhook dashboard and the unsubscribe flow; and reconcile `docs/metrics.json` (schema 51→56 + new tests) so `docs-freshness` CI stays green.

Purpose: the public-surface SEO/no-auth safety contract + the click-through E2E proof the user requires (memory `feedback_e2e_click_through`), and the mandatory docs-freshness reconcile (CLAUDE.md test-count invariant).
Output: public unsubscribe page + sitemap exclusion + docs/SITEMAP.md entry + 2 Playwright specs + a Jest render test + reconciled docs/metrics.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-notifications-comms/22-SPEC.md
@.planning/phases/22-notifications-comms/22-UI-SPEC.md
@.planning/phases/22-notifications-comms/22-PATTERNS.md

# Consumes 22-02 (public unsubscribe endpoint) + 22-06 (webhook dashboard for E2E).
@.planning/phases/22-notifications-comms/22-02-SUMMARY.md
@.planning/phases/22-notifications-comms/22-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Public unsubscribe page (Surface C) + sitemap exclusion + docs/SITEMAP.md + Jest render test</name>
  <files>frontend/app/unsubscribe/page.tsx, frontend/lib/public-api-client.ts, frontend/app/sitemap.ts, docs/SITEMAP.md, frontend/app/unsubscribe/__tests__/unsubscribe-page.test.tsx</files>
  <read_first>
    - frontend/app/track/page.tsx §10, 39-57, 111-115, 142, 180, 202 (Suspense-wrapped client component reading useSearchParams; publicApiClient POST; single-column max-w-lg card; orange-500 spinner/palette — COPY this template)
    - frontend/lib/public-api-client.ts (the no-auth axios client)
    - frontend/app/sitemap.ts (explicit allowlist — /unsubscribe must NOT be added; confirm it stays excluded) + docs/SITEMAP.md (keep the human inventory in sync — add /unsubscribe as noindex/excluded)
    - .planning/phases/22-notifications-comms/22-UI-SPEC.md §"Surface C" + §"SEO / Discoverability" (robots noindex,nofollow; exclude from sitemap; no OG/JSON-LD; never render email/token into meta/body) + §"Copywriting Contract" (the four unsubscribe states) + §"Accessibility Contract" (one h1, real text)
  </read_first>
  <action>
    Create `frontend/app/unsubscribe/page.tsx` mirroring `track/page.tsx`: outer default export exporting `const metadata = { title: "Unsubscribe — J'Toye", description: "Manage your email preferences.", robots: { index: false, follow: false } }` and wrapping a `<Suspense fallback={orange spinner}>` around a client component that reads `?tenant=&email=&category=&token=` via `useSearchParams`, POSTs to `/api/v1/public/unsubscribe` via `publicApiClient`, and renders one of four states (loading / success / already-unsubscribed / invalid-token) per the Copywriting Contract — each a single `<h1>` + body paragraph + a state icon (BellOff / CheckCircle2 / AlertTriangle). Layout: `mx-auto max-w-lg px-4 py-8 sm:py-12` + orange "J" brand tile + one `rounded-xl bg-white border border-slate-100 p-6 shadow-sm` card; orange palette (NOT blue). NEVER render `email`/`token` into meta or the visible body. Confirm `sitemap.ts` does NOT list `/unsubscribe` (allowlist); add a `/unsubscribe` row to `docs/SITEMAP.md` marked noindex/excluded (keep the two in sync). Jest: render each of the four states from mocked publicApiClient responses; assert the page emits `robots.index === false` and never prints the raw token string.
  </action>
  <verify>
    <automated>cd frontend && npm test -- unsubscribe 2>&1 | tail -10 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/app/unsubscribe/page.tsx` exports `metadata.robots` with `index: false, follow: false`
    - `frontend/app/sitemap.ts` does NOT contain `/unsubscribe` (grep absent); `docs/SITEMAP.md` records `/unsubscribe` as noindex/excluded
    - Jest renders the success, already-unsubscribed, and invalid states; asserts the raw `token` query value is never present in the rendered DOM
    - `npm test -- unsubscribe` and `npm run build` exit 0
  </acceptance_criteria>
  <done>The public unsubscribe page renders four mobile-first states, is noindex + sitemap-excluded, and leaks no PII; Jest covers it.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright E2E — webhook dashboard journey (create/list/filter/replay/375px) + unsubscribe flow</name>
  <files>frontend/e2e/webhooks-flow.spec.ts, frontend/e2e/unsubscribe-flow.spec.ts</files>
  <read_first>
    - frontend/e2e/vendor-refund-flow.spec.ts + frontend/e2e/dashboard-mobile.spec.ts (in-repo Playwright patterns: auth/login helper, 375px viewport spec, selectors) and frontend/playwright.config.ts (base URL, projects)
    - frontend/e2e/onboarding-blocked-flow.spec.ts (a full click-through journey template)
    - .planning/phases/22-notifications-comms/22-UI-SPEC.md §"Mobile-First / 375px Contract" (no horizontal overflow assertion) and §"Interaction Contracts"
    - memory feedback_e2e_click_through (CLICK the buttons and verify the OUTCOME, not just that they exist)
  </read_first>
  <action>
    Write `webhooks-flow.spec.ts`: log in as a vendor, navigate to `/dashboard/webhooks`, click **Add endpoint**, submit an HTTPS URL + ≥1 event type, assert the SecretRevealDialog shows a secret and the row appears in the list; open the endpoint detail, assert the delivery-log renders, apply a status filter and assert the visible rows narrow, click **Replay** on a delivery and assert a success toast + a new "Replay"-tagged attempt appears; run a 375px-viewport pass asserting no horizontal overflow (document.scrollingElement.scrollWidth <= viewport width, or no `overflow-x` on the main container). Write `unsubscribe-flow.spec.ts`: hit `/unsubscribe?tenant=..&email=..&category=orders&token=<valid>` (mint the token via a test helper or a seeded fixture) and assert the "You're unsubscribed" state; hit it with a tampered token and assert the "This link isn't valid" state. Use the existing login/auth + fixture helpers; CLICK through and verify OUTCOMES.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test webhooks-flow unsubscribe-flow 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `webhooks-flow.spec.ts` creates a subscription, asserts the secret dialog + the new list row, filters the delivery log, replays a delivery (asserting a new Replay-tagged attempt), and passes a 375px no-horizontal-overflow assertion
    - `unsubscribe-flow.spec.ts` asserts the success state for a valid token and the invalid state for a tampered token
    - `npx playwright test webhooks-flow unsubscribe-flow` exits 0 (against the rebuilt full stack)
  </acceptance_criteria>
  <done>End-to-end journeys click through the webhook dashboard (create→list→filter→replay→375px) and the unsubscribe flow, verifying outcomes.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile docs/metrics.json (schema 56 + new tests) + docs-freshness green + full-suite sweep</name>
  <files>docs/metrics.json</files>
  <read_first>
    - scripts/docs-freshness.sh (the `--write` regenerator; recomputes java_test_methods/jest_blocks/playwright_blocks/schema_version from source and compares to docs/metrics.json — the CI gate `.github/workflows/docs-freshness.yml` fails on drift)
    - docs/metrics.json (current baseline: total_logical_invocations 1300, schema_version 51 — this phase adds V54/V55/V56 → schema 56, plus all new Java @Test / Jest / Playwright blocks)
    - CLAUDE.md §Constraints (the test-count invariant is the single source of truth; every new code file needs tests)
  </read_first>
  <action>
    After all prior waves are merged, run the full suites so the counts are real: `cd core-java && ./gradlew test integrationTest` and `cd frontend && npm test && npm run build && npx playwright test`. Then run `scripts/docs-freshness.sh --write` to regenerate `docs/metrics.json` from source (schema_version becomes 56 with V54/V55/V56 present; test counts rise by the new Java/Jest/Playwright blocks). Re-run `scripts/docs-freshness.sh` (no `--write`) and confirm it exits 0 (committed manifest == source reality). Rebuild ALL containers first if any E2E is run (CLAUDE.md Docker rule). Do NOT hand-edit counts — let the script compute them.
  </action>
  <verify>
    <automated>bash scripts/docs-freshness.sh</automated>
  </verify>
  <acceptance_criteria>
    - `docs/metrics.json` `schema_version` == 56 (V54/V55/V56 applied)
    - `bash scripts/docs-freshness.sh` exits 0 (no drift — committed manifest matches recomputed source reality)
    - `total_logical_invocations` in docs/metrics.json increased from the 1300 baseline by the count of new Java @Test + Jest it/test + Playwright test() blocks added this phase
    - the full Java (`./gradlew test integrationTest`) + frontend (`npm test && npm run build`) suites are green before the reconcile commit
  </acceptance_criteria>
  <done>docs/metrics.json reflects schema 56 + the new tests; docs-freshness gate is green; full suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → public page | `/unsubscribe` is no-auth; the token in the URL is the only authorization; crawlers may fetch it |
| CI gate → repo | docs-freshness compares the committed manifest to source; drift fails the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07-01 | Information Disclosure | Email/token leaked via page meta, indexed content, or logs | mitigate | noindex,nofollow + sitemap exclusion; never render email/token into meta/body; Jest asserts the token is absent from the DOM |
| T-22-07-02 | Information Disclosure | Public unsubscribe page indexed by crawlers | mitigate | `robots: { index:false, follow:false }` + excluded from sitemap.xml (Task 1) |
| T-22-07-03 | Tampering | Docs/metrics drift hiding an untested new file (green-by-construction) | mitigate | docs-freshness.sh --write recomputes from source; the CI gate fails on any drift (Task 3) |
| T-22-07-SC | Tampering | package installs | accept | no packages installed (RESEARCH Package Legitimacy Audit — N/A) |
</threat_model>

<verification>
- `cd frontend && npm test -- unsubscribe && npm run build` green
- `cd frontend && npx playwright test webhooks-flow unsubscribe-flow` green (rebuilt stack)
- `bash scripts/docs-freshness.sh` exits 0; `docs/metrics.json` schema_version == 56
</verification>

<success_criteria>
- Public unsubscribe page is mobile-first, noindex, sitemap-excluded, PII-safe, four-state
- Playwright proves the webhook create→list→filter→replay→375px journey and the unsubscribe flow end-to-end
- docs/metrics.json reconciled (schema 56 + new tests); docs-freshness CI gate green; full suite passing
</success_criteria>

<output>
Create `.planning/phases/22-notifications-comms/22-07-SUMMARY.md` when done.
</output>
