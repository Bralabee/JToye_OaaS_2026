---
phase: 22-notifications-comms
plan: 04
type: execute
wave: 2
depends_on: [22-01, 22-02]
files_modified:
  - core-java/src/main/java/uk/jtoye/core/config/RabbitMQConfig.java
  - core-java/src/main/java/uk/jtoye/core/notification/dispatch/NotificationDispatchService.java
  - core-java/src/main/java/uk/jtoye/core/notification/dispatch/RecipientResolver.java
  - core-java/src/main/java/uk/jtoye/core/notification/listener/OnboardingNotificationListener.java
  - core-java/src/main/java/uk/jtoye/core/notification/listener/FinancialNotificationListener.java
  - core-java/src/test/java/uk/jtoye/core/notification/dispatch/NotificationDispatchServiceTest.java
  - core-java/src/test/java/uk/jtoye/core/notification/listener/OnboardingNotificationListenerIntegrationTest.java
  - core-java/src/test/java/uk/jtoye/core/notification/listener/FinancialNotificationListenerIntegrationTest.java
  - core-java/src/test/java/uk/jtoye/core/testsupport/MailhogAssertions.java
autonomous: true
requirements: [COMMS-01, COMMS-02]
must_haves:
  truths:
    - "The previously-unbound onboarding.events exchange is bound to a durable queue whose consumer sends a vendor email — an onboarding stall now lands in Mailhog (was discarded)"
    - "The previously-discarded order.refunded key is bound to a consumer that emails customer + vendor"
    - "A second payment.notifications queue consumes payment events WITHOUT stealing from the incumbent audit listener"
    - "Each event type resolves the correct recipient set (D-04: customer = order email, vendor = tenants.contact_email), passes the consent gate, renders, and sends via EmailChannel"
    - "The existing OrderStateChangeListener → EmailNotificationService order path is unchanged and its test stays green; no event type poison-dead-letters"
  artifacts:
    - path: "core-java/src/main/java/uk/jtoye/core/config/RabbitMQConfig.java"
      provides: "3 new email consumer queues+bindings (onboarding/payment/refund) + 1 webhook fanout queue+bindings (for 22-05)"
      contains: "onboarding.notifications"
    - path: "core-java/src/main/java/uk/jtoye/core/notification/dispatch/NotificationDispatchService.java"
      provides: "recipient resolve + consent gate + render + fan to channels"
      exports: ["dispatch"]
    - path: "core-java/src/main/java/uk/jtoye/core/notification/listener/OnboardingNotificationListener.java"
      provides: "@RabbitListener on onboarding.notifications → vendor email"
      contains: "@RabbitListener"
  key_links:
    - from: "OnboardingNotificationListener"
      to: "NotificationDispatchService"
      via: "TenantContext+GUC preamble then dispatch"
      pattern: "TenantContext.set"
    - from: "NotificationDispatchService"
      to: "ConsentGate.allows"
      via: "gate before EmailChannel.deliver"
      pattern: "allows\\("
    - from: "RecipientResolver"
      to: "tenants.contact_email"
      via: "vendor recipient (D-04)"
      pattern: "contactEmail"
---

<objective>
Bind the dead channels and fan every lifecycle event to email for both audiences (COMMS-01 + COMMS-02). Add the RabbitMQ topology (all new consumer queues + bindings, including the webhook fanout queue that 22-05 consumes), a `NotificationDispatchService` (recipient resolution D-04 → consent gate → render → EmailChannel/WhatsAppSmsChannel fan-out), and two new `@RabbitListener` consumers (onboarding + financial/refund) mirroring the working `OrderStateChangeListener`. The existing order-email path is left untouched (Pitfall 5 path A); no producer/flusher change (Pitfall 3 — all four dispatch branches already exist).

Purpose: this is the orchestration that makes the onboarding stall email (Phase 21's dangling seam) and payment/refund emails real, and it owns the single shared broker-topology file so the tracks don't conflict.
Output: RabbitMQConfig topology + dispatch service + recipient resolver + 2 listeners + Mailhog test helper + dispatch/landing integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-notifications-comms/22-SPEC.md
@.planning/phases/22-notifications-comms/22-CONTEXT.md
@.planning/phases/22-notifications-comms/22-PATTERNS.md
@.planning/phases/22-notifications-comms/22-RESEARCH.md

# Consumes from 22-01: NotificationChannel/EmailChannel/EmailTemplateRenderer/WhatsAppSmsChannel/NotificationMessage/NotificationProperties.
# Consumes from 22-02: ConsentGate + NotificationCategory (consent package) + UnsubscribeTokenService.
@.planning/phases/22-notifications-comms/22-01-SUMMARY.md
@.planning/phases/22-notifications-comms/22-02-SUMMARY.md

<interfaces>
<!-- Assembled from 22-01 + 22-02 (both merged before this Wave-2 plan runs). -->
From 22-01 (notification/dispatch): NotificationChannel.deliver(NotificationMessage) ; NotificationMessage(tenantId, recipient, eventType, RenderedEmail email, plainSummary, unsubscribeUrl) ; EmailChannel, WhatsAppSmsChannel ; EmailTemplateRenderer.render(...) -> RenderedEmail ; NotificationProperties.unsubscribe.baseUrl
From 22-02 (notification/consent): NotificationCategory { ORDERS, ONBOARDING, FINANCIAL, MARKETING } ; ConsentGate.allows(tenantId, recipient, category) ; UnsubscribeTokenService.tokenFor(tenantId, email, category)
Recipient resolution (D-04): customer = order email ; vendor = tenants.contact_email (fallback onboarding contact) ; onboarding = vendor-only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RabbitMQConfig — 3 email consumer queues+bindings + 1 webhook fanout queue+bindings + Mailhog test assertion helper</name>
  <files>core-java/src/main/java/uk/jtoye/core/config/RabbitMQConfig.java, core-java/src/test/java/uk/jtoye/core/testsupport/MailhogAssertions.java</files>
  <read_first>
    - core-java/src/main/java/uk/jtoye/core/config/RabbitMQConfig.java (WHOLE — §57-73 order queue+binding, §122-139 payment queue+binding, §156-159 the LONE unbound onboardingEventsExchange, DLX beans; ORDER_EVENTS_ROUTING_PATTERN="order.state.*" §22)
    - .planning/phases/22-notifications-comms/22-PATTERNS.md §"config/RabbitMQConfig.java" (lines 85-105 — the three NEW bindings; do NOT add a 2nd listener to an existing queue; do NOT widen order.state.* to order.*)
    - .planning/phases/22-notifications-comms/22-RESEARCH.md Pattern 1 + Pitfall 1 + Pitfall 2 (onboarding unbound; order.refunded matches nothing)
    - docker-compose.full-stack.yml §461 (Mailhog :8025 HTTP API for message assertions)
  </read_first>
  <action>
    Extend `RabbitMQConfig` with durable queue+binding bean triplets (copy the payment topology §122-139 shape, each queue given the existing DLX arg): (1) `onboarding.notifications` bound to the already-declared `onboardingEventsExchange()` with `onboarding.state.*` (this BINDS the Phase-21 dead channel); (2) `payment.notifications` — a SECOND durable queue on `paymentEventsExchange` with `payment.*` (does NOT compete with `PaymentEventAuditListener`); (3) `refund.notifications` on `orderEventsExchange` with routing key exactly `order.refunded` (currently matches nothing — do NOT widen `order.state.*`); (4) `webhook.deliveries` — ONE durable fanout-consumer queue on the topic exchanges bound to `onboarding.state.*`, `order.state.*`, `order.refunded`, and `payment.*` (four bindings; consumed by 22-05's WebhookFanoutListener). Declare queue-name constants for all four. Do NOT touch `PaymentEventOutboxFlusher.publishRow` (Pitfall 3 — all dispatch branches exist). Create `MailhogAssertions` test helper querying `http://<mailhog-host>:8025/api/v2/messages` (poll-with-timeout, filter by recipient/subject) for integration tests.
  </action>
  <verify>
    <automated>cd core-java && ./gradlew compileJava compileTestJava -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'onboarding.notifications\|payment.notifications\|refund.notifications\|webhook.deliveries' core-java/src/main/java/uk/jtoye/core/config/RabbitMQConfig.java` ≥ 4 (four new queues)
    - the refund binding uses routing key `order.refunded` (not `order.*`) — `grep 'order.refunded' RabbitMQConfig.java` present; `grep 'with("order.\*")' RabbitMQConfig.java` absent
    - `PaymentEventOutboxFlusher.java` is unmodified (git diff empty)
    - `MailhogAssertions` compiles and exposes a method that polls `/api/v2/messages` and asserts a message to a given recipient with a given subject substring
  </acceptance_criteria>
  <done>Onboarding exchange bound; refund + payment second queues added; webhook fanout queue declared; flusher untouched; Mailhog helper ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RecipientResolver (D-04) + NotificationDispatchService (gate → render → fan to channels) + unit test</name>
  <files>core-java/src/main/java/uk/jtoye/core/notification/dispatch/RecipientResolver.java, core-java/src/main/java/uk/jtoye/core/notification/dispatch/NotificationDispatchService.java, core-java/src/test/java/uk/jtoye/core/notification/dispatch/NotificationDispatchServiceTest.java</files>
  <read_first>
    - core-java/src/main/java/uk/jtoye/core/tenant/Tenant.java §57-58 (contactEmail VARCHAR(320) — the vendor recipient, D-04)
    - core-java/src/main/java/uk/jtoye/core/order/OrderStateChangeListener.java (how the order path resolves the customer email + calls the email service — mirror the recipient-resolution intent, do NOT modify this file)
    - 22-01-SUMMARY.md — NotificationChannel / EmailChannel / EmailTemplateRenderer / NotificationMessage / NotificationProperties signatures
    - 22-02-SUMMARY.md — ConsentGate.allows + NotificationCategory (in notification/consent) + UnsubscribeTokenService.tokenFor signatures
    - .planning/phases/22-notifications-comms/22-CONTEXT.md D-04 (customer=order email, vendor=tenants.contact_email w/ onboarding-contact fallback; onboarding = vendor-only) and D-01
  </read_first>
  <behavior>
    - RecipientResolver.forEvent(eventType, payload) returns the correct recipient set: order.* / payment.* / refund.* → {customer(order email), vendor(tenant.contactEmail)}; onboarding.state.* → {vendor only}. When contactEmail is null, fall back to the onboarding contact email; when neither exists, that recipient is omitted (no send).
    - NotificationDispatchService.dispatch(eventType, tenantId, payload): for each resolved (recipient, category), IF ConsentGate.allows(tenant,recipient,category) THEN render via EmailTemplateRenderer + build the unsubscribe URL (UnsubscribeTokenService + NotificationProperties.baseUrl) + EmailChannel.deliver(NotificationMessage); ELSE skip (no send). Always also offers to WhatsAppSmsChannel (no-op when off). A suppressed recipient receives NO email.
    - onboarding dispatch produces exactly one vendor recipient (never a customer).
  </behavior>
  <action>
    Create `RecipientResolver` implementing D-04 (map each event family to its recipient set using `Tenant.contactEmail` for the vendor, order email for the customer, onboarding-contact fallback). Create `NotificationDispatchService.dispatch(eventType, tenantId, payload)`: resolve recipients → for each, map the event family to a `NotificationCategory` (order→ORDERS, onboarding→ONBOARDING, payment/refund→FINANCIAL) → check `ConsentGate.allows(tenant,recipient,category)` (skip if false) → `EmailTemplateRenderer.render` → build the per-recipient one-click unsubscribe URL via `UnsubscribeTokenService.tokenFor(tenant,recipient,category)` + `NotificationProperties.unsubscribe.baseUrl` → construct a `NotificationMessage` and call `EmailChannel.deliver`; also call `WhatsAppSmsChannel.deliver` (no-op OFF). Inject channels as `List<NotificationChannel>` or explicitly. Note `NotificationMessage` carries no category field — the category is used here for the gate + token only. Unit-test recipient sets per event type + that a suppressed recipient (gate false) is not delivered to + onboarding is vendor-only.
  </action>
  <verify>
    <automated>cd core-java && ./gradlew test --tests 'uk.jtoye.core.notification.dispatch.NotificationDispatchServiceTest' -q</automated>
  </verify>
  <acceptance_criteria>
    - `NotificationDispatchServiceTest` asserts a refund event dispatches to BOTH the customer email and the vendor `contactEmail`, and an onboarding event dispatches to the vendor ONLY (no customer)
    - a test asserts that when `ConsentGate.allows` is stubbed false for a recipient, `EmailChannel.deliver` is NOT invoked for that recipient (verify Mockito never())
    - a test asserts the built NotificationMessage carries a non-null `unsubscribeUrl` for transactional emails
    - `./gradlew test --tests '*NotificationDispatchServiceTest'` exits 0
  </acceptance_criteria>
  <done>Dispatch resolves the right audiences per D-04, gates on consent, renders, and fans to channels; onboarding is vendor-only.</done>
</task>

<task type="auto">
  <name>Task 3: OnboardingNotificationListener + FinancialNotificationListener (+refund) with TenantContext/GUC preamble + Mailhog landing integration tests</name>
  <files>core-java/src/main/java/uk/jtoye/core/notification/listener/OnboardingNotificationListener.java, core-java/src/main/java/uk/jtoye/core/notification/listener/FinancialNotificationListener.java, core-java/src/test/java/uk/jtoye/core/notification/listener/OnboardingNotificationListenerIntegrationTest.java, core-java/src/test/java/uk/jtoye/core/notification/listener/FinancialNotificationListenerIntegrationTest.java</files>
  <read_first>
    - core-java/src/main/java/uk/jtoye/core/order/OrderStateChangeListener.java §74-90 (@RabbitListener + @Transactional + the TenantContext.set + set_config('app.current_tenant_id',?,true) GUC preamble + try/finally clear — copy this preamble exactly)
    - core-java/src/main/java/uk/jtoye/core/payment/PaymentEventAuditListener.java §19-20 (minimal typed 2nd-consumer shape; PaymentEvent payload)
    - core-java/src/main/java/uk/jtoye/core/onboarding/OnboardingStateChangeEvent.java (onboarding event payload) and the RefundEvent payload (order.refunded)
    - core-java/src/test/java/.../testsupport/MailhogAssertions.java (Task 1 helper) and IdempotencyKeysRlsPolicyIntegrationTest §50-70 (Testcontainers @DynamicPropertySource bootstrap)
  </read_first>
  <action>
    Create `OnboardingNotificationListener` (`@RabbitListener(queues = onboarding.notifications constant)`, `@Transactional`) consuming `OnboardingStateChangeEvent`, and `FinancialNotificationListener` with two `@RabbitListener` methods — one on `payment.notifications` (`PaymentEvent`) and one on `refund.notifications` (`RefundEvent`). Each method: run the `OrderStateChangeListener` §83-90 preamble (`TenantContext.set(event.tenantId())` + `set_config` GUC on the Hibernate session, try/finally `TenantContext.clear()`), then delegate to `NotificationDispatchService.dispatch(...)`. Integration tests (Testcontainers + Mailhog, listeners enabled): publish an onboarding-stall event → assert a vendor email lands in Mailhog (was discarded before); publish a RefundEvent → assert BOTH a customer and a vendor email land; publish a PaymentEvent → assert its recipient email lands; and assert `EmailNotificationServiceTest` remains green (order path regression guard) + no `payment_event_outbox` row flips `poison=true`.
  </action>
  <verify>
    <automated>cd core-java && ./gradlew integrationTest --tests 'uk.jtoye.core.notification.listener.*' -q && ./gradlew test --tests 'EmailNotificationServiceTest' -q</automated>
  </verify>
  <acceptance_criteria>
    - `OnboardingNotificationListenerIntegrationTest`: after publishing an onboarding stall event, Mailhog `/api/v2/messages` contains a message to the tenant `contact_email` (the dead channel is now bound)
    - `FinancialNotificationListenerIntegrationTest`: a refund event produces exactly two Mailhog messages (customer + vendor); a payment event produces its expected recipient message
    - `EmailNotificationServiceTest` still passes unchanged (git diff empty on that test + `EmailNotificationService.java`)
    - no `payment_event_outbox` row has `poison=true` after the run (flusher dispatch covers all types — Pitfall 3)
  </acceptance_criteria>
  <done>Onboarding/payment/refund events reach email consumers that land mail in Mailhog for the right audiences; order path un-regressed; no poison dead-letter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RabbitMQ → consumer thread | events arrive off-request with NO tenant context; the GUC must be pinned before any tenant-scoped read/write |
| dispatch → SMTP | outbound email carries tenant PII (recipient, order details) to the resolved recipient only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04-01 | Information Disclosure | Cross-tenant recipient resolution (emailing the wrong tenant's contact) | mitigate | TenantContext+GUC pinned from `event.tenantId()` before any read (OrderStateChangeListener §83-90 preamble); RecipientResolver reads only the pinned tenant |
| T-22-04-02 | Tampering/DoS | 2nd @RabbitListener stealing from the audit listener (competing consumers) | mitigate | each consumer gets its OWN durable queue (Pattern 1); payment.notifications is a separate queue |
| T-22-04-03 | Denial of Service | New event type poison-dead-letters | mitigate | no new producer/outbox type added (Pitfall 3); consumers only; Task 3 asserts no poison rows |
| T-22-04-04 | Information Disclosure | Emailing a recipient who opted out (GDPR) | mitigate | ConsentGate.allows checked before every EmailChannel.deliver (Task 2) |
| T-22-04-05 | Repudiation | Order-email regression (Pitfall 5) | mitigate | EmailNotificationService + its test untouched; new events on EmailChannel only; Task 3 asserts the order test stays green |
| T-22-04-SC | Tampering | package installs | accept | no packages installed (RESEARCH Package Legitimacy Audit — N/A) |
</threat_model>

<verification>
- `cd core-java && ./gradlew integrationTest --tests 'uk.jtoye.core.notification.listener.*'` green (Mailhog landing proofs)
- `cd core-java && ./gradlew test --tests 'uk.jtoye.core.notification.dispatch.*' --tests 'EmailNotificationServiceTest'` green
- `git diff --name-only` excludes `EmailNotificationService.java` and `PaymentEventOutboxFlusher.java`
</verification>

<success_criteria>
- Onboarding stall event now lands a vendor email in Mailhog (dead channel bound); refund/payment reach the right audiences
- Order-email path untouched + green; no poison dead-letter
- Dispatch resolves recipients (D-04), gates on consent, renders + sends
</success_criteria>

<output>
Create `.planning/phases/22-notifications-comms/22-04-SUMMARY.md` when done.
</output>
