---
phase: 22-notifications-comms
plan: 05
type: execute
wave: 3
depends_on: [22-03, 22-04]
files_modified:
  - core-java/src/main/resources/db/migration/V56__webhook_delivery.sql
  - core-java/src/main/java/uk/jtoye/core/webhook/WebhookDelivery.java
  - core-java/src/main/java/uk/jtoye/core/webhook/WebhookDeliveryRepository.java
  - core-java/src/main/java/uk/jtoye/core/webhook/WebhookProperties.java
  - core-java/src/main/java/uk/jtoye/core/webhook/WebhookSigner.java
  - core-java/src/main/java/uk/jtoye/core/webhook/WebhookEventEnvelope.java
  - core-java/src/main/java/uk/jtoye/core/webhook/WebhookFanoutListener.java
  - core-java/src/main/java/uk/jtoye/core/webhook/WebhookDeliveryWorker.java
  - core-java/src/main/java/uk/jtoye/core/webhook/WebhookRetentionCleanup.java
  - core-java/src/main/java/uk/jtoye/core/webhook/WebhookDeliveryController.java
  - core-java/src/main/resources/application.yml
  - core-java/src/test/java/uk/jtoye/core/webhook/WebhookDeliveryRlsPolicyIntegrationTest.java
  - core-java/src/test/java/uk/jtoye/core/webhook/WebhookSignerTest.java
  - core-java/src/test/java/uk/jtoye/core/webhook/WebhookDeliveryWorkerIntegrationTest.java
  - core-java/src/test/java/uk/jtoye/core/webhook/WebhookRetentionCleanupTest.java
autonomous: true
requirements: [COMMS-05]
must_haves:
  truths:
    - "Each outbox event matching an ACTIVE subscription is POSTed to the vendor URL with an X-JToye-Signature: t=<ts>,v1=<hmac> header a receiver can verify with HMAC-SHA256"
    - "A failing endpoint retries with bounded exponential backoff up to max-attempts then marks the delivery FAILED"
    - "A permanently-failing subscription auto-pauses and NEVER blocks deliveries to a healthy second subscription (no head-of-line block)"
    - "Every attempt writes a webhook_delivery status row (PENDING/DELIVERED/RETRYING/FAILED + HTTP code + attempt count); rows are pruned by a bounded retention window"
    - "A manual replay re-enqueues a past delivery as a NEW attempt tagged replay, leaving the original row's history intact (Idempotency-Key safe)"
  artifacts:
    - path: "core-java/src/main/resources/db/migration/V56__webhook_delivery.sql"
      provides: "webhook_delivery table, RLS helper form, claim indexes"
      contains: "current_tenant_id()"
    - path: "core-java/src/main/java/uk/jtoye/core/webhook/WebhookSigner.java"
      provides: "HMAC-SHA256 t=,v1= signer (signs the exact POSTed bytes)"
      contains: "HmacSHA256"
    - path: "core-java/src/main/java/uk/jtoye/core/webhook/WebhookDeliveryWorker.java"
      provides: "@Scheduled per-tenant SKIP-LOCKED delivery with backoff + auto-pause + per-subscription isolation"
      contains: "TransactionTemplate"
  key_links:
    - from: "WebhookFanoutListener"
      to: "webhook_delivery (PENDING rows)"
      via: "synchronous INSERT per matching ACTIVE subscription"
      pattern: "PENDING"
    - from: "WebhookDeliveryWorker"
      to: "WebhookSigner + WebClient"
      via: "sign exact bytes then POST"
      pattern: "writeValueAsBytes"
    - from: "WebhookRetentionCleanup"
      to: "webhook_delivery"
      via: "@Scheduled prune older than retention-days"
      pattern: "retention"
---

<objective>
Build the signed, retried, observable webhook delivery engine (COMMS-05): the `V56` migration (webhook_delivery, ENABLE+FORCE RLS + claim indexes), a `WebhookSigner` (HMAC-SHA256 Stripe `t=,v1=` over the exact bytes), a versioned `WebhookEventEnvelope`, a `WebhookFanoutListener` (consumes the `webhook.deliveries` queue from 22-04 → INSERT PENDING rows per matching ACTIVE subscription), a `WebhookDeliveryWorker` (`@Scheduled`, per-tenant `TransactionTemplate` + SKIP-LOCKED claim + `WebClient` POST + bounded exponential backoff + auto-pause + per-subscription isolation), a `WebhookRetentionCleanup` (`@Scheduled` prune), a delivery-log/replay `WebhookDeliveryController`, and all `webhook.*` config keys (GLOBAL_RULE_6).

Purpose: the machine channel (absorbed #205). Mirrors the proven `PaymentEventOutboxFlusher` shape on a DEDICATED per-`(subscription,event)` table so one hostile endpoint never stalls others.
Output: 1 migration + delivery entity/repo + signer + envelope + fanout listener + worker + retention + delivery/replay controller + config + signer/worker/retention/RLS tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-notifications-comms/22-SPEC.md
@.planning/phases/22-notifications-comms/22-CONTEXT.md
@.planning/phases/22-notifications-comms/22-PATTERNS.md
@.planning/phases/22-notifications-comms/22-RESEARCH.md
@.planning/phases/22-notifications-comms/22-UI-SPEC.md

# Consumes 22-03 (WebhookSubscription/WebhookEventType/WebhookUrlValidator) + 22-04 (webhook.deliveries queue constant).
@.planning/phases/22-notifications-comms/22-03-SUMMARY.md
@.planning/phases/22-notifications-comms/22-04-SUMMARY.md

<interfaces>
<!-- LOCKED wire contract (Assumption A4 resolved). Documented for vendors + consumed by 22-06 UI. -->
Envelope (WebhookEventEnvelope): { id (uuid, = dedupe key), type (e.g. "order.ready"), tenantId, occurredAt (ISO-8601), version (config webhook.envelope.version, default "1"), data (full existing DTO — OrderDto / refund / onboarding, D-05) }
Headers on every POST:
  - X-JToye-Signature: t=<unixSeconds>,v1=<hexHmacSha256>
  - X-JToye-Event-Id: <envelope.id>   (receiver dedupe key)
  - X-JToye-Event-Type: <envelope.type>
Receiver verify: recompute HMAC over `t + "." + rawBody`, constant-time compare, reject if |now - t| > tolerance (config, default 300s).
WebhookDelivery status: PENDING, DELIVERED, RETRYING, FAILED ; columns include is_replay (bool), replay_of (nullable uuid), attempt_count, last_http_status, last_error, next_attempt_at, payload (TEXT — the exact signed bytes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: V56 migration (webhook_delivery, RLS helper form + claim indexes) + entity/repo + WebhookProperties + webhook.* config + RLS test</name>
  <files>core-java/src/main/resources/db/migration/V56__webhook_delivery.sql, core-java/src/main/java/uk/jtoye/core/webhook/WebhookDelivery.java, core-java/src/main/java/uk/jtoye/core/webhook/WebhookDeliveryRepository.java, core-java/src/main/java/uk/jtoye/core/webhook/WebhookProperties.java, core-java/src/main/resources/application.yml, core-java/src/test/java/uk/jtoye/core/webhook/WebhookDeliveryRlsPolicyIntegrationTest.java</files>
  <read_first>
    - core-java/src/main/resources/db/migration/V51__rls_uuid_cast_safety.sql §84-87 (current_tenant_id() policy form)
    - core-java/src/main/java/uk/jtoye/core/payment/PaymentEventOutbox.java (status/attempts/next-attempt/backoff row shape to mirror) and PaymentEventOutboxFlusher §156-185 (the claimDueBatch FOR UPDATE SKIP LOCKED query)
    - core-java/src/main/java/uk/jtoye/core/tenant/keycloak/KeycloakAdminProperties.java (@ConfigurationProperties template for WebhookProperties)
    - core-java/src/test/java/uk/jtoye/core/common/idempotency/IdempotencyKeysRlsPolicyIntegrationTest.java (COPY for the RLS test)
    - .planning/phases/22-notifications-comms/22-CONTEXT.md D-07 (config-injected attempt count / backoff / auto-pause threshold / retention window)
  </read_first>
  <action>
    Write `V56__webhook_delivery.sql` (after V55): `webhook_delivery(id uuid pk default gen_random_uuid(), tenant_id uuid not null, subscription_id uuid not null, event_id uuid not null, event_type varchar(48) not null, payload text not null, status varchar(16) not null default 'PENDING', attempt_count int not null default 0, next_attempt_at timestamptz not null default now(), last_http_status int, last_error text, is_replay boolean not null default false, replay_of uuid, created_at timestamptz default now(), updated_at timestamptz default now())`; `ENABLE` + `FORCE ROW LEVEL SECURITY`; policy via `current_tenant_id()` (helper form). Add a claim index `(status, next_attempt_at)` and a retention index `(created_at)` and `(subscription_id)`. Create `WebhookDelivery` entity + repo with a native `claimDueBatch(int limit)` = `SELECT ... WHERE status IN ('PENDING','RETRYING') AND next_attempt_at <= now() ORDER BY next_attempt_at FOR UPDATE SKIP LOCKED LIMIT ?` (mirror PaymentEventOutboxFlusher's claim), plus a `deleteByCreatedAtBefore` prune finder. Create `WebhookProperties` (`@ConfigurationProperties(prefix="webhook")`) exposing `delivery.intervalMs`(5000) `delivery.batchSize`(50) `delivery.maxAttempts`(8) `delivery.backoffBaseMs`(1000) `delivery.backoffCapMs`(3600000) `delivery.autoPauseThreshold`(10) `delivery.retentionDays`(30) `delivery.timeoutSeconds`(10) `delivery.signatureToleranceSeconds`(300) `envelope.version`("1") `target.blockPrivateRanges`(true). Add all keys to `application.yml` under `webhook:` via `${ENV:default}` (no literals). Copy the RLS integration test for `webhook_delivery` (NOSUPERUSER cross-tenant → 0 rows).
  </action>
  <verify>
    <automated>cd core-java && ./gradlew integrationTest --tests 'uk.jtoye.core.webhook.WebhookDeliveryRlsPolicyIntegrationTest' --tests 'uk.jtoye.core.security.RlsContractTest' -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*--' core-java/src/main/resources/db/migration/V56__webhook_delivery.sql | grep -c "current_setting('app.current_tenant_id'"` equals 0; migration is `V56__`; declares ENABLE + FORCE RLS
    - `grep -v '^\s*#' core-java/src/main/resources/application.yml | grep -c 'retention-days'` ≥ 1 (webhook.delivery.retention-days injected, no literal in code)
    - `WebhookDeliveryRlsPolicyIntegrationTest` proves cross-tenant SELECT under NOSUPERUSER returns 0 rows
    - the repo `claimDueBatch` query contains `FOR UPDATE SKIP LOCKED`
  </acceptance_criteria>
  <done>V56 FORCE-RLS delivery table + claim/retention indexes; entity/repo/claim query; all webhook tunables config-injected; RLS proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WebhookSigner (HMAC-SHA256 t=,v1=) + versioned WebhookEventEnvelope + WebhookFanoutListener (INSERT PENDING rows) + signer test</name>
  <files>core-java/src/main/java/uk/jtoye/core/webhook/WebhookSigner.java, core-java/src/main/java/uk/jtoye/core/webhook/WebhookEventEnvelope.java, core-java/src/main/java/uk/jtoye/core/webhook/WebhookFanoutListener.java, core-java/src/test/java/uk/jtoye/core/webhook/WebhookSignerTest.java</files>
  <read_first>
    - .planning/phases/22-notifications-comms/22-RESEARCH.md Code Example 2 (Stripe t=,v1= scheme: signed_payload = timestamp + "." + rawBody; Mac HmacSHA256; MessageDigest.isEqual) and Pitfall 6 (sign the EXACT bytes you POST — serialize once)
    - core-java/src/main/java/uk/jtoye/core/payment/PaymentEventAuditListener.java §19-20 (2nd-consumer @RabbitListener shape) and OrderStateChangeListener.java §83-90 (TenantContext+GUC preamble)
    - 22-03-SUMMARY.md (WebhookSubscription + WebhookEventType + repo) and 22-04-SUMMARY.md (webhook.deliveries queue constant)
    - core-java/src/main/java/uk/jtoye/core/order/OrderDto.java (the DTO reused as envelope `data`, D-05) + the refund/onboarding event payloads
  </read_first>
  <behavior>
    - WebhookSigner.sign(byte[] rawBody, String secret, long unixTs) returns "t=<ts>,v1=<hex>" where hex = HmacSHA256(secret) over (ts + "." + rawBody); a byte-flipped body yields a different signature; signing with secret A then verifying against secret B fails (rotation invalidates old signatures).
    - WebhookFanoutListener.onEvent(...): pins TenantContext+GUC, finds ACTIVE subscriptions whose event_types match the event family, and for EACH inserts one PENDING webhook_delivery row carrying the envelope serialized ONCE to a stable string (payload column). It performs NO HTTP (delivery is the worker's job).
    - A REVOKED/PAUSED/AUTO_PAUSED subscription receives no new delivery row.
  </behavior>
  <action>
    Create `WebhookSigner` per RESEARCH Code Example 2 (JDK `Mac` HmacSHA256; `sign(byte[], secret, ts)` → `t=..,v1=..`; a `verify(...)` helper using `MessageDigest.isEqual`). Create `WebhookEventEnvelope` record (the <interfaces> shape; `version` from `WebhookProperties.envelope.version`). Create `WebhookFanoutListener` (`@RabbitListener(queues = webhook.deliveries constant)`) consuming `onboarding.state.*` / `order.state.*` / `order.refunded` / `payment.*`: run the OrderStateChangeListener §83-90 TenantContext+GUC preamble (try/finally), build the envelope with the full DTO as `data` (D-05), serialize ONCE to `objectMapper.writeValueAsString(envelope)`, then INSERT a PENDING `webhook_delivery` row per matching ACTIVE subscription (match `WebhookEventType`). No inline HTTP (Pattern 2). Unit-test the signer (deterministic sig, byte-flip changes it, old-secret verify fails).
  </action>
  <verify>
    <automated>cd core-java && ./gradlew test --tests 'uk.jtoye.core.webhook.WebhookSignerTest' -q</automated>
  </verify>
  <acceptance_criteria>
    - `WebhookSignerTest` asserts `sign(body, "A", ts)` != `sign(bodyFlipped, "A", ts)` and that a signature made with secret "A" fails a verify against secret "B"
    - `WebhookSignerTest` asserts the header format matches `^t=\d+,v1=[0-9a-f]{64}$`
    - `WebhookFanoutListener` compiles and inserts one PENDING row per matching ACTIVE subscription (asserted end-to-end in Task 3's worker integration test setup)
    - the envelope is serialized exactly once (payload stored as the signed string; no re-serialize at send — Pitfall 6)
  </acceptance_criteria>
  <done>HMAC signer + versioned envelope exist; fanout inserts PENDING delivery rows per matching ACTIVE subscription without doing HTTP.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WebhookDeliveryWorker (SKIP-LOCKED per-tenant, backoff, auto-pause, no HOL) + WebhookRetentionCleanup + WebhookDeliveryController (log+replay) + integration tests</name>
  <files>core-java/src/main/java/uk/jtoye/core/webhook/WebhookDeliveryWorker.java, core-java/src/main/java/uk/jtoye/core/webhook/WebhookRetentionCleanup.java, core-java/src/main/java/uk/jtoye/core/webhook/WebhookDeliveryController.java, core-java/src/test/java/uk/jtoye/core/webhook/WebhookDeliveryWorkerIntegrationTest.java, core-java/src/test/java/uk/jtoye/core/webhook/WebhookRetentionCleanupTest.java</files>
  <read_first>
    - core-java/src/main/java/uk/jtoye/core/payment/PaymentEventOutboxFlusher.java §112-128 (computeBackoffMillis — copy verbatim, incl the loop-not-shift overflow guard), §156-185 (@Scheduled + per-tenant deliverForTenant + TransactionTemplate + SKIP LOCKED), §86-90 (the @Transactional-private self-invocation trap)
    - core-java/src/main/java/uk/jtoye/core/config/ScheduledCleanupService.java §57-58, 94-112 (per-tenant prune shape for retention)
    - core-java/src/main/java/uk/jtoye/core/onboarding/client/CompaniesHouseClient.java §38, 78-108 (WebClient .block(TIMEOUT) under @CircuitBreaker; log STATUS ONLY, never the secret)
    - core-java/src/main/java/uk/jtoye/core/payment/RefundController.java §71-80 (replay POST reads @RequestHeader Idempotency-Key)
    - .planning/phases/22-notifications-comms/22-RESEARCH.md Pattern 2 + Pattern 3 (per-subscription isolation + auto-pause; async TenantContext landmine)
  </read_first>
  <behavior>
    - WebhookDeliveryWorker (@Scheduled) iterates tenants; per tenant, in its OWN TransactionTemplate tx with TenantContext pinned, claims due rows (claimDueBatch SKIP LOCKED), and for each: reads the subscription's CURRENT secret, signs the stored payload bytes, POSTs via WebClient with the three headers, on 2xx marks DELIVERED + resets the subscription's consecutive_failures, on failure increments attempt_count + sets next_attempt_at via computeBackoffMillis + marks RETRYING (or FAILED at maxAttempts) + increments consecutive_failures; when consecutive_failures crosses autoPauseThreshold the subscription flips to AUTO_PAUSED (worker's claim skips it).
    - No head-of-line block: with one subscription returning 500 and a second returning 200, the healthy subscription's delivery reaches DELIVERED in the same/next cycle while the failing one backs off independently.
    - WebhookRetentionCleanup (@Scheduled) prunes webhook_delivery rows older than retention-days per tenant (suppression rows are NOT time-pruned — bounded by their UNIQUE key; GDPR).
    - Replay: POST /webhooks/{id}/deliveries/{deliveryId}/replay inserts a NEW webhook_delivery row (is_replay=true, replay_of=original id, status PENDING) — the original row's status history is untouched; carries an Idempotency-Key header.
  </behavior>
  <action>
    Create `WebhookDeliveryWorker`: copy PaymentEventOutboxFlusher §156-185 (`@Scheduled(fixedDelayString="${webhook.delivery.interval-ms:5000}")` → per-tenant loop, `deliverForTenant` using an injected `TransactionTemplate` NOT a `@Transactional` private method) and §112-128 `computeBackoffMillis` verbatim; deliver via `WebClient` (CompaniesHouseClient timeout+`@CircuitBreaker`, log status only). Sign the stored `payload` bytes with the subscription's current secret + a fresh timestamp (`WebhookSigner`), POST with `X-JToye-Signature`/`X-JToye-Event-Id`/`X-JToye-Event-Type`. Implement backoff/auto-pause/consecutive-failures + status transitions per <behavior>. Create `WebhookRetentionCleanup` (ScheduledCleanupService shape) pruning `webhook_delivery` older than `retention-days`. Create `WebhookDeliveryController` (`@RequestMapping("/webhooks/{id}/deliveries")`, bearer-jwt+tenant-header): `GET` (paged delivery log, filter by event type + status), `POST /{deliveryId}/replay` (`@RequestHeader Idempotency-Key`, inserts the tagged replay row) — RFC 7807 errors. Integration test: two subscriptions (one endpoint 500, one 200) → assert the 200 sub reaches DELIVERED while the 500 sub retries then FAILED and finally AUTO_PAUSED (no HOL block); a WebClient-mocked/local-echo receiver verifies the HMAC over the exact bytes; a replay adds one is_replay row without mutating the original. Unit/integration-test retention prune removes rows older than the window and keeps newer ones.
  </action>
  <verify>
    <automated>cd core-java && ./gradlew integrationTest --tests 'uk.jtoye.core.webhook.WebhookDeliveryWorkerIntegrationTest' -q && ./gradlew test --tests 'uk.jtoye.core.webhook.WebhookRetentionCleanupTest' -q</automated>
  </verify>
  <acceptance_criteria>
    - `WebhookDeliveryWorkerIntegrationTest`: with a failing (500) and a healthy (200) subscription, the healthy subscription's delivery reaches `DELIVERED` while the failing one ends `FAILED`+`AUTO_PAUSED` — proving no head-of-line block
    - a local/mock receiver recomputes HMAC-SHA256 over `t + "." + rawBody` and matches the `X-JToye-Signature` header (payload bytes signed == bytes POSTed)
    - a replay POST creates exactly one new row with `is_replay=true`, `replay_of` = the original id, and the original row's status is unchanged
    - `WebhookRetentionCleanupTest` asserts a row older than `retention-days` is deleted and a recent row is retained
    - the worker uses a `TransactionTemplate` (not a `@Transactional` private method) — grep confirms `TransactionTemplate` present, guarding the self-invocation NULL-tenant trap
  </acceptance_criteria>
  <done>Signed deliveries retry with bounded backoff, auto-pause, and per-subscription isolation; retention prunes delivery rows; replay is a tagged Idempotency-Key-safe new attempt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → vendor URL | outbound HTTP egress to a vendor-supplied URL (validated at create in 22-03; re-guard at send) |
| scheduled thread → DB | off-request tenant-scoped writes — GUC must be pinned per tenant |
| payload → vendor endpoint | full-entity snapshot of the VENDOR'S OWN tenant data to the VENDOR'S OWN endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05-01 | Tampering/Spoofing | Forged/tampered webhook payload | mitigate | HMAC-SHA256 `t=,v1=` per-subscription secret; sign the exact POSTed bytes (Pitfall 6); receiver constant-time verify documented |
| T-22-05-02 | Repudiation | Replay of a captured webhook | mitigate | signed timestamp + receiver tolerance (config 300s) + envelope `id` dedupe key (X-JToye-Event-Id) |
| T-22-05-03 | Elevation/Info Disclosure | SSRF at delivery time (target re-resolves to internal IP / DNS rebinding) | mitigate | re-apply `WebhookUrlValidator` (block private/link-local/metadata) before egress; WebClient timeout |
| T-22-05-04 | Denial of Service | Head-of-line block starving other tenants/subscriptions | mitigate | per-`(subscription,event)` rows + SKIP LOCKED + per-tenant tx + auto-pause (Task 3 HOL proof) |
| T-22-05-05 | Denial of Service | Unbounded webhook_delivery growth (#107) | mitigate | `WebhookRetentionCleanup` @Scheduled prune of the delivery log by retention window |
| T-22-05-06 | Information Disclosure | signing_secret in logs | mitigate | log HTTP status only, never the secret (CompaniesHouseClient §106-108 posture) |
| T-22-05-07 | Information Disclosure | Cross-tenant read of delivery payloads | mitigate | webhook_delivery ENABLE+FORCE RLS via `current_tenant_id()`, NOSUPERUSER-proven (Task 1) |
| T-22-05-SC | Tampering | package installs | accept | no packages installed (RESEARCH Package Legitimacy Audit — N/A) |
</threat_model>

<verification>
- `cd core-java && ./gradlew test --tests 'uk.jtoye.core.webhook.WebhookSignerTest' --tests 'uk.jtoye.core.webhook.WebhookRetentionCleanupTest'` green
- `cd core-java && ./gradlew integrationTest --tests 'uk.jtoye.core.webhook.WebhookDelivery*' --tests 'RlsContractTest'` green
</verification>

<success_criteria>
- Delivered payloads carry a verifiable HMAC-SHA256 signature over the exact bytes
- Failing endpoint retries with bounded backoff → FAILED → AUTO_PAUSED; a healthy subscription still delivers (no HOL block)
- Delivery rows pruned by retention window; replay is a tagged, Idempotency-Key-safe new attempt
- webhook_delivery FORCE-RLS isolation proven under NOSUPERUSER; all tunables config-injected
</success_criteria>

<output>
Create `.planning/phases/22-notifications-comms/22-05-SUMMARY.md` when done.
</output>
