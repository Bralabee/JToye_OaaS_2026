---
phase: 22-notifications-comms
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - core-java/src/main/resources/db/migration/V55__webhook_subscription.sql
  - core-java/src/main/java/uk/jtoye/core/webhook/WebhookSubscription.java
  - core-java/src/main/java/uk/jtoye/core/webhook/WebhookSubscriptionRepository.java
  - core-java/src/main/java/uk/jtoye/core/webhook/WebhookEventType.java
  - core-java/src/main/java/uk/jtoye/core/webhook/WebhookSubscriptionService.java
  - core-java/src/main/java/uk/jtoye/core/webhook/WebhookUrlValidator.java
  - core-java/src/main/java/uk/jtoye/core/webhook/WebhookSubscriptionController.java
  - core-java/src/main/java/uk/jtoye/core/webhook/dto/CreateWebhookSubscriptionRequest.java
  - core-java/src/main/java/uk/jtoye/core/webhook/dto/WebhookSubscriptionDto.java
  - core-java/src/test/java/uk/jtoye/core/webhook/WebhookSubscriptionRlsPolicyIntegrationTest.java
  - core-java/src/test/java/uk/jtoye/core/webhook/WebhookSubscriptionServiceTest.java
  - core-java/src/test/java/uk/jtoye/core/webhook/WebhookUrlValidatorTest.java
  - core-java/src/test/java/uk/jtoye/core/webhook/WebhookSubscriptionControllerIntegrationTest.java
autonomous: true
requirements: [COMMS-04]
must_haves:
  truths:
    - "A vendor can create/list/rotate-secret/pause/resume/revoke webhook subscriptions via a tenant-scoped REST API"
    - "The signing secret is returned in plaintext only once (on create + rotate) and is never re-fetchable"
    - "Rotating the secret changes the stored secret so signatures made with the old secret no longer verify"
    - "A non-HTTPS or private/link-local target_url is rejected at create (RFC 7807 400)"
    - "A cross-tenant list under NOSUPERUSER returns empty/403 (webhook_subscription is ENABLE+FORCE RLS via current_tenant_id())"
  artifacts:
    - path: "core-java/src/main/resources/db/migration/V55__webhook_subscription.sql"
      provides: "webhook_subscription table, RLS helper form, plaintext signing_secret under FORCE RLS"
      contains: "current_tenant_id()"
    - path: "core-java/src/main/java/uk/jtoye/core/webhook/WebhookSubscriptionController.java"
      provides: "CRUD + rotate/pause/resume/revoke REST API (RFC 7807)"
      exports: ["create", "list", "rotateSecret", "pause", "resume", "revoke"]
    - path: "core-java/src/main/java/uk/jtoye/core/webhook/WebhookUrlValidator.java"
      provides: "HTTPS-only + SSRF (private/link-local/metadata) block for target_url"
      contains: "https"
  key_links:
    - from: "WebhookSubscriptionController.create"
      to: "WebhookUrlValidator"
      via: "reject non-https / private-range URL before persist"
      pattern: "validate|isAllowed"
    - from: "WebhookSubscriptionService.rotateSecret"
      to: "WebhookSubscription.signingSecret"
      via: "SecureRandom regenerate + persist"
      pattern: "SecureRandom"
---

<objective>
Stand up the vendor webhook subscription resource (COMMS-04): the `V55` migration (webhook_subscription, ENABLE+FORCE RLS, plaintext `signing_secret` protected by FORCE RLS), the entity/repo, per-subscription event-type selection (D-06), a `WebhookSubscriptionService` (create with `SecureRandom` secret, list, rotate-secret, pause/resume/revoke), an SSRF-aware `WebhookUrlValidator` (HTTPS-only + block private/link-local/metadata ranges), and a tenant-scoped `WebhookSubscriptionController` (RFC 7807; secret shown once).

Purpose: the data + management surface the delivery engine (22-05) and dashboard UI (22-06) build on. Fully independent of the email/consent tracks (own package) — Wave 1 parallel. Declares only its own files (no RabbitMQConfig, no application.yml — delivery tunables land in 22-05).
Output: 1 migration + entity/repo + event-type enum + service + URL validator + controller + DTOs + RLS/service/validator/controller tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-notifications-comms/22-SPEC.md
@.planning/phases/22-notifications-comms/22-CONTEXT.md
@.planning/phases/22-notifications-comms/22-PATTERNS.md
@.planning/phases/22-notifications-comms/22-RESEARCH.md
@.planning/phases/22-notifications-comms/22-UI-SPEC.md

<interfaces>
<!-- Contracts consumed by 22-05 (delivery) and 22-06 (UI). -->
WebhookEventType (NEW enum, D-06 families): ORDER_STATE_CHANGED, ORDER_REFUNDED, ONBOARDING_STATE_CHANGED, PAYMENT_EVENT
  (each maps to a routing key: order.state.*, order.refunded, onboarding.state.*, payment.*)

WebhookSubscription status enum: ACTIVE, PAUSED, AUTO_PAUSED, REVOKED
  (worker skips all but ACTIVE; manual pause=PAUSED, auto-pause=AUTO_PAUSED — UI distinguishes them)

WebhookSubscriptionDto (list/detail, NEVER carries signing_secret):
  { id, targetUrl, eventTypes[], status, consecutiveFailures, createdAt, updatedAt }

Create/rotate RESPONSE only: WebhookSubscriptionDto + `signingSecret` (plaintext, ONCE)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: V55 migration (webhook_subscription, RLS helper form) + entity + repo + WebhookEventType</name>
  <files>core-java/src/main/resources/db/migration/V55__webhook_subscription.sql, core-java/src/main/java/uk/jtoye/core/webhook/WebhookSubscription.java, core-java/src/main/java/uk/jtoye/core/webhook/WebhookSubscriptionRepository.java, core-java/src/main/java/uk/jtoye/core/webhook/WebhookEventType.java</files>
  <read_first>
    - core-java/src/main/resources/db/migration/V51__rls_uuid_cast_safety.sql §84-87 (current_tenant_id() policy form — Pitfall 4)
    - .planning/phases/22-notifications-comms/22-RESEARCH.md Code Example 4 (webhook_subscription DDL: target_url, event_types TEXT[], signing_secret plaintext under FORCE RLS, status, consecutive_failures) and Assumption A3 (plaintext secret + FORCE RLS is load-bearing, like V50 response_body)
    - core-java/src/main/java/uk/jtoye/core/review/Review.java (RLS JPA entity template)
    - .planning/phases/22-notifications-comms/22-CONTEXT.md D-05, D-06
  </read_first>
  <action>
    Write `V55__webhook_subscription.sql` (after V51; V52/V53 reserved): `webhook_subscription(id uuid pk default gen_random_uuid(), tenant_id uuid not null, target_url text not null, event_types text[] not null, signing_secret text not null, status varchar(16) not null default 'ACTIVE', consecutive_failures int not null default 0, created_at timestamptz default now(), updated_at timestamptz default now())`; `ENABLE` + `FORCE ROW LEVEL SECURITY`; policy `webhook_subscription_tenant FOR ALL USING (tenant_id = current_tenant_id()) WITH CHECK (tenant_id = current_tenant_id())` (helper form — NO raw cast). Index on `(tenant_id, status)`. `signing_secret` is stored plaintext (must re-sign every delivery) — FORCE RLS is the confidentiality boundary (mirror V50 response_body rationale). Create `WebhookEventType` enum (the four families in <interfaces>) with each value carrying its AMQP routing key. Create `WebhookSubscription` entity (map `event_types` as a `List<WebhookEventType>`/String[] converter; `status` as the status enum) + repo with `findByTenantId`, `findByStatus`, tenant-scoped finders.
  </action>
  <verify>
    <automated>cd core-java && ./gradlew integrationTest --tests 'uk.jtoye.core.security.RlsContractTest' -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*--' core-java/src/main/resources/db/migration/V55__webhook_subscription.sql | grep -c "current_setting('app.current_tenant_id'"` equals 0
    - migration is `V55__` and declares both `ENABLE` and `FORCE ROW LEVEL SECURITY`
    - `RlsContractTest.noPolicyUsesRawTenantGucCast` passes with the new table present
    - `WebhookEventType` has exactly four values mapping to order.state.*, order.refunded, onboarding.state.*, payment.*
  </acceptance_criteria>
  <done>V55 creates the FORCE-RLS webhook_subscription table via the helper form; entity/repo/event-type enum compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WebhookUrlValidator (HTTPS + SSRF block) + WebhookSubscriptionService (secret gen, rotate, pause/resume/revoke) + RLS/service/validator tests</name>
  <files>core-java/src/main/java/uk/jtoye/core/webhook/WebhookUrlValidator.java, core-java/src/main/java/uk/jtoye/core/webhook/WebhookSubscriptionService.java, core-java/src/test/java/uk/jtoye/core/webhook/WebhookUrlValidatorTest.java, core-java/src/test/java/uk/jtoye/core/webhook/WebhookSubscriptionServiceTest.java, core-java/src/test/java/uk/jtoye/core/webhook/WebhookSubscriptionRlsPolicyIntegrationTest.java</files>
  <read_first>
    - core-java/src/main/java/uk/jtoye/core/review/ReviewService.java (tenant-scoped @Transactional service template)
    - core-java/src/test/java/uk/jtoye/core/common/idempotency/IdempotencyKeysRlsPolicyIntegrationTest.java (COPY for the RLS test — NOSUPERUSER role-downgrade)
    - .planning/phases/22-notifications-comms/22-RESEARCH.md §"Security Domain" (V9 Communications — HTTPS-only + block internal/link-local; SSRF via target_url)
    - .planning/phases/22-notifications-comms/22-CONTEXT.md D-05 (HTTPS-only required)
  </read_first>
  <behavior>
    - WebhookUrlValidator.validate(url): passes for https://host with a public IP; throws (or returns invalid) for http:// scheme, for 127.0.0.1/localhost, for RFC1918 (10./172.16./192.168.), link-local 169.254.0.0/16 (incl 169.254.169.254 metadata), and ::1/fc00::/fe80::.
    - WebhookSubscriptionService.create(...): generates a signing secret via SecureRandom (≥256 bits, base64url), persists ACTIVE, returns the DTO + plaintext secret ONCE.
    - rotateSecret(id): generates a NEW secret, persists, returns the new plaintext once; the previously-stored secret value is no longer present.
    - pause/resume/revoke: transition status; resume resets consecutive_failures to 0; revoke is terminal.
  </behavior>
  <action>
    Create `WebhookUrlValidator` rejecting non-HTTPS and private/loopback/link-local/metadata IP targets (resolve host, test `InetAddress.isLoopbackAddress/isSiteLocalAddress/isLinkLocalAddress/isAnyLocalAddress` + explicit 169.254.169.254). A `@Value("${webhook.target.block-private-ranges:true}")` toggle (default on; yml key documented in 22-05). Create `WebhookSubscriptionService` (`@Transactional`, tenant-scoped like ReviewService): `create` (validate URL, ≥1 event type, `SecureRandom` secret → base64url, persist ACTIVE), `list` (DTOs, no secret), `rotateSecret`, `pause`/`resume`(reset consecutive_failures)/`revoke`. Never log the secret. Copy `IdempotencyKeysRlsPolicyIntegrationTest` → `WebhookSubscriptionRlsPolicyIntegrationTest` retargeted to `webhook_subscription` (cross-tenant list → 0 rows / forged write → RLS error). Unit-test the validator (each blocked range) and the service (rotate changes the stored secret; create rejects a non-HTTPS URL).
  </action>
  <verify>
    <automated>cd core-java && ./gradlew test --tests 'uk.jtoye.core.webhook.WebhookUrlValidatorTest' --tests 'uk.jtoye.core.webhook.WebhookSubscriptionServiceTest' -q && ./gradlew integrationTest --tests 'uk.jtoye.core.webhook.WebhookSubscriptionRlsPolicyIntegrationTest' -q</automated>
  </verify>
  <acceptance_criteria>
    - `WebhookUrlValidatorTest` asserts rejection of `http://x`, `https://127.0.0.1`, `https://169.254.169.254`, `https://10.0.0.5`, `https://192.168.1.1`, and acceptance of a public `https://` host
    - `WebhookSubscriptionServiceTest` asserts `rotateSecret` yields a secret different from the pre-rotation secret and the entity no longer holds the old value
    - `WebhookSubscriptionRlsPolicyIntegrationTest` proves a cross-tenant SELECT under NOSUPERUSER returns 0 rows
    - grep of service source shows no `log.*signing_?[Ss]ecret` cleartext logging
  </acceptance_criteria>
  <done>URL validator blocks non-HTTPS + SSRF ranges; service manages lifecycle + secret rotation; RLS isolation proven.</done>
</task>

<task type="auto">
  <name>Task 3: WebhookSubscriptionController (CRUD + rotate/pause/resume/revoke, RFC 7807, secret-once) + DTOs + integration test</name>
  <files>core-java/src/main/java/uk/jtoye/core/webhook/WebhookSubscriptionController.java, core-java/src/main/java/uk/jtoye/core/webhook/dto/CreateWebhookSubscriptionRequest.java, core-java/src/main/java/uk/jtoye/core/webhook/dto/WebhookSubscriptionDto.java, core-java/src/test/java/uk/jtoye/core/webhook/WebhookSubscriptionControllerIntegrationTest.java</files>
  <read_first>
    - core-java/src/main/java/uk/jtoye/core/shop/PromotionController.java §27-105 (tenant-scoped CRUD quartet + @Valid + ServletUriComponentsBuilder Location + @SecurityRequirement)
    - core-java/src/main/java/uk/jtoye/core/payment/RefundController.java §71-80 (custom-action POST shell)
    - core-java/src/main/java/uk/jtoye/core/common/GlobalExceptionHandler.java (RFC 7807 @RestControllerAdvice — throw ResourceNotFoundException for 404)
    - .planning/phases/22-notifications-comms/22-PATTERNS.md §"WebhookSubscriptionController" (lines 207-226) and 22-UI-SPEC.md §"Interaction Contracts" (secret shown once, HTTPS validation copy)
  </read_first>
  <action>
    Create `WebhookSubscriptionController` `@RequestMapping("/webhooks")` (→ `/api/v1/webhooks` via the WebConfig prefix), `@SecurityRequirement` bearer-jwt + tenant-header, constructor-injected service (PromotionController §27-37 shell). Endpoints: `GET /webhooks` (list DTOs), `GET /webhooks/{id}` (`.map(ok).orElse(notFound)`), `POST /webhooks` (`@Valid CreateWebhookSubscriptionRequest` — `targetUrl` HTTPS-validated via the DTO `@Pattern`/service validator, `eventTypes` ≥1; 201 + Location; response body carries the plaintext `signingSecret` ONCE), `POST /webhooks/{id}/rotate-secret` (returns new secret once), `POST /webhooks/{id}/pause`, `POST /webhooks/{id}/resume`, `POST /webhooks/{id}/revoke`. Missing id → `ResourceNotFoundException` (RFC 7807 404 via GlobalExceptionHandler); non-HTTPS/private URL → 400 problem-detail. `WebhookSubscriptionDto` (list/get) must NOT include `signingSecret`. Integration test (Testcontainers/MockMvc): create returns a secret; a subsequent GET does NOT return the secret; rotate returns a new secret; revoke → the row is REVOKED; a non-HTTPS create → 400 RFC 7807.
  </action>
  <verify>
    <automated>cd core-java && ./gradlew integrationTest --tests 'uk.jtoye.core.webhook.WebhookSubscriptionControllerIntegrationTest' -q</automated>
  </verify>
  <acceptance_criteria>
    - Integration test: `POST /api/v1/webhooks` returns 201 with a non-null `signingSecret`; the following `GET /api/v1/webhooks/{id}` response JSON has no `signingSecret` field
    - `POST /api/v1/webhooks/{id}/rotate-secret` returns a `signingSecret` different from the create-time secret
    - a create with `targetUrl` = `http://example.com` returns 400 with an RFC 7807 problem-detail (has `detail`/`title`)
    - a `GET /api/v1/webhooks/{unknown-uuid}` returns 404 RFC 7807
  </acceptance_criteria>
  <done>Full subscription CRUD + custom actions work; secret is shown once and never re-fetchable; errors are RFC 7807.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendor client → webhook API | authenticated (bearer-jwt + tenant-header); vendor supplies an arbitrary target_url |
| app → vendor-supplied URL | the URL becomes an egress target in 22-05 — validate at create |
| tenant ↔ tenant | webhook_subscription (incl. signing_secret) must be RLS-isolated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | Elevation/Info Disclosure | SSRF via attacker-set target_url (internal/metadata endpoints) | mitigate | `WebhookUrlValidator` HTTPS-only + block loopback/RFC1918/link-local/169.254.169.254 at create (Task 2) |
| T-22-03-02 | Information Disclosure | Cross-tenant read of signing_secret | mitigate | ENABLE+FORCE RLS via `current_tenant_id()`, NOSUPERUSER-proven (Tasks 1-2); secret never in list/get DTO |
| T-22-03-03 | Information Disclosure | Secret leakage via re-fetch/logs | mitigate | plaintext secret returned only once on create/rotate; never in GET DTO; no secret logging (Task 2-3) |
| T-22-03-04 | Spoofing | Old-secret signatures still valid after rotation | mitigate | rotate replaces the stored secret (Task 2); delivery signs with current secret (22-05) — HMAC with old secret fails receiver verify |
| T-22-03-SC | Tampering | package installs | accept | no packages installed (RESEARCH Package Legitimacy Audit — N/A) |
</threat_model>

<verification>
- `cd core-java && ./gradlew test --tests 'uk.jtoye.core.webhook.WebhookSubscription*' --tests 'uk.jtoye.core.webhook.WebhookUrlValidator*'` green
- `cd core-java && ./gradlew integrationTest --tests 'uk.jtoye.core.webhook.WebhookSubscription*' --tests 'RlsContractTest'` green
</verification>

<success_criteria>
- V55 webhook_subscription is FORCE-RLS (helper form), isolation proven under NOSUPERUSER
- Subscription CRUD + rotate/pause/resume/revoke work; secret shown once, never re-fetchable
- Non-HTTPS / SSRF target URLs rejected with RFC 7807; rotation invalidates old-secret signatures
</success_criteria>

<output>
Create `.planning/phases/22-notifications-comms/22-03-SUMMARY.md` when done.
</output>
