---
phase: 21-onboarding-blocker-ux
plan: 04
type: execute
wave: 3
depends_on: ["21-01", "21-03"]
files_modified:
  - frontend/types/api.ts
  - frontend/lib/env-validation.ts
  - frontend/.env.local.example
  - frontend/app/dashboard/onboarding/page.tsx
  - frontend/app/dashboard/onboarding/approvals/page.tsx
  - frontend/app/dashboard/onboarding/__tests__/page.test.tsx
  - frontend/app/dashboard/onboarding/approvals/__tests__/page.test.tsx
autonomous: true
requirements: [ONBD-01, ONBD-02, ONBD-03, ONBD-04, ONBD-05]

must_haves:
  truths:
    - "A vendor whose application is reviewPending sees an honest 'in review — a reviewer checks these within N business days' state (config-injected SLA), not the forever 'under a minute' spinner, and polling backs off"
    - "Each FAILED/MANUAL_REVIEW gate renders why -> what to do -> a button that goes there (company-number inline edit, allergen 'fix these N products' -> /dashboard/products, FHRS -> shop edit)"
    - "A vendor can withdraw from a confirm dialog on any pre-live state; the app reaches WITHDRAWN and restarting begins a fresh application"
    - "A vendor can edit the company number inline (calling the update endpoint) and then resubmit"
    - "REJECTED/SUSPENDED render the actual rejectionReason plus a config-injected support channel (mailto/URL), not a bare 'contact support'"
    - "An admin sees VERIFYING+MANUAL_REVIEW applications in the approvals surface with a gate-resolve control (decision + reason); the existing approve/reject queue is preserved"
    - "No hardcoded support mailto or SLA literal (GLOBAL_RULE_6); npm run build (tsc) passes"
  artifacts:
    - path: "frontend/types/api.ts"
      provides: "OnboardingDto widened (reviewPending, rejectionReason) + UpdateOnboardingRequest + ResolveGateRequest types"
      contains: "reviewPending"
    - path: "frontend/app/dashboard/onboarding/page.tsx"
      provides: "remediation map, in-review copy+backoff, withdraw dialog, inline company edit, rejection+support"
      contains: "REMEDIATION"
    - path: "frontend/app/dashboard/onboarding/approvals/page.tsx"
      provides: "review-pending list + gate-resolve dialog"
    - path: "frontend/lib/env-validation.ts"
      provides: "NEXT_PUBLIC_SUPPORT_EMAIL / _URL / _ONBOARDING_REVIEW_SLA_DAYS registration"
      contains: "SUPPORT"
  key_links:
    - from: "onboarding/page.tsx withdraw dialog"
      to: "POST /api/v1/onboarding/withdraw"
      via: "apiClient.post -> setOnboarding(res.data)"
      pattern: "onboarding/withdraw"
    - from: "onboarding/page.tsx inline company edit"
      to: "POST /api/v1/onboarding/company-number"
      via: "apiClient.post then existing resubmit"
      pattern: "onboarding/company-number"
    - from: "approvals/page.tsx gate-resolve"
      to: "POST /api/v1/onboarding/admin/{id}/gates/{gateType}/resolve"
      via: "apiClient.post {decision, reason}"
      pattern: "gates/.*resolve"
    - from: "REJECTED/SUSPENDED copy"
      to: "NEXT_PUBLIC_SUPPORT_EMAIL / _URL"
      via: "config-injected support channel"
      pattern: "NEXT_PUBLIC_SUPPORT"
---

<objective>
Turn the onboarding page from a silent black hole into an honest, actionable surface, and give the admin a way to unstick a review — consuming the backend endpoints and DTO fields shipped in 21-01/21-02/21-03.

Purpose: ONBD-04 (remediation blocks), the vendor-facing halves of ONBD-01/02/03/05, and the admin gate-resolve UI. This is an ADDITIVE rework of the existing vendor + admin pages: per the Incremental Betterment Doctrine, every existing good (create form, submit/resubmit/go-live CTAs, gate breakdown, milestone timeline, the approve/reject queue) is preserved and extended, not replaced. The forever "under a minute" spinner and the bare "Contact support for details" are the specific goods being *bettered*.

Output: widened `frontend/types/api.ts`, three `NEXT_PUBLIC_*` config keys, the reworked vendor `page.tsx`, the extended `approvals/page.tsx`, and Jest blocks — with `npm run build` (tsc) green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-onboarding-blocker-ux/21-CONTEXT.md
@.planning/phases/21-onboarding-blocker-ux/21-RESEARCH.md
@.planning/phases/21-onboarding-blocker-ux/21-PATTERNS.md

<interfaces>
<!-- Backend contracts this plan consumes (shipped by 21-01/21-03). Read the pages for the existing idioms. -->
- Backend endpoints: POST /api/v1/onboarding/withdraw (body-less -> OnboardingDto); POST /api/v1/onboarding/company-number {companyNumber}; POST /api/v1/onboarding/admin/{id}/gates/{gateType}/resolve {decision:"PASS"|"WAIVE"|"FAIL", reason}; GET /api/v1/onboarding/admin/reviews (List<AdminOnboardingDto>). Existing: POST /onboarding/resubmit, /submit, /go-live, GET /onboarding/me, GET /onboarding/admin/pending.
- OnboardingDto now includes reviewPending: boolean + rejectionReason: string | null (from 21-03).
- frontend/types/api.ts OnboardingDto interface (~:361-371) + AdminOnboardingDto + RejectOnboardingRequest (~:395-397) are the shape templates.
- onboarding/page.tsx: STATE_META/GATE_META static maps (~:54-94), GATE_FALLBACK defensive fallback (~:103), POLL_STATES + 4s interval (~:105-107, :183-189), go-live <Dialog> (~:516-533) + handleGoLive (~:262-288), create-form <Input>/<Label> (~:377-391), handleResubmit (~:245-260), failed-gate list reading gate.reason (~:449-467), "under a minute" (~:68-69), "Contact support" (~:74-75).
- approvals/page.tsx: loadQueue (~:98-116), reject dialog with required reason (~:324-376), handleReject (~:157-181), gateSummary/GATE_META/GATE_STATUS_META, the forbidden-403 pattern.
- env-validation.ts: EnvVars interface (~:8-21) + requiredEnvVars list (~:23-31). api-client.ts is the axios instance (baseURL NEXT_PUBLIC_API_URL) — USE, do not modify.
- Chosen config channel (A1): frontend NEXT_PUBLIC_* (build-time). Keys: NEXT_PUBLIC_SUPPORT_EMAIL, NEXT_PUBLIC_SUPPORT_URL, NEXT_PUBLIC_ONBOARDING_REVIEW_SLA_DAYS.
- Deep links (D-08): BUSINESS_VERIFIED:FAILED -> inline company-number edit anchor; ALLERGEN_DATA_COMPLETE:FAILED -> /dashboard/products; FOOD_HYGIENE_RATING:MANUAL_REVIEW -> /dashboard/shops (shop edit).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + config injection — widen api.ts, register NEXT_PUBLIC_* support/SLA keys</name>
  <files>frontend/types/api.ts, frontend/lib/env-validation.ts, frontend/.env.local.example</files>
  <read_first>
    - frontend/types/api.ts (~:361-397 — OnboardingDto/AdminOnboardingDto/RejectOnboardingRequest interfaces)
    - frontend/lib/env-validation.ts (~:8-31 — EnvVars interface + requiredEnvVars list)
    - frontend/.env.local.example (existing NEXT_PUBLIC_* entries)
  </read_first>
  <action>
    In frontend/types/api.ts add `rejectionReason: string | null` and `reviewPending: boolean` to the OnboardingDto interface (keep EXACT sync with the Java record or tsc breaks — Pitfall 6); add `UpdateOnboardingRequest { companyNumber?: string }` and `ResolveGateRequest { decision: "PASS" | "WAIVE" | "FAIL"; reason?: string }` mirroring RejectOnboardingRequest. In env-validation.ts add `NEXT_PUBLIC_SUPPORT_EMAIL`, `NEXT_PUBLIC_SUPPORT_URL`, `NEXT_PUBLIC_ONBOARDING_REVIEW_SLA_DAYS` to the EnvVars interface and the requiredEnvVars list (non-secret build-time values — GLOBAL_RULE_6, no hardcoded literals). Add the three keys with sensible example values to .env.local.example. These are read client-side by the vendor page; no hardcoded support mailto or SLA number anywhere.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/types/api.ts` OnboardingDto contains `reviewPending` and `rejectionReason`.
    - `frontend/lib/env-validation.ts` requiredEnvVars contains all three `NEXT_PUBLIC_SUPPORT_EMAIL`/`NEXT_PUBLIC_SUPPORT_URL`/`NEXT_PUBLIC_ONBOARDING_REVIEW_SLA_DAYS`.
    - `.env.local.example` contains the three keys.
    - `npm run build` (tsc typecheck) exits 0.
  </acceptance_criteria>
  <done>Types are in exact sync with the backend DTOs; the support channel + SLA are config-injected via NEXT_PUBLIC_* and validated at startup; the build typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Vendor page — remediation blocks, honest in-review + backoff, withdraw dialog, inline company edit, rejection + support (ONBD-01/02/03/04/05)</name>
  <files>frontend/app/dashboard/onboarding/page.tsx, frontend/app/dashboard/onboarding/__tests__/page.test.tsx</files>
  <read_first>
    - frontend/app/dashboard/onboarding/page.tsx (the whole page — STATE_META/GATE_META ~:54-94, GATE_FALLBACK ~:103, POLL_STATES + interval ~:105-107/:183-189, go-live Dialog ~:516-533, handleGoLive ~:262-288, create-form Input/Label ~:377-391, handleResubmit ~:245-260, failed-gate list ~:449-467, "under a minute" ~:68-69, "Contact support" ~:74-75)
    - frontend/app/dashboard/onboarding/__tests__/page.test.tsx (~:1-66 — jest.mock("@/lib/api-client") + routeGet router + onboarding(status, overrides) fixture idiom)
    - frontend/types/api.ts (the widened OnboardingDto from Task 1)
    - frontend/lib/env-validation.ts (the new NEXT_PUBLIC_* keys)
  </read_first>
  <action>
    Add a `REMEDIATION: Partial<Record<`${GateType}:${GateStatus}`, { why: string; what: string; href: string }>>` static map beside STATE_META/GATE_META (clone the static-map idiom). Deep links per D-08: BUSINESS_VERIFIED:FAILED -> the inline company-number edit anchor; ALLERGEN_DATA_COMPLETE:FAILED -> "/dashboard/products" (the reason already names the offending SKUs — render reason + "fix these products" CTA, no filter param needed); FOOD_HYGIENE_RATING:MANUAL_REVIEW -> "/dashboard/shops". EXTEND the existing failed-gate list (which already reads gate.reason) to also render the matched remediation block (why -> what -> button); unknown (gateType,status) falls back to the existing neutral render (never crash). In-review (ONBD-03): when onboarding.reviewPending is true, replace the "under a minute" VERIFYING copy with a config-driven "a reviewer checks these within `NEXT_PUBLIC_ONBOARDING_REVIEW_SLA_DAYS` business days" message, and back off polling — drop VERIFYING from the fast 4s interval (or lengthen it) once reviewPending. Withdraw (ONBD-01): clone the go-live <Dialog> as a `variant="destructive"` confirm shown for any pre-live status; on confirm apiClient.post("/api/v1/onboarding/withdraw", {}) -> setOnboarding(res.data), useToast on error; render the terminal WITHDRAWN copy (restart begins a fresh application). Inline company edit (ONBD-02): reuse the create-form Input/Label as an edit control; on save apiClient.post("/api/v1/onboarding/company-number", {companyNumber}) then let the vendor trigger the existing handleResubmit. Rejection + support (ONBD-05): on REJECTED and SUSPENDED render `onboarding.rejectionReason` plus a support link built from NEXT_PUBLIC_SUPPORT_EMAIL/NEXT_PUBLIC_SUPPORT_URL (replace the bare "Contact support for details"). PRESERVE the create form, submit/resubmit/go-live CTAs, gate breakdown, and milestone timeline (Incremental Betterment). Add Jest blocks (using the routeGet + onboarding fixture idiom): in-review copy + poll backoff; withdraw dialog + terminal copy; inline company-number edit calls the update endpoint; per-(gateType,status) remediation block renders why/what/deep-link; REJECTED/SUSPENDED render reason + config support channel.
  </action>
  <verify>
    <automated>cd frontend && npm test -- onboarding/__tests__/page.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` contains a `REMEDIATION` map keyed by `${GateType}:${GateStatus}` and references `/dashboard/products` and `/dashboard/shops` deep links.
    - `page.tsx` references `onboarding.reviewPending`, `NEXT_PUBLIC_ONBOARDING_REVIEW_SLA_DAYS`, `/api/v1/onboarding/withdraw`, `/api/v1/onboarding/company-number`, `onboarding.rejectionReason`, and `NEXT_PUBLIC_SUPPORT_EMAIL`/`NEXT_PUBLIC_SUPPORT_URL`.
    - grep confirms the literal "This usually takes under a minute" no longer renders for reviewPending, and no hardcoded "mailto:" literal exists in page.tsx (support link derives from the env keys).
    - The existing create-form / submit / resubmit / go-live / timeline elements are still present in page.tsx (no regression by omission).
    - `npm test -- onboarding/__tests__/page.test.tsx` and `npm run build` both exit 0.
  </acceptance_criteria>
  <done>The vendor page shows honest in-review copy with backoff, actionable per-gate remediation blocks, a withdraw confirm dialog, inline company-number editing, and rejection reason + a configured support channel — with every existing good preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Approvals page — review-pending list + gate-resolve dialog (ONBD-03 admin surface)</name>
  <files>frontend/app/dashboard/onboarding/approvals/page.tsx, frontend/app/dashboard/onboarding/approvals/__tests__/page.test.tsx</files>
  <read_first>
    - frontend/app/dashboard/onboarding/approvals/page.tsx (loadQueue ~:98-116, reject dialog + required reason ~:324-376, handleReject ~:157-181, gateSummary/GATE_META/GATE_STATUS_META, forbidden-403 pattern)
    - frontend/app/dashboard/onboarding/approvals/__tests__/page.test.tsx (the existing Jest idiom for this page)
    - frontend/types/api.ts (AdminOnboardingDto + ResolveGateRequest from Task 1)
  </read_first>
  <action>
    Add a review-pending fetch: apiClient.get("/api/v1/onboarding/admin/reviews") populating a second list alongside the existing /pending queue (KEEP the approve/reject queue intact — the review list is an addition, Incremental Betterment). For each review-pending application render its gate breakdown (reuse gateSummary/GATE_META/GATE_STATUS_META) with a gate-resolve control: a dialog cloning the reject-reason dialog shape with a decision select (PASS/WAIVE/FAIL) + reason textarea; on confirm apiClient.post(`/api/v1/onboarding/admin/${id}/gates/${gateType}/resolve`, { decision, reason }) then refresh the queues and toast. Reuse the forbidden-403 pattern. Add Jest blocks: the review list renders VERIFYING+MANUAL_REVIEW entries; the gate-resolve dialog posts {decision, reason} to the resolve endpoint; the existing approve/reject flow is untouched.
  </action>
  <verify>
    <automated>cd frontend && npm test -- approvals/__tests__/page.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `approvals/page.tsx` references `/api/v1/onboarding/admin/reviews` and a `gates/${...}/resolve` POST with a `decision` + `reason` payload.
    - The existing `/onboarding/admin/pending` fetch + approve/reject handlers are still present (no regression).
    - Jest blocks for the review list + gate-resolve dialog pass; `npm test -- approvals/__tests__/page.test.tsx` and `npm run build` exit 0.
  </acceptance_criteria>
  <done>The admin approvals surface lists review-pending applications and can resolve a stuck gate (decision + reason) via the backend endpoint; the approve/reject queue is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend DTO -> rendered vendor UI | rejectionReason + gate reason strings render in the vendor page |
| Build-time config -> client bundle | Support channel + SLA are injected as NEXT_PUBLIC_* (non-secret) |
| Admin UI -> gate-resolve endpoint | decision + reason submitted from the approvals page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-01 | Info Disclosure | Rendering gate/rejection reason | mitigate | Reasons are already fixed human-readable strings server-side (ASVS V7, enforced in 21-02/21-03); React escapes interpolated text by default (no dangerouslySetInnerHTML), so no raw provider text or markup injection reaches the DOM. |
| T-21-04-02 | Tampering / integrity | Hardcoded support/SLA literals | mitigate | Support channel + SLA are config-injected via NEXT_PUBLIC_* and validated in env-validation (GLOBAL_RULE_6); grep asserts no `mailto:`/SLA literal in page.tsx. |
| T-21-04-03 | Spoofing / EoP | Admin gate-resolve action from the UI | accept | The UI only submits; authZ is enforced server-side (@PreAuthorize + RLS, 21-03). No client-side trust; a forbidden-403 renders the existing forbidden state. |
| T-21-04-SC | Tampering (supply chain) | package installs | accept | No new npm packages this phase (RESEARCH Package Legitimacy Audit: N/A); only NEXT_PUBLIC_* env keys added. |
</threat_model>

<verification>
- `cd frontend && npm test -- onboarding && npm run build` green (Jest + tsc typecheck gate — Pitfall 6).
- grep confirms preserved goods (create form, submit/resubmit/go-live, timeline, approve/reject queue) still present; the "under a minute" forever-spinner and bare "Contact support" are replaced.
- Support channel + SLA are read from NEXT_PUBLIC_* (no hardcoded literals).
- No new npm dependency added to package.json.
</verification>

<success_criteria>
- Honest in-review state with config SLA + polling backoff; actionable per-gate remediation blocks; withdraw confirm dialog; inline company-number edit; rejection reason + configured support channel.
- Admin approvals surface lists review-pending apps + gate-resolve control; approve/reject queue preserved.
- `npm run build` (tsc) green; every existing good preserved (Incremental Betterment).
</success_criteria>

<output>
Create `.planning/phases/21-onboarding-blocker-ux/21-04-SUMMARY.md` when done.
</output>
