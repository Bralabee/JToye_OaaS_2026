---
phase: 21-onboarding-blocker-ux
plan: 05
type: execute
wave: 4
depends_on: ["21-01", "21-02", "21-03", "21-04"]
files_modified:
  - frontend/e2e/onboarding-blocked-flow.spec.ts
  - docs/metrics.json
autonomous: false
requirements: [ONBD-05]

must_haves:
  truths:
    - "A blocked onboarding journey passes end-to-end in Playwright: bad company number -> fix inline -> resubmit -> live"
    - "The FHRS manual-review path shows the honest 'in review' state (not the forever spinner) on the rebuilt stack, confirmed visually"
    - "docs/metrics.json is reconciled to the new test counts and the docs-freshness CI gate passes"
    - "The phase ships zero Flyway migrations"
  artifacts:
    - path: "frontend/e2e/onboarding-blocked-flow.spec.ts"
      provides: "the ONBD-05 journey-matrix Playwright spec"
      contains: "onboarding"
    - path: "docs/metrics.json"
      provides: "reconciled logical-invocation counts"
      contains: "total_logical_invocations"
  key_links:
    - from: "onboarding-blocked-flow.spec.ts"
      to: "the rebuilt compose stack (port 3100)"
      via: "vendorLogin + domcontentloaded (NOT networkidle)"
      pattern: "onboarding-blocked-flow"
    - from: "scripts/docs-freshness.sh --write"
      to: "docs/metrics.json"
      via: "recount + commit"
      pattern: "docs-freshness"
---

<objective>
Prove the flagship blocked-onboarding journey end-to-end, confirm the FHRS manual-review honesty visually on the rebuilt stack, and reconcile the test metrics so the docs-freshness CI gate stays green — closing the phase.

Purpose: the ONBD-05 journey-matrix requirement (drive one blocked onboarding end-to-end in Playwright) plus the mandatory metrics reconciliation. This plan depends on the entire backend + frontend being in place. The Playwright journey covers the company-number path automatically; a single human-verify checkpoint covers the FHRS fuzzy-match manual-review path that is hard to reproduce deterministically in Playwright (per 21-VALIDATION.md Manual-Only Verifications).

Output: `frontend/e2e/onboarding-blocked-flow.spec.ts`, a reconciled `docs/metrics.json`, and phase closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-onboarding-blocker-ux/21-CONTEXT.md
@.planning/phases/21-onboarding-blocker-ux/21-RESEARCH.md
@.planning/phases/21-onboarding-blocker-ux/21-PATTERNS.md
@.planning/phases/21-onboarding-blocker-ux/21-VALIDATION.md

<interfaces>
<!-- Read the analog spec + tooling. -->
- Playwright analog: frontend/e2e/vendor-refund-flow.spec.ts — port-3100 base URL, E2E_VENDOR_* env creds, vendorLogin helper, and the NOT-networkidle rule (~:41-45: waitForLoadState("domcontentloaded") + assert on concrete controls). An open polling interval prevents networkidle from ever settling (learnings_fleet_supervision + RESEARCH Pitfall 5).
- Metrics gate: scripts/docs-freshness.sh --write recomputes docs/metrics.json (currently total_logical_invocations = 1257); the .github/workflows/docs-freshness.yml CI gate fails on drift.
- Rebuild-all rule (CLAUDE.md): rebuild ALL containers before E2E; RULE 0 — compose XOR the minikube cluster (compose is canonical; the cluster must be stopped).
- Backend/frontend surfaces exercised: POST /onboarding/company-number, POST /onboarding/resubmit, POST /onboarding/go-live; vendor page reviewPending + remediation + inline edit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright blocked-onboarding journey spec (bad company number -> fix inline -> resubmit -> live)</name>
  <files>frontend/e2e/onboarding-blocked-flow.spec.ts</files>
  <read_first>
    - frontend/e2e/vendor-refund-flow.spec.ts (the port-3100 base URL, E2E_VENDOR_* creds, vendorLogin helper, and the domcontentloaded / NOT-networkidle assertion idiom)
    - frontend/playwright.config.ts (base URL + projects)
    - frontend/app/dashboard/onboarding/page.tsx (the vendor controls the spec drives — company-number input, remediation block, withdraw dialog, resubmit CTA — as built in 21-04)
  </read_first>
  <action>
    Create frontend/e2e/onboarding-blocked-flow.spec.ts cloning the vendor-refund-flow harness (port 3100, E2E_VENDOR_* creds, vendorLogin). Drive the journey: log in as the vendor, reach the onboarding page in a blocked/ACTION_REQUIRED state with a FAILED BUSINESS_VERIFIED gate (bad company number) -> assert the remediation block renders (why + what + the inline-edit control) -> use the inline company-number edit to submit a valid number -> trigger resubmit -> assert the application advances (VERIFYING then, once gates pass, the go-live/LIVE path). Use waitForLoadState("domcontentloaded") and assert on concrete visible controls/copy — NEVER wait on networkidle (the poll interval prevents settle). Rebuild ALL containers before running (CLAUDE.md rebuild-all + RULE 0: cluster stopped, compose canonical). If seeding a deterministic bad-company-number start state needs a fixture, add it in the spec's setup consistent with the analog.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test onboarding-blocked-flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/e2e/onboarding-blocked-flow.spec.ts` exists and contains at least one `test(` block.
    - The spec uses `waitForLoadState("domcontentloaded")` (or explicit control assertions) and contains NO `networkidle` wait.
    - `npx playwright test onboarding-blocked-flow.spec.ts` exits 0 against the rebuilt stack.
  </acceptance_criteria>
  <done>The bad-company-number -> fix inline -> resubmit -> live journey passes end-to-end in Playwright without relying on networkidle.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the FHRS manual-review honest "in review" path on the rebuilt stack</name>
  <what-built>The vendor onboarding page now derives reviewPending and renders an honest "a reviewer checks these within N business days" state with polling backoff (21-03/21-04); the admin approvals surface lists review-pending applications with a gate-resolve control; the FHRS gate returns MANUAL_REVIEW for a fuzzy shop name/address mismatch. Task 1 automates the company-number path; this checkpoint covers the FHRS-manual-review path (hard to reproduce deterministically in Playwright — per 21-VALIDATION.md).</what-built>
  <how-to-verify>
    1. Rebuild ALL containers (CLAUDE.md rebuild-all rule) and bring up the compose stack (RULE 0: ensure the minikube cluster is stopped; compose is canonical). Access the app at http://localhost:3100.
    2. As a vendor, create/submit an onboarding with a shop name/address that will NOT cleanly match FHRS (a fuzzy/unmatchable establishment), so the FHRS gate returns MANUAL_REVIEW and the app parks in VERIFYING.
    3. Confirm the vendor onboarding page shows the honest "In review — a reviewer checks these within N business days" copy (NOT the old "This usually takes under a minute" forever-spinner), and that polling has backed off.
    4. As the tenant admin, open the approvals surface and confirm the application appears in the review-pending queue with a gate-resolve control.
    5. Resolve the MANUAL_REVIEW gate (PASS or WAIVE) and confirm the application advances out of VERIFYING (the state machine progresses; no dead end for either party).
  </how-to-verify>
  <resume-signal>Type "approved" if the honest in-review copy, the admin review queue entry, and the gate-resolve advance all work; otherwise describe what rendered incorrectly.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Reconcile docs/metrics.json + phase closure</name>
  <files>docs/metrics.json</files>
  <read_first>
    - scripts/docs-freshness.sh (the --write recount behavior)
    - docs/metrics.json (current total_logical_invocations = 1257)
    - .github/workflows/docs-freshness.yml (the CI gate this must satisfy)
  </read_first>
  <action>
    Run `scripts/docs-freshness.sh --write` to recompute the logical-invocation counts across the new Java @Test methods (withdraw, update, gate-resolve, review-queue, stall-outbox, DTO derivation, GateChainRunner stall), the new Jest blocks (vendor + approvals pages), and the new Playwright spec; commit the updated docs/metrics.json. Then run the gate in check mode to confirm no drift. Update the ROADMAP Phase 21 plan checkboxes and write the phase-closure SUMMARY.
  </action>
  <verify>
    <automated>bash scripts/docs-freshness.sh --write && bash scripts/docs-freshness.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/docs-freshness.sh` (check mode) exits 0 (no drift) after the reconcile.
    - `docs/metrics.json` `total_logical_invocations` is greater than 1257 (new tests counted).
    - No file exists under `core-java/src/main/resources/db/migration/` newer than V51 (zero-migration boundary held for the whole phase).
  </acceptance_criteria>
  <done>docs/metrics.json is reconciled and the docs-freshness gate is green; the phase closes with zero migrations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test creds -> live stack | E2E_VENDOR_* credentials authenticate the Playwright run against port 3100 |
| Test-count metadata -> CI gate | docs/metrics.json is the trusted source the docs-freshness gate enforces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05-01 | Info Disclosure | E2E credentials in the spec | mitigate | Credentials come from `E2E_VENDOR_*` env vars (analog vendor-refund-flow.spec.ts), never hardcoded in the committed spec. |
| T-21-05-02 | Tampering | Metrics drift masking untested code | mitigate | `docs-freshness.sh` recomputes counts from source and the CI gate fails on drift, so the reconciled `docs/metrics.json` cannot silently under-count new tests. |
| T-21-05-SC | Tampering (supply chain) | package installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A); Playwright already present. |
</threat_model>

<verification>
- `cd frontend && npx playwright test onboarding-blocked-flow.spec.ts` green on the rebuilt stack.
- Human-verify checkpoint approved for the FHRS manual-review honest-copy + admin review-queue + gate-resolve advance path.
- `bash scripts/docs-freshness.sh` (check mode) exits 0; `docs/metrics.json` total > 1257.
- No new Flyway migration anywhere in the phase.
</verification>

<success_criteria>
- The blocked-onboarding journey passes end-to-end in Playwright.
- The FHRS manual-review honesty path is human-verified on the rebuilt stack.
- docs/metrics.json reconciled; docs-freshness CI gate green.
- Phase 21 ships with zero Flyway migrations.
</success_criteria>

<output>
Create `.planning/phases/21-onboarding-blocker-ux/21-05-SUMMARY.md` when done.
</output>
