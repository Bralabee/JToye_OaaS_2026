---
phase: quick-260712-t6b
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [ISSUE-98-P2-7-OBSERVABILITY-DO-NOW]
files_modified:
  - edge-go/cmd/edge/main.go
  - edge-go/cmd/edge/metrics.go
  - edge-go/cmd/edge/metrics_test.go
  - edge-go/go.mod
  - edge-go/go.sum
  - infra/monitoring/prometheus/prometheus.yml
  - infra/monitoring/prometheus/alerts.yml
  - core-java/src/main/java/uk/jtoye/core/security/JwtTenantFilter.java
  - core-java/src/main/java/uk/jtoye/core/payment/PaymentService.java
  - core-java/src/test/java/uk/jtoye/core/security/JwtTenantFilterTest.java
  - core-java/src/test/java/uk/jtoye/core/security/JwtTenantFilterMetricsTest.java
  - core-java/src/test/java/uk/jtoye/core/payment/PaymentServiceTest.java
  - core-java/src/main/resources/application-prod.yml
  - core-java/src/main/java/uk/jtoye/core/security/SecurityConfig.java
  - core-java/src/test/java/uk/jtoye/core/security/ManagementPortMetricsIntegrationTest.java
  - k8s/base/core-java-deployment.yaml
  - .github/workflows/ci-cd.yaml
  - scripts/smoke-test.sh
  - docs/metrics.json
  - docs/CHANGELOG.md
  - CLAUDE.md

user_setup: []

must_haves:
  truths:
    - "The prod JSON console log pattern emits each log message EXACTLY ONCE; a logged line containing quotes and newlines renders as a single valid JSON object (no duplicated message field content)."
    - "Every ACTIVE alert in alerts.yml references a metric that is actually emitted somewhere: TenantIsolationFailure resolves to a real Micrometer counter (tenant_context_missing_total) and a payment-failure alert resolves to a real counter (jtoye_payment_failed_total). The node-exporter DiskSpace rules (cluster-blocked, out of scope) are disabled/commented with a PENDING-node-exporter note so no live rule references a never-emitted metric."
    - "The edge-go Gin gateway serves GET /metrics returning Prometheus exposition text including HTTP request count + duration series labelled by method/route/status (route = low-cardinality path template, never a raw ID-bearing path), and the edge-go scrape job in prometheus.yml is enabled (uncommented)."
    - "In prod, /actuator/prometheus is served ONLY on a separate internal management port (default 9091, env-overridable); the public app port (9090) serves NO actuator/metrics endpoint; the app-port JWT posture is unchanged (all business paths still require a Bearer token)."
    - "The full core-java test suite still COMPILES and passes: JwtTenantFilterTest.java (line 41 `new JwtTenantFilter()`) is updated for the new required-arg constructor — no compileTestJava breakage."
    - "The production deploy pipeline survives the management-port move: the ci-cd.yaml in-cluster health check curls port 9091, and scripts/smoke-test.sh no longer requires public-URL actuator 200s in prod-shaped environments (EXPECT_PUBLIC_ACTUATOR gate, default false = prod-safe, mirroring the EXPECT_SWAGGER precedent) — a healthy prod release is never auto-rolled-back by smoke."
    - "Alertmanager's SMTP receiver is env-injectable (SMTP smarthost/from/to/require-tls) with the current Mailhog values as dev defaults — verified already present in docker-compose.monitoring.yml + entrypoint.sh; no regression introduced."
    - "docs/metrics.json and the CLAUDE.md narrative counts are resynced (Java @Test methods + Go Test funcs grow) via scripts/docs-freshness.sh --write, and the docs-freshness check passes afterwards."
  artifacts:
    - path: "edge-go/cmd/edge/metrics.go"
      provides: "Gin Prometheus middleware + /metrics handler (prometheus/client_golang): http_requests_total CounterVec + http_request_duration_seconds HistogramVec by method/route/status; promhttp handler."
      min_lines: 25
      contains: "promhttp"
    - path: "edge-go/cmd/edge/metrics_test.go"
      provides: "Go test asserting /metrics returns 200 and exposition text containing http_requests_total after a request flows through the middleware."
      contains: "http_requests_total"
    - path: "infra/monitoring/prometheus/prometheus.yml"
      provides: "edge-go scrape job enabled (was commented out); core-java scrape target points at the prod management port."
      contains: "job_name: 'edge-go'"
    - path: "infra/monitoring/prometheus/alerts.yml"
      provides: "Reconciled ruleset: every active alert references an emitted metric; new payment-failure alert; DiskSpace rules disabled pending node-exporter."
      contains: "jtoye_payment_failed_total"
    - path: "core-java/src/main/java/uk/jtoye/core/security/JwtTenantFilter.java"
      provides: "tenant.context.missing counter incremented when an authenticated JWT principal carries no resolvable tenant claim (the isolation-failure signal the alert references)."
      contains: "tenant.context.missing"
    - path: "core-java/src/test/java/uk/jtoye/core/security/JwtTenantFilterTest.java"
      provides: "Existing filter test updated for the new constructor (SimpleMeterRegistry/no-op ObjectProvider) so the test source set still compiles."
      contains: "new JwtTenantFilter("
    - path: "core-java/src/main/java/uk/jtoye/core/payment/PaymentService.java"
      provides: "jtoye.payment.failed counter incremented in handlePaymentIntentFailed (the natural payment-failure emission point)."
      contains: "jtoye.payment.failed"
    - path: "core-java/src/main/resources/application-prod.yml"
      provides: "Fixed single-emit JSON log pattern; separate management.server.port (default 9091) with prometheus exposed on it only."
      contains: "management.server.port"
    - path: "core-java/src/main/java/uk/jtoye/core/security/SecurityConfig.java"
      provides: "Actuator scrape/probe endpoints permitted on the internal management port without weakening the app-port chain."
      contains: "actuator/prometheus"
    - path: "k8s/base/core-java-deployment.yaml"
      provides: "Probes + scrape annotation + containerPort aligned to the management port (9091) — consistent for BOTH overlays since base sets SPRING_PROFILES_ACTIVE=prod."
      contains: "9091"
    - path: ".github/workflows/ci-cd.yaml"
      provides: "Production in-cluster health check curls the management port (9091) instead of the app port; staging gains the mirrored in-cluster check so positive actuator verification is preserved."
      contains: "9091"
    - path: "scripts/smoke-test.sh"
      provides: "Management-port-aware actuator checks: EXPECT_PUBLIC_ACTUATOR gate (default false = assert actuator NOT publicly exposed), preserving the PR #201 structure (T4/T5 liveness/readiness, EXPECT_SWAGGER conditional)."
      contains: "EXPECT_PUBLIC_ACTUATOR"
    - path: "docs/metrics.json"
      provides: "Resynced test counts (Java @Test + Go Test funcs grow) — the single source of truth the docs-freshness CI gate enforces."
  key_links:
    - from: "infra/monitoring/prometheus/prometheus.yml (edge-go job)"
      to: "edge-go/cmd/edge/metrics.go (/metrics handler)"
      via: "scrape target jtoye-edge-go:8080 path /metrics"
      pattern: "metrics_path:\\s*'/metrics'"
    - from: "infra/monitoring/prometheus/alerts.yml (TenantIsolationFailure)"
      to: "core-java JwtTenantFilter tenant.context.missing counter"
      via: "PromQL rate(tenant_context_missing_total[5m])"
      pattern: "tenant_context_missing_total"
    - from: "infra/monitoring/prometheus/alerts.yml (payment failure alert)"
      to: "core-java PaymentService jtoye.payment.failed counter"
      via: "PromQL rate(jtoye_payment_failed_total[5m])"
      pattern: "jtoye_payment_failed_total"
    - from: "k8s/base/core-java-deployment.yaml (scrape annotation + probes)"
      to: "application-prod.yml management.server.port"
      via: "prometheus.io/port + probe port = 9091"
      pattern: "9091"
    - from: ".github/workflows/ci-cd.yaml (deploy health checks) + scripts/smoke-test.sh"
      to: "application-prod.yml management.server.port"
      via: "kubectl-exec curl :9091/actuator/health; public smoke asserts actuator NOT on app port"
      pattern: "9091|EXPECT_PUBLIC_ACTUATOR"
---

<objective>
Close the five local-repo-provable observability gaps in GitHub issue #98 ([P2-7] "Observability demo-grade"):

1. Fix the prod JSON console log pattern that emits every message twice (malformed JSON).
2. Make every active Prometheus alert reference a metric that is actually emitted — emit real Micrometer counters at the natural tenant-isolation-failure and payment-failure code paths; disable the cluster-blocked node-exporter DiskSpace rules.
3. Add a Prometheus /metrics endpoint to the edge-go Gin gateway (it carries scrape annotations but exposes nothing) and re-enable its scrape job.
4. Expose /actuator/prometheus in prod on a separate internal management port (not the public app port), keeping the app-port security posture unchanged — INCLUDING the deploy-pipeline couplings (ci-cd.yaml in-cluster health check, scripts/smoke-test.sh public actuator checks) that the port move would otherwise break.
5. (Verification-only) Confirm the Alertmanager SMTP receiver is env-injectable with Mailhog dev defaults — already implemented; guard against regression.

Purpose: Turn "demo-grade" observability wiring into honest, provable coverage — no phantom alerts, no dead scrape targets, no publicly-exposed prod metrics, no deploy pipeline that fails against a healthy release.
Output: A working edge /metrics endpoint, two real Micrometer counters with tests, a reconciled alert ruleset, a prod-safe management-port metrics path with aligned CI/CD + smoke checks, and resynced docs metrics.

OUT OF SCOPE (issue #98 stays open for these — cluster-blocked): Loki/log aggregation deploy, trace collector + cross-service trace propagation, node-exporter/DiskSpace alert wiring, and live prod-shaped scrape proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Extracted contracts the executor uses directly — no codebase spelunking needed. -->

Established null-safe Micrometer counter pattern (core-java/.../security/RateLimitInterceptor.java):
- Constructor takes `ObjectProvider<MeterRegistry> meterRegistryProvider`.
- `MeterRegistry reg = meterRegistryProvider.getIfAvailable();`
- `Counter.builder("jtoye.ratelimit.fail_open").description("...").register(reg)` when reg != null, else null; guard every `.increment()` with `!= null`.
- Micrometer→Prometheus name mapping: dots become underscores and counters gain a `_total` suffix. So `tenant.context.missing` → `tenant_context_missing_total`; `jtoye.payment.failed` → `jtoye_payment_failed_total`.

TenantContext (core-java/.../security/TenantContext.java):
- `static Optional<UUID> get()`, `static void set(UUID)`, `static void clear()`.

JwtTenantFilter (core-java/.../security/JwtTenantFilter.java):
- `@Component @Order(200)` OncePerRequestFilter, currently NO constructor.
- In `doFilterInternal`, `auth.getPrincipal() instanceof Jwt jwt` then `extractTenant(jwt)` returns `Optional<UUID>` (empty when the token carries no tenant_id/tenantId/tid claim). Empty-for-an-authenticated-principal is the isolation-failure signal.
- EXISTING TEST COUPLING: `core-java/src/test/java/uk/jtoye/core/security/JwtTenantFilterTest.java:41` does `filter = new JwtTenantFilter();` — the ONLY no-arg call site repo-wide. Adding a required-arg constructor WITHOUT updating this file fails compileTestJava for the entire test source set.

PaymentService (core-java/.../payment/PaymentService.java):
- Constructor-injected deps (all `private final`): StripeProperties, OrderRepository, OrderEventPublisher, PaymentEventPublisher, FinancialTransactionService, JdbcTemplate, RefundService, StripeConnectService.
- `public void handleWebhookEvent(String payload, String sigHeader)` → switch on `event.getType()`; `case "payment_intent.payment_failed" -> handlePaymentIntentFailed(event)` (private, ~line 293). This is the emission point.
- Existing tests exercise the failed-payment path: PaymentServiceTest.java + StripeWebhookIdempotencyIntegrationTest.java (templates for the counter test). Update every direct `new PaymentService(...)` call site for the new constructor param.

edge-go main.go (edge-go/cmd/edge/main.go):
- `r := gin.Default()`; process-wide `r.Use(rateLimiter(ctx, rps, burst))`; public routes `r.GET("/health", h.Health)` / `r.GET("/ready", h.Ready)` registered before the JWT-protected group.
- `github.com/prometheus/client_golang v1.23.2` is present in the local module cache (~/go/pkg/mod). Use `prometheus/client_golang/prometheus` + `.../promhttp` (the official Prometheus Go client). Go test pattern lives in edge-go/cmd/edge/handlers_test.go (httptest + gin.CreateTestContext / full engine).

application-prod.yml current state (core-java/src/main/resources/application-prod.yml):
- `logging.pattern.console` (~line 85) contains TWO concatenated `%replace(%msg){...}` conversions → the message is emitted twice.
- `management.endpoints.web.exposure.include: health,info` (~line 100); no `management.server.port` set; `management.metrics.export.prometheus.enabled: true`.

SecurityConfig current state (core-java/.../security/SecurityConfig.java):
- `boolean isProd = Arrays.asList(env.getActiveProfiles()).contains("prod");`
- `/actuator/health`, `/actuator/health/**`, `/actuator/info` permitAll unconditionally; `/actuator/prometheus` permitAll ONLY when `!isProd`; then `anyRequest().authenticated()`.

k8s/base/core-java-deployment.yaml current state:
- `SPRING_PROFILES_ACTIVE` is hardcoded to `"prod"` IN THE BASE (~line 52-53) — BOTH the staging and production overlays run the prod profile, so a base-level probe/port change is consistent for both (no per-overlay patch needed).
- Pod annotations (~lines 25-28): `prometheus.io/scrape: "true"`, `prometheus.io/port: "9090"`, `prometheus.io/path: "/actuator/prometheus"` (points at the app port, which prod does NOT serve — the mismatch item 4 fixes).
- `containerPort: 9090`; startup/liveness probes `path: /actuator/health/liveness port: 9090`; readiness `path: /actuator/health/readiness port: 9090`.

Deploy-pipeline couplings to the management-port move (MUST be fixed in the same task):
- `.github/workflows/ci-cd.yaml:596` (deploy-production): `kubectl exec -n jtoye-production deployment/core-java -- curl -f http://localhost:9090/actuator/health || exit 1` — breaks once actuator leaves 9090.
- `.github/workflows/ci-cd.yaml:522-523` (deploy-staging): `EXPECT_SWAGGER=true ./scripts/smoke-test.sh https://staging-api.jtoye.co.uk`; `:605-606` (deploy-production): `./scripts/smoke-test.sh https://api.jtoye.co.uk`.
- `scripts/smoke-test.sh` (174 lines, reworked in PR #201 — PRESERVE its structure): `API_URL="${1:-http://localhost:9090}"`; `test_endpoint()` helper; Test 1 = `$API_URL/health` (app-level controller endpoint, stays on app port — NOT actuator); Tests 2-5 = `$API_URL/actuator/health`, `/actuator/info`, `/actuator/health/liveness`, `/actuator/health/readiness` (all break on the public URL once actuator moves); `EXPECT_SWAGGER="${EXPECT_SWAGGER:-false}"` conditional (default = prod posture); Tests 6-8 = protected-401/invalid/CORS; exits 1 on any failure.

Alertmanager env-injection (ALREADY DONE — item 5 is verification-only):
- infra/monitoring/docker-compose.monitoring.yml (~lines 73-76) injects ALERTMANAGER_SMTP_SMARTHOST/FROM/TO/REQUIRE_TLS with Mailhog defaults (`mailhog:1025`, `alerts@jtoye.local`, `ops@jtoye.local`, `false`).
- infra/monitoring/alertmanager/entrypoint.sh applies the same defaults and sed-renders alertmanager.yml.tmpl (__SMTP_*__ placeholders). No change required.
</interfaces>

<documentation_lookup>
For the Spring Boot separate-management-port + Spring Security interaction (Task 3), prefer Context7 MCP (resolve-library-id "Spring Boot", then query "management.server.port separate management port Spring Security actuator prometheus"). This is the one genuinely nuanced area — confirm behaviour rather than guessing, and let the test prove the outcome.
</documentation_lookup>
</context>

<tasks>

<task type="auto">
  <name>Task 1: edge-go Prometheus /metrics endpoint + scrape job</name>
  <files>edge-go/cmd/edge/metrics.go, edge-go/cmd/edge/metrics_test.go, edge-go/cmd/edge/main.go, edge-go/go.mod, edge-go/go.sum, infra/monitoring/prometheus/prometheus.yml</files>
  <action>
Add Prometheus instrumentation to the Gin edge gateway (issue #98 item 3). Use the official `github.com/prometheus/client_golang` (already in the local module cache at v1.23.2): run `go get github.com/prometheus/client_golang/prometheus github.com/prometheus/client_golang/prometheus/promhttp` then `go mod tidy` from edge-go/.

Create `edge-go/cmd/edge/metrics.go`:
- A `CounterVec` named `http_requests_total` (help text) labelled `method`, `route`, `status`.
- A `HistogramVec` named `http_request_duration_seconds` (default or explicit buckets) labelled `method`, `route`, `status`.
- Register both on the default registry via `promauto` (or explicit `MustRegister`).
- A `prometheusMiddleware() gin.HandlerFunc` that times `c.Next()` and records both metrics. CRITICAL for multi-tenancy + cardinality: use `c.FullPath()` (the matched route TEMPLATE, e.g. `/api/v1/sync/batch`) for the `route` label, NOT `c.Request.URL.Path` (raw path can carry IDs → unbounded cardinality / potential tenant leakage). When `c.FullPath()` is empty (unmatched route), use a constant like `"unmatched"`. `status` = `strconv.Itoa(c.Writer.Status())`.
- A `metricsHandler() gin.HandlerFunc` = `gin.WrapH(promhttp.Handler())`.

Wire into `edge-go/cmd/edge/main.go`:
- Register the metrics middleware early (via `r.Use(prometheusMiddleware())`) so all routes are counted.
- Register `r.GET("/metrics", metricsHandler())` alongside the public `/health` / `/ready` routes (keep it public — no JWT). The 15s scrape interval is well under the DoS rate limiter, so no exclusion is needed.

Create `edge-go/cmd/edge/metrics_test.go` (Go test, grows the go_test_funcs count): build a gin engine with the middleware + a dummy route + the `/metrics` route, fire a request at the dummy route through the engine, then GET `/metrics` and assert HTTP 200 and that the body contains `http_requests_total`. Follow the httptest/gin patterns in handlers_test.go.

Re-enable the edge-go scrape job in `infra/monitoring/prometheus/prometheus.yml`: uncomment the `job_name: 'edge-go'` block (currently DISABLED, ~lines 45-59) so Prometheus scrapes `jtoye-edge-go:8080` at `/metrics`. Update the leading comment to note the endpoint now exists (drop the "no /metrics endpoint yet" rationale).
  </action>
  <verify>
    <automated>cd edge-go && gofmt -l cmd/edge/metrics.go cmd/edge/metrics_test.go | (! grep .) && go vet ./... && go test ./cmd/edge/ -run 'Metrics' -v</automated>
    <automated>grep -q "job_name: 'edge-go'" infra/monitoring/prometheus/prometheus.yml && ! grep -qE "^\s*#\s*- job_name: 'edge-go'" infra/monitoring/prometheus/prometheus.yml</automated>
  </verify>
  <done>edge-go builds, vets, and its metrics test passes; GET /metrics returns 200 with http_requests_total; route labels use the matched template (no raw paths); the edge-go scrape job is active (uncommented) in prometheus.yml.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Real Micrometer counters + alert reconciliation + prod JSON log single-emit fix</name>
  <files>core-java/src/main/java/uk/jtoye/core/security/JwtTenantFilter.java, core-java/src/main/java/uk/jtoye/core/payment/PaymentService.java, core-java/src/test/java/uk/jtoye/core/security/JwtTenantFilterTest.java, core-java/src/test/java/uk/jtoye/core/security/JwtTenantFilterMetricsTest.java, core-java/src/test/java/uk/jtoye/core/payment/PaymentServiceTest.java, infra/monitoring/prometheus/alerts.yml, core-java/src/main/resources/application-prod.yml</files>
  <behavior>
    - JwtTenantFilter: given an authenticated request whose Jwt principal has NO resolvable tenant claim (tenant_id/tenantId/tid all absent/invalid), the `tenant.context.missing` counter increments by 1.
    - JwtTenantFilter: given a Jwt principal WITH a valid tenant_id claim, the counter does NOT increment.
    - PaymentService: handling a `payment_intent.payment_failed` webhook event increments the `jtoye.payment.failed` counter by 1.
    - Existing JwtTenantFilterTest behaviors are UNCHANGED — it compiles and passes with the new constructor.
  </behavior>
  <action>
Emit the two metrics the alerts assume exist (issue #98 item 2), following the null-safe `ObjectProvider<MeterRegistry>` precedent in RateLimitInterceptor, and fold in the one-line prod log fix (item 1).

JwtTenantFilter (`.../security/JwtTenantFilter.java`): add a constructor `JwtTenantFilter(ObjectProvider<MeterRegistry> meterRegistryProvider)` that builds a null-safe `Counter` named `tenant.context.missing` (description: authenticated request reached the tenant filter with no resolvable tenant claim — the isolation-failure signal for alert TenantIsolationFailure, issue #98). In `doFilterInternal`, when `auth` is present and `auth.getPrincipal() instanceof Jwt jwt` AND `extractTenant(jwt).isEmpty()`, increment the counter (guarded by `!= null`) and keep the existing log/flow. Do NOT add a tenant-id label (PII/cardinality) — keep the counter label-free.

MANDATORY constructor-coupling fix: `core-java/src/test/java/uk/jtoye/core/security/JwtTenantFilterTest.java:41` currently does `filter = new JwtTenantFilter();` — the only no-arg call site repo-wide. Update it to pass a provider (a `SimpleMeterRegistry`-backed `ObjectProvider<MeterRegistry>`, or a no-op provider whose `getIfAvailable()` returns null — a simple anonymous ObjectProvider is fine since ObjectProvider's other methods have defaults). Without this, compileTestJava fails for the WHOLE test source set.

PaymentService (`.../payment/PaymentService.java`): inject a `MeterRegistry` (add an `ObjectProvider<MeterRegistry>` constructor param following the RateLimitInterceptor precedent; build a null-safe `Counter` field `jtoye.payment.failed`). Increment it at the top of `handlePaymentIntentFailed(Event event)` (~line 293) — the natural detection point. Update EVERY direct `new PaymentService(...)` call site in tests for the new param (use a `SimpleMeterRegistry`-backed provider, or a no-op provider where the counter is not under assertion) — grep the test tree for `new PaymentService(` to find them all. Keep the counter low-cardinality (no tenant/order-id label; an optional bounded `reason` tag is acceptable only if it maps to Stripe's small decline-code enum).

Reconcile `infra/monitoring/prometheus/alerts.yml` so every ACTIVE alert references an emitted metric:
- TenantIsolationFailure (`rate(tenant_context_missing_total[5m])`) is now backed by the real counter — leave the expr, refresh the comment to note the emission point (JwtTenantFilter).
- ADD a payment-failure alert (e.g. `PaymentFailureSpike`) in the business_alerts group with expr `rate(jtoye_payment_failed_total[5m]) > <threshold>` (pick a sensible demo threshold, e.g. > 0.1), severity warning/critical, component `payment`, service `core-java`, with summary/description annotations. This is the "payment-failure metric" the issue calls for.
- DiskSpaceLow + DiskSpaceCritical reference node-exporter series (`node_filesystem_*`) that no service emits and node-exporter is cluster-blocked (OUT of scope). Comment out (disable) both rules with a clear `# PENDING node-exporter deploy (issue #98 — cluster-blocked)` note so no LIVE rule references a never-emitted metric. Do NOT delete them (they document intended coverage).
- Leave `up{...}`, `hikaricp_*`, `jvm_*`, `http_server_requests_*`, `pg_stat_database_numbackends` (postgres-exporter), and `rabbitmq_queue_messages_ready` (rabbitmq job) as-is — all are emitted/scraped.

ITEM 1 — single-emit JSON log fix (`application-prod.yml`, `logging.pattern.console` ~line 85): the current pattern concatenates two `%replace(%msg){...}` conversions, so the message is printed twice. Fix by NESTING: the newline-escaping `%replace` must WRAP the quote-escaping `%replace` of a SINGLE `%msg`, i.e. `%replace(%replace(%msg){'"','\\"'}){'\n','\\n'}` (preserve the existing YAML folded-scalar quote/backslash escaping). Result: `%msg` appears once; a message with quotes+newlines yields one valid JSON object. Do not mention `%msg` in any YAML comment (the verify gate counts occurrences).

Tests (grow the Java @Test count):
- Create `JwtTenantFilterMetricsTest.java`: construct the filter with a provider wrapping a `SimpleMeterRegistry`; set a `SecurityContext` whose authentication principal is a `Jwt` with (a) no tenant claim → assert `registry.get("tenant.context.missing").counter().count() == 1.0`, and (b) a valid tenant_id → assert count stays 0. Clear TenantContext + SecurityContext in teardown. Mirror the Jwt-building + SecurityContextImpl patterns already in JwtTenantFilterTest.
- Extend `PaymentServiceTest.java` (or add a sibling if extension is impractical — then add that file to the commit): drive a `payment_intent.payment_failed` event through `handleWebhookEvent` with a `SimpleMeterRegistry`-backed PaymentService and assert `jtoye.payment.failed` incremented by 1. Reuse the existing failed-payment event fixtures in this class / StripeWebhookIdempotencyIntegrationTest.
  </action>
  <verify>
    <automated>cd core-java && ./gradlew compileTestJava -q && ./gradlew test --tests 'uk.jtoye.core.security.JwtTenantFilterTest' --tests 'uk.jtoye.core.security.JwtTenantFilterMetricsTest' --tests 'uk.jtoye.core.payment.PaymentServiceTest' -q</automated>
    <automated>grep -q 'jtoye_payment_failed_total' infra/monitoring/prometheus/alerts.yml && grep -q 'tenant_context_missing_total' infra/monitoring/prometheus/alerts.yml && if grep -vE '^\s*#' infra/monitoring/prometheus/alerts.yml | grep -q 'node_filesystem_avail_bytes'; then echo "FAIL: DiskSpace rule still live"; exit 1; fi</automated>
    <automated>test "$(grep -v '^\s*#' core-java/src/main/resources/application-prod.yml | grep -oE '%replace\(%replace\(%msg' | wc -l)" = "1" && test "$(grep -v '^\s*#' core-java/src/main/resources/application-prod.yml | grep -oE '%msg' | wc -l)" = "1"</automated>
  </verify>
  <done>Both counters emit at their natural detection points and are proven by tests; the whole test source set compiles (JwtTenantFilterTest updated for the new constructor); alerts.yml has no LIVE alert referencing a never-emitted metric (DiskSpace disabled with note, payment-failure alert added, TenantIsolationFailure backed by the real counter); prod log pattern emits each message once (single %msg, nested %replace).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Management-port-scoped prod prometheus + deploy-pipeline alignment</name>
  <files>core-java/src/main/resources/application-prod.yml, core-java/src/main/java/uk/jtoye/core/security/SecurityConfig.java, core-java/src/test/java/uk/jtoye/core/security/ManagementPortMetricsIntegrationTest.java, k8s/base/core-java-deployment.yaml, infra/monitoring/prometheus/prometheus.yml, .github/workflows/ci-cd.yaml, scripts/smoke-test.sh</files>
  <behavior>
    - With a separate management port configured, GET /actuator/prometheus on the MANAGEMENT port returns 200 with Prometheus exposition text.
    - GET /actuator/prometheus on the APP port does NOT return metrics (404/401 — the app port serves no actuator).
    - smoke-test.sh with EXPECT_PUBLIC_ACTUATOR unset/false treats a non-2xx/3xx public actuator response as PASS (prod posture) and a 2xx as FAIL; with EXPECT_PUBLIC_ACTUATOR=true it asserts 200 as today (dev/local posture).
  </behavior>
  <action>
Item 4 with ALL of its couplings: config, security chain, k8s manifest, Prometheus scrape target, AND the deploy pipeline (ci-cd health check + smoke tests) that would otherwise hard-fail after the port move. No app-port security loosening.

CONFIG (`application-prod.yml`): add `management.server.port: ${MANAGEMENT_SERVER_PORT:9091}` (separate internal port; never hardcode — env-overridable) and change `management.endpoints.web.exposure.include` to `health,info,prometheus`. Because a separate management port serves ALL actuator endpoints on that port and NONE on `server.port`, the public app port (9090) will serve no metrics. Update the stale "scrape via sidecar" comment to describe the management-port approach.

SECURITY (`SecurityConfig.java`): ensure the actuator scrape + probe endpoints (`/actuator/prometheus`, `/actuator/health/**`, `/actuator/info`) are reachable WITHOUT auth on the management port, while leaving every app-port business rule unchanged. Concretely: make the `/actuator/prometheus` permitAll unconditional (drop the `if (!isProd)` gate) — this is safe because in prod the endpoint only lives on the internal management port; on the app port there is nothing to serve. Do NOT touch `anyRequest().authenticated()` or any other matcher. If Context7's Spring Boot docs show the separate management port needs a dedicated `SecurityFilterChain` for the endpoints to be permitted, add a minimal `@Order` management chain using `EndpointRequest.toAnyEndpoint()` scoped to the management port instead — the test below is the arbiter.

K8S (`k8s/base/core-java-deployment.yaml`): align the manifest to the new port split — set startup/liveness/readiness probe `port:` to `9091` (health now lives on the management port), add `containerPort: 9091` next to 9090, and update the pod scrape annotations to `prometheus.io/port: "9091"` (path stays `/actuator/prometheus`). Leave the Service's business port at 9090 (do NOT expose 9091 on the Service/Ingress). This base change is consistent for BOTH overlays because the base hardcodes `SPRING_PROFILES_ACTIVE: "prod"` (~line 52-53) — staging and production pods both run the prod profile. Note in a YAML comment that 9091 is the internal management port (not published via Service/Ingress) so metrics stay off the public surface.

PROMETHEUS (`infra/monitoring/prometheus/prometheus.yml`): update the `core-java` scrape job target/port to the management port (`core-java:9091`, path `/actuator/prometheus`) so the scrape hits the port that actually serves actuator in prod.

CI/CD HEALTH CHECK (`.github/workflows/ci-cd.yaml` line 596, deploy-production "Health check after core-java deployment"): change the in-cluster check to the management port: `kubectl exec -n jtoye-production deployment/core-java -- curl -f http://localhost:9091/actuator/health || exit 1` (kubectl exec runs inside the container, so the internal port is reachable). ALSO add the mirrored in-cluster health-check step to the deploy-staging job (after its core-java rollout step, against the staging namespace) — staging previously got positive actuator verification from the public smoke Tests 2-5, which the next paragraph converts to negative (not-publicly-exposed) assertions; the kubectl-exec check preserves positive actuator-health coverage for staging.

SMOKE TESTS (`scripts/smoke-test.sh` — reworked in PR #201; PRESERVE its structure: test_endpoint helper, test numbering, EXPECT_SWAGGER conditional, pass/fail counters): actuator Tests 2-5 (`/actuator/health`, `/actuator/info`, `/actuator/health/liveness`, `/actuator/health/readiness` against the public $API_URL) will fail once actuator moves to the internal management port. Gate them with a new `EXPECT_PUBLIC_ACTUATOR="${EXPECT_PUBLIC_ACTUATOR:-false}"` env var following the EXPECT_SWAGGER precedent (default false = prod posture, so a healthy prod release NEVER fails smoke — the PR #201 lesson):
- `EXPECT_PUBLIC_ACTUATOR=true` (local/dev stacks where actuator stays on the app port): Tests 2-5 assert 200 exactly as today.
- unset/false (staging + production, both prod-profile): Tests 2-5 flip to asserting the actuator surface is NOT publicly exposed — status must NOT be 2xx/3xx (401 or 404 both acceptable), mirroring the EXPECT_SWAGGER=false branch's negative assertions. Keep the same test count so the summary math stays intact.
- Test 1 (`$API_URL/health`, the app-level controller endpoint) is NOT actuator — leave it asserting 200 on the public URL in all modes.
- Document the new env var in the header comment block. No ci-cd.yaml change is needed for the two smoke invocations (lines 523/606) since the default is the prod posture; do not add EXPECT_PUBLIC_ACTUATOR=true to either.

TEST (`ManagementPortMetricsIntegrationTest.java`, grows Java @Test count): a `@SpringBootTest(webEnvironment = RANDOM_PORT)` with `management.server.port=0` (random) plus prometheus exposure, using `@LocalServerPort` + `@LocalManagementPort` (or `@Value("${local.management.port}")`). Assert: TestRestTemplate GET `/actuator/prometheus` on the management port → 200 body contains `jvm_` (or `# HELP`); GET `/actuator/prometheus` on the app port → NOT 200 (404 or 401). Keep it on the test datasource (H2/Testcontainers as the suite already does) — do NOT require the full prod profile/datasource. If activating a prod-shaped management config in the existing test harness is impractical, mirror the closest existing @SpringBootTest annotations and set the two `management.*` properties via `@TestPropertySource`/`properties`.
  </action>
  <verify>
    <automated>cd core-java && ./gradlew test --tests 'uk.jtoye.core.security.ManagementPortMetricsIntegrationTest' -q</automated>
    <automated>kubectl kustomize k8s/base >/dev/null && grep -q '9091' k8s/base/core-java-deployment.yaml && grep -q 'management.server.port' core-java/src/main/resources/application-prod.yml</automated>
    <automated>grep -q 'localhost:9091/actuator/health' .github/workflows/ci-cd.yaml && ! grep -q 'localhost:9090/actuator/health' .github/workflows/ci-cd.yaml</automated>
    <automated>bash -n scripts/smoke-test.sh && grep -q 'EXPECT_PUBLIC_ACTUATOR' scripts/smoke-test.sh && grep -q 'EXPECT_SWAGGER' scripts/smoke-test.sh</automated>
  </verify>
  <done>/actuator/prometheus is served only on the internal management port with the app port exposing no metrics (proven by test); k8s manifest + prometheus.yml aligned to port 9091; ci-cd production health check curls 9091 and staging gains the mirrored in-cluster check; smoke-test.sh is management-port-aware via EXPECT_PUBLIC_ACTUATOR (default false = prod-safe) with the PR #201 structure preserved; app-port JWT posture unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: Item-5 verification + docs resync + changelog</name>
  <files>docs/metrics.json, docs/CHANGELOG.md, CLAUDE.md</files>
  <action>
ITEM 5 (verification-only): confirm the Alertmanager SMTP receiver is already env-injectable — `grep ALERTMANAGER_SMTP infra/monitoring/docker-compose.monitoring.yml` shows the four env vars with Mailhog defaults, and `infra/monitoring/alertmanager/entrypoint.sh` sed-renders the template with the same defaults. No file change; record in the SUMMARY that item 5 was already satisfied (implemented in the phase-9 Alertmanager work; no regression introduced by this slice).

DOCS RESYNC (after Tasks 1-3 added tests): run `scripts/docs-freshness.sh --write` to regenerate `docs/metrics.json` (Java @Test methods/files + Go Test funcs/files grow), then update the CLAUDE.md testing-baseline narrative counts to match the new metrics.json (java_test_methods/files, go_test_funcs/files, total_logical_invocations). Add a `docs/CHANGELOG.md` entry summarising the #98 do-now observability slice (single-emit prod JSON log, real counters behind TenantIsolationFailure + new PaymentFailureSpike alert, DiskSpace rules disabled pending node-exporter, edge-go /metrics + scrape job re-enabled, prod metrics on internal management port 9091 with ci-cd/smoke alignment, Alertmanager env-injection confirmed pre-existing). Re-run `scripts/docs-freshness.sh` (check mode) and confirm it exits 0.
  </action>
  <verify>
    <automated>grep -c 'ALERTMANAGER_SMTP' infra/monitoring/docker-compose.monitoring.yml | grep -qE '^[4-9]' && grep -q 'ALERTMANAGER_SMTP_SMARTHOST:=mailhog:1025' infra/monitoring/alertmanager/entrypoint.sh</automated>
    <automated>bash scripts/docs-freshness.sh</automated>
  </verify>
  <done>Item 5 confirmed already-satisfied (recorded in SUMMARY, no diff); docs/metrics.json + CLAUDE.md narrative counts resynced; CHANGELOG entry added; docs-freshness check-mode exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → edge-go /metrics | New unauthenticated endpoint; must not leak per-request/tenant data |
| public internet → app port 9090 (core-java) | Business API; prod must expose NO actuator/metrics here |
| cluster-internal → management port 9091 (core-java) | Scrape + kubelet probe + deploy health-check surface; internal-only, not published via Service/Ingress |
| Prometheus → scrape targets | Reads exposition text; low-cardinality labels only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-t6b-01 | Information Disclosure | edge-go /metrics labels | mitigate | Use `c.FullPath()` route TEMPLATE for the `route` label (never raw path); no tenant/id labels — bounds cardinality and prevents tenant-path leakage. |
| T-t6b-02 | Information Disclosure | core-java /actuator/prometheus in prod | mitigate | Serve only on internal management port 9091 (separate from app port 9090); app port serves no actuator; 9091 not published via Service/Ingress; ci-cd health checks use kubectl exec (in-container) rather than a public route. |
| T-t6b-03 | Information Disclosure | tenant.context.missing / jtoye.payment.failed counters | mitigate | Label-free (or bounded enum only); never tag with tenant_id, order_id, or PII — low-cardinality per multi-tenancy rule. |
| T-t6b-04 | Elevation of Privilege | SecurityConfig actuator permit change | accept | `/actuator/prometheus` permit is safe: in prod the endpoint only exists on the internal management port; `anyRequest().authenticated()` and all business matchers are untouched — app-port posture unchanged (proven by ManagementPortMetricsIntegrationTest). |
| T-t6b-05 | Tampering | Malformed JSON log line breaks downstream log parsing | mitigate | Nested `%replace` emits each message once → valid single JSON object per line (grep-asserted single `%msg`). |
| T-t6b-06 | Information Disclosure | Actuator accidentally re-exposed publicly (regression) | mitigate | smoke-test.sh default (EXPECT_PUBLIC_ACTUATOR=false) actively asserts actuator paths are NOT 2xx/3xx on the public URL for staging + prod — a regression fails the deploy smoke suite. |
| T-t6b-SC | Tampering | Go module install (prometheus/client_golang) | mitigate | Official Prometheus Go client (github.com/prometheus/client_golang), already in local module cache at v1.23.2; go.sum checksum-verifies the module graph. Go module (not npm/pip/cargo) — no blocking legitimacy checkpoint required; verify source is github.com/prometheus if resolving a new version. |
</threat_model>

<verification>
- edge-go: `go vet ./...`, `go test ./cmd/edge/` green; GET /metrics → 200 with `http_requests_total`; edge-go scrape job uncommented in prometheus.yml.
- core-java: `./gradlew compileTestJava` clean (JwtTenantFilterTest updated); new + extended tests pass (`JwtTenantFilterMetricsTest`, `PaymentServiceTest`, `ManagementPortMetricsIntegrationTest`); full `./gradlew test` stays green.
- alerts.yml: `grep -vE '^\s*#'` shows no `node_filesystem_*` in a live rule; `jtoye_payment_failed_total` + `tenant_context_missing_total` present.
- application-prod.yml: exactly one `%msg` in the log pattern (comment-filtered count); `management.server.port` set; exposure includes prometheus.
- k8s: `kubectl kustomize k8s/base` builds clean; probes + scrape annotation + containerPort on 9091; Service/Ingress do NOT publish 9091.
- deploy pipeline: ci-cd.yaml has no `localhost:9090/actuator` check remaining (production on 9091, staging mirrored); `bash -n scripts/smoke-test.sh` clean with EXPECT_PUBLIC_ACTUATOR gating Tests 2-5 (default false = prod-safe) and EXPECT_SWAGGER structure preserved.
- item 5: env-injection confirmed present (no change).
- docs: `scripts/docs-freshness.sh` exits 0; CLAUDE.md narrative counts match docs/metrics.json.
</verification>

<success_criteria>
- All five issue-#98 do-now items resolved: (1) single-emit prod JSON log, (2) every live alert references an emitted metric with two real counters added, (3) edge-go /metrics endpoint live + scraped, (4) prod prometheus on a separate internal management port with the app-port posture unchanged AND the deploy pipeline (ci-cd health checks + smoke tests) aligned so healthy releases still pass, (5) env-injectable Alertmanager receiver confirmed already satisfied.
- New code is tested (2 Java tests + 1 Go test); the entire pre-existing test suite still compiles and passes; docs/metrics.json + CLAUDE.md resynced; docs-freshness gate green.
- No hardcoded env-varying values (management port + SMTP all env-injected with defaults); low-cardinality, PII-free metric labels.
</success_criteria>

<output>
Create `.planning/quick/260712-t6b-98-observability-do-now-slice/260712-t6b-SUMMARY.md` when done.
</output>
