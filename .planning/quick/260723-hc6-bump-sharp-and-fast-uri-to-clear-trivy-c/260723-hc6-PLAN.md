---
quick_id: 260723-hc6
slug: bump-sharp-and-fast-uri-to-clear-trivy-c
date: 2026-07-23
status: in-progress
branch: feature/phase-23-vendor-scoped-access
---

# Quick Task 260723-hc6: Clear Trivy CVE gate on PR #255

## Problem

PR #255 (Phase 23, gate-cleared feature) is `MERGEABLE` but `UNSTABLE` — 10/11 checks green,
1 red: **Security Scan → Trivy "fail build on fixable CRITICAL/HIGH"** (job 88988712315, run 29939270416).

The gate flags two newly-published (2026) transitive npm CVEs — a vuln-DB time-bomb, NOT a code
regression (PR was green at `b3fcc9c` on 2026-07-21; Trivy's DB has since ingested the CVEs):

| Package | Where | Installed | Fixed | CVE |
|---|---|---|---|---|
| `sharp` | `frontend/package-lock.json` (transitive via Next.js image opt) | 0.34.5 | 0.35.0 | CVE-2026-33327/33328/35590/35591 (libvips, HIGH) |
| `fast-uri` | `mcp-server/package-lock.json` (transitive via ajv) | 3.1.3 | 3.1.4 | CVE-2026-16221 (HIGH) |

Both are transitive → deterministic fix is an npm `overrides` pin + lockfile regen in each subproject.

## Plan

1. `frontend/package.json` — add `"overrides": { "sharp": "^0.35.0" }`; regen `frontend/package-lock.json`.
2. `mcp-server/package.json` — add `"overrides": { "fast-uri": "^3.1.4" }`; regen `mcp-server/package-lock.json`.

## Verify (before commit)

- frontend: `npm run build` (next build / tsc typecheck) passes AND `npm test` (jest) green.
- mcp-server: `npm test` (vitest) green.
- `bash scripts/docs-freshness.sh` → OK 1574 (test counts MUST NOT change).
- Trivy filesystem scan locally (if available) shows the two findings cleared.

## Constraints

- Stay on `feature/phase-23-vendor-scoped-access` (the PR #255 branch). No new branch. Never touch `main`.
- No test-count change → docs-freshness stays green at 1574 / schema V57.
- Atomic commit, push, confirm Security Scan flips green on PR #255.
