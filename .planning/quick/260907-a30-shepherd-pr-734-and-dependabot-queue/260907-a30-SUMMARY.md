---
id: 260907-a30
slug: shepherd-pr-734-and-dependabot-queue
date: 2026-09-07
type: quick
status: complete
---

# Quick Task 260907-a30: Shepherd PR #734 and the dependabot queue — SUMMARY

Six PRs shepherded to merge; five merged during the task, the sixth (#729, the PR
carrying this summary) merged after one deliberate redo cycle. One new issue (#736),
one new pin, zero gate weakenings.

## Root causes, measured

- **Shared Security Scan red (all 5 dependabot PRs)**: Trivy flagging transitive
  `fast-uri` 3.1.5 in `mcp-server/package-lock.json` (4 HIGH CVEs). Not the PRs'
  doing — every branch was cut from `9d9efaeb` (Sep 2) and the fix (3.1.7) landed
  on main in `a995c173` (#733) after they opened. The known daily-DB time-bomb
  shape; remedy was getting each branch onto current main. Confirmed: Security
  Scan green on all five after the merges/rebases.
- **#729's "flaky" timeouts were neither flaky nor caused by any of its six direct
  bumps.** Deterministic repro; bisection (pin back one dep at a time) exonerated
  jest-environment-jsdom, lucide-react and next individually, and convicted the
  lockfile regen's TRANSITIVE float `nwsapi` 2.2.24 → 2.2.27 (jsdom's selector
  engine; prior form for breaking testing-library role queries — dperini/nwsapi#139,
  #152). With only nwsapi pinned back, both Radix-Select suites pass 20/20 and run
  ~9x faster (3.4s vs 29s). Pinned via the existing `overrides` block; exit
  criteria in #736.
- **#730's Run Tests red**: jest 30 rejects the legacy `goo.gl` snapshot-header
  guide link; `jest -u` rewrote the one line. Full suite under jest 30: 172
  suites / 1873 tests green.
- **#728's two C-3 citation violations** resolved themselves in the origin/main
  merge — #726 had rewritten those citations; the citation gate was re-run locally
  (0 violations) rather than assumed.
- **#734 went CONFLICTING** when #726/#735 merged: one .gitignore tail-append
  conflict (both blocks kept, resolution verified mechanically via
  `git merge-tree`), plus the counted-claims maintenance its own DIAGRAMS.md
  predicts: migrations 64 → 66 (V65/V66), controllers/services/repositories
  re-counted unchanged (25/41/25), all 17 cited anchors byte-identical at the new
  pin `a990551e`, both HTMLs re-rendered (`deliver` 9/9, `visual-check` pass) and
  the PR body corrected + read back.

## Doc gates (all fired fail-direction first)

`check-doc-versions.sh` went FAIL→PASS three times with the drift named each time:
6 claims on #728 (AWS SDK 2.54.9, Stripe Java 33.4.0), 3 on #730 (Jest 30.5.1),
10 on #729 (Next.js 16.3.4); plus 4 ungated `@stripe/stripe-js 9.15.0` prose
stragglers caught by the old-version sweep. Final state on this branch: 147/147
claims, 43/43 citations.

## Process

Review records: independent release-QA subagent reviewed all six PRs read-only
with command evidence (including re-proving #728's gate failure at the pre-fix
commit), verdicts posted via `gh pr review --comment`, each postdating its final
head; the review-record verdict went green on every PR before merge. Merge order
respected the #729↔#730 lockfile coupling: #730 first, then #729's redo (merge
main, resolve manifest by hand, regenerate the lock from main's via `npm install`
— never hand-merged lock hunks). #732's residual is recorded in its review: the
download-artifact v8 step sits behind the integration path filter and first
executes on the next main run that takes it; failure mode is check-jacoco's rc=2
VOID, not a silent pass.

## Merged

| PR | What | Merge |
|----|------|-------|
| #734 | architecture diagrams (re-pinned to a990551e, 66 migrations) | d49d3325 |
| #730 | jest 29.7 → 30.5.1 + @types/jest 30 | 3001b38f |
| #728 | AWS SDK 2.54.9 + Stripe Java 33.4.0 | 60ecc410 |
| #731 | codeql-action 4.37.8 → 4.37.9 | 76e1f201 |
| #732 | download-artifact 7 → 8 | 4fd84364 |
| #729 | 6 frontend bumps + nwsapi 2.2.24 pin (this PR) | — merges last, carrying these artifacts |

## Follow-ups

- #736: remove the nwsapi override once upstream fixes the role-query regression.
- Vendored `scripts/gates/review-record-check.sh` has drifted from the canonical
  copy (session-start hook report); refresh via `review-gate-onboard.sh enroll`
  when convenient — not done here because guard-hook changes carry their own
  corpus-replay discipline.
