---
id: 260907-a30
slug: shepherd-pr-734-and-dependabot-queue
date: 2026-09-07
type: quick
status: in-progress
---

# Quick Task 260907-a30: Shepherd PR #734 and the dependabot queue (#728–#732)

## Diagnosis (measured, from CI logs — run links in the PR checks tabs)

All five dependabot branches were cut from `9d9efaeb` (2026-09-02). The shared
Security Scan failure is Trivy flagging `mcp-server/package-lock.json` fast-uri
**3.1.5** (4 HIGH CVEs, fixed ≥3.1.6); main already carries **3.1.7** since
`a995c173` (#733, merged after the PRs opened). Getting each branch onto current
main clears it. All five also lack a review record for their head (required
`review-record` check). Per-PR extras:

| PR | Bump | Extra fallout beyond rebase + review record |
|----|------|---------------------------------------------|
| #734 | (docs: architecture diagrams) | now CONFLICTING with main after #726/#735 merged 2026-09-07; needs conflict resolution + fresh review record (record must postdate head) |
| #732 | actions/download-artifact 7→8 | none — verify workflows using it actually ran in PR CI |
| #731 | github/codeql-action 4.37.8→4.37.9 | none |
| #728 | AWS SDK 2.54.3→2.54.9, Stripe Java 33.3.0→33.4.0 | docs-freshness FAIL (6 stale version claims / 4 docs); citation gate FAIL: `.planning/codebase/STACK.md:100` and `.planning/codebase/INTEGRATIONS.md:9` cite `core-java/build.gradle.kts:145` which the bump changed |
| #730 | jest 29.7→30 + @types/jest | jest 30 rejects the legacy snapshot-header guide link in `__tests__/header-snapshot.test.ts` snap file (`https://goo.gl/fbAQLP` → `https://jestjs.io/docs/snapshot-testing`); suite fails to run → jest count 1581 vs metrics 1583; docs quote Jest 29.7.0 |
| #729 | 6 frontend minor/patch (next 16.3.3→16.3.4, jest-environment-jsdom 30.4.1→30.5.1, @stripe/stripe-js, lucide-react, @types/node, eslint-config-next) | 2 test TIMEOUTS (products-orders-shop-scope tail-picker 15s; delivery-log status filter 5s) — diagnose locally, likely jsdom-bump slowdown; docs-freshness + Operational Contracts also red (version claims / citations) |

## Tasks

1. **PR #734**: fetch branch, merge `origin/main`, resolve conflicts (mind the
   CHANGELOG "take theirs" trap — keep own entry), push, fresh review record, merge.
2. **#731 + #732**: `@dependabot rebase`; when CI is green, review + review record, merge.
3. **#728**: merge main into branch; `scripts/docs-freshness.sh --write` + prose
   counts; repair the two `.planning/codebase` citations; push.
4. **#730**: merge main; update the header-snapshot snap header under jest 30;
   docs regen; push.
5. **#729**: merge main; reproduce the two timeouts locally; fix; docs regen +
   citation repair; push.
6. **Merge order** and lockfile coupling: #729 and #730 both touch
   `frontend/package-lock.json` — merge one, then re-base the other on the new
   main (regenerate the lock by re-applying the bump with npm on the new base,
   never hand-merge lockfile hunks) and let CI re-run. Every merge: CI green +
   review record for the exact head + `gh pr merge --squash`.

## Constraints

- No AI-attribution trailers in commits/PR text (global ruling 2026-08-30).
- Branch work happens in a linked worktree, never in the main checkout (a second
  session may drive it).
- Review records postdate the final head sha of each PR (push FIRST, review SECOND).
- Merges are squash merges (changelog gate depends on `(#PR)` first-parent shape).
