---
phase: quick-260714-3na
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # New files
  - frontend/lib/motion-features.ts
  - frontend/components/motion-provider.tsx
  - frontend/lib/motion.ts
  - frontend/components/ui/skeleton.tsx
  - frontend/hooks/use-cart-count.ts
  - frontend/hooks/use-count-up.ts
  - frontend/lib/chart-colors.ts
  - frontend/hooks/__tests__/use-cart-count.test.tsx
  - frontend/hooks/__tests__/use-count-up.test.tsx
  - frontend/components/ui/__tests__/skeleton.test.tsx
  - frontend/components/storefront/__tests__/storefront-nav-badge.test.tsx
  - frontend/components/__tests__/motion-provider.test.tsx
  # Modified files
  - frontend/app/globals.css
  - frontend/app/layout.tsx
  - frontend/app/dashboard/page.tsx
  - frontend/app/dashboard/customers/page.tsx
  - frontend/app/dashboard/finance/page.tsx
  - frontend/app/dashboard/marketing/page.tsx
  - frontend/app/dashboard/onboarding/page.tsx
  - frontend/app/dashboard/onboarding/approvals/page.tsx
  - frontend/app/dashboard/orders/page.tsx
  - frontend/app/dashboard/products/page.tsx
  - frontend/app/dashboard/products/import/page.tsx
  - frontend/app/dashboard/shops/page.tsx
  - frontend/app/dashboard/kitchen/page.tsx
  - frontend/components/storefront/cart-provider.tsx
  - frontend/components/storefront/storefront-nav.tsx
  - frontend/components/storefront/product-detail-modal.tsx
  - frontend/app/shop/[slug]/page.tsx
  - frontend/app/track/page.tsx
  - frontend/app/shop/[slug]/orders/[orderNumber]/page.tsx
  - docs/metrics.json
  - CLAUDE.md
autonomous: true
requirements: [QUICK-260714-3NA]
user_setup: []

must_haves:
  truths:
    - "App chrome (buttons, focus rings) uses the flame-orange brand primary instead of stock shadcn blue, in light and dark modes"
    - "Every framer-motion component renders through LazyMotion strict + MotionConfig reducedMotion=user; zero `import { motion }` remains in app code"
    - "Storefront nav shows a basket icon + live count badge on /shop/[slug] routes; badge spring-pops on count change and updates cross-tab"
    - "The floating basket bar on shop/[slug] springs in/out via AnimatePresence and pads for the safe-area inset"
    - "KDS order cards FLIP-animate when a status bump re-sorts the grid; newly arrived orders glow ember exactly once"
    - "Dashboard KPI numbers count up on load and jump instantly to the target under prefers-reduced-motion"
    - "Both order-tracking steppers animate connector + dots on status change with a finite active-step pulse; the 15s polling transport is untouched"
    - "Gates green: `npm run build` (tsc), `npx jest` full suite, `bash scripts/docs-freshness.sh` check mode"
  artifacts:
    - path: "frontend/lib/motion-features.ts"
      provides: "domMax feature bundle as default export for LazyMotion async loading"
    - path: "frontend/components/motion-provider.tsx"
      provides: "Client component wrapping LazyMotion strict + MotionConfig reducedMotion=user"
      exports: ["MotionProvider"]
    - path: "frontend/lib/motion.ts"
      provides: "Shared variants (fadeInUp, staggerContainer, staggerItem) + duration/spring constants"
    - path: "frontend/components/ui/skeleton.tsx"
      provides: "Shared Skeleton primitive using the shimmer keyframe"
      exports: ["Skeleton"]
    - path: "frontend/hooks/use-cart-count.ts"
      provides: "SSR-safe per-slug cart count subscribed to jtoye:cart-updated + storage events"
      exports: ["useCartCount"]
    - path: "frontend/hooks/use-count-up.ts"
      provides: "Count-up hook on framer-motion standalone animate() with useReducedMotion instant-jump"
      exports: ["useCountUp"]
    - path: "frontend/lib/chart-colors.ts"
      provides: "Brand palette hex constants for Recharts (ember/trust/slate/amber)"
      exports: ["CHART_COLORS"]
  key_links:
    - from: "frontend/app/layout.tsx"
      to: "frontend/components/motion-provider.tsx"
      via: "MotionProvider wraps Providers in RootLayout body"
      pattern: "MotionProvider"
    - from: "frontend/components/motion-provider.tsx"
      to: "frontend/lib/motion-features.ts"
      via: "async features import for LazyMotion"
      pattern: "import\\(.*motion-features.*\\)"
    - from: "frontend/components/storefront/cart-provider.tsx"
      to: "frontend/hooks/use-cart-count.ts"
      via: "jtoye:cart-updated CustomEvent dispatch/listen"
      pattern: "jtoye:cart-updated"
    - from: "frontend/components/storefront/storefront-nav.tsx"
      to: "frontend/hooks/use-cart-count.ts"
      via: "useCartCount(slug) drives the badge"
      pattern: "useCartCount"
    - from: "frontend/app/dashboard/page.tsx"
      to: "frontend/hooks/use-count-up.ts"
      via: "KPI stat values rendered through useCountUp"
      pattern: "useCountUp"
    - from: "frontend/app/dashboard/page.tsx"
      to: "frontend/lib/chart-colors.ts"
      via: "Recharts fills reference CHART_COLORS"
      pattern: "CHART_COLORS"
---

<objective>
Motion uplift foundation for the J'Toye frontend: promote the locked sketch-001 brand tokens into the shadcn CSS vars, install a LazyMotion(domMax)/MotionConfig provider with full `motion.`→`m.` conversion, and wire mechanism-level motion into the storefront (cart badge + basket bar + tap feedback), KDS (FLIP re-sort + new-order ember glow), dashboard overview (KPI count-ups + brand chart colors), and both order-tracking steppers — all with zero new runtime dependencies, then close with Jest coverage and the docs-freshness gate.

Purpose: the app currently ships stock shadcn blue and 10 pages of hand-rolled mount fades on the full `motion` import (no reduced-motion handling, no tree-shaking). This lands the brand identity + a motion architecture every later phase builds on.
Output: brand-tokened globals.css, MotionProvider + shared motion lib + Skeleton primitive, animated storefront/KDS/dashboard/tracking surfaces, ~8-14 new Jest blocks, refreshed docs/metrics.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>

**CRITICAL PATH RULE: every file operation happens in the git worktree at `/home/sanmi/IdeaProjects/JToye_OaaS_2026-motion` (branch `feature/motion-uplift-foundation`). NEVER touch `/home/sanmi/IdeaProjects/JToye_OaaS_2026` (the main checkout has in-flight work). All relative paths below are rooted at the worktree.**

@/home/sanmi/IdeaProjects/JToye_OaaS_2026-motion/CLAUDE.md
@/home/sanmi/IdeaProjects/JToye_OaaS_2026-motion/.planning/sketches/themes/default.css
@/home/sanmi/IdeaProjects/JToye_OaaS_2026-motion/frontend/app/globals.css
@/home/sanmi/IdeaProjects/JToye_OaaS_2026-motion/frontend/app/layout.tsx

<interfaces>
<!-- Verified against the worktree 2026-07-14. Executors use these directly — no re-exploration needed. -->

Locked sketch palette (from .planning/sketches/themes/default.css):
- primary #ea580c (orange-600) | primary-bright #f97316 | accent #fb923c | trust #059669 | trust-bright #10b981 | danger #ef4444 | slate-muted #64748b | glow-ember `0 8px 32px rgba(249,115,22,.35)`

HSL conversions for shadcn vars: #ea580c = `20.5 90.2% 48.2%`. shadcn's stock orange theme pairs it with `--primary-foreground: 60 9.1% 97.8%`.

`import { motion } from "framer-motion"` appears in exactly these 10 files (all under frontend/app/dashboard/): page.tsx, customers/page.tsx, finance/page.tsx, marketing/page.tsx, onboarding/page.tsx, onboarding/approvals/page.tsx, orders/page.tsx, products/page.tsx, products/import/page.tsx, shops/page.tsx. The kitchen page uses NO framer-motion today. framer-motion is `^12.42.2` in frontend/package.json; node_modules already installed.

From frontend/components/storefront/cart-provider.tsx:
- `CartProvider({ shopSlug, children })`; persists via `useEffect(() => { saveCart(shopSlug, items) }, [shopSlug, items])` — the single write point (also fires on mount hydration, which is harmless and helps badge sync)
- localStorage key format: `` `jtoye-cart-${slug}` `` storing `{ shopSlug, items }` where items have `quantity`
- context exposes `itemCount` (sum of quantities) and `totalPennies`
- Mounted at frontend/app/shop/[slug]/layout.tsx:15 — wraps ALL /shop/[slug]/* pages

From frontend/components/storefront/storefront-nav.tsx (201 lines):
- Rendered in frontend/app/shop/layout.tsx:30 header, wraps ALL /shop routes (with and without slug)
- Already imports `usePathname`; does NOT yet import `useParams`
- Structure: hidden-sm:flex desktop links div (lines 88-114) → profile/sign-in block (116-141) → mobile Sheet hamburger (145-198)

From frontend/app/shop/[slug]/page.tsx (751 lines):
- `ProductCard` (line 82): Add button line 166-172 and qty steppers 174-188 ALREADY have `active:scale-95`; article has `active:scale-[0.99]` — D-09 needs NO change here
- `FloatingCartBar({ slug, minimumOrderPennies })` (line ~711, bottom of file): ALREADY EXISTS — fixed bottom bar, `if (itemCount === 0) return null`, itemCount badge + "View basket" + total + below-minimum messaging, links to `/shop/${slug}/cart`, visible on ALL viewports. Rendered at the end of the page JSX (~line 707)

From frontend/components/storefront/product-detail-modal.tsx (295 lines):
- Sticky footer (lines 250+): Add-to-cart button (~line 260) has `transition-colors` but NO active:scale; the `quantity > 0` branch below it renders increment/decrement buttons — add tap feedback only where missing

From frontend/app/dashboard/kitchen/page.tsx (496 lines):
- Single sorted grid (NOT per-status columns): `<div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">` (line 417) with `sortedOrders.map((order) => <Card key={order.id} ...>)` (lines 418-491)
- STOMP live updates via `useStomp(stompTopic, handleWsMessage, fetchOrders)` (line 277); bump handler moves status CONFIRMED→PREPARING→READY→(remove) (lines 289-306)
- statusConfig at lines 45-47; KITCHEN_STATUSES at line 112

From frontend/app/dashboard/page.tsx (459 lines):
- `statusConfig` chartColor hexes lines 87-96 (CONFIRMED `#3b82f6`, PREPARING `#f59e0b`); `vatRateLabels` lines 100-103 (STANDARD `#3b82f6`)
- Inline `containerVariants`/`itemVariants` lines 191-202 — shapes exactly match the planned shared staggerContainer/fadeInUp
- statCards (plain integer values) lines 212-217, rendered at lines 280-282
- Pie `<Cell fill={entry.color}>` line 323 (entry.color plumbs from statusConfig — keep plumbing); Bar fills `#3b82f6`/`#f59e0b` lines 363-364

From frontend/app/track/page.tsx (283 lines):
- Horizontal stepper: STEPS lines 24-29, `currentStep` line 138, dot circles lines 234-250 (`isComplete = i <= currentStep`), progress fill div lines 254-258 (`style={{ width: %` with transition-all)
- Client-side polling with COMPLETED/CANCELLED guard at line 125 — KEEP

From frontend/app/shop/[slug]/orders/[orderNumber]/page.tsx (350 lines):
- Vertical stepper: STEPS lines 23-28, stepper render lines 205-245, connector `w-0.5 h-8` colored by isComplete lines 228-233
- `setInterval(fetchStatus, 15000)` line 114 — KEEP

Jest (frontend/jest.config.js): next/jest, jsdom, `@/` mapper, setupFilesAfterEach jest.setup.js, testMatch includes `**/__tests__/**/*.[jt]s?(x)`. docs-freshness counts ONLY files matching `^frontend/(app|components|lib|hooks|types|__tests__)/.*\.test\.tsx?$` — new test files MUST use the `.test.tsx`/`.test.ts` suffix.

docs/metrics.json current: jest_blocks 249, jest_files 35, total_logical_invocations 1258. `scripts/docs-freshness.sh --write` regenerates metrics.json only; CLAUDE.md line 15's count sentence is manually maintained (script check mode does NOT verify it — update it by hand to keep docs honest).

tailwind.config.ts already has a keyframes/animation block (accordion) — but per D-03 the shimmer keyframe goes in globals.css; Skeleton consumes it via an arbitrary `animate-[shimmer_...]` utility.
</interfaces>

<decision_coverage>
| Decision | Where implemented |
|----------|-------------------|
| D-01 keep framer-motion@12, no new deps | All tasks (verify: package.json untouched) |
| D-02 LazyMotion strict domMax + MotionConfig + m-conversion | Task 1 |
| D-03 brand tokens in globals.css + shimmer | Task 1 |
| D-04 viewport export | Task 1 |
| D-05 lib/motion.ts shared variants | Task 1 |
| D-06 Skeleton primitive (component + tests only) | Task 1 + Task 3 |
| D-07 cart event + use-cart-count + nav badge | Task 2 |
| D-08 sticky basket bar | Task 2 — ADAPTED: the check D-08 mandated revealed FloatingCartBar already ships this affordance; enhanced in place (see task note) |
| D-09 tap feedback where missing | Task 2 (modal footer only; ProductCard already has it) |
| D-10 KDS FLIP + ember glow | Task 2 — adapted to the real single-grid layout |
| D-11 KPI count-ups + chart-colors | Task 2 |
| D-12 tracking steppers, keep polling | Task 2 |
| D-13 tests + metrics.json | Task 3 |
| D-14 gates (build/jest/docs-freshness) | Tasks 1-3 verify steps |
| D-15 out of scope (cart drawer, GSAP, D3, SSE, next/image, mass skeleton, theme overhaul, backend) | Excluded everywhere |
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Foundation — brand tokens, MotionProvider, shared variants, Skeleton, m-conversion, viewport</name>
  <files>frontend/app/globals.css, frontend/app/layout.tsx, frontend/lib/motion-features.ts, frontend/components/motion-provider.tsx, frontend/lib/motion.ts, frontend/components/ui/skeleton.tsx, frontend/app/dashboard/page.tsx, frontend/app/dashboard/customers/page.tsx, frontend/app/dashboard/finance/page.tsx, frontend/app/dashboard/marketing/page.tsx, frontend/app/dashboard/onboarding/page.tsx, frontend/app/dashboard/onboarding/approvals/page.tsx, frontend/app/dashboard/orders/page.tsx, frontend/app/dashboard/products/page.tsx, frontend/app/dashboard/products/import/page.tsx, frontend/app/dashboard/shops/page.tsx</files>
  <action>
  All paths rooted at /home/sanmi/IdeaProjects/JToye_OaaS_2026-motion.

  1. **globals.css brand tokens (D-03):** In `:root`, change `--primary` to `20.5 90.2% 48.2%` (orange-600 #ea580c), `--primary-foreground` to `60 9.1% 97.8%`, `--ring` to `20.5 90.2% 48.2%`. In `.dark`, set the same three (`--primary: 20.5 90.2% 48.2%`, `--primary-foreground: 60 9.1% 97.8%`, `--ring: 20.5 90.2% 48.2%` — orange reads well on dark; keep every other dark var unchanged). Leave ALL other neutrals untouched (slate values already match the sketch). Append custom props to `:root`: `--trust: #059669`, `--trust-bright: #10b981`, `--ember-bright: #f97316`, `--shadow-glow-ember: 0 8px 32px rgba(249, 115, 22, 0.35)`. Add a `@keyframes shimmer` block after the `@layer base` blocks: from `background-position: 200% 0` to `background-position: -200% 0`. Do NOT restyle any component — token indirection only; the storefront theme overhaul is a separate future phase.

  2. **frontend/lib/motion-features.ts (D-02):** two lines — import `domMax` from framer-motion and default-export it (`export { domMax as default } from "framer-motion"` is fine). `domMax` (not domAnimation) because the KDS uses layout animations.

  3. **frontend/components/motion-provider.tsx (D-02):** `"use client"` component `MotionProvider({ children })` returning `LazyMotion` with `strict` and `features={loadFeatures}` where `loadFeatures = () => import("@/lib/motion-features").then((mod) => mod.default)`, wrapping `MotionConfig reducedMotion="user"`, wrapping children. Named export.

  4. **frontend/app/layout.tsx (D-02, D-04):** import MotionProvider and wrap the existing `<Providers>{children}</Providers>` as `<MotionProvider><Providers>{children}</Providers></MotionProvider>`. Change the type import to `import type { Metadata, Viewport } from "next"` and add `export const viewport: Viewport = { width: "device-width", initialScale: 1, themeColor: [{ media: "(prefers-color-scheme: light)", color: "#ffffff" }, { media: "(prefers-color-scheme: dark)", color: "#0f172a" }] }`. Keep the existing `dynamic = "force-dynamic"` export and its comment intact (CSP nonce dependency, issue #89).

  5. **frontend/lib/motion.ts (D-05):** shared motion vocabulary typed with `Variants`/`Transition` from framer-motion: `durations` (fast 0.2, base 0.5), `springPop` ({ type: "spring", stiffness: 500, damping: 30 }), `fadeInUp` ({ hidden: { opacity: 0, y: 20 }, visible: { opacity: 1, y: 0 } }), `staggerContainer` ({ hidden: { opacity: 0 }, visible: { opacity: 1, transition: { staggerChildren: 0.1 } } }), `staggerItem` (alias of fadeInUp shape). Match the shapes already hand-rolled in dashboard/page.tsx lines 191-202 so swaps are drop-in.

  6. **frontend/components/ui/skeleton.tsx (D-06):** `Skeleton({ className, ...props })` div using `cn()` from @/lib/utils with a slate gradient (`bg-gradient-to-r from-slate-100 via-slate-200 to-slate-100 bg-[length:200%_100%]`), `rounded-md`, and `animate-[shimmer_1.8s_ease-in-out_infinite] motion-reduce:animate-none` consuming the globals.css shimmer keyframe. Component + tests only — do NOT replace any existing hand-rolled animate-pulse blocks (adoption is incremental, per D-06).

  7. **m-conversion (D-02):** in each of the 10 dashboard files listed above, replace `import { motion } from "framer-motion"` with `import { m } from "framer-motion"` and every `<motion.X>`/`</motion.X>` JSX tag with `<m.X>`/`</m.X>` (grep shows usages include motion.div and motion.tr). This is MANDATORY before the strict LazyMotion provider lands in the same commit — `strict` throws at runtime on any full `motion` component. Where a page defines inline variants identical in shape to the shared ones (e.g. dashboard/page.tsx containerVariants/itemVariants), import `staggerContainer`/`staggerItem`/`fadeInUp` from `@/lib/motion` and delete the local copies — mechanical swaps only, do not chase pixel-perfect parity of delays (D-05).
  </action>
  <verify>
    <automated>cd /home/sanmi/IdeaProjects/JToye_OaaS_2026-motion/frontend && npm run build && [ "$(grep -rl 'import { motion }' app components lib hooks --include='*.tsx' --include='*.ts' | wc -l)" = "0" ] && [ "$(grep -rn '<motion\.' app components --include='*.tsx' | wc -l)" = "0" ] && grep -q 'MotionProvider' app/layout.tsx && grep -q 'shimmer' app/globals.css && grep -q '20.5 90.2% 48.2%' app/globals.css</automated>
  </verify>
  <done>Build (tsc) green; zero `import { motion }` or `motion.` JSX remain; layout wires MotionProvider + viewport export; globals.css carries the orange-600 primary/ring HSL triplets in :root and .dark plus --trust/--ember-bright/--shadow-glow-ember/shimmer; lib/motion.ts + skeleton.tsx exist; package.json dependencies unchanged (D-01).</done>
</task>

<task type="auto">
  <name>Task 2: Feature motion — cart badge/basket bar, tap feedback, KDS FLIP + glow, KPI count-ups + chart colors, tracking steppers</name>
  <files>frontend/components/storefront/cart-provider.tsx, frontend/hooks/use-cart-count.ts, frontend/components/storefront/storefront-nav.tsx, frontend/app/shop/[slug]/page.tsx, frontend/components/storefront/product-detail-modal.tsx, frontend/app/dashboard/kitchen/page.tsx, frontend/hooks/use-count-up.ts, frontend/lib/chart-colors.ts, frontend/app/dashboard/page.tsx, frontend/app/track/page.tsx, frontend/app/shop/[slug]/orders/[orderNumber]/page.tsx</files>
  <action>
  All paths rooted at /home/sanmi/IdeaProjects/JToye_OaaS_2026-motion. Minimal diffs that read like the surrounding code; import `m`, `AnimatePresence` from "framer-motion" and shared presets from `@/lib/motion` as needed.

  1. **Cart event (D-07)** — cart-provider.tsx: in the existing persist effect (`useEffect(() => { saveCart(shopSlug, items) }, [shopSlug, items])`), after saveCart, dispatch `window.dispatchEvent(new CustomEvent("jtoye:cart-updated", { detail: { slug: shopSlug, itemCount, totalPennies } }))` computing itemCount/totalPennies inline from `items` (reduce over quantity / quantity*pricePennies) to avoid new effect deps. Guard with the same `typeof window === "undefined"` idiom used by saveCart. No other provider changes.

  2. **frontend/hooks/use-cart-count.ts (D-07)** — `useCartCount(slug: string | undefined): number`. SSR-safe: `useState(0)`, returns 0 until mounted and whenever slug is undefined. On mount (effect keyed on slug): read localStorage `` `jtoye-cart-${slug}` `` (parse `{ shopSlug, items }`, sum quantities, tolerate parse errors → 0); subscribe to (a) `jtoye:cart-updated` CustomEvent — if `detail.slug === slug` set count from `detail.itemCount`, and (b) the cross-tab `storage` event — if `e.key` equals the slug's storage key, re-read from localStorage. Clean up both listeners.

  3. **Nav basket badge (D-07)** — storefront-nav.tsx: add `useParams<{ slug?: string }>()` (next/navigation, already partially imported) and `const slug = params?.slug`; `const cartCount = useCartCount(slug)`. When `slug` is present, render a basket affordance visible on all viewports (place it before the profile/sign-in block, OUTSIDE the `hidden sm:flex` div): a `Link` to `` `/shop/${slug}/cart` `` with a lucide `ShoppingBag` icon and, when `cartCount > 0`, an `m.span` count badge (small orange-500 circle, white bold text) with `key={cartCount}` so a count change remounts it and replays `initial={{ scale: 0.5 }} animate={{ scale: 1 }}` with `springPop` from @/lib/motion. Wrap in an element with `aria-live="polite"` and an sr-only text like `{cartCount} items in basket`. No badge/icon on slug-less routes (/shop, /track, /for-operators).

  4. **Floating basket bar (D-08, ADAPTED)** — shop/[slug]/page.tsx `FloatingCartBar` (~line 711): DEVIATION NOTE — D-08 specified a NEW component hidden on md+, conditioned on "the page already has visible cart affordances" and mandated checking the bottom of this page; the check shows FloatingCartBar ALREADY implements the bar (badge, "View basket", total, below-minimum messaging) on all viewports and is the page's ONLY cart affordance. Per the Incremental Betterment Doctrine (regression by omission is a defect), enhance it in place instead of duplicating, and KEEP all-viewport visibility (hiding on md+ would silently drop the desktop below-minimum signal; the new nav badge is additive, not a replacement). Changes: replace `if (itemCount === 0) return null` with `<AnimatePresence>` gating `{itemCount > 0 && ...}`; convert the fixed-position container to `m.div` with `initial={{ y: 96, opacity: 0 }} animate={{ y: 0, opacity: 1 }} exit={{ y: 96, opacity: 0 }}` and `springPop`; add `pb-[env(safe-area-inset-bottom)]` to the fixed container's classes. Keep everything else (belowMinimum styling, Link target) byte-identical.

  5. **Tap feedback (D-09)** — product-detail-modal.tsx sticky footer only: add `active:scale-[0.98]` to the Add-to-cart button (~line 260, change its `transition-colors` to `transition-all`) and `active:scale-95` to the increment/decrement buttons in the `quantity > 0` footer branch if missing. ProductCard in shop/[slug]/page.tsx ALREADY has active:scale on Add/steppers/article — make NO change there (D-09: only add where missing; keep the existing CSS approach rather than whileTap for consistency with sibling buttons).

  6. **KDS FLIP + ember glow (D-10, adapted to the real layout)** — dashboard/kitchen/page.tsx: the KDS is a single sorted grid (line 417), not per-status columns — the FLIP intent maps to re-sort animation. Inside the grid div, wrap the `sortedOrders.map(...)` output in `<AnimatePresence mode="popLayout">`. For each order, wrap the `<Card>` in `<m.div layout key={order.id} initial={{ opacity: 0, scale: 0.92 }} animate={{ opacity: 1, scale: 1 }} exit={{ opacity: 0, scale: 0.92 }}>` (move `key` from Card to the m.div; keep Card's className/props untouched). New-order one-shot glow: hold `const seenIdsRef = useRef<Set<string> | null>(null)`; in an effect on the orders state, initialize the set from the FIRST loaded batch WITHOUT glowing (skip on first render per D-10), then add ids as they appear. During render compute `isNew = seenIdsRef.current !== null && !seenIdsRef.current.has(order.id)`; when isNew, extend the m.div's animate prop with a boxShadow keyframe array `["0 0 0 rgba(249,115,22,0)", "0 8px 32px rgba(249,115,22,0.35)", "0 0 0 rgba(249,115,22,0)"]` over ~1.6s, NO repeat (one-shot ember glow). Reduced motion is covered by the global MotionConfig for m components — no per-component handling needed. Do not touch the STOMP wiring, bump handlers, beep, or sorting logic.

  7. **frontend/hooks/use-count-up.ts (D-11)** — `useCountUp(target: number, options?: { duration?: number }): number` built on framer-motion's standalone `animate()`. MUST call `useReducedMotion()` and, when true, set the value to `target` immediately (no tween). Otherwise animate from the previous value (0 on first mount, tracked in a ref) to target with duration ~0.8 ease-out, `onUpdate` → setState, and stop the animation controls in the effect cleanup. Return a rounded integer.

  8. **frontend/lib/chart-colors.ts (D-11)** — `export const CHART_COLORS = { ember: "#ea580c", emberBright: "#f97316", accent: "#fb923c", trust: "#059669", trustBright: "#10b981", danger: "#ef4444", slate: "#64748b", amber: "#f59e0b" } as const` with a comment tying it to .planning/sketches/themes/default.css.

  9. **Dashboard overview (D-11)** — dashboard/page.tsx: (a) KPI count-ups — statCards values are plain integers (no currency formatter involved); add a small local `StatValue({ value }: { value: number })` component that returns `useCountUp(value)` rendered as text (hooks can't run inside `.map`), and use it at the stat-value render (lines 280-282). (b) Chart colors — import CHART_COLORS; replace every `#3b82f6` literal with `CHART_COLORS.ember` (statusConfig.CONFIRMED line 89, vatRateLabels.STANDARD line 100, Bar revenue fill line 363) and every `#f59e0b` literal with `CHART_COLORS.amber` (statusConfig.PREPARING line 90, Bar vat fill line 364 — same hue, now centralized). Keep the pie `<Cell fill={entry.color}>` plumbing exactly as-is (entry.color may come from API data per D-11); leave all other status hexes untouched — hue changes beyond de-blue-ing are out of scope. Also replace the loading spinner's `border-blue-600` (line 207) with `border-orange-600` to match the new primary.

  10. **Tracking steppers (D-12)** — both pages, KEEP the polling transports (track/page.tsx guard line 125; orders/[orderNumber]/page.tsx `setInterval(fetchStatus, 15000)` line 114) — SSE is out of scope.
      - track/page.tsx (horizontal): convert the progress fill div (lines 254-258) to `m.div` animating `scaleX` to `Math.max(0.05, currentStep / (STEPS.length - 1))` with `style={{ transformOrigin: "left" }}` and full-width class (`initial={false}`, spring or 0.5s ease) — replace the width-percentage styling. Dots (lines 234-250): wrap each circle in an `m.div` keyed on `` `${step.key}-${isComplete}` `` so a step newly reaching complete remounts and springs in (`initial={{ scale: 0.6 }} animate={{ scale: 1 }}`, springPop); give the ACTIVE step (i === currentStep) a finite pulse — scale keyframes `[1, 1.08, 1]` with `repeat: 2` (2-3 pulses total, NOT infinite).
      - orders/[orderNumber]/page.tsx (vertical): the connector segments (`w-0.5 h-8`, lines 228-233) — render the emerald fill as an `m.div` inside the slate track animating `scaleY` 0→1 with `style={{ transformOrigin: "top" }}` when that segment's step is complete; dots spring in on completion and the active dot pulses finitely, same recipe as the horizontal stepper.
  </action>
  <verify>
    <automated>cd /home/sanmi/IdeaProjects/JToye_OaaS_2026-motion/frontend && npm run build && grep -q 'jtoye:cart-updated' components/storefront/cart-provider.tsx && grep -q 'useCartCount' components/storefront/storefront-nav.tsx && grep -q 'AnimatePresence' 'app/shop/[slug]/page.tsx' && grep -q 'popLayout' app/dashboard/kitchen/page.tsx && grep -q 'useReducedMotion' hooks/use-count-up.ts && grep -q 'CHART_COLORS' app/dashboard/page.tsx && [ "$(grep -c '#3b82f6' app/dashboard/page.tsx)" = "0" ] && grep -q 'setInterval(fetchStatus, 15000)' 'app/shop/[slug]/orders/[orderNumber]/page.tsx'</automated>
  </verify>
  <done>Build green. Cart writes dispatch jtoye:cart-updated; nav badge on slug routes; FloatingCartBar springs via AnimatePresence with safe-area padding and all-viewport visibility preserved (deviation documented); modal footer has tap feedback; KDS grid FLIP-animates with one-shot ember glow for unseen order ids; KPI values count up via useCountUp (reduced-motion instant); no blue #3b82f6 literals remain in dashboard/page.tsx; both steppers animate with finite pulses and unchanged polling.</done>
</task>

<task type="auto">
  <name>Task 3: Tests, metrics refresh, full gates</name>
  <files>frontend/hooks/__tests__/use-cart-count.test.tsx, frontend/hooks/__tests__/use-count-up.test.tsx, frontend/components/ui/__tests__/skeleton.test.tsx, frontend/components/storefront/__tests__/storefront-nav-badge.test.tsx, frontend/components/__tests__/motion-provider.test.tsx, docs/metrics.json, CLAUDE.md</files>
  <action>
  All paths rooted at /home/sanmi/IdeaProjects/JToye_OaaS_2026-motion. Co-located `__tests__` dirs per repo convention; files MUST end `.test.tsx`/`.test.ts` (the docs-freshness counter only matches that suffix). Target 8-14 new `it`/`test` blocks total (D-13). Follow the existing precedent in frontend/components/storefront/__tests__/cart-provider.test.tsx (renderHook/act idioms, localStorage seeding).

  1. **use-cart-count.test.tsx** (~3-4 blocks): returns 0 when slug is undefined (SSR-safe default); reads the seeded `jtoye-cart-<slug>` localStorage state on mount; updates when `jtoye:cart-updated` fires with a matching slug detail (and ignores a non-matching slug); updates on a cross-tab `storage` event for the slug's key (construct `new StorageEvent("storage", { key: ... })`, re-seed localStorage first).

  2. **use-count-up.test.tsx** (~2 blocks): with reduced motion forced on, the hook returns the target immediately (deterministic approach: `jest.mock("framer-motion", () => ({ ...jest.requireActual("framer-motion"), useReducedMotion: () => true }))`); and a target of 0 renders 0. Do not attempt to assert intermediate tween frames — jsdom has no rAF timing guarantees.

  3. **skeleton.test.tsx** (~2 blocks): renders a div carrying the shimmer animation class (`animate-[shimmer_1.8s_ease-in-out_infinite]`) and merges a caller className.

  4. **storefront-nav-badge.test.tsx** (~3 blocks): mock next/navigation (`usePathname` → `/shop/test-shop`, `useParams` → `{ slug: "test-shop" }`) and `@/lib/customer-auth` (`getCustomerSession` resolving null); assert the basket link renders with the count from seeded localStorage; assert the badge count updates when `jtoye:cart-updated` is dispatched inside `act()`; assert no basket link when `useParams` returns `{}` (slug-less route). Wrap renders in the real `MotionProvider` so m components are exercised under LazyMotion strict.

  5. **motion-provider.test.tsx** (~1 block): MotionProvider renders its children.

  6. **Metrics + docs (D-13):** from the worktree root run `bash scripts/docs-freshness.sh --write` (the arbiter — regenerates docs/metrics.json; expect jest_blocks 249 → ~257-263 and total 1258 → matching). The script does NOT manage CLAUDE.md: manually update CLAUDE.md line 15's testing sentence (total logical invocations + Jest block/file counts) to the exact new values from docs/metrics.json. Grep the repo for the stale literals (`grep -rn "1258\|249 Jest" README.md docs/*.md` excluding metrics.json) and sync any other count references found.

  7. **Full gates (D-14):** `npm run build` in frontend/ (tsc typecheck — jest does NOT typecheck), full `npx jest` green (fix any fallout in pre-existing suites caused by Task 1/2 changes — e.g. layout or nav snapshots), `bash scripts/docs-freshness.sh` (check mode) clean. Confirm `git diff --stat frontend/package.json frontend/package-lock.json` is empty (D-01: no new deps).
  </action>
  <verify>
    <automated>cd /home/sanmi/IdeaProjects/JToye_OaaS_2026-motion && bash scripts/docs-freshness.sh && cd frontend && npx jest --silent 2>&1 | tail -5 && npm run build && git diff --quiet -- package.json package-lock.json</automated>
  </verify>
  <done>8-14 new Jest blocks across the 5 new test files, full `npx jest` suite green, `npm run build` green, docs-freshness check mode green with docs/metrics.json regenerated via --write, CLAUDE.md count line updated to match, package.json/package-lock.json untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage → UI | Cart counts read client-side; values are attacker-local only |
| window CustomEvent bus | `jtoye:cart-updated` is same-document; any script in-page could forge it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3na-01 | Tampering | use-cart-count / cart badge | accept | Cart totals/counts are display-only UX; price and order authority stay server-side (checkout re-validates). Forging the event or localStorage only mis-renders the forger's own badge. |
| T-3na-02 | Information Disclosure | jtoye:cart-updated detail | accept | Detail carries slug/itemCount/totalPennies — non-PII, already present in localStorage. |
| T-3na-SC | Tampering | npm installs | mitigate | Zero new dependencies (D-01); Task 3 gate asserts package.json/package-lock.json unchanged. No package legitimacy checkpoint needed. |
</threat_model>

<verification>
- `cd /home/sanmi/IdeaProjects/JToye_OaaS_2026-motion/frontend && npm run build` — tsc-clean production build
- `cd /home/sanmi/IdeaProjects/JToye_OaaS_2026-motion/frontend && npx jest` — full suite green (existing 249 + new blocks)
- `cd /home/sanmi/IdeaProjects/JToye_OaaS_2026-motion && bash scripts/docs-freshness.sh` — check mode passes
- `grep -rl 'import { motion }' frontend/app frontend/components frontend/lib frontend/hooks` → empty (strict LazyMotion safety)
- `git -C /home/sanmi/IdeaProjects/JToye_OaaS_2026-motion diff --quiet -- frontend/package.json frontend/package-lock.json` → no dependency changes
</verification>

<success_criteria>
- Brand tokens live: `--primary`/`--ring` are orange-600 HSL in :root and .dark; --trust/--ember-bright/--shadow-glow-ember/shimmer present; no component restyled beyond token indirection
- MotionProvider (LazyMotion strict domMax + MotionConfig reducedMotion="user") wraps the app; viewport export with themeColor media array present; all 10 `motion` imports converted to `m`
- lib/motion.ts, ui/skeleton.tsx, hooks/use-cart-count.ts, hooks/use-count-up.ts, lib/chart-colors.ts exist and are consumed as wired in key_links
- Storefront: cart writes broadcast jtoye:cart-updated; nav badge with spring pop + aria-live on slug routes; FloatingCartBar spring in/out + safe-area (all-viewport visibility preserved — D-08 deviation documented); modal footer tap feedback added
- KDS: popLayout AnimatePresence + layout FLIP re-sort; one-shot ember glow only for order ids unseen after first load
- Dashboard: KPI count-ups honoring reduced motion; zero #3b82f6 literals; Recharts fills via CHART_COLORS; pie Cell plumbing untouched
- Tracking: both steppers animate (scaled connector, spring dots, finite active pulse); 15s polling untouched
- Gates: build + full jest + docs-freshness all green; metrics.json and CLAUDE.md counts in sync; zero dependency changes
</success_criteria>

<output>
Create `/home/sanmi/IdeaProjects/JToye_OaaS_2026-motion/.planning/quick/260714-3na-motion-uplift-foundation-brand-tokens-la/260714-3na-SUMMARY.md` when done.
</output>
