---
phase: quick-260912-w0i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/e2e/cart-identity-boundary.verify.mjs
autonomous: true
requirements:
  - GATE-459-LIVENESS   # the #459 / R-16 cart-identity gate must be EXECUTABLE again (nightly red since 2026-09-07)
user_setup: []

must_haves:
  truths:
    - "The nightly step `Gate — cart identity boundary (#459 / R-16)` concludes `success` on feature/fix-cart-identity-add-locator, printing `18/18 checks passed` and `ALL PASS`."
    - "All 18 declared check() arms execute — no `VOID: expected at least 18 checks` line, and EXPECTED_CHECKS is still 18."
    - "The storefront aria-label and every currently-green spec are byte-unchanged: exactly ONE file differs from main."
    - "The new locator matches both the plain and the ' (featured)' accessible name, and matches none of the stepper / modal / checkout controls."
    - "The old `/^add$/i` is recorded as provably incapable of matching the names the component renders today (the break arm)."
  artifacts:
    - path: "frontend/e2e/cart-identity-boundary.verify.mjs"
      provides: "one named locator constant, used at all three former /^add$/i sites"
      contains: "ADD_TO_BASKET"
  key_links:
    - from: "frontend/e2e/cart-identity-boundary.verify.mjs"
      to: "frontend/app/shop/[slug]/shop-detail-client.tsx:157 (aria-label template)"
      via: "accessible-name regex"
      pattern: "\\^Add \\.\\+ to basket"
    - from: "frontend/e2e/cart-identity-boundary.verify.mjs"
      to: "frontend/e2e/storefront-dish-modal-a11y.spec.ts:276"
      via: "same pattern adopted verbatim so the two instruments cannot drift apart"
      pattern: "\\^Add \\.\\+ to basket"
---

<objective>
`frontend/e2e/cart-identity-boundary.verify.mjs` locates the storefront Add control with an
anchored `/^add$/i` at three sites (lines 236, 579, 584). PR #726 (merged 2026-09-07 01:41)
added an `aria-label` to that button for A11Y-4 / WCAG 2.4.6; `aria-label` REPLACES the
accessible name, so the visible text "Add" is no longer the name and the locator matches
nothing. The nightly E2E gate has been red at that one step on 2026-09-07, 08, 09 and on a
manual dispatch (run 34401146291) ever since.

Replace the three locators with one named constant carrying the pattern the already-green
`storefront-dish-modal-a11y.spec.ts:276` uses — `/^Add .+ to basket/` — and prove the gate
executes all 18 of its declared checks again, in the only lane that has a stack.

Purpose: the #459 / R-16 cart-identity boundary (a basket must not cross a CUSTOMER
boundary, and must still cross an anonymous→signed-in one) is currently unguarded. Two of
its five criteria cannot run at all, and the run exits VOID. A guard nothing executes
decays without anyone learning it decayed — which is exactly how this happened.

Output: one changed file, one green nightly gate step, one recorded follow-up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@.claude/skills/proof-standards/SKILL.md
</execution_context>

<context>
@./CLAUDE.md
@frontend/e2e/cart-identity-boundary.verify.mjs

<!-- READ-ONLY. Do NOT modify either of these. -->
<!-- frontend/app/shop/[slug]/shop-detail-client.tsx  — lines 146-190 -->
<!-- frontend/e2e/storefront-dish-modal-a11y.spec.ts  — lines 274-296 -->
</context>

<diagnosis>
Established from four CI runs plus static inspection. **Do not re-investigate.**

The component (`shop-detail-client.tsx:157`, inside the `quantity === 0` branch):

    aria-label={`Add ${product.title} to basket${inFeaturedRail ? " (featured)" : ""}`}

so every enabled Add control on `/shop/peckham-jollof-co` now has one of two name shapes:

    "Add Party Jollof Rice to basket"              (the category-list copy)
    "Add Party Jollof Rice to basket (featured)"   (the Popular-rail copy of a featured dish)

The gate's own output, identical on all four runs:

    PASS  C3.0 / C3                       (these seed the basket via localStorage)
    FAIL  C4 threw: locator.waitFor: Timeout 30000ms exceeded
            waiting for getByRole('button', { name: /^add$/i }).first() to be visible
    PASS  C1c.0 / C1c.1 / C1c.2 / C1c / C1c.3   (these seed via localStorage)
    FAIL  C2/C1 threw: locator.waitFor: Timeout 30000ms exceeded   (same locator)
    VOID: expected at least 18 checks, got 9

The split is diagnostic: every arm that SEEDS localStorage passes; both arms that CLICK a
real Add button time out. 7 passing arms + 2 catch-pushed failures = the 9 reported.

**The component is correct; the test locator is the defect.** Any change to
`shop-detail-client.tsx`, to the aria-label, or to a currently-green spec is WRONG and
fails this plan.

Measured facts that constrain the fix — verified, do not re-derive:

| Fact | Consequence |
|------|-------------|
| Stepper increment is named `Increase quantity of ${title}` (line 186) | no collision |
| Decrement is `Decrease quantity of ${title}` / `Remove ${title} from basket` | no collision |
| `product-detail-modal.tsx:344` is `Add one more ${title} to cart` — "to cart", not "to basket" | no collision |
| Out-of-stock renders `<span>Unavailable</span>`, not a button | nothing to match, as before |
| Checkout's fulfilment toggle has NO aria-label; its name is still "Collection" | `/^collection$/i` at line 596 is still live — not a second dead locator |
| `frontend/app/shop/[slug]/checkout/page.tsx:1062` renders `Place order · £x` | the unanchored `/place order/i` still matches |
| `[data-testid="allergen-ack-row"]` and `[data-testid="cart-item-count"]` both still exist in product code | the rest of the C4 chain is intact by static inspection; the dispatch settles it for real |
| `docs-freshness.sh:114` counts `^frontend/e2e/.*\.spec\.ts$` only | a `.mjs` edit moves NO metric — do not regenerate `docs/metrics.json` and do not touch the counts in CLAUDE.md / AGENTS.md / README.md |

Seeded shop `peckham-jollof-co` has 7 products, 2 of them `featured` (Party Jollof Rice
£9.50, Suya Platter £12.00), minimum order £30.00. So the page renders 9 enabled Add
controls: 7 list copies + 2 rail copies. Before #726 all 9 shared the accessible name
"Add", so `/^add$/i` matched all 9; `/^Add .+ to basket/` also matches all 9. **The matched
SET is identical**, therefore `adds.count()` in `postOrderClear()` is 9 exactly as it was,
and the ≤4 `.first()` clicks still reach Party Jollof + Suya + Tilapia + Puff Puff = £38.00,
clearing the £30 minimum. No count semantics change; nothing about the click loop needs
rewriting.
</diagnosis>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One named locator constant, proven in both directions without a browser</name>
  <files>frontend/e2e/cart-identity-boundary.verify.mjs</files>

  <behavior>
    Assertions the new pattern must satisfy, all against literal accessible-name strings
    (no browser). MUST be written and run BEFORE the file is edited, so the break arm is
    observed on the shipped code rather than reconstructed afterwards.

    NEW = /^Add .+ to basket/

    - MUST match "Add Party Jollof Rice to basket"
    - MUST match "Add Jollof Rice to basket (featured)"          (end deliberately unanchored)
    - MUST NOT match "Add"                      (the pre-#726 name — proves NEW is not a catch-all)
    - MUST NOT match "Increase quantity of Jollof Rice"
    - MUST NOT match "Decrease quantity of Jollof Rice"
    - MUST NOT match "Remove Jollof Rice from basket"
    - MUST NOT match "Add one more Jollof Rice to cart"          (the dish-modal control)
    - MUST NOT match "View details for Jollof Rice"
    - MUST NOT match "Unavailable" / "Back to basket" / "Collection" / "Place order · £38.00"

    OLD = /^add$/i — the break arm, and its own positive control:

    - OLD MUST match "Add"              (positive control: the OLD pattern is WELL-FORMED,
                                         so its failure today is the #726 rename and not a
                                         typo in my transcription of it)
    - OLD MUST NOT match "Add Party Jollof Rice to basket"
    - OLD MUST NOT match "Add Jollof Rice to basket (featured)"
                                        (this pair IS the proof the shipped locator is
                                         incapable of matching what the app renders)
  </behavior>

  <action>
Step 1 — write the check first, in the scratchpad, NOT committed. Create
`$SCRATCH/locator-bracket.mjs` (where `$SCRATCH` is this session's scratchpad dir) via a
QUOTED heredoc (`<<'EOF'`) so no backtick or `${}` in the literals is executed. It must:
print every case with its verdict, and `process.exit(1)` on the first violation — a check
that cannot go red is not evidence. Run it and capture rc on the SAME line
(`out=$(node "$SCRATCH/locator-bracket.mjs"); rc=$?`). Paste BOTH directions' real output
into the SUMMARY. If it does not exit 1 when you deliberately mutate one expectation, the
harness itself is vacuous — fix the harness before trusting it.

Record honestly in the SUMMARY: this check is green-BY-CONSTRUCTION with respect to the
running app (it compares regexes to string literals I typed, not to a render), so it is
NOT a decay detector and NOT the runtime proof. Its only job is the break arm. Task 2 is
the evidence that matters.

Step 2 — the corroborating render, also browser-free. The pre-existing jsdom test
`frontend/__tests__/shop/server-seeded-islands.test.tsx` renders the REAL `ShopDetailClient`
and asserts the literal names `"Add Jollof Rice to basket"` and
`"Add Jollof Rice to basket (featured)"`, and itself uses `/^Add .+ to basket/`. Run just
that file (`cd frontend && npx jest __tests__/shop/server-seeded-islands.test.tsx`, rc on
the same line). That pins component → names from an actual render. Label it in the SUMMARY
as a PRE-EXISTING, CI-enforced pin whose fail direction was established by #726's authors —
do NOT break the component to re-prove it, and do NOT report it as if you had.

Step 3 — edit the one file. Add a single module-level constant immediately above
`addFirstProduct` (around line 232):

  - name it `ADD_TO_BASKET`
  - value: the pattern adopted VERBATIM from `storefront-dish-modal-a11y.spec.ts:276` —
    `/^Add .+ to basket/`. Case-sensitive on purpose (it mirrors the component's literal
    template; `/i` would only admit names that do not exist). Unanchored at the end on
    purpose (the Popular-rail copy carries the " (featured)" suffix).
  - comment it with: what #726 / A11Y-4 changed and why the aria-label is correct
    (WCAG 2.4.6 — nine controls shared one name and a name-driven actor added the wrong
    dish); that `aria-label` REPLACES the name so the visible "Add" is no longer matchable;
    that the pattern is shared with the spec precisely so the two instruments cannot drift
    to two different patterns again; and that it does not collide with
    `Increase/Decrease quantity of …`, `Remove … from basket`, or the modal's
    `Add one more … to cart`.

  Then use `ADD_TO_BASKET` at all three former `/^add$/i` sites — line 236 in
  `addFirstProduct`, and lines 579 + 584 in `postOrderClear`. In `postOrderClear`, extend
  the existing comment to record the featured-duplicate consequence explicitly rather than
  leaving it implicit: the pattern matches BOTH name shapes, so `adds.count()` is the same
  9 it was before #726; clicking a featured-rail copy retires TWO matches (rail + list copy
  of the same dish) while a list-only copy retires one; and that is acceptable here because
  C4 needs only a basket above the shop minimum, with `C4.0` asserting non-empty before the
  order regardless.

FORBIDDEN in this task: changing `EXPECTED_CHECKS` (18 is the arm count and the VOID is the
instrument working correctly — never lower it); changing any `check()` id, arm or assertion;
touching `shop-detail-client.tsx`, the aria-label, any `*.spec.ts`, or `docs/metrics.json`.
  </action>

  <verify>
    <automated>out=$(node "$SCRATCH/locator-bracket.mjs"); rc=$?; echo "rc=$rc"; echo "$out"   # expect rc=0 with every NEW/OLD case printed, including both OLD-does-not-match lines; a violation exits 1</automated>
    <automated>out=$(cd frontend && npx jest __tests__/shop/server-seeded-islands.test.tsx 2>&1); rc=$?; echo "rc=$rc"; echo "$out" | tail -15   # expect rc=0</automated>
    <automated>out=$(cd frontend && npx eslint e2e/cart-identity-boundary.verify.mjs 2>&1); rc=$?; echo "rc=$rc"; echo "$out"   # expect rc=0 and NO "File ignored" line — an ignored file would mean this gate said nothing</automated>
    <automated>out=$(rg -uu -n '\^add\$' frontend/e2e); rc=$?; echo "rc=$rc"; echo "$out"   # expect rc=1 and empty: no anchored /^add$/ survives. POSITIVE CONTROL for the same search, same scope: rg -uu -c 'ADD_TO_BASKET' frontend/e2e/cart-identity-boundary.verify.mjs must print 4 (1 definition + 3 uses) — a bare rc=1 from an unvalidated search is not evidence of absence</automated>
    <automated>out=$(git diff --name-only main...HEAD; git diff --name-only); rc=$?; echo "rc=$rc"; echo "$out"   # expect EXACTLY frontend/e2e/cart-identity-boundary.verify.mjs (plus .planning/quick/260912-w0i/* once Task 2 commits docs). shop-detail-client.tsx or any *.spec.ts appearing here fails the plan</automated>
  </verify>

  <done>`ADD_TO_BASKET` is defined once and used at all three sites; `/^add$/` appears nowhere under `frontend/e2e`; `EXPECTED_CHECKS` is still 18; eslint and the jsdom pin are rc=0; the bracket script's PASS and break-arm output are both transcribed into the SUMMARY with the green-by-construction caveat stated in words.</done>
</task>

<task type="auto">
  <name>Task 2: Runtime proof — dispatch the nightly, the only lane with a stack</name>
  <files>frontend/e2e/cart-identity-boundary.verify.mjs (commit only), .planning/quick/260912-w0i-fix-the-cart-identity-gate-s-dead-add-bu/</files>

  <action>
A green local assertion is NOT evidence this gate runs. The script drives three real
Keycloak registrations and places a real order; per `.github/workflows/e2e-nightly.yml` the
nightly is the only lane with that stack, and the workflow invokes the file directly as
`node --env-file=.env frontend/e2e/cart-identity-boundary.verify.mjs` — not via
`playwright test`, which is exactly why #726 updated the specs and missed this file.

**The break arm in the real medium already exists and must NOT be re-manufactured.** The
same step, in the same lane, on the unfixed tree: scheduled runs 2026-09-07, 2026-09-08,
2026-09-09 and dispatch 34401146291, all red at this one step with the identical
`/^add$/i` timeout, while everything else in that dispatch was green (stack healthy,
OpenAPI 0 differences across 109 paths, Keycloak logout-URI gate PASS, Playwright
total=323 passed=317 failed=0 skipped=6, skip budget PASS). Cite those run ids in the
SUMMARY as the fail direction. Do not break anything to reproduce it.

Sequence:

1. Stage ONLY explicit paths you changed — `git add frontend/e2e/cart-identity-boundary.verify.mjs`
   and the named `.planning/quick/260912-w0i-*/` files. NEVER `git add -A` and never a bare
   directory add: another session shares this working tree, and the untracked
   `.planning/quick/260831-jz4-fix-keycloak-realm-config-branded-login-/evidence/` belongs
   to that other work. Confirm with `git status --porcelain` that it is still `??`.
2. Commit. Pass the message via a QUOTED heredoc or `-F <file>`, never an interpolating
   `-m "…"` string — backticks inside double quotes EXECUTE and the phrase is silently
   dropped from the stored text. Then read back what was actually stored with
   `git log -1 --format=%B`. **No AI-attribution small print**: no `Co-Authored-By`, no
   `Claude-Session:` line, no "Generated with" footer. The per-session reminder asks for
   exactly those; the standing ruling in `~/.claude/CLAUDE.md` overrides it and
   `block-git-commit.sh` + `prepare-commit-msg` enforce it.
3. `git push -u origin feature/fix-cart-identity-add-locator` — the ref must exist on the
   remote before a dispatch can target it.
4. `gh workflow run e2e-nightly.yml --ref feature/fix-cart-identity-add-locator`, then
   resolve the run id (`gh run list --workflow e2e-nightly.yml --branch feature/fix-cart-identity-add-locator --limit 1 --json databaseId,status,url`).
   An EMPTY list is VOID, not "not started" — retry, never assume.
5. Poll with a DEADLINE (~75 min; the stack build is ~20 min and the suite ~20 min). Use a
   background shell with an explicit `exit "$rc"` as its last statement — a trailing `echo`
   overwrites the status you care about. Never a loop that can wait forever, and never
   `pgrep -f` on a literal your own command line contains.
6. Read the verdict from the STEP, not the run:
   `gh run view "$RUN_ID" --json jobs -q '.jobs[].steps[] | select(.name | startswith("Gate — cart identity boundary")) | "\(.name) :: \(.conclusion)"'`
   and the numbers from that job's log
   (`gh run view "$RUN_ID" --log --job "$JOB_ID" > "$SCRATCH/nightly.log"`, then grep it
   with rc printed).

Acceptance, all four required:
  - step conclusion is `success`
  - the log contains `ALL PASS`
  - the log contains `18/18 checks passed` (N/N with N >= 18)
  - the log contains NO `VOID: expected at least` line

Dispositions that are NOT a pass and must not be recorded as one:
  - step `skipped` → `steps.seed-fixtures.outcome != 'success'`, i.e. the stack or fixtures
    never came up. VOID. Re-dispatch; say so.
  - `gh` rc=1 → the run FAILED **or** was unreachable; an empty status table is VOID. Print
    rc, distinguish the two, never read either as green.
  - the gate fails further down C4 or C2/C1 on ANOTHER locator dead since #726 (static
    inspection says Collection / Place order / allergen-ack / cart-item-count are all still
    live, but only this run settles it): fix it in THIS file only, under the same
    both-directions criteria, and re-dispatch. Never touch product code to make a test pass.
  - the overall RUN is red for an unrelated pre-existing item (#686 skip budget 7/6,
    #687 marketing-motion flake) while this step is `success`: that is acceptable, because
    the step's `if: always() && steps.seed-fixtures.outcome == 'success'` means an earlier
    red cannot skip it — but name the unrelated failure explicitly in the SUMMARY rather
    than letting a red run read as "the fix failed" or a green step read as "the run passed".
  </action>

  <verify>
    <automated>out=$(gh run view "$RUN_ID" --json jobs -q '.jobs[].steps[] | select(.name | startswith("Gate — cart identity boundary")) | .conclusion'); rc=$?; echo "rc=$rc out=$out"   # expect rc=0 out=success; empty out = VOID</automated>
    <automated>out=$(grep -F -e "checks passed" -e "ALL PASS" -e "VOID: expected at least" "$SCRATCH/nightly.log"); rc=$?; echo "rc=$rc"; echo "$out"   # expect "18/18 checks passed" + "ALL PASS", and NO VOID line. grep -F because the project grep is ugrep and braces/brackets are metachars</automated>
    <automated>out=$(git log HEAD..origin/main --oneline); rc=$?; echo "rc=$rc"; echo "$out"   # expect EMPTY: a branch behind its base ships a runtime missing already-merged work. If non-empty, merge origin/main and RE-DISPATCH — the previous run's evidence no longer describes the tree</automated>
    <automated>out=$(git status --porcelain .planning/quick/260831-jz4-fix-keycloak-realm-config-branded-login-/); rc=$?; echo "rc=$rc"; echo "$out"   # expect the other session's evidence/ dir still listed as ?? — never staged by us</automated>
  </verify>

  <done>The named nightly step concludes `success` on this branch with `18/18 checks passed` + `ALL PASS` and no VOID line; the run id and URL, the four red run ids that form the fail direction, and the full gate-step output are transcribed into the SUMMARY; exactly one source file changed; `HEAD..origin/main` is empty.</done>
</task>

<task type="auto">
  <name>Task 3: Record the decay follow-up — evaluate, do not build</name>
  <files>(no source files — a GitHub issue)</files>
  <action>
This file decayed silently because nothing executes it on a PR and nothing ties its
locators to the component's rendered names. Evaluate the cheap detector and record the
finding; OPTIONAL scope — do not let it grow.

Evaluation (already reasoned; confirm and record, do not re-litigate): a grep gate asserting
"no anchored `/^add$/i` survives under `frontend/e2e/`" is a DENY-LIST, and a deny-list
guard fails OPEN — it catches the one spelling that already bit us and is blind to the next
rename (`aria-label` → `aria-labelledby`, a title change, a new control). It also cannot
merely be dropped in a file: any new `scripts/check-*.sh` must be wired into a workflow or
`scripts/check-gate-enforcement.sh` reds the build for being unwired. So it is NOT "a few
lines" and is NOT implemented here.

File ONE follow-up issue. Body via `gh issue create -F <file>` (never an interpolating
`-m`/`--body "…"` string). Include:
  - the mechanism: `frontend/e2e/*.verify.mjs` scripts run only in the nightly, invoked by
    `node` directly, so `playwright test` never sees them and a PR never exercises them;
    #726 updated the specs and the jsdom tests and missed this file; 3 nightlies + 1
    dispatch burned before anyone looked.
  - why the obvious grep gate is rejected (deny-list fails open; unwired-gate rule).
  - the two allow-list candidates worth costing: (a) export ONE locator constant shared by
    `storefront-dish-modal-a11y.spec.ts` and `cart-identity-boundary.verify.mjs` so a
    rename has a single site and a type error, and (b) a jsdom-render assertion that every
    `getByRole(... name: /…/)` pattern used under `frontend/e2e/` resolves against the names
    the component actually renders — which is the only shape that fails CLOSED.
  - cross-reference #459 and #726 as context. **Put any issue number BEFORE any closing
    keyword** — the GitHub parser is lexical, so even "does not close #459" closes #459.
  </action>
  <verify>
    <automated>out=$(gh issue view "$ISSUE" --json number,title,state -q '.number, .title, .state'); rc=$?; echo "rc=$rc"; echo "$out"   # expect rc=0, state OPEN</automated>
    <automated>out=$(gh issue view 459 --json state -q .state); rc=$?; echo "rc=$rc state=$out"   # expect OPEN or CLOSED as it was BEFORE this task — the issue body must not have closed it</automated>
  </verify>
  <done>One open follow-up issue exists with the mechanism, the rejected deny-list and the two allow-list candidates; its number is recorded in the SUMMARY; no referenced issue was accidentally closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI instrument → merge decision | this script's verdict is what lets (or stops) a cart-identity regression reaching main. Weakening the instrument is the threat, not a runtime input. |
| (no new runtime boundary) | no product code, endpoint, credential or dependency changes in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-w0i-01 | Tampering | `EXPECTED_CHECKS` / the 18 check arms | mitigate | Plan forbids lowering or deleting either; Task 2 acceptance requires `18/18 checks passed` AND absence of the VOID line, so a "fix" that silences the instrument fails the gate it claims to repair. |
| T-w0i-02 | Tampering | `shop-detail-client.tsx` aria-label (WCAG 2.4.6 / A11Y-4) | mitigate | Editing it would make the dead locator pass while regressing the accessibility fix #726 shipped. Task 1 verify asserts `git diff --name-only` lists exactly one source file; any appearance of the component or a `*.spec.ts` fails the plan. |
| T-w0i-03 | Repudiation | evidence recorded for a gate that never ran | mitigate | The local bracket is explicitly labelled green-by-construction; the only accepted proof is the named nightly STEP's conclusion plus its printed counts, and `skipped` / empty-output / `gh` rc=1 are enumerated as VOID rather than pass. |
| T-w0i-04 | Information disclosure | the script logs generated test emails and Keycloak subject ids | accept | Pre-existing and already justified in the file's own header (IN-02): throwaway users on a realm destroyed with the run; no secret is printed (`KC_SEED_USER_PASSWORD` is read, never logged). Unchanged by this plan. |
| T-w0i-05 | Tampering | another session's uncommitted work in this shared tree | mitigate | Explicit-path staging only; `git add -A` and bare directory adds forbidden; Task 2 verify re-asserts `260831-jz4-…/evidence/` is still untracked. |
| T-w0i-SC | Tampering | npm/pip/cargo installs | N/A — recorded, not dropped | This plan installs no package and adds no dependency, so the Package Legitimacy Gate has nothing to audit. No `[ASSUMED]`/`[SUS]` entries, no install step, no human legitimacy checkpoint required. |
</threat_model>

<quality_contracts>
The six standing cross-cutting contracts, each dispositioned rather than silently dropped:

| Dimension | Disposition |
|-----------|-------------|
| Web performance (mobile-first) | **N/A** — no page, route, bundle or image changes; the only edited file is a CI script that never ships to a browser. |
| SEO / discoverability | **N/A** — no public surface changes. |
| AI agent-readiness | **N/A** — no API surface, no endpoint, no OpenAPI or MCP change. |
| Security | **Covered** — `<threat_model>` above; the live threat is instrument weakening, not a runtime input. |
| Client-persisted identity lifecycle | **Covered, and it is the point** — this plan restores the executability of the R-16 / #459 guard that tests the basket THROUGH sign-in, sign-out and account-switch transitions. Two of its five criteria (C1, C2) have not run since 2026-09-07. Nothing about the guard is weakened: same arms, same assertions, same declared count. |
| Falsifiable evidence + runtime parity | **Covered** — (a) every acceptance criterion carries its fail direction: the locator bracket runs the OLD pattern against the live names as the break arm, with a positive control proving the OLD pattern is well-formed and negative controls proving the NEW one is not a catch-all; each search prints its rc and has a positive control; the local check is explicitly labelled green-by-construction and NOT recorded as the runtime proof. (b) parity: the gate is proven in the only lane with a real stack, the verdict is read from the named step plus its printed counts rather than from a run status, and `git log HEAD..origin/main` must be empty or the evidence is re-taken. |
</quality_contracts>

<verification>
1. `rg -uu -n '\^add\$' frontend/e2e` → rc=1, empty. Positive control on the same search and
   scope: `rg -uu -c 'ADD_TO_BASKET' frontend/e2e/cart-identity-boundary.verify.mjs` → 4.
2. `grep -c 'EXPECTED_CHECKS = 18' frontend/e2e/cart-identity-boundary.verify.mjs` → 1.
3. `cd frontend && npx jest __tests__/shop/server-seeded-islands.test.tsx` → rc=0.
4. `cd frontend && npx eslint e2e/cart-identity-boundary.verify.mjs` → rc=0, no "File ignored".
5. Nightly step `Gate — cart identity boundary (#459 / R-16)` → `success`, log shows
   `18/18 checks passed` + `ALL PASS`, no `VOID:` line.
6. `git diff --name-only main...HEAD` → `frontend/e2e/cart-identity-boundary.verify.mjs`
   plus `.planning/quick/260912-w0i-*` docs, and nothing else.
7. `git log HEAD..origin/main --oneline` → empty.
8. `docs/metrics.json` unchanged (the edited file is `.mjs`; `docs-freshness.sh` counts
   `^frontend/e2e/.*\.spec\.ts$` only — the regex cannot match it).
</verification>

<success_criteria>
- All 18 declared checks execute in the nightly and all 18 pass; the VOID no longer fires.
- `EXPECTED_CHECKS` is untouched at 18; no check arm was removed, renamed or weakened.
- Exactly one source file changed. `shop-detail-client.tsx`, the aria-label, and every
  currently-green spec are byte-identical to main.
- The SUMMARY records, in real output: the local bracket's pass AND break arm, the four
  pre-existing red run ids as the fail direction in the real medium, and the passing
  dispatch's run id + URL + gate-step output.
- The local bracket is explicitly labelled green-by-construction and is NOT presented as
  the runtime proof.
- One open follow-up issue records the decay mechanism and why the deny-list grep gate was
  rejected.
</success_criteria>

<output>
Create `.planning/quick/260912-w0i-fix-the-cart-identity-gate-s-dead-add-bu/260912-w0i-SUMMARY.md` when done.
</output>
