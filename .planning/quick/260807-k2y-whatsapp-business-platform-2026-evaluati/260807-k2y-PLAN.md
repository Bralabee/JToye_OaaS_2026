---
quick_id: 260807-k2y
description: WhatsApp Business Platform 2026 evaluation doc
date: 2026-08-07
mode: quick
docs_only: true
---

# Quick Task 260807-k2y: WhatsApp Business Platform 2026 evaluation

## Goal

Record an evidence-backed evaluation of the 2026 WhatsApp Business Platform changes
against the WhatsApp integration **as actually built in this repo**, answering the
build-vs-connect question: which capabilities Meta now ships off-the-shelf, which
OaaS must still build, and which 2026 change breaks current code.

Same document shape as `docs/analysis/MESSAGING-BROKER-EVALUATION-2026-07-26.md` —
an external-claims-vs-this-codebase evaluation with a method section, not a survey.

## Scope

**Docs only. No code changes, no config changes, no migrations.**

The doc records findings; acting on them is a separate planning decision (a phase,
or issues). Nothing here edits `edge-go/` or `core-java/`.

## Tasks

### Task 1 — Write the evaluation document

- **files**: `docs/analysis/WHATSAPP-BUSINESS-PLATFORM-EVALUATION-2026-08-07.md` (new)
- **action**: Write the evaluation. Sections: verdict; what is actually built
  (inbound edge-go path, outbound INERT stub) with `file:line` citations; the 2026
  platform changes that bear on it; a connect-vs-build table; the defects the review
  surfaced; recommendation; method + falsifiability (including what could NOT be
  verified).
- **verify**: every `file:line` citation resolves and the cited line carries a strong
  token from the claim — proven by running the repo's own citation gate against the
  new doc: `CITATION_DOCS="docs/analysis/WHATSAPP-BUSINESS-PLATFORM-EVALUATION-2026-08-07.md" bash scripts/check-doc-citations.sh`
- **done**: gate exits 0 on the doc, and is shown to exit non-zero on a deliberately
  corrupted copy (fail-direction arm), then the clean state re-asserted.

### Task 2 — Index the document

- **files**: `docs/analysis/README.md`
- **action**: Append a row to the Documents table, matching the existing
  one-line-summary style used for the other dated evaluations.
- **verify**: link target exists on disk.
- **done**: `docs/analysis/README.md` lists the new document.

## Out of scope

- Any change to `edge-go/internal/whatsapp/parser.go` or the webhook handler
- Enabling or reconfiguring the `WhatsAppSmsChannel` stub
- Opening GitHub issues (the doc names candidates; filing them is the user's call)
- Choosing between Meta-direct and a BSP — the doc frames the decision, does not make it

## Notes

Branch: `docs/issue-triage-disposition` (`branch_name` was null in init, so the
current branch is used). `.planning/` artifacts and the docs commit are separate.
