---
phase: 260708-jj1-issue-77-p0-1-add-frontend-api-health-ro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/api/health/route.ts
  - frontend/app/api/health/__tests__/route.test.ts
  - docker-compose.full-stack.yml
  - docs/metrics.json
  - CLAUDE.md
autonomous: true
requirements: [ISSUE-77-P0-1]
branch: fix/77-frontend-health-probe

must_haves:
  truths:
    - "GET /api/health returns HTTP 200 with no auth cookie present"
    - "Response body is exactly {\"status\":\"ok\"}"
    - "docker-compose.full-stack.yml frontend healthcheck targets /api/health"
    - "docs-freshness.sh exits 0 after the new Jest test is counted"
    - "Rebuilt frontend container reports Health.Status=healthy and serves /api/health on host :3100"
  artifacts:
    - path: "frontend/app/api/health/route.ts"
      provides: "Unauthenticated GET health handler"
      contains: "force-dynamic"
    - path: "frontend/app/api/health/__tests__/route.test.ts"
      provides: "Jest contract test for the health route"
      contains: "describe"
    - path: "docs/metrics.json"
      provides: "Regenerated test-count manifest (jest_blocks=102, jest_files=18, total=694)"
      contains: "jest_blocks"
  key_links:
    - from: "k8s liveness/readiness probes + frontend/Dockerfile HEALTHCHECK + compose healthcheck"
      to: "GET /api/health route"
      via: "path equivalence (all target /api/health on container :3000)"
      pattern: "app/api/health/route"
    - from: "docs/metrics.json"
      to: "source test counts"
      via: "scripts/docs-freshness.sh recount"
      pattern: "jest_blocks"
---

<objective>
Issue #77 [P0-1]: k8s liveness AND readiness probes for the frontend tier target `GET /api/health:3000`, and `frontend/Dockerfile` HEALTHCHECK hits `http://localhost:3000/api/health` — but that route does not exist. In Kubernetes the UI pod fails its readiness/liveness checks and crash-loops. The route only has to EXIST; the probes and Dockerfile are already correct and MUST NOT be touched.

Purpose: Stop the frontend tier from crash-looping in k8s by adding the missing health route, aligning the docker-compose healthcheck that currently masks the bug (it probes `/`), and proving the fix live in a rebuilt container.

Output:
- `frontend/app/api/health/route.ts` (new) — unauthenticated GET returning `{status:"ok"}` / 200
- `frontend/app/api/health/__tests__/route.test.ts` (new) — Jest contract test
- `docker-compose.full-stack.yml` — frontend healthcheck aligned to `/api/health`
- `docs/metrics.json` + `CLAUDE.md` — test counts kept honest (docs-freshness CI gate)
- Live verification: `curl` + `docker inspect` proof against the running :3100 stack
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- Route-handler idiom, verified from the codebase. Use directly — no exploration needed. -->

Existing route handler pattern — frontend/app/api/customer-auth/session/route.ts:
```typescript
import { NextRequest, NextResponse } from "next/server"
export async function GET(_req: NextRequest) {
  return NextResponse.json({ authenticated: false }, { status: 401 })
}
```

Existing Jest test pattern — frontend/app/api/customer-auth/__tests__/route.test.ts (node env, direct handler import):
```typescript
/**
 * @jest-environment node
 */
import { GET as sessionGET } from "../session/route"
import { NextRequest } from "next/server"

describe("/api/customer-auth/session", () => {
  it("returns 401 without cookies", async () => {
    const req = new NextRequest("http://localhost/api/customer-auth/session")
    const res = await sessionGET(req)
    expect(res.status).toBe(401)
  })
})
```

Jest discovery — frontend/jest.config.js `testMatch` includes `**/__tests__/**/*.[jt]s?(x)`, so a file at
`frontend/app/api/health/__tests__/route.test.ts` is auto-discovered. Test script: `npm test` → `jest`.

Compose healthcheck to change — docker-compose.full-stack.yml line ~238 (service `frontend`, container_name `jtoye-frontend`):
```yaml
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:3000"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s
```

docs-freshness Jest counting rule — scripts/docs-freshness.sh counts `\b(it|test)\(` occurrences across
`^frontend/(app|components|lib|hooks|types|__tests__)/.*\.test\.tsx?$` (includes untracked-but-not-ignored files).
`total_logical_invocations = java_test_methods + jest_blocks + go_test_funcs + playwright_blocks`.
The script gates ONLY docs/metrics.json — CLAUDE.md line 15 mirrors the numbers and must be kept consistent by hand.

DO NOT MODIFY (already correct): k8s/base/frontend-deployment.yaml, frontend/Dockerfile, frontend/middleware.ts
(matcher is `["/dashboard/:path*"]`, so `/api/health` is NOT auth-intercepted).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add /api/health route + Jest contract test</name>
  <files>frontend/app/api/health/route.ts, frontend/app/api/health/__tests__/route.test.ts</files>
  <behavior>
    - Test 1: `GET()` resolves to a response with `status === 200`
    - Test 2: awaiting `res.json()` deep-equals `{ status: "ok" }`
    (Exactly TWO `it()` blocks — this exact count feeds the metrics update in Task 2: +2 jest_blocks, +1 jest_file.)
  </behavior>
  <action>
Create `frontend/app/api/health/route.ts` following the codebase route-handler idiom (see <interfaces>):
import `NextResponse` from `next/server`; export `async function GET()` (no params — the test calls `GET()` with no args) returning `NextResponse.json({ status: "ok" }, { status: 200 })`; add `export const dynamic = "force-dynamic"` so the route is never prerendered or cached (a liveness/readiness probe must hit live code). No auth, no cookie reads, no request body — this endpoint is intentionally public and matches the exact path the k8s probes and Dockerfile HEALTHCHECK already call.

Create `frontend/app/api/health/__tests__/route.test.ts` following the customer-auth test conventions: start with the `/** @jest-environment node */` docblock, `import { GET } from "../route"`, wrap in a `describe("/api/health", ...)` with exactly the two `it()` blocks from <behavior>. Do NOT add extra cases — the block count is load-bearing for docs-freshness.

Run the new test scoped, capture and RECORD the output:
`cd frontend && npx jest app/api/health --verbose`
(Host node_modules exist. If host jest cannot run, fall back to a throwaway container: `docker run --rm -v "$PWD/frontend":/app -w /app node:20 npx jest app/api/health --verbose`.)

Then run the FULL frontend suite to prove no regression: `cd frontend && npm test`. Record the summary line (expect the pre-existing suite still green plus the 2 new blocks).

Commit exactly these two new files (atomic, conventional prefix, NO Co-Authored-By trailer):
`feat(frontend): add /api/health route for k8s liveness/readiness probes (#77)`
Note: this intermediate commit intentionally leaves docs/metrics.json stale until Task 2 — docs-freshness evaluates the final PR tree, and the two commits together are consistent.
  </action>
  <verify>
    <automated>cd frontend && npx jest app/api/health --verbose && npm test</automated>
  </verify>
  <done>Both new files exist; the 2 new `it()` blocks pass; full frontend Jest suite still passes; one atomic commit created on fix/77-frontend-health-probe.</done>
</task>

<task type="auto">
  <name>Task 2: Align compose healthcheck + refresh test-count metrics</name>
  <files>docker-compose.full-stack.yml, docs/metrics.json, CLAUDE.md</files>
  <action>
1. In `docker-compose.full-stack.yml`, change ONLY the frontend service healthcheck `test` line (~line 238) from
`["CMD", "wget", "--spider", "-q", "http://127.0.0.1:3000"]` to
`["CMD", "wget", "--spider", "-q", "http://127.0.0.1:3000/api/health"]`.
Keep `interval`, `timeout`, `retries`, `start_period` unchanged. This removes the mask — the compose healthcheck was probing `/` (which always 200s) and hiding the missing route.

2. Regenerate the test-count manifest using the script's own documented mechanism:
`bash scripts/docs-freshness.sh --write`
Then diff the result against the committed version and CONFIRM only the Jest fields moved: `jest_blocks 100→102`, `jest_files 17→18`, `total_logical_invocations 692→694`. If ANY other field changed, stop and investigate — that signals unexpected drift, not this task's work.

3. Keep `CLAUDE.md` line 15 consistent with metrics.json (the script does NOT gate CLAUDE.md, but the constraint requires the mirrored paragraph to match): update `692 logical invocations`→`694`, `100 Jest \`it/test\` blocks across 17 files`→`102 Jest \`it/test\` blocks across 18 files`. Leave the Java/Go/Playwright figures untouched.

4. Verify the gate passes in check mode: `bash scripts/docs-freshness.sh` must print `docs-freshness OK` and exit 0.

Commit exactly these three files (atomic, conventional prefix, NO Co-Authored-By trailer):
`test(frontend): align compose healthcheck to /api/health and refresh metrics (#77)`
  </action>
  <verify>
    <automated>bash scripts/docs-freshness.sh; test $? -eq 0 && grep -q 'api/health' docker-compose.full-stack.yml && grep -q '"jest_blocks": 102' docs/metrics.json</automated>
  </verify>
  <done>Compose frontend healthcheck targets /api/health; docs/metrics.json shows jest_blocks=102, jest_files=18, total=694; CLAUDE.md paragraph matches; docs-freshness.sh exits 0; one atomic commit created.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Task 1 added the `/api/health` route + test; Task 2 aligned the compose healthcheck. This checkpoint rebuilds ONLY the frontend image against the already-running :3100 stack and proves the route is live and the container reports healthy (per the rebuild-containers rule). This task changes no files and produces no commit.

Run these and RECORD each result:

1. Rebuild + recreate ONLY the frontend service (do not touch other containers):
   `docker compose -f docker-compose.full-stack.yml -f docker-compose.frontend-3100.yml build frontend`
   `docker compose -f docker-compose.full-stack.yml -f docker-compose.frontend-3100.yml up -d frontend`
   (The `up -d frontend` recreates the container so it picks up both the new image and the new healthcheck.)

2. (a) HTTP status, no auth cookie sent:
   `curl -s -o /dev/null -w '%{http_code}\n' http://localhost:3100/api/health`  → expect `200`
   (b) Response body:
   `curl -s http://localhost:3100/api/health`  → expect `{"status":"ok"}`
   (c) Container health (poll through the 40s start_period until it settles):
   `docker inspect --format '{{.State.Health.Status}}' jtoye-frontend`  → expect `healthy`
   (d) Path equivalence: the `frontend/Dockerfile` HEALTHCHECK and both k8s probes call `GET /api/health` on container port 3000 — the same route this plan added. No k8s/Dockerfile change was needed or made.
  </what-built>
  <how-to-verify>
Confirm all four recorded results: (a) 200, (b) exactly `{"status":"ok"}`, (c) `healthy`, (d) probe/route path equivalence holds. If the container is not yet `healthy`, wait past the 40s start_period and re-inspect before failing.
  </how-to-verify>
  <resume-signal>Type "approved" once (a)-(d) are confirmed, or describe any mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/k8s kubelet → frontend `/api/health` | Unauthenticated request crosses into the Next.js runtime by design (probes cannot present credentials) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01 | Information Disclosure | `GET /api/health` | mitigate | Response is a fixed literal `{"status":"ok"}` — no version, build info, tenant data, or internal state is emitted |
| T-77-02 | Denial of Service | unauthenticated `/api/health` | accept | Trivial O(1) handler, no DB/IO; edge-go rate limiter and k8s ingress already front the tier; standard for a liveness probe |
| T-77-03 | Elevation of Privilege | middleware bypass | accept | Intentionally public; `frontend/middleware.ts` matcher `["/dashboard/:path*"]` excludes `/api/health`, so no auth path is weakened |
| T-77-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan — only existing `next` runtime imports are used |
</threat_model>

<verification>
- New route file exists with `force-dynamic` and no auth logic.
- Scoped Jest test passes (2 blocks) and full frontend `npm test` stays green.
- `docker-compose.full-stack.yml` frontend healthcheck path = `/api/health`.
- `bash scripts/docs-freshness.sh` exits 0; metrics.json = jest_blocks 102 / jest_files 18 / total 694; CLAUDE.md consistent.
- Live: `curl` → 200 + `{"status":"ok"}` with no cookie; `jtoye-frontend` container `healthy`.
- k8s manifests + frontend/Dockerfile unchanged (path-equivalent to the new route).
</verification>

<success_criteria>
- `/api/health` returns 200 `{"status":"ok"}` unauthenticated, live on the rebuilt :3100 container.
- Frontend container reports `Health.Status=healthy`, ending the crash-loop condition #77 describes.
- CI gates stay green: full Jest suite passes; docs-freshness exits 0.
- Exactly two atomic commits (Task 1 route+test, Task 2 compose+metrics+CLAUDE) on `fix/77-frontend-health-probe`, conventional prefixes, no Co-Authored-By trailers. Task 3 adds no commit.
</success_criteria>

<output>
This is a quick task — no SUMMARY.md required unless the quick-task workflow requests one. Report the recorded verification results (Task 1 test output, docs-freshness exit code, Task 3 curl/inspect results) back to the orchestrator.
</output>
