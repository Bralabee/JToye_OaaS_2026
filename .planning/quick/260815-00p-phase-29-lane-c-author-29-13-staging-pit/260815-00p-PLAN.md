---
phase: 260815-00p-phase-29-lane-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/staging-pitr-drill.sh
  - scripts/check-deploy-digest-parity.sh
  - .github/workflows/ci-cd.yaml
  - docs/architecture/SYSTEM_DESIGN_V2.md
  - docs/runbooks/backups.md
  - k8s/DEPLOYMENT.md
  - .planning/phases/29-deployable-staging-with-its-own-monitoring/29-LANE-C-OFFLINE-AUTHORED.md
autonomous: true
requirements: [DPLY-01, DPLY-02, DPLY-04]

must_haves:
  truths:
    - "The PITR drill exists as a runnable script that cannot leave a billable Azure server behind, and the trap has been SHOWN to fire on a mid-drill failure"
    - "The drill cannot touch a cloud resource without an explicitly named subscription, and cannot be run by accident against the ambient (employer) default"
    - "CI's staging deploy authenticates with a short-lived federated Azure credential and the workflow contains no long-lived kubeconfig"
    - "A deploy that rolled out an image other than the one built for that commit fails the job, and a deploy that cannot read a digest fails too — both demonstrated against fabricated inputs"
    - "Production deploy flags remain OFF and nothing in this change makes anything look live"
    - "The repository's remaining WAL-G PITR direction is superseded in writing by ADR-0002, and the correction is NAMED as a correction"
    - "What this lane could NOT prove is written down as owed, not implied as done"
  artifacts:
    - path: "scripts/staging-pitr-drill.sh"
      provides: "Two-arm PITR + logical-dump drill with an EXIT trap that deletes the restored server, an explicit-subscription refusal, and an offline dry-run/failure-injection harness"
      min_lines: 150
    - path: "scripts/check-deploy-digest-parity.sh"
      provides: "The post-rollout digest assertion as a standalone, offline-falsifiable gate rather than an untestable inline run: block"
      min_lines: 80
    - path: ".planning/phases/29-deployable-staging-with-its-own-monitoring/29-LANE-C-OFFLINE-AUTHORED.md"
      provides: "What was authored offline, what remains OWED to 29-13/29-15 on a live cluster, and the measured findings that make both plans' stated premises stale"
      contains: "OWED"
  key_links:
    - from: "scripts/staging-pitr-drill.sh"
      to: "the restored Flexible Server"
      via: "a trap on EXIT that deletes it — PITR always creates a SECOND billable server"
      pattern: "trap"
    - from: ".github/workflows/ci-cd.yaml deploy-staging"
      to: "the Entra federated identity credential"
      via: "azure/login pinned by SHA, with id-token: write granted at JOB level only"
      pattern: "azure/login"
    - from: ".github/workflows/ci-cd.yaml deploy-staging"
      to: "scripts/check-deploy-digest-parity.sh"
      via: "a step invoked after the rollout waits, in the same shape as the DPLY-05 NetworkPolicy gate"
      pattern: "check-deploy-digest-parity.sh"
---

<objective>
Phase 29 is PAUSED at the wave-7 boundary on two owner actions (staging DNS A records, seven secret
values). Plans 29-13 and 29-15 both `depends_on: ["29-11"]` — but **their dependency is on their
EVIDENCE, not on their CODE**. This lane writes everything that can be written with no cluster, no
Azure call and no billable resource, so that when the estate comes back the remaining work is
*running* things rather than *writing* them.

Purpose: convert two blocked plans into two plans whose only remaining work is execution.
Output: the PITR drill script, the federated deploy path, the digest-parity gate, the doc
corrections that are already provably needed, and an honest record of what is still owed.

**WHAT THIS LANE EXPLICITLY DOES NOT CLAIM.** Neither deliverable can be PROVEN here. The drill
cannot run (it needs the staging Postgres and would create a second billable server; `jtoye-staging-aks`
and `snackpass-pg` are both deliberately Stopped). The deploy job cannot go green (it needs the
cluster, the owner's repo variables and a push to main). Every acceptance criterion below is about
what IS checkable offline — syntax, `actionlint`, the trap firing on an injected failure, the
digest logic exercised against fabricated inputs. The end-to-end proof is **OWED** to 29-13 and
29-15 on a live cluster and is **NOT** claimed by this plan or by its summary.

Branch: `phase-29-research` (current, clean, 0 behind `origin/main`). Never commit to main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/29-deployable-staging-with-its-own-monitoring/29-13-PLAN.md
@.planning/phases/29-deployable-staging-with-its-own-monitoring/29-15-PLAN.md
@.planning/phases/29-deployable-staging-with-its-own-monitoring/29-PROVISIONING-EVIDENCE.md
@scripts/check-networkpolicy-enforcement.sh
@scripts/gates/gate-enforcement.conf
@docs/runbooks/backups.md
@.github/workflows/ci-cd.yaml

<measured_starting_state>
<!-- MEASURED 2026-08-15 on this checkout. Do NOT re-derive these from the plans' prose;
     several of the plans' own stated premises are now STALE and are corrected here. -->

  scripts/staging-pitr-drill.sh                  ABSENT
  scripts/check-networkpolicy-enforcement.sh     EXISTS, 349 lines  (29-14's — READ ONLY, do not edit)
  scripts/restore-drill.sh                       EXISTS, 291 lines  (the local/compose drill — read for shape)
  .github/workflows/ci-cd.yaml                   1455 lines; deploy-staging at 1120-1291
  rg -uu -n 'azure/login' .github/workflows/     rc=1, ZERO hits — the federated path does not exist yet

  TOOLING ON THIS HOST (measured with `command -v`):
    shellcheck   MISSING          <- 29-13's own verify line calls `shellcheck -S error`; it would be rc=127
    actionlint   /home/sanmi/go/bin/actionlint   (may NOT be on the executor's PATH — resolve it, never assume)
    az           /usr/bin/az
    psql, pg_restore, python3, yamllint  present
  `rg -uu -n 'shellcheck' .github/workflows/` -> ONE hit, and it is a `# shellcheck disable=` comment
  in e2e-nightly.yml. **No CI job runs shellcheck.** So a shellcheck pass is not an existing contract
  and its absence here is a recorded VOID, never a silent pass.

  STALE PREMISE 1 — 29-13 Task 3's WAL-G target is already gone.
  29-13 says `docs/architecture/SYSTEM_DESIGN_V2.md:657` claims "WAL-G to S3 (PITR)". It does not.
  PR #197 (docs/CHANGELOG.md:2183) already relabelled that content TARGET. `rg -uu 'WAL-G'` now finds,
  in tracked non-worktree files:
    SYSTEM_DESIGN_V2.md:890   "Backup: WAL-G to S3 (PITR, 30-day retention)"  <- INSIDE an ASCII box
                              under a "⚠️ CURRENT STATE … none of the diagram below is deployed"
                              callout and the line "The diagram below is the TARGET architecture".
    SYSTEM_DESIGN_V2.md:1466  "WAL Archive: NONE — no archive_command, no WAL-G, no pgBackRest"
                              <- a TRUE negative statement. Do not touch it.
  So there is no false claim left to delete. What IS now wrong is different and real: **ADR-0002 is
  ACCEPTED (plan 29-01) and chose provider PITR, so the TARGET diagram still names a superseded
  direction.** That is the correction this lane makes, and it is fully offline.
  OUT OF SCOPE: the WAL-G hits under `.claude/worktrees/agent-acedd037d6b3648d0/` (a parked worktree
  that must survive untouched) and the two historical `docs/analysis/REMEDIATION-BACKLOG` /
  `.planning/quick/260708-bu6` records, which are dated findings and correctly say what was true then.

  STALE PREMISE 2 — 29-15's line numbers moved. The `Configure kubeconfig` step is at **1207-1210**,
  not 1194-1197. deploy-staging spans **1120-1291**. Read, do not trust the offsets.

  MECHANISM CORRECTION already recorded in the runbook (backups.md, "One correction to the
  mechanism"): arm A's `pg_dump` as the app role does **not** silently succeed. `pg_dump` requests
  `row_security=off`, Postgres REFUSES it for a non-BYPASSRLS role on a FORCE-RLS table, and pg_dump
  **exits 1**. The artifact left behind is PARTIAL — and that partial artifact is what still clears
  `MIN_BACKUP_BYTES` and `pg_restore --list` while restoring to zero rows. The drill's arm A must
  therefore expect a NON-ZERO pg_dump rc and operate on the partial file; a script written around
  "arm A's dump succeeds" would fail on the true behaviour of this database.
</measured_starting_state>

<interfaces>
<!-- House style for a cluster-touching script with control arms: scripts/check-networkpolicy-enforcement.sh.
     Copy its shape, not its content. The load-bearing parts, by line: -->

  :100      set -uo pipefail            (NOT -e — VOIDs are decided explicitly, never by exit-on-error)
  :106      void() { echo "VOID: $*" >&2; exit 2; }
  :110-125  every knob env-overridable with a documented default
  :128-137  explicit argv parse; an UNKNOWN argument is VOID —
            "refusing to guess what was meant while holding cluster credentials"
  :141-142  command -v <tool> || void
  :144-151  refusal to inherit the ambient context, with the employer-cluster hazard named
  :156-161  FORBIDDEN list honoured even when the forbidden target is named explicitly —
            "intent is not a safety mechanism"
  :163-166  `command grep -Fxq -- "$X" <<<"$LIST"` — a HERE-STRING, never `cmd | grep -q`
  :180-186  "no target is VOID, never clean"
  :193-200  cleanup() with --ignore-not-found, idempotent, installed as a trap
  :95       "NOTE ON docs/metrics.json: contributes 0. docs-freshness.sh counts no bash."

<!-- The gate-wiring rule, and the trap in it. scripts/check-gate-enforcement.sh:82 enumerates
     ONLY `find scripts -maxdepth 1 -type f -name 'check-*.sh'`. Therefore: -->

  scripts/staging-pitr-drill.sh is NOT a check-*.sh -> the gate never sees it -> there is NOTHING to
  declare. **DO NOT add it to scripts/gates/gate-enforcement.conf.** That file's own comment block
  records the MEASUREMENT (2026-08-10): adding a non-`check-*.sh` name made check-gate-enforcement.sh
  exit **2 (VOID)** — "a table that names nothing cannot be trusted to name the right things" — taking
  the whole gate-wiring check offline for every gate on every PR. The precedent for recording the
  reasoning as a COMMENT (which the parser ignores) is the three `staging-*.sh` scripts already
  documented there. Follow that precedent exactly.

  scripts/check-deploy-digest-parity.sh IS a check-*.sh -> the gate WILL enumerate it -> it must be
  referenced by a workflow or it fails default-deny. It is referenced from ci-cd.yaml by Task 2, which
  satisfies the gate. It must NOT be added to gate-enforcement.conf.

<!-- deploy-staging as it stands (ci-cd.yaml). Everything downstream of the credential step is
     already correct and must be left byte-unchanged: -->

  1120-1131  job header, needs: [build-and-push, k8s-validate], the DEPLOY_STAGING_ENABLED `if:`,
             environment: staging.  NO `permissions:` block exists on this job.
  1178-1205  CR-02 baked-frontend-origin guard — KEEP, unchanged
  1207-1210  Configure kubeconfig (secrets.KUBE_CONFIG_STAGING)  <- THE STEP THIS PLAN DELETES
  1212-1231  kustomize edit set image + the premortem render assertion + `kubectl apply -k`  — KEEP
  1233-1237  rollout waits for core-java / edge-go / frontend   — KEEP; the new digest step goes AFTER
  1239-1253  in-cluster actuator health check + smoke tests     — KEEP
  1275-1284  the DPLY-05 NetworkPolicy gate step — COPY ITS SHAPE for the digest step
  1286-1291  Rollback on failure                                 — KEEP, must remain LAST

  Workflow-level `permissions:` — there is NONE. Five jobs carry their own (lines 162/246/653/850/972).
  deploy-staging carries none, so adding one both ENABLES OIDC and TIGHTENS the job. That is the
  correct direction and the scope must be asserted, not merely the presence.

  build-and-push is a MATRIX job (`strategy.matrix.service: [core-java, edge-go, frontend]`, line 996)
  and its `docker/build-push-action` step carries NO `id:`. **Matrix job outputs are last-writer-wins
  in GitHub Actions**, so per-service digests CANNOT be plumbed through job outputs. The expected
  digest must be resolved from the REGISTRY at deploy time.

  The federated credential's real values live in 29-PROVISIONING-EVIDENCE.md §2.4 (client id, tenant
  id, principal id, and the exact subject `repo:Bralabee/JToye_OaaS_2026:environment:staging`,
  issuer `https://token.actions.githubusercontent.com`, audience `api://AzureADTokenExchange`,
  **no client secret**). The workflow REFERENCES them as `secrets.AZURE_*`; it never contains them.
  Do not copy the GUIDs into this plan's outputs either — `.planning` CONTEXT files are not gitleaks-
  allowlisted, and there is no reason to duplicate them.

  Azure estate facts the drill needs (29-PROVISIONING-EVIDENCE.md §2.3):
    server  jtoye-staging-pg   rg jtoye-rg   Standard_B2s / Burstable   32 GiB   7-day retention
    fqdn    jtoye-staging-pg.postgres.database.azure.com   public access Enabled
    firewall: exactly ONE rule, aks-jtoye-staging-aks-egress, 20.26.28.17/20.26.28.17
  Burstable has **no on-demand backup**, so the drill restores to a TIMESTAMP. PITR always creates a
  NEW server; parameters are not applied; firewall rules are NOT copied; no public/private crossover.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/staging-pitr-drill.sh — author the drill, and prove the trap fires without touching Azure</name>
  <files>scripts/staging-pitr-drill.sh, scripts/gates/gate-enforcement.conf</files>
  <read_first>
    - scripts/check-networkpolicy-enforcement.sh (the WHOLE file — this is the house style: void(), the explicit-argv refusal, the FORBIDDEN list, the here-string greps, the idempotent cleanup trap, the "found nothing is never clean" doctrine)
    - scripts/restore-drill.sh (the existing compose-side drill — reuse its restore/count shape rather than inventing a second one)
    - docs/runbooks/backups.md lines 272-360 (the Restore procedure, the two-arm recipe, and the "One correction to the mechanism" paragraph that says arm A's pg_dump EXITS 1)
    - infra/backups/k8s-backup.sh (MIN_BACKUP_BYTES and the pg_restore --list check that arm A must show PASSING over a zero-row artifact)
    - scripts/gates/gate-enforcement.conf (the whole header comment — especially the measured VOID from listing a non-check-*.sh)
  </read_first>
  <action>
    Create `scripts/staging-pitr-drill.sh`. Do NOT run it against Azure. Do NOT start any Azure
    resource. The estate is deliberately parked and a PITR restore creates a second billable server.

    STRUCTURE — mirror `check-networkpolicy-enforcement.sh` exactly: a long WHAT-GOES-WRONG /
    MECHANISM / WHAT-NOT-TO-FIX header, `set -uo pipefail` (never `-e`), a `void()` helper, exit codes
    0 clean / 1 a real detection defect / 2 VOID, every knob env-overridable, an explicit argv parser
    where an unknown argument is VOID.

    THE TRAP IS THE MOST IMPORTANT LINE IN THE FILE. Install `trap cleanup EXIT` BEFORE the restore is
    requested, not after it returns — a failure between the request and the assignment would otherwise
    orphan a server nobody knows the name of. Derive the restored server name deterministically
    (`${SRC_SERVER}-pitr-$(date -u +%Y%m%d%H%M%S)`) and export it into the trap's scope before the
    `az` call, so cleanup can delete a server whose creation call never returned. cleanup must be
    idempotent, must pass `--yes --no-wait` for the delete, and must print the delete's own rc.

    SUBSCRIPTION AND CONTEXT SAFETY — the ambient `az` default on this host is the owner's EMPLOYER
    subscription. Every `az` invocation passes `--subscription "$SUBSCRIPTION"` explicitly, and the
    script REFUSES to run with no `--subscription` argument (VOID), in the same words
    check-networkpolicy-enforcement.sh refuses an unnamed `--context`. Carry a `FORBIDDEN_SUBSCRIPTIONS`
    refusal list honoured even when named explicitly. There is no ambient fallback anywhere.

    ARMS. Arm A (the counterexample) dumps as the APP role and EXPECTS `pg_dump` to exit NON-ZERO —
    see the runbook's mechanism correction; the partial artifact left behind is the subject. Run the
    pipeline's OWN checks over that artifact (`MIN_BACKUP_BYTES` and `pg_restore --list`), record that
    they PASS, record the byte size as a RATIO against the floor the way 26-07's L4 recorded 149x,
    then restore and count. Arm B dumps as the BYPASSRLS role and must restore to counts equal to the
    source. The provider half restores to a TIMESTAMP (Burstable has no on-demand backup), re-applies
    the single firewall rule (rules are NOT copied), counts per table, and records the wall-clock
    duration and the achieved RPO.

    COUNTING ROLE. Under RLS an unpinned query returns 0 rows on a full table, so a perfect-looking
    `0 == 0` can be RLS blinding the instrument on both sides. The script must PRINT the counting role
    and its GUC state, and must run a control read proving the query CAN see rows before any
    comparison is trusted.

    VOID (2), never clean, on: a missing `az`/`psql`/`pg_restore`; no `--subscription`; a forbidden
    subscription; a restore that never reaches Ready; an unreachable restored server; and an EMPTY
    table list to compare — a comparison over zero tables passes vacuously.

    THE OFFLINE HARNESS — this is what makes the criteria below falsifiable with the estate parked.
    Route every external call through thin `az_call` / `psql_call` wrappers and add three documented,
    loudly-bannered env knobs used ONLY for falsification:
      PITR_DRILL_DRY_RUN=1     wrappers append their full argv to the trace and return without exec.
                               No cloud call, no DB call. Print a banner on every run so a dry run can
                               never be mistaken for a real drill in a log.
      PITR_DRILL_TRACE=<path>  where the argv trace is written.
      PITR_DRILL_FAIL_AFTER=<stage>   inject a failure after restore|firewall|connect|count.
    The trace is the instrument the acceptance criteria read. A dry run must be structurally incapable
    of reaching a real `az`: assert that by running it in a subshell whose PATH contains no `az` at all
    and confirming it still completes.

    GATE WIRING — the decision is already measured, do not re-derive it: this script is not a
    `check-*.sh`, `check-gate-enforcement.sh:82` never enumerates it, and adding it to
    `gate-enforcement.conf` would VOID that gate for every gate on every PR. So add NO row. Record the
    reasoning as a COMMENT in `gate-enforcement.conf` alongside the existing three-`staging-*.sh`
    precedent, stating the bar it was measured against, and then show `./scripts/check-gate-enforcement.sh`
    still exits 0.

    Do not modify `scripts/check-networkpolicy-enforcement.sh`. Do not touch `.claude/worktrees/`.
  </action>
  <verify>
    <automated>bash -n scripts/staging-pitr-drill.sh; rc=$?; echo "bash -n rc=$rc"; ./scripts/check-gate-enforcement.sh; echo "gate-enforcement rc=$?"</automated>
  </verify>
  <acceptance_criteria>
    - **COMMIT FIRST.** Every break arm below runs only after the clean tree is committed — `git checkout` restores from the INDEX and silently discards post-staging edits. Assert the clean state FIRST and LAST, and verify every restore BY CONTENT with `git hash-object`, never `git diff --stat` (which is empty both when a file is restored and when it was never written).
    - `bash -n scripts/staging-pitr-drill.sh` rc=0, with rc captured on the SAME statement as the command.
    - **shellcheck is MISSING on this host** (measured). Run `command -v shellcheck`; if absent, record the criterion as **VOID — not run**, never as a pass, and say so in the summary. Do not substitute `bash -n` and report it as a shellcheck result. Note in the summary that 29-13's own verify line (`shellcheck -S error … && …`) would abort at rc=127 on this host and needs the same handling.
    - TRAP, CLEAN ARM: `PITR_DRILL_DRY_RUN=1 PITR_DRILL_TRACE=<f>` completes rc=0 and the trace contains **exactly one** `flexible-server delete` line naming the restored server. Record the line verbatim.
    - TRAP, FAILURE ARM (the one that matters): `PITR_DRILL_FAIL_AFTER=restore` exits **non-zero** and the trace **still** contains the delete line. Record rc and the line.
    - TRAP, FAIL-DIRECTION OF THE ASSERTION: on a **scratch copy** in the scratchpad (never the tracked file), delete the `trap` line, re-run the failure arm, and confirm the delete line is **ABSENT** and the assertion reports failure. Without this arm, "the trace contains a delete line" is a check never shown to be capable of failing.
    - DRY RUN CANNOT REACH AZURE: run the clean arm inside a subshell whose PATH excludes `az` entirely and confirm it still completes rc=0. If it VOIDs on missing `az`, the wrapper is resolving the binary before the dry-run branch and must be restructured.
    - VOID ARMS, at least three, each with its rc printed: (a) no `--subscription` → 2; (b) a subscription on the FORBIDDEN list, named explicitly → 2; (c) an EMPTY table list to compare → 2. Record all three.
    - NO REAL CLOUD CALL WAS MADE: prove it, do not assert it. `az account show` was not run by this task; no `az` process was spawned outside the dry-run stub; the trace file is the only record produced. State plainly that `jtoye-staging-pg` was neither started nor contacted.
    - `./scripts/check-gate-enforcement.sh` exits 0, and `scripts/gates/gate-enforcement.conf` gained **zero rows** — only comment lines. Confirm by diff that the added lines all begin with `#`.
    - GATE-CONF FAIL DIRECTION: on a scratch copy of `gate-enforcement.conf`, add `staging-pitr-drill.sh <reason>` as a real row, point the gate at it, and confirm it exits **2** with the "names nothing" message — reproducing the 2026-08-10 measurement rather than trusting the comment. Restore by content afterwards.
    - The script is >= 150 lines and its header states, in its own words, that a dry run proves the STRUCTURE and proves nothing about a real restore.
  </acceptance_criteria>
  <done>A drill exists that cannot orphan a billable server, cannot run against an unnamed subscription, cannot pass over an empty comparison — and whose trap has been shown to fire on a failure, with the assertion itself shown capable of failing.</done>
</task>

<task type="auto">
  <name>Task 2: The federated azure/login deploy path + a digest-parity gate that is falsifiable offline</name>
  <files>.github/workflows/ci-cd.yaml, scripts/check-deploy-digest-parity.sh, k8s/DEPLOYMENT.md</files>
  <read_first>
    - .github/workflows/ci-cd.yaml lines 1112-1291 (the whole deploy-staging job) and 944-996 (the STANDING RULE forbidding a job-level `if:` on anything in build-and-push's needs list, and the matrix declaration)
    - .planning/phases/29-deployable-staging-with-its-own-monitoring/29-PROVISIONING-EVIDENCE.md §2.4 (the federated subject, issuer and audience — reference them, do not copy the GUIDs anywhere)
    - scripts/check-networkpolicy-enforcement.sh (the shape a cluster gate takes in this repo, and how ci-cd.yaml:1275-1284 invokes one)
    - scripts/check-runtime-freshness.sh (read only far enough to state WHY it is the COMPOSE half and can only ever VOID against a cluster)
    - k8s/DEPLOYMENT.md (its existing "Runtime-parity gates" section — this documents into it)
  </read_first>
  <action>
    THE CREDENTIAL SWAP. Delete the `Configure kubeconfig` step (currently 1207-1210 — read, do not
    trust the offset). In its place add, at JOB level on `deploy-staging` only:
      permissions:
        contents: read
        id-token: write
        packages: read      # the digest step reads the registry manifest; see below
    and the step pair `azure/login` (pinned by full commit SHA with a `# v2.x` trailing comment, in
    the exact style the file already uses for `azure/setup-kubectl` and `docker/login-action`) followed
    by `az aks get-credentials -g jtoye-rg -n jtoye-staging-aks --overwrite-existing`. Without
    `id-token: write` no OIDC token is issued and `azure/login` fails — assert the SCOPE, not the mere
    presence, because a workflow-level grant would widen it beyond this job.

    Change NOTHING downstream. CR-02, the premortem render assertion, `kubectl apply -k`, the rollout
    waits, the actuator check, the smoke tests, the DPLY-05 NetworkPolicy gate and the rollback are all
    already correct. `deploy-staging` keeps its own existing `if:` and must not be added to
    `build-and-push`'s `needs:` list.

    THE DIGEST GATE — put the logic in `scripts/check-deploy-digest-parity.sh`, NOT in an inline
    `run:` block. An inline block cannot be exercised offline, and this lane's entire value is that its
    assertions can be falsified before the cluster returns. The script follows the same house shape as
    the NetworkPolicy gate (`void()`, 0/1/2, explicit argv, refusal to inherit an ambient context) and
    is invoked from ci-cd.yaml AFTER the rollout waits and BEFORE the rollback step, in the same shape
    as the existing DPLY-05 step at 1275-1284. Because it is a `check-*.sh`,
    `check-gate-enforcement.sh` WILL enumerate it — the workflow reference is what satisfies default-
    deny, so it must NOT be added to `gate-enforcement.conf`.

    Its contract:
      For each of core-java, edge-go, frontend: read the running pods' image digest
      (`kubectl get pods -l app=<svc> -n <ns> -o jsonpath='{..imageID}'`) and assert it is in the set
      of digests published for `ghcr.io/bralabee/jtoye-<svc>:<SHA>`.
      Fail CLOSED: an unreadable digest, an empty actual set, an empty expected set, or ZERO services
      discovered is exit 2. "Could not check" is never "clean", and a step that exits 0 on an empty
      result measures nothing.

    WHY A SET AND NOT AN EQUALITY, stated in the script's header as a deliberate widening with its
    reason: core-java and edge-go are built `linux/amd64,linux/arm64`, so the TAG resolves to a
    manifest-list (index) digest while a node may report the platform-specific manifest digest instead.
    **Which one AKS/containerd reports is NOT measurable offline.** So build the expected set from
    `docker buildx imagetools inspect` — the index digest plus each platform manifest digest — and
    assert membership. This still fails on a genuinely different image, which is the property being
    bought. Record in the summary that narrowing the set to a single expected digest is a measurement
    OWED to 29-15 on the live cluster, and that until it is made the gate is deliberately the weaker
    of the two correct forms.

    The expected digests cannot come from `build-and-push` job outputs: it is a MATRIX job and matrix
    outputs are last-writer-wins. Resolve them from the registry at deploy time (`docker/login-action`
    with `GITHUB_TOKEN`, then `docker buildx imagetools inspect`), which is why `packages: read` is in
    the job permissions.

    SHELL DISCIPLINE, non-negotiable: no `cmd | grep -q X` anywhere in the added code — grep exits at
    the first hit, the writer takes SIGPIPE, `pipefail` promotes it to 141 and the guard fails OPEN.
    Use here-strings. Capture `rc=$?` on the SAME statement as its command; an intervening `echo`
    reports the echo's status, which is 0 essentially always. Never bound a stream with `head` while
    proving an absence.

    DOCUMENT IT in `k8s/DEPLOYMENT.md`'s existing "Runtime-parity gates" section: `check-runtime-
    freshness.sh` is the COMPOSE half and can only ever VOID against a cluster, so its VOID must never
    be recorded as a pass; `check-deploy-digest-parity.sh` is the KUBERNETES half. Phase 26 shipped a
    runtime missing its own `application.yml` change past four green gates; this is the gate that
    would have caught it in a cluster. State explicitly that the k8s half has NOT yet run.

    Production deploy flags stay OFF. Touch nothing in `deploy-production`.
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci-cd.yaml')); print('yaml ok')"; echo "yaml rc=$?"; bash -n scripts/check-deploy-digest-parity.sh; echo "bash -n rc=$?"; ./scripts/check-gate-enforcement.sh; echo "gate rc=$?"</automated>
  </verify>
  <acceptance_criteria>
    - **COMMIT FIRST**, arms after, clean state asserted FIRST and LAST, every restore verified with `git hash-object`.
    - `rg -uu -c 'KUBE_CONFIG_STAGING' .github/workflows/ci-cd.yaml` returns **0**. Deleting the GitHub SECRET itself is an OWNER action — record it as owed in Task 3's note; do not imply it is done.
    - `rg -uu -c 'azure/login' .github/workflows/ci-cd.yaml` >= 1, and `id-token: write` appears **inside the deploy-staging job block only**. Assert the SCOPE by extracting the job's own line range, not by a file-wide grep — a file-wide hit is satisfied by a workflow-level grant, which is the thing being avoided.
    - `deploy-production` is byte-unchanged and `vars.DEPLOY_ENABLED` is untouched: prove with a scoped `git diff` showing no `-`/`+` lines in the production job's range.
    - Everything downstream of the credential step is byte-unchanged: `git diff .github/workflows/ci-cd.yaml` shows changes ONLY in the credential region, the job `permissions:` block, and the added digest step.
    - ACTIONLINT, CLEAN ARM: resolve the binary (`command -v actionlint`; it lives at `/home/sanmi/go/bin/actionlint` and may not be on PATH — if it cannot be resolved, record **VOID**, never a pass). Run it on the real file; record rc and any pre-existing findings so the delta is attributable to this change.
    - ACTIONLINT, FAIL DIRECTION: on a **scratch copy** (never the tracked file), inject a deliberately malformed step — a broken `${{ }}` expression is the reliable shape — and confirm actionlint exits **non-zero** with a message naming it. Record both directions' real output and rc. If actionlint does NOT reject the injected break, the clean pass is vacuous: say so, and replace the check with a strictly stronger form rather than reporting the pass.
    - DIGEST LOGIC, FOUR ARMS against fabricated inputs (no cluster, no registry — the script must accept injected expected/actual sets for exactly this purpose): matching → **0**; a deliberately mismatched digest → **1**; an EMPTY actual set → **2**; ZERO services discovered → **2**. Record all four rcs. The mismatch arm is the one that matters — a gate shown only passing may be incapable of failing.
    - `rg -uu -c 'grep -q' .github/workflows/ci-cd.yaml` — record the count BEFORE the change and AFTER, and assert the delta is **0**. There are pre-existing occurrences elsewhere in the file; counting them separately is what makes the assertion about this change. Confirm by reading the added block that it contains none.
    - Every `rc=$?` in the added shell is on the same statement as its command — read the added block and state it, do not infer it.
    - `./scripts/check-gate-enforcement.sh` exits 0 with `check-deploy-digest-parity.sh` counted as WIRED (not exempt), and `gate-enforcement.conf` gained no row for it.
    - GATE FAIL DIRECTION: on a scratch copy, remove the ci-cd.yaml reference to the new script and confirm `check-gate-enforcement.sh` FAILS naming it as unwired — proving the wiring claim is not vacuous. Restore by content.
    - The YAML parses, and `k8s/DEPLOYMENT.md` states which half of the runtime-parity doctrine each instrument covers **and that the k8s half has never run**.
  </acceptance_criteria>
  <done>The workflow authenticates with a short-lived federated credential, carries no long-lived cluster credential, and a wrong-image deploy would fail the job — with the digest logic shown failing on mismatched inputs rather than only passing on matched ones.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the superseded WAL-G direction, and write down what is OWED</name>
  <files>docs/architecture/SYSTEM_DESIGN_V2.md, docs/runbooks/backups.md, .planning/phases/29-deployable-staging-with-its-own-monitoring/29-LANE-C-OFFLINE-AUTHORED.md</files>
  <read_first>
    - docs/architecture/SYSTEM_DESIGN_V2.md lines 855-900 (the CURRENT-STATE callout, the "TARGET architecture" sentence, the ASCII box containing the WAL-G line at 890, and the Key Decisions list) and 1450-1475 (§7.1, whose WAL-G mention is a TRUE negative and must NOT be edited)
    - docs/architecture/decisions/ADR-0002-managed-vs-manifest-datastores.md (Accepted in plan 29-01 — the decision that supersedes the WAL-G direction)
    - docs/runbooks/backups.md lines 382-400 (the "Pending (needs a live cluster)" section — this is the house shape for recording what is not yet done)
    - docs/CHANGELOG.md:2183 (PR #197 — the earlier correction, so this one is named as a SECOND correction and not as a rediscovery of the first)
  </read_first>
  <action>
    SYSTEM_DESIGN_V2 — the false claim is already gone (PR #197 relabelled it TARGET). What remains
    wrong is that the TARGET diagram at :890 still names WAL-G as the backup direction while
    **ADR-0002 is Accepted and chose provider PITR**. Correct the diagram line and the adjacent Key
    Decisions list to say what is now decided: Azure Flexible Server automated backups + PITR as the
    first line, the `pg-backup-cronjob` logical dump to AWS S3 eu-west-2 as the provider-independent
    second line. Name it as a correction and cite ADR-0002 — a silently-fixed superseded direction
    looks identical to one that was never there. Add the managed-server specifics a reader will
    otherwise be surprised by: PITR creates a NEW server, parameters are not applied, firewall rules
    are not copied, no public/private crossover, no on-demand backup on Burstable.

    Do NOT touch :1466 (`WAL Archive: NONE — no archive_command, no WAL-G, no pgBackRest`). It is a
    true statement about the current posture and deleting it would remove a correct fact.

    Do NOT touch the WAL-G hits under `.claude/worktrees/agent-acedd037d6b3648d0/` (a parked worktree
    that must survive), nor `docs/analysis/REMEDIATION-BACKLOG-2026-07-08.md` or the
    `.planning/quick/260708-bu6` findings — those are dated records that correctly state what was true
    when written.

    backups.md — ADDITIVE ONLY. Add a short pointer from the two-arm recipe to
    `scripts/staging-pitr-drill.sh`, stating that the script automates the recipe and that **it has
    not yet been run against staging**. Do NOT write a dated result section: there are no results, and
    that section belongs to 29-13 when the drill actually runs. A script that exists but is referenced
    nowhere in its own runbook is the "unwired" shape this repo keeps finding.

    Write `.planning/phases/29-deployable-staging-with-its-own-monitoring/29-LANE-C-OFFLINE-AUTHORED.md`
    — the record 29-13's and 29-15's executors will read when the estate returns. It must contain:
      1. WHAT WAS AUTHORED, per file, with what was proven about each and by which arm.
      2. **OWED** — an explicit list of everything this lane could not prove, each naming the plan that
         owns it: the two logical-dump arms and the provider PITR restore with per-table counts, RPO
         and wall-clock duration (29-13); one green deploy-staging run URL, the digest step's real
         output, the narrowing of the digest SET to a single measured form, the SKIP-when-flag-unset
         arm, and the operator deletion of the `KUBE_CONFIG_STAGING` secret (29-15).
      3. THE STALE PREMISES, so neither executor wastes a cycle: SYSTEM_DESIGN_V2:657 no longer holds
         the WAL-G claim; 29-15's line offsets moved; arm A's `pg_dump` exits 1 rather than succeeding
         silently; shellcheck is absent on this host so 29-13's verify line aborts at rc=127; matrix
         job outputs cannot carry per-service digests.
      4. WHICH CRITERIA ARE STRUCTURAL-ONLY, said plainly: a green `bash -n`, a green `actionlint` and
         a passing dry-run trace prove SHAPE. They are not evidence that a restore works, that a
         deploy authenticates, or that a digest ever matched. Do not let a green offline gate read as
         a working drill.
  </action>
  <verify>
    <automated>rg -uu -n 'WAL-G' docs/architecture/SYSTEM_DESIGN_V2.md; echo "rg rc=$?"; ./scripts/docs-freshness.sh; echo "docs-freshness rc=$?"; ./scripts/check-doc-metrics.sh; echo "doc-metrics rc=$?"</automated>
  </verify>
  <acceptance_criteria>
    - The repository-wide WAL-G search is run with an instrument that can see EVERY file — `rg -uu` (or `searchcheck WAL-G .`), never a bare `rg`/`grep`, both of which honour `.gitignore` and would silently miss `.planning/`. Record the full result and classify every remaining hit as: corrected / true-negative-kept / dated-record-out-of-scope / parked-worktree-out-of-scope. A hit left unclassified is a gap.
    - `docs/architecture/SYSTEM_DESIGN_V2.md:1466` is byte-unchanged — prove it by content (`git hash-object` on the pre/post file plus a scoped `git diff` showing no touch to that line), not by claim.
    - `git diff docs/runbooks/backups.md` shows **additions only** — zero `-` lines. No dated result section was invented.
    - `./scripts/docs-freshness.sh` and `./scripts/check-doc-metrics.sh` both exit 0. Bash gate scripts contribute **0** to the test counts, so this plan must not move them; if they moved, regenerate BY SCRIPT with the write flag, never by arithmetic.
    - DOCS-GATE FAIL DIRECTION: confirm `check-doc-metrics.sh` is capable of failing on this tree — perturb a quoted metric on a scratch copy, point the gate at it, and record the non-zero rc. A doc gate observed only passing may be reading nothing.
    - `29-LANE-C-OFFLINE-AUTHORED.md` contains an **OWED** section naming both 29-13 and 29-15 items, and states in its own words that no live cluster, Azure resource or database was contacted by this lane.
    - CLEAN STATE ASSERTED LAST: after every arm in all three tasks, `git status --short` is clean apart from this plan's intended files, and each file touched by a break arm is verified restored **by content**. Record the closing assertion — it is the only thing that proves the restores happened.
    - `git log HEAD..origin/main` is empty (or a merge is recorded) at the end, and `.claude/worktrees/agent-acedd037d6b3648d0` is untouched — confirm by listing it and comparing its git status to the start.
  </acceptance_criteria>
  <done>The superseded WAL-G direction is corrected and named as a correction, the runbook points at the drill without claiming a result, and everything this lane could not prove is written down as owed rather than implied as done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this lane -> the Azure control plane | A script authored here can create and delete billable resources; the ambient subscription default is the owner's EMPLOYER |
| GitHub Actions -> Azure | A workflow obtains cluster-admin-capable credentials |
| repository secrets -> anyone with write access | A long-lived kubeconfig is a standing cluster credential |
| the rolled-out pods -> the registry | The running image may differ from the one built for the commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-LC-01 | Denial of Service | A drill run during authoring creating a second billable Flexible Server | mitigate | No real `az` call is made by this plan; the falsification arms run under `PITR_DRILL_DRY_RUN=1`, and the dry run is proven to complete with no `az` on PATH at all |
| T-LC-02 | Tampering | An `az` call landing on the employer subscription via the ambient default | mitigate | Every call passes `--subscription` explicitly; the script VOIDs with no `--subscription`; a FORBIDDEN list is honoured even when named — intent is not a safety mechanism |
| T-LC-03 | Denial of Service | A drill failing mid-run and orphaning a restored server at the source SKU | mitigate | `trap cleanup EXIT` installed BEFORE the restore is requested, with a deterministically pre-derived server name; proven to fire by an injected failure, and the assertion proven capable of failing by removing the trap on a scratch copy |
| T-LC-04 | Spoofing | A long-lived kubeconfig in a GitHub secret | mitigate | Replaced by an Entra federated credential with an exact-match subject. The SECRET's deletion is an owner action and is recorded as OWED, not claimed |
| T-LC-05 | Elevation of Privilege | `id-token: write` granted workflow-wide | mitigate | Granted at JOB level on `deploy-staging` only; the criterion asserts the SCOPE by line range, because a file-wide grep is satisfied by the wider grant |
| T-LC-06 | Tampering | Deploying an image other than the one built for the commit | mitigate | `check-deploy-digest-parity.sh` after the rollout waits, failing closed on an unreadable or empty result; shown failing on a mismatched digest, not merely passing on a matched one |
| T-LC-07 | Repudiation | A green offline gate read as a working drill or a working deploy | mitigate | Structural-only criteria are named as such in the plan, in the OWED note and in the summary; the summary must not claim the end-to-end proof |
| T-LC-08 | Tampering | A break arm's restore silently failing, leaving later arms on a dirty tree | mitigate | Commit before arms; scratch copies in the scratchpad rather than in-tree edits where possible; every restore verified by `git hash-object`; the clean state asserted LAST as well as first |
| T-LC-09 | Denial of Service | Adding a non-`check-*.sh` row to `gate-enforcement.conf`, taking the gate-wiring check offline for every gate on every PR | mitigate | The measured 2026-08-10 VOID is reproduced on a scratch copy as the fail arm; the real file gains comment lines only, asserted by diff |
| T-LC-10 | Information Disclosure | Production hostnames or flags becoming live early | mitigate | `deploy-production` is byte-unchanged and proven so by a scoped diff; `DEPLOY_ENABLED` untouched |
| T-LC-SC | Tampering | `azure/login` action supply chain | mitigate | Pinned by full commit SHA with a version comment, in the style the file already uses for `azure/setup-kubectl` and `docker/login-action`. No npm/PyPI/cargo packages in scope |
</threat_model>

<verification>
- No Azure resource was started or contacted; `jtoye-staging-pg` and `jtoye-staging-aks` remain Stopped
- The PITR trap fires on an injected failure, and the assertion is shown capable of failing
- The digest logic exits 1 on a mismatch and 2 on an empty/unreadable input, against fabricated inputs
- `actionlint` rejects an injected break on a scratch copy, and passes on the real file
- `./scripts/check-gate-enforcement.sh`, `./scripts/docs-freshness.sh`, `./scripts/check-doc-metrics.sh` all rc=0
- `scripts/check-networkpolicy-enforcement.sh` and `.claude/worktrees/agent-acedd037d6b3648d0` untouched
- The tree is clean at the END as well as at the start, with every break-arm restore verified by content
</verification>

<success_criteria>
- `scripts/staging-pitr-drill.sh` exists (>= 150 lines), cannot orphan a billable server, cannot run without an explicit subscription, and cannot pass over an empty comparison
- `.github/workflows/ci-cd.yaml` contains no `KUBE_CONFIG_STAGING`, authenticates via `azure/login` with job-scoped `id-token: write`, and invokes a digest-parity gate after the rollout waits
- `scripts/check-deploy-digest-parity.sh` fails closed and has been shown to fail on mismatched digests
- The superseded WAL-G direction is corrected against ADR-0002 and the correction is named as one
- `29-LANE-C-OFFLINE-AUTHORED.md` records every OWED proof against the plan that owns it, and states plainly that the structural greens here are not evidence of a working drill or a working deploy
</success_criteria>

<output>
Create `.planning/quick/260815-00p-phase-29-lane-c-author-29-13-staging-pit/260815-00p-SUMMARY.md` when done.

The summary MUST carry, verbatim in substance: **the end-to-end proof of 29-13 and 29-15 is NOT
claimed by this lane. No cluster, Azure resource or database was contacted. Every green here is
structural.**
</output>
</content>
</invoke>
