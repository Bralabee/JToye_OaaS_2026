---
phase: 260708-g8c-issue-79-p0-3-purge-pii-dumps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backups/jtoye_jtoye_20251231_121414.sql.gz   # git rm --cached (untrack) + moved off-tree
  - infra/backups/backup.sh                       # default BACKUP_DIR off-tree
  - .gitignore                                     # broaden backups/*.sql.gz -> backups/
  - .github/workflows/pii-guard.yml                # new zero-tolerance CI guard
  - docs/security/PII-EXPOSURE-ASSESSMENT-2026-07-08.md  # new breach assessment
autonomous: true
requirements: [P0-3]

must_haves:
  truths:
    - "No .sql.gz file and no backups/ path is tracked by git at HEAD"
    - "The 147 dump/error files are preserved off-tree at ~/jtoye-db-backups (mode 700)"
    - "backup.sh default BACKUP_DIR resolves off-tree, so the nightly cron no longer writes into the repo"
    - "CI fails any push or PR that tracks a backups/ path or a .sql.gz file"
    - "A GDPR Art 33/34 breach-assessment doc records the incident and its no-notification conclusion, containing no real email addresses or personal names"
  artifacts:
    - path: "infra/backups/backup.sh"
      provides: "Hardened backup script whose default output dir is off-tree"
      contains: "BACKUP_DIR:-$HOME/jtoye-db-backups"
    - path: ".gitignore"
      provides: "Broadened ignore rule so a reappearing backups/ dir stays untracked"
      contains: "backups/"
    - path: ".github/workflows/pii-guard.yml"
      provides: "Fast fail-early CI guard rejecting tracked dumps"
      contains: "git ls-files"
    - path: "docs/security/PII-EXPOSURE-ASSESSMENT-2026-07-08.md"
      provides: "UK GDPR breach assessment and remediation log for P0-3"
      min_lines: 40
  key_links:
    - from: "infra/backups/backup.sh"
      to: "$HOME/jtoye-db-backups"
      via: "BACKUP_DIR default parameter expansion"
      pattern: "BACKUP_DIR:-\\$HOME/jtoye-db-backups"
    - from: ".github/workflows/pii-guard.yml"
      to: "git ls-files grep gate"
      via: "fail-on-match on push + pull_request"
      pattern: "git ls-files"
---

<objective>
Close the repo-side half of Issue #79 [P0-3]: stop tracking database dumps, move the 147 local dump/error files off the repo tree, redirect the nightly backup default off-tree, add a permanent zero-tolerance CI guard against re-introduction, and formally document the UK GDPR breach assessment.

Purpose: The public repo (github.com/Bralabee/JToye_OaaS_2026) currently tracks `backups/jtoye_jtoye_20251231_121414.sql.gz` at HEAD and carries a second dump blob in history. This plan removes the live exposure surface and prevents recurrence. The git-history rewrite (filter-repo / force-push) is handled by the orchestrator AFTER this plan merges and is explicitly OUT OF SCOPE here.

Output:
- `backups/` gone from the working tree; all 147 files relocated to `~/jtoye-db-backups` (mode 700)
- `infra/backups/backup.sh` default `BACKUP_DIR` pointing off-tree (env override preserved)
- `.gitignore` broadened `backups/*.sql.gz` -> `backups/`
- `.github/workflows/pii-guard.yml` failing any push/PR that tracks a dump
- `docs/security/PII-EXPOSURE-ASSESSMENT-2026-07-08.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@infra/backups/backup.sh
@.github/workflows/ci-cd.yaml
@.github/workflows/gitleaks.yml
@docs/analysis/REMEDIATION-BACKLOG-2026-07-08.md

<facts>
Verified by orchestrator recon this session (treat as ground truth — do NOT re-investigate):

- Repo is PUBLIC, 0 forks, no branch protection, remote has only `main`, tags v0.1.0/v1.2.0/v1.3.0/v2.0.0/v2.1.
- Tracked dump at HEAD: `backups/jtoye_jtoye_20251231_121414.sql.gz`. A second dump `backups/jtoye_jtoye_20251231_120249.sql.gz` was added in commit 331da2d and later deleted; both blobs live in history under `backups/`.
- Working tree has 147 `backups/*.sql.gz` files (1.1 MB): 19 real dumps + 128 gzipped pg_dump ERROR logs (nightly cron silently failing since ~Feb 2026 with "fe_sendauth: no password supplied").
- PII characterization: customers table in the tracked dump is EMPTY; ~2 customers_aud rows + ~5 order rows carry emails. ALL email domains across ALL 147 dumps are synthetic/dev (@test.com, @example.com, @tenanta.com, @jtoye.uk, @jollofexpress.co.uk, @verify.test, @ma.com, @council.test, @p.test). ZERO real-provider emails (gmail/yahoo/hotmail/outlook/icloud). All rows belong to seed dev tenant 00000000-0000-0000-0000-000000000001, timestamps 2025-12-31. No credentials/API keys/password hashes ("Password:" strings are pg_dump auth-failure prompts in the 128 error files).
- Exposure window: 2025-12-31 (commit 331da2d) -> planned history-rewrite date.
- Nightly cron: `0 2 * * * .../infra/backups/backup.sh`; script default `BACKUP_DIR` is repo `backups/` (line 26). Changing the default changes cron behavior; no crontab edit needed.
- `.gitignore` already has `backups/*.sql.gz` (~line 105).
- Branch fix/79-pii-purge-backups is already checked out. NO worktree isolation — untracked files must be moved from the real working tree.
</facts>

<interfaces>
infra/backups/backup.sh line 26 (current):
  BACKUP_DIR="${BACKUP_DIR:-/home/sanmi/IdeaProjects/JToye_OaaS_2026/backups}"
Target:
  BACKUP_DIR="${BACKUP_DIR:-$HOME/jtoye-db-backups}"
(env override `${BACKUP_DIR:-...}` semantics preserved; only the default moves off-tree.)
show_usage() help text (~line 298) documents `BACKUP_DIR ... (default: ./backups)` — update the printed default to `$HOME/jtoye-db-backups` for consistency.

gitleaks.yml (convention reference): a small dedicated workflow, `on: pull_request + push (main) + workflow_dispatch`, single fast job, `permissions: contents: read`, `actions/checkout@v4`. pii-guard.yml mirrors this shape.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Untrack + relocate dumps, harden backup.sh default off-tree</name>
  <files>backups/jtoye_jtoye_20251231_121414.sql.gz, infra/backups/backup.sh, .gitignore</files>
  <action>
Remove the tracked dump from the index without deleting the file: `git rm --cached backups/jtoye_jtoye_20251231_121414.sql.gz`. Create the off-tree store and relocate every dump/error file out of the working tree: `mkdir -p ~/jtoye-db-backups && chmod 700 ~/jtoye-db-backups && mv backups/*.sql.gz ~/jtoye-db-backups/ && rmdir backups/`. (If `rmdir` reports the dir is non-empty, list the residual files and move them too — the goal is `backups/` gone from the tree; do not delete any dump data.)

Edit `infra/backups/backup.sh` line 26 so the default `BACKUP_DIR` resolves off-tree: `BACKUP_DIR="${BACKUP_DIR:-$HOME/jtoye-db-backups}"`. Keep the `${BACKUP_DIR:-...}` env-override form intact — only the default literal changes. Also update the `show_usage` help text (~line 298) default annotation from `./backups` to `$HOME/jtoye-db-backups` so the documented default matches behavior. This aligns the nightly cron (which invokes the script with no BACKUP_DIR env) to write outside the repo per P0-3.

Broaden `.gitignore`: change the existing `backups/*.sql.gz` entry (~line 105) to `backups/` so a reappearing backups directory stays fully untracked (belt-and-braces alongside the CI guard). Do NOT stage or move any file into `~/jtoye-db-backups` that is a real credential — the dumps here are dev-only synthetic data per verified facts.

This is one atomic commit: `chore(79): untrack and relocate db dumps off-tree; default backup dir to ~/jtoye-db-backups [P0-3]`.
  </action>
  <verify>
    <automated>test ! -e backups && test -z "$(git ls-files backups/)" && test "$(ls ~/jtoye-db-backups | wc -l)" -eq 147 && grep -qF 'BACKUP_DIR:-$HOME/jtoye-db-backups' infra/backups/backup.sh && grep -qxF 'backups/' .gitignore && echo PASS</automated>
  </verify>
  <done>`backups/` no longer exists in the working tree; `git ls-files backups/` is empty; `~/jtoye-db-backups` holds 147 files at mode 700; backup.sh default and help text point off-tree; `.gitignore` ignores `backups/`. One atomic chore commit made.</done>
</task>

<task type="auto">
  <name>Task 2: Add zero-tolerance pii-guard CI workflow</name>
  <files>.github/workflows/pii-guard.yml</files>
  <action>
Create a new dedicated workflow `.github/workflows/pii-guard.yml`, mirroring the established `gitleaks.yml` fast-fail convention (chosen over adding a job to the heavy `ci-cd.yaml`: an independent tiny workflow is faster to fail, easier to read, and co-locates with the other secret/PII guard). Trigger `on: push` and `on: pull_request` (both targeting the default branches used by the repo — `main` at minimum) plus `workflow_dispatch`. `permissions: contents: read`. One job `pii-guard` on `ubuntu-latest` with `actions/checkout@v4` (default fetch-depth is sufficient — the guard inspects the tracked index at the checked-out ref, not history).

The guard step runs a bash check that fails on ANY match: run `git ls-files` piped to `grep -E '^backups/|\.sql\.gz$'`; if grep finds a match, emit a `::error::` annotation naming the offending paths and `exit 1`; if no match, print an OK message and pass. Structure it so a match fails the build and a clean tree passes (grep exit 0 = matches found = fail; grep exit 1 = clean = pass). Zero tolerance, no allowlist.

One atomic commit: `ci(79): add pii-guard rejecting tracked db dumps and .sql.gz [P0-3]`.
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/pii-guard.yml'))" && { git ls-files | grep -E '^backups/|\.sql\.gz$' && echo 'FAIL: tracked dumps present' && exit 1 || echo 'OK: guard logic passes against clean tree'; }</automated>
  </verify>
  <done>`pii-guard.yml` parses as valid YAML; the exact grep gate finds zero matches against the post-Task-1 tree (guard passes locally); workflow triggers on push + pull_request. One atomic ci commit made.</done>
</task>

<task type="auto">
  <name>Task 3: Write UK GDPR breach-exposure assessment doc</name>
  <files>docs/security/PII-EXPOSURE-ASSESSMENT-2026-07-08.md</files>
  <action>
Create `docs/security/PII-EXPOSURE-ASSESSMENT-2026-07-08.md` (create `docs/security/` if absent). Structure the doc with these sections, using ONLY the verified facts in `<facts>` (do not invent data):

1. Incident summary — what (database dumps committed to a public repo), when (2025-12-31, commit 331da2d), where (tracked `backups/*.sql.gz` at HEAD + a second dump blob in history).
2. Exposure window — 2025-12-31 (commit 331da2d) through the planned history-rewrite date.
3. Data characterization — synthetic/dev data only; seed tenant 00000000-0000-0000-0000-000000000001; the customers table in the tracked dump is empty; a handful of customers_aud + order rows carry emails; ALL email domains are synthetic dev domains (reference domains only, e.g. `@test.com`, `@example.com` — never a full localpart@ address); zero real-provider emails; no credentials, API keys, or password hashes (the "Password:" strings are pg_dump auth-failure prompts in the 128 error files).
4. UK GDPR Art 33/34 analysis — conclusion: the exposed rows contain no personal data of real data subjects, therefore no supervisory-authority (Art 33) or data-subject (Art 34) notification duty arises; recorded here per the accountability principle (Art 5(2)) and the Art 33(5) internal-record requirement.
5. Credential-rotation assessment — N/A: no credentials present in any dump. The local dev DB password is unchanged and dev-only (out of scope).
6. Residual risk — GitHub dangling/unreachable objects persist until GitHub support-side GC (note: contact GitHub Support to expedite purge after the rewrite); any unknown pre-existing clones of the public repo.
7. Remediation log — this PR (untrack + relocate + backup.sh default off-tree + pii-guard CI, tracing to REMEDIATION-BACKLOG-2026-07-08.md item P0-3) plus the planned orchestrator-run history rewrite (filter-repo + force-push, tracked separately).
8. Side-finding (out of scope, follow-up) — the nightly backup has silently failed since ~Feb 2026 (128 of 147 files are gzipped pg_dump error logs, "fe_sendauth: no password supplied"); flag as a separate operational follow-up.

The doc MUST NOT contain any real email address (no localpart@realprovider) and no personal names — synthetic domains only.

One atomic commit: `docs(79): PII exposure GDPR assessment for public-repo db dumps [P0-3]`.
  </action>
  <verify>
    <automated>test -f docs/security/PII-EXPOSURE-ASSESSMENT-2026-07-08.md && ! grep -qiE '[[:alnum:]._%+-]+@(gmail|yahoo|hotmail|outlook|icloud|live|aol|proton)\.[[:alnum:].]+' docs/security/PII-EXPOSURE-ASSESSMENT-2026-07-08.md && grep -qiE 'Art(icle)? 33|Art(icle)? 34' docs/security/PII-EXPOSURE-ASSESSMENT-2026-07-08.md && echo PASS</automated>
  </verify>
  <done>Assessment doc exists, cites UK GDPR Art 33/34 with a no-notification conclusion, references P0-3, records the side-finding, and contains zero real-provider email addresses and no personal names. One atomic docs commit made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree -> public git remote | Any tracked file becomes world-readable on push; the exposure vector this plan closes |
| CI runner -> repo index | The guard inspects `git ls-files` at the checked-out ref to reject dumps before merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01 | Information Disclosure | Tracked `backups/*.sql.gz` at HEAD | mitigate | Task 1 `git rm --cached` + relocate off-tree; dumps no longer tracked |
| T-79-02 | Information Disclosure | Nightly cron writing into repo `backups/` | mitigate | Task 1 backup.sh default `BACKUP_DIR` -> `$HOME/jtoye-db-backups`; cron no longer writes on-tree |
| T-79-03 | Tampering / regression | Future commit re-adds a dump | mitigate | Task 2 zero-tolerance `pii-guard` fails push + PR on any `backups/` or `.sql.gz` match; Task 1 `.gitignore backups/` as second layer |
| T-79-04 | Repudiation / compliance | No formal record of the exposure decision | mitigate | Task 3 GDPR Art 33(5)/5(2) accountability record with Art 33/34 no-notification conclusion |
| T-79-05 | Information Disclosure | Dangling blobs remain in history after this PR | accept (in this plan) | History rewrite + GitHub Support GC handled by orchestrator post-merge; documented as residual risk in Task 3 |

No npm/pip/cargo installs in this plan — Package Legitimacy Gate not applicable.
</threat_model>

<verification>
- `test ! -e backups` — working tree has no backups/ directory
- `git ls-files backups/` prints nothing; `git ls-files | grep -E '^backups/|\.sql\.gz$'` prints nothing
- `ls ~/jtoye-db-backups | wc -l` == 147; dir is mode 700
- `grep -F 'BACKUP_DIR:-$HOME/jtoye-db-backups' infra/backups/backup.sh` matches; `.gitignore` contains `backups/`
- `.github/workflows/pii-guard.yml` parses as YAML and its grep gate passes against the clean tree
- `docs/security/PII-EXPOSURE-ASSESSMENT-2026-07-08.md` exists, cites Art 33/34, contains no real-provider email addresses and no personal names
- Three atomic commits present on branch fix/79-pii-purge-backups (chore, ci, docs)
</verification>

<success_criteria>
- [ ] No .sql.gz file and no backups/ path is tracked at HEAD
- [ ] 147 dump/error files preserved at ~/jtoye-db-backups (mode 700); none deleted
- [ ] backup.sh default BACKUP_DIR resolves off-tree; env override preserved; help text consistent
- [ ] .gitignore ignores backups/
- [ ] pii-guard CI workflow rejects tracked dumps on push + PR; passes on the current clean tree
- [ ] GDPR breach-assessment doc written with Art 33/34 no-notification conclusion, P0-3 traceability, side-finding flagged, zero real PII
- [ ] Three atomic commits, no Co-Authored-By trailers
- [ ] docs/metrics.json untouched (no test-count change); history rewrite NOT attempted (orchestrator scope)
</success_criteria>

<output>
Create `.planning/quick/260708-g8c-issue-79-p0-3-purge-pii-dumps-from-git-h/260708-g8c-SUMMARY.md` when done.
</output>
