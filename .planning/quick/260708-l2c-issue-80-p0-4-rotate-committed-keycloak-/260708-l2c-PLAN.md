---
phase: 260708-l2c-issue-80-p0-4-rotate-committed-keycloak
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [ISSUE-80-P0-4]
files_modified:
  - infra/keycloak/realm-export.template.json   # git mv from realm-export.json
  - infra/keycloak/realm-export.json            # becomes gitignored rendered artifact
  - .gitignore
  - .gitleaks.toml
  - docker-compose.full-stack.yml
  - infra/docker-compose.yml
  - infra/keycloak/configure-keycloak.sh
  - infra/keycloak/README.md
  - .env.example
  - infra/.env.example
  - frontend/.env.local.example
  - scripts/verify-env.sh
  - scripts/start-dev.sh
  - .env                                         # UNTRACKED — rotated values written here
user_setup: []

must_haves:
  truths:
    - "No committed literal secret (core-api-secret-2026, edge-api-secret-2026, admin123, password123, minioadmin) survives in infra/, compose files, or scripts/ after Task 1"
    - "The realm import is templated: client secrets, seed-user passwords, and realm keys are no longer tracked as literals; the rendered realm-export.json is gitignored and produced at container start"
    - "docker-compose.full-stack.yml and infra/docker-compose.yml contain zero weak :- fallbacks; every required credential var uses the ${VAR:?message} fail-loud form"
    - "verify-env.sh exits 0 against a rotated .env and exits non-zero (naming the offending var) when a required var is unset/empty or matches the weak deny-list"
    - "Against the rotated live stack: NextAuth SSO login reaches /dashboard, a password-grant on core-api with the rotated secret returns 200 + access_token and calls a protected endpoint non-401, and the OLD core-api-secret-2026 returns 401 (invalidated)"
    - "No rotated secret VALUE appears in any tracked file, commit message, PLAN.md, or SUMMARY.md — variable NAMES only"
  artifacts:
    - path: "infra/keycloak/realm-export.template.json"
      provides: "Tracked realm template with env placeholders for client secrets + seed-user password, no KeyProvider secret material"
      contains: "${KEYCLOAK_CLIENT_SECRET}"
    - path: "docker-compose.full-stack.yml"
      provides: "keycloak-realm-render sidecar + :? guards on all credential vars"
      contains: "keycloak-realm-render"
    - path: "scripts/verify-env.sh"
      provides: "Env-var preflight: required-var + deny-list enforcement, fail-loud, usage docs"
      contains: "deny"
  key_links:
    - from: "keycloak-realm-render (envsubst)"
      to: "infra/keycloak/realm-export.json -> keycloak --import-realm"
      via: "envsubst of realm-export.template.json using KEYCLOAK_CLIENT_SECRET/EDGE_API_CLIENT_SECRET/KC_SEED_USER_PASSWORD"
      pattern: "envsubst"
    - from: "realm core-api client secret"
      to: "frontend KEYCLOAK_CLIENT_SECRET (NextAuth)"
      via: "same env var renders realm secret AND feeds frontend provider — guarantees they match"
      pattern: "KEYCLOAK_CLIENT_SECRET"
    - from: "realm seed-user credential (KC_SEED_USER_PASSWORD)"
      to: "Task 3 E2E_VENDOR_PASSWORD + password-grant"
      via: "single seed password renders realm login AND drives the proof"
      pattern: "KC_SEED_USER_PASSWORD"
---

<objective>
Close Issue #80 [P0-4]: confidential Keycloak client secrets, realm signing keys, and weak default credentials (admin123 / password123 / minioadmin) are committed to a PUBLIC repo. Rotate everything, template the realm export so no literal secret is tracked, remove all weak compose fallbacks, gate the stack with verify-env.sh, and prove the rotated stack end-to-end.

Purpose: Eliminate a live credential-exposure hole in a public repository and make weak/unset credentials fail the stack loudly instead of silently defaulting.
Output: A secret-free tracked infra surface (templated realm, `${VAR:?}` compose, hardened verify-env.sh), rotated values in the untracked `.env`, and a green rotated live stack with the old secrets provably invalidated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<facts>
Branch already checked out: fix/80-rotate-committed-secrets. NO worktree isolation (this plan needs the untracked .env + the live Docker stack). SINGLE plan, exactly 3 tasks. Target ~40% context.

Live stack bring-up: `docker compose -f docker-compose.full-stack.yml -f docker-compose.frontend-3100.yml`. Keycloak 24.0.5, realm `jtoye-dev`, frontend on host :3100, core-java :9090, Keycloak :8085.

Realm import mechanism (verified via Keycloak docs, context7 /keycloak/keycloak): native `${VAR}` substitution in the realm JSON is gated behind `ExportImportConfig.isReplacePlaceholders()` and is NOT reliably enabled by default in the 24.0.5 `--import-realm` flow. DECISION: use the envsubst render-sidecar fallback (blessed by the task constraints) — a tracked `.template.json`, a gitignored rendered `.json` produced at container start.

Where the tracked secrets live (verified, values deliberately not reproduced here):
- infra/keycloak/realm-export.json — core-api client `secret` (~L634), edge-api client `secret` (~L719); realm `org.keycloak.keys.KeyProvider` array with HMAC/AES/RSA secret material (~L1495–1535); and a `users` array (~L397–490) whose admin-user / tenant-a-user / tenant-b-user carry PBKDF2 `secretData`/`credentialData` password hashes. Those hashed creds — NOT configure-keycloak.sh — are the real seed logins (the script's create-user branches skip because the imported users already exist).
- infra/keycloak/configure-keycloak.sh — master-admin password literal (L25), seed-user password literal in two create bodies (L117, L148), password-echoing summary (L245–253).
- docker-compose.full-stack.yml — weak `:-minioadmin` fallbacks at S3 access/secret (L144–145), minio root user/pass (L252–253), and the minio-init `mc alias` line (L276, `$$`-escaped for container-shell expansion); bare `${KEYCLOAK_ADMIN_PASSWORD}` (L40) and `${KEYCLOAK_CLIENT_SECRET}`/`${NEXTAUTH_SECRET}` (frontend).
- infra/docker-compose.yml — weak `${KC_ADMIN_PASSWORD:-admin123}` (L39). This file IS used (scripts/start-dev.sh runs `cd infra && docker compose up -d`), so it needs the same render treatment, not just a fallback fix.
- Tracked example/docs with literals: .env.example (root, `MINIO_ROOT_USER=minioadmin` L50), infra/.env.example (`DB_PASSWORD=secret` L28, `KC_ADMIN_PASSWORD=admin123` L37, NOTES block L45–50), frontend/.env.local.example (`KEYCLOAK_CLIENT_SECRET=core-api-secret-2026` L27 + a literal NEXTAUTH_SECRET), infra/keycloak/README.md (core-api-secret-2026 L23/L149, password123/admin123 credential tables L52–54/L93).
- .gitleaks.toml allowlists the realm-export.json path (comment: "Flagged for follow-up: template these"). CHANGE_ME / REPLACE_WITH_* are content-allowlisted, so placeholder commits pass the gitleaks pre-commit hook.

Consumer wiring (verified):
- core-api client secret's ONLY live consumer is the frontend NextAuth provider (frontend/auth.ts: process.env.KEYCLOAK_CLIENT_SECRET, used for login + refresh). core-java is a resource server (application.yml oauth2.resourceserver.jwt.issuer-uri only — no client secret).
- edge-api client secret has NO live env consumer (edge-go only reads WHATSAPP_SERVICE_CLIENT_ID/SECRET, unset). Rotate it in the realm to remove the tracked literal; no downstream env change needed.
- Seed-user login (tenant-a-user) comes from the realm import hash. The vendor-refund E2E defaults VENDOR_PASSWORD to "password123" but honours E2E_VENDOR_PASSWORD.
</facts>

<interfaces>
frontend/auth.ts (already deployed — do NOT change): the Keycloak provider reads
  clientId: process.env.KEYCLOAK_CLIENT_ID  (= core-api)
  clientSecret: process.env.KEYCLOAK_CLIENT_SECRET
So the realm's core-api client `secret` MUST render from the SAME env var (KEYCLOAK_CLIENT_SECRET) — reuse it as the template placeholder so realm-side and frontend-side values are provably identical.

frontend/e2e/vendor-refund-flow.spec.ts vendorLogin() honours:
  PLAYWRIGHT_BASE_URL (default http://localhost:3100)
  E2E_VENDOR_USERNAME (default tenant-a-user)
  E2E_VENDOR_PASSWORD (default password123)
It reaches /dashboard on successful SSO; the refund steps self-skip when no refundable order is seeded. Reaching /dashboard is the SSO pass criterion.

playwright.config.ts: testDir ./e2e, projects "mobile" and "desktop"; baseURL from PLAYWRIGHT_BASE_URL.

Keycloak realm import accepts a PLAINTEXT credential object ({ "type":"password", "value":"...", "temporary":false }) and hashes it on import — this is how a seed password gets templated without pre-computing a PBKDF2 hash.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rotate + template all tracked secrets (the tracked-file surgery)</name>
  <files>infra/keycloak/realm-export.template.json, infra/keycloak/realm-export.json, .gitignore, .gitleaks.toml, docker-compose.full-stack.yml, infra/docker-compose.yml, infra/keycloak/configure-keycloak.sh, infra/keycloak/README.md, .env.example, infra/.env.example, frontend/.env.local.example, .env</files>
  <action>
Template the realm and purge every tracked literal. Step order:

1. `git mv infra/keycloak/realm-export.json infra/keycloak/realm-export.template.json`. In the template: replace the core-api client `secret` value with the placeholder `${KEYCLOAK_CLIENT_SECRET}` and the edge-api client `secret` value with `${EDGE_API_CLIENT_SECRET}`. For each of admin-user, tenant-a-user, tenant-b-user in the `users` array, replace their hashed credential object (the one carrying `id`/`createdDate`/`secretData`/`credentialData`) with a plaintext credential object of type password, value `${KC_SEED_USER_PASSWORD}`, temporary false — leaving the service-account users (empty credentials) untouched. DELETE the entire `org.keycloak.keys.KeyProvider` array from the `components` object (keep the sibling `org.keycloak.services.clientregistration.policy.ClientRegistrationPolicy` array) so Keycloak regenerates realm signing/enc keys on import. Keep JSON valid (mind the trailing comma left after removing the KeyProvider array).

2. Add `infra/keycloak/realm-export.json` to `.gitignore` (the rendered artifact must never be tracked). In `.gitleaks.toml`, remove the now-obsolete realm-export.json path-allowlist entry and its follow-up comment so gitleaks actively guards the template going forward.

3. docker-compose.full-stack.yml: add a one-shot render sidecar service `keycloak-realm-render` (minimal image providing envsubst — e.g. alpine plus `apk add --no-cache gettext`) that bind-mounts `./infra/keycloak` read-write, receives `KEYCLOAK_CLIENT_SECRET`, `EDGE_API_CLIENT_SECRET`, `KC_SEED_USER_PASSWORD` as environment each in `${VAR:?message}` form, and runs envsubst restricted to exactly those three variable names, reading realm-export.template.json and writing realm-export.json into the mounted dir. Make the `keycloak` service `depends_on` the render sidecar with `condition: service_completed_successfully` (its existing bind mount of `./infra/keycloak/realm-export.json` stays unchanged). Convert every weak fallback to fail-loud required form: S3_ACCESS_KEY/S3_SECRET_KEY (L144–145) to `${MINIO_ROOT_USER:?...}`/`${MINIO_ROOT_PASSWORD:?...}`, minio env (L252–253) likewise, and give minio-init an `environment:` block setting MINIO_ROOT_USER/MINIO_ROOT_PASSWORD via `${VAR:?...}` while keeping the `mc alias` command referencing `$$MINIO_ROOT_USER`/`$$MINIO_ROOT_PASSWORD` (container-shell expansion, so no literal is baked into the command string — drop the `:-minioadmin` defaults). Convert bare `${KEYCLOAK_ADMIN_PASSWORD}`, `${KEYCLOAK_CLIENT_SECRET}`, `${NEXTAUTH_SECRET}` to `${VAR:?...}`.

4. infra/docker-compose.yml: this file is live (start-dev.sh uses it). Add the SAME `keycloak-realm-render` sidecar (bind-mount `./keycloak`), wire keycloak `depends_on: service_completed_successfully`, and change `${KC_ADMIN_PASSWORD:-admin123}` (L39) to `${KC_ADMIN_PASSWORD:?...}`.

5. infra/keycloak/configure-keycloak.sh: add fail-loud guards at the top (`: "${KC_ADMIN_PASSWORD:?KC_ADMIN_PASSWORD must be set}"` and the same for `KC_SEED_USER_PASSWORD`). Replace the admin-token password literal (L25) with the KC_ADMIN_PASSWORD value and both seed-user password literals (L117, L148) with the KC_SEED_USER_PASSWORD value (adjust the single-quoted JSON bodies so the shell interpolates the var). Rewrite the summary echo block (L245–253) to reference credential var NAMES, never literal password values.

6. Example/docs de-literalisation (use obviously-fake CHANGE_ME__ placeholders with a one-line comment each): .env.example root — `MINIO_ROOT_USER` to a CHANGE_ME placeholder, add `EDGE_API_CLIENT_SECRET` and `KC_SEED_USER_PASSWORD`, keep KEYCLOAK_CLIENT_SECRET/NEXTAUTH_SECRET as CHANGE_ME and note that KEYCLOAK_CLIENT_SECRET also renders the realm core-api client. infra/.env.example — replace `DB_PASSWORD=secret`, `KC_ADMIN_PASSWORD=admin123`, and the NOTES block literals with CHANGE_ME placeholders; add KC_SEED_USER_PASSWORD, KEYCLOAK_CLIENT_SECRET, EDGE_API_CLIENT_SECRET (needed by the infra render sidecar). frontend/.env.local.example — KEYCLOAK_CLIENT_SECRET and NEXTAUTH_SECRET to CHANGE_ME. infra/keycloak/README.md — replace the working literal credentials (core-api-secret-2026, and the password123/admin123 credential tables) with guidance that points to the env var names (KEYCLOAK_CLIENT_SECRET, KC_ADMIN_PASSWORD, KC_SEED_USER_PASSWORD); a "don't use weak passwords like X" caution line may keep the words as negative examples if git-grep scope stays clean.

7. Generate fresh strong values into the UNTRACKED .env (create keys that are missing, overwrite weak ones) using `openssl rand -base64 32` (or -hex 32) for: KEYCLOAK_ADMIN_PASSWORD, KC_ADMIN_PASSWORD, KEYCLOAK_CLIENT_SECRET, EDGE_API_CLIENT_SECRET, MINIO_ROOT_USER, MINIO_ROOT_PASSWORD, KC_SEED_USER_PASSWORD, plus any still-weak required var flagged by the deny-list. Do this by piping openssl output directly into the .env write — never echo a generated value to stdout, into a committed file, or into the eventual SUMMARY. Then run the render sidecar once (or `envsubst` locally) so the gitignored infra/keycloak/realm-export.json exists for the compose bind mount.

SECURITY: no rotated value may appear in any tracked file or commit message. The template holds `${...}` placeholders only. Commit as ONE atomic commit, conventional prefix (e.g. `fix(security): rotate + template committed Keycloak/MinIO credentials`), NO Co-Authored-By trailer. If the gitleaks pre-commit hook blocks, confirm it is a CHANGE_ME/placeholder false positive before proceeding.
  </action>
  <verify>
    <automated>git grep -nI -e 'core-api-secret-2026' -e 'edge-api-secret-2026' -e 'password123' -e 'admin123' -e 'minioadmin' -- 'infra/**' 'scripts/**' 'docker-compose*.yml' | grep -v -E '^infra/keycloak/README\.md:.*(don.?t|avoid|weak|never)' | grep -vc '^$' | grep -q '^0$' && echo GREP_CLEAN</automated>
    <automated>test -f infra/keycloak/realm-export.template.json && git check-ignore -q infra/keycloak/realm-export.json && echo TEMPLATE_OK</automated>
    <automated>grep -q '"components"' infra/keycloak/realm-export.template.json && ! grep -q 'org.keycloak.keys.KeyProvider' infra/keycloak/realm-export.template.json && echo KEYS_STRIPPED</automated>
    <automated>set -a; . ./.env; set +a; envsubst '${KEYCLOAK_CLIENT_SECRET} ${EDGE_API_CLIENT_SECRET} ${KC_SEED_USER_PASSWORD}' < infra/keycloak/realm-export.template.json | jq -e '.clients[]|select(.clientId=="core-api").secret|length>0' >/dev/null && echo RENDER_VALID_JSON</automated>
    <automated>! grep -nE ':-(minioadmin|admin123|secret)\b' docker-compose.full-stack.yml infra/docker-compose.yml && echo NO_WEAK_FALLBACKS</automated>
  </verify>
  <done>Realm is a tracked template with placeholder client secrets + seed password and no KeyProvider secret material; rendered realm-export.json is gitignored and renders to valid JSON; both composes have a render sidecar and zero weak `:-` fallbacks; configure-keycloak.sh and all example/doc files are literal-free; .env holds fresh rotated values; one atomic commit made with no secret in it.</done>
</task>

<task type="auto">
  <name>Task 2: Harden verify-env.sh into a fail-loud env gate and wire it into bring-up</name>
  <files>scripts/verify-env.sh, scripts/start-dev.sh</files>
  <action>
Repurpose scripts/verify-env.sh (currently a running-stack smoke test that nothing calls) into an environment-variable preflight, preserving its existing colour/pass/fail helper style. Add a usage/wiring header comment. Add a FIRST section that loads `.env` (source it, or a path passed as `$1`, default `./.env`) and enforces:
  (a) Required vars are set AND non-empty — fail, naming each missing var: POSTGRES_PASSWORD, KEYCLOAK_ADMIN_PASSWORD, KC_ADMIN_PASSWORD, KC_DB_PASSWORD, REDIS_PASSWORD, RABBITMQ_DEFAULT_PASS, DB_PASSWORD, RABBITMQ_PASSWORD, MINIO_ROOT_USER, MINIO_ROOT_PASSWORD, NEXTAUTH_SECRET, KEYCLOAK_CLIENT_SECRET, EDGE_API_CLIENT_SECRET, KC_SEED_USER_PASSWORD.
  (b) No required var's value matches the weak deny-list, case-insensitive: admin123, password123, minioadmin, changeme, any value beginning with CHANGE_ME, and any value ending in the `secret-2026` pattern. On a match, fail and name BOTH the var and that it is deny-listed (never print the value).
Exit non-zero on any failure; exit 0 only when all required vars pass. Gate the pre-existing running-stack tests (health/DB/Flyway/RLS) behind an opt-in `--with-stack` flag so the default invocation is a pure, stack-independent env check (smoke-test.sh already covers stack health). Keep the deny-list tokens and required-var list defined as arrays near the top so they are maintainable.

Wire it: add `bash scripts/verify-env.sh "$@" || { echo 'verify-env failed'; exit 1; }` as a preflight near the top of scripts/start-dev.sh (before it brings up infra), and document that wiring point in the verify-env.sh header.

Commit as ONE atomic commit, conventional prefix (e.g. `feat(security): verify-env.sh enforces required + non-weak credentials`), NO Co-Authored-By.
  </action>
  <verify>
    <automated>bash scripts/verify-env.sh ./.env && echo ENV_PASSES_ROTATED</automated>
    <automated>printf 'KEYCLOAK_CLIENT_SECRET=changeme\n' > /tmp/claude-1000/weak.env 2>/dev/null || printf 'KEYCLOAK_CLIENT_SECRET=changeme\n' > /tmp/weak.env; WEAK=$(ls /tmp/claude-1000/weak.env 2>/dev/null || echo /tmp/weak.env); bash scripts/verify-env.sh "$WEAK"; test $? -ne 0 && echo WEAK_FAILS_AS_EXPECTED</automated>
    <automated>bash scripts/verify-env.sh "$WEAK" 2>&1 | grep -qi 'KEYCLOAK_CLIENT_SECRET' && echo NAMES_THE_VAR</automated>
    <automated>grep -q 'verify-env.sh' scripts/start-dev.sh && echo WIRED</automated>
  </verify>
  <done>verify-env.sh exits 0 on the rotated .env, exits non-zero and names the offending var on an unset/empty/deny-listed value, keeps the old stack tests behind `--with-stack`, documents its usage, and runs as a preflight in start-dev.sh.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the rotated stack end-to-end (no commit unless fixes needed)</name>
  <files></files>
  <action>
Bring up the rotated live stack and prove rotation worked. Read every secret from the environment by var NAME (e.g. `$KEYCLOAK_CLIENT_SECRET`, `$KC_SEED_USER_PASSWORD` after `set -a; . ./.env; set +a`) — never type a literal value anywhere.

1. Preflight: `bash scripts/verify-env.sh ./.env` passes (reuses Task 2).
2. Force a clean realm re-import with the rotated secrets: `docker compose -f docker-compose.full-stack.yml -f docker-compose.frontend-3100.yml down -v` (wipes volumes so the postgres-backed keycloak realm re-imports; note app data resets — dev-data seed under infra/db/init repopulates on the fresh DB). Then `docker compose -f docker-compose.full-stack.yml -f docker-compose.frontend-3100.yml up -d --build --force-recreate` so the render sidecar re-renders realm-export.json from the current .env before Keycloak imports it.
3. Wait for health: poll `docker compose ... ps` until keycloak, postgres, redis, rabbitmq, core-java, edge-go, frontend, minio are healthy (keycloak /health/ready on :8085, core-java /actuator/health on :9090, frontend /api/health on :3100). The keycloak-realm-render sidecar must have exited 0.
4. Run `KC_ADMIN_PASSWORD="$KC_ADMIN_PASSWORD" KC_SEED_USER_PASSWORD="$KC_SEED_USER_PASSWORD" bash infra/keycloak/configure-keycloak.sh` to (re)apply storefront-client, customer role, registration settings and the tenant_id mapper (imported seed users already exist; the script's create branches skip cleanly).
5. Positive token round-trip: POST to `http://localhost:8085/realms/jtoye-dev/protocol/openid-connect/token` with grant_type=password, client_id=core-api, client_secret=$KEYCLOAK_CLIENT_SECRET, username=tenant-a-user, password=$KC_SEED_USER_PASSWORD → expect HTTP 200 with a non-empty access_token. Call a protected endpoint `GET http://localhost:9090/shops` with `Authorization: Bearer <token>` → expect non-401 (the unauthenticated call returns 401, so non-401 proves the rotated credential authenticates).
6. Negative (invalidation) proof: repeat the same password grant but with client_secret=core-api-secret-2026 (the OLD literal) → expect HTTP 401 / invalid_client, proving the old secret no longer works.
7. E2E SSO gate: `cd frontend && PLAYWRIGHT_BASE_URL=http://localhost:3100 E2E_VENDOR_USERNAME=tenant-a-user E2E_VENDOR_PASSWORD="$KC_SEED_USER_PASSWORD" npx playwright test e2e/vendor-refund-flow.spec.ts --project=desktop` → the login portion reaches /dashboard (pass). The refund assertions may self-skip when no refundable order is seeded — a skip is acceptable; a login FAILURE is not.
8. Confirm the stack ends fully healthy and the frontend serves at http://localhost:3100.

No commit unless a fix is required to make the proof pass. If a fix IS needed (e.g. render wiring, a placeholder in the template, a compose var), amend the relevant tracked file and make ONE additional atomic commit with a conventional prefix and NO Co-Authored-By, then re-run the affected proof step. In the SUMMARY, record only var NAMES; explicitly note that rotating the realm keys + recreating Keycloak invalidated existing SSO sessions (users must re-login) and that `down -v` reset app data. Confirm docs/metrics.json is unchanged and the docs-freshness gate still passes (no Java/Jest/Go/Playwright test counts changed).
  </action>
  <verify>
    <automated>docker compose -f docker-compose.full-stack.yml -f docker-compose.frontend-3100.yml ps --format '{{.Service}} {{.Status}}' | grep -E 'keycloak|core-java|frontend' | grep -qiv 'unhealthy\|starting' && echo STACK_HEALTHY</automated>
    <automated>set -a; . ./.env; set +a; code=$(curl -s -o /tmp/tok.json -w '%{http_code}' -X POST http://localhost:8085/realms/jtoye-dev/protocol/openid-connect/token -d grant_type=password -d client_id=core-api -d client_secret="$KEYCLOAK_CLIENT_SECRET" -d username=tenant-a-user -d password="$KC_SEED_USER_PASSWORD"); test "$code" = 200 && jq -e '.access_token|length>0' /tmp/tok.json >/dev/null && echo POSITIVE_GRANT_200</automated>
    <automated>set -a; . ./.env; set +a; tok=$(jq -r .access_token /tmp/tok.json); s=$(curl -s -o /dev/null -w '%{http_code}' -H "Authorization: Bearer $tok" http://localhost:9090/shops); test "$s" != 401 && echo PROTECTED_NON_401</automated>
    <automated>old=$(curl -s -o /dev/null -w '%{http_code}' -X POST http://localhost:8085/realms/jtoye-dev/protocol/openid-connect/token -d grant_type=password -d client_id=core-api -d client_secret=core-api-secret-2026 -d username=tenant-a-user -d password=x); test "$old" = 401 && echo OLD_SECRET_INVALIDATED</automated>
    <automated>cd frontend && PLAYWRIGHT_BASE_URL=http://localhost:3100 E2E_VENDOR_USERNAME=tenant-a-user E2E_VENDOR_PASSWORD="$KC_SEED_USER_PASSWORD" npx playwright test e2e/vendor-refund-flow.spec.ts --project=desktop --reporter=line 2>&1 | grep -Eqi '([1-9][0-9]* passed)|(passed.*skipped)|(0 failed)' && echo E2E_LOGIN_OK</automated>
  </verify>
  <done>Rotated stack is healthy; password-grant on core-api with the rotated secret returns 200 + access_token and authenticates a protected endpoint non-401; the OLD core-api-secret-2026 returns 401; the Playwright SSO login reaches /dashboard; docs-freshness/metrics.json unchanged; SSO-session-invalidation + app-data-reset noted in SUMMARY; no secret value logged anywhere.</done>
</task>

</tasks>

<multi_source_coverage_audit>
Source = Issue #80 [P0-4] description + orchestrator constraints (no ROADMAP/REQUIREMENTS/CONTEXT/RESEARCH artifacts for a quick task).

| Item (from issue + constraints) | Covered by |
|---|---|
| Rotate committed client secrets (core-api, edge-api) | Task 1 (template + .env), Task 3 (invalidation proof) |
| Rotate weak defaults (admin123, password123, minioadmin) | Task 1 (.env + de-literalise), Task 2 (deny-list), Task 3 (proof) |
| Template realm export; no literal secret tracked | Task 1 (template + gitignore + gitleaks) |
| Strip/regenerate realm keystore key material | Task 1 (delete KeyProvider array) |
| Template seed-user password hashes | Task 1 (plaintext `${KC_SEED_USER_PASSWORD}` credential) |
| Remove weak compose fallbacks → `${VAR:?}` | Task 1 (full-stack + infra composes, incl. mc alias L276) |
| Gate with verify-env.sh (required + deny-list) | Task 2 |
| Prove rotated stack end-to-end (token round-trip + negative + E2E) | Task 3 |
| Old secrets invalidated | Task 3 negative grant → 401 |
| No secret value in any committed file / message / SUMMARY | All tasks (var-names-only rule) |
| Session/user impact (SSO re-login) noted | Task 3 SUMMARY note |
| metrics.json unchanged / docs-freshness passes | Task 3 confirmation |

No unplanned items. No PHASE SPLIT needed (fits one plan, 3 tasks, ~40% context).
</multi_source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public git repo → world | Anyone can read tracked files; committed secrets are compromised on push |
| .env (untracked) → containers | Rotated secrets injected as env; must never re-enter tracked surface |
| browser/client → Keycloak/core-api | OIDC login + bearer tokens cross here |
| render sidecar → keycloak import | envsubst writes the only file that carries resolved secrets, gitignored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-l2c-01 | Information Disclosure | committed client secrets / realm keys / seed hashes | mitigate | Task 1 templates the realm; literals removed; rendered file gitignored; keys regenerated on import |
| T-l2c-02 | Spoofing / Elevation | leaked core-api secret usable for token grants | mitigate | Task 1 rotates the secret; Task 3 negative grant proves the old secret returns 401 |
| T-l2c-03 | Tampering | weak `:-` compose fallbacks silently boot with minioadmin/admin123 | mitigate | Task 1 converts all to `${VAR:?}` fail-loud; Task 2 deny-list blocks weak values |
| T-l2c-04 | Information Disclosure | a rotated value re-entering a tracked file / commit / SUMMARY / log | mitigate | var-names-only rule across all tasks; gitleaks pre-commit hook + Task 1 git-grep gate |
| T-l2c-05 | Repudiation / DoS | SSO sessions silently break after key rotation | accept | Expected: recreating Keycloak invalidates sessions; Task 3 SUMMARY notes users must re-login |
| T-l2c-SC | Tampering | render sidecar base image / `apk add gettext` supply chain | accept | dev-only local bring-up, official alpine + apk repo; no npm/pip/cargo library install introduced |
</threat_model>

<verification>
- `git grep` across infra/, scripts/, docker-compose*.yml is clean of the five old literals (audit/backlog docs under docs/ and .planning/ may still reference them as findings — out of scope).
- Templated realm renders to valid JSON via envsubst; KeyProvider array absent; core-api/edge-api secrets and seed password are placeholders.
- Both composes free of weak `:-` fallbacks; required credential vars use `${VAR:?}`.
- verify-env.sh: exit 0 on rotated .env; non-zero (naming the var) on unset/empty/deny-listed value; stack tests behind `--with-stack`.
- Rotated live stack healthy; positive grant 200 + protected non-401; OLD secret 401; Playwright SSO login reaches /dashboard.
- docs/metrics.json unchanged; docs-freshness gate green.
</verification>

<success_criteria>
- No committed literal secret survives in infra/, scripts/, or compose files.
- Realm export is templated; rendered artifact gitignored; realm keys regenerate on import.
- Rotated values live only in the untracked .env; no value appears in any tracked file, commit message, PLAN.md, or SUMMARY.md.
- verify-env.sh is a fail-loud env gate wired into bring-up.
- The rotated stack is proven end-to-end and the old secrets are demonstrably invalidated.
- Two-to-three atomic commits (Task 1, Task 2, optional Task 3 fix), conventional prefixes, no Co-Authored-By trailers.
</success_criteria>

<output>
Create `.planning/quick/260708-l2c-issue-80-p0-4-rotate-committed-keycloak-/260708-l2c-SUMMARY.md` when done. Record var NAMES only; note SSO-session invalidation (re-login expected) and app-data reset from `down -v`; confirm metrics.json unchanged.
</output>
