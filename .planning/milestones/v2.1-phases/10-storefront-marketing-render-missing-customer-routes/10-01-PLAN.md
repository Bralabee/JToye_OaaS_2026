---
phase: 10-storefront-marketing-render-missing-customer-routes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core-java/src/test/java/uk/jtoye/core/storefront/PublicStorefrontControllerTest.java
autonomous: true
requirements: [STFR-01, STFR-02]
must_haves:
  truths:
    - "GET /public/shops/{slug}/promotions returns 200 + filtered list for a known shop"
    - "GET /public/shops/{slug}/promotions returns 404 for an unknown shop slug"
    - "GET /public/shops/{slug}/announcements returns 200 + filtered list for a known shop"
    - "GET /public/shops/{slug}/announcements returns 404 for an unknown shop slug"
    - "./gradlew :core-java:test --tests PublicStorefrontControllerTest is green (pre-existing + new tests)"
  artifacts:
    - path: "core-java/src/test/java/uk/jtoye/core/storefront/PublicStorefrontControllerTest.java"
      provides: "4 new MockMvc controller integration tests (2 endpoints x success+404)"
      contains: "getShopPromotions_returns200WithActivePromotions, getShopPromotions_nonexistent_returns404, getShopAnnouncements_returns200WithActiveAnnouncements, getShopAnnouncements_nonexistent_returns404"
  key_links:
    - from: "PublicStorefrontControllerTest"
      to: "PublicStorefrontService (mocked)"
      via: "@MockitoBean"
      pattern: "@MockitoBean\\s+private\\s+PublicStorefrontService"
---

<objective>
Close the STFR-01 / STFR-02 controller-level integration test gap identified in
10-RESEARCH.md Validation Architecture (§Wave 0 Gaps). The backend endpoints
`GET /public/shops/{slug}/promotions` and `/announcements` already exist and are
service-unit-tested — this plan adds the missing MockMvc slice tests so the REST
contract is locked in.

Purpose: Ensure the public marketing endpoints cannot regress silently — every
route used by the storefront render in plan 10-02 has a controller-level test.

Output: 4 new test methods added to the existing
`PublicStorefrontControllerTest.java`, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/10-storefront-marketing-render-missing-customer-routes/10-RESEARCH.md

@core-java/src/main/java/uk/jtoye/core/storefront/PublicStorefrontController.java
@core-java/src/main/java/uk/jtoye/core/storefront/PublicStorefrontService.java
@core-java/src/test/java/uk/jtoye/core/storefront/PublicStorefrontControllerTest.java
@core-java/src/test/java/uk/jtoye/core/storefront/PublicStorefrontServiceTest.java

<interfaces>
<!-- Key types the executor needs; from research lines 117-152 -->

Controller methods under test (PublicStorefrontController.java:64-74):
  GET /public/shops/{slug}/promotions   -> List<PublicPromotionDto>
  GET /public/shops/{slug}/announcements -> List<PublicAnnouncementDto>

Service dependency (mocked in test):
  PublicStorefrontService.getActivePromotions(String slug)   -> List<PublicPromotionDto>
  PublicStorefrontService.getActiveAnnouncements(String slug) -> List<PublicAnnouncementDto>

DTO fields the tests must assert on:
  PublicPromotionDto: label, discountType (enum PERCENTAGE|FLAT_AMOUNT),
                     discountPercent, discountAmountPennies, category, validUntil
  PublicAnnouncementDto: title, body, validUntil

404 path: service throws ResourceNotFoundException; handled by
  GlobalExceptionHandler -> HTTP 404.

Existing test class shape (PublicStorefrontControllerTest.java:28-39):
  @WebMvcTest(PublicStorefrontController.class)
  @AutoConfigureMockMvc(addFilters = false)
  class PublicStorefrontControllerTest {
      @Autowired private MockMvc mockMvc;
      @MockitoBean private PublicStorefrontService storefrontService;
      @MockitoBean private ReviewService reviewService;
      ...
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add promotions MockMvc tests (success + 404)</name>
  <files>core-java/src/test/java/uk/jtoye/core/storefront/PublicStorefrontControllerTest.java</files>
  <behavior>
    - getShopPromotions_returns200WithActivePromotions: when
      storefrontService.getActivePromotions("test-shop") returns a single
      PublicPromotionDto(label="Lunch special", discountType=PERCENTAGE,
      discountPercent=10, category="Mains"), GET /public/shops/test-shop/promotions
      returns 200 with JSON array, $[0].label="Lunch special",
      $[0].discountPercent=10.
    - getShopPromotions_nonexistent_returns404: when
      storefrontService.getActivePromotions("ghost") throws
      ResourceNotFoundException("Shop not found: ghost"),
      GET /public/shops/ghost/promotions returns 404.
  </behavior>
  <action>
    Open core-java/src/test/java/uk/jtoye/core/storefront/PublicStorefrontControllerTest.java.
    Confirm `@MockitoBean private PublicStorefrontService storefrontService;`
    is already declared on the class (per research lines 28-39 — it is).

    Add TWO new @Test methods following the exact shape in 10-RESEARCH.md
    Pattern 1 (lines 117-152). Use `when(...).thenReturn(...)` / `thenThrow(...)`
    on `storefrontService` — DO NOT mock EntityManager, Connection, or
    PreparedStatement (Pitfall 7, research lines 298-302).

    For the success test: construct a `PublicPromotionDto` via its no-arg
    constructor + setters (matches DTO style used elsewhere in the file),
    with label="Lunch special", discountType=DiscountType.PERCENTAGE,
    discountPercent=10, category="Mains". Assert jsonPath $[0].label and
    $[0].discountPercent. Import
    `uk.jtoye.core.shop.ShopPromotion.DiscountType` if the existing file
    does not already pull it in (check imports first).

    For the 404 test: stub the service to throw
    `new ResourceNotFoundException("Shop not found: ghost")` and assert
    `status().isNotFound()`. Import `uk.jtoye.core.exception.ResourceNotFoundException`
    (should already be in scope — verify).

    DO NOT add controller code. DO NOT add service code. Everything under test
    already exists (PublicStorefrontController.java:64-74, service at
    PublicStorefrontService.java:128-162).

    URL is `/public/shops/{slug}/promotions` — bare, NOT `/api/v1/public/...`
    (Pitfall 1, WebConfig.java:23-33 excludes the storefront package from the
    /api/v1 prefix). Copy the URL shape from the existing getShopProducts test
    already in this file.
  </action>
  <verify>
    <automated>cd /home/sanmi/IdeaProjects/JToye_OaaS_2026 &amp;&amp; JAVA_HOME=/usr/lib/jvm/jdk-21.0.6-oracle-x64 ./gradlew :core-java:test --tests 'uk.jtoye.core.storefront.PublicStorefrontControllerTest.getShopPromotions*'</automated>
  </verify>
  <done>
    Both new tests present and pass; full PublicStorefrontControllerTest class
    still green (no regressions to existing methods).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add announcements MockMvc tests (success + 404)</name>
  <files>core-java/src/test/java/uk/jtoye/core/storefront/PublicStorefrontControllerTest.java</files>
  <behavior>
    - getShopAnnouncements_returns200WithActiveAnnouncements: when
      storefrontService.getActiveAnnouncements("test-shop") returns a single
      PublicAnnouncementDto(title="Closed Sunday", body="Back Monday",
      validUntil=null), GET /public/shops/test-shop/announcements returns
      200 with $[0].title="Closed Sunday".
    - getShopAnnouncements_nonexistent_returns404: when
      storefrontService.getActiveAnnouncements("ghost") throws
      ResourceNotFoundException, GET /public/shops/ghost/announcements
      returns 404.
  </behavior>
  <action>
    Add TWO more @Test methods to the same file, mirroring task 1 but for
    the announcements endpoint. Construct `PublicAnnouncementDto` via no-arg
    constructor + setters; assert jsonPath $[0].title. Use the same
    `@MockitoBean` storefrontService already on the class — no new beans.

    Reuse the exact MockMvc + when/thenReturn patterns from task 1 and from
    the existing getShopProducts / getShopReviews tests in the file.
    URL is `/public/shops/{slug}/announcements` (bare path).

    Import `uk.jtoye.core.storefront.dto.PublicAnnouncementDto` if not
    already present (check the top of the file first — research did not
    enumerate the exact DTO package, so verify by opening
    PublicStorefrontController.java and following the import there).
  </action>
  <verify>
    <automated>cd /home/sanmi/IdeaProjects/JToye_OaaS_2026 &amp;&amp; JAVA_HOME=/usr/lib/jvm/jdk-21.0.6-oracle-x64 ./gradlew :core-java:test --tests 'uk.jtoye.core.storefront.PublicStorefrontControllerTest'</automated>
  </verify>
  <done>
    All 4 new tests (2 from task 1 + 2 from task 2) pass as part of the full
    PublicStorefrontControllerTest class. Test count for this class increases
    by 4. No changes to production sources.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full backend regression + atomic commit</name>
  <files>core-java/src/test/java/uk/jtoye/core/storefront/PublicStorefrontControllerTest.java</files>
  <action>
    Run the full backend test suite to confirm no regression against the
    M2 baseline (335+ Java tests). Test count should increase by exactly 4.

    Then create a single atomic commit on the current feature branch with
    message:
      test(stfr): add MockMvc tests for /public/shops/{slug}/promotions + /announcements (STFR-01, STFR-02)

    Do not update REQUIREMENTS.md Traceability in this commit — plan 10-03
    task 6 handles that as the final narrative commit for the phase.

    Reminder on branch policy: user's global CLAUDE.md forbids direct commits
    to main. If currently on main, create a feature branch first
    (e.g. `feature/phase-10-storefront-marketing-render`).
  </action>
  <verify>
    <automated>cd /home/sanmi/IdeaProjects/JToye_OaaS_2026 &amp;&amp; JAVA_HOME=/usr/lib/jvm/jdk-21.0.6-oracle-x64 ./gradlew :core-java:test</automated>
  </verify>
  <done>
    Full backend test suite green, new commit exists on a feature branch with
    only this file modified, no unrelated edits.
  </done>
</task>

</tasks>

<verification>
- `./gradlew :core-java:test --tests PublicStorefrontControllerTest` green
- `./gradlew :core-java:test` green (full backend regression, no drop from baseline)
- 4 new @Test methods exist in PublicStorefrontControllerTest.java
- No edits to any production source file under core-java/src/main/
- No edits to any frontend file (that's plan 10-02's surface)
</verification>

<success_criteria>
STFR-01 and STFR-02 are fully backed by controller-level MockMvc tests
(research §Validation Architecture Wave 0 Gaps item 1 closed). Service-layer
tests already exist and continue to pass.
</success_criteria>

<output>
After completion, create
`.planning/phases/10-storefront-marketing-render-missing-customer-routes/10-01-SUMMARY.md`
</output>
