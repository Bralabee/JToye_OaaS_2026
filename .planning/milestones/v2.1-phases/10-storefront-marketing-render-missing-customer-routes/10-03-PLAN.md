---
phase: 10-storefront-marketing-render-missing-customer-routes
plan: 03
type: execute
wave: 2
depends_on: [10-02]
files_modified:
  - frontend/app/shop/orders/page.tsx
  - frontend/__tests__/shop/orders-filter.test.tsx
  - frontend/e2e/storefront-flows.spec.ts
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [STFR-05, STFR-06]
must_haves:
  truths:
    - "Authenticated customer on /shop/orders sees a status filter (select) and date filter (input type=date)"
    - "Filtered results are paginated client-side at 10 per page with prev/next controls"
    - "A pure-logic Jest spec proves the filter+pagination derivation behaves correctly for empty, small, and overflow datasets"
    - "The existing Playwright storefront-flows e2e asserts the announcement banner + discount badge render on the shop detail page"
    - "The existing Playwright checkout path still ends on the COD confirmation screen (no real Stripe card entry introduced)"
    - "REQUIREMENTS.md Traceability table marks STFR-01..06 as Done and the coverage counts are updated"
  artifacts:
    - path: "frontend/app/shop/orders/page.tsx"
      provides: "Status filter + date filter + 10-per-page client pagination over the existing /public/orders?email= response"
      contains: "statusFilter"
    - path: "frontend/__tests__/shop/orders-filter.test.tsx"
      provides: "Jest unit coverage for the filter+pagination derivation (pure logic — no component render required if logic is extractable)"
    - path: "frontend/e2e/storefront-flows.spec.ts"
      provides: "Extended storefront e2e with banner/badge assertions and explicit cart-page navigation step"
      contains: "promotion banner"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase-10 traceability + coverage count updated"
      contains: "STFR-01 | Phase 10 | Done"
  key_links:
    - from: "frontend/app/shop/orders/page.tsx"
      to: "useMemo-derived filtered + paged orders"
      via: "client-side .filter + .slice"
      pattern: "statusFilter|dateFrom|PAGE_SIZE"
    - from: "frontend/e2e/storefront-flows.spec.ts"
      to: "shop detail page banner + badge DOM"
      via: "Playwright locator + toBeVisible assertion"
      pattern: "promotion banner|discount badge"
---

<objective>
Close STFR-05 (status filter + date filter + pagination on the already-existing
`/shop/orders` page) and STFR-06 (extend the existing Playwright e2e to assert
the banner/badge render on the shop detail page, keep it on the COD checkout
path per Pitfall 6, and include an explicit cart-page navigation step).
Then finalise phase 10 by updating `.planning/REQUIREMENTS.md` traceability.

Purpose: The orders page currently only has an active/past split; customers
need filters + pagination to navigate their history. The e2e flow already
walks browse→add→checkout on the COD fallback, but never verifies the
marketing render and never explicitly visits `/shop/[slug]/cart`.

Output:
- Orders page with status + date filters and client-side pagination
- Jest spec for the filter+pagination derivation
- Extended e2e spec with banner/badge + cart-page assertions
- REQUIREMENTS.md updated to mark phase 10 done
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/10-storefront-marketing-render-missing-customer-routes/10-RESEARCH.md
@.planning/phases/10-storefront-marketing-render-missing-customer-routes/10-02-PLAN.md

@frontend/app/shop/orders/page.tsx
@frontend/components/storefront/require-customer-auth.tsx
@frontend/e2e/storefront-flows.spec.ts
@frontend/playwright.config.ts

<interfaces>
<!-- Derived from 10-RESEARCH.md Example 3 (lines 416-437) and Example 4 (441-458) -->

Orders page existing contract (from research summary + Example 3):
  - Gated by <RequireCustomerAuth>.
  - Loads orders via GET /public/orders?email={customerEmail}
    (path is bare /public/** — NOT /api/v1/public/** — per Pitfall 1).
  - Response shape: List<PublicOrderStatus> with at minimum
    { id, status, createdAt, ... } fields — read the actual page to
    confirm before writing filters.

Filter+pagination derivation (pure logic, test-friendly):
  const filtered = orders.filter(o => {
    if (statusFilter !== "ALL" && o.status !== statusFilter) return false
    if (dateFrom && new Date(o.createdAt) < new Date(dateFrom)) return false
    return true
  })
  const paged = filtered.slice((page - 1) * PAGE_SIZE, page * PAGE_SIZE)
  const totalPages = Math.ceil(filtered.length / PAGE_SIZE)

Playwright spec existing structure (storefront-flows.spec.ts:202-232):
  - Uses a SHOP_SLUG constant; extends `test.describe` blocks that are
    already serial (sequential per playwright.config.ts).
  - Existing test walks browse → add to floating cart → checkout → COD
    confirmation. Do NOT introduce Stripe card entry (Pitfall 6).
  - New assertions must reuse existing setup fixtures; no new auth.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract filter+pagination derivation + wire into orders page</name>
  <files>frontend/app/shop/orders/page.tsx</files>
  <behavior>
    - New state: `statusFilter` (default "ALL"), `dateFrom` (default ""),
      `page` (default 1). PAGE_SIZE constant = 10.
    - `filtered` is a useMemo over `orders`, `statusFilter`, `dateFrom`.
    - `paged` and `totalPages` derive from `filtered` + `page`.
    - UI controls: a native `<select>` for statusFilter populated with
      "ALL" + the distinct statuses used in the existing active/past split
      (read the page first; typical set is DRAFT/PENDING/CONFIRMED/PREPARING/
      READY/DELIVERED/CANCELLED — use whatever the current page switches on).
    - A native `<input type="date">` bound to dateFrom.
    - Prev/Next `<button>` controls with `disabled` state; "Page X of Y"
      label; Next disabled when `page >= totalPages || totalPages === 0`.
    - Resetting either filter sets `page` back to 1 (use a useEffect keyed
      on `[statusFilter, dateFrom]`).
    - `RequireCustomerAuth` wrapping is preserved unchanged.
    - Existing "active" / "past" split can stay or be replaced — prefer
      the simpler single-table + filter layout, but do not break any
      existing test selectors.
  </behavior>
  <action>
    Read frontend/app/shop/orders/page.tsx in full before editing. Confirm:
      - The RequireCustomerAuth wrapper and its import path.
      - The exact Order / OrderStatus type(s) used.
      - The fetch pattern (useEffect + publicApiClient or similar).
      - Whether the `/public/orders?email=` URL is bare or already prefixed
        (it MUST stay bare per Pitfall 1).

    Implementation:

    1. Extract the filter+pagination derivation into a pure function in
       the same file so the Jest test in task 2 can import it directly
       without rendering the component. Suggested shape:

         export function deriveOrdersView(
           orders: CustomerOrder[],
           opts: { statusFilter: string, dateFrom: string, page: number, pageSize: number }
         ): { filtered: CustomerOrder[], paged: CustomerOrder[], totalPages: number } {
           const filtered = orders.filter(o => {
             if (opts.statusFilter !== "ALL" && o.status !== opts.statusFilter) return false
             if (opts.dateFrom && new Date(o.createdAt) < new Date(opts.dateFrom)) return false
             return true
           })
           const start = (opts.page - 1) * opts.pageSize
           return {
             filtered,
             paged: filtered.slice(start, start + opts.pageSize),
             totalPages: Math.max(1, Math.ceil(filtered.length / opts.pageSize)),
           }
         }

       Use the actual CustomerOrder type name found in the file. Export the
       function explicitly so the Jest spec can import it.

    2. Add the three state hooks (statusFilter, dateFrom, page) and call
       `deriveOrdersView(orders, {...})` inside a `useMemo`.

    3. Render the controls above the list: a labelled `<select>` for status,
       `<input type="date">` for the date, and the prev/next + "Page X of Y"
       bar beneath the list.

    4. Add a `useEffect(() => setPage(1), [statusFilter, dateFrom])` to
       reset pagination on filter change.

    5. Do NOT change the fetch URL. Do NOT add a `verify` param — the soft
       enumeration risk on `/public/orders?email=` is explicitly out of
       scope per Pitfall 5 and the planning instructions. Leave a
       one-line comment at the fetch site referencing
       `.planning/phases/10-storefront-marketing-render-missing-customer-routes/10-RESEARCH.md §Pitfall 5`
       so the milestone-4 security ticket is easy to find.

    6. Styling: match the existing Tailwind conventions on the page.
       Do NOT import new UI libraries. Use native controls.
  </action>
  <verify>
    <automated>cd /home/sanmi/IdeaProjects/JToye_OaaS_2026/frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <done>
    File type-checks and lints; `deriveOrdersView` is exported; UI renders
    in dev without console errors; old fetch URL unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Jest spec for deriveOrdersView (STFR-05 test gap)</name>
  <files>frontend/__tests__/shop/orders-filter.test.tsx</files>
  <behavior>
    - "empty list returns empty paged with totalPages=1".
    - "filters by statusFilter=CONFIRMED keeps only CONFIRMED orders".
    - "filters by dateFrom excludes orders older than dateFrom".
    - "paginates 25 orders into 3 pages of 10/10/5 with correct slices".
    - "returns page 1 when overflow page requested" (optional hardening —
      include only if the impl clamps; otherwise assert empty slice).
  </behavior>
  <action>
    Create frontend/__tests__/shop/orders-filter.test.tsx. Import
    `deriveOrdersView` from `@/app/shop/orders/page`. Since this is pure
    logic, no render is needed — use plain Jest:

      import { deriveOrdersView } from "@/app/shop/orders/page"

      function mkOrder(i: number, status: string, createdAt: string) {
        return { id: `o${i}`, status, createdAt, /* ...minimal fields */ } as any
      }

      describe("deriveOrdersView", () => {
        it("returns empty paged for empty orders", () => { ... })
        it("filters by status", () => { ... })
        it("filters by dateFrom", () => { ... })
        it("paginates 25 orders into 3 pages of 10/10/5", () => { ... })
      })

    If the real `CustomerOrder` type requires more fields than the test
    cares about, cast via `as unknown as CustomerOrder` — do not
    over-specify. If the page does not currently export a reusable type,
    export one alongside `deriveOrdersView` in task 1.

    DO NOT test React rendering of the orders page in this spec — that
    requires mocking RequireCustomerAuth + publicApiClient and is out of
    scope for a "pure logic" gap closure. A render-level test can be
    added in a milestone-4 follow-up.
  </action>
  <verify>
    <automated>cd /home/sanmi/IdeaProjects/JToye_OaaS_2026/frontend &amp;&amp; npm test -- --testPathPattern=orders-filter</automated>
  </verify>
  <done>
    New test file exists with at least 4 passing cases covering empty,
    status filter, date filter, and pagination overflow.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend existing Playwright e2e with banner/badge + cart-page navigation (STFR-06)</name>
  <files>frontend/e2e/storefront-flows.spec.ts</files>
  <behavior>
    - A new `test("promotion banner and discount badge render on shop
      detail", ...)` asserts that at least one banner element is visible
      above the menu and at least one product card shows a "% off" or
      "£X off" badge (research Example 4, lines 441-458).
    - The existing browse→add→checkout test adds an explicit visit to
      `/shop/{SHOP_SLUG}/cart` between "add to cart" and "checkout", and
      asserts the cart page renders the added item before proceeding.
    - The existing checkout assertion stays on the COD confirmation path —
      no Stripe card interaction, no PaymentElement iframe traversal
      (Pitfall 6).
    - Spec is conditionally skipped if the seed shop has no active
      promotions (see research Open Question 3) — prefer:
        test.skip(!(await page.locator('[data-promo-badge], article:has-text(/\\d+% off/)').first().isVisible().catch(() => false)), 'No active promotion seed — banner assertion requires V33 seed or vendor API fixture')
      ...or add a small Playwright-side `beforeAll` that POSTs a fixture
      promotion via the vendor API. Researcher recommended deferring the
      fixture decision to planning — decision here: **skip on absence**
      rather than mutate state, so the e2e runs against whatever the dev
      stack has. Note this in the 10-03-SUMMARY.md follow-ups list so a
      future task can add V33 seed SQL or a setup fixture.
  </behavior>
  <action>
    Read frontend/e2e/storefront-flows.spec.ts in full first. Identify:
      - The SHOP_SLUG constant and any BASE URL const.
      - The existing "add items, view cart, modify quantity, checkout"
        test (~lines 202-232 per research).
      - Any describe grouping for storefront tests.

    Edits:

    1. Add the new banner/badge test inside the same describe block.
       Use the locator strategy from research Example 4 but with the
       conditional skip described in the behavior block above. Pull
       BASE / SHOP_SLUG from the existing consts — do not redeclare.

    2. Modify the existing checkout test to:
         a. After the "add to cart" step, `await page.goto(...)` to
            `/shop/${SHOP_SLUG}/cart`.
         b. Assert the added item's title is visible on the cart page.
         c. Click whatever checkout control the cart page exposes (read
            the real cart page to get the exact selector — matches the
            copy you used in plan 10-02 task 3).
         d. Preserve the existing COD-confirmation assertion.

    3. Do NOT add `pk_test_...` or any Stripe env var. Do NOT touch
       `js.stripe.com` iframes. The checkout path remains COD.

    4. Do NOT wire any new fixture injection via vendor JWT in this task —
       record "add V33 seed SQL for promotion fixture" as a follow-up in
       the summary at the end of the plan.
  </action>
  <verify>
    <automated>cd /home/sanmi/IdeaProjects/JToye_OaaS_2026/frontend &amp;&amp; npx playwright test e2e/storefront-flows.spec.ts --project=desktop</automated>
  </verify>
  <done>
    The extended e2e passes locally against the running full-stack compose.
    The banner/badge test either asserts successfully or skips cleanly on
    absent seed; the cart-page navigation step is explicit; no Stripe
    interaction added. Stack requirement: docker-compose.full-stack.yml
    must be up (core-java, keycloak, postgres, redis, rabbitmq, mailhog,
    frontend) before running.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of the full stack e2e + visual render</name>
  <files>frontend/app/shop/[slug]/page.tsx, frontend/app/shop/orders/page.tsx, frontend/e2e/storefront-flows.spec.ts</files>
  <action>Pause for human verification of the integrated render + navigation on the running full docker-compose stack. Do not proceed to task 5 until the user responds with "approved" or a list of issues.</action>
  <verify><automated>echo "Checkpoint — human verification step; no automated command"</automated></verify>
  <done>User responds "approved" in chat after walking through the how-to-verify steps below.</done>
  <what-built>
    - `/shop/[slug]` now renders banner + discount badges from dedicated
      endpoints (plan 10-02).
    - `/shop/[slug]/cart` has Jest coverage (plan 10-02).
    - `/shop/orders` has status + date filters + pagination.
    - `storefront-flows.spec.ts` asserts banner/badge and visits the
      dedicated cart page explicitly on the COD path.
  </what-built>
  <how-to-verify>
    1. Ensure the full stack is up: `docker compose -f docker-compose.full-stack.yml up -d`
       and wait for `jtoye-core-java` to be healthy.
    2. Open http://localhost:3000/shop/jollof-express-brixton-900b57a8
       (or whatever seed slug exists). Confirm:
       a. An announcement banner renders above the menu (if a promotion/
          announcement seed exists — otherwise note that the banner is
          empty and move to step 4).
       b. At least one product card shows a red "% off" or "£X off" badge
          overlaid on the image corner (if an active promo seed exists).
       c. No hydration warnings in the browser console.
    3. Click a product to add to cart, then navigate to
       http://localhost:3000/shop/jollof-express-brixton-900b57a8/cart
       directly. Confirm the cart page renders the item, quantity
       controls work, and the checkout link is visible.
    4. Sign in as a customer (Keycloak seed user) and visit
       http://localhost:3000/shop/orders. Change the status filter and
       the date filter; confirm the list narrows and that the pagination
       label updates correctly. Page through to the next page if the
       account has more than 10 orders.
    5. Report back "approved" or describe any issue (missing seed,
       broken selector, hydration warning, etc.).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Full e2e regression</name>
  <files>frontend/e2e/storefront-flows.spec.ts</files>
  <action>
    Run the desktop project of the storefront e2e suite to confirm no
    regression, plus the full frontend unit suite and the full backend
    suite one last time (sanity against the merged plans 10-01 + 10-02
    changes on the same feature branch).

    Reminder: if any wave-1 work from plans 10-01 / 10-02 is on a
    different branch (parallel-agent scenario), rebase/merge before
    this task so the regression runs against the integrated diff.
  </action>
  <verify>
    <automated>cd /home/sanmi/IdeaProjects/JToye_OaaS_2026 &amp;&amp; JAVA_HOME=/usr/lib/jvm/jdk-21.0.6-oracle-x64 ./gradlew :core-java:test &amp;&amp; cd frontend &amp;&amp; npm test -- --watchAll=false &amp;&amp; npx playwright test e2e/storefront-flows.spec.ts --project=desktop</automated>
  </verify>
  <done>
    Backend 335+ tests green, frontend unit suite green (71+), storefront
    Playwright desktop project green.
  </done>
</task>

<task type="auto">
  <name>Task 6: Update REQUIREMENTS.md traceability + closing commit</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Open .planning/REQUIREMENTS.md and update the Traceability table so
    STFR-01..06 rows all read "| Phase 10 | Done". Update the Coverage
    summary block underneath:
      - Phase 10 done count increases by 6.
      - "Pending: STFR ×6 (Phase 10)" changes to "Pending: STMP ×5 (Phase 11)".
      - Add "Phase 10 is COMPLETE." line mirroring the phase 9 closing line.

    Also tick the STFR-01..06 checkboxes in the "Active" Work Order B
    section at the top (flip `[ ]` → `[x]`).

    Do NOT touch SECR or STMP rows. Do NOT touch the v2 Requirements
    section.

    Create a single atomic commit on the feature branch:
      docs(stfr): mark phase 10 STFR-01..06 done in REQUIREMENTS traceability

    Push the branch if not already pushed. Do NOT open a PR in this task
    — leave PR creation to the developer post-verification per the
    global CLAUDE.md feature-branch policy.
  </action>
  <verify>
    <automated>cd /home/sanmi/IdeaProjects/JToye_OaaS_2026 &amp;&amp; grep -c 'STFR-0[1-6] | Phase 10 | Done' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    REQUIREMENTS.md shows all 6 STFR rows as Done, coverage counters
    updated, Work Order B checkboxes flipped, commit exists on the
    feature branch.
  </done>
</task>

</tasks>

<verification>
- `./gradlew :core-java:test` green (baseline 335 +4 from plan 10-01)
- `npm test -- --watchAll=false` green in frontend/ (baseline 69 +2 from 10-02 cart +4 from 10-03 orders-filter)
- `npx playwright test e2e/storefront-flows.spec.ts --project=desktop` green on the full stack
- REQUIREMENTS.md grep: `STFR-0[1-6] | Phase 10 | Done` returns 6
- `deriveOrdersView` is exported from frontend/app/shop/orders/page.tsx
- storefront-flows.spec.ts contains the new banner/badge test and an explicit goto `/shop/*/cart` step in the existing checkout test
- No Stripe test-mode env vars added, no PaymentElement interaction in the spec
- `/public/orders?email=` URL unchanged in the orders page (enumeration risk deferred)
</verification>

<success_criteria>
STFR-05 ships with filters + pagination + Jest coverage; STFR-06 ships as an
extension of the existing Playwright e2e that also asserts marketing render
and explicit cart-page navigation on the COD path. Phase 10 is marked complete
in REQUIREMENTS.md.
</success_criteria>

<output>
After completion, create
`.planning/phases/10-storefront-marketing-render-missing-customer-routes/10-03-SUMMARY.md`
including a "Follow-ups for milestone 4+" section listing:
  1. `/public/orders?email=` soft enumeration risk (Pitfall 5) — add
     server-side claim validation against the Keycloak access token.
  2. Playwright promo fixture — add a V33 dev-profile seed SQL or a
     vendor-API-based beforeAll setup so the banner/badge assertion is
     deterministic (Open Question 3).
  3. Server-side pagination on `/public/orders` if the average order
     count per customer ever exceeds ~500 (Assumption A1).
</output>
