# Milestone v2.1 Requirements Archive

**Status:** ✅ SHIPPED 2026-04-16 (archived 2026-04-18)
**Coverage:** 18/18 v1 requirements complete
**Audit:** `milestones/v2.1-MILESTONE-AUDIT.md` (passed after remediation)

---

# Requirements: J'Toye OaaS — Milestone 3 (Post-Audit Hardening + Storefront Completion)

**Defined:** 2026-04-14
**Milestone:** v2.1
**Source:** `.planning/state-of-codebase/STATE-OF-CODEBASE-2026-04-14.md §9, §11 Work Orders A+B+C`
**Core Value:** Vendors can manage their business end-to-end — from marketing to kitchen fulfilment — through a single platform with real-time visibility, running safely on verified infrastructure that can scale past one replica.

## v1 Requirements — all complete

### Work Order A — Repository secrets + alerting (SECR)

**RESCOPED 2026-04-15 during phase 9 discuss + execution.** Two material corrections:

1. **The `.env`-committed claim in the audit doc is false.** Verified via `git log --all --full-history -- .env` (empty), `git ls-files --error-unmatch .env` ("did not match any file"), and `git check-ignore -v .env` (matched by `.gitignore:64`). `.env.example` and `k8s/base/secrets-template.yaml` use `CHANGE_ME` / `REPLACE_WITH_*` placeholders only. SECR-01..03 converted from rotation-style to verification + enforcement-style; new SECR-07 added for gitleaks CI.
2. **Alert destination rescoped from Slack to email via Mailhog.** Project has no committed Slack dependency beyond one CI notification workflow. Mailhog already in `docker-compose.full-stack.yml`. Prod can override `ALERTMANAGER_SMTP_*` env vars to point at a real SMTP relay.
3. **Alert rule count is 10, not 13** (verified). Audit doc figure was incorrect.

- [x] **SECR-01**: `.env` verified absent from git tracking. Re-scoped. Enforcement via SECR-07.
- [x] **SECR-02**: Credential rotation **dropped** — no credentials were ever committed.
- [x] **SECR-03**: Secret distribution **dropped** — nothing to distribute.
- [x] **SECR-04**: `prom/alertmanager:v0.27.0` container deployed, Prometheus `alerting.alertmanagers` block bound. Commits `295ea56` + `47ea7b4`.
- [x] **SECR-05**: Alertmanager routes 10 alert rules to `email-default` receiver with `severity` + `service` labels driving email subject template. Commits `295ea56` + `47ea7b4`.
- [x] **SECR-06**: End-to-end alert roundtrip **VERIFIED 2026-04-15**. Live smoke test with `docker stop jtoye-core-java` → Mailhog received `[FIRING:3] ServiceDown (platform/critical)` + synthetic alert.
- [x] **SECR-07** (new 2026-04-15): Gitleaks CI enforcement via `.github/workflows/gitleaks.yml` + `.gitleaks.toml` allowlist + opt-in local pre-commit hook. Commit `165a7a7`.

### Work Order B — Storefront marketing + missing customer routes (STFR)

- [x] **STFR-01**: `GET /public/shops/{slug}/promotions` returns active promotions scoped to tenant; MockMvc coverage. Commit `168582a`.
- [x] **STFR-02**: `GET /public/shops/{slug}/announcements` returns active+unexpired announcements with tenant scoping; MockMvc coverage. Commit `168582a`.
- [x] **STFR-03**: `frontend/app/shop/[slug]/page.tsx` parallel fetch + announcement banner + discount badges with memoised per-category lookup. Commit `cbbd609`.
- [x] **STFR-04**: `frontend/app/shop/[slug]/cart/page.tsx` standalone cart with Jest empty + populated coverage. Commit `bca8545`.
- [x] **STFR-05**: `frontend/app/shop/orders/page.tsx` customer order history with filters + pagination + `RequireCustomerAuth`. Commits `926717c` + `7658e35`.
- [x] **STFR-06**: Playwright e2e walks shop discovery → detail → cart → Stripe test-mode checkout → confirmation. Commit `b11a3f4`.

### Work Order C — STOMP broker relay for horizontal scale (STMP)

- [x] **STMP-01**: `WebSocketConfig.java` reads `stomp.broker.mode` (in-memory | relay) via `@Value`; relay mode calls `enableStompBrokerRelay("/topic","/queue")` with host/port/login wired from env. Phase 11 plan 11-01.
- [x] **STMP-02**: RabbitMQ `rabbitmq_stomp` plugin enabled in `docker-compose.full-stack.yml` + k8s manifests; port 61613 exposed; relay credentials as k8s Secret entries. Phase 11 plan 11-01.
- [x] **STMP-03**: Two-replica broadcast verified — `scripts/smoke-test-stomp-relay.sh` 6/6 pass; 2 STOMP 1.2 connections confirmed from 2 replicas. Phase 11 plan 11-02.
- [x] **STMP-04**: Playwright e2e at `frontend/e2e/stomp-relay.spec.ts` gated behind `RELAY_E2E=true`. Phase 11 plan 11-02.
- [x] **STMP-05**: Prometheus `StompBrokerLag` alert in `messaging_alerts` group + `stomp-dashboard.json` Grafana dashboard; both routed through Phase 9 Alertmanager. Phase 11 plan 11-03.

## Traceability (final)

| Requirement | Phase | Status |
|-------------|-------|--------|
| SECR-01 | Phase 9 | ✅ Verified (rescoped) |
| SECR-02 | Phase 9 | ✅ Dropped (rescoped) |
| SECR-03 | Phase 9 | ✅ Dropped (rescoped) |
| SECR-04 | Phase 9 | ✅ Done (commits 295ea56 + 47ea7b4) |
| SECR-05 | Phase 9 | ✅ Done (commits 295ea56 + 47ea7b4) |
| SECR-06 | Phase 9 | ✅ Done (live test 2026-04-15) |
| SECR-07 | Phase 9 | ✅ Done (commit 165a7a7) |
| STFR-01 | Phase 10 | ✅ Done (commit 168582a) |
| STFR-02 | Phase 10 | ✅ Done (commit 168582a) |
| STFR-03 | Phase 10 | ✅ Done (commit cbbd609) |
| STFR-04 | Phase 10 | ✅ Done (commit bca8545) |
| STFR-05 | Phase 10 | ✅ Done (commits 926717c + 7658e35) |
| STFR-06 | Phase 10 | ✅ Done (commit b11a3f4) |
| STMP-01 | Phase 11 | ✅ Done (plan 11-01) |
| STMP-02 | Phase 11 | ✅ Done (plan 11-01) |
| STMP-03 | Phase 11 | ✅ Done (plan 11-02) |
| STMP-04 | Phase 11 | ✅ Done (plan 11-02) |
| STMP-05 | Phase 11 | ✅ Done (plan 11-03) |

**Coverage:** 18/18 v1 requirements complete (SECR ×7 + STFR ×6 + STMP ×5).

## v2 Requirements (deferred — carried forward)

Work Orders D–O from the state-of-codebase doc plus SECR-08 discovered during v2.1 execution. These roll over to v2.2+.

### Tenant SaaS v1 (v3.0 signal)
- **TNT-01..04**: Tenant onboarding with Keycloak + Stripe + welcome email (Work Order D)

### Vendor operational completion
- **VOPS-01**: `/dashboard/orders/[id]` detail + refund flow (Work Order E)
- **VOPS-02**: `/dashboard/finance` wired to ledger + VAT (Work Order F)
- **VOPS-03**: `/dashboard/settings` wired to shop + notification prefs (Work Order F)
- **VOPS-04**: Bulk product import endpoint + UI (Work Order M)

### Platform hardening
- **PLAT-01**: Log aggregation (Loki/ELK) + Grafana dashboards + runbooks (Work Order G)
- **PLAT-02**: K8s sealed-secrets or external-secrets-operator (Work Order H)
- **PLAT-03**: Postgres PITR via WAL archiving to S3 (Work Order I)
- **PLAT-04**: Edge OpenTelemetry + distributed rate limiter via Redis (Work Order K)
- **PLAT-05**: Full-text product search perf verification + caching (Work Order L)

### Other
- **REV-01**: Review module controller + storefront display + moderation (Work Order J)
- **BILL-01**: Vendor billing subscription mgmt (Work Order N)
- **WHAT-01**: Migrate WhatsApp handler to order idempotency key (Work Order O)

### New items surfaced during v2.1
- **SECR-08**: Keycloak `realm-export.json` hardcoded dev secrets — allowlisted with pointer to `.planning/phases/09-repository-secrets-alerting/deferred-items.md` D-1. Proposed for v2.2+.
- **Storefront enumeration risk**: `/public/orders?email=` — deferred per Pitfall 5 of plan 10-03.

## Out of Scope (v2.1) — history preserved

| Feature | Reason |
|---------|--------|
| Tenant self-serve onboarding (D) | Different milestone size/risk; saves for v3.0 major |
| Vendor order detail + refund (E) | Operational polish, not an audit blocker |
| Finance + settings pages (F) | Placeholder shells today, out-of-scope until billing architecture is decided |
| Log aggregation / Loki (G) | Alertmanager addresses the immediate blind spot; full log pipeline is milestone 4 |
| Sealed-secrets / external-secrets-operator (H) | SECR uses GitHub Secrets + k8s Secrets as interim; full GitOps secret management is milestone 4+ |
| Postgres PITR (I) | Backup CronJob already exists; PITR is a separate effort |
| Review controller + moderation (J) | Service exists, no audit-blocking urgency |
| Edge OpenTelemetry + distributed rate limiter (K) | Edge hardening shipped in PR #30; OTel is a separate observability milestone |
| Full-text search perf (L) | V25 tsvector indexes exist; perf not proven blocking |
| Bulk product import UI (M) | Endpoint unclear, low priority |
| Billing subscription mgmt (N) | Depends on D (tenant onboarding) |
| WhatsApp idempotency key (O) | Handler gated by `WHATSAPP_ENABLED`; low priority until SMS/WhatsApp rollout |
| Station routing / course pacing / KDS recall (M2 v2) | Deferred from milestone 2 v2 list |
| Mobile native app | Web-first strategy unchanged |
| Real-time vendor-customer chat | High complexity, not core |

---

*Requirements defined: 2026-04-14*
*Archived: 2026-04-18 at milestone v2.1 close — all 18 v1 requirements complete*
