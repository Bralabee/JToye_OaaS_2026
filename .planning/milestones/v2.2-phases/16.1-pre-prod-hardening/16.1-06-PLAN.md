---
phase: 16.1-pre-prod-hardening
plan: 06
type: execute
wave: 3
depends_on:
  - 16.1-01
  - 16.1-02
  - 16.1-03
  - 16.1-04
  - 16.1-05
files_modified:
  - .planning/REQUIREMENTS.md
  - CHANGELOG.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "`.planning/REQUIREMENTS.md` lists `AUDIT-W0-01` through `AUDIT-W0-05` under a new `### Pre-prod hardening (AUDIT-W0)` section, each with file:line evidence and a Phase 16.1 completion marker."
    - "`CHANGELOG.md` `[Unreleased]` section has a Phase 16.1 entry documenting the 5 fixes."
    - "`.planning/ROADMAP.md` Phase 16.1 entry shows 6 plans complete and the phase is marked `[x]`."
    - "`.planning/STATE.md` `## Current Position` reflects Phase 16.1 complete and recommends Phase 17 as next."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Registers AUDIT-W0-01..05 in the project requirements ledger"
      contains: "AUDIT-W0-01"
    - path: "CHANGELOG.md"
      provides: "Phase 16.1 user-facing entry under [Unreleased]"
      contains: "Phase 16.1"
  key_links:
    - from: "REQUIREMENTS.md AUDIT-W0 section"
      to: "Phase 16.1 plans (16.1-01..05)"
      via: "Traceability table at bottom of REQUIREMENTS.md"
      pattern: "AUDIT-W0-0[1-5]"
---

<plan_header>
**Wave 3 closing/admin plan.** No production code change. Updates project metadata so the AUDIT-W0 requirement IDs (which originated post-roadmap from the 2026-04-27 council audit) are formally registered, the CHANGELOG reflects the work, and STATE/ROADMAP advance.

**Dependency:** depends on 16.1-01..05 because the SUMMARY.md files and verified test counts produced by those plans feed the CHANGELOG entry below.

**Why this isn't auto-handled:** REQUIREMENTS.md was authored on 2026-04-18 and pre-dates the council audit. The audit IDs are not yet in the ledger. We register them here so:
- Future audits cross-reference the requirement IDs cleanly.
- The traceability table at the bottom of REQUIREMENTS.md correctly maps each ID to a plan.
- `gsd-sdk` requirement-coverage checks don't flag the IDs as unmapped.
</plan_header>

<objective>
Close out Phase 16.1 by:
1. Registering `AUDIT-W0-01..05` in `.planning/REQUIREMENTS.md` as a new `### Pre-prod hardening (AUDIT-W0)` subsection (one bullet per ID with file:line evidence + completion marker).
2. Adding a Phase 16.1 entry to `CHANGELOG.md` under `[Unreleased]` (or whichever heading is in flight).
3. Marking Phase 16.1 complete in `.planning/ROADMAP.md` (status flip + plans count update).
4. Advancing `.planning/STATE.md` `## Current Position` to "Phase 16.1 complete; ready for PR; next: Phase 17".

Output: 4 metadata files updated. No source code, no tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16.1-pre-prod-hardening/16.1-CONTEXT.md
@.planning/phases/16.1-pre-prod-hardening/16.1-01-SUMMARY.md
@.planning/phases/16.1-pre-prod-hardening/16.1-02-SUMMARY.md
@.planning/phases/16.1-pre-prod-hardening/16.1-03-SUMMARY.md
@.planning/phases/16.1-pre-prod-hardening/16.1-04-SUMMARY.md
@.planning/phases/16.1-pre-prod-hardening/16.1-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register AUDIT-W0-01..05 in REQUIREMENTS.md</name>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — find the right insertion point)
    - .planning/phases/16.1-pre-prod-hardening/16.1-CONTEXT.md (canonical bullet text + file:line evidence — copy verbatim from the `<domain>` block items 1-5)
    - .planning/phases/16.1-pre-prod-hardening/16.1-01-SUMMARY.md (V35 completion confirmation)
    - .planning/phases/16.1-pre-prod-hardening/16.1-02-SUMMARY.md (SSE fix confirmation)
    - .planning/phases/16.1-pre-prod-hardening/16.1-03-SUMMARY.md (IDOR fix confirmation)
    - .planning/phases/16.1-pre-prod-hardening/16.1-04-SUMMARY.md (idempotency fix confirmation)
    - .planning/phases/16.1-pre-prod-hardening/16.1-05-SUMMARY.md (RLS contract test confirmation)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Add a new `### Pre-prod hardening (AUDIT-W0) — added 2026-04-27` section in `.planning/REQUIREMENTS.md`, placed AFTER the existing `### API documentation (DOC)` block and BEFORE `### Vendor operations (VOPS) — Work Order E`.

    Each requirement is a `[x]` (completed) bullet, file:line evidence preserved from CONTEXT.md, with a `**Shipped:**` trailer pointing to the plan SUMMARY:

    ```
    ### Pre-prod hardening (AUDIT-W0) — added 2026-04-27

    Five pre-prod blockers identified by the 2026-04-27 council audit (`docs/audit/COUNCIL-AUDIT-2026-04-27.md`). Inserted into Phase 16.1 (between original Phases 16 and 17). Source: council audit + 8 specialist remediation docs (`docs/audit/remediation/`); findings re-verified against live code on 2026-04-27.

    - [x] **AUDIT-W0-01**: `OrderSseService` cross-tenant SSE leak — every dashboard subscriber received every tenant's order state changes. Fix: per-tenant `ConcurrentHashMap<UUID, Set<SseEmitter>>` keyed at `subscribe()` from `TenantContext.get()`; `broadcast()` filters by `event.tenantId()`. Source: `core-java/src/main/java/uk/jtoye/core/order/OrderSseService.java:17,29-40`. **Shipped:** Phase 16.1 Plan 02 — see 16.1-02-SUMMARY.md. Regression: `OrderSseServiceTenantIsolationTest`.

    - [x] **AUDIT-W0-02**: `/public/orders` IDOR — `verify` order-number was opt-in; bare `?email=` returned the customer's full order list. Fix: `verify` parameter is now mandatory at the controller; `trackOrder(verify, email)` is called unconditionally before `getCustomerOrders(email)`; missing/blank → 400. Source: `core-java/src/main/java/uk/jtoye/core/storefront/PublicStorefrontController.java:91-104`. **Shipped:** Phase 16.1 Plan 03 — see 16.1-03-SUMMARY.md. Regression: `PublicStorefrontControllerIdorTest` (4 MockMvc tests). Phase 2 magic-link + rate limiter is deferred per CONTEXT `<deferred>`.

    - [x] **AUDIT-W0-03**: Stripe webhook idempotency — `handleWebhookEvent` had no de-dup; retried events double-wrote `financial_transactions` and double-published the order state change. Fix: V35 adds `processed_stripe_events(event_id TEXT PK, processed_at TIMESTAMPTZ)`; `handleWebhookEvent` runs TOCTOU-safe `INSERT ... ON CONFLICT DO NOTHING` immediately after signature verification — 0 rows affected ⇒ retry, return early. Source: `core-java/src/main/java/uk/jtoye/core/payment/PaymentService.java:113-132` + `core-java/src/main/resources/db/migration/V35__rls_idempotency_force_rls.sql`. **Shipped:** Phase 16.1 Plans 01 + 04 — see 16.1-04-SUMMARY.md. Regression: `StripeWebhookIdempotencyIntegrationTest` (Testcontainers Postgres).

    - [x] **AUDIT-W0-04**: `reviews_tenant_write` RLS policy rewrite — V27's policy read the wrong GUC name (`app.tenant_id` instead of canonical `app.current_tenant_id`) and the OR-clause `current_setting('app.customer_email', true) = customer_email` allowed anyone setting `app.customer_email` to insert a review on any tenant_id and any order_id. Fix: V35 drops the buggy policy and recreates with canonical GUC name + EXISTS-on-orders ownership proof in the customer branch. Source: original at `core-java/src/main/resources/db/migration/V27__customer_reviews.sql:31-36`; replacement in V35. **Shipped:** Phase 16.1 Plans 01 + 05 — see 16.1-05-SUMMARY.md. Regression: `ReviewsRlsPolicyIntegrationTest` (5 behavioural tests on Testcontainers Postgres).

    - [x] **AUDIT-W0-05**: FORCE ROW LEVEL SECURITY on 9 tables — `reviews`, `shop_promotions`, `shop_announcements`, and the 6 `_aud` audit tables had ENABLE but not FORCE; the table-owner DB role used by some Flyway migrations and Envers writes could bypass tenant isolation. Fix: V35 issues `ALTER TABLE ... FORCE ROW LEVEL SECURITY` for all 9. Source: per-table evidence in `docs/audit/sources/03-database-engineer.md`; replacement in V35. **Shipped:** Phase 16.1 Plans 01 + 05 — see 16.1-05-SUMMARY.md. Regression: `RlsContractTest` (walks `pg_class`, asserts ENABLE+FORCE on every non-exempt public table; future migrations missing RLS+FORCE break the build).
    ```

    Then, in the `## Traceability` table at the bottom of REQUIREMENTS.md, append 5 new rows:

    ```
    | AUDIT-W0-01 | Phase 16.1 (Plan 16.1-02) | Complete (2026-04-27) |
    | AUDIT-W0-02 | Phase 16.1 (Plan 16.1-03) | Complete (2026-04-27) |
    | AUDIT-W0-03 | Phase 16.1 (Plans 16.1-01 + 16.1-04) | Complete (2026-04-27) |
    | AUDIT-W0-04 | Phase 16.1 (Plans 16.1-01 + 16.1-05) | Complete (2026-04-27) |
    | AUDIT-W0-05 | Phase 16.1 (Plans 16.1-01 + 16.1-05) | Complete (2026-04-27) |
    ```

    Update the **Coverage** block at the very bottom:
    - Was: `v1 requirements: 11 total`
    - Now: `v1 requirements: 11 + 5 (AUDIT-W0) = 16 total`
    - Update the "Mapped to phases: 11" line to "Mapped to phases: 16".

    Append a dated note under the existing `Last updated:` line: `*Last updated: 2026-04-27 — registered AUDIT-W0-01..05 retrospectively from the 2026-04-27 council audit (Phase 16.1 closure).*`

    Notes for the executor:
    - DO NOT renumber existing requirements. AUDIT-W0 is its own ID space — keeps blame trails clean for the council-audit origin.
    - Use `[x]` checkboxes (matching the existing pattern in REQUIREMENTS.md) — Phase 16.1 ships them all in one wave so all 5 land complete simultaneously.
    - If actual completion date when this plan executes is later than 2026-04-27 (e.g. work spans across midnight), update the Complete date in the Traceability rows to today's date — but keep "added 2026-04-27" on the section heading because that is when the IDs entered the ledger.
  </action>
  <verify>
    <automated>grep -c 'AUDIT-W0-0[1-5]' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'AUDIT-W0-01' .planning/REQUIREMENTS.md` returns at least 2 (one in section bullet, one in traceability table)
    - `grep -c 'AUDIT-W0-02' .planning/REQUIREMENTS.md` returns at least 2
    - `grep -c 'AUDIT-W0-03' .planning/REQUIREMENTS.md` returns at least 2
    - `grep -c 'AUDIT-W0-04' .planning/REQUIREMENTS.md` returns at least 2
    - `grep -c 'AUDIT-W0-05' .planning/REQUIREMENTS.md` returns at least 2
    - `grep -c 'Pre-prod hardening (AUDIT-W0)' .planning/REQUIREMENTS.md` returns at least 1
    - `grep -c 'Phase 16.1' .planning/REQUIREMENTS.md` returns at least 5 (one per traceability row)
    - The new section is positioned BETWEEN `### API documentation (DOC)` and `### Vendor operations (VOPS) — Work Order E` — verify with `awk '/### API documentation/,/### Vendor operations/' .planning/REQUIREMENTS.md | grep -c 'AUDIT-W0'` returns at least 5
  </acceptance_criteria>
  <done>REQUIREMENTS.md has the new AUDIT-W0 section with 5 completed bullets, the traceability table includes 5 new rows, the coverage count is updated, and the Last updated line carries the new dated note.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 16.1 entry to CHANGELOG.md under [Unreleased]</name>
  <read_first>
    - CHANGELOG.md (full file — locate `[Unreleased]` heading or the topmost dated heading)
    - .planning/phases/16.1-pre-prod-hardening/16.1-01-SUMMARY.md through 16.1-05-SUMMARY.md (extract the test counts and any developer-facing operational notes)
  </read_first>
  <files>CHANGELOG.md</files>
  <action>
    Add a `### Phase 16.1 — Pre-prod hardening (Wave 0 council audit fixes)` block under the `## [Unreleased]` heading. If `[Unreleased]` does not exist (the previous milestone may have moved it to a dated heading), create one above the topmost dated heading.

    Entry shape:

    ```markdown
    ## [Unreleased]

    ### Phase 16.1 — Pre-prod hardening (Wave 0 council audit fixes) — 2026-04-27

    **Security & data-integrity bug fixes** identified by the 2026-04-27 council audit. All five blockers must land before any production rollout to a second tenant or real Stripe payments.

    #### Fixed
    - **AUDIT-W0-01** Cross-tenant SSE leak: `OrderSseService` previously broadcast every tenant's order state changes to every connected dashboard. Now uses per-tenant emitter routing keyed by `TenantContext.get()` at subscribe time. (`core-java/src/main/java/uk/jtoye/core/order/OrderSseService.java`)
    - **AUDIT-W0-02** Customer-orders IDOR: `GET /public/orders?email=…` no longer returns order history without the `verify` order-number proof. Bare email-only requests now return 400. (`core-java/src/main/java/uk/jtoye/core/storefront/PublicStorefrontController.java`)
    - **AUDIT-W0-03** Stripe webhook idempotency: duplicate `event.id` deliveries no longer write duplicate `financial_transactions` rows. New TOCTOU-safe `INSERT ... ON CONFLICT DO NOTHING` against `processed_stripe_events`. (`core-java/src/main/java/uk/jtoye/core/payment/PaymentService.java`)
    - **AUDIT-W0-04** `reviews_tenant_write` RLS policy: replaced V27's broken policy (read wrong GUC name; OR-clause allowed arbitrary `tenant_id` writes) with canonical `current_setting('app.current_tenant_id')` + EXISTS-on-orders ownership proof.
    - **AUDIT-W0-05** FORCE ROW LEVEL SECURITY on 9 tables: `reviews`, `shop_promotions`, `shop_announcements`, and the 6 `_aud` audit tables now FORCE RLS — table-owner / superuser writes are subject to policy, closing the audit/marketing cross-tenant read surface.

    #### Database migration
    - **V35 `__rls_idempotency_force_rls.sql`** (transactional, online-safe) — bundles items 3, 4, and 5 into one atomic Flyway migration. Forward-only.

    #### Tests added
    - `OrderSseServiceTenantIsolationTest` (4 unit tests)
    - `PublicStorefrontControllerIdorTest` (4 MockMvc tests)
    - `StripeWebhookIdempotencyIntegrationTest` (3 Testcontainers integration tests)
    - `RlsContractTest` (3 Testcontainers integration tests — schema-walk drift guard)
    - `ReviewsRlsPolicyIntegrationTest` (5 Testcontainers integration tests — policy behavioural test)

    Net test count delta: +19 Java `@Test` methods.

    #### Operational notes
    - V35 is forward-only. Deploys must apply V35 in the same release as the Java changes — otherwise plan 16.1-04 will start TOCTOU-safe inserts against a missing table.
    - Pruning of `processed_stripe_events` is deferred to a future housekeeping phase.
    ```

    Notes for the executor:
    - Read the SUMMARY files from 16.1-01..05 first; if any reports a different test count than the estimate above (19 net), adjust the entry to match reality.
    - Keep the dated heading exactly: `### Phase 16.1 — Pre-prod hardening (Wave 0 council audit fixes) — 2026-04-27`. The date can be advanced if work spills to a later day; in that case match the date used in REQUIREMENTS.md and STATE.md so all metadata agrees.
    - Use the existing CHANGELOG style (header levels, list shapes, bold conventions). Mimic an earlier dated entry if shape is ambiguous.
  </action>
  <verify>
    <automated>grep -c 'AUDIT-W0-0[1-5]' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Phase 16.1' CHANGELOG.md` returns at least 1
    - `grep -c 'AUDIT-W0-01' CHANGELOG.md` returns at least 1
    - `grep -c 'AUDIT-W0-02' CHANGELOG.md` returns at least 1
    - `grep -c 'AUDIT-W0-03' CHANGELOG.md` returns at least 1
    - `grep -c 'AUDIT-W0-04' CHANGELOG.md` returns at least 1
    - `grep -c 'AUDIT-W0-05' CHANGELOG.md` returns at least 1
    - `grep -c 'V35' CHANGELOG.md` returns at least 1
    - `grep -c 'Unreleased' CHANGELOG.md` returns at least 1
  </acceptance_criteria>
  <done>CHANGELOG.md has a Phase 16.1 entry under `[Unreleased]` listing all 5 AUDIT-W0 fixes, V35 migration callout, test counts, and operational notes.</done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md and STATE.md to reflect Phase 16.1 complete</name>
  <read_first>
    - .planning/ROADMAP.md (current Phase 16.1 entry around line 133-150 — has `[ ] TBD` placeholder)
    - .planning/STATE.md (`## Current Position` block, `last_activity` field, progress fractions)
    - .planning/phases/16.1-pre-prod-hardening/16.1-01-SUMMARY.md through 16.1-05-SUMMARY.md
  </read_first>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    **ROADMAP.md edits:**

    1. In the Phase 16.1 entry (currently around lines 133-150), update the `**Plans:**` line and the inline plan list:

       Replace:
       ```
       **Plans:** 0 plans (run `/gsd-plan-phase 16.1` to break down — likely a single V35-migration plan + per-fix Java tasks + RlsContractTest as recommended in `docs/audit/remediation/03-database-remediation.md` F11).

       Plans:
       - [ ] TBD (run /gsd-plan-phase 16.1 to break down)
       ```

       With:
       ```
       **Plans:** 6 plans (all complete)

       Plans:
       - [x] 16.1-01-PLAN.md — V35 Flyway migration: processed_stripe_events table + reviews_tenant_write rewrite + FORCE RLS on 9 tables (AUDIT-W0-03/04/05)
       - [x] 16.1-02-PLAN.md — OrderSseService per-tenant emitter routing + OrderSseServiceTenantIsolationTest (AUDIT-W0-01)
       - [x] 16.1-03-PLAN.md — Mandatory `verify` param on /public/orders + PublicStorefrontControllerIdorTest (AUDIT-W0-02)
       - [x] 16.1-04-PLAN.md — PaymentService.handleWebhookEvent idempotency guard + StripeWebhookIdempotencyIntegrationTest (AUDIT-W0-03)
       - [x] 16.1-05-PLAN.md — RlsContractTest + ReviewsRlsPolicyIntegrationTest (AUDIT-W0-04/05)
       - [x] 16.1-06-PLAN.md — REQUIREMENTS/CHANGELOG/STATE/ROADMAP closure
       ```

    2. In the `## Phases` overview list near the top, update the Phase 16.1 line marker:
       Old: `[ ] **Phase 16.1: Pre-prod Hardening — Wave 0 Council Audit Fixes (INSERTED)**`
       New: `[x] **Phase 16.1: Pre-prod Hardening — Wave 0 Council Audit Fixes** — DONE 2026-04-27 on `feature/phase-16.1-pre-prod-hardening` (V35 migration + 19 new Java tests; ready for PR)`

    3. In the `## Progress` table (bottom of ROADMAP.md), append a new row:
       ```
       | 16.1. Pre-prod Hardening (Wave 0) | v2.2 | 6/6 | Complete (ready for PR) | 2026-04-27 |
       ```

    **STATE.md edits:**

    1. Update `stopped_at` (top of frontmatter) to:
       `"Phase 16.1 (pre-prod hardening) DRAFTING COMPLETE — all 6 plans shipped on branch feature/phase-16.1-pre-prod-hardening. V35 migration + OrderSseService per-tenant routing + /public/orders mandatory verify + Stripe webhook idempotency + RlsContractTest + ReviewsRlsPolicyIntegrationTest. 19 new Java @Test methods. Ready for PR. Phase 17 (vendor order detail + Stripe refund) is the next phase."`

    2. Update `last_updated` to today's date (2026-04-28 if executing on the 28th, 2026-04-27 if on the 27th — reflect actual completion date).

    3. Update `last_activity` to:
       `"2026-04-27 — Phase 16.1 plans 16.1-01..06 shipped on feature/phase-16.1-pre-prod-hardening. 5 council-audit blockers closed: cross-tenant SSE leak, /public/orders IDOR, Stripe webhook idempotency, reviews_tenant_write RLS rewrite, FORCE RLS on 9 tables. V35 migration is the load-bearing schema change. 19 new Java @Test methods (4 SSE isolation + 4 IDOR MockMvc + 3 Stripe idempotency Testcontainers + 3 RLS contract Testcontainers + 5 reviews RLS Testcontainers)."`

    4. Update `progress.completed_plans` from 7 to 13 (added 6 plans this phase). Update `total_plans` from 5 to whatever the running total in v2.2 is — read STATE.md's existing arithmetic and continue it. Update `percent` accordingly. If the math is unclear, leave `total_plans` unchanged and update `completed_plans` += 6 only.

    5. Update `## Current Position` block:

       Replace:
       ```
       Phase: 16.1 — Pre-prod Hardening (Wave 0 council audit fixes) — INSERTED, not planned yet
       Plan: none yet — run /gsd-plan-phase 16.1
       Status: Phase 16.1 inserted 2026-04-27 ...
       ```

       With:
       ```
       Phase: 17 — Vendor Order Detail + Stripe Refund Flow — NEXT, not planned yet
       Plan: none yet — run /gsd-plan-phase 17
       Status: Phase 16.1 (pre-prod hardening) drafting complete; branch feature/phase-16.1-pre-prod-hardening has 6 atomic commits + plan SUMMARYs ready for PR. All 5 council-audit Wave-0 blockers closed.
       ```

    6. In `### Roadmap Evolution`, append a closing-bullet:
       ```
       - Phase 16.1 (Pre-prod Hardening) — DONE 2026-04-27. 5 council-audit blockers closed (cross-tenant SSE leak, /public/orders IDOR, Stripe webhook idempotency, reviews_tenant_write RLS rewrite, FORCE RLS on 9 tables). V35 migration ships them atomically. RlsContractTest is a permanent CI guard against future RLS drift.
       ```

    Notes for the executor:
    - DO NOT touch the existing `### Pending Todos` list except to optionally add: "Phase 16.1 PR open + merge before Phase 17 starts (Phase 17 depends on 16.1 because the V35 idempotency table is part of the Stripe surface)." If unsure, skip — pending todos can be appended in a follow-up admin commit.
    - DO NOT change the Performance Metrics table arithmetic by guessing; if the existing per-phase rows have known durations and yours doesn't, append a row with `~Xh` based on actual measured runtime from the plan SUMMARYs.
    - Date alignment: use whatever today's date is when executing this plan (it may be later than the 2026-04-27 reference date used elsewhere in the planning artefacts). Match across REQUIREMENTS.md, CHANGELOG.md, and STATE.md so the trail is consistent.
  </action>
  <verify>
    <automated>grep -c '16.1' .planning/ROADMAP.md &amp;&amp; grep -c 'Phase 17' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '16.1-01-PLAN.md' .planning/ROADMAP.md` returns at least 1
    - `grep -c '16.1-06-PLAN.md' .planning/ROADMAP.md` returns at least 1
    - `grep -c '6/6' .planning/ROADMAP.md` returns at least 1 (the new progress-table row)
    - `grep -E '^\| 16.1\.' .planning/ROADMAP.md` finds at least one row in the Progress table
    - `grep -c '\[x\] \*\*Phase 16.1' .planning/ROADMAP.md` returns at least 1 (Phase 16.1 marked complete in the overview)
    - `grep -c 'Phase 17' .planning/STATE.md` returns at least 1 (Current Position now points at 17)
    - The previous `Phase: 16.1 — Pre-prod Hardening (Wave 0 council audit fixes) — INSERTED, not planned yet` string is GONE: `grep -c 'INSERTED, not planned yet' .planning/STATE.md` returns 0
    - YAML frontmatter still parses: `head -15 .planning/STATE.md | grep -E '^last_updated:'` finds the updated value
  </acceptance_criteria>
  <done>ROADMAP shows Phase 16.1 [x] complete with 6/6 plans listed; STATE.md Current Position points at Phase 17 with the previous "INSERTED" string removed; YAML frontmatter remains parseable.</done>
</task>

</tasks>

<verification>
1. `grep -c 'AUDIT-W0' .planning/REQUIREMENTS.md` returns at least 10 (5 in section, 5 in traceability).
2. `grep -c 'Phase 16.1' CHANGELOG.md` returns at least 1.
3. `grep -c 'INSERTED, not planned yet' .planning/STATE.md` returns 0 (legacy placeholder removed).
4. `head -10 .planning/STATE.md` shows valid YAML frontmatter.
5. `grep -E '^\| 16.1\.' .planning/ROADMAP.md` finds the new progress-table row.
</verification>

<success_criteria>
- AUDIT-W0-01..05 registered in REQUIREMENTS.md with file:line evidence and `[x]` markers.
- CHANGELOG.md `[Unreleased]` has a Phase 16.1 entry mentioning all 5 fixes + V35 + test counts.
- ROADMAP.md Phase 16.1 entry is marked complete with 6/6 plans.
- STATE.md Current Position advances to Phase 17.
- All four metadata files parse cleanly (no broken YAML, no broken markdown tables).
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-pre-prod-hardening/16.1-06-SUMMARY.md` with: per-file diff size, the 5 traceability-table rows added, and a one-line note that Phase 17 is now the next phase to plan.
</output>
