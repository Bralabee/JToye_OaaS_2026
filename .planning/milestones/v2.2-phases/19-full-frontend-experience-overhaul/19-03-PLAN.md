---
phase: 19-full-frontend-experience-overhaul
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/ui/sheet.tsx
  - frontend/components/public/public-shell.tsx
  - frontend/components/public/public-header.tsx
  - frontend/components/public/public-footer.tsx
  - frontend/app/page.tsx
  - frontend/app/__tests__/landing.test.tsx
  - frontend/components/storefront/storefront-nav.tsx
  - frontend/app/shop/layout.tsx
  - frontend/__tests__/link-graph.test.ts
  - docs/SITEMAP.md
autonomous: true
requirements: [UIX-01]

must_haves:
  truths:
    - "/ renders a real server-rendered public landing page (no blind redirect) with a split-persona hero routing customers to /shop and prospective vendors to /for-operators"
    - "A shared public header + footer connect /, /for-operators, /business-model-guide, /track, /shop — every previously-orphan route now has >=1 inbound nav link"
    - "A static link-graph test fails if any app route becomes an orphan"
    - "The public shell is server-rendered and preserves the force-dynamic/CSP-nonce contract (no auth-gated data in the public shell)"
  artifacts:
    - path: "frontend/app/page.tsx"
      provides: "landing page replacing redirect('/dashboard')"
      min_lines: 40
    - path: "frontend/components/public/public-shell.tsx"
      provides: "shared header+footer wrapper"
    - path: "frontend/__tests__/link-graph.test.ts"
      provides: "orphan-route guard (static)"
      contains: "href"
    - path: "frontend/components/ui/sheet.tsx"
      provides: "shadcn sheet primitive (vendored, no npm install)"
  key_links:
    - from: "frontend/app/page.tsx"
      to: "/shop and /for-operators"
      via: "two door links in the split-persona hero"
      pattern: "href=\"/(shop|for-operators)\""
    - from: "frontend/components/public/public-footer.tsx"
      to: "/track and /business-model-guide"
      via: "footer links de-orphaning both routes"
      pattern: "href=\"/(track|business-model-guide)\""
---

<objective>
Close UIX-01 (backlog #4, #5): the app has no front door — `/` blind-redirects to a login wall, and `/track` + `/business-model-guide` have ZERO inbound links (`/for-operators` has one, from another orphan). Build the real landing page + shared public shell that connect all three surfaces, and lock the fix with a link-graph test so IA cannot silently regress again.

This plan also vendors the shadcn `sheet` primitive (no npm install — built on `@radix-ui/react-dialog@^1.1.15` already present) that plans 19-04 (dashboard More drawer) and 19-05 (public mobile nav / content pages) consume.

Purpose: a customer, prospective vendor, or operator lands on a coherent product with a path to their surface.
Output: `sheet` primitive, `components/public/*` shell, a persona-routed `/` landing page, cross-links, and a static orphan-route guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-full-frontend-experience-overhaul/19-UI-SPEC.md
@.planning/phases/19-full-frontend-experience-overhaul/19-PATTERNS.md
@.planning/phases/19-full-frontend-experience-overhaul/19-RESEARCH.md

<interfaces>
<!-- The shell skeleton + wordmark idiom to copy verbatim. -->
From frontend/app/shop/layout.tsx:15-50 (sticky header + flex-1 main + footer skeleton to mirror; footer :39-50 with Allergen note :46).
Wordmark idiom (app/shop/layout.tsx:25-28 — orange J badge):
  <span className="inline-flex h-8 w-8 items-center justify-center rounded-lg bg-orange-500 text-sm font-bold text-white">J</span>
  <span>J&apos;Toye</span>
Active-link idiom (components/dashboard/sidebar.tsx:37,84,89-94): const pathname = usePathname(); cn(base, isActive ? active : inactive).
Vendoring analog (components/ui/dialog.tsx:1-52): sheet uses the SAME @radix-ui/react-dialog base + forwardRef + displayName + portal/overlay + sr-only close.
Current bug (app/page.tsx): `import { redirect } from "next/navigation"; export default function Home() { redirect("/dashboard") }` — REPLACE.
middleware.ts has NO authorized callback → / is already public; the /dashboard gate lives in app/dashboard/layout.tsx. Do NOT add middleware gating.
Root app/layout.tsx exports `dynamic = "force-dynamic"` which cascades to children (CSP nonce) — keep app/page.tsx a Server Component (no "use client").
SafeImage: components/ui/safe-image.tsx (plain <img> + onError fallback; Playwright asserts naturalWidth>0).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor sheet primitive + shared public shell (header + footer)</name>
  <files>frontend/components/ui/sheet.tsx, frontend/components/public/public-shell.tsx, frontend/components/public/public-header.tsx, frontend/components/public/public-footer.tsx</files>
  <read_first>
    - frontend/components/ui/dialog.tsx (vendoring convention: forwardRef, displayName, portal/overlay, cn(), sr-only close)
    - frontend/app/shop/layout.tsx:15-50 (header + footer skeleton + wordmark)
    - frontend/components/dashboard/sidebar.tsx:37,84-94 (usePathname active-link idiom)
    - frontend/components/storefront/storefront-nav.tsx:91-97 (slate-900 sign-in pill idiom)
    - 19-UI-SPEC.md § Surface B (header nav, footer columns, active state, icon-only accessibility contract) + § Design System (idioms)
  </read_first>
  <action>
    Add the shadcn `sheet` primitive: run `cd frontend && npx shadcn add sheet` (vendors source into `components/ui/sheet.tsx`; MUST NOT change package.json/package-lock — it uses the already-present `@radix-ui/react-dialog`). If the CLI is unavailable offline, hand-vendor `sheet.tsx` mirroring `dialog.tsx` exactly with a `side` variant (`top|bottom|left|right`), keeping the `sr-only` close label. Create `components/public/public-header.tsx` (`"use client"`): sticky `bg-white border-b border-slate-200 shadow-sm h-14 max-w-7xl`; left = orange `J` wordmark linking `/`; right (≥sm) = nav links Shops (`/shop`), For operators (`/for-operators`), Track order (`/track`) + a slate-900 Sign in pill (`/auth/signin`); active link via `usePathname` prefix match (`text-slate-900 font-semibold` vs `text-slate-600 hover:text-slate-900`); right (<sm) = icon-only hamburger (`Menu` lucide) opening the `sheet` with the same four links stacked at ≥44px targets. Accessibility contract: hamburger is a real `<button aria-label="Open menu">` with `focus-visible:ring-2 focus-visible:ring-orange-300`; sheet close carries `aria-label="Close menu"`. Create `components/public/public-footer.tsx`: `border-t border-slate-200 bg-white max-w-7xl py-12` columns (Brand / For customers: Browse shops `/shop` + Track order `/track` / For operators: Become a vendor `/for-operators` + Business model guide `/business-model-guide` + Vendor sign in `/auth/signin`) + bottom row `© {year} J'Toye OaaS` and preserve "Allergen info available on all products". Create `components/public/public-shell.tsx` — a PLAIN server component wrapper: `<PublicHeader/>{children}<PublicFooter/>` (no `"use client"`; touches no route-segment config so force-dynamic/CSP nonce are inherited). Do NOT create a `(public)/` route group.
  </action>
  <acceptance_criteria>
    - `git diff --stat frontend/package.json frontend/package-lock.json` shows NO change after adding sheet (grep: `git status --porcelain frontend/package.json` empty).
    - `components/ui/sheet.tsx` exists and imports from `@radix-ui/react-dialog`.
    - `public-footer.tsx` contains `href="/track"` and `href="/business-model-guide"` and `href="/for-operators"`.
    - `public-header.tsx` hamburger button carries `aria-label="Open menu"` and a `focus-visible:ring` class.
    - `public-shell.tsx` is NOT a client component (no `"use client"` directive).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && test -f components/ui/sheet.tsx && grep -q 'href="/track"' components/public/public-footer.tsx && grep -q 'href="/business-model-guide"' components/public/public-footer.tsx && ! grep -q '"use client"' components/public/public-shell.tsx && [ -z "$(git status --porcelain package.json package-lock.json)" ] && echo OK</automated>
  </verify>
  <done>The sheet primitive is vendored with no npm churn; the shared public shell renders a header (with mobile sheet nav) + a footer that de-orphans /track and /business-model-guide.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persona-routed landing page at / + landing render test</name>
  <files>frontend/app/page.tsx, frontend/app/__tests__/landing.test.tsx, docs/SITEMAP.md</files>
  <read_first>
    - frontend/app/page.tsx (the redirect("/dashboard") to replace)
    - frontend/app/for-operators/page.tsx (the "server page returns a component" idiom)
    - frontend/components/ui/safe-image.tsx (gradient fallback for door/hero imagery)
    - frontend/__tests__/shop/cart.test.tsx (Jest render/screen conventions, @/ alias)
    - 19-UI-SPEC.md § Surface A (split-persona hero, doors table, gradient art direction, responsive H1 sizes) + § Typography (Public Hero Display text-5xl md:text-6xl, text-4xl at 390px floor) + § Copywriting Contract (Landing H1, door CTAs)
  </read_first>
  <behavior>
    - Test: `/` renders a heading naming both audiences (no redirect), a customer door linking `/shop`, an operator door linking `/for-operators`.
    - Test: the public header + footer render on `/`.
    - Test: no "use client" — page is a Server Component (asserted structurally: no client-only hooks at module top).
  </behavior>
  <action>
    Replace `app/page.tsx`'s `redirect("/dashboard")` with a real server-rendered landing page wrapped in `<PublicShell>`. Sections per UI-SPEC Surface A: (1) split-persona hero — H1 "Order from local kitchens. Or run yours." (`text-4xl sm:text-5xl md:text-6xl font-bold leading-[1.05] tracking-tight`), subhead (`text-lg text-slate-600 max-w-xl`), two equal doors: Customer "Order food near you" (`UtensilsCrossed`, `bg-orange-500 text-white`, hover `-translate-y-0.5 shadow-lg`) → `/shop`; Operator "Run your food business" (`Store`, `bg-slate-900 text-white`, orange CTA) → `/for-operators`; gradient-forward backdrop `bg-gradient-to-br from-orange-400 via-orange-500 to-rose-500` as a soft masked wash on off-white (no stock-photo dependency; any imagery via SafeImage with the gradient fallback). (2) "How it works" 3-step band (`py-20`). (3) Trust strip (real product truths, emerald checks). Even signed-in vendors land here — the header offers a path to the dashboard, no auto-redirect. Keep the page a Server Component (no `"use client"`) so the CSP nonce reaches it (the #89 failure mode). No serif, no `text-7xl`, no `font-black`. Create `app/__tests__/landing.test.tsx` (Jest + @testing-library) asserting the heading, both door links, and header/footer render. Update the `/` row in `docs/SITEMAP.md` from "Root redirect: signed-in vendor → /dashboard..." to "Public landing page (persona routing: order food / run your business)".
  </action>
  <acceptance_criteria>
    - `grep -c 'redirect("/dashboard")' app/page.tsx` == 0; `grep -c '"use client"' app/page.tsx` == 0.
    - `app/page.tsx` contains `href="/shop"` and `href="/for-operators"` and imports `PublicShell`.
    - `app/page.tsx` H1 uses `text-4xl` (390px floor) stepping to `text-5xl`/`text-6xl`; no `text-7xl`/`font-black`/serif (grep: `grep -cE "text-7xl|font-black|font-serif" app/page.tsx` == 0).
    - `landing.test.tsx` asserts both door links + header/footer render and passes.
    - `docs/SITEMAP.md` `/` row no longer says "redirect".
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx jest app/__tests__/landing.test.tsx && grep -cE "text-7xl|font-black|font-serif" app/page.tsx | grep -qx 0 && echo OK</automated>
  </verify>
  <done>/ is a persona-routed landing page (no redirect), Server-rendered (CSP-nonce-safe), with both doors linking /shop and /for-operators; render test green; SITEMAP updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: IA cross-links (storefront-nav + shop footer) + static link-graph orphan guard</name>
  <files>frontend/components/storefront/storefront-nav.tsx, frontend/app/shop/layout.tsx, frontend/__tests__/link-graph.test.ts</files>
  <read_first>
    - frontend/components/storefront/storefront-nav.tsx:58-97 (Browse /shop, My Orders /shop/orders, Sign in — add "Track order")
    - frontend/app/shop/layout.tsx:39-50 (footer — add "For operators")
    - frontend/__tests__/shop/cart.test.tsx + frontend/jest.config.js (test conventions, testMatch, @/ alias, e2e excluded)
    - 19-RESEARCH.md § Pattern 8 (static Node/Jest link-graph test — walk app/**/page.tsx, grep href, assert every route >=1 inbound href from a different file; NOT a Playwright crawl)
    - 19-UI-SPEC.md § Surface B (storefront cross-link) + § "Link-graph invariant (success criterion, testable)"
  </read_first>
  <behavior>
    - Test: every route derived from app/**/page.tsx has >=1 inbound `href` occurrence in a DIFFERENT .tsx file.
    - Test: dynamic routes ([slug], [id], [orderNumber]) are normalised to patterns before matching.
    - Test: the guard FAILS if a route has zero inbound links (add a self-check comment demonstrating the assertion).
  </behavior>
  <action>
    Add a "Track order" link (`/track`) to `components/storefront/storefront-nav.tsx` alongside Browse/My Orders (client nav, `usePathname` active idiom). Add a "For operators" (`/for-operators`) link to the storefront footer in `app/shop/layout.tsx` (so a browsing customer can discover the vendor pitch). Create `frontend/__tests__/link-graph.test.ts` (static, no browser/stack): (a) enumerate routes by walking `app/**/page.tsx`, normalising to URL paths — strip `(groups)`, map `[slug]`/`[id]`/`[orderNumber]` to a pattern; (b) collect every `href="..."` occurrence across all `.tsx`/`.ts` under `app` + `components`; (c) assert every route has >=1 inbound href from a different file. Exclude intrinsic entry routes (`/`, and API/callback utility routes that are not user-navigable) with a documented allowlist. This adds a `jest_blocks` count (reconciled by plan 19-09).
  </action>
  <acceptance_criteria>
    - `grep -c 'href="/track"' components/storefront/storefront-nav.tsx` >= 1.
    - `grep -c 'href="/for-operators"' app/shop/layout.tsx` >= 1.
    - App-wide `grep -rn 'href="/track"' app components | wc -l` >= 3 (public footer + public header + storefront-nav).
    - `link-graph.test.ts` walks `app/**/page.tsx` and asserts zero orphans; it passes against the current tree.
    - The test's allowlist is explicitly commented (which non-navigable routes are exempt and why).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx jest __tests__/link-graph.test.ts && [ "$(grep -rn 'href="/track"' app components | wc -l)" -ge 3 ] && echo OK</automated>
  </verify>
  <done>Track order + For operators cross-links exist; a static link-graph test enforces zero orphan routes and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous visitor → / (landing) + public shell | NEW unauthenticated surface — must expose no auth-gated data |
| public nav graph → internal routes | link-graph must not surface internal-only/dashboard routes into public navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-01 | Info Disclosure | landing/public shell renders auth-gated data | mitigate | Public shell is a static server wrapper; landing renders only marketing content (no apiClient/session fetch); /dashboard gate stays in dashboard/layout.tsx |
| T-19-03-02 | Tampering (XSS) | CSP regression on new landing/sheet | mitigate | app/page.tsx stays a Server Component; root force-dynamic inherited; no inline scripts; csp-no-violations Playwright gate runs at closure (19-09) |
| T-19-03-03 | Elevation | middleware gains route gating "to protect" landing | accept/avoid | Explicitly NO middleware change — middleware stays session-resolve + CSP-nonce only |
| T-19-03-04 | Info Disclosure | link-graph leaks internal-only routes into public nav | mitigate | Public header/footer link only the 5 public routes (Shops/For operators/Track/Business-model-guide/Sign in); link-graph allowlist documents non-navigable exemptions |
| T-19-03-SC | Tampering | supply chain (shadcn sheet) | mitigate | `sheet` is vendored source from the official shadcn registry (no third-party registry, no npm install); acceptance asserts zero package.json/lockfile change |
</threat_model>

<verification>
Scoped: `npx jest app/__tests__/landing.test.tsx __tests__/link-graph.test.ts` + `npm run build` (tsc — jest does NOT typecheck). Confirm no package.json/lockfile churn. Playwright landing/csp specs run at closure (19-09).
</verification>

<success_criteria>
- / is a persona-routed landing page (no blind redirect), Server-rendered, CSP-nonce-safe.
- Shared public header + footer connect all 5 public routes; `href="/track"` app-wide count >= 3 (was 0).
- Static link-graph test enforces zero orphans and passes; sheet primitive vendored with no npm install.
</success_criteria>

<output>
Create `.planning/phases/19-full-frontend-experience-overhaul/19-03-SUMMARY.md` when done. Commit (do NOT push).
</output>
