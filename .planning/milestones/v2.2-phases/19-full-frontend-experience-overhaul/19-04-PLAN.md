---
phase: 19-full-frontend-experience-overhaul
plan: 04
type: execute
wave: 2
depends_on: [19-03]
files_modified:
  - frontend/components/dashboard/sidebar.tsx
  - frontend/components/dashboard/mobile-tab-bar.tsx
  - frontend/components/dashboard/dashboard-shell.tsx
  - frontend/components/dashboard/__tests__/dashboard-shell.test.tsx
  - frontend/e2e/dashboard-mobile.spec.ts
autonomous: true
requirements: [UIX-02]

must_haves:
  truths:
    - "All 11 dashboard routes are usable at 390px — the 256px sidebar is hidden on mobile and replaced by a bottom tab bar (4 tabs + More drawer)"
    - "Page titles no longer wrap one-word-per-line on mobile"
    - "Desktop (>=md) is visually unchanged — the existing 256px sidebar stays"
    - "/dashboard/onboarding (the Phase 18 quality reference) is not regressed"
  artifacts:
    - path: "frontend/components/dashboard/mobile-tab-bar.tsx"
      provides: "4-tab bottom bar + More sheet driven off the shared navigation array"
      contains: "md:hidden"
    - path: "frontend/e2e/dashboard-mobile.spec.ts"
      provides: "Playwright mobile-viewport (390x844) proof across 11 routes"
      contains: "project"
  key_links:
    - from: "frontend/components/dashboard/mobile-tab-bar.tsx"
      to: "frontend/components/dashboard/sidebar.tsx navigation"
      via: "imports the shared exported navigation array (no fork)"
      pattern: "import.*navigation.*sidebar"
    - from: "frontend/components/dashboard/dashboard-shell.tsx"
      to: "MobileTabBar + MobileTopBar"
      via: "renders md:hidden mobile chrome + pb-20 scroll clearance"
      pattern: "md:hidden"
---

<objective>
Close UIX-02 (backlog #1, Blocker): the dashboard is unusable on any phone — `components/dashboard/sidebar.tsx` is a fixed `w-64` (256px) column with zero responsive classes, consuming ~66% of a 390px viewport and wrapping page titles one word per line across all 11 dashboard routes.

Per the LOCKED decision: desktop keeps the 256px sidebar; mobile gets a bottom tab bar (4 primary tabs: Dashboard, Orders, Products, Kitchen) + a "More" drawer (shadcn `sheet`) holding the rest (Square/Toast operator pattern). Breakpoint = `md` (768px). Drive BOTH bars off the single existing `navigation` array — do not fork it.

Purpose: a vendor checking orders or the kitchen from a phone gets a usable, thumb-zone nav.
Output: exported navigation, a mobile tab bar + top bar, an evolved dashboard shell, and an automated Playwright mobile spec across all 11 routes. (The human visual sign-off is covered by the whole-app UAT in plan 19-09.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-full-frontend-experience-overhaul/19-UI-SPEC.md
@.planning/phases/19-full-frontend-experience-overhaul/19-PATTERNS.md
@.planning/phases/19-full-frontend-experience-overhaul/19-RESEARCH.md

<interfaces>
<!-- The single navigation array to export + drive both bars off. Do NOT fork. -->
From frontend/components/dashboard/sidebar.tsx:24-34 (currently `const navigation = [...]` — change to `export const navigation`):
  Dashboard(/dashboard, LayoutDashboard), Shops(/dashboard/shops, Store), Products(/dashboard/products, Package),
  Orders(/dashboard/orders, ShoppingCart), Customers(/dashboard/customers, Users), Finance(/dashboard/finance, Banknote),
  Marketing(/dashboard/marketing, Megaphone), Kitchen(/dashboard/kitchen, UtensilsCrossed), Go live(/dashboard/onboarding, Rocket)
Sidebar shell: :56 `<div className="flex h-full w-64 flex-col bg-slate-900 text-white">`; active idiom :37,84,89-94; theme-toggle+sign-out footer :104-121; avatar gradient :70.
Dashboard shell (dashboard-shell.tsx:14-22): `flex h-screen overflow-hidden` + <Sidebar/> + <main flex-1 overflow-y-auto> + container p-8.
sheet primitive: frontend/components/ui/sheet.tsx (created in plan 19-03 — import Sheet/SheetTrigger/SheetContent/SheetClose).
Playwright: playwright.config.ts has `mobile` (390x844) + `desktop` (1440x900) projects, baseURL :3000; e2e/kitchen-flow.spec.ts:15-19 is the stub/session idiom to copy (use domcontentloaded, NOT networkidle).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export navigation + make sidebar desktop-only</name>
  <files>frontend/components/dashboard/sidebar.tsx</files>
  <read_first>
    - frontend/components/dashboard/sidebar.tsx (whole file — navigation :24-34, shell :56, active :37/84-94, footer controls :104-121, avatar :70)
    - 19-UI-SPEC.md § Surface D ("Single source of truth: keep the navigation array in sidebar.tsx and drive BOTH bars from it"; ">=md unchanged")
  </read_first>
  <action>
    Change `const navigation` at sidebar.tsx:24 to `export const navigation` so `mobile-tab-bar.tsx` can consume the SAME list (no fork). Make the sidebar desktop-only: the root sidebar container becomes `hidden md:flex` (add to the `w-64` wrapper at :56) so it disappears below `md`. Do NOT change the desktop appearance, the active-state idiom, the dark-slate chrome, or the theme-toggle/sign-out footer for desktop. Preserve the `Go live` (`Rocket`) item. Do NOT touch the avatar-gradient purple here (that is the plan 19-08 palette sweep's job — leave `sidebar.tsx:70` as-is for now to avoid a same-file double edit; the later sweep wave owns it).
  </action>
  <acceptance_criteria>
    - `grep -c "export const navigation" sidebar.tsx` == 1.
    - The sidebar root wrapper carries `hidden md:flex` (grep: `grep -c "hidden md:flex" sidebar.tsx` >= 1).
    - The 9 navigation entries are unchanged (Dashboard..Go live).
    - `npm run build` (tsc) succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c "export const navigation" components/dashboard/sidebar.tsx && grep -c "hidden md:flex" components/dashboard/sidebar.tsx && npm run build</automated>
  </verify>
  <done>navigation is exported once; the sidebar is `hidden md:flex` (desktop-only) with no desktop visual change.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile tab bar (4 tabs + More sheet) + evolved dashboard shell</name>
  <files>frontend/components/dashboard/mobile-tab-bar.tsx, frontend/components/dashboard/dashboard-shell.tsx, frontend/components/dashboard/__tests__/dashboard-shell.test.tsx</files>
  <read_first>
    - frontend/components/dashboard/sidebar.tsx (the exported navigation + footer controls to relocate into the drawer)
    - frontend/components/ui/sheet.tsx (created in 19-03 — the More drawer primitive)
    - frontend/components/dashboard/dashboard-shell.tsx:14-22 (shell to evolve)
    - frontend/components/dashboard/__tests__/dashboard-shell.test.tsx (existing test to extend with mobile-bar assertions)
    - 19-UI-SPEC.md § Surface D (tab list, active = text-blue-600 not the dark pill, h-14 + pb-[env(safe-area-inset-bottom)], top bar, pb-20 scroll clearance) + § Spacing (56px bar exception, 44px touch targets)
  </read_first>
  <action>
    Create `components/dashboard/mobile-tab-bar.tsx` (`"use client"`): import the shared `navigation` from `sidebar.tsx`; render a fixed bottom bar `fixed inset-x-0 bottom-0 z-50 h-14 bg-white border-t border-slate-200 flex md:hidden` + `pb-[env(safe-area-inset-bottom)]` with 4 primary tabs — Dashboard(`/dashboard`), Orders(`/dashboard/orders`), Products(`/dashboard/products`), Kitchen(`/dashboard/kitchen`) — plus a 5th "More" tab (`Menu` icon) opening a shadcn `sheet` listing the remaining routes (Shops, Customers, Finance, Marketing, Go live) plus the user block + theme toggle + sign-out relocated from the sidebar footer. Each tab: vertical 20px icon + `text-xs` label, `min-h-11` (>=44px), active = `text-blue-600` (icon+label) via `usePathname`, inactive = `text-slate-500` (NO full blue pill — that stays the dark sidebar's). Every icon-only control gets an `aria-label` + `focus-visible:ring`. Evolve `dashboard-shell.tsx` to render a slim mobile top bar (`h-14 bg-white border-b md:hidden` with the wordmark) inside `<main>` and `<MobileTabBar className="md:hidden" />` after `<main>`; container padding `p-4 sm:p-8 pb-20 md:pb-8` (pb-20 clears the fixed bar on mobile). Extend `dashboard-shell.test.tsx` with assertions that the mobile bars render (and are `md:hidden`) and the sidebar is present.
  </action>
  <acceptance_criteria>
    - `mobile-tab-bar.tsx` imports `navigation` from `../sidebar` (or `@/components/dashboard/sidebar`) — no re-declared array (grep: `grep -c "const navigation = \[" mobile-tab-bar.tsx` == 0).
    - Bottom bar wrapper carries `fixed inset-x-0 bottom-0`, `h-14`, `md:hidden`, and `pb-[env(safe-area-inset-bottom)]`.
    - Exactly 4 primary tab links (`/dashboard`, `/dashboard/orders`, `/dashboard/products`, `/dashboard/kitchen`) + a More trigger; active uses `text-blue-600` (grep: `grep -c "text-blue-600" mobile-tab-bar.tsx` >= 1) and NOT `bg-blue-600` pill.
    - `dashboard-shell.tsx` renders `pb-20` on mobile and `<MobileTabBar` with `md:hidden`.
    - `dashboard-shell.test.tsx` asserts mobile bars render; `npx jest components/dashboard/__tests__/dashboard-shell.test.tsx` passes; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx jest components/dashboard/__tests__/dashboard-shell.test.tsx && npm run build && grep -c "pb-\[env(safe-area-inset-bottom)\]" components/dashboard/mobile-tab-bar.tsx</automated>
  </verify>
  <done>A mobile bottom bar (4 tabs + More sheet, driven off the shared navigation) + top bar render below md; desktop unchanged; shell test green.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright mobile spec — 11 dashboard routes at 390px</name>
  <files>frontend/e2e/dashboard-mobile.spec.ts</files>
  <read_first>
    - frontend/e2e/kitchen-flow.spec.ts:15-19 (route() stubbing + fake session + PLAYWRIGHT_BASE_URL; use domcontentloaded NOT networkidle — SSE/STOMP never idles)
    - frontend/playwright.config.ts (mobile 390x844 + desktop projects, baseURL)
    - 19-UI-SPEC.md § Surface D Verification (all 11 routes usable at 390px; titles not one-word-wrapped; sidebar hidden, bottom bar shown; onboarding visually unchanged)
    - 19-RESEARCH.md § Pitfall 5 (domcontentloaded) + § Pitfall 8 (confirm the port the stack binds)
  </read_first>
  <action>
    Create `e2e/dashboard-mobile.spec.ts` (Playwright, `--project=mobile`) that, with a stubbed vendor session, visits all 11 dashboard routes (`/dashboard`, `/dashboard/shops`, `/dashboard/products`, `/dashboard/products/import`, `/dashboard/orders`, `/dashboard/orders/[id]`, `/dashboard/customers`, `/dashboard/finance`, `/dashboard/marketing`, `/dashboard/kitchen`, `/dashboard/onboarding`) at 390px and asserts per route: the 256px sidebar is NOT visible (`.hidden md:flex` hides it), the bottom tab bar IS visible, and the page `<h1>` fits without one-word-per-line wrapping (assert the title element bounding box width / no forced overflow — e.g. the `<h1>` scrollWidth <= clientWidth). Use `domcontentloaded` (or explicit element waits) for kitchen/order-detail (SSE/STOMP). Include an assertion that `/dashboard/onboarding` still renders its known headings (not regressed). Run it against the running stack; the human visual confirmation is deferred to the whole-app UAT in plan 19-09.
  </action>
  <acceptance_criteria>
    - `e2e/dashboard-mobile.spec.ts` covers all 11 routes and asserts sidebar-hidden + bottom-bar-visible + no one-word-wrap (h1 scrollWidth <= clientWidth) per route.
    - The spec uses `domcontentloaded` on kitchen/order-detail (grep: `grep -c "networkidle" e2e/dashboard-mobile.spec.ts` == 0).
    - Includes a `/dashboard/onboarding` not-regressed assertion.
    - `npx playwright test --project=mobile dashboard-mobile.spec` passes against the running stack (final confirmation at 19-09 against the rebuilt stack).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c "networkidle" e2e/dashboard-mobile.spec.ts | grep -qx 0 && npx playwright test --project=mobile dashboard-mobile.spec</automated>
  </verify>
  <done>An automated Playwright mobile spec asserts all 11 dashboard routes are usable at 390px (sidebar hidden, bottom bar shown, titles unwrapped, onboarding intact).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard mobile nav → routes | mobile nav must expose only the same authenticated dashboard routes the sidebar does (no new/unauthenticated route) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04-01 | Elevation | mobile nav surfaces a route the sidebar gates out | mitigate | Both bars are driven off the SAME `navigation` array; no new hrefs; the /dashboard auth gate in dashboard/layout.tsx is unchanged |
| T-19-04-02 | Info Disclosure | drawer relocates sign-out/session controls incorrectly | mitigate | Theme-toggle + sign-out are moved (not duplicated) into the More sheet; sign-out reuses the existing handler |
| T-19-04-SC | Tampering | supply chain | mitigate | No npm install; `sheet` was vendored in 19-03; acceptance in 19-03 asserted zero lockfile churn |
</threat_model>

<verification>
Scoped: `npx jest components/dashboard/__tests__/dashboard-shell.test.tsx` + `npm run build` (tsc) + `npx playwright test --project=mobile dashboard-mobile.spec` against the running stack. Full suite + human UAT at closure (19-09).
</verification>

<success_criteria>
- All 11 dashboard routes usable at 390px; sidebar hidden, 4-tab bottom bar + More drawer shown; titles readable.
- Desktop (>=md) unchanged; /dashboard/onboarding not regressed.
- Both bars driven off the single shared navigation array; Playwright mobile spec green.
</success_criteria>

<output>
Create `.planning/phases/19-full-frontend-experience-overhaul/19-04-SUMMARY.md` when done. Commit (do NOT push).
</output>
