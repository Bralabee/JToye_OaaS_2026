---
phase: 19-full-frontend-experience-overhaul
plan: 07
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - frontend/app/dashboard/kitchen/page.tsx
  - frontend/app/dashboard/kitchen/__tests__/page.test.tsx
  - frontend/components/dashboard/orders/OrderDetailPanel.tsx
  - frontend/components/dashboard/orders/__tests__/OrderDetailPanel.test.tsx
  - frontend/app/dashboard/orders/[id]/page.tsx
  - frontend/e2e/kitchen-flow.spec.ts
autonomous: true
requirements: [UIX-03, UIX-06]

must_haves:
  truths:
    - "The kitchen display and order detail show real product names on seeded live orders — 'Unknown Product' never renders for a product that exists"
    - "Kitchen order-card status badges no longer clip/overlap wrapped long order IDs — the order number truncates cleanly"
    - "KDS elapsed time is capped/formatted (just now / Xm / Xh / Xd) instead of raw uncapped minutes ('2245m ago')"
    - "The real-time STOMP feed, age-border colours, and mute toggle are unchanged"
  artifacts:
    - path: "frontend/app/dashboard/kitchen/page.tsx"
      provides: "badge-clip fix + elapsed-time cap"
    - path: "frontend/e2e/kitchen-flow.spec.ts"
      provides: "no-Unknown-Product + badge + elapsed assertions (domcontentloaded)"
  key_links:
    - from: "frontend/app/dashboard/kitchen/page.tsx"
      to: "OrderItem.productName (populated by 19-01)"
      via: "renders {item.quantity}x {item.productName}"
      pattern: "item\\.productName"
---

<objective>
Close backlog #2 (UI side), #8, #12 under UIX-06 Surface F: the Kitchen Display and order-detail render "Unknown Product" for real order line items (the root cause is fixed at the backend in plan 19-01; this plan verifies the render + backfill land), the kitchen card status badge clips/overlaps wrapped long order IDs (#8), and KDS elapsed time shows raw uncapped minutes like "2245m ago" (#12).

Purpose: a KDS that can't say what to cook fails at its one job — real product names, clean badges, and readable elapsed time.
Output: kitchen card header fix, elapsed-time formatter, verified product-name render on kitchen + order-detail, and updated unit + e2e coverage.

Note: the `PREPARING` purple→amber colour change is OWNED by the plan 19-08 palette sweep (Surface I / #10) — do NOT change purple here (avoids a same-file double edit across waves).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-full-frontend-experience-overhaul/19-UI-SPEC.md
@.planning/phases/19-full-frontend-experience-overhaul/19-PATTERNS.md
@.planning/phases/19-full-frontend-experience-overhaul/19-RESEARCH.md

<interfaces>
<!-- The render already reads productName correctly; the data fix is backend (19-01). -->
kitchen/page.tsx: card header ~L422-424 `flex items-start justify-between` + order number `text-2xl font-bold` (wraps + collides with badge); line item :449 `{item.quantity}x {item.productName}` (correct render); elapsedText() :75-79 (uncapped minutes).
OrderDetailPanel.tsx: renders productName + qty + unit price.
Regression tripwires: kitchen/__tests__/page.test.tsx, OrderDetailPanel.test.tsx, e2e/kitchen-flow.spec.ts — use domcontentloaded (SSE/STOMP never idles).
Backend dependency (19-01): OrderItem.productName is now snapshotted at guest-order creation + backfilled → seeded orders carry real titles.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Kitchen card badge-clip fix + elapsed-time cap</name>
  <files>frontend/app/dashboard/kitchen/page.tsx, frontend/app/dashboard/kitchen/__tests__/page.test.tsx</files>
  <read_first>
    - frontend/app/dashboard/kitchen/page.tsx (card header ~L422-424; elapsedText() :75-79; line-item render :449; age-border colours + mute toggle — PRESERVE)
    - frontend/app/dashboard/kitchen/__tests__/page.test.tsx (existing tests to update)
    - 19-UI-SPEC.md § Surface F (badge-clip fix: order number min-w-0 truncate text-lg font-semibold, badge flex-shrink-0, header gap-2; elapsed cap thresholds) + § Copywriting Contract (kitchen line item)
  </read_first>
  <behavior>
    - Test: a long ORD-... number truncates (does not wrap) and the status badge does not overlap it.
    - Test: elapsedText caps — <1m "just now", <60m "Xm ago", <24h "Xh ago", >=24h "Xd ago" (no "2245m ago").
    - Test: age-border colour logic + mute toggle unchanged.
  </behavior>
  <action>
    Fix the kitchen card header: order number → `min-w-0 truncate text-lg font-semibold`, status badge → `flex-shrink-0`, header container → `gap-2` (so long `ORD-…` numbers truncate cleanly instead of wrapping under the badge). Rewrite `elapsedText()` to cap/format: `<1m` → "just now"; `<60m` → "Xm ago"; `<24h` → "Xh ago" (or "Xh Ym"); `>=24h` → "Xd ago". Do NOT touch the STOMP feed, age-border colours, or the mute toggle. Do NOT change the `PREPARING` purple (sweep 19-08 owns it). Update `kitchen/__tests__/page.test.tsx` for the elapsed formatter + badge/title classes.
  </action>
  <acceptance_criteria>
    - Kitchen card order-number element uses `truncate` + `min-w-0` + `text-lg font-semibold`; badge uses `flex-shrink-0`.
    - `elapsedText` returns capped strings; a 2245-minute input yields a day/hour form, not "2245m".
    - `grep -c "purple-" app/dashboard/kitchen/page.tsx` is UNCHANGED from before (sweep owns purple) — this task does not add/remove purple.
    - `npx jest app/dashboard/kitchen/__tests__/page.test.tsx` passes; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx jest app/dashboard/kitchen/__tests__/page.test.tsx && npm run build</automated>
  </verify>
  <done>Long order IDs truncate without badge overlap; elapsed time is capped/formatted; STOMP/borders/mute untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Order-detail product-name render + OrderDetailPanel test</name>
  <files>frontend/components/dashboard/orders/OrderDetailPanel.tsx, frontend/components/dashboard/orders/__tests__/OrderDetailPanel.test.tsx, frontend/app/dashboard/orders/[id]/page.tsx</files>
  <read_first>
    - frontend/components/dashboard/orders/OrderDetailPanel.tsx (item-line render: productName + qty + unit price)
    - frontend/components/dashboard/orders/__tests__/OrderDetailPanel.test.tsx (update fixtures to real names)
    - frontend/app/dashboard/orders/[id]/page.tsx (order-detail page — confirm it renders the panel with fulfilment/address once the DTO exposes it via 19-01)
    - 19-UI-SPEC.md § Surface F (order-detail renders productName + qty + unit price; "Unknown Product" never for an existing product)
  </read_first>
  <behavior>
    - Test: OrderDetailPanel renders the fixture's real product names + qty + unit price; a fixture with a real product never shows "Unknown Product".
    - Test: the panel renders the delivery address block when the order is a delivery order (DTO field from 19-01), and omits it for collection.
  </behavior>
  <action>
    Ensure `OrderDetailPanel.tsx` renders each line item's `productName` + qty + unit price (it already reads productName — confirm and keep). If the order-detail DTO now carries `fulfilmentType` + address (from 19-01), render a small delivery-address block on `orders/[id]` for delivery orders (read-only; no address in any client log). Update `OrderDetailPanel.test.tsx` fixtures to use real product names and add an assertion that "Unknown Product" is NOT rendered for a fixture referencing an existing product. Do NOT change purple here (sweep 19-08 owns OrderDetailPanel purple).
  </action>
  <acceptance_criteria>
    - `OrderDetailPanel.tsx` renders `productName` per line (grep: `grep -c "productName" OrderDetailPanel.tsx` >= 1).
    - `orders/[id]/page.tsx` renders a delivery-address block for delivery orders (guarded by fulfilmentType).
    - `OrderDetailPanel.test.tsx` asserts real names render and "Unknown Product" is absent for an existing-product fixture.
    - `npx jest components/dashboard/orders/__tests__/OrderDetailPanel.test.tsx` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npx jest components/dashboard/orders/__tests__/OrderDetailPanel.test.tsx && npm run build</automated>
  </verify>
  <done>Order detail renders real product names + qty + price and (for delivery) the address; tests assert no "Unknown Product" for existing products.</done>
</task>

<task type="auto">
  <name>Task 3: Kitchen-flow e2e — no "Unknown Product", clean badge, capped elapsed (domcontentloaded)</name>
  <files>frontend/e2e/kitchen-flow.spec.ts</files>
  <read_first>
    - frontend/e2e/kitchen-flow.spec.ts:15-19 (route()/session stub idiom; domcontentloaded — NOT networkidle)
    - 19-RESEARCH.md § Pitfall 5 (SSE/STOMP never idles) + § Regression Tripwires (kitchen-flow updates)
    - 19-VALIDATION.md § Per-Task Verification Map (UIX-03 no-Unknown-Product on kitchen/order-detail)
  </read_first>
  <action>
    Update `e2e/kitchen-flow.spec.ts` to assert, against seeded live orders (real names from 19-01/19-02): NO "Unknown Product" string appears on the kitchen display or order-detail; the status badge does not overlap the (truncated) order number; elapsed time shows a capped form. Use `domcontentloaded` (or explicit element waits) — never `networkidle`. Do NOT edit docs/metrics.json (plan 19-09 reconciles).
  </action>
  <acceptance_criteria>
    - `kitchen-flow.spec.ts` asserts `getByText("Unknown Product")` has count 0 on kitchen + order-detail for seeded orders.
    - The spec uses `domcontentloaded` (grep: `grep -c "networkidle" kitchen-flow.spec.ts` == 0).
    - Spec runs green against the rebuilt stack (deferred confirmation at 19-09 UAT; syntax/type check now).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && grep -c "networkidle" e2e/kitchen-flow.spec.ts | grep -qx 0 && npx tsc --noEmit -p tsconfig.json 2>/dev/null; npm run build</automated>
  </verify>
  <done>Kitchen-flow e2e asserts real product names + clean badge + capped elapsed, using domcontentloaded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| order-detail render → delivery address (PII) | address shown to the authenticated vendor only; never logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07-01 | Info Disclosure | delivery address (PII) logged from order-detail render | mitigate | Address block is read-only render; no console/analytics logging of address; RLS/auth gate on /dashboard already scopes to tenant |
| T-19-07-02 | Tampering | e2e flakiness masks a real "Unknown Product" regression | mitigate | Use domcontentloaded + explicit element waits (not networkidle); assert exact string count 0 on seeded orders |
| T-19-07-SC | Tampering | supply chain | accept | No package installs |
</threat_model>

<verification>
Scoped: `npx jest app/dashboard/kitchen/__tests__/page.test.tsx components/dashboard/orders/__tests__/OrderDetailPanel.test.tsx` + `npm run build`. kitchen-flow Playwright runs against the rebuilt stack at closure (19-09). Depends on 19-01 backend for real names.
</verification>

<success_criteria>
- Kitchen + order-detail show real product names (no "Unknown Product" for existing products); badges don't clip; elapsed time is capped.
- STOMP feed / age borders / mute toggle unchanged; purple deferred to the 19-08 sweep.
- Unit tests updated + green; e2e uses domcontentloaded.
</success_criteria>

<output>
Create `.planning/phases/19-full-frontend-experience-overhaul/19-07-SUMMARY.md` when done. Commit (do NOT push).
</output>
