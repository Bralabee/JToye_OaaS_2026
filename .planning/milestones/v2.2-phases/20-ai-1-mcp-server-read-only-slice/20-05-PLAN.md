---
phase: 20-ai-1-mcp-server-read-only-slice
plan: 05
type: execute
wave: 4
depends_on: [20-03, 20-04]
files_modified: []
autonomous: false
requirements: [AI-1]
must_haves:
  truths:
    - "ALL containers are rebuilt and the dev Keycloak realm is re-imported so integration-catalog-ro exists in the running IdP"
    - "e2e.sh proves the read happy-path live: integration-catalog-ro token → MCP list_products → tenant-A rows (200)"
    - "e2e-rls.sh proves cross-tenant isolation live: token A and token B return disjoint non-empty product sets through the MCP tool"
    - "A real MCP client can connect to the running server and invoke a read tool per the README"
  artifacts: []
  key_links:
    - from: "rebuilt dev stack + re-imported realm"
      to: "mcp-server/scripts/e2e.sh + e2e-rls.sh"
      via: "live token mint + MCP tool calls"
      pattern: "e2e"
---

<objective>
Prove the deliverable LIVE. Everything before this wave was authored and unit/integration-proven against a mocked core. This wave satisfies the operational precondition (rebuild ALL containers + re-import the realm so `integration-catalog-ro` exists) and then runs the two live proofs against the real dev stack: the read happy-path and the load-bearing cross-tenant RLS proof. Finally, a human confirms a real MCP client can connect and call a tool.

Purpose: The CONTEXT standing rule is "rebuild ALL containers before any live E2E claim," and the running realm has NOT been re-imported since #206 merged — so the reference client does not yet exist in the live IdP. This gate closes both, then executes the live sweep that turns the mocked proofs into real ones (AC#4 + AC#5).
Output: Live E2E evidence (token mint → list_products rows; disjoint cross-tenant sets) captured in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/20-ai-1-mcp-server-read-only-slice/20-CONTEXT.md
@.planning/phases/20-ai-1-mcp-server-read-only-slice/20-RESEARCH.md
@.planning/phases/20-ai-1-mcp-server-read-only-slice/20-VALIDATION.md
@docs/security-scopes.md
@mcp-server/README.md

<interfaces>
<!-- Live preconditions + facts. -->
- Re-import per docs/security-scopes.md §Re-import: either drop the Keycloak DB/volume and restart, or `kc.sh import --override true`. Ports: Keycloak :8085 host / :8080 internal, core :9090, MCP :9100, frontend :3000. Login admin-user / <dev password — see .env>.
- Verify curl (CONTEXT specifics): POST :8085/realms/jtoye-dev/protocol/openid-connect/token grant_type=client_credentials client_id=integration-catalog-ro client_secret=$INTEGRATION_CATALOG_RO_SECRET → token; MCP list_products with it → 200; a core write attempt → 403.
- Scripts authored in 20-04: mcp-server/scripts/e2e.sh (happy-path), mcp-server/scripts/e2e-rls.sh (cross-tenant RLS proof).
- Known dev-env hazard (STATE blockers): port 3000 held by an MCP dev holder → frontend on 3100; Postgres 5432 shared with unrelated dealflow_* containers. Stop conflicting containers before the sweep if bring-up fails.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Rebuild ALL containers + re-import the dev realm (operational precondition)</name>
  <read_first>
    - docs/security-scopes.md § "Re-import" (drop-volume-and-restart OR kc.sh import --override true)
    - 20-RESEARCH.md § "Common Pitfalls" Pitfall 2 (start-dev --import-realm only creates realms that don't exist → running jtoye-dev predates #206 → integration-catalog-ro absent) + § "Environment Availability" (realm re-import is BLOCKING for live E2E)
    - 20-CONTEXT.md § specifics (verify curl + ports + login)
  </read_first>
  <action>Automate the precondition, then pause for the human health/mint confirmation: set the required `.env` values, run `docker compose -f docker-compose.full-stack.yml build` for ALL services, re-import the `jtoye-dev` realm per `docs/security-scopes.md §Re-import` (drop the Keycloak volume + restart, or `kc.sh import --override true`), bring the stack up, and wait for core-java + keycloak + mcp-server healthchecks. Do NOT proceed to Task 2 until the human confirms all three verification checks pass.</action>
  <what-built>
    Claude will: (a) ensure `INTEGRATION_CATALOG_RO_SECRET` and tenant-user passwords are set in `.env`; (b) rebuild ALL containers — `docker compose -f docker-compose.full-stack.yml build` (mcp-server + core-java + every service, since code changed); (c) re-import the `jtoye-dev` realm per `docs/security-scopes.md §Re-import` so `integration-catalog-ro` + `catalog:read`/`orders:read` scopes exist in the RUNNING IdP; (d) bring the stack up and wait for core-java + keycloak + mcp-server healthchecks to pass.
  </what-built>
  <how-to-verify>
    Human confirms the stack is healthy and the reference client now exists in the live realm:
    1. `docker compose -f docker-compose.full-stack.yml ps` — core-java, keycloak, mcp-server all healthy (stop conflicting dealflow_* / port-3000 holders first if bring-up fails).
    2. Token mint works (proves re-import took):
       `curl -s -X POST http://localhost:8085/realms/jtoye-dev/protocol/openid-connect/token -d grant_type=client_credentials -d client_id=integration-catalog-ro -d client_secret=$INTEGRATION_CATALOG_RO_SECRET` → returns an access_token (NOT invalid_client).
    3. `curl -s http://localhost:9100/health` → 200.
  </how-to-verify>
  <acceptance_criteria>
    - All containers rebuilt from current source (not cached stale images).
    - The realm re-import succeeded: the integration-catalog-ro token mint returns an access_token (no invalid_client/unauthorized_client).
    - core-java, keycloak, and mcp-server report healthy; GET :9100/health → 200.
  </acceptance_criteria>
  <resume-signal>Type "stack-ready" once all three checks pass, or paste the failing output.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run the live E2E sweep (read happy-path + cross-tenant RLS proof)</name>
  <files>—</files>
  <read_first>
    - mcp-server/scripts/e2e.sh + mcp-server/scripts/e2e-rls.sh (the scripts authored in 20-04)
    - 20-VALIDATION.md § "Cross-tenant RLS proof design" (assert THROUGH the MCP tool; disjoint non-empty sets) + § "Sampling Rate" (live E2E is the phase gate)
    - 20-RESEARCH.md § "Common Pitfalls" Pitfall 1 (mint tokens from host :8085) + Pitfall 4 (live boundary, not superuser Testcontainers)
  </read_first>
  <action>
    Against the now-healthy, realm-re-imported stack (Task 1), run `bash mcp-server/scripts/e2e.sh` then `bash mcp-server/scripts/e2e-rls.sh`. e2e.sh must show the integration-catalog-ro token → MCP `list_products` → 200 with tenant-A rows (non-error). e2e-rls.sh must show the disjoint, bidirectional, non-empty proof through the MCP tool: token A contains tenant-A products and NOT "TENANTB-PROBE-1"; token B contains "TENANTB-PROBE-1" and NOT tenant-A products; plus the cross-tenant order read → 404/empty. Capture the PASS lines (row counts / ids only, never tokens or PII bodies) for the SUMMARY. If either script fails on a realm/audience symptom, cross-check Pitfalls 1–3 (host-endpoint mint, re-import took, aud=core-api) before re-running.
  </action>
  <verify>
    <automated>bash mcp-server/scripts/e2e.sh && bash mcp-server/scripts/e2e-rls.sh && echo LIVE_E2E_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - e2e.sh exits 0: integration-catalog-ro token → list_products → 200 + non-empty tenant-A rows (satisfies AC#4 read happy-path live).
    - e2e-rls.sh exits 0: disjoint non-empty product sets through the MCP tool + cross-tenant order read → 404/empty (satisfies AC#5 RLS-proven, live).
    - Captured evidence in the SUMMARY shows row counts/ids only — no tokens or PII bodies.
  </acceptance_criteria>
  <done>Both live proofs are green against the rebuilt, re-imported dev stack — the read happy-path and cross-tenant isolation are real, not mocked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Real MCP client connects and invokes a read tool</name>
  <read_first>
    - mcp-server/README.md (client-credentials setup + how to point an MCP client at the running server)
    - 20-VALIDATION.md § "Manual-Only Verifications" (MCP client can actually connect and call a tool)
  </read_first>
  <action>No further code automation — the server + tools are already built and the live sweep (Task 2) is green. Present the README-driven client-connection steps below and pause for the human to confirm the end-user experience.</action>
  <what-built>
    A running, network-exposed MCP server (:9100) with three read tools, proven by the automated live sweep. This checkpoint confirms the END-USER experience: a real MCP client can discover and call a tool.
  </what-built>
  <how-to-verify>
    1. Follow `mcp-server/README.md` to mint a tenant-scoped token and configure an MCP client (e.g., Claude / an MCP inspector) against `http://localhost:9100/mcp` with the Bearer token.
    2. List tools → observe list_shops, list_products, read_orders.
    3. Invoke `list_products` → observe the tenant's product rows returned as tool content.
    4. (Optional) Invoke `read_orders` → observe orders (RLS-scoped); confirm no other tenant's data appears.
  </how-to-verify>
  <acceptance_criteria>
    - An MCP client connects to the running server and lists the three read tools.
    - Invoking list_products returns the tenant's rows as tool content (not an error).
    - Human confirms the deliverable works end-to-end from a client's perspective.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the MCP client lists the tools and list_products returns rows, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → running Keycloak realm | integration-catalog-ro exists only after re-import |
| MCP client → MCP server (:9100) | Bearer over the network; core is the validator |
| two tenant tokens → core RLS | Live isolation assertion resolves here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-06 | Operational (correct auth availability) | realm not re-imported / stale images | mitigate | Blocking-human gate rebuilds ALL containers + re-imports the realm; token-mint check proves the reference client exists before any live claim. |
| T-20-03 | Elevation of Privilege / Info Disclosure | cross-tenant read at the live boundary | mitigate | e2e-rls.sh proves disjoint non-empty sets through the MCP tool with two real tenant tokens (not superuser Testcontainers — Pitfall 4). |
| T-20-01 | Information Disclosure | live evidence capture | mitigate | Evidence records status + row counts/ids only; tokens and PII response bodies never logged or pasted into the SUMMARY. |
| T-20-02 | Spoofing | token audience at the live boundary | mitigate | Only core-api / integration-catalog-ro tokens (carrying aud=core-api) pass the #88 gate; a 200 on list_products confirms the audience path. |
</threat_model>

<verification>
- Task 1 checkpoint: stack healthy + integration-catalog-ro token mint returns an access_token + :9100/health 200.
- `bash mcp-server/scripts/e2e.sh && bash mcp-server/scripts/e2e-rls.sh` — both exit 0.
- Task 3 checkpoint: MCP client lists three tools + list_products returns rows.
</verification>

<success_criteria>
- ALL containers rebuilt + realm re-imported (integration-catalog-ro exists live).
- Read happy-path live-proven (AC#4) and cross-tenant RLS live-proven with disjoint non-empty sets (AC#5).
- A real MCP client connects and reads through the tools (deliverable works end-to-end).
</success_criteria>

<output>
Create `.planning/phases/20-ai-1-mcp-server-read-only-slice/20-05-SUMMARY.md` when done — capture live evidence (row counts/ids only, no tokens/PII).
</output>
