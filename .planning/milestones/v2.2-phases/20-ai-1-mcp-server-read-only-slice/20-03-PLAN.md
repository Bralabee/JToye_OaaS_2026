---
phase: 20-ai-1-mcp-server-read-only-slice
plan: 03
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - mcp-server/Dockerfile
  - mcp-server/.dockerignore
  - docker-compose.full-stack.yml
  - mcp-server/README.md
  - core-java/src/main/java/uk/jtoye/core/dev/DemoDataSeeder.java
autonomous: true
requirements: [AI-1]
must_haves:
  truths:
    - "The MCP server builds as its own multi-stage node:20-alpine container"
    - "docker-compose.full-stack.yml runs the mcp-server service, depends_on core-java + keycloak (service_healthy), CORE_BASE_URL=http://core-java:9090, no DB credentials"
    - "The dev-profile seeder gives tenant B (…0002) one shop + one product so the cross-tenant RLS proof asserts disjoint NON-EMPTY sets"
    - "README documents the client-credentials setup (integration-catalog-ro + INTEGRATION_CATALOG_RO_SECRET, pass-through model)"
  artifacts:
    - path: "mcp-server/Dockerfile"
      provides: "multi-stage node:20-alpine builder→runner, non-root, tsc→dist, /health healthcheck on :9100"
      contains: "node:20-alpine"
    - path: "docker-compose.full-stack.yml"
      provides: "mcp-server service block wired into jtoye-network"
      contains: "mcp-server"
    - path: "mcp-server/README.md"
      provides: "client-credentials setup + pass-through auth model + live-E2E preconditions"
      contains: "integration-catalog-ro"
    - path: "core-java/src/main/java/uk/jtoye/core/dev/DemoDataSeeder.java"
      provides: "tenant-B (…0002) shop+product seed for the bidirectional RLS proof"
      contains: "000000000002"
  key_links:
    - from: "docker-compose.full-stack.yml"
      to: "mcp-server/Dockerfile"
      via: "build.context ./mcp-server"
      pattern: "context:\\s*\\./mcp-server"
    - from: "docker-compose.full-stack.yml mcp-server"
      to: "core-java:9090"
      via: "CORE_BASE_URL env"
      pattern: "CORE_BASE_URL:\\s*http://core-java:9090"
---

<objective>
Package the MCP server as its own Docker container wired into the full-stack dev compose, write the README that documents the client-credentials setup, and extend the dev-profile seeder to give tenant B real data — so the final-wave cross-tenant RLS proof asserts DISJOINT NON-EMPTY sets (unfakeable) rather than a doubly-explained "empty for B." These three deliverables have no file overlap with the tool work (20-02) and run in parallel with it.

Purpose: Everything the final live-E2E wave needs must exist and be reachable — a buildable container in compose, seeded tenant-B data, and a README a human/agent follows to mint the reference token.
Output: `mcp-server/Dockerfile` + `.dockerignore`, the `mcp-server:` compose service, `mcp-server/README.md`, and a tenant-B seed in `DemoDataSeeder.java`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/20-ai-1-mcp-server-read-only-slice/20-CONTEXT.md
@.planning/phases/20-ai-1-mcp-server-read-only-slice/20-RESEARCH.md
@.planning/phases/20-ai-1-mcp-server-read-only-slice/20-PATTERNS.md
@docs/security-scopes.md
@frontend/Dockerfile
@core-java/src/main/java/uk/jtoye/core/dev/DemoDataSeeder.java

<interfaces>
<!-- Facts this plan depends on. -->
- MCP server listens on :9100, POST /mcp + GET /health (from 20-01 index.ts). Healthcheck hits GET /health (does NOT call core).
- Compose service names: core-java:9090 (internal), keycloak:8080 (internal) / :8085 (host), jtoye-network.
- DemoDataSeeder: @Profile("dev"), DEMO_TENANT = 00000000-0000-0000-0000-000000000001 (tenant A, already seeded); run() sets TenantContext before a transactionTemplate.execute(seed) then clears. Helpers upsertShop(...) / upsertProduct(...) exist.
- Tenant B UUID = 00000000-0000-0000-0000-000000000002 (matches seeded tenant-b-user's tenant claim).
- Reference machine credential (from #206): client integration-catalog-ro (client-credentials, core-api audience mapper, managed tenant_id on its SA user = tenant A) + INTEGRATION_CATALOG_RO_SECRET env (already in .env.example + verify-env.sh + composes).
- Auth model LOCKED = pass-through (stateless, per-request Bearer, NO secret in the MCP container). Server-minted token is an OPTIONAL README note only — do NOT build it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Multi-stage Dockerfile + .dockerignore + compose service wiring</name>
  <files>mcp-server/Dockerfile, mcp-server/.dockerignore, docker-compose.full-stack.yml</files>
  <read_first>
    - frontend/Dockerfile (analog: multi-stage node:20-alpine, manifest-first COPY, non-root uid 1001, node -e http.get healthcheck) + frontend/.dockerignore
    - 20-PATTERNS.md § "mcp-server/Dockerfile", § "mcp-server/.dockerignore", § "docker-compose service wiring" (edge-go block lines 259-289 analog; frontend depends_on core+keycloak service_healthy)
    - 20-RESEARCH.md § "Code Examples" (Dockerfile + docker-compose service wiring examples; pass-through = NO secret env)
    - docker-compose.full-stack.yml (locate the edge-go: and frontend: service blocks + the networks/jtoye-network + keycloak/core-java healthchecks)
  </read_first>
  <action>
    `mcp-server/Dockerfile`: mirror frontend/Dockerfile's two-stage node:20-alpine build — builder does manifest-first `COPY package*.json ./`, `npm ci`, `COPY . .`, `npm run build` (tsc→dist/); runner is node:20-alpine, `NODE_ENV=production`, non-root user (addgroup/adduser uid 1001), `npm ci --omit=dev`, `COPY --from=builder /app/dist ./dist`, `EXPOSE 9100`, `HEALTHCHECK` using the `node -e "require('http').get('http://localhost:9100/health', r => process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))"` idiom, `CMD ["node","dist/index.js"]`. Divergence from the analog: plain `dist/` copy (no Next standalone), EXPOSE 9100 not 3000.
    `mcp-server/.dockerignore`: copy frontend/.dockerignore exclusions (node_modules/, .env*, IDE files) and swap `.next/`/`out/` for `dist/`.
    `docker-compose.full-stack.yml`: add an `mcp-server:` service — `build: { context: ./mcp-server, dockerfile: Dockerfile }`, `container_name: jtoye-mcp-server`, `restart: unless-stopped`, `environment: { TZ: Europe/London, CORE_BASE_URL: http://core-java:9090 }` (NO secret — pass-through model per LOCKED decision), `ports: ["9100:9100"]`, `depends_on: { core-java: { condition: service_healthy }, keycloak: { condition: service_healthy } }`, `networks: [jtoye-network]`, and a `healthcheck` hitting its own /health. Do NOT add issuer/JWKS env — the MCP server forwards an opaque token and never validates iss (20-PATTERNS § "Split-horizon issuer — inherit").
  </action>
  <verify>
    <automated>docker compose -f docker-compose.full-stack.yml config >/dev/null && echo COMPOSE_VALID</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f docker-compose.full-stack.yml config` parses with no error and shows the mcp-server service.
    - `grep -n "context: ./mcp-server" docker-compose.full-stack.yml` and `grep -n "CORE_BASE_URL: http://core-java:9090" docker-compose.full-stack.yml` both match.
    - The mcp-server service block contains NO secret env (no INTEGRATION_CATALOG_RO_SECRET) — pass-through model.
    - Dockerfile is multi-stage node:20-alpine, non-root, healthcheck on :9100/health.
  </acceptance_criteria>
  <done>The MCP server builds as its own container and is wired into the full-stack compose depending on healthy core-java + keycloak.</done>
</task>

<task type="auto">
  <name>Task 2: Seed tenant B (…0002) with one shop + one product (dev profile)</name>
  <files>core-java/src/main/java/uk/jtoye/core/dev/DemoDataSeeder.java</files>
  <read_first>
    - core-java/src/main/java/uk/jtoye/core/dev/DemoDataSeeder.java (the run() TenantContext.set→transactionTemplate.execute(seed)→clear pattern at lines ~135-152; DEMO_TENANT at line 87; upsertShop/upsertProduct helpers)
    - 20-RESEARCH.md § "Open Questions" #2 (bidirectional proof: tenant B currently has ZERO rows → "empty for B" is doubly-explained; seed one shop+product for B to make the assertion disjoint NON-EMPTY and unfakeable)
    - 20-VALIDATION.md § "Cross-tenant RLS proof design" step 2 (disjoint seeded data)
  </read_first>
  <action>
    Add a `TENANT_B = UUID.fromString("00000000-0000-0000-0000-000000000002")` constant. After the existing tenant-A seed completes in `run()`, add a second, independent block: `TenantContext.set(TENANT_B)` → `transactionTemplate.execute(status -> seedTenantB())` → `TenantContext.clear()` in a finally (so TenantSetLocalAspect pins the RLS GUC to tenant B for those repository ops). `seedTenantB()` upserts exactly ONE shop and ONE product for tenant B using the existing upsert helpers (or minimal equivalents), with a distinct SKU/slug (e.g. product SKU "TENANTB-PROBE-1", shop slug "tenant-b-probe") so the RLS proof can assert token B sees this product-id and token A does NOT. Keep it minimal — this is a dev-only probe fixture, not a full catalogue. Idempotent (upsert-by-slug/sku) so repeated boots do not duplicate. Log a single INFO line "DemoDataSeeder tenant-B probe seeded" on success. No Flyway migration (schema stays V50). Do NOT add a new Java @Test — this seed's proof is the live cross-tenant E2E in 20-05 (superuser Testcontainers cannot prove RLS — RESEARCH Pitfall 4); the Java @Test baseline (872) stays unchanged.
  </action>
  <verify>
    <automated>cd core-java && ./gradlew compileJava -q && echo JAVA_COMPILES</automated>
  </verify>
  <acceptance_criteria>
    - DemoDataSeeder.java references `00000000-0000-0000-0000-000000000002` and seeds one shop + one product under TenantContext.set(TENANT_B).
    - The tenant-B block is wrapped in its own TenantContext.set/clear (finally) and transactionTemplate.execute (so RLS GUC pins to B) — mirrors the tenant-A pattern.
    - Idempotent upsert (no duplicate rows on repeated boot).
    - `./gradlew compileJava` succeeds; no migration added (schema stays V50).
  </acceptance_criteria>
  <done>Tenant B has exactly one seeded shop + product on dev boot, enabling a disjoint non-empty bidirectional RLS proof.</done>
</task>

<task type="auto">
  <name>Task 3: README — client-credentials setup + pass-through auth model + live-E2E preconditions</name>
  <files>mcp-server/README.md</files>
  <read_first>
    - docs/security-scopes.md § "Re-import" + § 6 "Feeds [AI-1] MCP Auth Model (#203)" (the reference client, re-import step, scope taxonomy)
    - 20-CONTEXT.md § decisions (Auth REUSE #206; reference credential integration-catalog-ro + INTEGRATION_CATALOG_RO_SECRET) + § specifics (verify curl, ports)
    - 20-RESEARCH.md § "Open Questions" #3 (pass-through is the LOCKED default; server-minted is an OPTIONAL note only) + § "Architecture Patterns" System Architecture Diagram
    - 20-PATTERNS.md § "Env / secret handling" (secrets from .env, never literals)
  </read_first>
  <action>
    Write `mcp-server/README.md` documenting: (1) What the server is — a thin, stateless read-only MCP tool layer over the core REST API; RLS in core is the sole isolation boundary; the server holds no DB credentials and no client secret. (2) The three tools (list_shops, list_products, read_orders) and their core endpoints. (3) Auth model — pass-through: the calling agent mints its OWN tenant-scoped Keycloak token and presents it as `Authorization: Bearer`; the server forwards it verbatim to `http://core-java:9090`. (4) Client-credentials setup for the reference machine credential: client `integration-catalog-ro`, secret from `INTEGRATION_CATALOG_RO_SECRET` (`.env`, never a literal), the host token-mint curl against `http://localhost:8085/realms/jtoye-dev/protocol/openid-connect/token` (grant_type=client_credentials), and the expected `catalog:read` scope + `core-api` audience. (5) Live preconditions: rebuild ALL containers + re-import the realm per `docs/security-scopes.md §Re-import` before the client exists in the running IdP; then the verify curl (token → list_products 200 → a write attempt 403). (6) A brief OPTIONAL note that a single-tenant demo could mint its own token in-container via INTEGRATION_CATALOG_RO_SECRET, explicitly marked NOT built in this slice. Do not paste secrets.
  </action>
  <verify>
    <automated>grep -q "integration-catalog-ro" mcp-server/README.md && grep -q "INTEGRATION_CATALOG_RO_SECRET" mcp-server/README.md && grep -q "Re-import" mcp-server/README.md && echo README_OK</automated>
  </verify>
  <acceptance_criteria>
    - README documents the client-credentials setup (integration-catalog-ro + INTEGRATION_CATALOG_RO_SECRET) — satisfies AC#7.
    - README states the pass-through model (no secret in the container) as the default and marks server-minted as an optional, not-built note.
    - README lists the realm re-import + rebuild-all precondition and the verify curl.
    - No literal secret values appear in the README.
  </acceptance_criteria>
  <done>A reader can mint the reference token and call the server per the README; the client-credentials setup is documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP container → core-java | Fixed CORE_BASE_URL, no secret, no DB access |
| operator → running Keycloak realm | Reference client exists only after re-import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Tampering / Info Disclosure | compose CORE_BASE_URL | mitigate | Fixed compose env `http://core-java:9090`, not caller-controlled (SSRF guard). |
| T-20-06 | Operational (availability of correct auth) | realm not re-imported since #206 | mitigate | README documents rebuild-all + re-import precondition + verify curl before any live token/E2E; enforced as the blocking gate in 20-05. |
| T-20-07 | Information Disclosure | secret sprawl in MCP container | mitigate | Pass-through model holds NO secret in the mcp-server compose block; reference secret stays in .env consumed by Keycloak only. |
| T-20-08 | Information Disclosure | tenant-B DemoDataSeeder rows (dev only) | accept | The tenant-B seed shop/product is synthetic dev-profile data with no real PII; it exists solely to make the RLS proof bidirectional and is gated behind `@Profile("dev")` (never seeded in staging/prod). |
| T-20-88 | Spoofing / Elevation | forwarded Bearer audience/expiry | transfer | The MCP container never validates tokens; core-java enforces the `core-api` audience (#88) + issuer/expiry (#87). Cross-tenant isolation resolves to Postgres FORCE RLS on the `tenant_id` claim — proven live in 20-05, not by this plan's seeder. |
</threat_model>

<verification>
- `docker compose -f docker-compose.full-stack.yml config` parses and shows mcp-server.
- `cd core-java && ./gradlew compileJava` — tenant-B seed compiles; schema stays V50.
- README greps: integration-catalog-ro, INTEGRATION_CATALOG_RO_SECRET, Re-import.
</verification>

<success_criteria>
- The MCP server is its own node:20-alpine container in the full-stack compose (AC#1), depending on healthy core + keycloak, with no secret.
- Tenant B has seeded data enabling an unfakeable disjoint RLS proof.
- README documents the client-credentials setup (AC#7).
</success_criteria>

<output>
Create `.planning/phases/20-ai-1-mcp-server-read-only-slice/20-03-SUMMARY.md` when done.
</output>
