# ===========================
# J'Toye OaaS - Infrastructure Environment Configuration
# ===========================
#
# INSTRUCTIONS:
# 1. Copy this file to .env in the same directory
#    Windows: copy .env.example .env
#    Linux/Mac: cp .env.example .env
#
# 2. Values below are suitable for local development
# 3. For production, change all passwords and secrets!
# 4. NEVER commit .env to version control
#
# This file is used by infra/docker-compose.yml for local infrastructure
#
# ===========================

# ===========================
# DATABASE CONFIGURATION
# ===========================
# PostgreSQL configuration for Docker Compose
# These values are used inside the Docker network
DB_HOST=postgres
DB_PORT=5432
DB_NAME=jtoye
DB_USER=jtoye
# Default password for local development (change for production!)
DB_PASSWORD=secret

# ===========================
# KEYCLOAK CONFIGURATION
# ===========================
# Keycloak admin console credentials
# Access at: http://localhost:8085
KC_ADMIN=admin
# Default password for local development (change for production!)
KC_ADMIN_PASSWORD=admin123

# Keycloak realm name
KC_REALM=jtoye-dev

# ===========================
# NOTES
# ===========================
# - These credentials match the pre-configured Keycloak realm in infra/keycloak/realm-export.json
# - Test users are configured in the realm export:
#   * admin-user / admin123
#   * tenant-a-user / password123
#   * tenant-b-user / password123
# - Client secrets are in the realm export file
